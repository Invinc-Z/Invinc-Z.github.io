<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>视频音乐资源获取</title>
    <url>/2024/05/11/misc/get-video-music-resource/</url>
    <content><![CDATA[<p>本文记录如何获取视频音乐资源和一些使用技巧。</p>
<span id="more"></span>
<hr />
<h2 id="视频资源">视频资源</h2>
<p>视频转音频：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ffmpeg -i input.mp4 -vn -c:a copy output.aac <span class="comment"># 无损提取</span></span><br><span class="line">ffmpeg -i input.mp4 -vn -c:a libmp3lame -b:a 192k output.mp3  <span class="comment"># 通用但有损</span></span><br></pre></td></tr></table></figure>
<h3 id="bilibili">Bilibili</h3>
<p><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL25JQ0Vubm5ubm5uTGVlL0JpbGliaWxpRG93bg==">BilibiliDown<i class="fa fa-external-link-alt"></i></span></p>
<hr />
<h2 id="音乐资源">音乐资源</h2>
<h3 id="资源下载和上传">资源下载和上传</h3>
<h4 id="下载">下载</h4>
<p>无损音乐<strong>免费下载</strong> 微信公众号：w西肆</p>
<h4 id="上传">上传</h4>
<p>网易云音乐提供了一个60G的免费<strong>音乐云盘</strong>空间，可以将本地音乐上传到云盘就能实现通过在网易云音乐听自己上传的音乐。</p>
<h3 id="提取专辑封面">提取专辑封面</h3>
<p>从 FLAC
文件中<strong>提取封面</strong>（嵌入的专辑封面图片），可以使用如下方法（安装ffmpeg）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ffmpeg -i input.flac -an -vcodec copy cover.jpg</span><br></pre></td></tr></table></figure>
<ul>
<li><code>-i input.flac</code>：指定输入 FLAC 文件。</li>
<li><code>-an</code>：忽略音频流（不提取音频）。</li>
<li><code>-vcodec copy</code>：直接复制封面图片流，不重新编码。</li>
<li><code>cover.jpg</code>：输出文件名（可以是
<code>.jpg</code>、<code>.png</code> 等）。</li>
</ul>
<h3 id="提取歌词">提取歌词</h3>
<p>提取歌词文件：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2NoZW5tb3poaWppbi9MRERD">github项目<i class="fa fa-external-link-alt"></i></span></p>
]]></content>
      <categories>
        <category>Misc</category>
      </categories>
      <tags>
        <tag>Tips</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux 中 epoll 的用法与底层实现</title>
    <url>/2026/02/21/programming/linux/epoll/</url>
    <content><![CDATA[<p>在 Linux 网络编程里，<code>epoll</code> 是高并发场景下最常用的 I/O
多路复用机制之一。相比早期的 <code>select</code> 和
<code>poll</code>，<code>epoll</code>
在文件描述符数量较大、活跃连接较多的情况下通常具有更好的性能表现。</p>
<span id="more"></span>
<hr />
<h2 id="一什么是-epoll">一、什么是 epoll</h2>
<p><code>epoll</code> 是 Linux
内核提供的一组系统调用，用来让一个线程同时监视多个文件描述符（file
descriptor，简称 fd）上的事件，例如：</p>
<ul>
<li>socket 可读</li>
<li>socket 可写</li>
<li>对端关闭连接</li>
<li>出现错误</li>
</ul>
<p>它的核心目标不是“让 I/O 变快”，而是：</p>
<blockquote>
<p>让应用程序高效地知道“哪些 fd 已经就绪，可以进行非阻塞读写了”。</p>
</blockquote>
<p>在典型的服务器模型中，一个进程往往要同时维护大量连接。如果对每个连接都创建一个线程，不仅线程切换成本高，内存占用也大。<code>epoll</code>
提供了一种单线程或少量线程管理大量连接的方式，因此它被广泛用于：</p>
<ul>
<li>Redis</li>
<li>Nginx</li>
<li>各类 Reactor 网络框架</li>
<li>游戏服务器、网关、代理服务</li>
</ul>
<hr />
<h2 id="二为什么需要-epoll">二、为什么需要 epoll</h2>
<p>在 <code>epoll</code> 出现之前，Linux 常见的 I/O 多路复用方式是
<code>select</code> 和 <code>poll</code>。</p>
<h3 id="select-的问题">1. select 的问题</h3>
<p><code>select</code> 的主要缺点有：</p>
<ul>
<li><strong>fd 数量受限</strong>：通常受 <code>FD_SETSIZE</code>
限制</li>
<li><strong>每次调用都要重复传入整个 fd 集合</strong></li>
<li><strong>内核返回后，用户态还要遍历所有 fd 找出就绪项</strong></li>
</ul>
<p>也就是说，<code>select</code> 更像是：</p>
<blockquote>
<p>“你把全班同学名单每次都交给老师，老师告诉你有人到了，但你还得自己再一个个点名确认是谁。”</p>
</blockquote>
<h3 id="poll-的改进与不足">2. poll 的改进与不足</h3>
<p><code>poll</code> 去掉了 <code>select</code> 的 fd
数量上限问题，但它依然存在两个核心问题：</p>
<ul>
<li>每次调用都需要把关注的 fd 列表从用户态拷贝到内核态</li>
<li>返回后应用仍需要线性遍历整个列表，时间复杂度接近
<code>O(n)</code></li>
</ul>
<h3 id="epoll-的改进方向">3. epoll 的改进方向</h3>
<p><code>epoll</code> 针对上面的问题做了两件关键优化：</p>
<ol type="1">
<li><strong>把“关注哪些 fd”这件事长期保存在内核中</strong>，避免每次
<code>wait</code> 都重复传入全部集合</li>
<li><strong>把“已经就绪的 fd”单独组织起来</strong>，避免每次都扫描全部
fd</li>
</ol>
<p>因此在“大量连接、少量活跃”的场景下，<code>epoll</code>
的优势会非常明显。</p>
<hr />
<h2 id="三epoll-的三个核心系统调用">三、epoll 的三个核心系统调用</h2>
<p><code>epoll</code> 主要由以下三个系统调用组成：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">epoll_create1</span><span class="params">(<span class="type">int</span> flags)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">epoll_ctl</span><span class="params">(<span class="type">int</span> epfd, <span class="type">int</span> op, <span class="type">int</span> fd, <span class="keyword">struct</span> epoll_event *event)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">epoll_wait</span><span class="params">(<span class="type">int</span> epfd, <span class="keyword">struct</span> epoll_event *events, <span class="type">int</span> maxevents, <span class="type">int</span> timeout)</span>;</span><br></pre></td></tr></table></figure>
<p>可以把它们理解成：</p>
<ul>
<li><code>epoll_create1</code>：创建一个 epoll 实例</li>
<li><code>epoll_ctl</code>：往 epoll 实例里增删改要监听的 fd</li>
<li><code>epoll_wait</code>：阻塞等待事件发生并取回就绪结果</li>
</ul>
<h3 id="epoll_create1">1. epoll_create1</h3>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> epfd = epoll_create1(<span class="number">0</span>);</span><br></pre></td></tr></table></figure>
<p>它会返回一个文件描述符 <code>epfd</code>，这个 fd 代表一个 epoll
对象。后续所有操作都围绕这个 <code>epfd</code> 展开。</p>
<p>如果创建失败，返回 <code>-1</code>。</p>
<h3 id="epoll_ctl">2. epoll_ctl</h3>
<p><code>epoll_ctl</code> 用于管理关注列表：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">ev</span>;</span></span><br><span class="line">ev.events = EPOLLIN;</span><br><span class="line">ev.data.fd = listenfd;</span><br><span class="line"></span><br><span class="line">epoll_ctl(epfd, EPOLL_CTL_ADD, listenfd, &amp;ev);</span><br></pre></td></tr></table></figure>
<p>其中 <code>op</code> 常见取值有：</p>
<ul>
<li><code>EPOLL_CTL_ADD</code>：添加 fd</li>
<li><code>EPOLL_CTL_MOD</code>：修改 fd 关注的事件</li>
<li><code>EPOLL_CTL_DEL</code>：删除 fd</li>
</ul>
<h3 id="epoll_wait">3. epoll_wait</h3>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">events</span>[1024];</span></span><br><span class="line"><span class="type">int</span> n = epoll_wait(epfd, events, <span class="number">1024</span>, <span class="number">-1</span>);</span><br></pre></td></tr></table></figure>
<p>参数含义：</p>
<ul>
<li><code>events</code>：用于接收就绪事件的数组</li>
<li><code>maxevents</code>：本次最多返回多少个就绪事件</li>
<li><code>timeout</code>：超时时间，毫秒
<ul>
<li><code>-1</code>：一直阻塞</li>
<li><code>0</code>：立即返回</li>
<li><code>&gt;0</code>：等待指定毫秒数</li>
</ul></li>
</ul>
<p>返回值 <code>n</code> 表示本次就绪的 fd 个数。</p>
<hr />
<h2 id="四epoll-的基本使用流程">四、epoll 的基本使用流程</h2>
<p>一个典型的基于 <code>epoll</code> 的 TCP 服务端流程通常如下：</p>
<ol type="1">
<li>创建监听 socket</li>
<li>设置监听 socket 为非阻塞</li>
<li><code>bind</code> + <code>listen</code></li>
<li>创建 epoll 实例</li>
<li>把监听 socket 加入 epoll</li>
<li>循环调用 <code>epoll_wait</code></li>
<li>处理就绪事件：
<ul>
<li>如果是监听 socket，就 <code>accept</code></li>
<li>如果是连接 socket，就 <code>read</code> / <code>write</code></li>
</ul></li>
<li>连接关闭时，从 epoll 中删除并关闭 fd</li>
</ol>
<p>下面是一个简化版示例：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/epoll.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> MAX_EVENTS 1024</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 8080</span></span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">int</span> <span class="title function_">set_nonblock</span><span class="params">(<span class="type">int</span> fd)</span> &#123;</span><br><span class="line">    <span class="type">int</span> flags = fcntl(fd, F_GETFL, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (flags == <span class="number">-1</span>) <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">return</span> fcntl(fd, F_SETFL, flags | O_NONBLOCK);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> listenfd = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">addr</span>;</span></span><br><span class="line">    <span class="built_in">memset</span>(&amp;addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(addr));</span><br><span class="line"></span><br><span class="line">    addr.sin_family = AF_INET;</span><br><span class="line">    addr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line">    addr.sin_port = htons(PORT);</span><br><span class="line"></span><br><span class="line">    bind(listenfd, (<span class="keyword">struct</span> sockaddr *)&amp;addr, <span class="keyword">sizeof</span>(addr));</span><br><span class="line">    listen(listenfd, <span class="number">128</span>);</span><br><span class="line">    set_nonblock(listenfd);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> epfd = epoll_create1(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">ev</span>;</span></span><br><span class="line">    ev.events = EPOLLIN;</span><br><span class="line">    ev.data.fd = listenfd;</span><br><span class="line">    epoll_ctl(epfd, EPOLL_CTL_ADD, listenfd, &amp;ev);</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">events</span>[<span class="title">MAX_EVENTS</span>];</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="type">int</span> n = epoll_wait(epfd, events, MAX_EVENTS, <span class="number">-1</span>);</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            <span class="type">int</span> fd = events[i].data.fd;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (fd == listenfd) &#123;</span><br><span class="line">                <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">                    <span class="type">int</span> connfd = accept(listenfd, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">                    <span class="keyword">if</span> (connfd == <span class="number">-1</span>) &#123;</span><br><span class="line">                        <span class="keyword">if</span> (errno == EAGAIN || errno == EWOULDBLOCK) &#123;</span><br><span class="line">                            <span class="keyword">break</span>;</span><br><span class="line">                        &#125;</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                    set_nonblock(connfd);</span><br><span class="line">                    <span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">client_ev</span>;</span></span><br><span class="line">                    client_ev.events = EPOLLIN | EPOLLET;</span><br><span class="line">                    client_ev.data.fd = connfd;</span><br><span class="line">                    epoll_ctl(epfd, EPOLL_CTL_ADD, connfd, &amp;client_ev);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="type">char</span> buf[<span class="number">4096</span>];</span><br><span class="line">                <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">                    <span class="type">ssize_t</span> cnt = read(fd, buf, <span class="keyword">sizeof</span>(buf));</span><br><span class="line">                    <span class="keyword">if</span> (cnt &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                        write(fd, buf, cnt);</span><br><span class="line">                    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (cnt == <span class="number">0</span>) &#123;</span><br><span class="line">                        close(fd);</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="keyword">if</span> (errno == EAGAIN || errno == EWOULDBLOCK) &#123;</span><br><span class="line">                            <span class="keyword">break</span>;</span><br><span class="line">                        &#125;</span><br><span class="line">                        close(fd);</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    close(listenfd);</span><br><span class="line">    close(epfd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个示例主要用来说明 <code>epoll</code>
的结构，真正上线还需要补充：</p>
<ul>
<li>错误处理</li>
<li>写缓冲区管理</li>
<li>半包 / 粘包处理</li>
<li>优雅关闭连接</li>
<li>忽略 <code>SIGPIPE</code></li>
<li>更清晰的连接状态机</li>
</ul>
<hr />
<h2 id="五epoll-中常见的事件类型">五、epoll 中常见的事件类型</h2>
<p><code>struct epoll_event</code> 中的 <code>events</code>
字段用于指定关注哪些事件，也用于返回实际发生了哪些事件。</p>
<p>常见标志位如下：</p>
<ul>
<li><code>EPOLLIN</code>：可读</li>
<li><code>EPOLLOUT</code>：可写</li>
<li><code>EPOLLERR</code>：发生错误</li>
<li><code>EPOLLHUP</code>：挂断</li>
<li><code>EPOLLRDHUP</code>：对端关闭写端，常用于判断半关闭</li>
<li><code>EPOLLET</code>：边沿触发（Edge Triggered）</li>
<li><code>EPOLLONESHOT</code>：只触发一次，需手动重新 armed</li>
</ul>
<p>一个常见写法：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">ev.events = EPOLLIN | EPOLLRDHUP | EPOLLET;</span><br></pre></td></tr></table></figure>
<p>表示：</p>
<ul>
<li>关心可读事件</li>
<li>关心对端关闭</li>
<li>使用边沿触发模式</li>
</ul>
<hr />
<h2 id="六水平触发-lt-与边沿触发-et">六、水平触发 LT 与边沿触发 ET</h2>
<p>这是 <code>epoll</code> 最容易被问到，也是最容易写错的地方。</p>
<h3 id="ltlevel-triggered水平触发">1. LT：Level Triggered，水平触发</h3>
<p>这是默认模式。</p>
<p>它的语义是：</p>
<blockquote>
<p>只要 fd 仍然处于“可读/可写”状态，<code>epoll_wait</code>
就会反复通知你。</p>
</blockquote>
<p>例如一个 socket 接收缓冲区里还有数据没读完，那么后续
<code>epoll_wait</code> 仍会不断返回这个 fd 的可读事件。</p>
<p>优点：</p>
<ul>
<li>语义直观</li>
<li>不容易漏事件</li>
<li>编程难度较低</li>
</ul>
<p>缺点：</p>
<ul>
<li>重复通知较多</li>
</ul>
<h3 id="etedge-triggered边沿触发">2. ET：Edge Triggered，边沿触发</h3>
<p>ET 的语义是：</p>
<blockquote>
<p>只有状态从“不可读变为可读”、“不可写变为可写”时，才通知一次。</p>
</blockquote>
<p>比如 socket
收到新数据时通知你一次。如果你这次没有把缓冲区里的数据全部读完，那么后面可能不会再次提醒你。</p>
<p>因此 ET 模式通常要求：</p>
<ol type="1">
<li><strong>fd 必须设置为非阻塞</strong></li>
<li><strong>读操作要一直读到返回 <code>EAGAIN</code></strong></li>
<li><strong>写操作要一直写到返回 <code>EAGAIN</code>
或数据发完</strong></li>
</ol>
<p>典型伪代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> ((n = read(fd, buf, <span class="keyword">sizeof</span>(buf))) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">// 持续读取直到读空</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (n == <span class="number">-1</span> &amp;&amp; errno == EAGAIN) &#123;</span><br><span class="line">    <span class="comment">// 数据已经读完</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="lt-与-et-如何选择">3. LT 与 ET 如何选择</h3>
<p>如果是学习、功能验证或者业务逻辑较复杂的场景，通常建议优先使用
<strong>LT</strong>，因为更稳妥。</p>
<p>如果是成熟的高性能网络框架，且你已经能正确处理非阻塞 I/O
和状态机，那么可以使用 <strong>ET</strong> 来减少重复唤醒。</p>
<p>一句话概括：</p>
<ul>
<li><strong>LT 更好写</strong></li>
<li><strong>ET 更考验实现质量</strong></li>
</ul>
<hr />
<h2 id="七为什么-epoll-通常要配合非阻塞-socket">七、为什么 epoll
通常要配合非阻塞 socket</h2>
<p>虽然 <code>epoll</code> 本身不强制要求 fd
一定是非阻塞的，但在实际网络编程中，几乎总是配合 <code>O_NONBLOCK</code>
使用。</p>
<p>原因很简单：</p>
<p>如果某个连接上的 <code>read</code> 或 <code>write</code>
阻塞住了，那么整个事件循环线程都会被卡住，其他连接也就没法处理了。</p>
<p>例如：</p>
<ol type="1">
<li><code>epoll_wait</code> 告诉你某个 fd 可读</li>
<li>你调用 <code>read</code></li>
<li>只读了一部分后又因为某些条件阻塞住</li>
<li>整个 Reactor 线程停摆</li>
</ol>
<p>这就违背了 I/O 多路复用的初衷。</p>
<p>因此在实践中通常遵循：</p>
<ul>
<li>监听 fd：非阻塞</li>
<li>已连接 fd：非阻塞</li>
<li>ET 模式：更是必须非阻塞</li>
</ul>
<hr />
<h2 id="八epoll-的底层实现思路">八、epoll 的底层实现思路</h2>
<p>这一部分是理解 <code>epoll</code> 的重点。</p>
<p>很多资料会简单说：</p>
<blockquote>
<p><code>epoll</code> 是 O(1) 的。</p>
</blockquote>
<p>这句话并不严谨。更准确地说，<code>epoll</code> 的优势在于：</p>
<ul>
<li>不需要每次调用都把整个 fd 集合从用户态拷入内核</li>
<li>不需要每次都线性扫描全部 fd 查找就绪项</li>
<li>只返回已经准备好的事件</li>
</ul>
<p>它的底层可以从两个核心数据结构来理解。</p>
<h3 id="红黑树维护关注列表">1. 红黑树：维护“关注列表”</h3>
<p>当你调用：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">epoll_ctl(epfd, EPOLL_CTL_ADD, fd, &amp;ev);</span><br></pre></td></tr></table></figure>
<p>内核需要把这个 fd 注册到 epoll 实例中。Linux 内核内部会用一棵
<strong>红黑树</strong> 来维护这些被关注的 fd。</p>
<p>这样做的目的包括：</p>
<ul>
<li>快速查找某个 fd 是否已注册</li>
<li>支持添加、删除、修改操作</li>
<li>保持管理结构稳定</li>
</ul>
<p>也就是说，<strong>红黑树负责“我关心谁”</strong>。</p>
<h3 id="就绪链表维护已经就绪的-fd">2. 就绪链表：维护“已经就绪的 fd”</h3>
<p>仅有关注列表还不够，关键在于如何快速拿到已经发生事件的 fd。</p>
<p><code>epoll</code> 内部还维护了一个 <strong>ready
list（就绪链表）</strong>。当某个 fd 对应的设备或 socket
状态发生变化时，驱动层/协议栈会通过回调机制通知 <code>epoll</code>：</p>
<blockquote>
<p>这个 fd 已经就绪，可以把它挂到 ready list 上。</p>
</blockquote>
<p>这样 <code>epoll_wait</code> 做的事情就不是“扫描所有 fd”，而是：</p>
<blockquote>
<p>直接从 ready list 中取出已经准备好的事件并返回给用户态。</p>
</blockquote>
<p>这正是 <code>epoll</code> 在高并发下性能较好的关键原因。</p>
<h3 id="回调机制事件到来时主动通知">3. 回调机制：事件到来时主动通知</h3>
<p>当 socket 被加入 epoll 时，内核并不是每次等 <code>epoll_wait</code>
才去检查状态，而是会在 fd 对应的等待队列上注册回调。</p>
<p>后续当：</p>
<ul>
<li>网卡收到数据</li>
<li>TCP 协议栈把数据放入 socket 接收缓冲区</li>
<li>socket 状态变化为可读/可写</li>
</ul>
<p>对应回调就会被触发，进而把这个 fd 放入 epoll 的就绪队列。</p>
<p>这是一种“事件驱动”思路，而不是“轮询整个集合”。</p>
<hr />
<h2
id="九从内核视角看一次事件到达过程">九、从内核视角看一次事件到达过程</h2>
<p>可以把一次“客户端发数据给服务端，服务端被 <code>epoll_wait</code>
唤醒”的过程粗略理解为：</p>
<ol type="1">
<li>应用调用 <code>epoll_ctl</code>，把某个 socket 加入 epoll</li>
<li>内核在该 socket 的等待队列中挂接回调</li>
<li>客户端发送数据</li>
<li>网卡收到数据并触发中断</li>
<li>内核网络协议栈处理数据包</li>
<li>数据被放入目标 socket 的接收缓冲区</li>
<li>socket 变为可读</li>
<li>回调函数把该 socket 对应的 epitem 挂入 ready list</li>
<li>若有线程阻塞在 <code>epoll_wait</code>，则被唤醒</li>
<li><code>epoll_wait</code> 把就绪事件拷贝到用户态数组并返回</li>
</ol>
<p>这条链路里最重要的思想是：</p>
<blockquote>
<p>不是应用反复问“谁准备好了”，而是内核在事件发生时把“准备好了的人”主动登记起来。</p>
</blockquote>
<hr />
<h2 id="十epoll-为什么适合高并发">十、epoll 为什么适合高并发</h2>
<p><code>epoll</code>
适合高并发，主要不是因为它“没有任何成本”，而是它把成本集中到了更合理的地方。</p>
<h3 id="注册成本和等待成本分离">1. 注册成本和等待成本分离</h3>
<ul>
<li><code>epoll_ctl</code> 负责维护关注集合</li>
<li><code>epoll_wait</code> 负责拿取已就绪事件</li>
</ul>
<p>而 <code>select/poll</code> 往往是每次等待都重新提交全集。</p>
<h3 id="活跃连接少时收益巨大">2. 活跃连接少时收益巨大</h3>
<p>很多服务器都有这个特点：</p>
<ul>
<li>总连接数很多</li>
<li>但同一时刻真正活跃的连接只占一部分</li>
</ul>
<p>例如有 10 万连接，但每次真正有数据收发的可能只有几百个。此时：</p>
<ul>
<li><code>poll</code> 更像遍历 10 万人名单</li>
<li><code>epoll</code> 更像只处理已举手的几百人</li>
</ul>
<h3 id="更适合事件驱动架构">3. 更适合事件驱动架构</h3>
<p><code>epoll</code> 很适合 Reactor 模型：</p>
<ul>
<li>主循环等待事件</li>
<li>收到事件后分发给对应处理器</li>
<li>尽量避免阻塞调用</li>
</ul>
<p>这类模型天然适合：</p>
<ul>
<li>连接数大</li>
<li>每个连接计算不算太重</li>
<li>I/O 事件频繁</li>
</ul>
<hr />
<h2 id="十一使用-epoll-时的常见坑">十一、使用 epoll 时的常见坑</h2>
<h3 id="et-模式下没有读到-eagain">1. ET 模式下没有读到 EAGAIN</h3>
<p>这是最经典的问题。</p>
<p>如果 ET
模式下只读一次就返回，而没有把缓冲区读空，那么剩余数据可能一直躺在内核缓冲区里，但应用再也收不到提醒。</p>
<h3 id="忘记设置非阻塞">2. 忘记设置非阻塞</h3>
<p>尤其是在 ET 模式下，如果 fd 不是非阻塞的，循环 <code>read</code> /
<code>accept</code> 很容易把线程卡死。</p>
<h3 id="关闭-fd-前未正确处理-epoll-状态">3. 关闭 fd 前未正确处理 epoll
状态</h3>
<p>虽然关闭 fd 时内核通常会把它从 epoll 中清理掉，但工程上仍建议：</p>
<ol type="1">
<li>先 <code>epoll_ctl(..., EPOLL_CTL_DEL, ...)</code></li>
<li>再 <code>close(fd)</code></li>
</ol>
<p>这样逻辑更清晰，也更利于排查问题。</p>
<h3 id="误以为-epollout-应该一直监听">4. 误以为 EPOLLOUT
应该一直监听</h3>
<p>对 socket 来说，“可写”通常是很常见的状态。如果你长期监听
<code>EPOLLOUT</code>，事件循环可能会被大量可写通知淹没。</p>
<p>更常见的策略是：</p>
<ul>
<li>默认只监听 <code>EPOLLIN</code></li>
<li>当发送缓冲区有未发完数据时，再临时打开 <code>EPOLLOUT</code></li>
<li>数据发完后，再取消 <code>EPOLLOUT</code></li>
</ul>
<h3 id="没有处理对端关闭">5. 没有处理对端关闭</h3>
<p>应关注：</p>
<ul>
<li><code>read</code> 返回 <code>0</code></li>
<li><code>EPOLLRDHUP</code></li>
<li><code>EPOLLHUP</code></li>
<li><code>EPOLLERR</code></li>
</ul>
<p>否则很容易出现连接已经断开但业务侧还以为在线的情况。</p>
<hr />
<h2 id="十二epoll-与-selectpoll-的对比">十二、epoll 与 select、poll
的对比</h2>
<table>
<colgroup>
<col style="width: 25%" />
<col style="width: 25%" />
<col style="width: 25%" />
<col style="width: 25%" />
</colgroup>
<thead>
<tr class="header">
<th>对比项</th>
<th>select</th>
<th>poll</th>
<th>epoll</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>fd 数量限制</td>
<td>有，通常受 <code>FD_SETSIZE</code> 限制</td>
<td>无明显固定上限</td>
<td>无明显固定上限</td>
</tr>
<tr class="even">
<td>每次调用是否重复传全集</td>
<td>是</td>
<td>是</td>
<td>否</td>
</tr>
<tr class="odd">
<td>就绪事件获取方式</td>
<td>线性扫描</td>
<td>线性扫描</td>
<td>从就绪队列取</td>
</tr>
<tr class="even">
<td>典型复杂度特征</td>
<td><code>O(n)</code></td>
<td><code>O(n)</code></td>
<td>更接近按活跃 fd 处理</td>
</tr>
<tr class="odd">
<td>适合场景</td>
<td>小规模 fd</td>
<td>中等规模</td>
<td>大规模高并发</td>
</tr>
<tr class="even">
<td>Linux 特性</td>
<td>通用 POSIX</td>
<td>通用 POSIX</td>
<td>Linux 特有</td>
</tr>
</tbody>
</table>
<p>如果项目要求跨平台，往往还需要：</p>
<ul>
<li>Linux 用 <code>epoll</code></li>
<li>BSD / macOS 用 <code>kqueue</code></li>
<li>Windows 用 <code>IOCP</code></li>
</ul>
<p>很多跨平台网络库会把这些机制再封装一层。</p>
<hr />
<h2 id="十三epoll-在工程中的常见使用模式">十三、epoll
在工程中的常见使用模式</h2>
<h3 id="单-reactor-单线程">1. 单 Reactor 单线程</h3>
<p>适合：</p>
<ul>
<li>逻辑简单</li>
<li>连接很多但每次处理很轻</li>
<li>学习或轻量服务</li>
</ul>
<p>模式如下：</p>
<ul>
<li>一个线程 <code>epoll_wait</code></li>
<li>同一个线程完成 accept、read、write、业务处理</li>
</ul>
<p>优点是简单，缺点是业务计算重时容易拖慢整个事件循环。</p>
<h3 id="单-reactor-多工作线程">2. 单 Reactor 多工作线程</h3>
<p>常见模式：</p>
<ul>
<li>一个线程负责 I/O 事件分发</li>
<li>多个工作线程负责业务逻辑</li>
</ul>
<p>这样可以把：</p>
<ul>
<li>I/O 就绪通知</li>
<li>业务 CPU 计算</li>
</ul>
<p>分离开来。</p>
<h3 id="多-reactor-多线程">3. 多 Reactor 多线程</h3>
<p>更高性能的网络服务器中常见：</p>
<ul>
<li>一个主 Reactor 负责监听和 accept</li>
<li>多个子 Reactor 各自维护自己的 epoll 实例</li>
<li>新连接按策略分发给某个子 Reactor</li>
</ul>
<p>这是许多成熟网络框架会采用的思路。</p>
<hr />
<h2 id="十四一个简化的理解模型">十四、一个简化的理解模型</h2>
<p>如果用“教室点名”来类比：</p>
<ul>
<li><code>select/poll</code>：每次上课都拿全班名单，一个一个问“到了没？”</li>
<li><code>epoll_ctl</code>：先把全班名单交给班长长期保存</li>
<li>就绪链表：已经到教室的人主动去签到</li>
<li><code>epoll_wait</code>：老师只看签到表，不看全班总名单</li>
</ul>
<p>这个类比虽然不完全严谨，但很适合理解 <code>epoll</code>
的核心优势：</p>
<blockquote>
<p>把“全量扫描”变成“增量通知 + 就绪集合提取”。</p>
</blockquote>
<hr />
<h2 id="十五总结">十五、总结</h2>
<p><code>epoll</code> 的价值可以浓缩成三点：</p>
<ol type="1">
<li><strong>使用上</strong>：通过
<code>epoll_create1</code>、<code>epoll_ctl</code>、<code>epoll_wait</code>
三步完成事件驱动式 I/O 管理</li>
<li><strong>编程上</strong>：通常配合非阻塞 socket，尤其在 ET
模式下必须读/写到 <code>EAGAIN</code></li>
<li><strong>实现上</strong>：内核通过红黑树维护关注集合，通过就绪链表维护已发生事件，并借助回调机制在事件到达时主动通知</li>
</ol>
<p>所以，<code>epoll</code> 真正高效的地方，不是简单一句“时间复杂度
O(1)”就能说清，而是：</p>
<blockquote>
<p>它避免了每次等待都重复提交和扫描整个 fd
集合，把精力集中在真正活跃的连接上。</p>
</blockquote>
<p>如果只是记忆结论，那么你会觉得 <code>epoll</code> 只是一个 API；
如果理解了它的内核组织方式，就会明白它为什么能成为 Linux
高并发网络编程的基础设施。</p>
]]></content>
      <categories>
        <category>操作系统与系统编程（Systems）</category>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>IO多路复用</tag>
        <tag>网络编程</tag>
      </tags>
  </entry>
  <entry>
    <title>大师语录</title>
    <url>/2023/12/31/notes-reading/master-quotes/</url>
    <content><![CDATA[<p>本文记录了一些大师的语录，旨在帮助自己加深对工作与生活的理解，并从中获得启发。</p>
<span id="more"></span>
<hr />
<h2 id="计算机">计算机</h2>
<h3 id="linus-torvalds">Linus Torvalds</h3>
<p><img data-src="https://avatars.githubusercontent.com/u/1024025?v=4" alt="Linus Torvalds" style="zoom: 50%;" /></p>
<p>Linux、Git发明者，<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL3RvcnZhbGRz">github主页<i class="fa fa-external-link-alt"></i></span>。</p>
<ol type="1">
<li>What makes a great game is not usually how fast it is or how good
the graphics are. There has to be something that makes you play it,
something that keeps you with it.</li>
<li>If you try to make money by controlling a resource, you’ll
eventually find yourself out of business.</li>
</ol>
<h3 id="bjarne-stroustrup">Bjarne Stroustrup</h3>
<p><img data-src="https://www.stroustrup.com/Bjarne2018.jpg" style="zoom: 67%;" alt="Bjarne Stroustrup"/></p>
<p>C++之父，<span class="exturl" data-url="aHR0cHM6Ly93d3cuc3Ryb3VzdHJ1cC5jb20vaW5kZXguaHRtbA==">个人主页<i class="fa fa-external-link-alt"></i></span>。</p>
<ol type="1">
<li>Ease yourself into the language. Don’t feel you have to use all of
the features, and don’t try to use them all on the first day.</li>
<li>One of the guiding principles of C++ is that you shouldn’t have to
pay (in memory usage or processing time) for features you don’t
use.</li>
</ol>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Misc</category>
      </categories>
      <tags>
        <tag>Thoughts</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux 中 poll 的用法与底层实现</title>
    <url>/2026/02/14/programming/linux/poll/</url>
    <content><![CDATA[<p>在 Linux 的 I/O 多路复用机制中，<code>poll</code> 可以看作是
<code>select</code>
的改进版本。它保留了“一个线程同时等待多个文件描述符事件”的基本思想，同时解决了
<code>select</code> 在 fd 表示方式上的一些局限。</p>
<span id="more"></span>
<hr />
<h2 id="一什么是-poll">一、什么是 poll</h2>
<p><code>poll</code> 是 Linux/Unix 系统中常见的 I/O
多路复用接口，用于同时监视多个文件描述符的状态变化。</p>
<p>它的系统调用原型如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;poll.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">poll</span><span class="params">(<span class="keyword">struct</span> pollfd *fds, <span class="type">nfds_t</span> nfds, <span class="type">int</span> timeout)</span>;</span><br></pre></td></tr></table></figure>
<p>其中：</p>
<ul>
<li><code>fds</code>：一个 <code>pollfd</code>
数组，每个元素描述一个待监视 fd</li>
<li><code>nfds</code>：数组元素个数</li>
<li><code>timeout</code>：超时时间，单位毫秒</li>
</ul>
<p>与 <code>select</code> 相比，<code>poll</code> 最直观的区别在于：</p>
<blockquote>
<p>它不再使用位图，而是使用结构体数组来描述关注的 fd 和事件。</p>
</blockquote>
<hr />
<h2 id="二poll-解决了-select-的哪些问题">二、poll 解决了 select
的哪些问题</h2>
<p>理解 <code>poll</code>，最好先从 <code>select</code> 的问题出发。</p>
<p><code>select</code> 的几个典型缺点是：</p>
<ul>
<li><code>fd_set</code> 通常受 <code>FD_SETSIZE</code> 限制</li>
<li>需要通过“最大 fd + 1”来决定扫描范围</li>
<li>使用位图表达集合，不够直观</li>
</ul>
<p><code>poll</code> 对此做了两点重要改进：</p>
<ol type="1">
<li><strong>不再使用固定大小的位图，而是使用数组</strong></li>
<li><strong>不再依赖最大 fd + 1，而是直接依赖数组长度</strong></li>
</ol>
<p>也就是说，<code>poll</code> 更像是：</p>
<blockquote>
<p>直接把“要关注谁、关注什么事件”整理成一张列表交给内核。</p>
</blockquote>
<p>不过它并没有彻底解决“每次都提交全集”和“线性扫描”的问题，所以它只是改进，不是质变。</p>
<hr />
<h2 id="三pollfd-结构体与事件含义">三、pollfd 结构体与事件含义</h2>
<p><code>poll</code> 的核心数据结构是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">pollfd</span> &#123;</span></span><br><span class="line">    <span class="type">int</span>   fd;</span><br><span class="line">    <span class="type">short</span> events;</span><br><span class="line">    <span class="type">short</span> revents;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>字段含义如下：</p>
<ul>
<li><code>fd</code>：要监听的文件描述符</li>
<li><code>events</code>：希望监听的事件</li>
<li><code>revents</code>：内核返回的实际发生事件</li>
</ul>
<p>常见事件包括：</p>
<ul>
<li><code>POLLIN</code>：可读</li>
<li><code>POLLOUT</code>：可写</li>
<li><code>POLLERR</code>：出错</li>
<li><code>POLLHUP</code>：挂断</li>
<li><code>POLLNVAL</code>：fd 非法</li>
<li><code>POLLRDHUP</code>：对端关闭写端（Linux 常见扩展）</li>
</ul>
<p>例如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">pollfd</span> <span class="title">fds</span>[2];</span></span><br><span class="line">fds[<span class="number">0</span>].fd = listenfd;</span><br><span class="line">fds[<span class="number">0</span>].events = POLLIN;</span><br><span class="line"></span><br><span class="line">fds[<span class="number">1</span>].fd = connfd;</span><br><span class="line">fds[<span class="number">1</span>].events = POLLIN;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="四poll-的基本用法">四、poll 的基本用法</h2>
<h3 id="调用方式">1. 调用方式</h3>
<p>一个最简单的调用例子：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> ret = poll(fds, nfds, <span class="number">5000</span>);</span><br></pre></td></tr></table></figure>
<p>表示：</p>
<ul>
<li>监视 <code>fds</code> 数组中的所有 fd</li>
<li>最多等待 5000 毫秒</li>
<li>返回后在 <code>revents</code> 中查看哪些 fd 就绪</li>
</ul>
<h3 id="timeout-语义">2. timeout 语义</h3>
<ul>
<li><code>timeout &lt; 0</code>：一直阻塞</li>
<li><code>timeout == 0</code>：立即返回</li>
<li><code>timeout &gt; 0</code>：等待指定毫秒数</li>
</ul>
<h3 id="返回值">3. 返回值</h3>
<ul>
<li><code>&gt; 0</code>：就绪 fd 的数量</li>
<li><code>0</code>：超时</li>
<li><code>&lt; 0</code>：出错</li>
</ul>
<hr />
<h2 id="五poll-的典型使用流程">五、poll 的典型使用流程</h2>
<p>一个基于 <code>poll</code> 的 TCP 服务端，大体流程如下：</p>
<ol type="1">
<li>创建监听 socket</li>
<li><code>bind</code> + <code>listen</code></li>
<li>准备 <code>pollfd</code> 数组</li>
<li>把监听 fd 放入数组并关注 <code>POLLIN</code></li>
<li>循环调用 <code>poll</code></li>
<li>遍历数组，查看哪些元素的 <code>revents</code> 非空</li>
<li>处理对应 fd 的读写或关闭逻辑</li>
</ol>
<p>下面是一个简化示例：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;poll.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 8080</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> MAXFDS 1024</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> listenfd = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">addr</span>;</span></span><br><span class="line">    <span class="built_in">memset</span>(&amp;addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(addr));</span><br><span class="line">    addr.sin_family = AF_INET;</span><br><span class="line">    addr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line">    addr.sin_port = htons(PORT);</span><br><span class="line"></span><br><span class="line">    bind(listenfd, (<span class="keyword">struct</span> sockaddr *)&amp;addr, <span class="keyword">sizeof</span>(addr));</span><br><span class="line">    listen(listenfd, <span class="number">128</span>);</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">pollfd</span> <span class="title">fds</span>[<span class="title">MAXFDS</span>];</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; MAXFDS; i++) &#123;</span><br><span class="line">        fds[i].fd = <span class="number">-1</span>;</span><br><span class="line">        fds[i].events = <span class="number">0</span>;</span><br><span class="line">        fds[i].revents = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    fds[<span class="number">0</span>].fd = listenfd;</span><br><span class="line">    fds[<span class="number">0</span>].events = POLLIN;</span><br><span class="line">    <span class="type">int</span> nfds = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="type">int</span> nready = poll(fds, nfds, <span class="number">-1</span>);</span><br><span class="line">        <span class="keyword">if</span> (nready &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (fds[<span class="number">0</span>].revents &amp; POLLIN) &#123;</span><br><span class="line">            <span class="type">int</span> connfd = accept(listenfd, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">            <span class="keyword">if</span> (connfd &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt; MAXFDS; i++) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (fds[i].fd == <span class="number">-1</span>) &#123;</span><br><span class="line">                        fds[i].fd = connfd;</span><br><span class="line">                        fds[i].events = POLLIN;</span><br><span class="line">                        <span class="keyword">if</span> (i &gt;= nfds) nfds = i + <span class="number">1</span>;</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (--nready &lt;= <span class="number">0</span>) <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt; nfds; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (fds[i].fd == <span class="number">-1</span>) <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (fds[i].revents &amp; (POLLIN | POLLERR | POLLHUP)) &#123;</span><br><span class="line">                <span class="type">char</span> buf[<span class="number">4096</span>];</span><br><span class="line">                <span class="type">ssize_t</span> n = read(fds[i].fd, buf, <span class="keyword">sizeof</span>(buf));</span><br><span class="line">                <span class="keyword">if</span> (n &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    write(fds[i].fd, buf, n);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    close(fds[i].fd);</span><br><span class="line">                    fds[i].fd = <span class="number">-1</span>;</span><br><span class="line">                    fds[i].events = <span class="number">0</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    close(listenfd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个示例体现了 <code>poll</code> 的典型写法：</p>
<ul>
<li>用数组保存所有关注对象</li>
<li>返回后遍历数组查看 <code>revents</code></li>
</ul>
<hr />
<h2 id="六poll-相比-select-的改进点">六、poll 相比 select 的改进点</h2>
<h3 id="不再受固定大小位图的直接限制">1.
不再受固定大小位图的直接限制</h3>
<p><code>poll</code> 不使用
<code>fd_set</code>，而是使用数组，因此在接口层面上不再像
<code>select</code> 那样天然绑定到 <code>FD_SETSIZE</code>。</p>
<p>当然，这并不意味着它可以无限扩展，实际仍受：</p>
<ul>
<li>进程可打开文件数限制</li>
<li>内存开销</li>
<li>线性扫描成本</li>
</ul>
<h3 id="不依赖最大-fd-1">2. 不依赖最大 fd + 1</h3>
<p><code>poll</code> 直接传数组长度 <code>nfds</code>，不需要再维护
<code>maxfd + 1</code> 这种额外信息。</p>
<h3 id="表达更直接">3. 表达更直接</h3>
<p>每个 <code>pollfd</code> 元素都显式写出：</p>
<ul>
<li>监听哪个 fd</li>
<li>关心什么事件</li>
<li>实际发生了什么事件</li>
</ul>
<p>可读性通常比 <code>select</code> 更好。</p>
<hr />
<h2 id="七poll-仍然存在的核心问题">七、poll 仍然存在的核心问题</h2>
<p>虽然 <code>poll</code> 比 <code>select</code>
更灵活，但它并没有改变整体工作模式。</p>
<h3 id="每次调用仍要把全集交给内核">1. 每次调用仍要把全集交给内核</h3>
<p><code>poll</code> 每次调用时，应用依旧要把整个 <code>pollfd</code>
数组传给内核。</p>
<p>也就是说，关注集合并没有像 <code>epoll</code>
那样长期保存在内核里。</p>
<h3 id="内核仍需要遍历整个数组">2. 内核仍需要遍历整个数组</h3>
<p>内核需要检查数组中的每个元素，看对应 fd 是否就绪。</p>
<h3 id="用户态返回后仍要遍历整个数组">3.
用户态返回后仍要遍历整个数组</h3>
<p>即使本次只有少数几个 fd 真正发生事件，应用仍然需要扫描数组，判断谁的
<code>revents</code> 非零。</p>
<p>所以从复杂度特征上看，<code>poll</code> 仍然是接近 <code>O(n)</code>
的。</p>
<hr />
<h2 id="八poll-的底层实现思路">八、poll 的底层实现思路</h2>
<p><code>poll</code> 的实现思路可以概括为：</p>
<blockquote>
<p>每次调用把“关注列表”作为数组交给内核，内核逐项检查，并在等待期间挂入相应等待队列，事件到来后再整理结果返回。</p>
</blockquote>
<h3 id="数组是核心输入结构">1. 数组是核心输入结构</h3>
<p>与 <code>select</code> 的位图不同，<code>poll</code> 以
<code>pollfd</code> 数组作为输入。内核拿到数组后，会逐项查看：</p>
<ul>
<li>这个元素对应哪个 fd</li>
<li>想监听什么事件</li>
</ul>
<h3 id="对每个-fd-调用对应的-poll-回调">2. 对每个 fd 调用对应的 poll
回调</h3>
<p>在 Linux 内核里，不同类型的文件对象通常会提供各自的 <code>poll</code>
回调逻辑。</p>
<p>例如
socket、pipe、字符设备，它们判断“当前是否可读/可写”的方式并不完全相同。</p>
<p>因此内核在处理 <code>poll</code> 时，会对每个 fd
对应的文件对象调用相应的 poll 方法，以判断当前状态。</p>
<h3 id="如果暂时未就绪则把当前线程挂到等待队列">3.
如果暂时未就绪，则把当前线程挂到等待队列</h3>
<p>如果当前没有感兴趣的事件发生，而调用者允许阻塞，那么内核会把当前线程与相关等待队列关联起来。</p>
<p>后续一旦某个 fd 状态变化，对应等待队列就能唤醒该线程。</p>
<h3 id="唤醒后重新扫描并填写-revents">4. 唤醒后重新扫描并填写
revents</h3>
<p>线程被唤醒后，内核会再次检查数组中的 fd，把就绪结果写入各元素的
<code>revents</code> 字段，然后返回给用户态。</p>
<p>这里要注意：</p>
<blockquote>
<p><code>poll</code> 虽然也利用等待队列和唤醒机制，但它并没有像
<code>epoll</code> 那样专门维护一个长期存在的“就绪事件队列”。</p>
</blockquote>
<p>所以它依然需要围绕整个数组做遍历。</p>
<hr />
<h2 id="九从内核视角看一次-poll-等待过程">九、从内核视角看一次 poll
等待过程</h2>
<p>可以粗略理解为：</p>
<ol type="1">
<li>用户态准备 <code>pollfd</code> 数组</li>
<li>调用 <code>poll</code></li>
<li>内核复制数组到内核态</li>
<li>逐项检查每个 fd 当前是否满足 <code>events</code></li>
<li>若没有事件且允许阻塞，则线程进入等待</li>
<li>某个 socket 收到数据或状态变化</li>
<li>等待线程被唤醒</li>
<li>内核再次扫描数组</li>
<li>把实际发生事件填写到 <code>revents</code></li>
<li>将结果返回用户态</li>
</ol>
<p>这说明 <code>poll</code> 相比 <code>select</code>
的改进主要在“接口和数据表达”，不是在“整体处理模型”上发生根本变化。</p>
<hr />
<h2 id="十poll-为什么仍不适合超大规模高并发">十、poll
为什么仍不适合超大规模高并发</h2>
<p>假设服务端维护 5 万个连接，而某一时刻只有 200 个连接活跃。</p>
<p><code>poll</code> 通常仍需要：</p>
<ul>
<li>把 5 万个 <code>pollfd</code> 元素传给内核</li>
<li>内核遍历这 5 万项</li>
<li>返回后应用再遍历这 5 万项</li>
</ul>
<p>因此它更像是：</p>
<blockquote>
<p>不管今天真正活跃的是谁，都先把整个通讯录翻一遍。</p>
</blockquote>
<p>这就是为什么在超大规模并发连接场景里，<code>poll</code> 往往会被
<code>epoll</code> 替代。</p>
<hr />
<h2 id="十一使用-poll-时的常见坑">十一、使用 poll 时的常见坑</h2>
<h3 id="忘记检查-revents-而只看-events">1. 忘记检查 revents 而只看
events</h3>
<p><code>events</code> 是“你想监听什么”，<code>revents</code>
才是“实际发生了什么”。</p>
<p>返回后要重点看 <code>revents</code>。</p>
<h3 id="没有处理错误和挂断事件">2. 没有处理错误和挂断事件</h3>
<p>除了 <code>POLLIN</code>，通常还应注意：</p>
<ul>
<li><code>POLLERR</code></li>
<li><code>POLLHUP</code></li>
<li><code>POLLNVAL</code></li>
</ul>
<p>否则容易漏掉异常关闭连接。</p>
<h3 id="数组中删除元素策略混乱">3. 数组中删除元素策略混乱</h3>
<p>有些程序在关闭 fd 后会：</p>
<ul>
<li>直接把该项 <code>fd = -1</code></li>
<li>或者把最后一个元素挪过来覆盖</li>
</ul>
<p>两种都可以，但要保持整体策略一致，否则很容易出 bug。</p>
<h3 id="误以为-poll-返回值就是数组下标">4. 误以为 poll
返回值就是数组下标</h3>
<p>返回值表示“就绪 fd
的数量”，不是哪个元素就绪，因此仍要自己遍历数组判断。</p>
<h3 id="长期监听-pollout">5. 长期监听 POLLOUT</h3>
<p>和 <code>epoll</code> 类似，socket 在很多时候都可写。如果一直监听
<code>POLLOUT</code>，可能造成大量无意义唤醒。</p>
<hr />
<h2 id="十二poll-与-selectepoll-的对比">十二、poll 与 select、epoll
的对比</h2>
<table>
<colgroup>
<col style="width: 25%" />
<col style="width: 25%" />
<col style="width: 25%" />
<col style="width: 25%" />
</colgroup>
<thead>
<tr class="header">
<th>对比项</th>
<th>select</th>
<th>poll</th>
<th>epoll</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>fd 表示方式</td>
<td>位图</td>
<td><code>pollfd</code> 数组</td>
<td>内核对象 + 事件注册</td>
</tr>
<tr class="even">
<td>fd 数量限制</td>
<td>常受 <code>FD_SETSIZE</code> 影响</td>
<td>无明显固定上限</td>
<td>无明显固定上限</td>
</tr>
<tr class="odd">
<td>每次调用重复传全集</td>
<td>是</td>
<td>是</td>
<td>否</td>
</tr>
<tr class="even">
<td>用户态是否线性扫描</td>
<td>是</td>
<td>是</td>
<td>通常只处理返回的就绪项</td>
</tr>
<tr class="odd">
<td>复杂度特征</td>
<td><code>O(n)</code></td>
<td><code>O(n)</code></td>
<td>更接近按活跃事件处理</td>
</tr>
<tr class="even">
<td>适用场景</td>
<td>小规模/通用</td>
<td>中等规模</td>
<td>大规模高并发</td>
</tr>
</tbody>
</table>
<p>可以把三者理解为递进关系：</p>
<ul>
<li><code>select</code>：最早的基础版本</li>
<li><code>poll</code>：接口更合理的改进版</li>
<li><code>epoll</code>：Linux 下更适合高并发的版本</li>
</ul>
<hr />
<h2 id="十三poll-在工程中的适用场景">十三、poll 在工程中的适用场景</h2>
<p><code>poll</code> 一般适合以下情况：</p>
<h3 id="连接数中等规模">1. 连接数中等规模</h3>
<p>当连接数还没有大到必须用 <code>epoll</code>，但又不想受
<code>select</code> 位图限制时，<code>poll</code>
是一个自然过渡选择。</p>
<h3 id="想保留较通用的-unix-接口风格">2. 想保留较通用的 Unix
接口风格</h3>
<p>相比 <code>epoll</code>，<code>poll</code> 不是 Linux
专有接口，在很多 Unix-like 系统中也存在类似支持。</p>
<h3 id="教学与对比分析">3. 教学与对比分析</h3>
<p>如果要理解为什么 <code>epoll</code> 比前两者更强，那么
<code>poll</code> 是一个很好的中间层：</p>
<ul>
<li>它修正了 <code>select</code> 的一部分接口问题</li>
<li>但保留了线性扫描这一核心瓶颈</li>
</ul>
<hr />
<h2 id="十四总结">十四、总结</h2>
<p><code>poll</code> 的核心特点可以概括为：</p>
<ol type="1">
<li><strong>接口上比 <code>select</code> 更自然</strong>：使用
<code>pollfd</code> 数组而不是位图</li>
<li><strong>能力上比 <code>select</code> 更灵活</strong>：不依赖
<code>maxfd + 1</code>，fd 管理更直观</li>
<li><strong>性能模型上仍接近线性扫描</strong>：每次调用仍要提交全集、遍历全集</li>
</ol>
<p>因此，<code>poll</code> 可以看作是 <code>select</code> 到
<code>epoll</code> 之间的一个过渡形态：</p>
<ul>
<li>它改善了接口设计</li>
<li>但没有从根本上解决大规模 fd 下的扫描成本问题</li>
</ul>
<p>也正因如此，理解 <code>poll</code> 之后，再去看 <code>epoll</code>
的“注册集合 +
就绪队列”设计，就会更容易明白后者为什么更适合高并发服务器。</p>
]]></content>
      <categories>
        <category>操作系统与系统编程（Systems）</category>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>IO多路复用</tag>
        <tag>网络编程</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux 中 select 的用法与底层实现</title>
    <url>/2026/02/07/programming/linux/select/</url>
    <content><![CDATA[<p>在 Linux 网络编程里，<code>select</code> 是最早被广泛使用的 I/O
多路复用机制之一。虽然如今在高并发场景中它通常会被 <code>epoll</code>
替代，但 <code>select</code> 依然是理解 I/O 多路复用思想的重要起点。</p>
<span id="more"></span>
<hr />
<h2 id="一什么是-select">一、什么是 select</h2>
<p><code>select</code> 的作用是：</p>
<blockquote>
<p>让一个线程同时等待多个文件描述符上的事件，并在其中某些 fd
就绪时返回。</p>
</blockquote>
<p>它最常见的使用场景是网络编程，比如一个服务端同时监听：</p>
<ul>
<li>监听 socket 是否有新连接到来</li>
<li>已连接 socket 是否有数据可读</li>
<li>某些 socket 是否可以继续发送数据</li>
</ul>
<p>对应的系统调用原型如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/select.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">select</span><span class="params">(<span class="type">int</span> nfds,</span></span><br><span class="line"><span class="params">           fd_set *readfds,</span></span><br><span class="line"><span class="params">           fd_set *writefds,</span></span><br><span class="line"><span class="params">           fd_set *exceptfds,</span></span><br><span class="line"><span class="params">           <span class="keyword">struct</span> timeval *timeout)</span>;</span><br></pre></td></tr></table></figure>
<p>从接口上看，它允许我们分别关注三类事件：</p>
<ul>
<li>可读</li>
<li>可写</li>
<li>异常</li>
</ul>
<hr />
<h2 id="二为什么会有-select">二、为什么会有 select</h2>
<p>在没有 I/O 多路复用之前，如果一个进程要同时处理多个
socket，常见办法有两种：</p>
<ol type="1">
<li><strong>阻塞式串行处理</strong>：一次只处理一个连接</li>
<li><strong>每连接一个线程/进程</strong>：让每个连接由独立执行流处理</li>
</ol>
<p>这两种方式都有明显问题：</p>
<ul>
<li>串行处理时，并发能力很差</li>
<li>每连接一个线程会带来较高的调度、栈空间和上下文切换成本</li>
</ul>
<p><code>select</code> 的出现提供了第三种思路：</p>
<blockquote>
<p>一个线程先统一等待，哪个 fd 准备好了，再去处理哪个 fd。</p>
</blockquote>
<p>这就是 I/O 多路复用的基本思想。</p>
<hr />
<h2 id="三select-的基本用法">三、select 的基本用法</h2>
<h3 id="fd_set-与相关宏">1. fd_set 与相关宏</h3>
<p><code>select</code> 使用 <code>fd_set</code>
表示一个文件描述符集合，并配套提供了一组宏：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">FD_ZERO(fd_set *<span class="built_in">set</span>);   <span class="comment">// 清空集合</span></span><br><span class="line">FD_SET(<span class="type">int</span> fd, fd_set *<span class="built_in">set</span>);   <span class="comment">// 添加 fd</span></span><br><span class="line">FD_CLR(<span class="type">int</span> fd, fd_set *<span class="built_in">set</span>);   <span class="comment">// 删除 fd</span></span><br><span class="line">FD_ISSET(<span class="type">int</span> fd, fd_set *<span class="built_in">set</span>); <span class="comment">// 判断 fd 是否在集合中</span></span><br></pre></td></tr></table></figure>
<p>最常见的写法如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">fd_set readfds;</span><br><span class="line">FD_ZERO(&amp;readfds);</span><br><span class="line">FD_SET(listenfd, &amp;readfds);</span><br><span class="line">FD_SET(connfd, &amp;readfds);</span><br></pre></td></tr></table></figure>
<h3 id="nfds-的含义">2. nfds 的含义</h3>
<p><code>nfds</code> 不是 fd 的数量，而是：</p>
<blockquote>
<p>所有被监视 fd 中“最大 fd 值 + 1”。</p>
</blockquote>
<p>例如监视 <code>3、5、8</code>，那么 <code>nfds</code> 应该传
<code>9</code>。</p>
<h3 id="timeout-的含义">3. timeout 的含义</h3>
<p><code>timeout</code> 控制 <code>select</code> 的阻塞时间：</p>
<ul>
<li><code>NULL</code>：一直阻塞，直到有事件发生</li>
<li><code>{0, 0}</code>：立即返回，非阻塞轮询</li>
<li>其他值：阻塞指定时间</li>
</ul>
<p>例如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timeval</span> <span class="title">tv</span>;</span></span><br><span class="line">tv.tv_sec = <span class="number">5</span>;</span><br><span class="line">tv.tv_usec = <span class="number">0</span>;</span><br></pre></td></tr></table></figure>
<p>表示最多等待 5 秒。</p>
<hr />
<h2 id="四select-的典型使用流程">四、select 的典型使用流程</h2>
<p>一个简单的 TCP 服务端使用 <code>select</code>
的基本流程通常如下：</p>
<ol type="1">
<li>创建监听 socket</li>
<li><code>bind</code> + <code>listen</code></li>
<li>准备一个“主集合”，保存所有需要监视的 fd</li>
<li>每次循环前，把主集合拷贝到临时集合</li>
<li>调用 <code>select</code></li>
<li>遍历所有可能的 fd，检查谁就绪了</li>
<li>分别处理：
<ul>
<li>监听 fd：执行 <code>accept</code></li>
<li>连接 fd：执行 <code>read</code> / <code>write</code></li>
</ul></li>
<li>连接关闭时将 fd 从集合中移除</li>
</ol>
<p>下面是一个简化示例：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/select.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 8080</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> listenfd = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">addr</span>;</span></span><br><span class="line">    <span class="built_in">memset</span>(&amp;addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(addr));</span><br><span class="line">    addr.sin_family = AF_INET;</span><br><span class="line">    addr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line">    addr.sin_port = htons(PORT);</span><br><span class="line"></span><br><span class="line">    bind(listenfd, (<span class="keyword">struct</span> sockaddr *)&amp;addr, <span class="keyword">sizeof</span>(addr));</span><br><span class="line">    listen(listenfd, <span class="number">128</span>);</span><br><span class="line"></span><br><span class="line">    fd_set allset, rset;</span><br><span class="line">    FD_ZERO(&amp;allset);</span><br><span class="line">    FD_SET(listenfd, &amp;allset);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> maxfd = listenfd;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        rset = allset;</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> nready = select(maxfd + <span class="number">1</span>, &amp;rset, <span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">        <span class="keyword">if</span> (nready &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (FD_ISSET(listenfd, &amp;rset)) &#123;</span><br><span class="line">            <span class="type">int</span> connfd = accept(listenfd, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">            <span class="keyword">if</span> (connfd &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                FD_SET(connfd, &amp;allset);</span><br><span class="line">                <span class="keyword">if</span> (connfd &gt; maxfd) maxfd = connfd;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (--nready &lt;= <span class="number">0</span>) <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> fd = <span class="number">0</span>; fd &lt;= maxfd; fd++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (fd == listenfd) <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (FD_ISSET(fd, &amp;rset)) &#123;</span><br><span class="line">                <span class="type">char</span> buf[<span class="number">4096</span>];</span><br><span class="line">                <span class="type">ssize_t</span> n = read(fd, buf, <span class="keyword">sizeof</span>(buf));</span><br><span class="line">                <span class="keyword">if</span> (n &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    write(fd, buf, n);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    close(fd);</span><br><span class="line">                    FD_CLR(fd, &amp;allset);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    close(listenfd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个例子是典型的 <code>select</code> 版 echo
server，重点在于理解结构，而不是直接用于生产环境。</p>
<hr />
<h2 id="五select-返回后该怎么看结果">五、select 返回后该怎么看结果</h2>
<p>这是初学者很容易忽略的一个点。</p>
<p><code>select</code> 调用前，你把关心的 fd 填进
<code>readfds/writefds/exceptfds</code>；
调用返回后，这几个集合会被<strong>原地修改</strong>，只保留已经就绪的那些
fd。</p>
<p>例如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">rset = allset;</span><br><span class="line"><span class="type">int</span> nready = select(maxfd + <span class="number">1</span>, &amp;rset, <span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure>
<p>返回后需要这样判断：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (FD_ISSET(fd, &amp;rset)) &#123;</span><br><span class="line">    <span class="comment">// fd 可读</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>也正因为 <code>select</code>
会修改集合，所以每次调用前都需要重新拷贝一份。</p>
<hr />
<h2 id="六select-能监听哪些事件">六、select 能监听哪些事件</h2>
<p><code>select</code> 主要支持三类集合：</p>
<h3 id="readfds">1. readfds</h3>
<p>表示关心“可读”事件，常见情况：</p>
<ul>
<li>socket 接收缓冲区中有数据</li>
<li>监听 socket 上有新连接到来</li>
<li>对端关闭连接，此时读可能返回 <code>0</code></li>
</ul>
<h3 id="writefds">2. writefds</h3>
<p>表示关心“可写”事件，常见情况：</p>
<ul>
<li>socket 发送缓冲区有空间</li>
<li>非阻塞 <code>connect</code> 已完成</li>
</ul>
<h3 id="exceptfds">3. exceptfds</h3>
<p>表示关心“异常条件”，在普通 TCP 服务端里相对少用。</p>
<p>它不是一般意义上的“程序异常”，更多是一些带外数据等特殊情况。</p>
<hr />
<h2 id="七select-的优点和局限">七、select 的优点和局限</h2>
<h3 id="优点">1. 优点</h3>
<p><code>select</code> 的优点主要有：</p>
<ul>
<li>历史悠久，资料多</li>
<li>接口标准化，很多 Unix-like 系统都支持</li>
<li>适合学习 I/O 多路复用的基本思想</li>
<li>在小规模连接场景下完全够用</li>
</ul>
<h3 id="局限">2. 局限</h3>
<p><code>select</code> 最大的问题并不是“不能用”，而是它在 fd
多起来后效率会明显下降。</p>
<p>主要体现在以下几个方面。</p>
<h4 id="fd-数量有限制">（1）fd 数量有限制</h4>
<p><code>fd_set</code> 通常受 <code>FD_SETSIZE</code>
限制，很多系统上默认是 <code>1024</code>。</p>
<p>这意味着：</p>
<blockquote>
<p>单次 <code>select</code> 可监视的 fd 数量通常不能无限增长。</p>
</blockquote>
<h4 id="每次都要重新传入全集">（2）每次都要重新传入全集</h4>
<p>应用每次调用 <code>select</code>，都要把所有关心的 fd
集合重新准备好，再交给内核。</p>
<h4 id="返回后仍要线性扫描">（3）返回后仍要线性扫描</h4>
<p>即使只有 3 个 fd 真正就绪了，应用也通常要从 <code>0</code> 扫描到
<code>maxfd</code>，逐个调用 <code>FD_ISSET</code> 检查。</p>
<p>这使得它的处理成本与 <code>maxfd</code> 高度相关。</p>
<h4
id="用户态与内核态之间有集合拷贝成本">（4）用户态与内核态之间有集合拷贝成本</h4>
<p>每次 <code>select</code> 调用，用户态的 <code>fd_set</code>
需要传入内核，返回时又需要把结果带回用户态。</p>
<hr />
<h2 id="八select-的底层实现思路">八、select 的底层实现思路</h2>
<p>理解 <code>select</code> 的关键是明白：</p>
<blockquote>
<p>它本质上更接近“每次调用时，拿着一张完整名单去检查一遍”。</p>
</blockquote>
<h3 id="位图表示-fd-集合">1. 位图表示 fd 集合</h3>
<p><code>fd_set</code>
在底层通常可以理解为一个<strong>位图（bitmap）</strong>。</p>
<p>例如某个 fd 是否在集合里，本质上就是看对应 bit 是否置位。</p>
<p>这种表示方式很紧凑，但它也天然意味着：</p>
<ul>
<li>需要按照 fd 编号组织</li>
<li>处理时往往按编号顺序扫描</li>
</ul>
<h3 id="内核遍历用户给出的监视集合">2. 内核遍历用户给出的监视集合</h3>
<p>调用 <code>select</code> 后，内核会根据 <code>nfds</code>，检查
<code>[0, nfds)</code> 范围内哪些 fd 在对应集合里被置位。</p>
<p>对于这些 fd，内核会进一步检查：</p>
<ul>
<li>是否可读</li>
<li>是否可写</li>
<li>是否存在异常条件</li>
</ul>
<p>如果没有任何 fd 就绪，而调用者又允许阻塞，当前线程就会睡眠等待。</p>
<h3 id="事件发生后再重新筛选">3. 事件发生后再重新筛选</h3>
<p>当某个 fd
状态变化时，内核会唤醒等待中的线程；线程恢复后，<code>select</code>
再根据当前状态整理结果集合，把真正就绪的 fd 保留下来。</p>
<p>因此 <code>select</code>
的核心特征不是“保存一份长期关注列表”，而是：</p>
<blockquote>
<p>每次调用都重新提交一份关注集合，然后让内核基于这份集合完成检查。</p>
</blockquote>
<h3 id="复杂度特征">4. 复杂度特征</h3>
<p><code>select</code> 通常会表现出接近 <code>O(n)</code> 的特征，其中
<code>n</code> 与 <code>nfds</code> 或 <code>maxfd</code>
范围强相关。</p>
<p>这也是它在高并发场景下不如 <code>epoll</code> 的根本原因之一。</p>
<hr />
<h2 id="九从内核视角看一次-select-等待过程">九、从内核视角看一次 select
等待过程</h2>
<p>可以粗略理解为：</p>
<ol type="1">
<li>用户态准备好 <code>readfds/writefds/exceptfds</code></li>
<li>调用 <code>select</code></li>
<li>内核接收这些集合并复制到内核空间</li>
<li>内核遍历 <code>[0, nfds)</code> 范围内的 fd</li>
<li>若没有任何 fd 就绪，则线程进入等待</li>
<li>某个 socket 收到数据或状态发生变化</li>
<li>内核唤醒等待线程</li>
<li>内核再次整理结果集合</li>
<li>把就绪结果复制回用户态</li>
<li><code>select</code> 返回，应用再线性检查哪个 fd 被置位</li>
</ol>
<p>这套流程里，最值得记住的是两个扫描：</p>
<ul>
<li>内核扫描关注范围</li>
<li>用户态扫描结果范围</li>
</ul>
<hr />
<h2 id="十为什么-select-在高并发下不占优势">十、为什么 select
在高并发下不占优势</h2>
<p>假设一个服务器维护 2 万连接，但某一时刻只有 50 个连接真正活跃。</p>
<p>对 <code>select</code> 而言，它通常仍然需要：</p>
<ul>
<li>根据 <code>nfds</code> 检查一大段 fd 范围</li>
<li>返回后应用再遍历这一大段范围</li>
</ul>
<p>这意味着：</p>
<blockquote>
<p>成本更接近“跟总监视范围有关”，而不是“只和活跃连接数有关”。</p>
</blockquote>
<p>因此 <code>select</code> 适合：</p>
<ul>
<li>连接数不多</li>
<li>代码可移植性要求高</li>
<li>教学或基础示例</li>
</ul>
<p>但不太适合超大规模连接场景。</p>
<hr />
<h2 id="十一使用-select-时的常见坑">十一、使用 select 时的常见坑</h2>
<h3 id="忘记每次重新初始化集合">1. 忘记每次重新初始化集合</h3>
<p>因为 <code>select</code>
会修改传入集合，所以不能把主集合直接传进去反复用。</p>
<p>正确做法通常是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">rset = allset;</span><br><span class="line">select(maxfd + <span class="number">1</span>, &amp;rset, <span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure>
<h3 id="nfds-传错">2. nfds 传错</h3>
<p>很多人会误传成“总共多少个 fd”，但正确的是“最大 fd + 1”。</p>
<h3 id="忽略监听-fd-与连接-fd-的不同语义">3. 忽略监听 fd 与连接 fd
的不同语义</h3>
<ul>
<li>监听 fd 可读：通常表示可以 <code>accept</code></li>
<li>连接 fd 可读：通常表示可以 <code>read</code></li>
</ul>
<p>这两类 fd 的处理逻辑不能混在一起。</p>
<h3 id="close-后忘记-fd_clr">4. close 后忘记 FD_CLR</h3>
<p>关闭 fd 后如果没有把它从集合里移除，下次 <code>select</code>
可能出现错误或逻辑混乱。</p>
<h3 id="误以为可读就一定能读到业务数据">5.
误以为可读就一定能读到业务数据</h3>
<p>对端关闭连接时，也会表现为“可读”，但此时 <code>read</code> 返回
<code>0</code>。</p>
<hr />
<h2 id="十二select-与-pollepoll-的关系">十二、select 与 poll、epoll
的关系</h2>
<p>从历史和设计思路上可以这样理解：</p>
<ul>
<li><code>select</code>：最早期、最经典的多路复用接口</li>
<li><code>poll</code>：改进了 <code>select</code> 的位图和 fd
数量限制问题</li>
<li><code>epoll</code>：进一步优化了关注集合维护与就绪事件提取方式</li>
</ul>
<p>一个简化对比如下：</p>
<table>
<thead>
<tr class="header">
<th>对比项</th>
<th>select</th>
<th>poll</th>
<th>epoll</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>数据结构</td>
<td>位图</td>
<td>数组</td>
<td>红黑树 + 就绪链表</td>
</tr>
<tr class="even">
<td>fd 上限</td>
<td>常受 <code>FD_SETSIZE</code> 影响</td>
<td>无明显固定上限</td>
<td>无明显固定上限</td>
</tr>
<tr class="odd">
<td>每次调用重复提交全集</td>
<td>是</td>
<td>是</td>
<td>否</td>
</tr>
<tr class="even">
<td>返回后用户态扫描</td>
<td>是</td>
<td>是</td>
<td>通常只处理就绪项</td>
</tr>
<tr class="odd">
<td>典型场景</td>
<td>小规模、跨平台</td>
<td>中等规模</td>
<td>高并发 Linux</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="十三select-在工程里适合什么场景">十三、select
在工程里适合什么场景</h2>
<p><code>select</code> 并不是“过时到不能用”，它仍然适合一些场景：</p>
<h3 id="教学和学习">1. 教学和学习</h3>
<p>如果要理解：</p>
<ul>
<li>什么是 I/O 多路复用</li>
<li>为什么要区分可读和可写</li>
<li>单线程如何管理多个连接</li>
</ul>
<p><code>select</code> 是很好的入门材料。</p>
<h3 id="连接数少的工具型程序">2. 连接数少的工具型程序</h3>
<p>例如：</p>
<ul>
<li>简单代理</li>
<li>调试工具</li>
<li>内部服务</li>
<li>小型 demo</li>
</ul>
<h3 id="对可移植性要求高的代码">3. 对可移植性要求高的代码</h3>
<p>因为 <code>select</code> 是较通用的接口，很多 Unix / POSIX
环境都支持它。</p>
<hr />
<h2 id="十四总结">十四、总结</h2>
<p><code>select</code> 的核心价值在于：</p>
<ol type="1">
<li><strong>提供了最基础的 I/O 多路复用能力</strong></li>
<li><strong>允许一个线程等待多个 fd 的就绪事件</strong></li>
<li><strong>为后续理解 <code>poll</code> 和 <code>epoll</code>
打下了基础</strong></li>
</ol>
<p>但它的底层方式决定了它存在天然瓶颈：</p>
<ul>
<li>用位图管理 fd 集合</li>
<li>每次调用都要重新提交全集</li>
<li>内核和用户态都要扫描 fd 范围</li>
</ul>
<p>所以，<code>select</code> 更适合理解思想与处理中小规模连接；
而当连接数持续增长时，就需要进一步考虑 <code>poll</code> 和
<code>epoll</code> 这类机制。</p>
]]></content>
      <categories>
        <category>操作系统与系统编程（Systems）</category>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>IO多路复用</tag>
        <tag>网络编程</tag>
      </tags>
  </entry>
  <entry>
    <title>代码审查清单</title>
    <url>/2025/07/07/programming/misc/general-code-review-checklist/</url>
    <content><![CDATA[<p>本文记录了一份通用的代码审查清单，可结合实际项目调整。</p>
<span id="more"></span>
<hr />
<p>代码审查是保障代码质量的重要环节，以下是一份通用的代码审查清单，涵盖功能、可读性、安全性、性能、可维护性等多个维度，可根据具体项目场景（如前端、后端、移动端等）灵活调整：</p>
<h3 id="一功能正确性"><strong>一、功能正确性</strong></h3>
<ol type="1">
<li>代码是否完全实现了需求文档中的功能点？是否覆盖了所有场景（包括正常流程、边界条件、异常情况）？</li>
<li>逻辑是否严谨？是否存在逻辑漏洞（如条件判断遗漏、循环边界错误等）？</li>
<li>单元测试/集成测试是否覆盖关键逻辑？测试用例是否合理（包括正向、反向、边界值测试）？</li>
<li>与其他模块/系统的交互是否正确？接口调用参数、返回值处理是否符合约定？</li>
</ol>
<h3 id="二代码可读性"><strong>二、代码可读性</strong></h3>
<ol type="1">
<li>命名是否规范？变量、函数、类、常量的命名是否清晰易懂，能否准确反映其含义（避免拼音、缩写不明确的命名）？</li>
<li>注释是否完整且必要？
<ul>
<li>复杂逻辑、算法是否有注释说明设计思路？</li>
<li>函数/类是否有文档注释（如参数含义、返回值、异常说明）？</li>
<li>是否存在冗余注释（如注释与代码完全重复）或过时注释？</li>
</ul></li>
<li>代码格式是否统一？缩进、换行、空格等是否符合团队编码规范（可通过格式化工具保障）？</li>
<li>代码结构是否清晰？是否避免了过度嵌套（如多层<code>if-else</code>、循环嵌套）？是否通过拆分函数/类降低复杂度？</li>
</ol>
<h3 id="三安全性"><strong>三、安全性</strong></h3>
<ol type="1">
<li>输入验证是否完善？是否存在注入风险（如SQL注入、XSS、命令注入）？</li>
<li>敏感数据（如密码、token）是否加密存储/传输？是否避免在日志中明文打印敏感信息？</li>
<li>权限控制是否严谨？是否校验了用户/角色的操作权限？</li>
<li>依赖是否安全？是否使用了存在已知漏洞的第三方库（可通过工具扫描确认）？</li>
<li>异常处理是否合理？是否避免将详细错误信息直接暴露给用户（如堆栈信息）？</li>
</ol>
<h3 id="四性能与效率"><strong>四、性能与效率</strong></h3>
<ol type="1">
<li>是否存在明显的性能瓶颈？
<ul>
<li>数据库查询是否优化（如是否避免全表扫描、是否合理使用索引）？</li>
<li>循环/递归是否存在不必要的重复计算？</li>
<li>大数据量处理是否考虑分批、异步等方式？</li>
</ul></li>
<li>资源使用是否合理？
<ul>
<li>是否及时释放资源（如文件句柄、数据库连接、内存）？</li>
<li>是否存在内存泄漏风险（如未销毁的定时器、全局变量无节制增长）？</li>
</ul></li>
<li>网络请求是否优化？是否避免不必要的请求（如重复请求、冗余数据传输）？</li>
</ol>
<h3 id="五可维护性"><strong>五、可维护性</strong></h3>
<ol type="1">
<li>代码是否符合DRY原则（Don’t Repeat
Yourself）？是否存在重复代码（可通过抽取公共函数/工具类优化）？</li>
<li>耦合度是否过低？模块/类之间是否存在过度依赖（如直接修改其他类的私有属性）？</li>
<li>扩展性是否良好？是否便于后续功能迭代（如通过配置、抽象接口替代硬编码）？</li>
<li>是否遵循团队编码规范/设计模式？是否与项目现有代码风格保持一致？</li>
</ol>
<h3 id="六错误处理与健壮性"><strong>六、错误处理与健壮性</strong></h3>
<ol type="1">
<li>异常处理是否全面？是否捕获了可能的异常（如网络错误、数据格式错误）并进行合理处理（如重试、降级、友好提示）？</li>
<li>是否存在空指针/未定义引用风险？对<code>null</code>/<code>undefined</code>的处理是否严谨？</li>
<li>边界条件是否考虑？如数组越界、数值溢出、字符串长度限制等。</li>
<li>日志打印是否合理？是否包含关键操作日志（便于问题排查），且日志级别（info/warn/error）使用得当？</li>
</ol>
<h3 id="七其他细节"><strong>七、其他细节</strong></h3>
<ol type="1">
<li>是否删除了调试代码（如<code>console.log</code>、断点、测试用临时变量）？</li>
<li>配置是否合理？是否区分了开发/测试/生产环境的配置？</li>
<li>代码是否兼容目标运行环境（如浏览器版本、Node.js版本、操作系统）？</li>
</ol>
<h3 id="审查建议">审查建议</h3>
<ul>
<li>结合自动化工具（如ESLint、SonarQube、PMD等）辅助检查格式、潜在bug，聚焦人工审查逻辑和设计层面。</li>
<li>审查时以“帮助开发者提升”为目标，避免过度纠结细节（如命名风格争议），优先关注功能和安全性问题。</li>
</ul>
]]></content>
      <categories>
        <category>工具与工程实践（Tooling &amp; Engineering）</category>
      </categories>
      <tags>
        <tag>代码规范</tag>
      </tags>
  </entry>
  <entry>
    <title>C语言项目开发常用目录结构</title>
    <url>/2024/06/10/programming/misc/c-project-directory-architecture/</url>
    <content><![CDATA[<p>本文记录了C语言项目开发中几种常用的目录结构。</p>
<span id="more"></span>
<hr />
<p>C
项目的目录结构没有“唯一标准”，但有几种<strong>业界常见、可扩展的组织方式</strong>。选哪种，取决于你的项目规模和复杂度。</p>
<hr />
<h2 id="最简单结构小项目-练习">最简单结构（小项目 / 练习）</h2>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">project/</span><br><span class="line">├── main.c</span><br><span class="line">├── util.c</span><br><span class="line">├── util.h</span><br><span class="line">└── Makefile</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="按模块拆分推荐入门">按“模块拆分”（推荐入门）</h2>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">project/</span><br><span class="line">├── src/</span><br><span class="line">│   ├── main.c</span><br><span class="line">│   ├── net.c</span><br><span class="line">│   ├── file.c</span><br><span class="line">│   └── log.c</span><br><span class="line">├── include/</span><br><span class="line">│   ├── net.h</span><br><span class="line">│   ├── file.h</span><br><span class="line">│   └── log.h</span><br><span class="line">├── Makefile</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="按模块分目录工程常用">按“模块分目录”（工程常用）</h2>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">project/</span><br><span class="line">├── src/</span><br><span class="line">│   ├── main.c</span><br><span class="line">│   ├── net/</span><br><span class="line">│   │   ├── server.c</span><br><span class="line">│   │   ├── client.c</span><br><span class="line">│   │   └── socket.c</span><br><span class="line">│   ├── file/</span><br><span class="line">│   │   ├── file.c</span><br><span class="line">│   │   └── mmap.c</span><br><span class="line">│   ├── log/</span><br><span class="line">│   │   └── log.c</span><br><span class="line">│   └── utils/</span><br><span class="line">│       └── utils.c</span><br><span class="line">├── include/</span><br><span class="line">│   ├── net/</span><br><span class="line">│   ├── file/</span><br><span class="line">│   ├── log/</span><br><span class="line">│   └── utils/</span><br><span class="line">├── Makefile</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="企业级结构完整工程">企业级结构（完整工程）</h2>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">project/</span><br><span class="line">├── src/          # 源码</span><br><span class="line">├── include/      # 头文件</span><br><span class="line">├── build/        # 编译中间文件</span><br><span class="line">├── bin/          # 可执行文件</span><br><span class="line">├── lib/          # 静态/动态库</span><br><span class="line">├── tests/        # 测试代码</span><br><span class="line">├── docs/         # 文档</span><br><span class="line">├── scripts/      # 脚本</span><br><span class="line">├── config/       # 配置</span><br><span class="line">├── third_party/  # 第三方库</span><br><span class="line">├── logs/         # 日志文件</span><br><span class="line">├── Makefile</span><br><span class="line">└── README.md</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>工具与工程实践（Tooling &amp; Engineering）</category>
      </categories>
      <tags>
        <tag>代码规范</tag>
      </tags>
  </entry>
  <entry>
    <title>字节序</title>
    <url>/2026/01/17/programming/unix-posix/byte-ordering/</url>
    <content><![CDATA[<p><strong>大端字节序（Big-Endian）和小端字节序（Little-Endian）</strong>
是两种不同的多字节数据存储方式，主要区别在于字节在内存中的排列顺序。它们在计算机体系结构、网络通信和文件存储中非常重要。</p>
<span id="more"></span>
<hr />
<h2 id="基本概念">基本概念</h2>
<ul>
<li><strong>字节序（Endianness）</strong>：指多字节数据（如
<code>int</code>, <code>float</code> 等）在内存中的存储顺序。</li>
<li><strong>核心问题</strong>：一个 32 位整数
<code>0x12345678</code>（十六进制），在内存中如何排列？</li>
</ul>
<hr />
<h2 id="大端字节序big-endian">大端字节序（Big-Endian）</h2>
<ul>
<li><p><strong>高字节在前，低字节在后</strong>（类似人类书写习惯）。<br />
</p></li>
<li><p><strong>示例</strong>：<code>0x12345678</code> 的存储顺序：</p>
<table>
<thead>
<tr class="header">
<th>内存地址</th>
<th>存储内容</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>0x1000</td>
<td><code>0x12</code></td>
</tr>
<tr class="even">
<td>0x1001</td>
<td><code>0x34</code></td>
</tr>
<tr class="odd">
<td>0x1002</td>
<td><code>0x56</code></td>
</tr>
<tr class="even">
<td>0x1003</td>
<td><code>0x78</code></td>
</tr>
</tbody>
</table></li>
<li><p><strong>特点</strong>：</p>
<ul>
<li>高位字节（<code>0x12</code>）存储在低地址。</li>
<li><strong>常见场景</strong>：网络协议（如 TCP/IP）、Java
虚拟机、PowerPC 处理器。</li>
</ul></li>
</ul>
<hr />
<h2 id="小端字节序little-endian">小端字节序（Little-Endian）</h2>
<ul>
<li><p><strong>低字节在前，高字节在后</strong>（与人类书写习惯相反）。<br />
</p></li>
<li><p><strong>示例</strong>：<code>0x12345678</code> 的存储顺序：</p>
<table>
<thead>
<tr class="header">
<th>内存地址</th>
<th>存储内容</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>0x1000</td>
<td><code>0x78</code></td>
</tr>
<tr class="even">
<td>0x1001</td>
<td><code>0x56</code></td>
</tr>
<tr class="odd">
<td>0x1002</td>
<td><code>0x34</code></td>
</tr>
<tr class="even">
<td>0x1003</td>
<td><code>0x12</code></td>
</tr>
</tbody>
</table></li>
<li><p><strong>特点</strong>：</p>
<ul>
<li>低位字节（<code>0x78</code>）存储在低地址。</li>
<li><strong>常见场景</strong>：x86/x64
处理器、ARM（可配置）、Windows/Linux 系统。</li>
</ul></li>
</ul>
<hr />
<h2 id="如何判断当前系统的字节序">如何判断当前系统的字节序？</h2>
<p>C 代码示例：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> num = <span class="number">0x12345678</span>;</span><br><span class="line">    <span class="type">char</span> *p = (<span class="type">char</span> *)&amp;num;</span><br><span class="line">    <span class="keyword">if</span> (*p == <span class="number">0x78</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Little-Endian\n&quot;</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Big-Endian\n&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>如果输出
<code>Little-Endian</code>，说明系统是小端；否则是大端。</li>
</ul>
<hr />
<h2 id="字节序的实际影响">字节序的实际影响</h2>
<ol type="1">
<li><strong>网络通信</strong>：
<ul>
<li>网络协议（如 IP、TCP）默认使用
<strong>大端字节序</strong>（称为“网络字节序”）。<br />
</li>
<li>发送数据前需调用 <code>htonl()</code>（Host to Network
Long）转换字节序。</li>
</ul></li>
<li><strong>文件读写</strong>：
<ul>
<li>跨平台读取二进制文件时（如 BMP 图片），需注意字节序。</li>
</ul></li>
<li><strong>跨平台开发</strong>：
<ul>
<li>不同处理器（如 x86 vs
PowerPC）可能使用不同字节序，需处理兼容性。</li>
</ul></li>
</ol>
<hr />
<h2 id="记忆技巧">记忆技巧</h2>
<ul>
<li><strong>大端</strong>：高字节在<strong>低地址</strong>（“高个子站前面”）。<br />
</li>
<li><strong>小端</strong>：低字节在<strong>低地址</strong>（“小个子站前面”）。</li>
</ul>
<hr />
<h2 id="总结">总结</h2>
<table>
<thead>
<tr class="header">
<th>特性</th>
<th>大端字节序</th>
<th>小端字节序</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>字节顺序</strong></td>
<td>高位在前（<code>0x12...</code>）</td>
<td>低位在前（<code>0x78...</code>）</td>
</tr>
<tr class="even">
<td><strong>常见架构</strong></td>
<td>PowerPC, SPARC</td>
<td>x86, ARM（通常）</td>
</tr>
<tr class="odd">
<td><strong>网络协议</strong></td>
<td>是（TCP/IP）</td>
<td>需转换</td>
</tr>
<tr class="even">
<td><strong>人类友好</strong></td>
<td>是（类似书写顺序）</td>
<td>否</td>
</tr>
</tbody>
</table>
<p>理解字节序对底层编程、网络协议和跨平台开发至关重要！</p>
]]></content>
      <categories>
        <category>操作系统与系统编程（Systems）</category>
        <category>Unix &amp; POSIX</category>
      </categories>
  </entry>
  <entry>
    <title>socket、bind、listen、connect、accept 执行过程中的用户态/内核态与结构变化</title>
    <url>/2026/01/25/programming/unix-posix/socket-user-kernel/</url>
    <content><![CDATA[<p>本文围绕
<code>socket</code>、<code>bind</code>、<code>listen</code>、<code>connect</code>、<code>accept</code>
这几个最核心的套接字函数，梳理它们执行时<strong>用户进程可见的对象</strong>与<strong>内核中对应数据结构</strong>是如何一步步建立、关联和变化的；同时也顺着这个过程说明什么是用户态、什么是内核态，以及为什么这些调用本质上都是通过系统调用进入内核完成工作的。</p>
<span id="more"></span>
<hr />
<p>很多同学在学习 socket 编程时，容易把这几个函数记成“固定模板”：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">listenfd = socket(...);</span><br><span class="line">bind(listenfd, ...);</span><br><span class="line">listen(listenfd, ...);</span><br><span class="line">connfd = accept(listenfd, ...);</span><br></pre></td></tr></table></figure>
<p>客户端则是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sockfd = socket(...);</span><br><span class="line">connect(sockfd, ...);</span><br></pre></td></tr></table></figure>
<p>从 API
的角度看，这样记当然没问题；但如果想真正理解“<strong>为什么服务端在
<code>accept</code> 之后会拿到一个新的描述符</strong>”“<strong>为什么
<code>connect</code> 之前客户端通常不必手动
<code>bind</code></strong>”“<strong>为什么 <code>listen</code>
之后套接字就变成了监听套接字</strong>”，就必须往下看一层：</p>
<ul>
<li>用户态看到的是<strong>整数文件描述符 fd</strong>；</li>
<li>内核态维护的是<strong>文件描述符表、文件对象、socket
对象、协议控制块、连接队列</strong>等结构；</li>
<li>这些函数的核心工作，就是把这些结构创建出来、连起来，并改变它们的状态。</li>
</ul>
<p>本文以 <strong>TCP/IPv4</strong> 为主线说明。不同 UNIX/Linux
内核版本在具体结构命名上会有差异，但整体思想是稳定的。</p>
<hr />
<h2
id="总图用户态和内核态分别看到什么">总图：用户态和内核态分别看到什么</h2>
<p>先给出一个总的认识：应用程序并不能直接操作网卡、协议栈和连接队列，它只能通过<strong>系统调用</strong>陷入内核，请内核代为完成。</p>
<p><strong>用户态视角</strong></p>
<p>用户进程通常只能直接接触到这些东西：</p>
<ul>
<li>变量 <code>int sockfd</code></li>
<li><code>struct sockaddr_in addr</code></li>
<li>调用 <code>socket/bind/listen/connect/accept</code></li>
<li>用 <code>read/write/send/recv</code> 读写数据</li>
</ul>
<p>也就是说，在用户态，“socket”首先表现为一个<strong>文件描述符</strong>。</p>
<p><strong>内核态视角</strong></p>
<p>内核中，和一个 socket 相关的典型对象可以粗略理解为：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">进程 task_struct</span><br><span class="line">   └─ 文件描述符表 files_struct / fdtable</span><br><span class="line">        └─ fd = 3</span><br><span class="line">            └─ struct file</span><br><span class="line">                └─ struct socket</span><br><span class="line">                    └─ struct sock / TCP 控制块</span><br><span class="line">                        ├─ 本地 IP / 本地端口</span><br><span class="line">                        ├─ 对端 IP / 对端端口</span><br><span class="line">                        ├─ 状态（CLOSED/LISTEN/SYN_SENT/ESTABLISHED ...）</span><br><span class="line">                        ├─ 收发缓冲区</span><br><span class="line">                        └─ 连接队列 / 半连接队列 / 全连接队列</span><br></pre></td></tr></table></figure>
<p>这条链路很关键：</p>
<ul>
<li><strong>fd</strong> 是用户态拿着的“句柄”；</li>
<li><strong><code>struct file</code></strong> 是 VFS
层的通用打开文件对象；</li>
<li><strong><code>struct socket</code></strong> 是套接字抽象；</li>
<li><strong><code>struct sock</code></strong> 则更靠近具体协议实现（例如
TCP）。</li>
</ul>
<p>因此，很多 socket 调用本质上就是：</p>
<ol type="1">
<li>用户态传入 fd 和参数；</li>
<li>内核根据 fd 找到 <code>struct file</code>；</li>
<li>再从 <code>struct file</code> 找到 <code>struct socket</code> /
<code>struct sock</code>；</li>
<li>修改其中的地址、状态、队列或创建新的内核对象。</li>
</ol>
<hr />
<h2 id="用户态与内核态">用户态与内核态</h2>
<p>在分析这些函数之前，先把“用户态/内核态”说清楚。</p>
<p><strong>用户态</strong></p>
<p>用户态（user mode）是普通应用程序执行的状态。它的特点是：</p>
<ul>
<li>权限受限；</li>
<li>不能直接访问硬件；</li>
<li>不能直接操作网卡、页表、调度器等核心资源；</li>
<li>访问内核对象必须通过系统调用。</li>
</ul>
<p>比如下面这句：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sockfd = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>
<p>从 C 代码看只是函数调用，但底层会触发系统调用，CPU
从用户态切到内核态。</p>
<p><strong>内核态</strong></p>
<p>内核态（kernel
mode）是操作系统内核执行的状态。它有更高权限，可以：</p>
<ul>
<li>分配和释放内核对象；</li>
<li>维护协议栈状态；</li>
<li>操作网卡与中断；</li>
<li>把网络包放入接收队列；</li>
<li>唤醒阻塞在 <code>accept</code>、<code>recv</code>
等系统调用上的进程。</li>
</ul>
<p><strong>一次系统调用发生了什么</strong></p>
<p>以 <code>bind(sockfd, ...)</code> 为例，简化过程如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">用户态进程</span><br><span class="line">   |</span><br><span class="line">   | 调用 bind()</span><br><span class="line">   v</span><br><span class="line">CPU 执行陷入指令，切换到内核态</span><br><span class="line">   |</span><br><span class="line">   | 内核检查参数、根据 fd 找 socket、修改内核对象</span><br><span class="line">   v</span><br><span class="line">返回用户态</span><br></pre></td></tr></table></figure>
<p>因此，<code>socket</code>、<code>bind</code>、<code>listen</code>、<code>connect</code>、<code>accept</code>
的共同点是：</p>
<ul>
<li><strong>接口在用户态调用</strong>；</li>
<li><strong>核心工作在内核态完成</strong>；</li>
<li><strong>返回用户态一个结果</strong>（成功/失败、fd、对端地址等）。</li>
</ul>
<hr />
<h2 id="五个函数分别做了什么">五个函数分别做了什么</h2>
<h3 id="socket"><code>socket</code></h3>
<p><strong>用户代码</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> sockfd = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>
<p>对用户进程来说，结果只是得到一个整数，例如 <code>3</code>。</p>
<p><strong>内核中发生的关键变化</strong></p>
<p>当执行 <code>socket()</code> 时，内核大致会做这些事：</p>
<ol type="1">
<li>为该进程分配一个尚未使用的文件描述符，比如 <code>fd=3</code>；</li>
<li>创建一个 <code>struct file</code> 对象；</li>
<li>创建一个 <code>struct socket</code> 对象；</li>
<li>为 TCP 分配更底层的协议控制块（可理解为
<code>struct sock</code>）；</li>
<li>把这些对象关联起来；</li>
<li>将该 fd 填入进程的 fd 表。</li>
</ol>
<p>此时套接字通常还没有：</p>
<ul>
<li>本地 IP/端口；</li>
<li>对端 IP/端口；</li>
<li>监听队列；</li>
<li>已建立连接。</li>
</ul>
<p>它只是一个<strong>被创建出来的通信端点骨架</strong>。</p>
<p><strong>结构示意</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">用户态</span><br><span class="line">+---------------------------+</span><br><span class="line">| int sockfd = 3            |</span><br><span class="line">+---------------------------+</span><br><span class="line"></span><br><span class="line">内核态</span><br><span class="line">+---------------------------+</span><br><span class="line">| 当前进程 fd 表            |</span><br><span class="line">|   3 ─────────────┐        |</span><br><span class="line">+------------------|--------+</span><br><span class="line">                   v</span><br><span class="line">            +-------------+</span><br><span class="line">            | struct file |</span><br><span class="line">            +------+------+ </span><br><span class="line">                   |</span><br><span class="line">                   v</span><br><span class="line">            +-------------+</span><br><span class="line">            | struct socket|</span><br><span class="line">            +------+------+ </span><br><span class="line">                   |</span><br><span class="line">                   v</span><br><span class="line">            +-------------+</span><br><span class="line">            | TCP sock    |</span><br><span class="line">            | state=CLOSED|</span><br><span class="line">            | local=?     |</span><br><span class="line">            | peer=?      |</span><br><span class="line">            +-------------+</span><br></pre></td></tr></table></figure>
<p><strong>这一阶段的本质</strong></p>
<p><code>socket()</code> 的本质不是“建立连接”，而是：</p>
<blockquote>
<p><strong>让用户进程获得一个指向内核 socket
对象的文件描述符入口。</strong></p>
</blockquote>
<p>所以只 <code>socket()</code> 而不 <code>connect()</code> /
<code>bind()</code> / <code>listen()</code>，并不能完成通信。</p>
<hr />
<h3 id="bind"><code>bind</code></h3>
<p><strong>用户代码</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">addr</span>;</span></span><br><span class="line"><span class="built_in">memset</span>(&amp;addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(addr));</span><br><span class="line">addr.sin_family = AF_INET;</span><br><span class="line">addr.sin_port = htons(<span class="number">8080</span>);</span><br><span class="line">addr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line"></span><br><span class="line">bind(sockfd, (<span class="keyword">struct</span> sockaddr *)&amp;addr, <span class="keyword">sizeof</span>(addr));</span><br></pre></td></tr></table></figure>
<p>在用户态，我们只是准备了一个地址结构并调用 <code>bind()</code>。</p>
<p><strong>内核中发生了什么</strong></p>
<p>内核接到 <code>bind</code> 后，大致会完成这些动作：</p>
<ol type="1">
<li>根据 <code>sockfd</code> 找到对应的 socket；</li>
<li>从用户态拷贝 <code>sockaddr_in</code> 到内核；</li>
<li>检查地址是否合法；</li>
<li>检查该端口是否已被占用，或是否允许复用；</li>
<li>把“本地 IP / 本地端口”写入 socket 对应的内核结构；</li>
<li>把该 socket 放入对应的绑定哈希表/端口管理结构中，便于后续查找。</li>
</ol>
<p><strong>这里绑定的到底是什么</strong></p>
<p><code>bind</code> 绑定的不是“进程”，而是：</p>
<blockquote>
<p><strong>某个 socket 内核对象的本地地址信息。</strong></p>
</blockquote>
<p>绑定完成后，这个 socket 才真正拥有了“我是谁”的身份，例如：</p>
<ul>
<li>本地 IP：<code>0.0.0.0</code></li>
<li>本地端口：<code>8080</code></li>
</ul>
<p><strong><code>INADDR_ANY</code> 的含义</strong></p>
<p>如果服务端绑定的是 <code>INADDR_ANY</code>，内核可以理解为：</p>
<blockquote>
<p>这个监听 socket 接收“发往本机任意本地网卡地址、端口为
8080”的连接。</p>
</blockquote>
<p><strong><code>bind</code> 前后结构变化</strong></p>
<p><code>bind</code> 之前：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">TCP sock</span><br><span class="line">  state = CLOSED</span><br><span class="line">  local_ip   = ?</span><br><span class="line">  local_port = ?</span><br><span class="line">  peer_ip    = ?</span><br><span class="line">  peer_port  = ?</span><br></pre></td></tr></table></figure>
<p><code>bind</code> 之后：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">TCP sock</span><br><span class="line">  state = CLOSED</span><br><span class="line">  local_ip   = 0.0.0.0</span><br><span class="line">  local_port = 8080</span><br><span class="line">  peer_ip    = ?</span><br><span class="line">  peer_port  = ?</span><br></pre></td></tr></table></figure>
<p><strong>为什么客户端常常不手动 <code>bind</code></strong></p>
<p>因为客户端如果不显式 <code>bind</code>，通常在 <code>connect</code>
时由内核自动分配：</p>
<ul>
<li>一个合适的本地 IP；</li>
<li>一个临时端口（ephemeral port）。</li>
</ul>
<p>所以很多客户端代码直接：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sockfd = socket(...);</span><br><span class="line">connect(sockfd, ...);</span><br></pre></td></tr></table></figure>
<p>也能正常工作。</p>
<hr />
<h3 id="listen"><code>listen</code></h3>
<p><strong>用户代码</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">listen(sockfd, <span class="number">128</span>);</span><br></pre></td></tr></table></figure>
<p>从用户态看，<code>sockfd</code> 没变，仍然是同一个 fd。</p>
<p><strong>内核中的关键变化</strong></p>
<p><code>listen</code> 不会新建一个
fd，也不会建立某个具体连接；它做的核心工作是：</p>
<ol type="1">
<li>检查该 socket 是否适合进入监听状态；</li>
<li>将 socket 状态从 <code>CLOSED</code> 初始状态转为
<code>LISTEN</code>；</li>
<li>为它建立或启用监听相关队列；</li>
<li>记录 backlog 参数，用于限制等待队列容量。</li>
</ol>
<p>对于 TCP 来说，监听 socket 需要维护至少概念上的两类队列：</p>
<ul>
<li><strong>半连接队列</strong>：收到 SYN、尚未完成三次握手的请求；</li>
<li><strong>全连接队列</strong>：三次握手已经完成、等待应用调用
<code>accept</code> 取走的连接。</li>
</ul>
<p>不同实现细节略有差异，但“监听 socket
管理连接请求队列”这个思想不变。</p>
<p><strong><code>listen</code> 前后变化图</strong></p>
<p><code>listen</code> 之前：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fd=3</span><br><span class="line">  -&gt; socket</span><br><span class="line">      -&gt; TCP sock</span><br><span class="line">           state = CLOSED</span><br><span class="line">           local = 0.0.0.0:8080</span><br><span class="line">           accept queue = 无</span><br></pre></td></tr></table></figure>
<p><code>listen</code> 之后：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fd=3</span><br><span class="line">  -&gt; socket</span><br><span class="line">      -&gt; TCP sock</span><br><span class="line">           state = LISTEN</span><br><span class="line">           local = 0.0.0.0:8080</span><br><span class="line">           syn queue / accept queue 已建立</span><br><span class="line">           backlog = 128</span><br></pre></td></tr></table></figure>
<p><strong><code>listen</code> 的本质</strong></p>
<p><code>listen()</code> 的本质可以概括为：</p>
<blockquote>
<p><strong>把一个已绑定本地地址的主动通信端点，改造成一个专门用于接收入站连接请求的监听端点。</strong></p>
</blockquote>
<p>注意这里最容易混淆的一点：</p>
<ul>
<li>监听 socket 本身并不承载具体已建立连接的数据流；</li>
<li>它主要负责“接收连接请求”和“派生已连接 socket”。</li>
</ul>
<p>这正是后面 <code>accept</code> 会返回<strong>新
fd</strong>的根本原因。</p>
<hr />
<h3 id="connect"><code>connect</code></h3>
<p>下面切到客户端视角。</p>
<p><strong>用户代码</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> sockfd = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">servaddr</span>;</span></span><br><span class="line"><span class="built_in">memset</span>(&amp;servaddr, <span class="number">0</span>, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line">servaddr.sin_family = AF_INET;</span><br><span class="line">servaddr.sin_port = htons(<span class="number">8080</span>);</span><br><span class="line">inet_pton(AF_INET, <span class="string">&quot;192.168.1.10&quot;</span>, &amp;servaddr.sin_addr);</span><br><span class="line"></span><br><span class="line">connect(sockfd, (<span class="keyword">struct</span> sockaddr *)&amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br></pre></td></tr></table></figure>
<p><strong><code>connect</code> 时内核做了什么</strong></p>
<p>对 TCP 客户端而言，<code>connect</code> 的关键步骤可以概括为：</p>
<ol type="1">
<li>根据 <code>sockfd</code> 找到客户端 socket；</li>
<li>将用户提供的服务器地址拷入内核；</li>
<li>如果该 socket 尚未绑定本地地址，则自动选择：
<ul>
<li>出接口对应的本地 IP；</li>
<li>一个临时端口；</li>
</ul></li>
<li>把对端地址写入该 socket；</li>
<li>将 TCP 状态改为 <code>SYN_SENT</code>；</li>
<li>发送 SYN 报文；</li>
<li>等待三次握手完成；</li>
<li>握手成功后，状态进入 <code>ESTABLISHED</code>，<code>connect</code>
返回。</li>
</ol>
<p><strong><code>connect</code> 前后的 socket 信息变化</strong></p>
<p><code>connect</code> 之前：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">客户端 TCP sock</span><br><span class="line">  state = CLOSED</span><br><span class="line">  local_ip   = ?</span><br><span class="line">  local_port = ?</span><br><span class="line">  peer_ip    = ?</span><br><span class="line">  peer_port  = ?</span><br></pre></td></tr></table></figure>
<p>调用 <code>connect</code> 后，发送 SYN 之前/之后：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">客户端 TCP sock</span><br><span class="line">  state = SYN_SENT</span><br><span class="line">  local_ip   = 192.168.1.20      // 自动选择或显式 bind</span><br><span class="line">  local_port = 49152             // 临时端口示例</span><br><span class="line">  peer_ip    = 192.168.1.10</span><br><span class="line">  peer_port  = 8080</span><br></pre></td></tr></table></figure>
<p>三次握手完成后：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">客户端 TCP sock</span><br><span class="line">  state = ESTABLISHED</span><br><span class="line">  local = 192.168.1.20:49152</span><br><span class="line">  peer  = 192.168.1.10:8080</span><br></pre></td></tr></table></figure>
<p><strong>从用户态/内核态角度看 <code>connect</code></strong></p>
<p>对用户来说，<code>connect</code> 像是“去连服务器”。</p>
<p>对内核来说，它实际上至少做了三件事：</p>
<ul>
<li>确定本地通信身份；</li>
<li>记录远端通信身份；</li>
<li>驱动 TCP 状态机开始握手。</li>
</ul>
<p>所以 <code>connect</code> 并不只是“写入一个地址”，而是<strong>让这个
socket 从未连接对象，进入 TCP 建连状态机</strong>。</p>
<hr />
<h3 id="服务端在-connect-到来时监听-socket-内核里发生什么">服务端在
<code>connect</code> 到来时，监听 socket 内核里发生什么</h3>
<p>这一段是理解 <code>listen + accept</code> 的关键。</p>
<p>当客户端发送 SYN 到达服务端后，服务端并不是直接让监听 socket
变成“已连接”。而是由内核基于监听 socket 衍生出与连接相关的请求对象/子
socket。</p>
<p>可以把过程粗略理解成下面这样：</p>
<p><strong>监听 socket 保持 <code>LISTEN</code></strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">监听 socket（一直存在）</span><br><span class="line">  local = 0.0.0.0:8080</span><br><span class="line">  state = LISTEN</span><br></pre></td></tr></table></figure>
<p><strong>收到 SYN 后，内核创建“连接请求项”</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">监听 socket</span><br><span class="line">  ├─ 半连接队列</span><br><span class="line">  |    └─ request_sock / 请求项</span><br><span class="line">  |         peer = 192.168.1.20:49152</span><br><span class="line">  |         state = SYN_RECV（概念上）</span><br><span class="line">  └─ 全连接队列</span><br></pre></td></tr></table></figure>
<p><strong>三次握手完成后，连接进入全连接队列</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">监听 socket</span><br><span class="line">  ├─ 半连接队列</span><br><span class="line">  └─ 全连接队列</span><br><span class="line">       └─ 已建立连接的子 socket</span><br><span class="line">            local = 192.168.1.10:8080</span><br><span class="line">            peer  = 192.168.1.20:49152</span><br><span class="line">            state = ESTABLISHED</span><br></pre></td></tr></table></figure>
<p>这里非常重要：</p>
<blockquote>
<p><strong>监听 socket 自己仍然是 LISTEN 状态，不会变成
ESTABLISHED。</strong></p>
</blockquote>
<p>它负责“招待下一批连接”；具体某个客户端的连接，放在全连接队列里的<strong>子
socket</strong>里。</p>
<hr />
<h3 id="accept"><code>accept</code></h3>
<p><strong>用户代码</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> connfd = accept(listenfd, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure>
<p>很多初学者最疑惑的就是这里：</p>
<ul>
<li><code>listenfd</code> 已经是一个 socket 了；</li>
<li>为什么 <code>accept</code> 还要返回一个新的
<code>connfd</code>？</li>
</ul>
<p>答案就在内核结构变化上。</p>
<p><strong><code>accept</code> 执行时的内核动作</strong></p>
<p>当服务端调用 <code>accept(listenfd, ...)</code> 时，内核大致会：</p>
<ol type="1">
<li>根据 <code>listenfd</code> 找到监听 socket；</li>
<li>查看其全连接队列是否有已完成握手的连接；</li>
<li>如果没有，则当前进程睡眠/阻塞，等待连接到来；</li>
<li>如果有，则从全连接队列取出一个已建立连接的子 socket；</li>
<li>为当前进程分配一个新的 fd，例如 <code>fd=4</code>；</li>
<li>创建新的 <code>struct file</code>（或建立对应文件对象关系）；</li>
<li>让这个新的 fd 指向那个“已建立连接的子 socket”；</li>
<li>将 <code>4</code> 返回给用户态，作为 <code>connfd</code>。</li>
</ol>
<p>因此：</p>
<ul>
<li><code>listenfd</code> 继续监听新的连接；</li>
<li><code>connfd</code> 专门和某一个客户端通信。</li>
</ul>
<p><strong><code>accept</code> 前后的图</strong></p>
<p><code>accept</code> 之前：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">当前进程 fd 表</span><br><span class="line">  3 -&gt; 监听 socket (LISTEN)</span><br><span class="line"></span><br><span class="line">监听 socket</span><br><span class="line">  全连接队列</span><br><span class="line">    └─ 子 socket A (ESTABLISHED, peer=192.168.1.20:49152)</span><br></pre></td></tr></table></figure>
<p><code>accept</code> 之后：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">当前进程 fd 表</span><br><span class="line">  3 -&gt; 监听 socket (LISTEN)</span><br><span class="line">  4 -&gt; 已连接 socket A (ESTABLISHED)</span><br><span class="line"></span><br><span class="line">fd=3 对应：</span><br><span class="line">  local = 0.0.0.0:8080</span><br><span class="line">  state = LISTEN</span><br><span class="line"></span><br><span class="line">fd=4 对应：</span><br><span class="line">  local = 192.168.1.10:8080</span><br><span class="line">  peer  = 192.168.1.20:49152</span><br><span class="line">  state = ESTABLISHED</span><br></pre></td></tr></table></figure>
<p><strong><code>accept</code> 的本质</strong></p>
<p><code>accept()</code> 的本质不是“让监听 socket 建立连接”，而是：</p>
<blockquote>
<p><strong>从监听 socket
的已完成连接队列中取出一个连接，并为它在当前进程中安装一个新的文件描述符。</strong></p>
</blockquote>
<p>这就是为什么服务器通常是：</p>
<ul>
<li>用 <code>listenfd</code> 做 <code>accept</code>；</li>
<li>用 <code>connfd</code> 做 <code>read/write</code>。</li>
</ul>
<hr />
<h2 id="完整生命周期图">完整生命周期图</h2>
<p>下面把服务端与客户端串在一起看。</p>
<p><strong>服务端路径</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">socket()</span><br><span class="line">  用户态：拿到 listenfd</span><br><span class="line">  内核态：创建 socket 内核对象，state=CLOSED</span><br><span class="line"></span><br><span class="line">bind()</span><br><span class="line">  用户态：传入本地地址</span><br><span class="line">  内核态：记录 local_ip/local_port</span><br><span class="line"></span><br><span class="line">listen()</span><br><span class="line">  用户态：声明愿意接收连接</span><br><span class="line">  内核态：state=LISTEN，建立监听队列</span><br><span class="line"></span><br><span class="line">accept()</span><br><span class="line">  用户态：等待新连接，得到 connfd</span><br><span class="line">  内核态：从全连接队列取出子 socket，为其分配新 fd</span><br></pre></td></tr></table></figure>
<p><strong>客户端路径</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">socket()</span><br><span class="line">  用户态：拿到 sockfd</span><br><span class="line">  内核态：创建 socket 内核对象，state=CLOSED</span><br><span class="line"></span><br><span class="line">connect()</span><br><span class="line">  用户态：传入服务器地址</span><br><span class="line">  内核态：必要时自动绑定本地地址，写入 peer 地址，发送 SYN，状态机推进到 ESTABLISHED</span><br></pre></td></tr></table></figure>
<p><strong>双方配合图</strong></p>
<pre><code class="highlight mermaid">sequenceDiagram
    participant C as 客户端进程
    participant CK as 客户端内核
    participant SK as 服务端内核
    participant S as 服务端进程

    C-&gt;&gt;CK: socket()
    CK--&gt;&gt;C: sockfd
    S-&gt;&gt;SK: socket()
    SK--&gt;&gt;S: listenfd
    S-&gt;&gt;SK: bind(listenfd, 0.0.0.0:8080)
    S-&gt;&gt;SK: listen(listenfd, backlog)

    C-&gt;&gt;CK: connect(sockfd, 192.168.1.10:8080)
    CK-&gt;&gt;CK: 自动选择本地 IP/临时端口
    CK-&gt;&gt;SK: SYN
    SK-&gt;&gt;SK: 为监听 socket 创建请求项/子 socket
    SK-&gt;&gt;CK: SYN+ACK
    CK-&gt;&gt;SK: ACK
    SK-&gt;&gt;SK: 子 socket 进入全连接队列

    S-&gt;&gt;SK: accept(listenfd)
    SK--&gt;&gt;S: connfd</code></pre>
<hr />
<h2 id="结构变化总结">结构变化总结</h2>
<p>下面用一张表把每个函数对“用户对象”和“内核对象”的影响压缩总结一下。</p>
<table>
<colgroup>
<col style="width: 33%" />
<col style="width: 33%" />
<col style="width: 33%" />
</colgroup>
<thead>
<tr class="header">
<th>函数</th>
<th>用户态看到的变化</th>
<th>内核态发生的关键变化</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>socket</code></td>
<td>获得一个新的 fd</td>
<td>创建
<code>struct file</code>、<code>struct socket</code>、协议控制块；socket
初始状态建立</td>
</tr>
<tr class="even">
<td><code>bind</code></td>
<td>fd 不变，只是调用成功</td>
<td>给 socket 写入本地 IP/端口，并登记到绑定表</td>
</tr>
<tr class="odd">
<td><code>listen</code></td>
<td>fd 不变，成为监听 fd</td>
<td>socket 状态变为 <code>LISTEN</code>，建立/启用监听相关队列</td>
</tr>
<tr class="even">
<td><code>connect</code></td>
<td>fd 不变，调用返回后可通信</td>
<td>必要时自动绑定本地地址，设置对端地址，驱动 TCP 三次握手，状态进入
<code>ESTABLISHED</code></td>
</tr>
<tr class="odd">
<td><code>accept</code></td>
<td>返回新的 fd</td>
<td>从监听 socket 的已完成队列中取出子 socket，并为其分配新的 fd</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="小结">小结</h2>
<p>如果只记 API 顺序，socket
编程会显得像“背模板”；但从用户态/内核态和内核结构变化的角度看，这几个函数的职责其实非常清晰：</p>
<ul>
<li><code>socket</code>：创建通信端点，并把它暴露成一个 fd；</li>
<li><code>bind</code>：为这个端点确定本地身份；</li>
<li><code>listen</code>：把它变成监听端点，并维护连接队列；</li>
<li><code>connect</code>：让客户端端点带着本地/远端地址进入 TCP
建连状态机；</li>
<li><code>accept</code>：从监听端点管理的已完成连接中取出一个具体连接，并返回新的
fd。</li>
</ul>
<p>其中最关键的认识有两点：</p>
<ol type="1">
<li><strong>用户态拿到的只是 fd，真正的 socket
状态和连接队列都在内核里。</strong></li>
<li><strong>服务端的监听 socket 不负责具体数据通信，真正通信的是
<code>accept</code> 返回的已连接 socket。</strong></li>
</ol>
<p>理解了这一点，再去看 <code>epoll</code>、非阻塞 I/O、TCP
状态机、连接队列溢出、<code>SO_REUSEADDR</code>
等主题，就会顺畅很多。</p>
]]></content>
      <categories>
        <category>操作系统与系统编程（Systems）</category>
        <category>Unix &amp; POSIX</category>
      </categories>
      <tags>
        <tag>网络编程</tag>
        <tag>Socket</tag>
      </tags>
  </entry>
  <entry>
    <title>使用 Visual Studio 2022 创建动态库和静态库</title>
    <url>/2024/03/11/programming/windows/vs2022-static-dynamic-lib/</url>
    <content><![CDATA[<p>本文介绍了Windows环境下使用Visual Studio
2022如何创建和使用静态库和动态库。</p>
<span id="more"></span>
<hr />
<h2 id="一些概念">一些概念</h2>
<h3 id="常用宏">常用宏</h3>
<table>
<colgroup>
<col style="width: 18%" />
<col style="width: 38%" />
<col style="width: 42%" />
</colgroup>
<thead>
<tr class="header">
<th>宏</th>
<th>含义</th>
<th>示例</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>$(SolutionDir)</code></td>
<td>当前解决方案(.sln 文件)所在的目录路径</td>
<td><code>$(SolutionDir)external\include</code></td>
</tr>
<tr class="even">
<td><code>$(ProjectDir)</code></td>
<td>当前项目文件(.vcxproj)所在目录</td>
<td><code>$(ProjectDir)libs\</code></td>
</tr>
<tr class="odd">
<td><code>$(Configuration)</code></td>
<td>当前配置名称(Debug/Release)</td>
<td><code>$(SolutionDir)output\$(Configuration)\</code></td>
</tr>
<tr class="even">
<td><code>$(Platform)</code></td>
<td>当前平台名称(Win32/x64)</td>
<td><code>lib\$(Platform)\</code></td>
</tr>
</tbody>
</table>
<p>在 VS
属性页中，这些宏会被自动展开显示实际路径，可以在项目属性的任意路径框中点击下拉箭头
-&gt; 编辑 -&gt;
点击”宏&gt;&gt;“按钮<strong>查看所有可用宏</strong>。</p>
<p><code>$(SolutionDir)</code> 是 VS 的预定义宏，表示：</p>
<ul>
<li><p>当前解决方案(.sln 文件)所在的目录路径；</p></li>
<li><p>自动包含结尾的反斜杠<code>\</code>。</p></li>
</ul>
<p>例如：如果解决方案在 <code>C:\Projects\MyApp\</code>，则
<code>$(SolutionDir)</code> 展开就是
<code>C:\Projects\MyApp\</code>，</p>
<p>完整路径 <code>$(SolutionDir)external\include</code> 表示：</p>
<p>在解决方案目录下寻找 <code>external</code> 子文件夹，然后在其中寻找
<code>include</code>
子文件夹，示例展开结果：<code>C:\Projects\MyApp\external\include\</code>。</p>
<h3 id="项目属性选项">项目属性选项</h3>
<p><strong>C/C++ &gt; 常规 &gt; 附加包含目录</strong>：</p>
<ul>
<li>作用：指定编译器在<strong>编译阶段</strong>查找头文件(.h/.hpp)的额外目录路径</li>
<li>使用场景：当你的项目使用第三方库的头文件时，如果这些头文件不在项目目录或默认包含路径中</li>
<li>示例：<code>D:\libraries\boost\include;$(SolutionDir)external\include</code></li>
</ul>
<p><strong>链接器 &gt; 常规 &gt; 附加库目录</strong>：</p>
<ul>
<li>作用：指定编译器在<strong>链接阶段</strong>查找库文件(.lib)的额外<strong>目录路径</strong></li>
<li>使用场景：当你的项目依赖第三方库时，如果这些库不在默认搜索路径中，就需要在这里添加它们的存放路径</li>
<li>示例：<code>D:\libraries\boost\lib</code></li>
</ul>
<p><strong>链接器 &gt; 输入 &gt; 附加依赖项</strong>：</p>
<ul>
<li>作用：<strong>明确指定</strong>项目链接时需要使用的<strong>库文件名</strong>(.lib)</li>
<li>使用场景：列出所有需要链接的库文件，包括第三方库和系统库</li>
<li>示例：<code>opengl32.lib;glfw3.lib;assimp.lib</code></li>
</ul>
<p><strong>它们之间的关系</strong>：</p>
<ul>
<li>编译阶段：<code>附加包含目录</code>帮助编译器找到头文件</li>
<li>链接阶段：<code>附加库目录</code>帮助链接器找到库文件位置，<code>附加依赖项</code>告诉链接器具体要链接哪些库</li>
</ul>
<p><strong>最佳实践建议</strong>：</p>
<ol type="1">
<li>使用相对路径(如<code>$(SolutionDir)</code>)而非绝对路径，方便项目迁移</li>
<li>对于大型项目，考虑使用属性表(.props)来统一管理这些设置</li>
<li>调试时如果出现”未找到头文件”错误，检查附加包含目录；出现”无法解析的外部符号”错误，检查附加库目录和附加依赖项</li>
</ol>
<h3 id="为什么这样写是好习惯">为什么这样写是好习惯</h3>
<ol type="1">
<li><strong>跨机器兼容性</strong>：无论项目被复制到什么位置都能正确找到路径</li>
<li><strong>团队协作友好</strong>：不同开发者可以有不同的本地路径，但都能正确编译</li>
<li><strong>清晰的项目结构</strong>：明确将第三方库放在
<code>external</code> 目录是常见做法</li>
</ol>
<hr />
<h2 id="静态库">静态库</h2>
<p>待续…</p>
]]></content>
      <categories>
        <category>操作系统与系统编程（Systems）</category>
        <category>Windows</category>
      </categories>
  </entry>
  <entry>
    <title>APUE 中 TELL_WAIT 五个函数方法总结</title>
    <url>/2026/01/11/programming/unix-posix/child/</url>
    <content><![CDATA[<p>本文总结 APUE 中 <code>TELL_WAIT</code>
相关的五个函数，并说明它们为什么能够作为父子进程同步的方法；同时也补充梳理几种常见的父子进程同步手段，方便横向比较。</p>
<span id="more"></span>
<hr />
<p>在 APUE 中，<code>TELL_WAIT</code>
这一组函数的目标非常明确：<strong>为父进程和子进程提供一种简单、可靠的同步机制</strong>。其典型用途是让父进程先做某件事，再通知子进程继续；或者让子进程先完成某一步，再通知父进程继续。</p>
<p>这种机制本质上解决的是：</p>
<ul>
<li>父子进程虽然有亲缘关系，但调度顺序是不确定的；</li>
<li>如果不做同步，就可能出现竞态条件（race condition）；</li>
<li>因而需要一种“你先做完、再叫我”的协调方式。</li>
</ul>
<h2 id="一tell_wait-五个函数概览">一、TELL_WAIT 五个函数概览</h2>
<p>APUE 中通常定义如下五个接口：</p>
<ul>
<li><code>TELL_WAIT(void)</code></li>
<li><code>TELL_PARENT(pid_t pid)</code></li>
<li><code>WAIT_PARENT(void)</code></li>
<li><code>TELL_CHILD(pid_t pid)</code></li>
<li><code>WAIT_CHILD(void)</code></li>
</ul>
<p>它们通常结合信号机制实现，常见做法是使用：</p>
<ul>
<li><code>SIGUSR1</code></li>
<li><code>SIGUSR2</code></li>
<li>信号屏蔽字</li>
<li><code>sigsuspend</code></li>
</ul>
<p>这样做的关键目的，是避免“信号先到、进程后等待”导致的丢失唤醒问题。</p>
<hr />
<h2 id="二这五个函数分别做什么">二、这五个函数分别做什么</h2>
<h2 id="tell_waitvoid"><code>TELL_WAIT(void)</code></h2>
<p>这是初始化函数，用来在父子进程同步开始之前完成准备工作。</p>
<p>它通常负责：</p>
<ul>
<li>为同步所用的信号安装处理函数；</li>
<li>将相关信号加入阻塞集合；</li>
<li>初始化全局标志变量；</li>
<li>为后续 <code>WAIT_PARENT</code> / <code>WAIT_CHILD</code>
的等待逻辑创造安全条件。</li>
</ul>
<h3 id="作用理解">作用理解</h3>
<p>可以把它看成“搭建同步环境”。</p>
<p>如果没有这一步，后面的等待与通知很容易因为时序问题出错。例如：</p>
<ul>
<li>子进程还没开始等待，父进程就已经发出了信号；</li>
<li>结果信号被错过，子进程可能永久阻塞。</li>
</ul>
<p>因此 <code>TELL_WAIT()</code>
的重点不是“通知”，而是<strong>预先建立不会错过通知的机制</strong>。</p>
<hr />
<h2 id="tell_parentpid_t-pid"><code>TELL_PARENT(pid_t pid)</code></h2>
<p>该函数由<strong>子进程调用</strong>，用于通知父进程：“我已经完成了当前阶段，你可以继续了。”</p>
<p>典型实现通常是向父进程发送一个约定好的信号，例如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">kill(pid, SIGUSR2);</span><br></pre></td></tr></table></figure>
<p>其中 <code>pid</code> 一般就是父进程的 PID。</p>
<h3 id="使用场景">使用场景</h3>
<p>例如：</p>
<ul>
<li>子进程先修改某个共享资源；</li>
<li>修改完成后调用 <code>TELL_PARENT(getppid())</code>；</li>
<li>父进程在 <code>WAIT_CHILD()</code>
或对应等待流程结束后再继续执行。</li>
</ul>
<h3 id="本质">本质</h3>
<p>它扮演的是“<strong>子 -&gt; 父 的完成通知</strong>”角色。</p>
<hr />
<h2 id="wait_parentvoid"><code>WAIT_PARENT(void)</code></h2>
<p>该函数由<strong>子进程调用</strong>，表示：“我要先停下来，等父进程通知我以后再继续。”</p>
<p>它一般会：</p>
<ul>
<li>检查同步标志是否已被置位；</li>
<li>若没有，则通过 <code>sigsuspend()</code>
临时切换信号掩码并挂起；</li>
<li>收到父进程发来的同步信号后恢复执行。</li>
</ul>
<h3 id="为什么不用简单的-pause">为什么不用简单的
<code>pause()</code></h3>
<p>因为 <code>pause()</code> 容易产生竞态问题：</p>
<ul>
<li>如果信号在调用 <code>pause()</code> 之前已经送达，</li>
<li>那么后续 <code>pause()</code> 可能就会一直阻塞。</li>
</ul>
<p>而 APUE 的写法通常通过“<strong>先阻塞信号，再用
<code>sigsuspend()</code>
原子地解除阻塞并等待</strong>”来避免这个问题。</p>
<h3 id="本质-1">本质</h3>
<p>它是“<strong>子等父</strong>”的等待入口。</p>
<hr />
<h2 id="tell_childpid_t-pid"><code>TELL_CHILD(pid_t pid)</code></h2>
<p>该函数由<strong>父进程调用</strong>，用于通知子进程：“我这边已经完成了，你可以继续执行了。”</p>
<p>典型实现通常是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">kill(pid, SIGUSR1);</span><br></pre></td></tr></table></figure>
<p>这里的 <code>pid</code> 一般是 <code>fork()</code> 返回得到的子进程
PID。</p>
<h3 id="使用场景-1">使用场景</h3>
<p>例如：</p>
<ul>
<li>父进程先完成初始化；</li>
<li>完成后调用 <code>TELL_CHILD(child_pid)</code>；</li>
<li>子进程原本在 <code>WAIT_PARENT()</code>
中阻塞，收到信号后继续运行。</li>
</ul>
<h3 id="本质-2">本质</h3>
<p>它扮演的是“<strong>父 -&gt; 子 的完成通知</strong>”角色。</p>
<hr />
<h2 id="wait_childvoid"><code>WAIT_CHILD(void)</code></h2>
<p>该函数由<strong>父进程调用</strong>，表示：“我要先等待，直到子进程通知我再继续。”</p>
<p>它的实现思想与 <code>WAIT_PARENT()</code> 类似，也是通过：</p>
<ul>
<li>全局标志位；</li>
<li>信号阻塞；</li>
<li><code>sigsuspend()</code>；</li>
</ul>
<p>来可靠地等待子进程的通知。</p>
<h3 id="典型用途">典型用途</h3>
<p>例如：</p>
<ul>
<li>父进程希望等子进程先写完一段数据；</li>
<li>于是父进程调用 <code>WAIT_CHILD()</code>；</li>
<li>子进程完成后调用 <code>TELL_PARENT()</code>；</li>
<li>父进程被唤醒并继续执行。</li>
</ul>
<h3 id="本质-3">本质</h3>
<p>它是“<strong>父等子</strong>”的等待入口。</p>
<hr />
<h2 id="三五个函数之间的配合关系">三、五个函数之间的配合关系</h2>
<p>这五个函数并不是孤立的，而是两两配合使用：</p>
<table>
<colgroup>
<col style="width: 25%" />
<col style="width: 25%" />
<col style="width: 25%" />
<col style="width: 25%" />
</colgroup>
<thead>
<tr class="header">
<th>方向</th>
<th>通知函数</th>
<th>等待函数</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>父通知子</td>
<td><code>TELL_CHILD(pid)</code></td>
<td><code>WAIT_PARENT()</code></td>
<td>子进程等待父进程发令</td>
</tr>
<tr class="even">
<td>子通知父</td>
<td><code>TELL_PARENT(pid)</code></td>
<td><code>WAIT_CHILD()</code></td>
<td>父进程等待子进程发令</td>
</tr>
</tbody>
</table>
<p>而 <code>TELL_WAIT()</code> 则是整个同步机制的初始化入口。</p>
<p>可以把它们理解成一套小型协议：</p>
<ol type="1">
<li>先 <code>TELL_WAIT()</code>，把通信规则搭起来；</li>
<li>谁需要等，就调用对应的 <code>WAIT_*()</code>；</li>
<li>谁完成了，就调用对应的 <code>TELL_*()</code> 发通知。</li>
</ol>
<hr />
<h2
id="四为什么说这是父子进程同步的方法">四、为什么说这是父子进程同步的方法</h2>
<p>因为这组函数专门用于协调<strong>父进程与 <code>fork()</code>
出来的子进程</strong>之间的执行先后关系。</p>
<p>“同步”在这里并不是指数据完全共享，而是指：</p>
<ul>
<li>某个进程先执行到某一步；</li>
<li>另一个进程必须等它完成后才能继续；</li>
<li>这种“顺序约束”正是同步的核心。</li>
</ul>
<p>例如下面这种逻辑：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">TELL_WAIT();</span><br><span class="line">pid = fork();</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (pid == <span class="number">0</span>) &#123;</span><br><span class="line">    WAIT_PARENT();</span><br><span class="line">    <span class="comment">/* 子进程继续执行 */</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">/* 父进程先做一些事 */</span></span><br><span class="line">    TELL_CHILD(pid);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里就明确规定了：</p>
<ul>
<li>父进程先执行某一步；</li>
<li>子进程必须等到父进程通知后才能继续。</li>
</ul>
<p>因此，<code>TELL_WAIT</code>
这一组函数就是一种<strong>基于信号的父子进程同步方法</strong>。</p>
<hr />
<h2 id="五这种方法的特点">五、这种方法的特点</h2>
<h2 id="优点">优点</h2>
<ul>
<li><strong>轻量</strong>：不需要额外创建复杂 IPC 对象；</li>
<li><strong>适合父子关系</strong>：<code>fork()</code> 后天然就知道对方
PID；</li>
<li><strong>可表达顺序控制</strong>：尤其适合演示和处理简单同步场景；</li>
<li><strong>APUE
风格典型</strong>：很好地展示了如何安全地用信号避免竞态。</li>
</ul>
<h2 id="局限">局限</h2>
<ul>
<li><strong>只适合较简单的同步</strong>，不适合复杂状态机；</li>
<li><strong>信号只适合表达事件，不适合携带大量数据</strong>；</li>
<li>编写不当容易出现竞态、信号丢失、可重入等问题；</li>
<li>可读性和可维护性通常不如更结构化的 IPC 机制。</li>
</ul>
<p>因此在工程实践里，<code>TELL_WAIT</code>
更多是一个经典教学模型，帮助理解“如何安全地使用信号完成同步”。</p>
<hr />
<h2
id="六其他父子进程同步的方法总结">六、其他父子进程同步的方法总结</h2>
<p>除了 APUE 里的 <code>TELL_WAIT</code>
方案，父子进程还可以通过很多方式同步。下面按常见程度做一个总结。</p>
<h2 id="wait-waitpid">1. <code>wait()</code> /
<code>waitpid()</code></h2>
<p>这是最基础、最常见的父子同步方式。</p>
<h3 id="作用">作用</h3>
<p>父进程通过 <code>wait()</code> 或 <code>waitpid()</code>：</p>
<ul>
<li>等待子进程结束；</li>
<li>回收子进程资源；</li>
<li>获取子进程退出状态。</li>
</ul>
<h3 id="特点">特点</h3>
<ul>
<li>非常适合“<strong>父等子结束</strong>”这一类同步；</li>
<li>语义清晰；</li>
<li>是处理僵尸进程的标准方式。</li>
</ul>
<h3 id="局限-1">局限</h3>
<ul>
<li>它只能表达“等子进程终止”，</li>
<li>不适合“子进程做到一半通知父进程继续”这种更细粒度的同步。</li>
</ul>
<p>所以，<code>wait()/waitpid()</code>
更像是<strong>生命周期终点同步</strong>。</p>
<hr />
<h2 id="信号">2. 信号</h2>
<p><code>TELL_WAIT</code> 本身就是信号同步的一个精心封装版本。</p>
<p>父子进程还可以直接使用：</p>
<ul>
<li><code>SIGUSR1</code></li>
<li><code>SIGUSR2</code></li>
<li><code>SIGCHLD</code></li>
</ul>
<p>来做事件通知。</p>
<h3 id="特点-1">特点</h3>
<ul>
<li>响应快；</li>
<li>实现简单；</li>
<li>适合“一次事件通知”。</li>
</ul>
<h3 id="局限-2">局限</h3>
<ul>
<li>需要认真处理屏蔽、竞态和中断；</li>
<li>不适合传输复杂数据；</li>
<li>稍不注意就容易写出脆弱代码。</li>
</ul>
<hr />
<h2 id="管道pipe">3. 管道（pipe）</h2>
<p>管道是父子进程之间极常见的同步与通信方式。</p>
<h3 id="作用-1">作用</h3>
<p>父进程和子进程共享同一个管道端点关系：</p>
<ul>
<li>一方写；</li>
<li>另一方读；</li>
<li>当读操作阻塞时，就形成了同步效果。</li>
</ul>
<h3 id="同步方式">同步方式</h3>
<p>例如：</p>
<ul>
<li>父进程先阻塞读；</li>
<li>子进程写入一个字节作为“完成信号”；</li>
<li>父进程读到后继续执行。</li>
</ul>
<h3 id="特点-2">特点</h3>
<ul>
<li>既能同步，又能顺便传递少量数据；</li>
<li>语义直观；</li>
<li>非常适合父子进程。</li>
</ul>
<h3 id="局限-3">局限</h3>
<ul>
<li>需要管理读写端关闭；</li>
<li>稍复杂一些；</li>
<li>主要适合有亲缘关系的进程。</li>
</ul>
<hr />
<h2 id="fifo命名管道">4. FIFO（命名管道）</h2>
<p>FIFO 是带名字的管道。</p>
<h3 id="特点-3">特点</h3>
<ul>
<li>不要求必须由同一个父进程 <code>fork()</code> 出来；</li>
<li>父子进程当然也可以使用；</li>
<li>可以借助打开、读写阻塞特性实现同步。</li>
</ul>
<h3 id="适用场景">适用场景</h3>
<p>如果你希望同步机制不仅限于当前父子进程，还能扩展到其他无亲缘关系进程，FIFO
比匿名管道更灵活。</p>
<hr />
<h2 id="system-v-信号量-posix-信号量">5. System V 信号量 / POSIX
信号量</h2>
<p>信号量是更通用、更正式的同步手段。</p>
<h3 id="作用-2">作用</h3>
<p>通过 <code>P/V</code>
操作（或等待/投递操作）控制临界资源访问和执行顺序。</p>
<h3 id="特点-4">特点</h3>
<ul>
<li>非常适合严格同步和互斥；</li>
<li>能表达计数资源；</li>
<li>比简单信号更结构化。</li>
</ul>
<h3 id="局限-4">局限</h3>
<ul>
<li>接口相对复杂；</li>
<li>对于只做一次父子通知的场景，显得偏重。</li>
</ul>
<p>如果同步逻辑比较复杂，比如多进程竞争多个资源，信号量通常比
<code>TELL_WAIT</code> 更合适。</p>
<hr />
<h2 id="共享内存-同步原语">6. 共享内存 + 同步原语</h2>
<p>共享内存本身更偏向“通信”，不是天然同步工具；但配合：</p>
<ul>
<li>信号量；</li>
<li>互斥锁；</li>
<li>条件变量；</li>
<li>futex；</li>
</ul>
<p>就可以实现高效同步。</p>
<h3 id="特点-5">特点</h3>
<ul>
<li>数据交换效率高；</li>
<li>适合高性能、多数据量场景；</li>
<li>工程上很常见。</li>
</ul>
<h3 id="局限-5">局限</h3>
<ul>
<li>设计和调试难度较高；</li>
<li>必须额外引入同步机制，否则容易出现数据竞争。</li>
</ul>
<hr />
<h2 id="socketpair">7. socketpair</h2>
<p><code>socketpair()</code> 可以创建一对相互连通的本地套接字。</p>
<h3 id="特点-6">特点</h3>
<ul>
<li>父子进程之间使用方便；</li>
<li>支持双向通信；</li>
<li>既能传数据，也能通过阻塞读写实现同步。</li>
</ul>
<h3 id="适用场景-1">适用场景</h3>
<p>如果你既需要同步，又需要双向消息交互，<code>socketpair()</code>
往往比单向管道更灵活。</p>
<hr />
<h2 id="文件锁-记录锁">8. 文件锁 / 记录锁</h2>
<p>父子进程也可以借助文件锁来同步，例如：</p>
<ul>
<li><code>fcntl</code> 记录锁；</li>
<li><code>flock</code> 文件锁。</li>
</ul>
<h3 id="特点-7">特点</h3>
<ul>
<li>适合围绕某个共享文件进行协调；</li>
<li>在某些守护进程、单实例控制场景里很有用。</li>
</ul>
<h3 id="局限-6">局限</h3>
<ul>
<li>不如信号量和管道直观；</li>
<li>更适合资源访问控制，而不是简单的“你先我后”通知。</li>
</ul>
<hr />
<h2 id="七几种方法的简要比较">七、几种方法的简要比较</h2>
<table>
<thead>
<tr class="header">
<th>方法</th>
<th>是否适合父子进程</th>
<th>是否能传数据</th>
<th>适合的同步粒度</th>
<th>复杂度</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>TELL_WAIT</code>（信号）</td>
<td>是</td>
<td>否/极弱</td>
<td>事件级</td>
<td>低到中</td>
</tr>
<tr class="even">
<td><code>wait()/waitpid()</code></td>
<td>是</td>
<td>否</td>
<td>子进程结束级</td>
<td>低</td>
</tr>
<tr class="odd">
<td>管道 <code>pipe</code></td>
<td>是</td>
<td>是</td>
<td>事件级/数据级</td>
<td>中</td>
</tr>
<tr class="even">
<td>FIFO</td>
<td>是</td>
<td>是</td>
<td>事件级/数据级</td>
<td>中</td>
</tr>
<tr class="odd">
<td>信号量</td>
<td>是</td>
<td>否</td>
<td>严格同步/互斥</td>
<td>中到高</td>
</tr>
<tr class="even">
<td>共享内存 + 锁</td>
<td>是</td>
<td>是</td>
<td>高性能复杂同步</td>
<td>高</td>
</tr>
<tr class="odd">
<td><code>socketpair</code></td>
<td>是</td>
<td>是</td>
<td>双向事件/数据级</td>
<td>中</td>
</tr>
<tr class="even">
<td>文件锁</td>
<td>可用</td>
<td>否</td>
<td>资源访问级</td>
<td>中</td>
</tr>
</tbody>
</table>
<p>如果只是在 APUE
语境下理解“父子进程同步”，可以先记住下面这条主线：</p>
<ul>
<li><strong>等子进程结束</strong>：优先想到
<code>wait()/waitpid()</code>；</li>
<li><strong>做轻量级顺序通知</strong>：优先想到 <code>TELL_WAIT</code>
这种信号方案；</li>
<li><strong>既要同步又要传数据</strong>：优先想到 <code>pipe</code> 或
<code>socketpair</code>；</li>
<li><strong>同步关系更复杂</strong>：优先想到信号量或共享内存配合同步原语。</li>
</ul>
<hr />
<h2 id="八小结">八、小结</h2>
<p><code>TELL_WAIT</code>
这一组五个函数，本质上是一套基于信号的父子进程同步接口：</p>
<ul>
<li><code>TELL_WAIT()</code>：初始化同步环境；</li>
<li><code>TELL_PARENT()</code>：子通知父；</li>
<li><code>WAIT_PARENT()</code>：子等父；</li>
<li><code>TELL_CHILD()</code>：父通知子；</li>
<li><code>WAIT_CHILD()</code>：父等子。</li>
</ul>
<p>它们解决的是父子进程执行顺序不确定带来的竞态问题，是 APUE
中非常经典的一种同步设计。</p>
<p>从更大的视角看，父子进程同步并不只有这一种方法。<code>wait()/waitpid()</code>、信号、管道、FIFO、信号量、共享内存、<code>socketpair</code>、文件锁等都可以承担同步职责。不同方法适合不同粒度和复杂度的场景，而
<code>TELL_WAIT</code>
的价值在于：<strong>它用一个简洁而经典的模型，展示了如何安全地利用信号完成父子进程同步。</strong></p>
]]></content>
      <categories>
        <category>操作系统与系统编程（Systems）</category>
        <category>Unix &amp; POSIX</category>
      </categories>
      <tags>
        <tag>进程与线程</tag>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>UNIX-like 文件系统层次结构标准</title>
    <url>/2025/01/03/programming/unix-posix/unix-like-filesystem-hierarchy-standard-overview/</url>
    <content><![CDATA[<p>本文总结了 UNIX-like 操作系统下的文件系统目录的层次结构。</p>
<span id="more"></span>
<hr />
<p>详细标准参考 <span class="exturl" data-url="aHR0cHM6Ly9yZWZzcGVjcy5saW51eGZvdW5kYXRpb24ub3JnL2Zocy5zaHRtbA==">Filesystem
Hierarchy Standard (FHS)<i class="fa fa-external-link-alt"></i></span>。</p>
<h2 id="根文件系统">根文件系统<code>/</code></h2>
<p>在根目录（<code>/</code>）中需要以下目录或指向目录的符号链接。</p>
<table>
<colgroup>
<col style="width: 15%" />
<col style="width: 84%" />
</colgroup>
<thead>
<tr class="header">
<th>Directory</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>bin</code></td>
<td>Essential command binaries, where many GNU user-level utilities are
stored</td>
</tr>
<tr class="even">
<td><code>boot</code></td>
<td>Static files of the boot loader</td>
</tr>
<tr class="odd">
<td><code>dev</code></td>
<td>Device files</td>
</tr>
<tr class="even">
<td><code>etc</code></td>
<td>Host-specific system configuration</td>
</tr>
<tr class="odd">
<td><code>lib</code></td>
<td>Essential shared libraries and kernel modules</td>
</tr>
<tr class="even">
<td><code>media</code></td>
<td>Mount point for removable media</td>
</tr>
<tr class="odd">
<td><code>mnt</code></td>
<td>Mount point for mounting a filesystem temporarily</td>
</tr>
<tr class="even">
<td><code>opt</code></td>
<td>Add-on application software packages</td>
</tr>
<tr class="odd">
<td><code>run</code></td>
<td>Data relevant to running processes</td>
</tr>
<tr class="even">
<td><code>sbin</code></td>
<td>Essential system binaries, where many GNU admin-level utilities are
stored</td>
</tr>
<tr class="odd">
<td><code>srv</code></td>
<td>Data for services provided by this system</td>
</tr>
<tr class="even">
<td><code>tmp</code></td>
<td>Temporary files</td>
</tr>
<tr class="odd">
<td><code>usr</code></td>
<td>User directory, a secondary directory hierarchy</td>
</tr>
<tr class="even">
<td><code>var</code></td>
<td>Variable directory, for files that change frequently, such as log
files</td>
</tr>
<tr class="odd">
<td><code>home</code></td>
<td>User home directories (optional)</td>
</tr>
<tr class="even">
<td><code>lib&lt;qual&gt;</code></td>
<td>Alternate format essential shared libraries (optional)</td>
</tr>
<tr class="odd">
<td><code>root</code></td>
<td>Home directory for the root user (optional)</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="usr-目录结构"><code>/usr</code> 目录结构</h2>
<p>在<code>/usr</code>中需要以下目录或指向目录的符号链接。</p>
<table>
<thead>
<tr class="header">
<th>Directory</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>bin</code></td>
<td>Most user commands</td>
</tr>
<tr class="even">
<td><code>lib</code></td>
<td>Libraries</td>
</tr>
<tr class="odd">
<td><code>local</code></td>
<td>Local hierarchy (empty after main installation)</td>
</tr>
<tr class="even">
<td><code>sbin</code></td>
<td>Non-vital system binaries</td>
</tr>
<tr class="odd">
<td><code>share</code></td>
<td>Architecture-independent data</td>
</tr>
<tr class="even">
<td><code>games</code></td>
<td>Games and educational binaries (optional)</td>
</tr>
<tr class="odd">
<td><code>include</code></td>
<td>Header files included by C programs</td>
</tr>
<tr class="even">
<td><code>libexec</code></td>
<td>Binaries run by other programs (optional)</td>
</tr>
<tr class="odd">
<td><code>lib&lt;qual&gt;</code></td>
<td>Alternate Format Libraries (optional)</td>
</tr>
<tr class="even">
<td><code>src</code></td>
<td>Source code (optional)</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="var-目录结构"><code>/var</code> 目录结构</h2>
<p>在<code>/var</code>中需要以下目录或指向目录的符号链接。</p>
<table>
<colgroup>
<col style="width: 13%" />
<col style="width: 86%" />
</colgroup>
<thead>
<tr class="header">
<th>Directory</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>cache</code></td>
<td>Application cache data</td>
</tr>
<tr class="even">
<td><code>lib</code></td>
<td>Variable state information</td>
</tr>
<tr class="odd">
<td><code>local</code></td>
<td>Variable data for <code>/usr/local</code></td>
</tr>
<tr class="even">
<td><code>lock</code></td>
<td>Lock files</td>
</tr>
<tr class="odd">
<td><code>log</code></td>
<td>Log files and directories</td>
</tr>
<tr class="even">
<td><code>opt</code></td>
<td>Variable data for <code>/opt</code></td>
</tr>
<tr class="odd">
<td><code>run</code></td>
<td>Data relevant to running processes</td>
</tr>
<tr class="even">
<td><code>spool</code></td>
<td>Application spool data</td>
</tr>
<tr class="odd">
<td><code>tmp</code></td>
<td>Temporary files preserved between system reboots</td>
</tr>
<tr class="even">
<td><code>account</code></td>
<td>Process accounting logs (optional)</td>
</tr>
<tr class="odd">
<td><code>crash</code></td>
<td>System crash dumps (optional)</td>
</tr>
<tr class="even">
<td><code>games</code></td>
<td>Variable game data (optional)</td>
</tr>
<tr class="odd">
<td><code>mail</code></td>
<td>User mailbox files (optional)</td>
</tr>
<tr class="even">
<td><code>yp</code></td>
<td>Network Information Service (NIS) database files (optional)</td>
</tr>
</tbody>
</table>
]]></content>
      <categories>
        <category>操作系统与系统编程（Systems）</category>
        <category>Unix &amp; POSIX</category>
      </categories>
      <tags>
        <tag>文件系统</tag>
      </tags>
  </entry>
  <entry>
    <title>git与远程仓库配置和使用指南</title>
    <url>/2025/04/11/tools/git/git-github-usage/</url>
    <content><![CDATA[<p>本文介绍了Git与Github或其他远程仓库的常规配置和使用方法。</p>
<span id="more"></span>
<hr />
<h2 id="配置远程仓库">配置远程仓库</h2>
<h3 id="生成-ssh-key">生成 SSH Key</h3>
<p>以Github仓库为例，其他远程仓库配置类似。</p>
<p>由于本地 Git 仓库和 GitHub
仓库之间的<strong>传输是通过SSH加密的</strong>，所以需要配置验证信息：</p>
<p>使用以下命令生成 SSH Key：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t ed25519 -C <span class="string">&quot;your_email@example.com&quot;</span>  <span class="comment"># 推荐（Ed25519 算法，更安全）</span></span><br><span class="line"><span class="comment"># 或</span></span><br><span class="line">ssh-keygen -t rsa -b 4096 -C <span class="string">&quot;your_email@example.com&quot;</span>  <span class="comment"># RSA 4096（兼容性更好）</span></span><br></pre></td></tr></table></figure>
<ul>
<li><code>-t</code>：指定密钥类型（<code>ed25519</code> 或
<code>rsa</code>）<br />
</li>
<li><code>-b</code>：密钥位数（RSA 建议 4096）<br />
</li>
<li><code>-C</code>：注释（通常用你的邮箱）</li>
</ul>
<p>之后会要求确认路径和输入密码，使用默认的一路回车就行。</p>
<p>成功的话会在 <code>~/</code> 下生成 <code>.ssh</code>
文件夹，进去，打开 <code>id_rsa.pub</code>，复制里面的全部内容。</p>
<p>回到 github 上，进入 Account =&gt; Settings（账户配置）。左边选择
<strong>SSH and GPG keys</strong>，然后点击 <strong>New SSH key</strong>
按钮,title 设置标题，粘贴<code>id_rsa.pub</code>里面的全部内容。</p>
<p>为了验证是否成功，输入以下命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh -T git@github.com</span><br><span class="line">The authenticity of host <span class="string">&#x27;github.com (52.74.223.119)&#x27;</span> can<span class="string">&#x27;t be established.</span></span><br><span class="line"><span class="string">RSA key fingerprint is SHA256:nThbg6kXUpJWGl7E1IGOCspRomTxdCARLviKw6E5SY8.</span></span><br><span class="line"><span class="string">Are you sure you want to continue connecting (yes/no/[fingerprint])? yes                   # 输入 yes</span></span><br><span class="line"><span class="string">Warning: Permanently added &#x27;</span>github.com,52.74.223.119<span class="string">&#x27; (RSA) to the list of known hosts.</span></span><br><span class="line"><span class="string">Hi XXXX! You&#x27;</span>ve successfully authenticated, but GitHub does not provide shell access. <span class="comment"># 成功信息</span></span><br></pre></td></tr></table></figure>
<p>以上命令说明已成功连上 Github。</p>
<hr />
<h3 id="为不同远程仓库生成不同的-ssh-key">为不同远程仓库生成不同的 SSH
Key</h3>
<h4 id="github">GitHub</h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t ed25519 -C <span class="string">&quot;github@example.com&quot;</span> -f ~/.ssh/github_ed25519</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>文件名</strong>：<code>~/.ssh/github_ed25519</code>（私钥）和
<code>~/.ssh/github_ed25519.pub</code>（公钥）<br />
</li>
<li><strong>添加公钥</strong>：复制 <code>github_ed25519.pub</code>
内容到 GitHub → <code>Settings</code> →
<code>SSH and GPG keys</code></li>
</ul>
<h4 id="gitlab">GitLab</h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa -b 4096 -C <span class="string">&quot;gitlab@example.com&quot;</span> -f ~/.ssh/gitlab_rsa</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>文件名</strong>：<code>~/.ssh/gitlab_rsa</code>（私钥）和
<code>~/.ssh/gitlab_rsa.pub</code>（公钥）<br />
</li>
<li><strong>添加公钥</strong>：复制 <code>gitlab_rsa.pub</code> 内容到
GitLab → <code>Preferences</code> → <code>SSH Keys</code></li>
</ul>
<h4 id="gitee码云">Gitee（码云）</h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t ed25519 -C <span class="string">&quot;gitee@example.com&quot;</span> -f ~/.ssh/gitee_ed25519</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>文件名</strong>：<code>~/.ssh/gitee_ed25519</code>（私钥）和
<code>~/.ssh/gitee_ed25519.pub</code>（公钥）<br />
</li>
<li><strong>添加公钥</strong>：复制 <code>gitee_ed25519.pub</code>
内容到 Gitee → <code>设置</code> → <code>SSH 公钥</code></li>
</ul>
<hr />
<h3 id="配置-.sshconfig-管理多个-ssh-key">配置
<code>~/.ssh/config</code> 管理多个 SSH Key</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim ~/.ssh/config  <span class="comment"># 编辑 SSH 配置文件</span></span><br></pre></td></tr></table></figure>
<p>添加如下内容：</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="comment"># GitHub</span></span><br><span class="line">Host github.com</span><br><span class="line">    HostName github.com</span><br><span class="line">    User git</span><br><span class="line">    IdentityFile ~/.ssh/github_ed25519</span><br><span class="line"></span><br><span class="line"><span class="comment"># GitLab</span></span><br><span class="line">Host gitlab.com</span><br><span class="line">    HostName gitlab.com</span><br><span class="line">    User git</span><br><span class="line">    IdentityFile ~/.ssh/gitlab_rsa</span><br><span class="line"></span><br><span class="line"><span class="comment"># Gitee</span></span><br><span class="line">Host gitee.com</span><br><span class="line">    HostName gitee.com</span><br><span class="line">    User git</span><br><span class="line">    IdentityFile ~/.ssh/gitee_ed25519</span><br></pre></td></tr></table></figure>
<p>配置<code>~/.ssh/config</code> 文件的<strong>作用是告诉 SSH
客户端针对不同的远程仓库使用不同的密钥</strong>，避免冲突并自动选择正确的密钥进行认证。</p>
<p>保存后运行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">chmod</span> 600 ~/.ssh/config  <span class="comment"># 设置权限</span></span><br></pre></td></tr></table></figure>
<p>测试 SSH 连接</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh -T git@github.com      <span class="comment"># 测试 GitHub</span></span><br><span class="line">ssh -T git@gitlab.com      <span class="comment"># 测试 GitLab</span></span><br><span class="line">ssh -T git@gitee.com       <span class="comment"># 测试 Gitee</span></span><br></pre></td></tr></table></figure>
<p>如果看到
<code>"Hi username! You've successfully authenticated."</code>
表示配置成功。</p>
<hr />
<h2 id="本地仓库与远程仓库连接">本地仓库与远程仓库连接</h2>
<p>有两种方式可以使本地与远端连接。</p>
<p><strong>第一种方式</strong>，首先在Github上新建仓库，然后将仓库克隆到本地，再将内容移入本地仓库，然后再同步至远端。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> git@github.com:your-username/your-projects.git</span><br><span class="line"><span class="built_in">mv</span> local-file your-projects/</span><br><span class="line">git add *</span><br><span class="line">git commit -m <span class="string">&quot;first commit&quot;</span></span><br><span class="line"></span><br><span class="line">git remote add origin git@github.com:your-username/your-projects.git <span class="comment"># 将本地仓库关联到远程仓库</span></span><br><span class="line">$ git push -u origin master <span class="comment"># 将本地master分支推送至远端的master分支</span></span><br></pre></td></tr></table></figure>
<p><strong>第二种方式</strong>，首先在Github上新建仓库，然后在本地创建同名仓库目录，在该目录中添加文件，初始化仓库，然后再同步至远端。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> your-projects</span><br><span class="line"><span class="built_in">cd</span> your-projects</span><br><span class="line">... <span class="comment"># 增加文件等操作</span></span><br><span class="line"></span><br><span class="line">git init</span><br><span class="line">git add *</span><br><span class="line">git commit -m <span class="string">&quot;first commit&quot;</span></span><br><span class="line"></span><br><span class="line">git remote add origin git@github.com:your-username/your-projects.git <span class="comment"># 将本地仓库关联到远程仓库</span></span><br><span class="line">$ git push -u origin master <span class="comment"># 将本地master分支推送至远端的master分支</span></span><br></pre></td></tr></table></figure>
<p>更多具体git命令参考<a
href="https://invinc-z.com/version-control-system/git-common-command-reference/">博文</a>的总结。</p>
<hr />
<h2 id="参考博文">参考博文</h2>
<ol type="1">
<li><span class="exturl" data-url="aHR0cHM6Ly93d3cucnVub29iLmNvbS9naXQvZ2l0LXJlbW90ZS1yZXBvLmh0bWw=">Git
远程仓库(Github)<i class="fa fa-external-link-alt"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly93d3cucnVub29iLmNvbS93M2Nub3RlL2dpdC1ndWlkZS5odG1s">Github
简明教程<i class="fa fa-external-link-alt"></i></span></li>
</ol>
]]></content>
      <categories>
        <category>工具与工程实践（Tooling &amp; Engineering）</category>
        <category>版本控制</category>
      </categories>
      <tags>
        <tag>Git</tag>
        <tag>Github</tag>
      </tags>
  </entry>
  <entry>
    <title>安装Ubuntu后常用设置</title>
    <url>/2024/04/17/tools/misc/common-configurations-after-installing-ubuntu/</url>
    <content><![CDATA[<p>本文总结了在安装好Linux环境后，需要安装和配置的一些用于开发的常用命令。</p>
<span id="more"></span>
<hr />
<h2 id="安装ubuntu后常用设置">安装Ubuntu后常用设置</h2>
<p>Ubuntu安装好之后，终端安装：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ubuntu先测试是否联网 </span></span><br><span class="line">ping www.baidu.com</span><br><span class="line"></span><br><span class="line"><span class="comment"># 更新软件包索引</span></span><br><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"></span><br><span class="line"><span class="comment"># 升级所有软件包</span></span><br><span class="line"><span class="built_in">sudo</span> apt full-upgrade</span><br><span class="line"></span><br><span class="line"><span class="comment"># ifconfig使用</span></span><br><span class="line"><span class="built_in">sudo</span> apt install net-tools</span><br><span class="line">ifconfig</span><br><span class="line"></span><br><span class="line"><span class="comment"># xshell连接需安装软件包</span></span><br><span class="line"><span class="built_in">sudo</span> apt install ssh</span><br><span class="line"><span class="built_in">sudo</span> apt install vim</span><br><span class="line"><span class="built_in">sudo</span> apt install git</span><br><span class="line"></span><br><span class="line"><span class="comment"># C/C++编译工具链</span></span><br><span class="line"><span class="built_in">sudo</span> apt install gcc g++</span><br><span class="line"></span><br><span class="line"><span class="comment"># 修改终端命令行显示的颜色</span></span><br><span class="line"><span class="comment"># 将下面两行添加到.bashrc文件中</span></span><br><span class="line"><span class="built_in">set</span> PS1</span><br><span class="line"><span class="built_in">export</span> PS1=<span class="string">&#x27;\[\e[32;40m\]\u\[\e[33;40m\]@\[\e[36;40m\]\h\[\e[35;40m\]:\[\e[31;40m\]\W\[\e[00m\]\$ &#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装中文man手册</span></span><br><span class="line"><span class="built_in">sudo</span> apt-get install manpages-zh</span><br><span class="line">man -L zh_CN <span class="built_in">ls</span>  <span class="comment"># 查看 `ls` 的中文手册</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 在~/.bashrc文件中添加以下行来支持中英文man切换</span></span><br><span class="line"><span class="comment"># 使用man查看英文man手册</span></span><br><span class="line"><span class="built_in">alias</span> man=<span class="string">&#x27;man -M /usr/share/man&#x27;</span> </span><br><span class="line"><span class="comment"># 使用cman查看中文man手册</span></span><br><span class="line"><span class="built_in">alias</span> cman=<span class="string">&#x27;man -M /usr/share/man/zh_CN&#x27;</span> </span><br><span class="line"><span class="comment"># 刷新配置</span></span><br><span class="line"><span class="built_in">source</span> ~/.bashrc</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="用户添加sudo权限">用户添加sudo权限</h2>
<p>用sudo时提示”xxx is not in the sudoers file. This incident will be
reported”.
其中XXX是你的用户名，也就是你的用户名没有权限使用sudo,只要修改一下<code>/etc/sudoers</code>
文件就行了。</p>
<ol type="1">
<li><p>进入超级用户模式。也就是输入”su -”
，系统会让你输入超级用户密码，输入密码后就进入了超级用户模式。也就是root用户模式，为root用户到密码也是root。注意这里有”-”
，这和su是不同的，在用命令”su”的时候只是切换到root，但没有把root的环境变量传过去，还是当前用户的环境变量，用”su
-”命令将环境变量也一起带过去，就象和root登录一样。</p></li>
<li><p>添加文件的写权限。</p>
<p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">chmod</span> u+w /etc/sudoers  </span><br></pre></td></tr></table></figure></p></li>
<li><p>编辑<code>/etc/sudoers</code>文件。找到这一 行：</p>
<p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">root  ALL=(ALL) ALL  <span class="comment">#在它下面添加一行</span></span><br><span class="line">aliyun  ALL=(ALL) ALL  <span class="comment">#aliyun 就是用户的名字</span></span><br></pre></td></tr></table></figure></p></li>
<li><p>撤销<code>/etc/sudoers</code>写权限。</p>
<p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">chmod</span> u-w /etc/sudoers</span><br></pre></td></tr></table></figure></p></li>
</ol>
]]></content>
      <categories>
        <category>Linux</category>
        <category>基础知识</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>版本控制系统简介</title>
    <url>/2025/04/08/tools/git/version-control-system-intro/</url>
    <content><![CDATA[<p>本文对版本控制系统进行简要介绍，包括版本控制系统简介、Git和SVN的区别。</p>
<span id="more"></span>
<hr />
<h2 id="引言">引言</h2>
<h3 id="什么是版本控制">什么是版本控制</h3>
<p>什么是“版本控制”？版本控制是一种<strong>记录一个或若干文件内容变化，以便将来查阅特定版本修订情况</strong>的系统。</p>
<h3 id="版本控制系统分类">版本控制系统分类</h3>
<h4 id="本地版本控制系统">本地版本控制系统</h4>
<p>其中最流行的一种叫做 RCS（Version Control
Systems），现今许多计算机系统上都还看得到它的踪影。 RCS
的工作原理是在硬盘上保存补丁集（补丁是指文件修订前后的变化）；通过应用所有的补丁，可以重新计算出各个版本的文件内容。</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210160840152.png" alt="image-20241210160840152" style="zoom:33%;" /></p>
<h4 id="集中化的版本控制系统">集中化的版本控制系统</h4>
<p>如何让在不同系统上的开发者协同工作？ 于是，集中化的版本控制系统（
<strong>CVCS</strong>）应运而生。 这类系统，诸如
<strong>CVS</strong>、<strong>Subversion</strong> 以及Perforce
等，都有一个<strong>单一的集中管理的服务器</strong>，保存所有文件的修订版本，而协同工作的人们都通过客户端连到这台服务器，取出最新的文件或者提交更新。</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210161127422.png" alt="image-20241210161127422" style="zoom: 50%;" /></p>
<p><strong>缺点</strong>：中央服务器的单点故障。</p>
<h4 id="分布式版本控制系统">分布式版本控制系统</h4>
<p>分布式版本控制系统（Distributed Version Control
System，DVCS），像Git、Mercurial 以及 Darcs
等，客户端并不只提取最新版本的文件快照，
而是把代码仓库完整地镜像下来，包括完整的历史记录。
这么一来，任何一处协同工作用的服务器发生故障，事后都可以用任何一个镜像出来的本地仓库恢复。
因为每一次的克隆操作，实际上都是一次对代码仓库的完整备份。</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210161625132.png" alt="image-20241210161625132" style="zoom: 25%;" /></p>
<h3 id="版本控制工具比较">版本控制工具比较</h3>
<table>
<colgroup>
<col style="width: 16%" />
<col style="width: 24%" />
<col style="width: 25%" />
<col style="width: 33%" />
</colgroup>
<thead>
<tr class="header">
<th><strong>特性</strong></th>
<th><strong>VisualSVN</strong></th>
<th><strong>纯 Subversion</strong></th>
<th><strong>Git</strong></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>平台</strong></td>
<td>Windows 专用</td>
<td>跨平台</td>
<td>跨平台</td>
</tr>
<tr class="even">
<td><strong>易用性</strong></td>
<td>高，提供 GUI 管理工具</td>
<td>中，需要手动配置和管理</td>
<td>高，许多 GUI 客户端可选</td>
</tr>
<tr class="odd">
<td><strong>分布式功能</strong></td>
<td>支持主从架构（VDFS）</td>
<td>无</td>
<td>原生分布式版本控制</td>
</tr>
<tr class="even">
<td><strong>学习曲线</strong></td>
<td>平缓</td>
<td>较陡</td>
<td>平缓（客户端）/较陡（命令行）</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="svn">SVN</h2>
<p>SVN（Subversion）项目的初衷是为了替换版本控制软件CVS，在CVS
的功能的基础上有很多的提升同时也能较好的解决CVS 系统的一些不足。</p>
<h3 id="svn-服务端安装配置">svn 服务端安装配置</h3>
<p>有两种服务端安装包</p>
<h4 id="官方安装包">官方安装包</h4>
<p><span class="exturl" data-url="aHR0cHM6Ly9zdWJ2ZXJzaW9uLmFwYWNoZS5vcmcv">官方网站<i class="fa fa-external-link-alt"></i></span></p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210171529663.png" alt="image-20241210171529663" style="zoom:50%;" /></p>
<p>官方提供的服务端安装包，安装后需要通过命令行操作，适用于专业配置管理员使用。</p>
<h4 id="图形化服务端">图形化服务端</h4>
<p><strong>VisualSVN</strong> 是一款基于 <strong>Apache
Subversion</strong> 的商业版本控制服务器解决方案，专为 <strong>Windows
平台</strong>设计。它为 Subversion 提供了一个易于管理和部署的 GUI
界面，并集成了许多增强功能，适合需要在 Windows 环境中使用 Subversion
的团队和企业。</p>
<p><span class="exturl" data-url="aHR0cHM6Ly93d3cudmlzdWFsc3ZuLmNvbS9kb3dubG9hZHMv">下载地址<i class="fa fa-external-link-alt"></i></span></p>
<p>安装过程：</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210172021355.png" alt="image-20241210172021355" style="zoom:50%;" /></p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210172725603.png" alt="image-20241210172725603" style="zoom:50%;" /></p>
<h3 id="创建仓库">创建仓库</h3>
<p>安装完成后,启动VisualSVN Server
Manager，可以在窗口的右边看到版本库的一些信息,比如状态,日志,用户认证,版本库等.
要建立版本库,需要右键单击左边窗口的Repositores,如下图:</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210174143740.png" alt="image-20241210174143740" style="zoom: 50%;" /></p>
<p>在弹出的右键菜单中选择Create New Repository 或者 新建-&gt;Repository:
两者都一样。</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210174450673.png" alt="image-20241210174450673" style="zoom:50%;" /></p>
<p>选择仓库类型：</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210175138909.png" alt="image-20241210175138909" style="zoom:50%;" /></p>
<p>在 VisualSVN 中，<strong>FSFS</strong> 和 <strong>VDFS</strong>
是两种不同的数据存储后端，它们在用途、性能和特性上存在明显差异。以下是它们的主要区别：</p>
<table>
<colgroup>
<col style="width: 10%" />
<col style="width: 44%" />
<col style="width: 44%" />
</colgroup>
<thead>
<tr class="header">
<th><strong>特性</strong></th>
<th><strong>FSFS</strong></th>
<th><strong>VDFS</strong></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>全称</strong></td>
<td>Flat File System</td>
<td>VisualSVN Distributed File System</td>
</tr>
<tr class="even">
<td><strong>架构</strong></td>
<td>单机存储</td>
<td>分布式存储，支持主从架构</td>
</tr>
<tr class="odd">
<td><strong>数据存储</strong></td>
<td>数据存储为普通的平面文件，使用自定义格式</td>
<td>使用专有的数据存储格式，支持跨服务器的双向数据复制</td>
</tr>
<tr class="even">
<td><strong>部署模型</strong></td>
<td>适合单一服务器或简单的网络共享环境</td>
<td>适合需要多服务器同步和高可用性的分布式环境</td>
</tr>
<tr class="odd">
<td><strong>功能等价性</strong></td>
<td>是标准 Subversion 的默认后端，功能全面，适用于几乎所有使用场景</td>
<td>功能等同于 FSFS，但支持透明的分布式数据同步</td>
</tr>
<tr class="even">
<td><strong>数据复制</strong></td>
<td>不支持内置的分布式数据复制功能，需要依赖第三方工具实现</td>
<td>内置支持主从复制，主库的提交会自动同步到从库；从库也支持写操作，写入的数据会同步到主库及其他从库</td>
</tr>
<tr class="odd">
<td><strong>数据一致性</strong></td>
<td>单机存储，无数据一致性问题</td>
<td>提供强一致性，确保主库和从库之间的数据同步</td>
</tr>
<tr class="even">
<td><strong>扩展性</strong></td>
<td>不适合大规模分布式环境</td>
<td>专为分布式环境设计，适合需要高扩展性和容错能力的场景</td>
</tr>
<tr class="odd">
<td><strong>配置复杂度</strong></td>
<td>配置简单，适合小型团队或单机开发环境</td>
<td>配置较复杂，需要设置主库和从库</td>
</tr>
<tr class="even">
<td><strong>使用成本</strong></td>
<td>开源且免费</td>
<td>专有技术，可能需要购买 VisualSVN Server 企业版才能使用</td>
</tr>
</tbody>
</table>
<p><strong>选择建议</strong>:</p>
<ol type="1">
<li><strong>选择 FSFS 的场景</strong>：
<ul>
<li>小型团队或单机开发环境。</li>
<li>不需要分布式存储或高可用性。</li>
<li>偏好使用开源工具，降低成本。</li>
</ul></li>
<li><strong>选择 VDFS 的场景</strong>：
<ul>
<li>需要多服务器分布式部署和高可用性。</li>
<li>希望利用内置的主从复制功能简化同步操作。</li>
<li>对性能、可靠性和一致性要求较高的大型团队或企业环境。</li>
</ul></li>
</ol>
<p>可以随意取名字，或者你项目的名字</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210175705835.png" alt="image-20241210175705835" style="zoom:50%;" /></p>
<p>为新仓库选择初始布局：</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210175934576.png" alt="image-20241210175934576" style="zoom:50%;" /></p>
<p>设置用户访问仓库权限：</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210180141084.png" alt="image-20241210180141084" style="zoom:50%;" /></p>
<p>仓库创建成功： 仓库地址为
<code>https://ip地址或计算机名/svn/仓库名称</code></p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210180257823.png" alt="image-20241210180257823" style="zoom:50%;" /></p>
<h3 id="创建工程目录">创建工程目录</h3>
<p>仓库中存放开发项目代码、文档等，需要创建一个工程目录。</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210181751209.png" alt="image-20241210181751209" style="zoom:50%;" /></p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210182055123.png" alt="image-20241210182055123" style="zoom: 67%;" /></p>
<p>创建成功：</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210182216954.png" alt="image-20241210182216954" style="zoom: 67%;" /></p>
<blockquote>
<p>trunk：项目开发代码的主体，是从项目开始直到当前都处于活动的状态，从这里可以获得项目最新的源代码以及几乎所有的变更历史信息。</p>
<p>branch：从trunk
的某个点分离出来的代码拷贝，通常可以在不影响主干的前提下在这里进行重大bug
的修改，或者做实验性的开发，以及定制功能开发等。如果分支达到了预期的目的，通常可以被合并（Merge）到主干中。</p>
<p>tag：用来表示trunk 和branch
的某个点的状态，以代表项目的某个稳定状态，通常为最终发布状态。</p>
</blockquote>
<h3 id="tortoisesvn-客户端">TortoiseSVN 客户端</h3>
<h4 id="svn-客户端类型">SVN 客户端类型</h4>
<p>svn 客户端需要通过网络访问svn
服务端提交文件、查询文件等，可通过以下客户端类型访问svn服务端：</p>
<ul>
<li>使用Subversion
提供的客户端命令。使用方式：在命令行下输入命令操作。</li>
<li>使用Torotise 图形化界面操作（推荐）</li>
<li>使用Eclipse 等开发工具插件操作（推荐）</li>
</ul>
<h4 id="tortoisesvn-下载安装">TortoiseSVN 下载安装</h4>
<p>TortoiseSVN 是Subversion
版本控制系统的一个免费开源客户端，不需要为使用它而付费。 TortoiseSVN
是Subversion 的Windows 扩展。它使你避免接触Subversion
枯燥而且不方便的Command Line。它完全嵌入Windows
Explorer，使用时只需在正常的窗口里右键操作就可以了。</p>
<p><span class="exturl" data-url="aHR0cDovL3RvcnRvaXNlc3ZuLm5ldC9kb3dubG9hZHMuaHRtbA==">下载<i class="fa fa-external-link-alt"></i></span>：提供32
位和64 位不同版本，安装tortoiseSVN。
需要修改客户端电脑右键菜单，安装后需要重启电脑。</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210185334196.png" alt="image-20241210185334196" style="zoom:50%;" /></p>
<h4 id="浏览仓库">浏览仓库</h4>
<p>使用Tortoise 浏览svn 服务端的仓库的内容：</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210202533648.png" alt="image-20241210202533648" style="zoom:50%;" /></p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210202612712.png" alt="image-20241210202612712" style="zoom:50%;" /></p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210202632437.png" alt="image-20241210202632437" style="zoom:50%;" /></p>
<h3 id="权限管理">权限管理</h3>
<h4 id="认证授权机制">认证授权机制</h4>
<p>在企业开发中会为每位程序员、测试人员等分配一个账号，用户通过使用svn客户端连接svn
服务时需要输入账号和密码，svn
服务对账号和密码进行校验，输入正确可以继续访问，当用户访问仓库下某个目录时，svn
服务对用户进行授权，如果用户拥有该目录的访问权限方可访问。
判断账号和密码输入是否正确的过程即认证过程。
判断用户是否拥有目录的读/写权限时即授权过程。</p>
<h4 id="创建用户">创建用户</h4>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210203805987.png" alt="image-20241210203805987" style="zoom:50%;" /></p>
<h4 id="创建组">创建组</h4>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210203952605.png" alt="image-20241210203952605" style="zoom:50%;" /></p>
<h4 id="分配权限">分配权限</h4>
<ul>
<li>删除系统安装后默认权限：</li>
</ul>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210204134586.png" alt="image-20241210204134586" style="zoom:50%;" /></p>
<ul>
<li>开发人员拥有读写权限:</li>
</ul>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210204509786.png" alt="image-20241210204509786" style="zoom:50%;" /></p>
<ul>
<li>清除认证缓存：</li>
</ul>
<p>有几种情况需要清除认证缓存：
1、本地使用多个账号登陆，每次输入的账号和密码都不一样
2、当账号密码修改后（建议清理）</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210204853196.png" alt="image-20241210204853196" style="zoom:50%;" /></p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210205040201.png" alt="image-20241210205040201" style="zoom:50%;" /></p>
<h3 id="tortoisesvn日常使用">TortoiseSVN日常使用</h3>
<h4 id="浏览仓库-1">浏览仓库</h4>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210205445036.png" alt="image-20241210205445036" style="zoom:50%;" /></p>
<h4 id="导入导出">导入导出</h4>
<p>Export:
导出项目，和checkout区别（checkout检出文件后，含有.svn隐藏文件夹，会和SVN仓库交互，export导出没有隐藏文件夹）。</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210205731439.png" alt="image-20241210205731439" style="zoom:67%;" /></p>
<p>import 将本地资源导入到svn 服务器。</p>
<h4 id="修改提交">修改提交</h4>
<h5 id="checkout">Checkout</h5>
<p>检出项目，复制项目的副本到本地。在要检出的目录中右键：</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210211126149.png" alt="image-20241210211126149" style="zoom:50%;" /></p>
<h5 id="add">add</h5>
<p>在检出的目录中添加文件：</p>
<figure>
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241211131310571.png"
alt="image-20241211131310571" />
<figcaption aria-hidden="true">image-20241211131310571</figcaption>
</figure>
<p>图标问号表示这是一个新文件</p>
<p>Add to ignore list ：添加到忽略列表（标记该文件不需要版本控制）</p>
<p>Add ： 标记这个文件添加到服务器</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241210212709899.png" alt="image-20241210212709899" style="zoom:50%;" /></p>
<p>文件上带“+”表示已经标记要添加到版本库。</p>
<blockquote>
<p><strong>这里可能遇到的问题：</strong>文件夹不显示任何SVN相关的小图标了，例如：绿色打钩，红色感叹等</p>
<p>原因： Windows Explorer Shell最多支持15个Overlay
Icon,也就是注册表排在前15个才能显示，后面的不能显示。</p>
<p>解决方法：</p>
<ol type="1">
<li><p>打开注册表：Win+R快捷键打开运行窗口，输入regedit</p></li>
<li><p>注册表内前往这个路径：“计算机_LOCAL_MACHINE”</p></li>
<li><p>重命名SVN相关的注册表，即原名字前面加几个空格，确保能排到前面去，空格越多越靠前（或直接删除前面没啥用的图标）。</p></li>
<li><p>注册表重命名后，重启电脑，再进来看看是否已经排到前面，若是则完成。</p></li>
</ol>
<p>只要确保SVN相关的注册表排在前15的位置就能显示文件夹小图标。</p>
</blockquote>
<h5 id="commit">Commit</h5>
<p>当检出目录或子目录中内容有修改，目录图标变为：<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241211133930021.png"
alt="image-20241211133930021" /></p>
<p>提交Commit 提交本地修改至svn 服务器：
在检出目录或要提交修改的目录右键：</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241211134259384.png" alt="image-20241211134259384" style="zoom:50%;" /></p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241211140905639.png" alt="image-20241211140905639" style="zoom: 50%;" /></p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241211141030289.png" alt="image-20241211141030289" style="zoom: 50%;" /></p>
<p>提交后目录中的内容与svn 服务同步，目录图标变为：<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241211141141149.png"
alt="image-20241211141141149" /></p>
<h5 id="update">update</h5>
<p>更新仓库的文件到本地</p>
<h5 id="delete">Delete</h5>
<p>删除版本库文件</p>
<p>标记删除后，本地文件删除，标记删除后需要commit。</p>
<h5 id="revert">revert</h5>
<p>在检出目录或子目录操作会记录操作日志，提交前可以回滚操作。</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241211142307778.png" alt="image-20241211142307778" style="zoom:50%;" /></p>
<h4 id="冲突处理">冲突处理</h4>
<p>两个客户端同时修改同一个文件，
改动同一个位置，发生冲突情况如果当commit
遇到文件已经过时，说明另一个人可能改动过—– update</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241211144245574.png" alt="image-20241211144245574" style="zoom:50%;" /></p>
<p>main.c 将本地和服务器合并到一起的文件</p>
<p>main.c.mine 我本地自己修改后的文件</p>
<p>main.c.r9 我修改之前的文件</p>
<p>main.c.r10别人修改后的文件</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241211144421946.png" alt="image-20241211144421946" style="zoom:50%;" /></p>
<p>冲突解决后再进行commit</p>
<hr />
<h2 id="git">Git</h2>
<h3 id="起步">起步</h3>
<h4 id="什么是git">什么是Git?</h4>
<p>Git 和其它版本控制系统（包括 Subversion 和近似工具）的主要差别在于
Git 对待数据的方式。
从概念上来说，其它大部分系统以文件变更列表的方式存储信息，这类系统（CVS、Subversion、Perforce
等等） 将它们存储的信息看作是一组基本文件和每个文件随时间逐步累积的差异
（它们通常称作
<strong>基于差异</strong>（delta-based）的版本控制）。</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241211162649883.png" alt="image-20241211162649883" style="zoom: 33%;" /></p>
<p>Git 不按照以上方式对待或保存数据。反之，Git
更像是把数据看作是对小型文件系统的一系列快照。 在
Git中，每当你提交更新或保存项目状态时，它基本上就会对当时的全部文件创建一个快照并保存这个快照的索引。为了效率，如果文件没有修改，Git
不再重新存储该文件，而是只保留一个链接指向之前存储的文件。 Git
对待数据更像是一个 <strong>快照流</strong>。</p>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241211162716274.png" alt="image-20241211162716274" style="zoom: 33%;" /></p>
<h3 id="git常用命令">git常用命令</h3>
<p>参考<a
href="https://invinc-z.com/version-control-system/git-common-command-reference/">博文</a></p>
<hr />
<h2 id="参考资料">参考资料</h2>
<ol type="1">
<li><span class="exturl" data-url="aHR0cHM6Ly96aC56LWxpYi5nbC9ib29rLzExOTE3MTcyL2Q3MGNiYi8lRTQlQkMlQTAlRTYlOTklQkElRTYlOTIlQUQlRTUlQUUlQTItc3ZuLSVFOCVBRSVCMiVFNCVCOSU4OS5odG1s">传智播客
SVN 讲义<i class="fa fa-external-link-alt"></i></span></li>
<li>S. Chacon and B. Straub, <em>Pro Git</em>, 2nd ed. New York, NY,
USA: Apress, 2014. [Online]. <span class="exturl" data-url="aHR0cHM6Ly9naXQtc2NtLmNvbS9ib29rL3poL3Yy">Available<i class="fa fa-external-link-alt"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9ob21lLmNuYmxvZ3MuY29tL3UveGlhb2ZlaWxpbi8=">小林不会飞<i class="fa fa-external-link-alt"></i></span>，“解决TortoiseSVN或者TortoiseGit拉取的文件夹不能完整显示绿色打钩、黄色、红色感叹号、蓝色加号等小图标的问题”，<span class="exturl" data-url="aHR0cHM6Ly93d3cuY25ibG9ncy5jb20veGlhb2ZlaWxpbi9wLzE2NTk4ODU5Lmh0bWw=">网址<i class="fa fa-external-link-alt"></i></span></li>
</ol>
]]></content>
      <categories>
        <category>工具与工程实践（Tooling &amp; Engineering）</category>
        <category>版本控制</category>
      </categories>
      <tags>
        <tag>Git</tag>
        <tag>SVN</tag>
      </tags>
  </entry>
  <entry>
    <title>电脑常用键盘快捷键整理</title>
    <url>/2024/01/02/tools/misc/computer-keyboard-shortcuts/</url>
    <content><![CDATA[<p>本文总结日常电脑使用中好用的键盘快捷键操作。</p>
<span id="more"></span>
<hr />
<h2 id="ubuntu">Ubuntu</h2>
<p>终端</p>
<table>
<colgroup>
<col style="width: 64%" />
<col style="width: 35%" />
</colgroup>
<thead>
<tr class="header">
<th>快捷键</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><kbd>Ctrl</kbd> + <kbd>Alt</kbd> + <kbd>F3</kbd></td>
<td>进入控制台终端（F3-F6可进行切换）</td>
</tr>
<tr class="even">
<td><kbd>Ctrl</kbd> + <kbd>Alt</kbd> + <kbd>F1</kbd>或<kbd>F2</kbd></td>
<td>从控制台终端返回图形界面</td>
</tr>
<tr class="odd">
<td><kbd>Ctrl</kbd> + <kbd>Alt</kbd> + <kbd>T</kbd></td>
<td>打开GNOME 终端模拟器</td>
</tr>
<tr class="even">
<td><kbd>Shift</kbd> + <kbd>Ctrl</kbd> + <kbd>C</kbd></td>
<td>GNOME 终端 copy</td>
</tr>
<tr class="odd">
<td><kbd>Shift</kbd> + <kbd>Ctrl</kbd> + <kbd>V</kbd></td>
<td>GNOME 终端 paste</td>
</tr>
</tbody>
</table>
]]></content>
      <categories>
        <category>工具与工程实践（Tooling &amp; Engineering）</category>
        <category>效率工具</category>
      </categories>
      <tags>
        <tag>快捷键</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo NexT 主题配置</title>
    <url>/2025/06/30/tools/misc/hexo-next-theme-configure/</url>
    <content><![CDATA[<p>本文记录如何使用Hexo中的NexT主题来配置和美化自己的博客页面。</p>
<span id="more"></span>
<hr />
<h2 id="好用插件">好用插件</h2>
<h3 id="评论系统">评论系统</h3>
<h4 id="giscus">giscus</h4>
<p>安装<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL25leHQtdGhlbWUvaGV4by1uZXh0LWdpc2N1cw==">hexo-next-giscus<i class="fa fa-external-link-alt"></i></span>。</p>
<p>配置参考<span class="exturl" data-url="aHR0cHM6Ly9zaGlyeW5sLmdpdGh1Yi5pby8yMDI1LzAxLzI4LyVFOCVBRiU4NCVFOCVBRSVCQSVFNiVCNSU4QiVFOCVBRiU5NS8=">hexo-next
&amp; giscus 评论测试<i class="fa fa-external-link-alt"></i></span>，<span class="exturl" data-url="aHR0cHM6Ly9sdWh1YWRvbmcuY29tL2hleG8vaGV4by1jb21tZW50cy1naXNjdXM=">Hexo
评论系统（Giscus）<i class="fa fa-external-link-alt"></i></span>。</p>
<h3 id="文章加密">文章加密</h3>
<p>安装<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0QwbjlYMW4vaGV4by1ibG9nLWVuY3J5cHQvYmxvYi9tYXN0ZXIvUmVhZE1lLnpoLm1k">hexo-blog-encrypt<i class="fa fa-external-link-alt"></i></span>。</p>
<p>配置参考<span class="exturl" data-url="aHR0cHM6Ly9udW0xY2hlbnppeWFuZy5naXRodWIuaW8vMjAyNS8wNC8wMS8lRTclQUMlOTQlRTglQUUlQjAlRTUlOEQlOUElRTUlQUUlQTIlRTYlOTAlQUQlRTUlQkIlQkEvNS5ORVhUJUU2JTk2JTg3JUU3JUFCJUEwJUU1JThBJUEwJUU1JUFGJTg2Lw==">NEXT文章加密<i class="fa fa-external-link-alt"></i></span>
,<span class="exturl" data-url="aHR0cHM6Ly9ibG9nLnBpaWljay50b3AvMjAyNS8wNi8xNi9oZXhvJUU2JUI3JUJCJUU1JThBJUEwJUU1JUFGJTg2JUU3JUEwJTgxLw==">为hexo文章加密<i class="fa fa-external-link-alt"></i></span>。</p>
<h3 id="书单页影单页">书单页、影单页</h3>
<p>安装<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL215dGhzbWFuL2hleG8tZG91YmFu">hexo-douban<i class="fa fa-external-link-alt"></i></span>，嵌入豆瓣个人主页。</p>
<p><strong>注意</strong>：在hexo-theme-next主题中，需要将<code>_config.next.yml</code>中的<code>pjax</code>设置为false，否则书单页面无法正常渲染。</p>
<h3 id="文章隐藏">文章隐藏</h3>
<p><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL3ByaW5zc3MvaGV4by1oaWRlLXBvc3Rz">hexo-hide-posts<i class="fa fa-external-link-alt"></i></span></p>
<p>我的 <code>_config.yml</code> 配置如下：</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">hide_posts:</span></span><br><span class="line"><span class="comment"># 是否启用 hexo-hide-posts</span></span><br><span class="line"><span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line"><span class="comment"># 隐藏文章的 front-matter 标识，也可以改成其他你喜欢的名字</span></span><br><span class="line"><span class="attr">filter:</span> <span class="string">hidden</span></span><br><span class="line"><span class="comment"># 为隐藏的文章添加 noindex meta 标签，阻止搜索引擎收录</span></span><br><span class="line"><span class="attr">noindex:</span> <span class="literal">false</span></span><br><span class="line"><span class="comment"># 设置白名单，白名单中的 generator 可以访问隐藏文章</span></span><br><span class="line"><span class="comment"># 常见的 generators 有：index, tag, category, archive, sitemap, feed, etc.</span></span><br><span class="line"><span class="attr">allowlist_generators:</span> [<span class="string">&#x27;*&#x27;</span>]</span><br><span class="line"><span class="comment"># 设置黑名单，黑名单中的 generator 不可以访问隐藏文章</span></span><br><span class="line"><span class="attr">blocklist_generators:</span> [<span class="string">&#x27;index&#x27;</span>, <span class="string">&#x27;feed&#x27;</span>]</span><br></pre></td></tr></table></figure>
<p>配置后首页sidebar日志数量显示的不包含隐藏的，archive页面日志数量和按年统计数量不匹配（都是不包含隐藏的）。</p>
<p>修改代码如下：</p>
<p><code>node_modules\hexo-theme-next\layout\_partials\sidebar\site-overview.njk</code>和
<code>node_modules\hexo-theme-next\layout\archive.njk</code>中：</p>
<p><code>site.posts.length</code> 修改为
<code>(site.all_posts or site.posts).length</code></p>
<p><code>node_modules\hexo-theme-next\layout\_macro\post-collapse.njk</code>中将
<code>post_count(year)</code> 函数替换为
<code>all_posts_count_by_year(year)</code>,</p>
<p>并在在站点根目录新建文件：<code>scripts/all-posts-helper.js</code>，添加内容：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">hexo.<span class="property">extend</span>.<span class="property">helper</span>.<span class="title function_">register</span>(<span class="string">&#x27;all_posts_count_by_year&#x27;</span>, <span class="keyword">function</span> (<span class="params">year</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> posts = <span class="variable language_">this</span>.<span class="property">site</span>.<span class="property">all_posts</span> || <span class="variable language_">this</span>.<span class="property">site</span>.<span class="property">posts</span>;</span><br><span class="line">  <span class="keyword">if</span> (!posts) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Hexo 的 posts 通常是 Warehouse Query，toArray() 更稳</span></span><br><span class="line">  <span class="keyword">const</span> arr = <span class="keyword">typeof</span> posts.<span class="property">toArray</span> === <span class="string">&#x27;function&#x27;</span> ? posts.<span class="title function_">toArray</span>() : posts;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> arr.<span class="title function_">filter</span>(<span class="function"><span class="params">post</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (!post || !post.<span class="property">date</span>) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">return</span> post.<span class="property">date</span>.<span class="title function_">format</span>(<span class="string">&#x27;YYYY&#x27;</span>) === <span class="title class_">String</span>(year);</span><br><span class="line">  &#125;).<span class="property">length</span>;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>后续更新NexT主题时可能会覆盖修改，需将其手动改回来。（目前没时间考虑
universality）</p>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>博客搭建</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>NexT</tag>
      </tags>
  </entry>
  <entry>
    <title>如何写Makefile</title>
    <url>/2025/06/19/tools/misc/how-to-write-makefile/</url>
    <content><![CDATA[<p>本文介绍了如何写Makefile文件。</p>
<span id="more"></span>
<hr />
<h2 id="makefile介绍">Makefile介绍</h2>
<h3 id="使用变量">使用变量</h3>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">objects = main.o kbd.o command.o display.o \</span><br><span class="line">insert.o search.o files.o utils.o</span><br><span class="line"></span><br><span class="line">edit : <span class="variable">$(objects)</span></span><br><span class="line">  cc -o edit <span class="variable">$(objects)</span></span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<p>⼀个变量的定义很简单，就是⼀个名字（变量名）后跟上⼀个等号，然后在等号的后放这个变量所期望的值。对于变量的引用，则需要采⽤$(变量名)或者${变量名}这种模式。</p>
<h3 id="makefile-里面有什么">Makefile 里面有什么</h3>
<p>Makefile
里主要包含了五个东西：显式规则、隐晦规则、变量定义、文件指示和注释。</p>
<ol type="1">
<li>显式规则。显式规则说明了如何生成一个或多个目标文件。这是由Makefile的书写者明显指出要生成的文件、文件的依赖文件和生成的命令。</li>
<li>隐晦规则。由于make有自动推导的功能，所以隐晦的规则可以让我们比较简略地书写Makefile，这是由make
所支持的。</li>
<li>变量的定义。在Makefile
中要定义一系列的变量，变量一般都是字符串，这有点像C中的宏，当Makefile
被执行时，其中的变量会被扩展到相应的引用位置上。</li>
<li>文件指示。其包括了三个部分，一个是在一个Makefile
中引用另一个Makefile，就像C 语言中的include
一样；另一个是指根据某些情况指定Makefile 中的有效部分，就像C
语言中的预编译#if 一样；还有就是定义一个多行的命令。</li>
<li>注释。Makefile 中只有行注释，和UNIX 的Shell 脚本一样，其注释是用#
字符。 最后，还值得一提的是，在Makefile 中的命令，必须要以Tab
键开始。</li>
</ol>
<h3 id="引用其他的makefile">引用其他的Makefile</h3>
<p>在Makefile 使用include 关键字可以把别的Makefile
包含进来，语法是：<code>include filename</code></p>
<h3 id="make的工作方式">make的工作方式</h3>
<p>GNU 的make 工作时的执行步骤如下：</p>
<ol type="1">
<li>读入所有的Makefile。</li>
<li>读入被include 的其它Makefile。</li>
<li>初始化文件中的变量。</li>
<li>推导隐晦规则，并分析所有规则。</li>
<li>为所有的目标文件创建依赖关系链。</li>
<li>根据依赖关系，决定哪些目标要重新生成。</li>
<li>执行生成命令。</li>
</ol>
<p>1-5 步为第一个阶段，6-7
为第二个阶段。第一个阶段中，如果定义的变量被使用了，那么，make会把其展开在使用的位置。但make
并不会完全马上展开，make
使用的是拖延战术，如果变量出现在依赖关系的规则中，那么仅当这条依赖被决定要使用了，变量才会在其内部展开。</p>
<hr />
<h2 id="书写规则">书写规则</h2>
<h3 id="文件搜寻">文件搜寻</h3>
<p>Makefile 文件中的特殊变量 <code>VPATH</code>
就是完成这个功能的，如果没有指明这个变量，make
只会在当前的目录中去找寻依赖文件和目标文件。如果定义了这个变量，那么，make
就会在当前目录找不到的情况下，到所指定的目录中去找寻文件了。</p>
<p><code>VPATH = src:../headers</code></p>
<p>上面的定义指定两个目录，“src”和“../headers”，make
会按照这个顺序进行搜索。目录由“冒号”分隔。（当然，当前目录永远是最高优先搜索的地方）</p>
<hr />
<h2 id="书写命令">书写命令</h2>
<h3 id="显示命令">显示命令</h3>
<p>通常，make
会把其要执行的命令行在命令执行前输出到屏幕上。当用<strong><span
class="citation"
data-cites="字符">@字符</span></strong>在命令行前，那么，这个命令将不被make显示出来，最具代表性的例子是，用这个功能来向屏幕显示一些信息。如:</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">@echo 正在编译XXX 模块......</span><br></pre></td></tr></table></figure>
<p>当make 执行时，会输出“正在编译XXX
模块⋯⋯”字串，但不会输出命令，如果没有“@”，那么，make 将输出:</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">echo 正在编译XXX 模块......</span><br><span class="line">正在编译XXX 模块......</span><br></pre></td></tr></table></figure>
<p>如果make 执行时，带入make 参数<code>-n</code>
或<code>--just-print</code>
，那么其只是显示命令，但不会执行命令，这个功能很有利于调试Makefile，看看书写的命令是执行起来是什么样子的或是什么顺序的。而make
参数<code>-s</code> 或<code>--silent</code> 或<code>--quiet</code>
则是全面禁止命令的显示。</p>
<h3 id="命令出错">命令出错</h3>
<p>每当命令运行完后，make
会检测每个命令的返回码，如果命令返回成功，那么make
会执行下一条命令，当规则中所有的命令成功返回后，这个规则就算是成功完成了。如果一个规则中的某个命令出错了（命令退出码非零），那么make
就会终止执行当前规则，这将有可能终止所有规则的执行。有些时候，命令的出错并不表示就是错误的。例如mkdir
命令，我们一定需要建立一个目录，如果目录不存在，那么mkdir
就成功执行，万事大吉，如果目录存在，那么就出错了。我们之所以使用mkdir的意思就是一定要有这样的一个目录，于是我们就不希望mkdir
出错而终止规则的运行。</p>
<p>为了做到这一点，忽略命令的出错，可以在Makefile 的命令行前加一个减号-
（在Tab 键之后），标记为不管命令出不出错都认为是成功的。如：</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">clean:</span></span><br><span class="line">  -rm -f *.o</span><br></pre></td></tr></table></figure>
<p>还有一个全局的办法是，给make 加上<code>-i</code>
或是<code>--ignore-errors</code> 参数，那么，Makefile
中所有命令都会忽略错误。而如果一个规则是以<code>.IGNORE</code>
作为目标的，那么这个规则中的所有命令将会忽略错误。这些是不同级别的防止命令出错的方法，你可以根据你的不同喜欢设置。</p>
<p>还有一个make 的参数的是<code>-k</code> 或是<code>--keep-going</code>
，这个参数的意思是，如果某规则中的命令出错了，那么就终止该规则的执行，但继续执行其它规则。</p>
<hr />
<h2 id="使用变量-1">使用变量</h2>
<h3 id="变量赋值的区别">变量赋值的区别</h3>
<p>Makefile 中有几种不同的变量赋值方式：</p>
<table>
<colgroup>
<col style="width: 30%" />
<col style="width: 21%" />
<col style="width: 48%" />
</colgroup>
<thead>
<tr class="header">
<th>赋值方式</th>
<th>示例</th>
<th>特点</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong><code>=</code></strong>（递归展开）</td>
<td><code>A = $(B)</code></td>
<td>使用时才展开，可能受后续赋值影响</td>
</tr>
<tr class="even">
<td><strong><code>:=</code></strong>（立即展开）</td>
<td><code>A := $(B)</code></td>
<td>定义时立即展开，后续赋值不影响</td>
</tr>
<tr class="odd">
<td><strong><code>?=</code></strong>（条件赋值）</td>
<td><code>A ?= default</code></td>
<td>仅当变量未定义时赋值</td>
</tr>
<tr class="even">
<td><strong><code>+=</code></strong>（追加赋值）</td>
<td><code>A += value</code></td>
<td>向已有变量追加值</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="使用函数">使用函数</h2>
<h3 id="函数的调用语法">函数的调用语法</h3>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">$(&lt;function&gt; &lt;arguments&gt;)</span><br></pre></td></tr></table></figure>
<p>或</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">$&#123;&lt;function&gt; &lt;arguments&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>这里，<code>&lt;function&gt;</code>就是函数名。<code>&lt;arguments&gt;</code>
为函数的参数，参数间以逗号分隔，而函数名和参数之间以“空格”分隔。</p>
<h3 id="字符串处理函数">字符串处理函数</h3>
<h4 id="subst">subst</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">subst</span> &lt;from&gt;,&lt;to&gt;,&lt;text&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：字符串替换函数。</li>
<li>功能：
把字符串<code>&lt;text&gt;</code>中的<code>&lt;from&gt;</code>字符串替换成<code>&lt;to&gt;</code>。</li>
<li>返回：函数返回被替换过后的字符串。</li>
<li>示例：<code>$(subst ee,EE,feet on the street)</code>，结果返回 <span
class="math inline">\(\textrm{fEEt on the strEEt}\)</span>。</li>
</ul>
<h4 id="patsubst">patsubst</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">patsubst</span> &lt;pattern&gt;,&lt;replacement&gt;,&lt;text&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：模式字符串替换函数</li>
<li>功能：查找<code>&lt;text&gt;</code>
中的单词（单词以“空格”、“Tab”或“回车”“换行”分隔）是否符合模式<code>&lt;pattern&gt;</code>
，如果匹配的话，则以<code>&lt;replacement&gt;</code>
替换。这里，<code>&lt;pattern&gt;</code> 可以包括通配符%
，表示任意长度的字串。如果<code>&lt;replacement&gt;</code> 中也包含%
，那么，<code>&lt;replacement&gt;</code> 中的这个%
将是<code>&lt;pattern&gt;</code>中的那个%
所代表的字串。（可以用<code>\</code>来转义，以<code>\%</code>
来表示真实含义的<code>%</code>字符）</li>
<li>返回：被替换过后的字符串</li>
<li>示例：<code>$(patsubst %.c,%.o,x.c.c bar.c)</code>，把字串x.c.c
bar.c 符合模式%.c 的单词替换成%.o ，返回结果是x.c.o bar.o</li>
</ul>
<h4 id="strip">strip</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">strip</span> &lt;string&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称： 去空格函数</li>
<li>功能：去掉<code>&lt;string&gt;</code>字符中开头和结尾的空字符</li>
<li>返回：被去掉空格的字符串值</li>
<li>示例：<code>$(strip a b c )</code> 去掉字符串开头结尾的空格。</li>
</ul>
<h4 id="findstring">findstring</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">findstring</span> &lt;find&gt;,&lt;in&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：查找字符串函数</li>
<li>功能：在字符串<code>&lt;in&gt;</code>中查找<code>&lt;find&gt;</code>字符串。</li>
<li>返回：如果找到，返回<code>&lt;find&gt;</code>，否则返回空字符串。</li>
<li>示例： <code>$(findstring a,a b c); $(findstring a,b c)</code>，
第一个函数返回a 字符串，第二个返回空字符串</li>
</ul>
<h4 id="filter">filter</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">filter</span> &lt;pattern...&gt;,&lt;text&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：过滤函数</li>
<li>功能：以<code>&lt;pattern&gt;</code>
模式过滤<code>&lt;text&gt;</code>
字符串中的单词，保留符合模式<code>&lt;pattern&gt;</code>
的单词。可以有多个模式。</li>
<li>返回：返回符合模式<code>&lt;pattern&gt;</code> 的字串。</li>
<li>示例：</li>
</ul>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">sources := foo.c bar.c baz.s ugh.h</span><br><span class="line"><span class="section">foo: <span class="variable">$(sources)</span></span></span><br><span class="line">  cc <span class="variable">$(<span class="built_in">filter</span> %.c %.s,<span class="variable">$(sources)</span>)</span> -o foo</span><br></pre></td></tr></table></figure>
<p><code>$(filter %.c %.s,$(sources))</code>
返回的值是<code>foo.c bar.c baz.s</code> 。</p>
<h4 id="filter-out">filter-out</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">filter</span>-out &lt;pattern...&gt;,&lt;text&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称： 反过滤函数</li>
<li>功能：以<code>&lt;pattern&gt;</code>
模式过滤<code>&lt;text&gt;</code>
字符串中的单词，去除符合模式<code>&lt;pattern&gt;</code>
的单词。可以有多个模式。</li>
<li>返回：返回不符合模式<code>&lt;pattern&gt;</code> 的字串。</li>
<li>示例：</li>
</ul>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">objects=main1.o foo.o main2.o bar.o</span><br><span class="line">mains=main1.o main2.o</span><br></pre></td></tr></table></figure>
<p><code>$(filter-out $(mains),$(objects))</code>
返回值是<code>foo.o bar.o</code> 。</p>
<h4 id="sort">sort</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">sort</span> &lt;list&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称： 排序函数</li>
<li>功能：给字符串<code>&lt;list&gt;</code>中的单词排序（升序）。</li>
<li>返回：返回排序后的字符串。</li>
<li>示例：<code>$(sort foo bar lose)</code> 返回
<code>bar foo lose</code>。</li>
<li>备注：<code>sort</code>函数会去掉重复的单词。</li>
</ul>
<h4 id="word">word</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">word</span> &lt;n&gt;,&lt;text&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：取单词函数</li>
<li>功能：取字符串<code>&lt;text&gt;</code>中第<code>&lt;n&gt;</code>个单词。（从1开始）</li>
<li>返回：返回排字符串<code>&lt;text&gt;</code>中第<code>&lt;n&gt;</code>个单词。如果<code>&lt;n&gt;</code>比<code>&lt;text&gt;</code>中的单词数大，返回空字符串。</li>
<li>示例：<code>$(word 2, foo bar baz)</code> 返回
<code>bar</code>。</li>
</ul>
<h4 id="wordlist">wordlist</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">wordlist</span> &lt;ss&gt;,&lt;e&gt;,&lt;text&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：取单词串函数</li>
<li>功能：从字符串<code>&lt;text&gt;</code>中取从<code>&lt;ss&gt;</code>到<code>&lt;e&gt;</code>的单词串。<code>&lt;ss&gt;</code>和<code>&lt;e&gt;</code>是一个数字。</li>
<li>返回：返回字符串<code>&lt;text&gt;</code>中取从<code>&lt;ss&gt;</code>到<code>&lt;e&gt;</code>的单词串。。如果<code>&lt;ss&gt;</code>比<code>&lt;text&gt;</code>中的单词数大，返回空字符串。如果<code>&lt;e&gt;</code>大于<code>&lt;text&gt;</code>单词数，返回从<code>&lt;ss&gt;</code>开始，到<code>&lt;text&gt;</code>结束的单词串。</li>
<li>示例：<code>$(wordlist 2,3, foo bar baz)</code> 返回
<code>bar baz</code>。</li>
</ul>
<h4 id="words">words</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">wordlist</span> &lt;text&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：单词个数统计函数</li>
<li>功能：统计<code>&lt;text&gt;</code>中字符串中单词个数。</li>
<li>返回：单词个数。</li>
<li>示例：<code>$(words foo bar baz)</code> 返回 3。</li>
<li>备注：若取<code>&lt;text&gt;</code>中最后一个单词，可以
<code>$(word $(words &lt;text&gt;), &lt;text&gt;)</code>。</li>
</ul>
<h4 id="firstword">firstword</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">firstword</span> &lt;text&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：首单词函数。</li>
<li>功能：取字符串<code>&lt;text&gt;</code>中的第一个单词。</li>
<li>返回：字符串<code>&lt;text&gt;</code>中的第一个单词。</li>
<li>示例：<code>$(firstword foo bar baz)</code> 返回
<code>foo</code>。</li>
<li>备注：该函数可以用<code>word</code>函数实现：
<code>$(word 1, &lt;text&gt;)</code>。</li>
</ul>
<h3 id="文件名操作函数">文件名操作函数</h3>
<h4 id="dir">dir</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">dir</span> &lt;names...&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：取目录函数。</li>
<li>功能：从文件名序列<code>&lt;names&gt;</code>
中取出目录部分。目录部分是指最后一个反斜杠（/
）之前的部分。如果没有反斜杠，那么返回<code>./</code>。</li>
<li>返回：文件名序列<code>&lt;names&gt;</code> 中目录部分。</li>
<li>示例：<code>$(dir src/foo.c hacks)</code> 返回
<code>src/ ./</code>。</li>
</ul>
<h4 id="notdir">notdir</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">notdir</span> &lt;names...&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：取文件函数。</li>
<li>功能：从文件名序列<code>&lt;names&gt;</code>
中取出非目录部分。目录部分是指最后一个反斜杠（/ ）之后的部分。</li>
<li>返回：文件名序列<code>&lt;names&gt;</code> 中非目录部分。</li>
<li>示例：<code>$(notdir src/foo.c hacks)</code> 返回
<code>foo.c hacks</code>。</li>
</ul>
<h4 id="suffix">suffix</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">suffix</span> &lt;names...&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：取后缀函数。</li>
<li>功能：从文件名序列<code>&lt;names&gt;</code>
中取出各个文件名的后缀。</li>
<li>返回：文件名序列<code>&lt;names&gt;</code>
的后缀序列，如果文件没有后缀，则返回空字符串。</li>
<li>示例：<code>$(suffix src/foo.c src-1/bar.c hacks)</code> 返回
<code>.c .c</code>。</li>
</ul>
<h4 id="basename">basename</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">basename</span> &lt;names...&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：取前缀函数。</li>
<li>功能：从文件名序列<code>&lt;names&gt;</code>
中取出各个文件名的前缀部分。</li>
<li>返回：文件名序列<code>&lt;names&gt;</code>
的前缀序列，如果文件没有前缀，则返回空字符串。</li>
<li>示例：<code>$(suffix src/foo.c src-1.0/bar.c hacks)</code> 返回
<code>src/foo src-1.0/bar hacks</code>。</li>
</ul>
<h4 id="addsuffix">addsuffix</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">addsuffix</span> &lt;<span class="built_in">suffix</span>&gt;,&lt;names...&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：加后缀函数。</li>
<li>功能：把后缀<code>&lt;suffix&gt;</code>加到<code>&lt;names&gt;</code>中的每个单词后面。</li>
<li>返回：加过后缀的文件名序列。</li>
<li>示例：<code>$(addsuffix .c, foo bar)</code> 返回
<code>foo.c bar.c</code>。</li>
</ul>
<h4 id="addprefix">addprefix</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">addprefix</span> &lt;<span class="built_in">suffix</span>&gt;,&lt;names...&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：加前缀函数。</li>
<li>功能：把前缀<code>&lt;prefix&gt;</code>加到<code>&lt;names&gt;</code>中的每个单词前面。</li>
<li>返回：加过前缀的文件名序列。</li>
<li>示例：<code>$(addprefix src/, foo bar)</code> 返回
<code>src/foo src/bar</code>。</li>
</ul>
<h4 id="join">join</h4>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">join</span> &lt;list1&gt;,&lt;list2&gt;)</span></span><br></pre></td></tr></table></figure>
<ul>
<li>名称：连接函数。</li>
<li>功能：把<code>&lt;list2&gt;</code>
中的单词对应地加到<code>&lt;list1&gt;</code>的单词后面。如果<code>&lt;list1&gt;</code>
的单词个数要比<code>&lt;list2&gt;</code>
的多，那么，<code>&lt;list1&gt;</code>
中的多出来的单词将保持原样。如果<code>&lt;list2&gt;</code>的单词个数要比<code>&lt;list1&gt;</code>多，那么，<code>&lt;list2&gt;</code>
多出来的单词将被复制到<code>&lt;list1&gt;</code> 中。</li>
<li>返回：返回连接过后的字符串。</li>
<li>示例：<code>$(join aaa bbb, 111 222 333)</code> 返回
<code>aaa111 bbb222 333</code>。</li>
</ul>
<h3 id="foreach函数">foreach函数</h3>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">foreach</span> &lt;var&gt;,&lt;list&gt;,&lt;text&gt;)</span></span><br></pre></td></tr></table></figure>
<p>这个函数的意思是，把参数<code>&lt;list&gt;</code>
中的单词逐一取出放到参数<code>&lt;var&gt;</code>所指定的变量中，然后再执行<code>&lt;text&gt;</code>
所包含的表达式。每一次<code>&lt;text&gt;</code>
会返回一个字符串，循环过程中，<code>&lt;text&gt;</code>
的所返回的每个字符串会以空格分隔，最后当整个循环结束时，<code>&lt;text&gt;</code>
所返回的每个字符串所组成的整个字符串（以空格分隔）将会是foreach
函数的返回值。</p>
<p>所以，<code>&lt;var&gt;</code>
最好是一个变量名，<code>&lt;list&gt;</code>
可以是一个表达式，而<code>&lt;text&gt;</code>
中一般会使用<code>&lt;var&gt;</code>
这个参数来依次枚举<code>&lt;list&gt;</code> 中的单词。举个例子：</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">names := a b c d</span><br><span class="line">files := <span class="variable">$(<span class="built_in">foreach</span> n,<span class="variable">$(names)</span>,<span class="variable">$(n)</span>.o)</span></span><br></pre></td></tr></table></figure>
<p>上面的例子中，<code>$(names)</code>
中的单词会被挨个取出，并存到变量n中，<code>$(n).o</code>
每次根据<code>$(n)</code>
计算出一个值，这些值以空格分隔，最后作为foreach
函数的返回，所以，<code>$(files)</code>
的值是<code>a.o b.o c.o d.o</code>。 注意，foreach
中的<code>&lt;var&gt;</code> 参数是一个临时的局部变量，foreach
函数执行完后，参数<code>&lt;var&gt;</code>
的变量将不在作用，其作用域只在foreach 函数当中。</p>
<h3 id="if函数">if函数</h3>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">if</span> &lt;condition&gt;,&lt;then-part&gt;)</span></span><br><span class="line"><span class="comment"># 或</span></span><br><span class="line"><span class="variable">$(<span class="built_in">if</span> &lt;condition&gt;,&lt;then-part&gt;,&lt;else-part&gt;)</span></span><br></pre></td></tr></table></figure>
<p>可见，if
函数可以包含“else”部分，或是不含。即if函数的参数可以是两个，也可以是三个。<code>&lt;condition&gt;</code>
参数是if
的表达式，如果其返回的为非空字符串，那么这个表达式就相当于返回真，于是，<code>&lt;then-part&gt;</code>
会被计算，否则<code>&lt;else-part&gt;</code> 会被计算。</p>
<h3 id="call函数">call函数</h3>
<p>call 函数是唯一一个可以用来创建新的参数化的函数。其语法是：</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">call</span> &lt;expression&gt;,&lt;parm1&gt;,&lt;parm2&gt;,...,&lt;parmn&gt;)</span></span><br></pre></td></tr></table></figure>
<p>当make 执行这个函数时，<code>&lt;expression&gt;</code>
参数中的变量，如<code>$(1)</code> 、<code>$(2)</code>
等，会被参数<code>&lt;parm1&gt;</code>、<code>&lt;parm2&gt;</code>
、<code>&lt;parm3&gt;</code> 依次取代。而<code>&lt;expression&gt;</code>
的返回值就是call 函数的返回值。例如：</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">reverse = $(1) $(2)</span><br><span class="line">foo = <span class="variable">$(<span class="built_in">call</span> reverse,a,b)</span></span><br></pre></td></tr></table></figure>
<p>那么，foo 的值就是a b
。当然，参数的次序是可以自定义的，不一定是顺序的，如：</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">reverse = $(2) $(1)</span><br><span class="line">foo = <span class="variable">$(<span class="built_in">call</span> reverse,a,b)</span></span><br></pre></td></tr></table></figure>
<p>此时的foo的值就是b a 。</p>
<p>需要注意：在向call函数传递参数时要尤其注意空格的使用。call
函数在处理参数时，第2
个及其之后的参数中的空格会被保留，因而可能造成一些奇怪的效果。因而在向call
函数提供参数时，最安全的做法是去除所有多余的空格。</p>
<h3 id="origin函数">origin函数</h3>
<p>origin
函数不像其它的函数，他并不操作变量的值，他只是告诉你你的这个变量是哪里来的？其语法是：</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">origin</span> &lt;variable&gt;)</span></span><br></pre></td></tr></table></figure>
<p>注意，<code>&lt;variable&gt;</code>是变量的名字，不应该是引用。所以最好不要在<code>variable</code>中使用<code>$</code>字符。</p>
<p>origin 函数会以其返回值来告诉你这个变量的“出生情况”，下面，是origin
函数的返回值:</p>
<p><code>undefined</code> 如果<code>&lt;variable&gt;</code>
从来没有定义过，origin 函数返回这个值<code>undefined</code></p>
<p><code>default</code> 如果<code>&lt;variable&gt;</code>
是一个默认的定义，比如“CC”这个变量。</p>
<p><code>environment</code> 如果<code>&lt;variable&gt;</code>
是一个环境变量，并且当Makefile 被执行时，-e 参数没有被打开。</p>
<p><code>file</code> 如果<code>&lt;variable&gt;</code>
这个变量被定义在Makefile 中。</p>
<p><code>command line</code> 如果<code>&lt;variable&gt;</code>
这个变量是被命令行定义的。</p>
<p><code>override</code> 如果<code>&lt;variable&gt;</code> 是被override
指示符重新定义的。</p>
<p><code>automatic</code> 如果<code>&lt;variable&gt;</code>
是一个命令运行中的自动化变量。</p>
<h3 id="shell函数">shell函数</h3>
<p>shell
函数它的参数应该就是操作系统Shell的命令。它和反引号是相同的功能。这就是说，shell
函数把执行操作系统命令后的输出作为函数返回。于是，我们可
以用操作系统命令以及字符串处理命令awk，sed
等等命令来生成一个变量，如：</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">contents := <span class="variable">$(<span class="built_in">shell</span> cat foo)</span></span><br><span class="line">files := <span class="variable">$(<span class="built_in">shell</span> echo *.c)</span></span><br></pre></td></tr></table></figure>
<p>注意，这个函数会新生成一个Shell程序来执行命令，所以你要注意其运行性能，如果你的Makefile
中有一些比较复杂的规则，并大量使用了这个函数，那么对于你的系统性能是有害的。特别是Makefile
的隐晦的规则可能会让你的shell 函数执行的次数比你想像的多得多。</p>
<h3 id="控制make的函数">控制make的函数</h3>
<p>make 提供了一些函数来控制make
的运行。通常，你需要检测一些运行Makefile
时的运行时信息，并且根据这些信息来决定，你是让make
继续执行，还是停止。</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">error</span> &lt;text ...&gt;)</span></span><br></pre></td></tr></table></figure>
<p>产生一个致命的错误，<code>&lt;text ...&gt;</code>
是错误信息。注意，error
函数不会在一被使用就会产生错误信息，所以如果你把其定义在某个变量中，并在后续的脚本中使用这个变量，那么也是可以的。例如：</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 示例一</span></span><br><span class="line"><span class="keyword">ifdef</span> ERROR_001</span><br><span class="line">  <span class="variable">$(<span class="built_in">error</span> <span class="built_in">error</span> is <span class="variable">$(ERROR_001)</span>)</span></span><br><span class="line"><span class="keyword">endif</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 示例二</span></span><br><span class="line">ERR = <span class="variable">$(<span class="built_in">error</span> found an <span class="built_in">error</span>!)</span></span><br><span class="line"><span class="meta"><span class="keyword">.PHONY</span>: err</span></span><br><span class="line"><span class="section">err: <span class="variable">$(ERR)</span></span></span><br></pre></td></tr></table></figure>
<p>示例一会在变量 <code>ERROR_001</code> 定义了后执行时产生error
调用，而示例二则在目标err 被执行时才发生error 调用。</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(<span class="built_in">warning</span> &lt;text ...&gt;)</span></span><br></pre></td></tr></table></figure>
<p>这个函数很像error函数，只是它并不会让make退出，只是输出一段警告信息，而make
继续执行。</p>
<hr />
<h2 id="make的运行">make的运行</h2>
<h3 id="make的退出码">make的退出码</h3>
<p>make 命令执行后有三个退出码：</p>
<p>0 表示成功执行。</p>
<p>1 如果make 运行时出现任何错误，其返回1。</p>
<p>2 如果你使用了make 的“-q”选项，并且make
使得一些目标不需要更新，那么返回2。</p>
<h3 id="指定makefile">指定Makefile</h3>
<p>GNU make 找寻默认的Makefile
的规则是在当前目录下依次找三个文件——“GNUmakefile”、“makefile”和“Makefile”。其按顺序找这三个文件，一旦找到，就开始读取这个文件并执行。</p>
<p>也可以给make
命令指定一个特殊名字的Makefile。要达到这个功能，我们要使用make的<strong>-f
或是–file</strong> 参数（–makefile 参数也行）。例如，有个makefile
的名字是“hchen.mk”，那么，我们可以这样来让make
来执行这个文件：<code>make –f hchen.mk</code>。</p>
<p>如果在make 的命令行是，不只一次地使用了-f
参数，那么，所有指定的makefile 将会被连在一起传递给make执行。</p>
<h3 id="指定目标">指定目标</h3>
<p>即然make 可以指定所有makefile
中的目标，那么也包括“伪目标”，于是可以根据这种性质来让我们的makefile
根据指定的不同的目标来完成不同的事。在Unix 世界中，软件发布时，特别是GNU
这种开源软件的发布时，其makefile
都包含了编译、安装、打包等功能。可以参照这种规则来书写我们的makefile
中的目标。</p>
<ul>
<li>all: 这个伪目标是所有目标的目标，其功能一般是编译所有的目标。</li>
<li>clean: 这个伪目标功能是删除所有被make 创建的文件。</li>
<li>install:
这个伪目标功能是安装已编译好的程序，其实就是把目标执行文件拷贝到指定的目标中去。</li>
<li>print: 这个伪目标的功能是例出改变过的源文件。</li>
<li>tar: 这个伪目标功能是把源程序打包备份。也就是一个tar 文件。</li>
<li>dist: 这个伪目标功能是创建一个压缩文件，一般是把tar 文件压成Z
文件。或是gz 文件。</li>
<li>TAGS: 这个伪目标功能是更新所有的目标，以备完整地重编译使用。</li>
<li>check 和test: 这两个伪目标一般用来测试makefile 的流程。</li>
</ul>
<hr />
<h2 id="隐含规则">隐含规则</h2>
<h3 id="隐含规则使用的变量">隐含规则使用的变量</h3>
<h4 id="关于命令的变量">关于命令的变量</h4>
<table>
<colgroup>
<col style="width: 11%" />
<col style="width: 88%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">命令变量</th>
<th style="text-align: left;">解释 | 默认命令</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;">AR</td>
<td style="text-align: left;">函数库打包程序。默认命令是ar</td>
</tr>
<tr class="even">
<td style="text-align: left;">AS</td>
<td style="text-align: left;">汇编语言编译程序。默认命令是as</td>
</tr>
<tr class="odd">
<td style="text-align: left;">CC</td>
<td style="text-align: left;">C 语言编译程序。默认命令是cc</td>
</tr>
<tr class="even">
<td style="text-align: left;">CXX</td>
<td style="text-align: left;">C++ 语言编译程序。默认命令是g++</td>
</tr>
<tr class="odd">
<td style="text-align: left;">CO</td>
<td style="text-align: left;">从RCS
文件中扩展文件程序。默认命令是co</td>
</tr>
<tr class="even">
<td style="text-align: left;">CPP</td>
<td style="text-align: left;">C
程序的预处理器（输出是标准输出设备）。默认命令是$(CC) –E</td>
</tr>
<tr class="odd">
<td style="text-align: left;">FC</td>
<td style="text-align: left;">Fortran 和Ratfor
的编译器和预处理程序。默认命令是f77</td>
</tr>
<tr class="even">
<td style="text-align: left;">GET</td>
<td style="text-align: left;">从SCCS
文件中扩展文件的程序。默认命令是get</td>
</tr>
<tr class="odd">
<td style="text-align: left;">LEX</td>
<td style="text-align: left;">Lex 方法分析器程序（针对于C
或Ratfor）。默认命令是lex</td>
</tr>
<tr class="even">
<td style="text-align: left;">PC</td>
<td style="text-align: left;">Pascal 语言编译程序。默认命令是pc</td>
</tr>
<tr class="odd">
<td style="text-align: left;">YACC</td>
<td style="text-align: left;">Yacc 文法分析器（针对于C
程序）。默认命令是yacc</td>
</tr>
<tr class="even">
<td style="text-align: left;">YACCR</td>
<td style="text-align: left;">Yacc 文法分析器（针对于Ratfor
程序）。默认命令是yacc –r</td>
</tr>
<tr class="odd">
<td style="text-align: left;">MAKEINFO</td>
<td style="text-align: left;">转换Texinfo 源文件（.texi）到Info
文件程序。默认命令是makeinfo</td>
</tr>
<tr class="even">
<td style="text-align: left;">TEX</td>
<td style="text-align: left;">从TeX 源文件创建TeX DVI
文件的程序。默认命令是tex</td>
</tr>
<tr class="odd">
<td style="text-align: left;">TEXI2DVI</td>
<td style="text-align: left;">从Texinfo 源文件创建TeX DVI
文件的程序。默认命令是texi2dvi</td>
</tr>
<tr class="even">
<td style="text-align: left;">WEAVE</td>
<td style="text-align: left;">转换Web 到TeX 的程序。默认命令是weave</td>
</tr>
<tr class="odd">
<td style="text-align: left;">CWEAVE</td>
<td style="text-align: left;">转换C Web 到TeX
的程序。默认命令是cweave</td>
</tr>
<tr class="even">
<td style="text-align: left;">CTANGLE</td>
<td style="text-align: left;">转换C Web 到C。默认命令是ctangle</td>
</tr>
<tr class="odd">
<td style="text-align: left;">RM</td>
<td style="text-align: left;">删除文件命令。默认命令是rm –f</td>
</tr>
</tbody>
</table>
<h4 id="关于命令参数的变量">关于命令参数的变量</h4>
<p>下面的这些变量都是相关上面的命令的参数。如果没有指明其默认值，那么其默认值都是空。</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">命令参数的变量</th>
<th style="text-align: left;">解释 | 默认命令</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;">ARFLAGS</td>
<td style="text-align: left;">函数库打包程序AR
命令的参数。默认值是rv</td>
</tr>
<tr class="even">
<td style="text-align: left;">ASFLAGS</td>
<td style="text-align: left;">汇编语言编译器参数。（当明显地调用.s 或.S
文件时）</td>
</tr>
<tr class="odd">
<td style="text-align: left;">CFLAGS</td>
<td style="text-align: left;">C 语言编译器参数</td>
</tr>
<tr class="even">
<td style="text-align: left;">CXXFLAGS</td>
<td style="text-align: left;">C++ 语言编译器参数</td>
</tr>
<tr class="odd">
<td style="text-align: left;">COFLAGS</td>
<td style="text-align: left;">RCS 命令参数</td>
</tr>
<tr class="even">
<td style="text-align: left;">CPPFLAGS</td>
<td style="text-align: left;">C 预处理器参数（C 和Fortran
编译器也会用到）</td>
</tr>
<tr class="odd">
<td style="text-align: left;">FFLAGS</td>
<td style="text-align: left;">Fortran 语言编译器参数</td>
</tr>
<tr class="even">
<td style="text-align: left;">GFLAGS</td>
<td style="text-align: left;">SCCS “get”程序参数</td>
</tr>
<tr class="odd">
<td style="text-align: left;">LDFLAGS</td>
<td style="text-align: left;">链接器参数（如：ld ）</td>
</tr>
<tr class="even">
<td style="text-align: left;">LFLAGS</td>
<td style="text-align: left;">Lex 文法分析器参数</td>
</tr>
<tr class="odd">
<td style="text-align: left;">PFLAGS</td>
<td style="text-align: left;">Pascal 语言编译器参数</td>
</tr>
<tr class="even">
<td style="text-align: left;">RFLAGS</td>
<td style="text-align: left;">Ratfor 程序的Fortran 编译器参数</td>
</tr>
<tr class="odd">
<td style="text-align: left;">YFLAGS</td>
<td style="text-align: left;">Yacc 文法分析器参数</td>
</tr>
</tbody>
</table>
<h3 id="定义模式规则">定义模式规则</h3>
<h4 id="自动化变量">自动化变量</h4>
<p>下面是所有的自动化变量及其说明：</p>
<table>
<colgroup>
<col style="width: 14%" />
<col style="width: 85%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">自动化变量</th>
<th style="text-align: left;">说明</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><code>$@</code></td>
<td style="text-align: left;">表示规则中的目标文件集。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>$%</code></td>
<td
style="text-align: left;">仅当目标是函数库文件中，表示规则中的目标成员名。例如，如果一个目标是foo.a(bar.o)，那么，<code>$%</code>
就是bar.o ，<code>$@</code> 就是foo.a 。如果目标不是函数库文件（Unix
下是.a ，Windows下是.lib），那么，其值为空。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>$&lt;</code></td>
<td
style="text-align: left;">依赖目标中的第一个目标名字。如果依赖目标是以模式（即<code>%</code>）定义的，那么<code>$&lt;</code>将是符合模式的一系列的文件集。注意，其是一个一个取出来的。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>$?</code></td>
<td
style="text-align: left;">所有比目标新的依赖目标的集合。以空格分隔。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>$^</code></td>
<td
style="text-align: left;">所有的依赖目标的集合。以空格分隔。如果在依赖目标中有多个重复的，那个这个变量会去除重复的依赖目标，只保留一份。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>$+</code></td>
<td style="text-align: left;">这个变量很像<code>$^</code>
，也是所有依赖目标的集合。只是它不去除重复的依赖目标。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>$*</code></td>
<td style="text-align: left;">这个变量表示目标模式中<code>%</code>
及其之前的部分。如果目标是<code>dir/a.foo.b</code>
，并且目标的模式是<code>a.%.b</code> ，那么，<code>$*</code>
的值就是<code>dir/a.foo</code> 。</td>
</tr>
</tbody>
</table>
<p>在上述所列出来的自动量变量中。四个变量（<code>$@</code>
、<code>$&lt;</code> 、<code>$%</code> 、<code>$*</code>
）在扩展时只会有一个文件，而另三个的值是一个文件列表。这七个自动化变量还可以取得文件的目录名或是在当前目录下的符合模式的文件名，只需要搭配上D
或F 字样。这是GNU make 中老版本的特性，在新版本中，我们使用函数dir
或notdir 就可以做到了。D 的含义就是Directory，就是目录，F
的含义就是File，就是文件。</p>
<p>这些变量只使用在<strong>规则的命令中</strong>，而且一般都是“显式规则”和“静态模式规则”。其在隐含规则中并没有意义。</p>
<hr />
<h2 id="使用make更新函数库文件">使用make更新函数库文件</h2>
<h3 id="函数库文件的成员">函数库文件的成员</h3>
<p>一个函数库文件由多个文件组成。你可以用如下格式指定函数库文件及其组成:</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">archive(member)</span><br></pre></td></tr></table></figure>
<p>这个不是一个命令，而一个目标和依赖的定义。一般来说，这种用法基本上就是为了ar
命令来服务的。如:</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">foolib(hack.o) : hack.o</span><br><span class="line">  ar cr foolib hack.o</span><br></pre></td></tr></table></figure>
<p>如果要指定多个member，那就以空格分开，如:
<code>foolib(hack.o kludge.o)</code>，其等价于<code>foolib(hack.o) foolib(kludge.o)</code></p>
<p>还可以使用Shell的文件通配符来定义,如：</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">foolib(*.o)</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="模板">模板</h2>
<h3 id="每个.c编译成.o再链接在一起">每个.c编译成.o再链接在一起</h3>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">OUT:=main</span><br><span class="line">SRCS:=<span class="variable">$(<span class="built_in">wildcard</span> *.c)</span></span><br><span class="line">OBJS:=<span class="variable">$(<span class="built_in">patsubst</span> %.c,%.o,<span class="variable">$(SRCS)</span>)</span></span><br><span class="line">CC:=gcc</span><br><span class="line"><span class="variable">$(OUT)</span>:<span class="variable">$(OBJS)</span></span><br><span class="line">  <span class="variable">$(CC)</span> <span class="variable">$^</span> -o <span class="variable">$@</span> -lpthread</span><br><span class="line"><span class="section">%.o:%.c</span></span><br><span class="line">  <span class="variable">$(CC)</span> -c <span class="variable">$^</span> -o <span class="variable">$@</span> -g</span><br><span class="line"><span class="meta"><span class="keyword">.PHONY</span>:clean rebuild</span></span><br><span class="line"><span class="section">clean:</span></span><br><span class="line">  <span class="variable">$(RM)</span> <span class="variable">$(OBJS)</span> <span class="variable">$(OUT)</span></span><br><span class="line"><span class="section">rebuild: clean main</span></span><br></pre></td></tr></table></figure>
<h3
id="每个.c文件各自编译链接成可执行文件">每个.c文件各自编译链接成可执行文件</h3>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">SRCS:=<span class="variable">$(<span class="built_in">wildcard</span> *.c)</span></span><br><span class="line">EXES:=<span class="variable">$(<span class="built_in">patsubst</span> %.c,%,<span class="variable">$(SRCS)</span>)</span></span><br><span class="line">CC:=gcc</span><br><span class="line"><span class="section">all:<span class="variable">$(EXES)</span></span></span><br><span class="line"><span class="section">%:%.c</span></span><br><span class="line">  <span class="variable">$(CC)</span> <span class="variable">$^</span> -o <span class="variable">$@</span> -g</span><br><span class="line"><span class="section">clean:</span></span><br><span class="line">  <span class="variable">$(RM)</span> <span class="variable">$(EXES)</span></span><br><span class="line"><span class="section">rebuild:clean all</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="参考博文">参考博文</h2>
<ol type="1">
<li><span class="exturl" data-url="aHR0cHM6Ly93d3cuY25ibG9ncy5jb20vcGF1bC02MTcvcC8xNTUwMTg3NS5odG1s">makefile详解<i class="fa fa-external-link-alt"></i></span></li>
<li>跟我一起写Makefile.陈皓.</li>
</ol>
]]></content>
      <categories>
        <category>工具与工程实践（Tooling &amp; Engineering）</category>
        <category>构建与编译系统</category>
      </categories>
      <tags>
        <tag>Makefile</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux之生成和使用静态库与动态库</title>
    <url>/2024/02/17/tools/misc/linux-static-dynamic-lib/</url>
    <content><![CDATA[<p>本文介绍了Linux环境下如何生成和使用静态库和动态库。</p>
<span id="more"></span>
<hr />
<h2 id="动态库-vs-静态库">动态库 vs 静态库</h2>
<table>
<thead>
<tr class="header">
<th><strong>特性</strong></th>
<th><strong>动态库（.so）</strong></th>
<th><strong>静态库（.a）</strong></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>链接方式</strong></td>
<td>运行时动态加载</td>
<td>编译时直接嵌入可执行文件</td>
</tr>
<tr class="even">
<td><strong>文件大小</strong></td>
<td>可执行文件较小</td>
<td>可执行文件较大</td>
</tr>
<tr class="odd">
<td><strong>更新方式</strong></td>
<td>替换 <code>.so</code> 文件即可生效</td>
<td>需重新编译程序</td>
</tr>
<tr class="even">
<td><strong>加载速度</strong></td>
<td>稍慢（需运行时加载）</td>
<td>更快（代码已在二进制中）</td>
</tr>
<tr class="odd">
<td><strong>适用场景</strong></td>
<td>多进程共享、热更新</td>
<td>独立部署、无依赖</td>
</tr>
</tbody>
</table>
<h2 id="静态库">静态库</h2>
<p>在 Linux/Unix 系统中，静态库（Static
Library）是一种包含多个目标文件（<code>.o</code>
文件）的归档文件，通常以 <code>.a</code>
结尾（Archive）。静态库在编译时会被直接链接到可执行文件中，使得程序运行时不再依赖外部库文件。以下是生成静态库的详细步骤：</p>
<hr />
<h3 id="准备源文件">准备源文件</h3>
<p>假设有两个源文件：</p>
<ul>
<li><code>file1.c</code></li>
<li><code>file2.c</code></li>
</ul>
<h4 id="示例代码"><strong>示例代码</strong></h4>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file1.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">func1</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;This is func1()\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file2.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">func2</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;This is func2()\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h3 id="编译生成目标文件.o-文件">编译生成目标文件（<code>.o</code>
文件）</h3>
<p>使用 <code>gcc</code> 或 <code>clang</code>
编译源文件，生成位置无关的目标文件：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc -c file1.c -o file1.o</span><br><span class="line">gcc -c file2.c -o file2.o</span><br></pre></td></tr></table></figure>
<ul>
<li><code>-c</code>：只编译不链接，生成 <code>.o</code> 文件。</li>
<li><code>-o</code>：指定输出文件名。</li>
</ul>
<blockquote>
<p>🔍 <strong>注意</strong>：静态库不需要
<code>-fPIC</code>（位置无关代码），因为代码会被直接嵌入可执行文件。</p>
</blockquote>
<hr />
<h3 id="使用-ar-打包成静态库">使用 <code>ar</code> 打包成静态库</h3>
<p>静态库的命名规范为 <code>lib&lt;name&gt;.a</code>（例如
<code>libmylib.a</code>）。<br />
使用 <code>ar</code>（归档工具）的 <code>rcs</code> 选项：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ar rcs libmylib.a file1.o file2.o</span><br></pre></td></tr></table></figure>
<ul>
<li><strong><code>r</code></strong>：替换或添加文件到库中。</li>
<li><strong><code>c</code></strong>：静默创建库（不显示警告）。</li>
<li><strong><code>s</code></strong>：生成索引（加快链接速度）。</li>
</ul>
<hr />
<h3 id="验证静态库">验证静态库</h3>
<h4 id="查看库中包含的-.o-文件"><strong>查看库中包含的 <code>.o</code>
文件</strong></h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ar -t libmylib.a</span><br></pre></td></tr></table></figure>
<p>输出：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">file1.o</span><br><span class="line">file2.o</span><br></pre></td></tr></table></figure>
<h4
id="查看导出的符号函数变量"><strong>查看导出的符号（函数/变量）</strong></h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nm libmylib.a</span><br></pre></td></tr></table></figure>
<p>输出示例：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">file1.o:</span><br><span class="line">0000000000000000 T func1</span><br><span class="line"></span><br><span class="line">file2.o:</span><br><span class="line">0000000000000000 T func2</span><br></pre></td></tr></table></figure>
<ul>
<li><code>T</code> 表示该符号在库中定义。</li>
</ul>
<hr />
<h3 id="使用静态库">使用静态库</h3>
<h4 id="编译并链接静态库"><strong>编译并链接静态库</strong></h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc main.c -L. -lmylib -o myprogram</span><br></pre></td></tr></table></figure>
<ul>
<li><strong><code>-L.</code></strong>：指定库的搜索路径（<code>.</code>
表示当前目录）。</li>
<li><strong><code>-lmylib</code></strong>：链接
<code>libmylib.a</code>（省略 <code>lib</code> 和
<code>.a</code>）。</li>
</ul>
<h4 id="运行程序"><strong>运行程序</strong></h4>
<p>静态库已嵌入可执行文件，直接运行即可：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./myprogram</span><br></pre></td></tr></table></figure>
<hr />
<h3 id="高级操作">高级操作</h3>
<h4 id="添加新文件到静态库"><strong>添加新文件到静态库</strong></h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ar rcs libmylib.a newfile.o</span><br></pre></td></tr></table></figure>
<h4 id="从静态库中删除文件"><strong>从静态库中删除文件</strong></h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ar d libmylib.a file1.o</span><br></pre></td></tr></table></figure>
<h4 id="更新库中的文件"><strong>更新库中的文件</strong></h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ar r libmylib.a file1.o  <span class="comment"># 重新添加 file1.o</span></span><br></pre></td></tr></table></figure>
<hr />
<h3 id="完整示例流程">完整示例流程</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 编译源文件</span></span><br><span class="line">gcc -c file1.c -o file1.o</span><br><span class="line">gcc -c file2.c -o file2.o</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 打包静态库</span></span><br><span class="line">ar rcs libmylib.a file1.o file2.o</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 使用静态库</span></span><br><span class="line">gcc main.c -L. -lmylib -o myprogram</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 运行</span></span><br><span class="line">./myprogram</span><br></pre></td></tr></table></figure>
<hr />
<h3 id="常见问题">常见问题</h3>
<h4
id="q1静态库和动态库能否混合使用"><strong>Q1：静态库和动态库能否混合使用？</strong></h4>
<p>可以，但需确保符号冲突已解决。例如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc main.c -L. -lmylib -ldynamiclib -o myprogram</span><br></pre></td></tr></table></figure>
<h4
id="q2如何查看可执行文件依赖的库"><strong>Q2：如何查看可执行文件依赖的库？</strong></h4>
<p>对静态库编译的程序：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ldd myprogram  <span class="comment"># 静态库部分不会显示，动态库会列出</span></span><br></pre></td></tr></table></figure>
<h4 id="q3为什么静态库不需要--fpic"><strong>Q3：为什么静态库不需要
<code>-fPIC</code>？</strong></h4>
<p>因为静态库代码会被直接复制到可执行文件中，地址在链接时固定。</p>
<hr />
<h3 id="总结">总结</h3>
<ol type="1">
<li><strong>编译 <code>.o</code>
文件</strong>：<code>gcc -c file1.c -o file1.o</code><br />
</li>
<li><strong>打包静态库</strong>：<code>ar rcs lib&lt;name&gt;.a *.o</code><br />
</li>
<li><strong>使用静态库</strong>：<code>gcc main.c -L. -l&lt;name&gt; -o program</code></li>
</ol>
<p>静态库适合需要 <strong>独立部署、避免外部依赖</strong>
的场景，而动态库更适合 <strong>节省空间和多进程共享</strong>。</p>
<hr />
<h2 id="动态库">动态库</h2>
<p>在 Linux/Unix 系统中，动态库（Dynamic Library，也称为共享库，Shared
Library）通常以 <code>.so</code>（Shared
Object）结尾。动态库在程序运行时被加载，而不是在编译时静态链接到可执行文件中，这使得它们更节省磁盘和内存空间，并支持热更新。以下是生成和使用动态库的详细步骤。</p>
<hr />
<h3 id="准备源文件-1">准备源文件</h3>
<p>假设有两个源文件：</p>
<ul>
<li><code>file1.c</code></li>
<li><code>file2.c</code></li>
</ul>
<h4 id="示例代码-1"><strong>示例代码</strong></h4>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file1.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">func1</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;This is func1()\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file2.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">func2</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;This is func2()\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h3 id="编译生成位置无关代码pic">编译生成位置无关代码（PIC）</h3>
<p>动态库需要编译为 <strong>位置无关代码（Position-Independent Code,
PIC）</strong>，以便在运行时加载到任意内存地址。使用 <code>-fPIC</code>
选项：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc -c -fPIC file1.c -o file1.o</span><br><span class="line">gcc -c -fPIC file2.c -o file2.o</span><br></pre></td></tr></table></figure>
<ul>
<li><code>-fPIC</code>：生成位置无关代码（必须加，否则链接时会报错）。</li>
</ul>
<hr />
<h3 id="生成动态库.so-文件">生成动态库（<code>.so</code> 文件）</h3>
<p>使用 <code>gcc</code> 的 <code>-shared</code> 选项将 <code>.o</code>
文件打包成动态库：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc -shared file1.o file2.o -o libmylib.so</span><br></pre></td></tr></table></figure>
<ul>
<li><code>-shared</code>：告诉 <code>gcc</code> 生成动态库。</li>
<li><code>-o libmylib.so</code>：指定输出文件名（通常格式为
<code>lib&lt;name&gt;.so</code>）。</li>
</ul>
<hr />
<h3 id="验证动态库">验证动态库</h3>
<h4
id="查看动态库的符号导出的函数"><strong>查看动态库的符号（导出的函数）</strong></h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nm -D libmylib.so</span><br></pre></td></tr></table></figure>
<p>输出示例：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0000000000001110 T func1</span><br><span class="line">0000000000001125 T func2</span><br></pre></td></tr></table></figure>
<ul>
<li><code>T</code> 表示该符号在库中定义。</li>
</ul>
<h4 id="检查动态库的依赖"><strong>检查动态库的依赖</strong></h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ldd libmylib.so</span><br></pre></td></tr></table></figure>
<p>输出示例：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">linux-vdso.so.1 (0x00007ffd3a3f0000)</span><br><span class="line">libc.so.6 =&gt; /lib/x86_64-linux-gnu/libc.so.6 (0x00007f8c3a3f0000)</span><br><span class="line">/lib64/ld-linux-x86-64.so.2 (0x00007f8c3a3f0000)</span><br></pre></td></tr></table></figure>
<hr />
<h3 id="使用动态库">使用动态库</h3>
<h4
id="编译可执行文件并链接动态库"><strong>编译可执行文件并链接动态库</strong></h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc main.c -L. -lmylib -o myprogram</span><br></pre></td></tr></table></figure>
<ul>
<li><code>-L.</code>：告诉编译器在当前目录查找库文件。</li>
<li><code>-lmylib</code>：链接 <code>libmylib.so</code>（<code>-l</code>
后接库名，省略 <code>lib</code> 和 <code>.so</code>）。</li>
</ul>
<h4 id="运行程序-1"><strong>运行程序</strong></h4>
<p>由于动态库在运行时加载，需要确保系统能找到它：</p>
<h5 id="方法-1临时设置-ld_library_path"><strong>方法 1：临时设置
<code>LD_LIBRARY_PATH</code></strong></h5>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> LD_LIBRARY_PATH=.:<span class="variable">$LD_LIBRARY_PATH</span></span><br><span class="line">./myprogram</span><br></pre></td></tr></table></figure>
<ul>
<li><code>LD_LIBRARY_PATH</code> 指定动态库的搜索路径。</li>
</ul>
<h5 id="方法-2永久配置推荐"><strong>方法
2：永久配置（推荐）</strong></h5>
<p>将库文件复制到标准库路径（如
<code>/usr/local/lib</code>），然后更新动态库缓存：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">cp</span> libmylib.so /usr/local/lib</span><br><span class="line"><span class="built_in">sudo</span> ldconfig</span><br></pre></td></tr></table></figure>
<p>然后直接运行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./myprogram</span><br></pre></td></tr></table></figure>
<hr />
<h3 id="高级用法">高级用法</h3>
<h4 id="指定动态库版本"><strong>指定动态库版本</strong></h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc -shared file1.o file2.o -o libmylib.so.1.0</span><br><span class="line"><span class="built_in">ln</span> -s libmylib.so.1.0 libmylib.so  <span class="comment"># 创建软链接</span></span><br></pre></td></tr></table></figure>
<ul>
<li>版本号格式通常为 <code>libname.so.major.minor</code>。</li>
</ul>
<h4 id="控制符号导出"><strong>控制符号导出</strong></h4>
<p>在代码中使用 <code>__attribute__((visibility("hidden")))</code>
隐藏符号：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file1.c</span></span><br><span class="line"><span class="type">void</span> __attribute__((visibility(<span class="string">&quot;hidden&quot;</span>))) internal_func() &#123;</span><br><span class="line">    <span class="comment">// 该函数不会导出，仅在库内部使用</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4
id="动态库的加载卸载dlopen-dlclose"><strong>动态库的加载卸载（<code>dlopen</code>
/ <code>dlclose</code>）</strong></h4>
<p>在程序运行时手动加载动态库：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;dlfcn.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">void</span> *handle = dlopen(<span class="string">&quot;./libmylib.so&quot;</span>, RTLD_LAZY);</span><br><span class="line">    <span class="keyword">if</span> (!handle) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Error: %s\n&quot;</span>, dlerror());</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">void</span> (*func1)() = dlsym(handle, <span class="string">&quot;func1&quot;</span>);</span><br><span class="line">    func1();</span><br><span class="line"></span><br><span class="line">    dlclose(handle);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>编译时需要链接 <code>-ldl</code>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc main.c -ldl -o myprogram</span><br></pre></td></tr></table></figure>
<hr />
<h3 id="总结-1">总结</h3>
<ol type="1">
<li><strong>编译 PIC
代码</strong>：<code>gcc -c -fPIC file1.c -o file1.o</code><br />
</li>
<li><strong>生成动态库</strong>：<code>gcc -shared *.o -o lib&lt;name&gt;.so</code><br />
</li>
<li><strong>使用动态库</strong>：
<ul>
<li>编译时：<code>gcc main.c -L. -l&lt;name&gt;</code><br />
</li>
<li>运行时：确保 <code>LD_LIBRARY_PATH</code> 包含库路径，或复制到
<code>/usr/local/lib</code> 并运行 <code>ldconfig</code>。</li>
</ul></li>
</ol>
<p>动态库适合需要 <strong>多进程共享、减少磁盘占用、支持热更新</strong>
的场景。</p>
]]></content>
      <categories>
        <category>Linux</category>
        <category>基础知识</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>库文件</tag>
      </tags>
  </entry>
  <entry>
    <title>Markdown写作常用组件</title>
    <url>/2024/06/17/tools/misc/markdown-writing-widgets/</url>
    <content><![CDATA[<p>本文总结了Markdown中一些基本的语法和经常使用的组件环境，方便在写作过程中随时复制使用。</p>
<span id="more"></span>
<hr />
<h2 id="基本语法">基本语法</h2>
<h3 id="标题">标题</h3>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="section"># Heading level 1  </span></span><br><span class="line"><span class="section">## Heading level 2</span></span><br><span class="line"><span class="section">### Heading level 3</span></span><br><span class="line"><span class="section">#### Heading level 4</span></span><br><span class="line"><span class="section">##### Heading level 5</span></span><br><span class="line"><span class="section">###### Heading level 6</span></span><br></pre></td></tr></table></figure>
<h3 id="段落">段落</h3>
<p>要创建段落，请使用<strong>空白行</strong>将一行或多行文本进行分隔。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">I really like using Markdown.</span><br><span class="line"></span><br><span class="line">I think I&#x27;ll use it to format all of my documents from now on.</span><br></pre></td></tr></table></figure>
<h3 id="换行">换行</h3>
<p>在一行的末尾<strong>添加两个或多个空格</strong>，然后按回车键，即可创建一个换行(<code>&lt;br&gt;</code>)。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">This is the first line.  </span><br><span class="line">And this is the second line.</span><br></pre></td></tr></table></figure>
<h3 id="强调">强调</h3>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="strong">**粗体**</span></span><br><span class="line"><span class="emphasis">*斜体*</span></span><br><span class="line"><span class="strong">**<span class="emphasis">*粗斜体*</span>**</span></span><br></pre></td></tr></table></figure>
<p><strong>粗体</strong></p>
<p><em>斜体</em></p>
<p><strong><em>粗斜体</em></strong></p>
<h3 id="引用">引用</h3>
<p>要创建块引用，请在段落前添加一个 <code>&gt;</code> 符号。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="quote">&gt; Dorothy followed her through many of the beautiful rooms in her castle.</span></span><br><span class="line"><span class="quote">&gt;</span></span><br><span class="line"><span class="quote">&gt; The Witch bade her clean the pots and kettles and sweep the floor and keep the fire fed with wood.</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>Dorothy followed her through many of the beautiful rooms in her
castle.</p>
<p>The Witch bade her clean the pots and kettles and sweep the floor and
keep the fire fed with wood.</p>
</blockquote>
<p>嵌套块引用</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="quote">&gt; Dorothy followed her through many of the beautiful rooms in her castle.</span></span><br><span class="line"><span class="quote">&gt;</span></span><br><span class="line"><span class="quote">&gt;&gt; The Witch bade her clean the pots and kettles and sweep the floor and keep the fire fed with wood.</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>Dorothy followed her through many of the beautiful rooms in her
castle.</p>
<blockquote>
<p>The Witch bade her clean the pots and kettles and sweep the floor and
keep the fire fed with wood.</p>
</blockquote>
</blockquote>
<h3 id="列表">列表</h3>
<p>要在保留列表连续性的同时在列表中添加另一种元素（段落，引用块、代码块、图片、列表），请将该元素缩进四个空格或一个制表符，如下例所示：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="bullet">1.</span> First item</span><br><span class="line"><span class="bullet">2.</span> Second item</span><br><span class="line"><span class="bullet">3.</span> Third item</span><br><span class="line"><span class="bullet">    -</span> Indented item</span><br><span class="line"><span class="bullet">    -</span> Indented item</span><br><span class="line"><span class="bullet">4.</span> Fourth item</span><br></pre></td></tr></table></figure>
<ol type="1">
<li>First item</li>
<li>Second item</li>
<li>Third item
<ul>
<li>Indented item</li>
<li>Indented item</li>
</ul></li>
<li>Fourth item</li>
</ol>
<h3 id="行中代码">行中代码</h3>
<p>要将单词或短语表示为代码，请将其包裹在反引号 (`)
中。转义反引号可以通过将单词或短语包裹在双反引号(``)中。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">At the command prompt, type <span class="code">`nano`</span>.</span><br><span class="line"></span><br><span class="line"><span class="code">``Use `</span>code<span class="code">` in your Markdown file.`</span>`</span><br></pre></td></tr></table></figure>
<p>At the command prompt, type <code>nano</code>.</p>
<p><code>Use `code` in your Markdown file.</code></p>
<h3 id="分隔线">分隔线</h3>
<p>为了兼容性，请在分隔线的前后均添加空白行。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">Try to put a blank line before...</span><br><span class="line"></span><br><span class="line">---</span><br><span class="line"></span><br><span class="line">...and after a horizontal rule.</span><br></pre></td></tr></table></figure>
<h3 id="链接">链接</h3>
<h4 id="基本语法-1">基本语法</h4>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">这是一个链接 [<span class="string">Markdown语法</span>](<span class="link">https://markdown.com.cn</span>)。</span><br></pre></td></tr></table></figure>
<p>这是一个链接 <span class="exturl" data-url="aHR0cHM6Ly9tYXJrZG93bi5jb20uY24=">Markdown语法<i class="fa fa-external-link-alt"></i></span>。</p>
<h4 id="给链接增加-title">给链接增加 Title</h4>
<p>链接title是当鼠标悬停在链接上时会出现的文字，这个title是可选的，它放在圆括号中链接地址后面，跟链接地址之间以空格分隔。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">这是一个链接 [<span class="string">Markdown语法</span>](<span class="link">https://markdown.com.cn &quot;最好的markdown教程&quot;</span>)。</span><br></pre></td></tr></table></figure>
<p>这是一个链接 <span class="exturl" data-url="aHR0cHM6Ly9tYXJrZG93bi5jb20uY24=" title="最好的markdown教程">Markdown语法<i class="fa fa-external-link-alt"></i></span>。</p>
<h4 id="网址和email地址">网址和Email地址</h4>
<p>使用尖括号可以很方便地把URL或者email地址变成可点击的链接。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;https://markdown.com.cn&gt;</span><br><span class="line">&lt;fake@example.com&gt;</span><br></pre></td></tr></table></figure>
<p><span class="exturl" data-url="aHR0cHM6Ly9tYXJrZG93bi5jb20uY24=">https://markdown.com.cn<i class="fa fa-external-link-alt"></i></span></p>
<p><span class="exturl" data-url="bWFpbHRvOmZha2VAZXhhbXBsZS5jb20=">fake@example.com<i class="fa fa-external-link-alt"></i></span></p>
<h4 id="带格式化的链接">带格式化的链接</h4>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">I love supporting the **[EFF](https://eff.org)**.</span><br><span class="line">This is the *[Markdown Guide](https://www.markdownguide.org)*.</span><br></pre></td></tr></table></figure>
<p>I love supporting the <strong><span class="exturl" data-url="aHR0cHM6Ly9lZmYub3Jn">EFF<i class="fa fa-external-link-alt"></i></span></strong>.</p>
<p>This is the <em><span class="exturl" data-url="aHR0cHM6Ly93d3cubWFya2Rvd25ndWlkZS5vcmc=">Markdown
Guide<i class="fa fa-external-link-alt"></i></span></em>.</p>
<h3 id="图片">图片</h3>
<p>要添加图像，请使用感叹号 (<code>!</code>),
然后在方括号增加替代文本，图片链接（可以是本地或网页图片）放在圆括号里，括号里的链接后可以增加一个<strong>可选的图片标题文本。</strong></p>
<p>插入图片Markdown语法代码：<code>![图片alt](图片链接 "图片title")</code>。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">![<span class="string">这是图片</span>](<span class="link">/assets/img/philly-magic-garden.jpg &quot;Magic Gardens&quot;</span>)</span><br></pre></td></tr></table></figure>
<h3 id="转义字符">转义字符</h3>
<p>要显示原本用于格式化 Markdown 文档的字符，请在字符前面添加反斜杠字符
<code>\</code>。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">\* Without the backslash, this would be a bullet in an unordered list.</span><br></pre></td></tr></table></figure>
<p>渲染效果如下： * Without the backslash, this would be a bullet in an
unordered list.</p>
<h4 id="可做转义的字符">可做转义的字符</h4>
<table>
<thead>
<tr class="header">
<th>Character</th>
<th>Name</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>\</td>
<td>backslash</td>
</tr>
<tr class="even">
<td>`</td>
<td>backtick</td>
</tr>
<tr class="odd">
<td>*</td>
<td>asterisk</td>
</tr>
<tr class="even">
<td>_</td>
<td>underscore</td>
</tr>
<tr class="odd">
<td>{}</td>
<td>curly braces</td>
</tr>
<tr class="even">
<td>[]</td>
<td>brackets</td>
</tr>
<tr class="odd">
<td>()</td>
<td>parentheses</td>
</tr>
<tr class="even">
<td>#</td>
<td>pound sign</td>
</tr>
<tr class="odd">
<td>+</td>
<td>plus sign</td>
</tr>
<tr class="even">
<td>-</td>
<td>minus sign (hyphen)</td>
</tr>
<tr class="odd">
<td>.</td>
<td>dot</td>
</tr>
<tr class="even">
<td>!</td>
<td>exclamation mark</td>
</tr>
<tr class="odd">
<td>|</td>
<td>pipe</td>
</tr>
</tbody>
</table>
<h4 id="特殊字符自动转义">特殊字符自动转义</h4>
<p>在 HTML 文件中，有两个字符需要特殊处理： <code>&lt;</code> 和
<code>&amp;</code> 。 <code>&lt;</code>
符号用于起始标签，<code>&amp;</code> 符号则用于标记 HTML
实体，如果你只是想要使用这些符号，你必须要使用实体的形式，像是
<code>&amp;lt;</code> 和 <code>&amp;amp;</code>，对应的
<code>&gt;</code> 是 <code>&amp;gt;</code>。</p>
<p>在 Markdown 的块级元素和内联元素中， <code>&lt;</code> 和
<code>&amp;</code> 两个符号都会被自动转换。</p>
<p>在 HTML 语法（包括在 Markdown 文件中使用的 HTML
语法）中，要手动把所有的 <code>&lt;</code> 和 <code>&amp;</code>
都转换为 HTML 实体。</p>
<h3 id="markdown-内嵌-html-标签">Markdown 内嵌 HTML 标签</h3>
<h4 id="行级內联标签">行级內联标签</h4>
<p>HTML 的行级內联标签如
<code>&lt;span&gt;</code>、<code>&lt;cite&gt;</code>、<code>&lt;del&gt;</code>
不受限制，可以在 Markdown 的段落、列表或是标题里任意使用。HTML
行级內联标签和区块标签不同，在內联标签的范围内， Markdown
的语法是可以解析的。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">This <span class="strong">**word**</span> is bold. This <span class="language-xml"><span class="tag">&lt;<span class="name">em</span>&gt;</span></span>word<span class="language-xml"><span class="tag">&lt;/<span class="name">em</span>&gt;</span></span> is italic.</span><br></pre></td></tr></table></figure>
<p>渲染效果如下: This <strong>word</strong> is bold. This <em>word</em>
is italic.</p>
<h4 id="区块标签">区块标签</h4>
<p>区块元素──比如
<code>&lt;div&gt;</code>、<code>&lt;table&gt;</code>、<code>&lt;pre&gt;</code>、<code>&lt;p&gt;</code>
等标签，必须在<strong>前后加上空行</strong>，以便于内容区分。而且<strong>这些标签的开始与结尾标签，不可以用
tab 或是空白来缩进</strong>。Markdown
会自动识别这区块元素，避免在区块标签前后加上没有必要的
<code>&lt;p&gt;</code> 标签。 例如，在 Markdown 文件里加上一段 HTML
表格：.</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">This is a regular paragraph.</span><br><span class="line"></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">table</span>&gt;</span></span></span><br><span class="line"><span class="code">    &lt;tr&gt;</span></span><br><span class="line"><span class="code">        &lt;td&gt;Foo&lt;/td&gt;</span></span><br><span class="line"><span class="code">    &lt;/tr&gt;</span></span><br><span class="line"><span class="code">&lt;/table&gt;</span></span><br><span class="line"><span class="code"></span></span><br><span class="line">This is another regular paragraph.</span><br></pre></td></tr></table></figure>
<p>请注意，Markdown 语法在 HTML
<strong>区块标签</strong>中将不会被进行处理。</p>
<hr />
<p><a id="sec2"></a></p>
<h2 id="扩展语法">扩展语法</h2>
<h3 id="表格">表格</h3>
<p>要添加表，请使用三个或多个连字符（<code>---</code>）创建每列的标题，并使用管道（<code>|</code>）分隔每列。您可以选择在表的任一端添加管道。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">| Syntax      | Description |</span><br><span class="line">| ----------- | ----------- |</span><br><span class="line">| Header      | Title       |</span><br><span class="line">| Paragraph   | Text        |</span><br></pre></td></tr></table></figure>
<p>呈现的输出如下所示：</p>
<table>
<thead>
<tr class="header">
<th>Syntax</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Header</td>
<td>Title</td>
</tr>
<tr class="even">
<td>Paragraph</td>
<td>Text</td>
</tr>
</tbody>
</table>
<p>可以在表格中设置文本格式。例如，您可以添加链接，代码（仅反引号（`）中的单词或短语，而不是代码块）和强调。
您不能添加标题，块引用，列表，水平规则，图像或HTML标签。</p>
<p>若想在表格中打出（|）字符，可以使用表格的HTML字符代码（<code>&amp;#124;</code>）来转义管道字符（|）。</p>
<h3 id="围栏代码块">围栏代码块</h3>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="code">```c</span></span><br><span class="line"><span class="code">#include &lt;stdio.h&gt;</span></span><br><span class="line"><span class="code"></span></span><br><span class="line"><span class="code">int main()&#123;</span></span><br><span class="line"><span class="code">  printf(&quot;Hello,World!\n&quot;);</span></span><br><span class="line"><span class="code">  </span></span><br><span class="line"><span class="code">  return0;</span></span><br><span class="line"><span class="code">&#125;</span></span><br><span class="line"><span class="code">```</span></span><br></pre></td></tr></table></figure>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Hello,World!\n&quot;</span>);</span><br><span class="line">  </span><br><span class="line">  return0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="脚注">脚注</h3>
<p>要创建脚注参考，请在方括号（<code>[^1]</code>）内添加插入符号和标识符。标识符可以是数字或单词，但不能包含空格或制表符。
在括号内使用另一个插入符号和数字添加脚注，并用冒号和文本（<code>[^1]: My footnote.</code>）。不必在文档末尾添加脚注。可以将它们放在除列表，块引号和表之类的其他元素之外的任何位置。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">Here&#x27;s a simple footnote,[^1] and here&#x27;s a longer one.[^bignote]</span><br><span class="line"></span><br><span class="line">[<span class="symbol">^1</span>]: <span class="link">This is the first footnote.</span></span><br><span class="line"></span><br><span class="line">[<span class="symbol">^bignote</span>]: <span class="link">Here&#x27;s one with multiple paragraphs and code.</span></span><br><span class="line"></span><br><span class="line"><span class="code">    Indent paragraphs to include them in the footnote.</span></span><br><span class="line"><span class="code"></span></span><br><span class="line"><span class="code">    `&#123; my code &#125;`</span></span><br><span class="line"><span class="code"></span></span><br><span class="line"><span class="code">    Add as many paragraphs as you like.</span></span><br></pre></td></tr></table></figure>
<p>呈现的输出效果如下：</p>
<p>Here’s a simple footnote,<a href="#fn1" class="footnote-ref"
id="fnref1" role="doc-noteref"><sup>1</sup></a> and here’s a longer
one.<a href="#fn2" class="footnote-ref" id="fnref2"
role="doc-noteref"><sup>2</sup></a></p>
<h3 id="删除线">删除线</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">~~世界是平坦的。~~ 我们现在知道世界是圆的。</span><br></pre></td></tr></table></figure>
<p><del>世界是平坦的。</del> 我们现在知道世界是圆的。</p>
<h3 id="任务列表">任务列表</h3>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="bullet">-</span> [x] Write the press release</span><br><span class="line"><span class="bullet">-</span> [ ] Update the website</span><br><span class="line"><span class="bullet">-</span> [ ] Contact the media</span><br></pre></td></tr></table></figure>
<ul class="task-list">
<li><label><input type="checkbox" checked="" />Write the press
release</label></li>
<li><label><input type="checkbox" />Update the website</label></li>
<li><label><input type="checkbox" />Contact the media</label></li>
</ul>
<h3 id="使用-emoji-表情">使用 Emoji 表情</h3>
<p>有两种方法可以将表情符号添加到Markdown文件中：</p>
<ul>
<li>将表情符号复制并粘贴到Markdown格式的文本中</li>
<li>或者键入表情简码</li>
</ul>
<p>表情包来源：</p>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly9lbW9qaXBlZGlhLm9yZy8=">Emojipedia<i class="fa fa-external-link-alt"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9naXN0LmdpdGh1Yi5jb20vcnhhdmllcnMvNzM2MDkwOA==">表情符号简码列表<i class="fa fa-external-link-alt"></i></span></li>
<li>Windows下快捷键<kbd>Win</kbd> +
<kbd>.</kbd>来选择想要添加的Emoji表情</li>
</ul>
<hr />
<h2 id="高级操作">高级操作</h2>
<p>Markdown 作为轻量级标记语言，有一定的局限性，但借助 HTML 和
CSS，可以实现许多 Markdown 不支持的功能。</p>
<h3 id="下划线">下划线</h3>
<p>如果你的 Markdown 处理器支持 HTML，可以使用 <code>&lt;ins&gt;</code>
或 <code>&lt;u&gt;</code>标签：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">一些文字 &lt;ins&gt;将被加下划线&lt;/ins&gt;。</span><br><span class="line">一些文字 &lt;u&gt;将被加下划线&lt;/u&gt;。</span><br></pre></td></tr></table></figure>
<p>一些文字 <ins>将被加下划线</ins>。</p>
<p>一些文字 <u>将被加下划线</u>。</p>
<h3 id="文字居中">文字居中</h3>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">p</span> <span class="attr">style</span>=<span class="string">&quot;text-align:center&quot;</span>&gt;</span></span>这段文字居中显示。<span class="language-xml"><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></span><br></pre></td></tr></table></figure>
<p style="text-align:center">
这段文字居中显示。
</p>
<h3 id="文字颜色">文字颜色</h3>
<p>Markdown 不支持更改文字颜色，但 HTML 可以：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">font</span> <span class="attr">color</span>=<span class="string">&quot;red&quot;</span>&gt;</span></span>这段文字是红色的！<span class="language-xml"><span class="tag">&lt;/<span class="name">font</span>&gt;</span></span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">p</span> <span class="attr">style</span>=<span class="string">&quot;color:blue&quot;</span>&gt;</span></span>这段文字是蓝色的。<span class="language-xml"><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></span><br></pre></td></tr></table></figure>
<p><font color="red">这段文字是红色的！</font></p>
<p style="color:blue">
这段文字是蓝色的。
</p>
<h3 id="符号特殊字符">符号（特殊字符）</h3>
<p>Markdown 不能直接插入特殊符号，但可以复制粘贴，或者使用 HTML
实体：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">版权 (©) — &amp;copy;</span><br><span class="line">注册商标 (®) — &amp;reg;</span><br><span class="line">商标 (™) — &amp;trade;</span><br><span class="line">欧元 (€) — &amp;euro;</span><br><span class="line">左箭头 (←) — &amp;larr;</span><br><span class="line">上箭头 (↑) — &amp;uarr;</span><br><span class="line">右箭头 (→) — &amp;rarr;</span><br><span class="line">下箭头 (↓) — &amp;darr;</span><br><span class="line">度数 (°) — &amp;#176;</span><br><span class="line">圆周率 (π) — &amp;#960;</span><br></pre></td></tr></table></figure>
<p>渲染效果：</p>
<p>版权 (©) — ©</p>
<p>注册商标 (®) — ®</p>
<p>商标 (™) — ™</p>
<p>欧元 (€) — €</p>
<p>左箭头 (←) — ←</p>
<p>上箭头 (↑) — ↑</p>
<p>右箭头 (→) — →</p>
<p>下箭头 (↓) — ↓</p>
<p>度数 (°) — °</p>
<p>圆周率 (π) — π</p>
<h3 id="插入视频">插入视频</h3>
<p>Markdown 不能直接嵌入视频，但可以使用 HTML，或变通方式（图片+链接）
点击B站的分享图标，然后选择嵌入链接，会获得如下代码：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">iframe</span> <span class="attr">src</span>=<span class="string">&quot;//player.bilibili.com/player.html?isOutside=true&amp;aid=114980686925459&amp;bvid=BV1y1twzqE9n&amp;cid=31548313549&amp;p=1&quot;</span> <span class="attr">scrolling</span>=<span class="string">&quot;no&quot;</span> <span class="attr">border</span>=<span class="string">&quot;0&quot;</span> <span class="attr">frameborder</span>=<span class="string">&quot;no&quot;</span> <span class="attr">framespacing</span>=<span class="string">&quot;0&quot;</span> <span class="attr">allowfullscreen</span>=<span class="string">&quot;true&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">iframe</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>或者</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">[<span class="string">![视频预览</span>](<span class="link">https://img.youtube.com/vi/YOUTUBE-ID/0.jpg</span>)](<span class="link">https://www.youtube.com/watch?v=YOUTUBE-ID</span>)</span><br></pre></td></tr></table></figure>
<p>渲染效果：</p>
<iframe src="//player.bilibili.com/player.html?isOutside=true&amp;aid=114980686925459&amp;bvid=BV1y1twzqE9n&amp;cid=31548313549&amp;p=1" scrolling="no" border="0" frameborder="no" framespacing="0" allowfullscreen="true">
</iframe>
<h3 id="代码块折叠">代码块折叠</h3>
<details>
<summary>
<font size="4" color="orange">Show Code</font>
</summary>
<pre><code class="language-cpp">这里填充代码</code> </pre>
</details>
<p>HTML语法:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">details</span>&gt;</span> </span><br><span class="line"><span class="tag">&lt;<span class="name">summary</span>&gt;</span><span class="tag">&lt;<span class="name">font</span> <span class="attr">size</span>=<span class="string">&quot;4&quot;</span> <span class="attr">color</span>=<span class="string">&quot;orange&quot;</span>&gt;</span>Show Code<span class="tag">&lt;/<span class="name">font</span>&gt;</span><span class="tag">&lt;/<span class="name">summary</span>&gt;</span> </span><br><span class="line"><span class="tag">&lt;<span class="name">pre</span>&gt;</span><span class="tag">&lt;<span class="name">code</span> <span class="attr">class</span>=<span class="string">&quot;language-cpp&quot;</span>&gt;</span>这里填充代码<span class="tag">&lt;/<span class="name">code</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">pre</span>&gt;</span> <span class="tag">&lt;/<span class="name">details</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>标签解释:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">details:折叠语法</span><br><span class="line">summary:折叠语法展示的摘要</span><br><span class="line">pre:以原有格式显示元素内的文字是已经格式化的文本</span><br><span class="line">code：指定代码块</span><br></pre></td></tr></table></figure>
<p>注意事项：</p>
<ul>
<li>对于代码块中的 <code>&lt;&gt;</code>的特殊符号应该用
<code>\&amp;lt;</code>
<code>\&amp;gt;</code>代替，不然会显示错误。比如<code>&lt;stdio.h&gt;</code>要写成<code>\&amp;lt;stdio.h\&amp;gt;</code>。</li>
<li><code>&lt;code&gt;</code>必须紧贴代码开头（避免多出首行），<code>\&lt;/code&gt;</code>需单独一行（避免少了尾行）</li>
<li><strong>代码块内部不能有空行</strong></li>
</ul>
<h3 id="图片居中">图片居中</h3>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202311161556943.png" alt="单向链表结构-示意图" style="zoom: 33%;" />
</div>
<p>实现语法如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">align</span>=<span class="string">center</span>&gt;</span><span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">&quot;https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202311161556943.png&quot;</span> <span class="attr">alt</span>=<span class="string">&quot;单向链表结构-示意图&quot;</span> <span class="attr">style</span>=<span class="string">&quot;zoom: 33%;&quot;</span> /&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="markdown-表格单元格合并跨行跨列及文本换行">Markdown
表格单元格合并（跨行、跨列）及文本换行</h3>
<p><span class="exturl" data-url="aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3UwMTM2Njk5MTIvYXJ0aWNsZS9kZXRhaWxzLzEzOTQ5ODQxNw==">参考解决方案<i class="fa fa-external-link-alt"></i></span></p>
<p>示例</p>
<div data-align="center">
<table>
<tr>
<th>
</th>
<th>
</th>
<th>
选择排序
</th>
<th>
冒泡排序
</th>
<th>
插入排序
</th>
</tr>
<tr>
<td rowspan="3">
时间复杂度
</td>
<td>
最佳情况
</td>
<td>
O(n^2)
</td>
<td>
<b>O(n)</b>
</td>
<td>
<b>O(n)</b>
</td>
</tr>
<tr>
<td>
平均情况
</td>
<td>
O(n^2)
</td>
<td>
O(n^2)
</td>
<td>
O(n^2)
</td>
</tr>
<tr>
<td>
最坏情况
</td>
<td>
O(n^2)
</td>
<td>
O(n^2)
</td>
<td>
O(n^2)
</td>
</tr>
<tr>
<td colspan="2">
空间复杂度
</td>
<td>
O(1)
</td>
<td>
O(1)
</td>
<td>
O(1)
</td>
</tr>
<tr>
<td colspan="2">
稳定性
</td>
<td>
不稳定
</td>
<td>
稳定
</td>
<td>
稳定
</td>
</tr>
</table>
</div>
<p>实现语法如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">align</span>=<span class="string">center</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">table</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">th</span>&gt;</span> <span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">th</span>&gt;</span> <span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">th</span>&gt;</span>选择排序<span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">th</span>&gt;</span>冒泡排序<span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">th</span>&gt;</span>插入排序<span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span> <span class="attr">rowspan</span>=<span class="string">&quot;3&quot;</span>&gt;</span>时间复杂度<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span>最佳情况<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> O(n^2) <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> <span class="tag">&lt;<span class="name">b</span>&gt;</span>O(n)<span class="tag">&lt;/<span class="name">b</span>&gt;</span> <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> <span class="tag">&lt;<span class="name">b</span>&gt;</span>O(n)<span class="tag">&lt;/<span class="name">b</span>&gt;</span> <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span>平均情况<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> O(n^2) <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> O(n^2) <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> O(n^2) <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span>最坏情况<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> O(n^2) <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> O(n^2) <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> O(n^2) <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span> <span class="attr">colspan</span>=<span class="string">&quot;2&quot;</span>&gt;</span>空间复杂度<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> O(1) <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> O(1) <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> O(1) <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line">     <span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span> <span class="attr">colspan</span>=<span class="string">&quot;2&quot;</span>&gt;</span>稳定性<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> 不稳定 <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> 稳定 <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">td</span>&gt;</span> 稳定 <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="添加键盘按键图标">添加键盘按键图标</h3>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">使用 <span class="tag">&lt;<span class="name">kbd</span>&gt;</span>Ctrl<span class="tag">&lt;/<span class="name">kbd</span>&gt;</span> + <span class="tag">&lt;<span class="name">kbd</span>&gt;</span>Shift<span class="tag">&lt;/<span class="name">kbd</span>&gt;</span> + <span class="tag">&lt;<span class="name">kbd</span>&gt;</span>Esc<span class="tag">&lt;/<span class="name">kbd</span>&gt;</span> 打开任务管理器</span><br></pre></td></tr></table></figure>
<p>渲染效果：</p>
<p>使用 <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>Esc</kbd>
打开任务管理器</p>
<h3 id="内部锚点跳转">内部锚点跳转</h3>
<h4
id="标题自动生成锚点好像只能本地使用">标题自动生成锚点（好像只能本地使用）</h4>
<p>Markdown 的标题会自动生成一个锚点，链接格式如下：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">[<span class="string">要显示的文字</span>](<span class="link">#锚点</span>)</span><br></pre></td></tr></table></figure>
<p>锚点的规则：</p>
<table>
<thead>
<tr class="header">
<th>原标题</th>
<th>锚点</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code># 我的标题</code></td>
<td><code>#我的标题</code> → 中英文都直接写</td>
</tr>
<tr class="even">
<td>空格会变为 <code>-</code></td>
<td>例如 <code>我的 标题</code> → <code>#我的-标题</code></td>
</tr>
<tr class="odd">
<td>全部转换为小写（英文）</td>
<td></td>
</tr>
</tbody>
</table>
<p><a href="#基本语法">点击跳转到基本语法</a></p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="section">## 基本语法</span></span><br><span class="line">...</span><br><span class="line">[<span class="string">点击跳转到基本语法</span>](<span class="link">#基本语法</span>)</span><br></pre></td></tr></table></figure>
<h4 id="自定义锚点html-写法通用">自定义锚点（HTML 写法，通用）</h4>
<p>写法如下（注意空行要保留）：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">a</span> <span class="attr">id</span>=<span class="string">&quot;sec2&quot;</span>&gt;</span></span><span class="language-xml"><span class="tag">&lt;/<span class="name">a</span>&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="section">## 扩展语法</span></span><br><span class="line"></span><br><span class="line">...</span><br><span class="line">[<span class="string">跳转到扩展语法</span>](<span class="link">#sec2</span>)</span><br></pre></td></tr></table></figure>
<p><a href="#sec2">点击跳转到扩展语法</a></p>
<hr />
<h2 id="参考博文">参考博文</h2>
<ol type="1">
<li><span class="exturl" data-url="aHR0cHM6Ly9tYXJrZG93bi5jb20uY24v">Markdown 教程<i class="fa fa-external-link-alt"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly93d3cuY25ibG9ncy5jb20vUmlvVGlhbi9wLzEzODI4NTQ4Lmh0bWw=">为博客园markdown代码块添加折叠<i class="fa fa-external-link-alt"></i></span></li>
</ol>
<aside id="footnotes" class="footnotes footnotes-end-of-document"
role="doc-endnotes">
<hr />
<ol>
<li id="fn1"><p>This is the first footnote.<a href="#fnref1"
class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn2"><p>Here’s one with multiple paragraphs and code.</p>
<p>Indent paragraphs to include them in the footnote.</p>
<p><code>{ my code }</code></p>
<p>Add as many paragraphs as you like.<a href="#fnref2"
class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</aside>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>Markdown</category>
      </categories>
      <tags>
        <tag>Markdown</tag>
      </tags>
  </entry>
  <entry>
    <title>MathType 安装及使用教程</title>
    <url>/2024/03/17/tools/misc/mathtype-install-use-tutorial/</url>
    <content><![CDATA[<p>本文介绍了MathType的安装、配置及在Word中的使用方法。内容包括软件下载与安装步骤、在Word中添加和删除MathType快捷方式、MathType与LaTeX公式的相互转换，并提供了常见问题的解决方案和相关参考链接，适合需要在Windows平台下高效使用MathType的用户参考。</p>
<span id="more"></span>
<hr />
<h2 id="安装">安装</h2>
<p>参考Github仓库<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0phczBuYU4vbWF0aHR5cGVfY3JhY2s=">地址<i class="fa fa-external-link-alt"></i></span>，这个里面没有中文版，下面这个云盘里面的带中文版。仅用于教育和个人使用，支持正版，如果有能力，建议购买官方软件。</p>
<p>（1）下载压缩包MathType_Crack_7.8.0.0.rar <span class="exturl" data-url="aHR0cHM6Ly9wYW4uYmFpZHUuY29tL3MvMVY0dVRfNVlKanBjcUFwMXVHQzEzd0E/cHdkPWt0djg=">百度云盘<i class="fa fa-external-link-alt"></i></span></p>
<p>解压软件包，得到如下文件</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012105104267.png" alt="解压软件包" style="zoom:60%;" />
</div>
<p>（2）右键以管理员方式运行MathType-win-zh-7.8.0.0.exe；</p>
<p>（3）选择目标文件夹，可更换其他文件夹；</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012105625497.png"  alt="选择目标文件夹" style="zoom:60%;" />
</div>
<p>（4）选择中文语言；</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012105851599.png"  alt="选择中文语言"  style="zoom:60%;" />
</div>
<p>（5）替换文件,找到解压后的替换文件中的MathType.exe，复制粘贴到第3步的安装目标文件夹完成替换。</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012110528483.png" alt="替换文件" style="zoom:40%;" />
</div>
<p>（6）Windows Defender
防火墙设置阻止MathType联网，防止MathType强制更新弹窗，并删除注册表多余信息。步骤如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">打开 Windows Defender 防火墙设置</span><br><span class="line">1. 打开控制面板</span><br><span class="line">2. 转到：系统和安全 &gt; Windows Defender 防火墙</span><br><span class="line">3. 在左侧窗格中，点高级设置</span><br><span class="line"></span><br><span class="line">创建新的出站规则</span><br><span class="line">1. 在&quot;高级安全 Windows Defender 防火墙&quot;窗口的左侧窗格中，选择&quot;出站规则&quot;</span><br><span class="line">2. 在右侧窗格（操作窗格）中，点击&quot;新建规则...&quot;</span><br><span class="line"></span><br><span class="line">选择规则类型</span><br><span class="line">1. 在&quot;新建出站规则向导&quot;中，选择&quot;程序&quot;</span><br><span class="line">2. 点击&quot;下一步&quot;</span><br><span class="line"></span><br><span class="line">指定 MathType 程序</span><br><span class="line">1. 选择&quot;此程序路径：&quot;</span><br><span class="line">2. 点击&quot;浏览...&quot; 并导航到您计算机上 MathType 的安装目录（例如：`C:\Program Files (x86)\MathType`）</span><br><span class="line">3. 选择 MathType.exe 文件 并点击&quot;打开&quot;</span><br><span class="line">4. 点击&quot;下一步&quot;</span><br><span class="line"></span><br><span class="line">选择操作</span><br><span class="line">1. 选择&quot;阻止连接&quot;</span><br><span class="line">2. 点击&quot;下一步&quot;</span><br><span class="line"></span><br><span class="line">选择规则应用时机</span><br><span class="line">1. 勾选所有三个网络配置文件复选框：</span><br><span class="line">   - 域网络</span><br><span class="line">   - 专用网络  </span><br><span class="line">   - 公用网络</span><br><span class="line">2. 点击&quot;下一步&quot;</span><br><span class="line"></span><br><span class="line">命名规则</span><br><span class="line">1. 为规则指定一个清晰的名称，例如：Block MathType Internet Access 或 Prevent MathType Updates</span><br><span class="line">2. （可选）添加描述，如：&quot;阻止 MathType.exe 以防止强制更新弹窗&quot;</span><br><span class="line">3. 点击&quot;完成&quot;</span><br><span class="line"></span><br><span class="line">删除注册表多余信息</span><br><span class="line">1. 键盘&quot;Win+R&quot;，打开&quot;regedit.exe&quot;</span><br><span class="line">2. 打开路径：计算机\HKEY_CURRENT_USER\Software\Install Options</span><br><span class="line">3. 删除名称类似为&quot;OptionsX.X&quot;即可</span><br><span class="line">4. 重启计算机生效</span><br><span class="line"> </span><br><span class="line">完成</span><br><span class="line">新规则现已生效。MathType.exe 将被阻止访问互联网，从而防止连接到其更新服务器并停止强制更新弹窗。</span><br><span class="line"></span><br><span class="line">规则生效验证</span><br><span class="line">规则创建后：</span><br><span class="line">- ✅ MathType 无法检查更新</span><br><span class="line">- ✅ 不会出现更新弹窗</span><br><span class="line">- ✅ 程序其他功能正常使用</span><br><span class="line">- ✅ 不影响其他网络连接</span><br><span class="line"></span><br><span class="line">注意：此方法只阻止 MathType 联网，不会影响软件的正常功能使用。</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="配置">配置</h2>
<h3 id="添加打开快捷方式">添加打开快捷方式</h3>
<p>（1）打开word，点击“视图”——“宏”—— “录制宏”；</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012124348443.png" alt="打开word" style="zoom:50%;" />
</div>
<p>（2）修改宏名称，可以分别将宏指定到按钮或键盘，此处指定到按钮（键盘快捷键配置方式类似），点击按钮；</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012124818785.png" alt="修改宏名称" style="zoom:50%;" />
</div>
<p>（3）点击“Normal.NewMacros.MathTypeButton”——“添加”—— “确定”；</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012125805945.png" alt="点击Button" style="zoom:50%;" />
</div>
<p>（4）点击 “插入”——“对象”—— “MathType7.0 Equation”—— “确定”；</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012130310442.png" alt="点击插入" style="zoom:30%;" />
</div>
<p>（5）此时MathType已打开，回到“视图”——“宏”—— “停止录制”；</p>
<p>（6）点击左上角按钮即可快捷打开MathType编辑器。</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012131947077.png" alt="点击左上角按钮" style="zoom:80%;" />
</div>
<h3 id="删除打开快捷方式">删除打开快捷方式</h3>
<ol type="1">
<li>打开word，点击“视图”——“宏”—— “查看宏”；</li>
<li>点击“Normal.dotm(共用模板)”——“MathTypeButton”—— “删除”即可。</li>
</ol>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012131433808.png" alt="点击Normal.dotm" style="zoom:50%;" />
</div>
<hr />
<h2 id="使用">使用</h2>
<h3
id="latex公式与mathtype公式快速转换">LaTeX公式与MathType公式快速转换</h3>
<p><strong>将MathType公式转换为LaTeX公式:</strong></p>
<p>在Word文档中选中用MathType编辑的公式，按住 <code>Alt+\</code>
键，可以将 MathType 公式快速转换成 LaTeX格式。</p>
<p>也可以在MathType编辑窗口中直接将公式转化为LaTeX，具体方法请参考教程：<span class="exturl" data-url="aHR0cHM6Ly93d3cubWF0aHR5cGUuY24vamlxaWFvL2dvbmdzaGktZGFpbWEuaHRtbA==">如何实现MathType公式转换为LaTex代码。<i class="fa fa-external-link-alt"></i></span></p>
<p><strong>将LaTeX公式转换为MathType公式：</strong></p>
<p>将LateX格式的公式代码复制到 Word 中，前后分别加上“$”符号，按下
<code>Alt+\</code>键，就将公式切换成MathType格式。</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012120156230.png" alt="LaTeX公式转换为MathType公式" style="zoom:70%;" />
</div>
<h3 id="word中公式上浮">Word中公式上浮</h3>
<p>解决方法有两种，具体操作如下：</p>
<h4 id="方法一设置word格式字体">方法一、设置Word格式字体</h4>
<p>（1）选中公式，在Word中选择“格式”——“字体”——“字符间距”——“位置”——“标准”。</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012120659324.png" alt="选中公式" style="zoom:50%;" />
</div>
<p>（2）选择“格式”——“段落”——“中文版式”，将“文本对齐方式”改为“居中”即可。</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012120805629.png" alt="居中" style="zoom:50%;" />
</div>
<p><strong>提示:</strong>
在Word中除了可以设置字体外，还可以调整公式行间距，具体教程可以参考<span class="exturl" data-url="aHR0cHM6Ly93d3cubWF0aHR5cGUuY24vd2VudGkvdGlhb3poZW5nLWhhbmdqdS5odG1s">插入MathType公式行距变大的解决方法<i class="fa fa-external-link-alt"></i></span>。</p>
<h4 id="方法二设置mathtype格式">方法二、设置MathType格式</h4>
<p>（1）双击公式打开Mathtype公式编辑器，进入编辑状态，点击“大小”——“定义”——“完整”对应的pt值，一般五号对应10pt，小四对应12pt。其他可以自己按照具体要求自行调节。其他默认大小设置不推荐改动。</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012121203661.png" alt="图片描述" style="zoom:60%;" />
</div>
<p>（2） 点击“预置”——“公式预置”——
“保存到文件”，存一个与默认配置文件不同的名字，比如“Test”然后关闭Mathtype回到Word文档。</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012121324011.png" alt="图片描述" style="zoom:60%;" />
</div>
<p>（3）点击Word界面上的“Mathtype” ——“Format Equation”—— MathType
preference
file选项下面的“browse”按钮，选中刚才存的配置文件“Test”，点选“Whole
document”选项，点OK，就安心等着公式一个个改过来。</p>
<div data-align="center">
<img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20241012121459730.png" alt="图片描述"  style="zoom:60%;" />
</div>
<p><strong>提示：</strong>以上两种方法中，第一种方法适合公式比较少时，单个手动修改，第二种方法是批量修改公式的方法，大量修改公式时一般采用此种方法。</p>
<hr />
<h2 id="问题">问题</h2>
<h3
id="word中有两个一样的mathtype按钮怎么删除">word中有两个一样的MathType按钮怎么删除</h3>
<p>参考<span class="exturl" data-url="aHR0cHM6Ly93d3cubWF0aHR5cGUuY24vamlxaWFvL2FuLW5pdS1zaGFuY2h1Lmh0bWw=">博文<i class="fa fa-external-link-alt"></i></span></p>
<hr />
<h2 id="参考博文">参考博文</h2>
<p>[1] <span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0phczBuYU4vbWF0aHR5cGVfY3JhY2s=">MathType
Cracked Software Repository<i class="fa fa-external-link-alt"></i></span></p>
<p>[2] <span class="exturl" data-url="aHR0cHM6Ly93d3cuZS1sZWFybi5jbi90b3BpYy8zNzYyMzcw">MathType公式编辑器过期（失效，联网无法使用）的解决方案<i class="fa fa-external-link-alt"></i></span></p>
]]></content>
      <categories>
        <category>工具与工程实践（Tooling &amp; Engineering）</category>
      </categories>
      <tags>
        <tag>Crack</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL常用命令速查</title>
    <url>/2026/03/28/tools/mysql/mysql-common-commands/</url>
    <content><![CDATA[<p>本文按使用场景整理了一份简洁版 MySQL 常用命令速查表。</p>
<span id="more"></span>
<h2 id="最小速查版">最小速查版</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysql -u root -p                              <span class="comment"># 登录</span></span><br><span class="line">mysqldump -u root -p demo &gt; demo.sql          <span class="comment"># 导出</span></span><br><span class="line">mysql -u root -p demo &lt; demo.sql              <span class="comment"># 导入</span></span><br></pre></td></tr></table></figure>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> DATABASES;                               <span class="comment">-- 看库</span></span><br><span class="line">USE demo;                                     <span class="comment">-- 切库</span></span><br><span class="line"><span class="keyword">SHOW</span> TABLES;                                  <span class="comment">-- 看表</span></span><br><span class="line"><span class="keyword">DESC</span> users;                                   <span class="comment">-- 看结构</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> users LIMIT <span class="number">10</span>;                 <span class="comment">-- 查数据</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">CREATE TABLE</span> users\G                     <span class="comment">-- 看建表语句</span></span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> users <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;     <span class="comment">-- 看执行计划</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">FULL</span> PROCESSLIST;                        <span class="comment">-- 看连接</span></span><br></pre></td></tr></table></figure>
<h2 id="连接">连接</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysql -u root -p                             <span class="comment"># 本地连接，输入密码</span></span><br><span class="line">mysql -h 127.0.0.1 -P 3306 -u root -p        <span class="comment"># 指定主机和端口连接</span></span><br><span class="line">mysql -u root -p mydb                        <span class="comment"># 连接后直接进入指定数据库</span></span><br><span class="line">mysql -u root -p -e <span class="string">&quot;SHOW DATABASES;&quot;</span>        <span class="comment"># 执行一条 SQL 后退出</span></span><br></pre></td></tr></table></figure>
<h2 id="数据库">数据库</h2>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> DATABASES;                                                  <span class="comment">-- 查看所有数据库</span></span><br><span class="line"><span class="keyword">CREATE</span> DATABASE demo;                                            <span class="comment">-- 创建数据库</span></span><br><span class="line"><span class="keyword">CREATE</span> DATABASE demo <span class="keyword">CHARACTER SET</span> utf8mb4 <span class="keyword">COLLATE</span> utf8mb4_unicode_ci; <span class="comment">-- 创建数据库并指定字符集</span></span><br><span class="line"><span class="keyword">DROP</span> DATABASE demo;                                              <span class="comment">-- 删除数据库</span></span><br><span class="line">USE demo;                                                        <span class="comment">-- 切换数据库</span></span><br><span class="line"><span class="keyword">SELECT</span> DATABASE();                                               <span class="comment">-- 查看当前数据库</span></span><br></pre></td></tr></table></figure>
<h2 id="表">表</h2>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> TABLES;                           <span class="comment">-- 查看当前库所有表</span></span><br><span class="line"><span class="keyword">DESC</span> users;                            <span class="comment">-- 查看表结构</span></span><br><span class="line"><span class="keyword">SHOW</span> COLUMNS <span class="keyword">FROM</span> users;               <span class="comment">-- 查看表字段</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">CREATE TABLE</span> users\G              <span class="comment">-- 查看建表语句，纵向显示</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">TABLE</span> STATUS <span class="keyword">LIKE</span> <span class="string">&#x27;users&#x27;</span>;        <span class="comment">-- 查看表状态</span></span><br><span class="line">RENAME <span class="keyword">TABLE</span> old_name <span class="keyword">TO</span> new_name;     <span class="comment">-- 重命名表</span></span><br><span class="line"><span class="keyword">TRUNCATE</span> <span class="keyword">TABLE</span> users;                  <span class="comment">-- 清空表并重置自增</span></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> users;                      <span class="comment">-- 删除表</span></span><br></pre></td></tr></table></figure>
<h2 id="数据增删改查">数据增删改查</h2>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> users;                                           <span class="comment">-- 查询全部数据</span></span><br><span class="line"><span class="keyword">SELECT</span> id, name <span class="keyword">FROM</span> users <span class="keyword">WHERE</span> status <span class="operator">=</span> <span class="number">1</span> LIMIT <span class="number">10</span>;          <span class="comment">-- 条件查询</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> users(name, email) <span class="keyword">VALUES</span> (<span class="string">&#x27;Alice&#x27;</span>, <span class="string">&#x27;a@x.com&#x27;</span>);    <span class="comment">-- 插入数据</span></span><br><span class="line"><span class="keyword">UPDATE</span> users <span class="keyword">SET</span> email <span class="operator">=</span> <span class="string">&#x27;b@x.com&#x27;</span> <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;               <span class="comment">-- 更新数据</span></span><br><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> users <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;                                <span class="comment">-- 删除数据</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">FROM</span> users;                                    <span class="comment">-- 统计行数</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">DISTINCT</span> status <span class="keyword">FROM</span> users;                             <span class="comment">-- 去重查询</span></span><br></pre></td></tr></table></figure>
<h2 id="用户与权限">用户与权限</h2>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">USER</span>(), <span class="built_in">CURRENT_USER</span>();                                        <span class="comment">-- 查看当前用户</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> <span class="string">&#x27;dev&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span> IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;strong_password&#x27;</span>;        <span class="comment">-- 创建本地用户</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> <span class="string">&#x27;dev&#x27;</span>@<span class="string">&#x27;%&#x27;</span> IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;strong_password&#x27;</span>;                <span class="comment">-- 创建可远程连接用户</span></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">USER</span> <span class="string">&#x27;dev&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span> IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;new_password&#x27;</span>;            <span class="comment">-- 修改密码</span></span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">ALL</span> PRIVILEGES <span class="keyword">ON</span> demo.<span class="operator">*</span> <span class="keyword">TO</span> <span class="string">&#x27;dev&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span>;                  <span class="comment">-- 授予库全部权限</span></span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">SELECT</span> <span class="keyword">ON</span> demo.<span class="operator">*</span> <span class="keyword">TO</span> <span class="string">&#x27;dev&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span>;                          <span class="comment">-- 授予只读权限</span></span><br><span class="line"><span class="keyword">SHOW</span> GRANTS <span class="keyword">FOR</span> <span class="string">&#x27;dev&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span>;                                    <span class="comment">-- 查看授权</span></span><br><span class="line">FLUSH PRIVILEGES;                                                     <span class="comment">-- 刷新权限</span></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">USER</span> <span class="string">&#x27;dev&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span>;                                          <span class="comment">-- 删除用户</span></span><br></pre></td></tr></table></figure>
<h2 id="事务">事务</h2>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">START</span> TRANSACTION;        <span class="comment">-- 开启事务</span></span><br><span class="line"><span class="keyword">BEGIN</span>;                    <span class="comment">-- 开启事务（等价写法）</span></span><br><span class="line"><span class="keyword">COMMIT</span>;                   <span class="comment">-- 提交事务</span></span><br><span class="line"><span class="keyword">ROLLBACK</span>;                 <span class="comment">-- 回滚事务</span></span><br><span class="line"><span class="keyword">SAVEPOINT</span> sp1;            <span class="comment">-- 创建保存点</span></span><br><span class="line"><span class="keyword">ROLLBACK</span> <span class="keyword">TO</span> sp1;          <span class="comment">-- 回滚到保存点</span></span><br></pre></td></tr></table></figure>
<h2 id="索引与执行计划">索引与执行计划</h2>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> INDEX <span class="keyword">FROM</span> users;                                             <span class="comment">-- 查看索引</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_users_email <span class="keyword">ON</span> users(email);                      <span class="comment">-- 创建索引</span></span><br><span class="line"><span class="keyword">DROP</span> INDEX idx_users_email <span class="keyword">ON</span> users;                               <span class="comment">-- 删除索引</span></span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> users <span class="keyword">WHERE</span> email <span class="operator">=</span> <span class="string">&#x27;alice@example.com&#x27;</span>;     <span class="comment">-- 查看执行计划</span></span><br></pre></td></tr></table></figure>
<h2 id="状态与排查">状态与排查</h2>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> VERSION();                               <span class="comment">-- 查看 MySQL 版本</span></span><br><span class="line"><span class="keyword">SHOW</span> STATUS;                                    <span class="comment">-- 查看服务器状态</span></span><br><span class="line"><span class="keyword">SHOW</span> STATUS <span class="keyword">LIKE</span> <span class="string">&#x27;Threads_connected&#x27;</span>;           <span class="comment">-- 查看当前连接数相关状态</span></span><br><span class="line"><span class="keyword">SHOW</span> VARIABLES;                                 <span class="comment">-- 查看系统变量</span></span><br><span class="line"><span class="keyword">SHOW</span> VARIABLES <span class="keyword">LIKE</span> <span class="string">&#x27;character_set_%&#x27;</span>;          <span class="comment">-- 查看字符集配置</span></span><br><span class="line"><span class="keyword">SHOW</span> VARIABLES <span class="keyword">LIKE</span> <span class="string">&#x27;max_connections&#x27;</span>;          <span class="comment">-- 查看最大连接数</span></span><br><span class="line"><span class="keyword">SHOW</span> PROCESSLIST;                               <span class="comment">-- 查看当前连接</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">FULL</span> PROCESSLIST;                          <span class="comment">-- 查看完整连接信息</span></span><br><span class="line">KILL <span class="number">12345</span>;                                     <span class="comment">-- 杀掉指定连接</span></span><br></pre></td></tr></table></figure>
<h2 id="导入导出">导入导出</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysqldump -u root -p demo &gt; demo.sql                            <span class="comment"># 导出整个数据库</span></span><br><span class="line">mysqldump -u root -p demo <span class="built_in">users</span> &gt; users.sql                     <span class="comment"># 导出指定表</span></span><br><span class="line">mysqldump -u root -p --routines --triggers --events demo &gt; demo_full.sql  <span class="comment"># 连同存储过程/触发器/事件导出</span></span><br><span class="line">mysql -u root -p demo &lt; demo.sql                                <span class="comment"># 导入 SQL 文件</span></span><br></pre></td></tr></table></figure>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">SOURCE G:<span class="operator">/</span>backup<span class="operator">/</span>demo.sql;       <span class="comment">-- 在 mysql 客户端中导入 SQL 文件</span></span><br></pre></td></tr></table></figure>
<h2 id="mysql-客户端命令">mysql 客户端命令</h2>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">help           <span class="comment">-- 查看帮助</span></span><br><span class="line">\h             <span class="comment">-- 查看帮助（简写）</span></span><br><span class="line">status         <span class="comment">-- 查看当前连接状态</span></span><br><span class="line">\s             <span class="comment">-- 查看当前连接状态（简写）</span></span><br><span class="line"><span class="keyword">system</span> cls     <span class="comment">-- Windows 下清屏</span></span><br><span class="line"><span class="keyword">system</span> clear   <span class="comment">-- Linux/macOS 下清屏</span></span><br><span class="line"><span class="keyword">system</span> dir     <span class="comment">-- Windows 下执行系统命令</span></span><br><span class="line">exit           <span class="comment">-- 退出客户端</span></span><br><span class="line">quit           <span class="comment">-- 退出客户端</span></span><br><span class="line">\q             <span class="comment">-- 退出客户端（简写）</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>工具与工程实践（Tooling &amp; Engineering）</category>
      </categories>
      <tags>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>Windows、Office、Visual Studio 升级激活脚本</title>
    <url>/2024/03/05/tools/misc/microsoft-activation-scripts/</url>
    <content><![CDATA[<p>Microsoft Activation Scripts (MAS)，<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21hc3NncmF2ZWwvTWljcm9zb2Z0LUFjdGl2YXRpb24tU2NyaXB0cw==">参考项目地址<i class="fa fa-external-link-alt"></i></span>，<span class="exturl" data-url="aHR0cHM6Ly9tYXNzZ3JhdmUuZGV2Lw==">参考说明文档<i class="fa fa-external-link-alt"></i></span>。</p>
<p>项目简介：Open-source Windows and Office activator featuring HWID,
Ohook, TSforge, KMS38, and Online KMS activation methods, along with
advanced troubleshooting.</p>
<span id="more"></span>
<hr />
<h2 id="windows和office激活">Windows和Office激活</h2>
<h3 id="windows激活密钥">Windows激活密钥</h3>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">J8WVF-9X3GM-4WVYC-VDHQG-42CXT   // Windows 11 专业版</span><br></pre></td></tr></table></figure>
<p>其他密钥见<span class="exturl" data-url="aHR0cHM6Ly9tYXNzZ3JhdmUuZGV2L2h3aWQjc3VwcG9ydGVkLXByb2R1Y3Rz">支持产品<i class="fa fa-external-link-alt"></i></span>。</p>
<h3 id="步骤">步骤</h3>
<ol type="1">
<li>先断网输入产品密钥，更新时自动重启电脑。</li>
<li>重启电脑后显示已经更新到Win11专业版，待激活状态。</li>
<li>联网后按<code>Win+X</code>，选择终端管理员（Windows PowerShell）打开
。</li>
<li>再在弹出的窗口内输入：<code>irm https://get.activated.win | iex</code>，回车，等待弹出窗口（窗口不会立马弹出，需等待一会）。</li>
<li>弹出窗口按需选择序号（如下图）。</li>
<li>回到系统查看会显示<strong>已激活</strong>。</li>
</ol>
<p><img data-src="https://zzz-drawing-bed.oss-cn-nanjing.aliyuncs.com/img/image-20250702165005755.png" alt="image-20250702165005755" style="zoom: 50%;" /></p>
<ul>
<li><a href="https://massgrave.dev/hwid"><strong>HWID
(数字证书)</strong> 方法</a>永久激活Windows</li>
<li><strong>Ohook</strong> 方法永久激活Office</li>
<li><strong>TSforge</strong> 方法永久激活 Windows/ESU/Office</li>
<li><strong>KMS38</strong> 方法激活 Windows (有效期到2038年)</li>
<li><strong>Online KMS</strong> 激活Windows/Office
180天（终身续订任务）</li>
</ul>
<table>
<colgroup>
<col style="width: 13%" />
<col style="width: 30%" />
<col style="width: 21%" />
<col style="width: 34%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">激活类型</th>
<th style="text-align: left;">支持产品</th>
<th style="text-align: left;">激活有效期</th>
<th style="text-align: left;">是否联网</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;">HWID</td>
<td style="text-align: left;">Windows 10-11</td>
<td style="text-align: left;">永久有效</td>
<td style="text-align: left;">是</td>
</tr>
<tr class="even">
<td style="text-align: left;">Ohook</td>
<td style="text-align: left;">Office</td>
<td style="text-align: left;">永久有效</td>
<td style="text-align: left;">无须联网</td>
</tr>
<tr class="odd">
<td style="text-align: left;">TSforge</td>
<td style="text-align: left;">Windows / ESU / Office</td>
<td style="text-align: left;">永久有效</td>
<td style="text-align: left;">是的，19041及以后版本需要</td>
</tr>
<tr class="even">
<td style="text-align: left;">KMS38</td>
<td style="text-align: left;">Windows</td>
<td style="text-align: left;">到2038年之前有效</td>
<td style="text-align: left;">无须联网</td>
</tr>
<tr class="odd">
<td style="text-align: left;">Online KMS</td>
<td style="text-align: left;">Windows / Office</td>
<td style="text-align: left;">180天内有效</td>
<td style="text-align: left;">是</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="visual-studio">Visual Studio</h2>
<h3 id="激活密钥">激活密钥</h3>
<p><span class="exturl" data-url="aHR0cHM6Ly9tYXNzZ3JhdmUuZGV2L3Vuc3VwcG9ydGVkX3Byb2R1Y3RzX2FjdGl2YXRpb24jdmlzdWFsLXN0dWRpbw==">Visual
Studio激活密钥<i class="fa fa-external-link-alt"></i></span></p>
<table>
<thead>
<tr class="header">
<th>版本</th>
<th>激活密钥</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Visual Studio 2022 Professional</td>
<td>TD244-P4NB7-YQ6XK-Y8MMM-YWV2J</td>
</tr>
<tr class="even">
<td>Visual Studio 2022 Enterprise</td>
<td>VHF9H-NXBBB-638P6-6JHCY-88JWH</td>
</tr>
<tr class="odd">
<td>Visual Studio 2019 Professional</td>
<td>NYWVH-HT4XC-R2WYW-9Y3CM-X4V3Y</td>
</tr>
<tr class="even">
<td>Visual Studio 2019 Enterprise</td>
<td>BF8Y8-GN2QH-T84XB-QVY3B-RC4DF</td>
</tr>
</tbody>
</table>
<h3 id="安装方法">安装方法</h3>
<p><span class="exturl" data-url="aHR0cHM6Ly96aHVhbmxhbi56aGlodS5jb20vcC8xOTE5MTMzMjYyMDI4MTkwMzkw">参考博文<i class="fa fa-external-link-alt"></i></span></p>
<hr />
]]></content>
      <categories>
        <category>工具与工程实践（Tooling &amp; Engineering）</category>
        <category>软件安装与使用</category>
      </categories>
      <tags>
        <tag>Crack</tag>
      </tags>
  </entry>
  <entry>
    <title>Vim常用命令</title>
    <url>/2024/04/11/tools/vim/vim-common-commands/</url>
    <content><![CDATA[<p>本文总结了Vim的一些常用命令，主要包括移动光标、删除、复制、修改、查找、替换、撤销、插入模式、视图模式、文件操作和多窗口操作等。</p>
<span id="more"></span>
<hr />
<h2 id="vim常用命令总结">vim常用命令总结</h2>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/vim%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4.png"
alt="vim常用命令" />
<figcaption aria-hidden="true">vim常用命令</figcaption>
</figure>
<hr />
<h2 id="normal-mode">Normal Mode</h2>
<h3 id="移动光标">移动光标</h3>
<blockquote>
<p><code>9</code> 光标往下移动9行，其他数字类似。</p>
</blockquote>
<h3 id="常用命令">常用命令</h3>
<blockquote>
<p><code>ggdG</code> 删除全文</p>
<p><code>gg=G</code> 全文格式化</p>
<p><code>:set paste</code> 关闭缩进</p>
<p><code>:set nopaste</code> 开启缩进</p>
<p><code>:set nonumber</code> 用于取消绝对行号</p>
<p><code>:set norelativenumber</code> 用于取消相对行号</p>
<p><code>shift + k</code> 选中指定函数快速查看手册</p>
<p><code>:n,md</code> 删除第 <code>n</code> 行到第 <code>m</code> 行</p>
</blockquote>
<hr />
<h3 id="vim多窗口操作">vim多窗口操作</h3>
<table>
<colgroup>
<col style="width: 68%" />
<col style="width: 31%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">命令</th>
<th style="text-align: left;">解释</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><code>:e</code></td>
<td style="text-align: left;">创建或者打开另一个文件</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>:bn</code></td>
<td style="text-align: left;">切换到下一个窗口</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>:bp</code></td>
<td style="text-align: left;">切换到上一个窗口</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>:bd</code></td>
<td style="text-align: left;">关闭当前窗口</td>
</tr>
<tr class="odd">
<td style="text-align: left;">修改 <code>.vimrc.custom.config</code>
加入 <code>nmap &lt;Tab&gt; :bn&lt;cr&gt;</code></td>
<td style="text-align: left;">在多标签页面实现快速切换</td>
</tr>
</tbody>
</table>
<hr />
<h3 id="替换">替换</h3>
<table>
<colgroup>
<col style="width: 35%" />
<col style="width: 64%" />
</colgroup>
<thead>
<tr class="header">
<th>命令</th>
<th>解释</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>:s/oldword/newword/</code></td>
<td>替换<strong>当前行第一个</strong> <span
class="math inline">\(\textrm{oldword}\)</span> 为 <span
class="math inline">\(\textrm{newword}\)</span></td>
</tr>
<tr class="even">
<td><code>:s/oldword/newword/g</code></td>
<td>替换<strong>当前行所有</strong> <span
class="math inline">\(\textrm{oldword}\)</span> 为 <span
class="math inline">\(\textrm{newword}\)</span></td>
</tr>
<tr class="odd">
<td><code>:%s/oldword/newword/g</code></td>
<td>替换<strong>整个文件中所有</strong> <span
class="math inline">\(\textrm{oldword}\)</span> 为 <span
class="math inline">\(\textrm{newword}\)</span></td>
</tr>
<tr class="even">
<td><code>r</code></td>
<td>替换单个字符</td>
</tr>
<tr class="odd">
<td><code>R</code></td>
<td>替换连续多个字符</td>
</tr>
</tbody>
</table>
]]></content>
      <categories>
        <category>Linux</category>
        <category>基础知识</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>Vim</tag>
      </tags>
  </entry>
  <entry>
    <title>WSL命令速查表</title>
    <url>/2025/12/22/tools/wsl/wsl2-common-commands/</url>
    <content><![CDATA[<p>本文总结了 WSL 常用的命令。</p>
<span id="more"></span>
<hr />
<h2 id="命令速查表">命令速查表</h2>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 基础信息查看</span></span><br><span class="line"><span class="comment"># 查看 WSL 版本</span></span><br><span class="line">wsl <span class="literal">--version</span></span><br><span class="line"><span class="comment"># 查看本地已安装发行版</span></span><br><span class="line">wsl <span class="literal">-l</span> <span class="literal">-v</span> </span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 在线安装 / 管理发行版</span></span><br><span class="line"><span class="comment"># 查看在线可安装发行版</span></span><br><span class="line">wsl <span class="literal">--list</span> <span class="literal">--online</span></span><br><span class="line"><span class="comment"># 默认安装 Ubuntu 22.04</span></span><br><span class="line">wsl <span class="literal">--install</span></span><br><span class="line"><span class="comment"># 安装指定发行版</span></span><br><span class="line">wsl <span class="literal">--install</span> <span class="literal">-d</span> Ubuntu<span class="literal">-24</span>.<span class="number">04</span></span><br><span class="line"><span class="comment"># 安装但不启动</span></span><br><span class="line">wsl <span class="literal">--install</span> <span class="literal">-d</span> Ubuntu<span class="literal">-22</span>.<span class="number">04</span> <span class="literal">--no-launch</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 启动 / 切换 / 删除发行版</span></span><br><span class="line"><span class="comment"># 启动默认发行版</span></span><br><span class="line">wsl</span><br><span class="line"><span class="comment"># 启动指定发行版</span></span><br><span class="line">wsl <span class="literal">-d</span> Ubuntu<span class="literal">-22</span>.<span class="number">04</span></span><br><span class="line"><span class="comment"># 设置默认发行版</span></span><br><span class="line">wsl <span class="literal">--set-default</span> Ubuntu<span class="literal">-22</span>.<span class="number">04</span></span><br><span class="line"><span class="comment"># 删除某个发行版（⚠️不可恢复）</span></span><br><span class="line">wsl <span class="literal">--unregister</span> Ubuntu<span class="literal">-20</span>.<span class="number">04</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. WSL1 / WSL2 管理</span></span><br><span class="line"><span class="comment"># 设置某发行版使用 WSL2</span></span><br><span class="line">wsl <span class="literal">--set-version</span> Ubuntu<span class="literal">-22</span>.<span class="number">04</span> <span class="number">2</span></span><br><span class="line"><span class="comment"># 设置默认使用 WSL2</span></span><br><span class="line">wsl <span class="literal">--set-default-version</span> <span class="number">2</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. 关闭</span></span><br><span class="line"><span class="comment"># 关闭所有 WSL 实例</span></span><br><span class="line">wsl <span class="literal">--shutdown</span></span><br><span class="line"><span class="comment"># 强制停止这个发行版对应的 Linux 实例</span></span><br><span class="line">wsl <span class="literal">--terminate</span> Ubuntu<span class="literal">-22</span>.<span class="number">04</span></span><br><span class="line"><span class="comment"># or</span></span><br><span class="line">wsl <span class="literal">-t</span> Ubuntu<span class="literal">-22</span>.<span class="number">04</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 6. 磁盘 / 路径 / 迁移</span></span><br><span class="line"><span class="comment"># 导出发行版为 tar（备份 / 迁移）</span></span><br><span class="line">wsl <span class="literal">--export</span> Ubuntu<span class="literal">-22</span>.<span class="number">04</span> D:\WSL\ubuntu2204.tar</span><br><span class="line"><span class="comment"># 导入到指定目录（核心）</span></span><br><span class="line">wsl <span class="literal">--import</span> Ubuntu<span class="literal">-22</span>.<span class="number">04</span> D:\WSL\Ubuntu<span class="literal">-22</span>.<span class="number">04</span> ubuntu2204.tar <span class="literal">--version</span> <span class="number">2</span></span><br><span class="line"><span class="comment"># 默认 ext4.vhdx 路径</span></span><br><span class="line">C:\Users\&lt;User&gt;\AppData\Local\Packages\</span><br><span class="line">  CanonicalGroupLimited.Ubuntu_XXXX\</span><br><span class="line">    LocalState\ext4.vhdx</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Linux</category>
        <category>WSL</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>WSL</tag>
      </tags>
  </entry>
  <entry>
    <title>WSL如何将Linux发行版安装到其他盘</title>
    <url>/2025/12/15/tools/wsl/wsl2-install-tutorial/</url>
    <content><![CDATA[<p>本文总结了使用 WSL 安装 Linux
发行版时如何将根文件系统安装到其他盘。</p>
<span id="more"></span>
<hr />
<h2 id="安装步骤">安装步骤</h2>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 选择要安装的发行版，查看可安装列表</span></span><br><span class="line">wsl <span class="literal">--list</span> <span class="literal">--online</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 安装发行版（但不启动）</span></span><br><span class="line">wsl <span class="literal">--install</span> <span class="literal">-d</span> Ubuntu<span class="literal">-22</span>.<span class="number">04</span> <span class="literal">--no-launch</span> <span class="comment"># 这一步无法选目录，只是为了拿到官方 rootfs</span></span><br><span class="line"><span class="comment"># 初次安装需重启</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 确认发行版已存在</span></span><br><span class="line">wsl <span class="literal">-l</span> <span class="literal">-v</span></span><br><span class="line"><span class="comment"># 应该能看到</span></span><br><span class="line"><span class="comment"># Ubuntu-22.04   Stopped   2</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 关闭所有 WSL 实例</span></span><br><span class="line">wsl <span class="literal">--shutdown</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. 导出为 tar（中间文件）</span></span><br><span class="line"><span class="comment"># 选择你想要的临时目录，例如：</span></span><br><span class="line">mkdir E:\WSL\tmp</span><br><span class="line"><span class="comment"># 导出tar，这个 `tar` 是完整 Linux 文件系统</span></span><br><span class="line">wsl <span class="literal">--export</span> Ubuntu<span class="literal">-22</span>.<span class="number">04</span> E:\WSL\tmp\ubuntu2204.tar <span class="comment"># </span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 6. 注销默认安装（⚠️不可恢复）</span></span><br><span class="line">wsl <span class="literal">--unregister</span> Ubuntu<span class="literal">-22</span>.<span class="number">04</span> <span class="comment"># 删除 C 盘的那份 Ubuntu，但 tar 还在</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 7. 导入到你指定的盘和目录</span></span><br><span class="line">mkdir E:\WSL\Ubuntu<span class="literal">-22</span>.<span class="number">04</span></span><br><span class="line">wsl <span class="literal">--import</span> Ubuntu<span class="literal">-22</span>.<span class="number">04</span> E:\WSL\Ubuntu<span class="literal">-22</span>.<span class="number">04</span> D:\WSL\tmp\ubuntu2204.tar <span class="literal">--version</span> <span class="number">2</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 8. 启动并初始化</span></span><br><span class="line"><span class="comment"># 首次进入后：需要重新创建 Linux 用户，密码重新设置一次（正常现象）</span></span><br><span class="line">wsl <span class="literal">-d</span> Ubuntu<span class="literal">-22</span>.<span class="number">04</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 9. 验证是否真的在其他盘</span></span><br><span class="line"><span class="comment"># Windows 侧查看</span></span><br><span class="line">E:\WSL\Ubuntu<span class="literal">-22</span>.<span class="number">04</span>\ext4.vhdx </span><br><span class="line"><span class="comment"># 看到这个文件说明成功</span></span><br><span class="line"><span class="comment"># Linux 侧确认</span></span><br><span class="line">df <span class="literal">-h</span> /</span><br><span class="line"></span><br><span class="line"><span class="comment"># 10. 删除 tar（可选但推荐）</span></span><br><span class="line"><span class="built_in">del</span> E:\WSL\tmp\ubuntu2204.tar</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="常见疑问速答">常见疑问速答</h2>
<h3
id="为什么不能一开始就选安装目录">为什么不能一开始就选安装目录？</h3>
<p>👉 微软当前 <strong>不支持 <code>wsl --install</code>
指定路径</strong></p>
<h3 id="一个盘能放多个发行版吗">一个盘能放多个发行版吗？</h3>
<p>✅ 完全可以：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">E:\WSL\</span><br><span class="line"> ├─ Ubuntu-22.04\</span><br><span class="line"> ├─ Ubuntu-24.04\</span><br><span class="line"> └─ Debian\</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Linux</category>
        <category>WSL</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>WSL</tag>
      </tags>
  </entry>
  <entry>
    <title>WSL2与VMware冲突解决方案</title>
    <url>/2025/12/29/tools/wsl/wsl2-vmware-conflict-solution/</url>
    <content><![CDATA[<p>本文总结了 WSL2 与 VMware 虚拟化冲突的解决方案。</p>
<span id="more"></span>
<hr />
<h2 id="结论">结论</h2>
<p>在 <strong>Windows 11 25H2 + VMware Workstation（17.x /
25H2）</strong> 下：</p>
<blockquote>
<p>✅ <strong>VMware 与 WSL2 可以共存</strong> ❌ <strong>但 VMware
不再是“直通 VT-x”的传统模式</strong> 👉 而是 <strong>运行在 Hyper-V
之上（兼容模式）</strong></p>
</blockquote>
<p>换句话说：</p>
<ul>
<li><strong>Hyper-V ON</strong>
<ul>
<li>WSL2 ✅</li>
<li>Docker Desktop（WSL2 后端）✅</li>
<li>VMware ✅（但走 Hyper-V 后端）</li>
</ul></li>
<li><strong>Hyper-V OFF</strong>
<ul>
<li>WSL2 ❌</li>
<li>VMware ✅（性能最好）</li>
</ul></li>
</ul>
<hr />
<h2 id="两种模式">两种模式</h2>
<table>
<thead>
<tr class="header">
<th>模式</th>
<th>Hyper-V</th>
<th>WSL2</th>
<th>VMware</th>
<th>性能</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Hyper-V ON</td>
<td>ON</td>
<td>✅</td>
<td>✅</td>
<td>⭐⭐⭐</td>
</tr>
<tr class="even">
<td>Hyper-V OFF</td>
<td>OFF</td>
<td>❌</td>
<td>✅</td>
<td>⭐⭐⭐⭐⭐</td>
</tr>
</tbody>
</table>
<p>Hyper-V ON模式下虚拟化路径如下①和③所示：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">路径 ①（Linux / Windows VM）</span><br><span class="line">硬件</span><br><span class="line"> └── Hyper-V (L0)</span><br><span class="line"><span class="code">       └── Windows</span></span><br><span class="line"><span class="code">             └── VMware / VirtualBox (L1)</span></span><br><span class="line"><span class="code">                   └── Linux / Windows VM (L2)</span></span><br><span class="line"><span class="code"></span></span><br><span class="line">路径 ②（macOS VM）</span><br><span class="line">硬件</span><br><span class="line"> └── VMware (L0)</span><br><span class="line"><span class="code">       └── macOS (L1)</span></span><br><span class="line"><span class="code"></span></span><br><span class="line">路径 ③（WSL2）</span><br><span class="line">硬件</span><br><span class="line"> └── Hyper-V (L0)</span><br><span class="line"><span class="code">       └── Windows</span></span><br><span class="line"><span class="code">             └── WSL2 (L1)</span></span><br></pre></td></tr></table></figure>
<p>由于苹果内核不支持Hyper-V这种虚拟化模型，<strong>macOS
虚拟机只能走路径 ②</strong>，即 Hyper-V OFF。</p>
<p>Hyper-V ON模式下性能损失是客观存在的：</p>
<table>
<thead>
<tr class="header">
<th>项目</th>
<th>直通 VT-x</th>
<th>Hyper-V 后端</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>CPU</td>
<td>100%</td>
<td>~85–90%</td>
</tr>
<tr class="even">
<td>I/O</td>
<td>原生</td>
<td>稍慢</td>
</tr>
<tr class="odd">
<td>Nested VT</td>
<td>强</td>
<td>弱</td>
</tr>
</tbody>
</table>
<p>由于Hyper-V
ON模式会导致VMware性能损失，此外我的虚拟机环境还有MacOS，MacOS
虚拟机不支持在 Hyper-V 之上的“二级虚拟化路径”，所以我优先Hyper-V
OFF，如果需要用到WSL2，再切换到Hyper-V
ON。（如果觉得每次切换需要重启麻烦，可以配置开机双启动的选项来选择）</p>
<hr />
<h2 id="hyper-v-off">Hyper-V OFF</h2>
<h3 id="如何切换">如何切换</h3>
<p>切换 VMware / VirtualBox：</p>
<figure class="highlight cmd"><table><tr><td class="code"><pre><span class="line">bcdedit /<span class="built_in">set</span> hypervisorlaunchtype off</span><br></pre></td></tr></table></figure>
<p>该命令控制<strong>Windows 启动时不会再加载 Hyper-V 的
Hypervisor</strong>，也就是说：</p>
<ul>
<li>Hyper-V 不工作</li>
<li>WSL2 不工作</li>
<li>Virtual Machine Platform 不工作</li>
<li>Windows Sandbox 不工作</li>
<li><strong>CPU 的 VT-x / SVM 虚拟化能力释放给第三方虚拟机</strong></li>
</ul>
<p>切换到 WSL2：</p>
<figure class="highlight cmd"><table><tr><td class="code"><pre><span class="line">bcdedit /<span class="built_in">set</span> hypervisorlaunchtype auto</span><br></pre></td></tr></table></figure>
<p>👉 <strong>相互切换必须重启才生效</strong></p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">shutdown /<span class="built_in">r</span> /t <span class="number">0</span> <span class="comment"># 或手动重启</span></span><br></pre></td></tr></table></figure>
<p>换来的是：</p>
<ul>
<li>✔ VMware 满血</li>
<li>✔ WSL2 满血</li>
<li>✔ 不互相妥协</li>
</ul>
<h3 id="快捷切换脚本">快捷切换脚本</h3>
<p>可将下面代码保存为切换脚本文件<code>toggle_hypervisor.bat</code>：</p>
<figure class="highlight bat"><table><tr><td class="code"><pre><span class="line">@<span class="built_in">echo</span> off</span><br><span class="line"><span class="built_in">chcp</span> <span class="number">437</span> &gt;<span class="built_in">nul</span></span><br><span class="line"><span class="built_in">setlocal</span> EnableDelayedExpansion</span><br><span class="line"></span><br><span class="line">:: ================================</span><br><span class="line">:: Admin check</span><br><span class="line">:: ================================</span><br><span class="line"><span class="built_in">net</span> session &gt;<span class="built_in">nul</span> <span class="number">2</span>&gt;&amp;<span class="number">1</span></span><br><span class="line"><span class="keyword">if</span> <span class="variable">%errorlevel%</span> <span class="keyword">neq</span> <span class="number">0</span> (</span><br><span class="line">    <span class="built_in">echo</span> Please run this script as Administrator.</span><br><span class="line">    <span class="built_in">pause</span></span><br><span class="line">    <span class="keyword">exit</span> /b <span class="number">1</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">:: ================================</span><br><span class="line">:: Get current hypervisorlaunchtype</span><br><span class="line">:: ================================</span><br><span class="line"><span class="built_in">set</span> CURRENT=</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> /f &quot;tokens=<span class="number">2</span>&quot; <span class="variable">%%i</span> <span class="keyword">in</span> (&#x27;</span><br><span class="line">    bcdedit /enum &#123;current&#125; ^| <span class="built_in">findstr</span> /i hypervisorlaunchtype</span><br><span class="line">&#x27;) <span class="keyword">do</span> (</span><br><span class="line">    <span class="built_in">set</span> CURRENT=<span class="variable">%%i</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> <span class="keyword">not</span> <span class="keyword">defined</span> CURRENT (</span><br><span class="line">    <span class="built_in">set</span> CURRENT=Auto</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">:MENU</span><br><span class="line"><span class="built_in">cls</span></span><br><span class="line"><span class="built_in">echo</span> ================================</span><br><span class="line"><span class="built_in">echo</span>  Hypervisor Launch <span class="built_in">Type</span> Manager</span><br><span class="line"><span class="built_in">echo</span> ================================</span><br><span class="line"><span class="built_in">echo</span>.</span><br><span class="line"><span class="built_in">echo</span> Current state: <span class="variable">%CURRENT%</span></span><br><span class="line"><span class="built_in">echo</span>.</span><br><span class="line"><span class="built_in">echo</span> <span class="number">1</span>. Toggle (Auto ^&lt;^&gt; Off)</span><br><span class="line"><span class="built_in">echo</span> <span class="number">2</span>. <span class="built_in">Set</span> Auto  (Enable Hyper-V)</span><br><span class="line"><span class="built_in">echo</span> <span class="number">3</span>. <span class="built_in">Set</span> Off   (Disable Hyper-V)</span><br><span class="line"><span class="built_in">echo</span> <span class="number">4</span>. <span class="keyword">Exit</span></span><br><span class="line"><span class="built_in">echo</span>.</span><br><span class="line"></span><br><span class="line"><span class="built_in">set</span> /p CHOICE=Select (<span class="number">1</span>-<span class="number">4</span>): </span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> &quot;<span class="variable">%CHOICE%</span>&quot;==&quot;<span class="number">1</span>&quot; <span class="keyword">goto</span> TOGGLE</span><br><span class="line"><span class="keyword">if</span> &quot;<span class="variable">%CHOICE%</span>&quot;==&quot;<span class="number">2</span>&quot; <span class="keyword">goto</span> SET_AUTO</span><br><span class="line"><span class="keyword">if</span> &quot;<span class="variable">%CHOICE%</span>&quot;==&quot;<span class="number">3</span>&quot; <span class="keyword">goto</span> SET_OFF</span><br><span class="line"><span class="keyword">if</span> &quot;<span class="variable">%CHOICE%</span>&quot;==&quot;<span class="number">4</span>&quot; <span class="keyword">goto</span> END</span><br><span class="line"></span><br><span class="line"><span class="built_in">echo</span> Invalid selection.</span><br><span class="line">timeout /t <span class="number">1</span> &gt;<span class="built_in">nul</span></span><br><span class="line"><span class="keyword">goto</span> MENU</span><br><span class="line"></span><br><span class="line">:TOGGLE</span><br><span class="line"><span class="keyword">if</span> /i &quot;<span class="variable">%CURRENT%</span>&quot;==&quot;Auto&quot; (</span><br><span class="line">    <span class="built_in">set</span> NEW=off</span><br><span class="line">) <span class="keyword">else</span> (</span><br><span class="line">    <span class="built_in">set</span> NEW=auto</span><br><span class="line">)</span><br><span class="line"><span class="keyword">goto</span> APPLY</span><br><span class="line"></span><br><span class="line">:SET_AUTO</span><br><span class="line"><span class="built_in">set</span> NEW=auto</span><br><span class="line"><span class="keyword">goto</span> APPLY</span><br><span class="line"></span><br><span class="line">:SET_OFF</span><br><span class="line"><span class="built_in">set</span> NEW=off</span><br><span class="line"><span class="keyword">goto</span> APPLY</span><br><span class="line"></span><br><span class="line">:APPLY</span><br><span class="line"><span class="built_in">echo</span>.</span><br><span class="line"><span class="built_in">echo</span> Setting hypervisorlaunchtype to <span class="variable">%NEW%</span> ...</span><br><span class="line">bcdedit /<span class="built_in">set</span> hypervisorlaunchtype <span class="variable">%NEW%</span></span><br><span class="line"><span class="keyword">if</span> <span class="keyword">errorlevel</span> <span class="number">1</span> (</span><br><span class="line">    <span class="built_in">echo</span> Failed to update hypervisorlaunchtype.</span><br><span class="line">    <span class="built_in">pause</span></span><br><span class="line">    <span class="keyword">exit</span> /b <span class="number">1</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="built_in">set</span> CURRENT=<span class="variable">%NEW%</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">echo</span>.</span><br><span class="line"><span class="built_in">set</span> /p REBOOT=Reboot now? (Y/N): </span><br><span class="line"><span class="keyword">if</span> /i &quot;<span class="variable">%REBOOT%</span>&quot;==&quot;Y&quot; (</span><br><span class="line">    shutdown /r /t <span class="number">0</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">goto</span> MENU</span><br><span class="line"></span><br><span class="line">:END</span><br><span class="line"><span class="keyword">exit</span> /b <span class="number">0</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>以管理员身份运行，效果如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">================================</span><br><span class="line"> Hypervisor Launch Type Manager</span><br><span class="line">================================</span><br><span class="line"></span><br><span class="line">Current state: Auto</span><br><span class="line"></span><br><span class="line">1. Toggle (Auto &lt;&gt; Off)</span><br><span class="line">2. Set Auto  (Enable Hyper-V)</span><br><span class="line">3. Set Off   (Disable Hyper-V)</span><br><span class="line">4. Exit</span><br><span class="line"></span><br><span class="line">Select (1-4):</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="hyper-v-on">Hyper-V ON</h2>
<h3 id="核心思想">核心思想</h3>
<p>让 VMware <strong>接受 Hyper-V
的存在</strong>，使用微软的虚拟化平台：</p>
<ul>
<li>WHP（Windows Hypervisor Platform）</li>
<li>VBS（Virtualization-Based Security）</li>
</ul>
<h3 id="开启-windows-必要组件">开启 Windows 必要组件</h3>
<p>打开：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">控制面板</span><br><span class="line">→ 程序</span><br><span class="line">→ 启用或关闭 Windows 功能</span><br></pre></td></tr></table></figure>
<p>✅ 勾选：☑ Virtual Machine Platform</p>
<h3 id="确认-hyper-v-启动">确认 Hyper-V 启动</h3>
<p>Windows PowerShell 执行：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">bcdedit /<span class="class"><span class="keyword">enum</span> | <span class="title">findstr</span> <span class="title">hypervisor</span></span></span><br></pre></td></tr></table></figure>
<p>应看到：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">hypervisorlaunchtype    Auto</span><br></pre></td></tr></table></figure>
<p>❗ <strong>不要设为 off</strong></p>
<h3 id="验证是否真的共存">验证是否真的共存</h3>
<p>WSL2 验证：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">wsl <span class="literal">-l</span> <span class="literal">-v</span></span><br></pre></td></tr></table></figure>
<p>输出类似：<code>Ubuntu-22.04   Running   2</code>。</p>
<p>VMware 验证：</p>
<p>正常启动虚拟机，不会再提示：<code>VMware Workstation and Hyper-V are not compatible</code>。</p>
<blockquote>
<p>[!CAUTION]</p>
<p>MacOS 虚拟机在此方式下无法打开。</p>
</blockquote>
]]></content>
      <categories>
        <category>Linux</category>
        <category>WSL</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>WSL</tag>
      </tags>
  </entry>
  <entry>
    <title>二分查找算法设计与实现</title>
    <url>/2024/02/05/programming/data-structure-algorithm/binary-search-algorithm/</url>
    <content><![CDATA[<p>本文介绍了二分查找算法，首先分析了算法的基本实现思想，接着使用C语言以递归和循环两种方式进行代码实现，并且给出了一些二分查找的变体代码，最后简述了其典型的应用场景。</p>
<span id="more"></span>
<hr />
<h2 id="基本思想">基本思想</h2>
<p>二分查找的基本思想如下：</p>
<ol type="1">
<li><strong>找到中间元素：</strong>查找过程从数组的中间元素开始，如果中间元素恰好是目标元素，则查找过程结束。</li>
<li><strong>比较并缩小范围：</strong>如果中间元素不是目标元素，那么将中间元素与目标值进行比较：
<ul>
<li>如果目标值小于中间元素，则说明目标值位于当前搜索范围的左半部分。</li>
<li>如果目标值大于中间元素，则说明目标值位于当前搜索范围的右半部分。</li>
</ul></li>
<li><strong>重复上述过程。</strong>不断重复上述过程，每次都将搜索范围缩小一半，直到找到目标值或者发现目标元素不存在。</li>
</ol>
<p>二分查找的关键在于每次比较后都能排除一半的搜索范围，这使得查找效率非常高，尤其是在大型数组中。</p>
<p><strong>这种算法的时间复杂度为 <span class="math inline">\(O(\log
n)\)</span>，其中 <span class="math inline">\(n\)</span>
是数组的长度。</strong></p>
<hr />
<h2 id="代码实现">代码实现</h2>
<h3 id="递归实现">递归实现</h3>
<details>
<summary>
<font size="4" color="orange">递归实现二分查找</font>
</summary>
<pre><code class="language-c">#include &lt;stdio.h&gt;
#define ARR_LEN(arr) (sizeof(arr)/sizeof(arr[0]))
int bs_recursion(int arr[], int left, int right, int target) {
    // 递归的出口
    // 如果区间已不存在, 则二分查找结束, 没找到目标元素
    if (left > right) {
        return -1;
    }
    int mid = left + (right - left >> 1);
    int diff = target - arr[mid];
    if (diff < 0)
        return bs_recursion(arr, left, mid - 1, target);
    if (diff > 0)
        return bs_recursion(arr, mid + 1, right, target);
    return mid;
}
int binary_search(int arr[], int len, int target) {
    bs_recursion(arr, 0, len - 1, target);
}
int main(void) {
    int arr[] = { 0,3,5,7,7,9,10, 20,100 };
    int len = ARR_LEN(arr);
    int target = 7;
    int idx = binary_search(arr, len, target);
    printf("查找目标元素%d, 它在数组中的索引是%d\n", target, idx);
    return 0;
}</code>
</pre>
</details>
<h3 id="循环实现">循环实现</h3>
<details>
<summary>
<font size="4" color="orange">循环实现二分查找</font>
</summary>
<pre><code class="language-c">#include &lt;stdio.h&gt;
#define ARR_LEN(arr) (sizeof(arr)/sizeof(arr[0]))
int binary_search(int arr[], int len, int target) {
    int left = 0, right = len - 1;
    while (left <= right) {
        int mid = left + (right - left >> 1);
        int diff = target - arr[mid];
        if (diff < 0)
            right = mid - 1;
        else if (diff > 0)
            left = mid + 1;
        else
            return mid;
    }
    return -1;
}
int main(void) {
    int arr[] = { 0,3,5,7,7,9,10,20,100 };
    int len = ARR_LEN(arr);
    int target = 100;
    int idx = binary_search(arr, len, target);
    printf("查找目标元素%d, 它在数组中的索引是%d\n", target, idx);
    return 0;
}</code>
</pre>
</details>
<h3 id="二分查找变体">二分查找变体</h3>
<details>
<summary>
<font size="4" color="orange">查找数组中第一个与目标值相等的元素</font>
</summary>
<pre><code class="language-c">#include &lt;stdio.h&gt;
#define ARR_LEN(arr) (sizeof(arr)/sizeof(arr[0]))
int binary_search_first(int arr[], int len, int target) {
    int left = 0, right = len - 1;
    while (left <= right) {
        int mid = left + (right - left >> 1);
        int diff = target - arr[mid];
        if (diff < 0)
            right = mid - 1;
        else if (diff > 0)
            left = mid + 1;
        else {
            if (mid == left || arr[mid - 1] < target)
                return mid;
            right = mid - 1;
        }
    }
    return -1;
}
int main(void) {
    int arr[] = { 0,3,5,7,7,9,10,20,100 };
    int len = ARR_LEN(arr);
    int target = 7;
    int idx = binary_search_first(arr, len, target);
    printf("查找目标元素%d, 它在数组中的第一个索引是%d\n", target, idx);
    return 0;
}</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange">查找数组中最后一个与目标值相等的元素</font>
</summary>
<pre><code class="language-c">#include &lt;stdio.h&gt;
#define ARR_LEN(arr) (sizeof(arr)/sizeof(arr[0]))
int binary_search_last(int arr[], int len, int target) {
    int left = 0, right = len - 1;
    while (left <= right) {
        int mid = left + (right - left >> 1);
        int diff = target - arr[mid];
        if (diff < 0)
            right = mid - 1;
        else if (diff > 0)
            left = mid + 1;
        else {
            if (mid == right || arr[mid + 1] > target)
                return mid;
            left = mid + 1;
        }
    }
    return -1;
}
int main(void) {
    int arr[] = { 0,3,5,7,7,7,9,10,20,100 };
    int len = ARR_LEN(arr);
    int target = 7;
    int idx = binary_search_last(arr, len, target);
    printf("查找目标元素%d, 它在数组中的最后一个索引是%d\n", target, idx);
    return 0;
}</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange">查找数组中第一个大于等于目标值的元素</font>
</summary>
<pre><code class="language-c">#include &lt;stdio.h&gt;
#define ARR_LEN(arr) (sizeof(arr)/sizeof(arr[0]))
int binary_search_first_gt(int arr[], int len, int target) {
    int left = 0, right = len - 1;
    while (left <= right) {
        int mid = left + (right - left >> 1);
        if (arr[mid] < target)
            left = mid + 1;
        else {
            if (mid == left || arr[mid - 1] < target)
                return mid;
            right = mid - 1;
        }
    }
    return -1;
}
int main(void) {
    int arr[] = { 0,3,5,7,7,7,9,10,20,100 };
    int len = ARR_LEN(arr);
    int target = 7;
    int idx = binary_search_first_gt(arr, len, target);
    printf("查找数组中第一个大于等于目标值%d的元素, 它是%d, 在索引%d\n", target,arr[idx], idx);
    return 0;
}</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange">查找数组中最后一个小于等于目标值的元素</font>
</summary>
<pre><code class="language-c">#include &lt;stdio.h&gt;
#define ARR_LEN(arr) (sizeof(arr)/sizeof(arr[0]))
int binary_search_last_lt(int arr[], int len, int target) {
    int left = 0, right = len - 1;
    while (left <= right) {
        int mid = left + (right - left >> 1);
        if (arr[mid] > target)
            right = mid - 1;
        else {
            if (mid == right || arr[mid + 1] > target)
                return mid;
            left = mid + 1;
        }
    }
    return -1;
}
int main(void) {
    int arr[] = { 0,3,5,7,7,7,9,10,20,100 };
    int len = ARR_LEN(arr);
    int target = 8;
    int idx = binary_search_last_lt(arr, len, target);
    printf("查找数组中最后一个小于等于目标值%d的元素, 它是%d, 在索引%d\n", target, arr[idx], idx);
    return 0;
}</code>
</pre>
</details>
<hr />
<h2 id="典型应用">典型应用</h2>
<h3 id="ip归属地查询">IP归属地查询</h3>
<h3 id="改进插入排序">改进插入排序</h3>
<p>二分查找是可以改进插入排序的，其核心思路是，每一轮插入排序都会有一张”新抓手牌”，而”旧手牌”是一个已经有序的数组。传统的做法是：</p>
<p>让”新手牌”逐一和”旧手牌”有序数组中的元素逐一比较，然后确定”新手牌”的插入位置，向后移动元素，将”新手牌”插入。如下图所示：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202401150718625.png" alt="二分查找-改进插入排序" style="zoom: 50%;" />
</div>
<p>当数据量比较大时，“旧手牌”比较多，有序数组很长时，可以考虑用二分查找，替代逐一比较查找待插入位置。那么这个二分查找是在找什么呢？</p>
<p>当然是查找<strong>最后一个小于等于”新手牌”值的元素</strong>，这样就可以直接将它后面的所有元素后移一位，然后再它后面直接插入”新手牌”。</p>
<p>当然，这种改进在实际应用中并不常见：</p>
<ol type="1">
<li>插入排序本身就应用于小数据集，大数据集情况下大多不会选择插入排序。</li>
<li>既然是应用于小数据集，数组本身较短，用二分查找的意义就不大了。对效率的提升不明显，而且还增加了复杂性。</li>
</ol>
<details>
<summary>
<font size="4" color="orange">使用二分查找改进的插入排序</font>
</summary>
<pre><code class="language-c">#include &lt;stdio.h&gt;
#define ARR_LEN(arr) (sizeof(arr)/sizeof(arr[0]))
int binary_sort(int arr[], int left,int right, int target) {
    while (left <= right) {
        int mid = left + (right - left >> 1);
        if (arr[mid] > target)
            right = mid - 1;
        else
        {
            if (mid == right || arr[mid + 1] > target)
                return mid;
            left = mid + 1;
        }
    }
    return -1;
}
void binary_insertion_sort(int arr[], int len) {
    for (int i = 1; i < len; i++)
    {
        int value = arr[i];
        // 使用二分查找找到应插入的位置
        // idx(不包括)后面的所有元素都有后移一位，idx + 1是value要插入的位置
        int idx = binary_sort(arr, 0,i-1, value);
        int j = i - 1;  // j在这里赋值为有序数组的最后一个元素
        while (j > idx) {       // 有序数组中的元素只要下标大于idx就后移 也就是不包括idx，但idx后面的所有元素都要后移
            arr[j + 1] = arr[j];
            j--;
        } // j == idx;
        arr[j + 1] = value;
    }
}
int main(void) {
    int arr[] = { 9, 5, 2, 7, 1, 3, 4, 0, 8, 6 };
    int len = ARR_LEN(arr);
    binary_insertion_sort(arr, len);
    for (int i = 0; i < len; i++) 
        printf("%d ", arr[i]);
    printf("\n");
    return 0;
}</code>
</pre>
</details>
]]></content>
      <categories>
        <category>数据结构与算法（DSA）</category>
        <category>算法</category>
      </categories>
      <tags>
        <tag>二分法</tag>
      </tags>
  </entry>
  <entry>
    <title>常用数据结构设计与实现</title>
    <url>/2024/01/06/programming/data-structure-algorithm/common-data-structure/</url>
    <content><![CDATA[<p>日常工作开发中常用的数据结构主要包括数组、链表、栈、队列、哈希表和二叉搜索树，本文介绍这些数据结构的设计思路，并且给出了C语言版本的代码实现。</p>
<span id="more"></span>
<hr />
<p>在日常的工作开发中，最常用的数据结构有：数组、链表、栈、队列、哈希表和二叉搜索树。其中：</p>
<ol type="1">
<li><p><strong>数组和链表是线性数据结构的两种典型物理实现。</strong>
它们是最基础的数据结构，是构成其它更复杂数据结构的基石。</p>
<ul>
<li><p>数组采用一段连续的内存空间存储，最大的特点是基于索引的快速随机访问。</p></li>
<li><p>链表则由非连续存储的、基于指针链接的结点组成，提供了灵活的插入和删除操作。</p></li>
</ul></li>
<li><p><strong>栈和队列是数组和链表的经典应用场景。</strong>
它们都可以基于数组或链表实现，不同的是栈遵循先进后出的原则，而队列遵循先进先出的原则。</p></li>
<li><p><strong>哈希表</strong>
通常结合数组与链表来实现，用于提供快速的数据查询能力。</p></li>
<li><p><strong>二叉搜索树</strong>
是一种基于树形的数据结构，它提供了优秀的搜索效率。二叉搜索树在实现时可以借鉴链表的实现思路，所以它和链表也有一定的关联。</p></li>
</ol>
<p><strong>如何学习数据结构？</strong></p>
<p>学习一种数据结构，可以从三个角度入手：</p>
<ol type="1">
<li><strong>理论基础与模型。</strong>
学习一种数据结构，首先要做的就是理解它的概念模型以及知晓其理论基础，包括其特点、用途和适用场景等。</li>
<li><strong>基础操作。</strong>
在掌握了基本理论后，就需要进一步熟悉该数据结构的基础操作以及这些操作的工作原理。这些基础操作一般包括：增、删、查以及遍历。(改操作是查操作的扩展)</li>
<li><strong>实现与应用。</strong>
在了解理论和操作后，就可以编写代码来实现这些数据结构了，这不仅有助于巩固理解数据结构，同时也能提升编程能力。</li>
</ol>
<p><strong>推荐两个学习网站：</strong></p>
<ol type="1">
<li><span class="exturl" data-url="aHR0cHM6Ly92aXN1YWxnby5uZXQvemgv">Visualgo
可视化数据结构与算法<i class="fa fa-external-link-alt"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly93d3cuY3MudXNmY2EuZWR1L35nYWxsZXMvdmlzdWFsaXphdGlvbi9BbGdvcml0aG1zLmh0bWw=">Data
Structure Visualization (usfca.edu)<i class="fa fa-external-link-alt"></i></span></li>
</ol>
<hr />
<h2 id="动态数组">动态数组</h2>
<p>数组是一种基本的、常见的数据结构，它以其快速的访问速度而著称。</p>
<p>然而，在 C
语言中，传统的数组(指在栈上分配内存空间的数组)拥有一个显著的限制：它们的长度一旦确定便无法更改。这在处理动态数据集时显得尤为不便，如在不确定数据量的情况下存储用户输入。</p>
<p>C++通过引入 Vector 这样的动态数组解决了这个问题(Java 中的 ArrayList
也是类似的)，它可以根据需要动态调整大小。遗憾的是，C
语言标准库中并未提供类似的结构。但不要担心，借助于动态内存分配函数如
malloc 和 realloc，我们完全可以在 C 中手动实现类似的功能。</p>
<h3 id="设计思路">设计思路</h3>
<p>在 C 语言中实现一个动态数组，通常使用一个结构体定义，如下所示：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 使用别名来命名元素类型，如果未来需要改变元素类型，只需修改这个别名即可。</span></span><br><span class="line"><span class="comment">// 这么做提升代码的可维护性和扩展性, 这实际上是模拟了 C++的泛型编程</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="type">int</span> ElementType;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    ElementType *data;      <span class="comment">// 指向动态分配数组的指针</span></span><br><span class="line">    <span class="type">int</span> size;    <span class="comment">// 当前动态数组中元素的数量</span></span><br><span class="line">    <span class="type">int</span> capacity; <span class="comment">// 动态数组当前分配的最大容量</span></span><br><span class="line">&#125; Vector;</span><br></pre></td></tr></table></figure>
可以用下图来理解一个 Vector 动态数组：
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202311131037946.png" alt="Vector动态数组-内存示意图" style="zoom: 33%;" />
</div>
<p>除此之外，为了满足正常的动态数组的功能，我们还需要实现以下函数：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 初始化一个 Vector 动态数组.这实际上是模拟了 C++的默认构造函数</span></span><br><span class="line">Vector* <span class="title function_">vector_create</span><span class="params">()</span>;</span><br><span class="line"><span class="comment">// 销毁一个 Vector 动态数组，释放内存。这实际上模拟了 C++的析构函数</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">vector_destroy</span><span class="params">(Vector* v)</span>;</span><br><span class="line"><span class="comment">// 向动态数组末尾添加一个元素</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">vector_push_back</span><span class="params">(Vector* v, ElementType element)</span>;</span><br><span class="line"><span class="comment">// 在动态数组最前面添加元素，所有元素依次后移</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">vector_push_front</span><span class="params">(Vector* v, ElementType val)</span>;</span><br><span class="line"><span class="comment">// 将元素 val 添加到索引为 idx 的位置，idx 后面的元素依次后移</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">vector_insert</span><span class="params">(Vector* v, <span class="type">int</span> idx, ElementType val)</span>;</span><br><span class="line"><span class="comment">// 遍历打印整个 Vector 动态数组</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">vector_print</span><span class="params">(Vector* v)</span>;</span><br></pre></td></tr></table></figure>
<h3 id="代码实现">代码实现</h3>
<details>
<summary>
<font size="4" color="orange"> vector.h </font>
</summary>
<pre> <code class="language-c"> #pragma once
// 使用别名来命名元素类型，如果未来需要改变元素类型，只需修改这个别名即可。
// 这么做提升代码的可维护性和扩展性, 这实际上是模拟了 C++/Java 等编程语言的泛型编程
typedef int ElementType;
typedef struct {
    ElementType* data;      // 指向动态分配数组的指针
    int size;    // 当前动态数组中元素的数量
    int capacity; // 动态数组当前分配的最大容量
} Vector;
// 初始化一个 Vector 动态数组.这实际上是模拟了 C++的默认构造函数
Vector* vector_create();
// 销毁一个 Vector 动态数组，释放内存。这实际上模拟了 C++的析构函数
void vector_destroy(Vector* v);
// 向动态数组末尾添加一个元素
void vector_push_back(Vector* v, ElementType element);
// 在动态数组最前面添加元素，所有元素依次后移
void vector_push_front(Vector* v, ElementType val);
// 将元素 val 添加到索引为 idx 的位置，idx 后面的元素依次后移
void vector_insert(Vector* v, int idx, ElementType val);
// 遍历打印整个 Vector 动态数组
void vector_print(Vector* v);
  </code> </pre>
</details>
<p>头文件主要用于存放以下结构：</p>
<ol type="1">
<li>函数的声明</li>
<li>结构体的定义</li>
<li>类型别名的定义</li>
<li>宏定义</li>
<li>…</li>
</ol>
<p><strong>头文件允许程序员在多个源文件之间共享函数的声明以及结构体、类型别名和宏的定义，从这个角度讲头文件类似
C++、Java 等编程语言中的接口。</strong></p>
<p>普遍采取的做法是：<strong>在头文件中声明函数，然后在某个源文件中包含头文件实现这些函数，最后在其余源文件中包含头文件，使用这些函数。</strong></p>
<p>使用头文件，促进了 <strong>模块化编程</strong>，提高了
<strong>代码的复用性</strong>，使得代码更加易于维护。</p>
<p><strong>注意：头文件作为接口的目的是对外暴露公共部分，某些不属于公共部分不需要对外暴露的不要放在头文件当中！！！</strong></p>
<p>包含头文件与实现函数</p>
<details>
<summary>
<font size="4" color="orange"> vector.c </font>
</summary>
<pre> <code class="language-c">#include "vector.h"
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#define DEFAULT_CAPACITY 10 // 设置动态数组的默认最小容量
#define THRESHOLD 1024
static void vector_resize(Vector* v) {
    // 只要调用这个函数肯定就是需要扩容的
    int old_capacity = v-> capacity;
    int new_capacity = (old_capacity < THRESHOLD) ?
        (old_capacity << 1) :   // 容量还未超出阈值每次扩容 2 倍
        (old_capacity + (old_capacity >> 1));   // 容量超出阈值每次扩容 1.5 倍
    // 利用 realloc 重新分配动态数组
    ElementType * tmp = realloc(v-> data, new_capacity * sizeof(ElementType));    // realloc 惯用法
    if (tmp == NULL) {
        printf("realloc failed in resize_vector.\n");
        exit(1);    // 扩容失败，退出整个程序。或者也可以做别的处理
    }
    // 扩容成功，重新赋值 Vector 成员
    v-> data = tmp;
    v-> capacity = new_capacity;
}
Vector* vector_create() {
    Vector * v = (Vector*)calloc(1, sizeof(Vector));
    if (! v)
    {
        printf("Error: calloc failed in vector_create!\n");
        return NULL;
    }
    v-> data = (ElementType*)calloc(DEFAULT_CAPACITY, sizeof(ElementType));
    if (! v-> data) {
        printf("Error: calloc failed in vector_create!\n");
        free(v);
        return NULL;
    }
    // 继续初始化 Vector 的其它成员
    v-> capacity = DEFAULT_CAPACITY;
    // size 已自动初始化为 0 值，所以不需要再次赋值了。但如果用 malloc 就不要忘记初始化它
    return v;
}
void vector_destroy(Vector* v) {
    free(v-> data);
    free(v);
}
void vector_push_back(Vector* v, ElementType element) {
    if (v-> size == v-> capacity)
    {
        vector_resize(v);
    }
    v-> data [v-> size] = element;
    v-> size++;
}
void vector_push_front(Vector* v, ElementType val) {
    if (v-> size == v-> capacity)
    {
        vector_resize(v);
    }
    // 扩容完成后或不需要扩容，即从首元素开始将所有元素向后移动
        // 倒着遍历数组方便向后移动元素
    for (int i = v-> size - 1; i >= 0; i--) {
        v-> data [i + 1] = v-> data [i];
    }
    // 新增的元素成为首元素
    v-> data [0] = val;
    v-> size++;
}
void vector_insert(Vector* v, int idx, ElementType val) {
    // 先做参数校验，避免传入非法索引
    if (idx < 0 || idx > v-> size) {     // 做插入操作时索引合法范围是 [0, size]
        printf("Illegal argument: idx = %d, size = %d\n", idx, v-> size);
        exit(1);    // 直接退出进程，也可以用别的方式进行错误处理
    }
    if (v-> size == v-> capacity)
    {
        vector_resize(v);
    }
    for (int i = v-> size - 1; i >= idx; i--) {
        v-> data [i + 1] = v-> data [i];
    }
    v-> data [idx] = val;
    v-> size++;
}
void vector_print(Vector* v) {
    printf("[");
    for (int i = 0; i < v-> size; i++) {
        printf("%d, ", v-> data [i]);
    }
    printf("\b\b]\n");
}</code> </pre>
</details>
<p>完成一系列关于 Vector 的开发实现后，接下来的关键步骤就是测试。</p>
<p><strong>在实际的开发中，当我们完成了某个独立模块、组件的开发后，都需要编写一系列测试用例，进行彻底的单元测试工作，来及时的发现和修复程序中的缺陷和
bug，从而提高整体代码质量。</strong></p>
<p>编写测试用例，完成单元测试开发，也是一个普通程序员的本职工作之一。</p>
<p>在这里，可以创建一个 “test.c”
的源文件，用于运行一系列的测试用例：</p>
<ol type="1">
<li>创建和销毁。验证 Vector 是否可以成功创建和销毁，没有内存泄漏。</li>
<li>添加元素：测试向 Vector
添加元素的功能，确保数组可以正确地扩容。</li>
<li>边界条件检查：检查 Vector
在极端情况下（如恰好超出容量）的行为是否正确。</li>
<li>….</li>
</ol>
<details>
<summary>
<font size="4" color="orange"> test.c </font>
</summary>
<pre> <code class="language-c">#define _CRT_SECURE_NO_WARNINGS
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#include "vector.h"
#define N 100
int main(void) {
    Vector *v = vector_create();
    if (v == NULL) {
        printf("error: vector_create failed in main.\n");
        exit(1);
    }
    // 测试自动扩容机制
    for (int i = 0; i < N; ++i) {
        vector_push_back(v, i + 1);
    }
    vector_print(v);    // 期望打印结果: 1-100
    // 测试头部插入新元素
    vector_push_front(v, 0);
    vector_print(v);    // 期望打印结果: 0, 1-100
    // 测试根据索引插入新元素
    vector_insert(v, 0, -1);
    vector_print(v);    // 期望打印结果: -1, 0, 1-100
    vector_destroy(v);
    return 0;
}</code>
</pre>
</details>
<hr />
<h2 id="链表">链表</h2>
<p>链表：是一种线性数据结构，由一系列结点 <strong>“链接”</strong>
构成。</p>
<p>结点：在 C
语言中，一个结点通常是一个结构体对象。该结构体对象由数据域和指针域两部分组成。数据域用于存储数据，而指针域则用于存放其它结点的指针。</p>
<p>如下图所示(其中 D 代表 <strong>数据域</strong>，P 代表
<strong>指针域</strong>)：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202311152035255.png" alt="结点和链表-示意图" style="zoom: 33%;" />
</div>
<p>常见的链表主要可以分为以下几种类型：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202311152050220.png" alt="常见链表分类-示意图" style="zoom: 33%;" />
</div>
<h3 id="单向链表">单向链表</h3>
<p>单向链表是由一系列结点单向链接组成的数据结构，简称单链表。</p>
<p>基础概念：</p>
<ol type="1">
<li><p><strong>结点：</strong>
链表中的每一个存储单元被称为结点，每个结点都包含两部分：</p>
<ol type="1">
<li>一部分用于存储结点的数据(数据域)</li>
<li>另一部分用于存储指向下一个结点的指针(指针域)。</li>
</ol>
<p>在 C 程序中一个结点，就是一个结点结构体类型的对象。</p></li>
<li><p><strong>头结点：</strong>
某些单链表的实现中，会在第一个结点前附设一个
<strong>“一般不存储数据或者存储链表长度信息”</strong>
的结点，这个结点就是头结点。</p>
<ol type="1">
<li>头结点也叫做 “哑结点”、“虚拟结点”、“哨兵结点”
等。头结点的指针域指向链表的第一个结点。</li>
<li>在实际应用中，单链表一般不带头结点，但在需要统一链表头部操作的场景中，头结点具有
“奇效”。</li>
</ol></li>
<li><p><strong>尾结点：</strong> 单链表的最后一个结点。它的指针域指向
NULL，表示链表的结束。</p></li>
<li><p><strong>头指针：</strong>
指向链表第一个结点的指针，<strong>头指针非常重要，单链表的任何操作都是以头指针为入口的，单链表一定会有头指针。</strong>
具体而言：</p>
<ol type="1">
<li>如果链表为空，则头指针为 NULL，是一个空指针。</li>
<li>如果单链表没有头结点，那么头指针就指向链表的第一个存储数据的结点。</li>
<li>如果单链表有头结点，那么头指针就指向该头结点。总之，头指针就指向单链表的第一个结点。</li>
</ol></li>
<li><p><strong>尾指针：</strong>
指向链表最后一个结点的指针。对于单链表而言，尾指针不是必须的，但拥有尾指针可以使得链表尾部的操作更加简单高效。</p></li>
</ol>
<p>基本操作：</p>
<ol type="1">
<li><p>创建链表</p></li>
<li><p>销毁链表</p></li>
<li><p>插入结点：对于链表而言，只要在一个确定的结点后面插入结点，都是一个基础的，时间复杂度为
O(1)的操作。</p>
<ol type="1">
<li>在一个存在头指针和尾指针的链表当中，头插和尾插就是基础的插入结点操作。</li>
<li>更复杂的插入操作都是对基础插入操作的扩展，比如根据索引插入一个结点。</li>
</ol></li>
<li><p>搜索结点：链表并不支持随机访问，任何的访问操作都意味着遍历链表。</p>
<ol type="1">
<li>所以不管是有序无序还是搜索的条件是什么，单链表的搜索操作时间复杂度总是
O(n)</li>
<li>比较常见的搜索条件是：根据索引查找或者根据数据查找</li>
</ol></li>
<li><p>删除结点：对于链表而言，只要是删除一个确定结点后面的一个结点，都是一个基础的，时间复杂度为
O(1)的操作。</p>
<ul>
<li><p>在一个存在头指针和尾指针的链表当中，删除第一个结点就是一个基础的
O(1)时间复杂度操作。</p></li>
<li><p>如果需要删除尾结点或中间结点，则通常需要遍历链表，时间复杂度是
O(n)</p></li>
</ul></li>
</ol>
<h4 id="设计思路-1">设计思路</h4>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202311161556943.png" alt="单向链表结构-示意图" style="zoom: 33%;" />
</div>
<h4 id="代码实现-1">代码实现</h4>
<details>
<summary>
<font size="4" color="orange"> linked_list.h </font>
</summary>
<pre> <code class="language-c">// 头文件保护语法
#ifndef LINKED_LIST_H
#define LINKED_LIST_H
// 包含 linked_list.h 头文件也会同步包含它包含的其它头文件
// 根据这一特点, 可以选择将一些共用的头文件包含在此头文件中
#include &lt;stdbool.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;stdio.h&gt;
#define ERROR_CHECK(ret, error_flag, msg) \
 do { \
     if ((ret) == (error_flag)) { \
         printf("error: %s\n", msg); \
         exit(1); \
     } \
 } while(0)
typedef int DataType;
// 定义链表结点结构
typedef struct node {
    DataType data;      // 数据域
    struct node* next;  // 指针域
} Node;
// 定义链表结构本身
typedef struct {
    Node* head;     // 头指针
    Node* tail;     // 尾结点指针
    int size;       // 用于记录链表的长度
} LinkedList;
// 创建一个空的链表
LinkedList* create_linked_list();
// 销毁链表
void destroy_linked_list(LinkedList* list);
// 头插法
bool add_before_head(LinkedList* list, DataType new_data);
// 尾插法
bool add_behind_tail(LinkedList* list, DataType new_data);
// 根据索引插入一个新结点
bool add_by_idx(LinkedList* list, int idx, DataType new_data);
// 根据索引搜索一个结点
Node * search_by_idx(LinkedList* list, int idx);
// 根据数据搜索一个结点
Node * search_by_data(LinkedList* list, DataType data);
// 根据数据删除一个结点
bool delete_by_data(LinkedList* list, DataType data);
// 扩展: 根据索引删除一个结点
bool delete_by_idx(LinkedList* list, int idx);
// 打印单链表 格式为：1 -> 2 -> 3 ->...
void print_list(LinkedList* list);
#endif // ! LINKED_LIST_H
</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> linked_list.c </font>
</summary>
<pre> <code class="language-c">#include "linked_list.h"
LinkedList* create_linked_list()
{
    LinkedList* list = calloc(1, sizeof(LinkedList));
    ERROR_CHECK(list, NULL, "Error: malloc failed in creat_linked_list.\n");
    return list;
}
void destroy_linked_list(LinkedList* list)
{
    Node* curr = list-> head;
    while (curr) {
        Node* temp = curr-> next;
        free(curr);
        curr = temp;
    }
    free(list);
}
// 头插法
bool add_before_head(LinkedList *list, DataType new_data) {
    // 1.分配一个新结点
    Node *new_node = (Node *)malloc(sizeof(Node));
    if (new_node == NULL) {
        printf("malloc failed in add_before_head.\n");
        return false;
    }
    // 2.初始化这个新结点
    new_node-> data = new_data;
    new_node-> next = list-> head;    // 新结点指向原本的第一个结点
    // 3.更新头指针指向新结点
    list-> head = new_node;  // 新结点成为第一个结点
    // 4.如果尾结点指针是 NULL，说明链表头插前为空，那么新结点同时成为尾结点
    // 需要更新尾结点指针
    if (list-> tail == NULL) {
        list-> tail = new_node;
    }
    list-> size++;
    return true;
}
// 尾插法
bool add_behind_tail(LinkedList *list, DataType new_data) {
    // 1.分配一个新结点
    Node *new_node = malloc(sizeof(Node));
    if (new_node == NULL) {
        printf("malloc failed in add_behind_tail.\n");
        return false;
    }
    // 2.初始化新结点的指针域和数据域，指针域初始为 NULL
    new_node-> data = new_data;
    new_node-> next = NULL;
    // 3.如果尾结点指针是 NULL，说明链表尾插前为空，那么新结点同时成为第一个结点和尾结点
    if (list-> tail == NULL) {
        // 链表为空
        list-> head = new_node;
        list-> tail = new_node;
        list-> size++;
        return true;
    }
    // 4.如果尾指针不是 NULL，让以前的尾结点指向新结点，然后更新尾指针
    list-> tail-> next = new_node;
    list-> tail = new_node;
    list-> size++;
    return true;
}
bool add_by_idx(LinkedList* list, int idx, DataType new_data)
{
    // 参数校验
    if (idx <0 || idx> list-> size) {
        printf("Index is not valid.\n");
        return false;
    }
    Node* new_node = calloc(1, sizeof(Node));
    if (! new_node)
    {
        printf("Error: calloc failed in add_behind_tail.\n");
        return false;
    }
    if (idx == 0)
    {
        return add_before_head(list, new_data);
    }
    else if (idx == list-> size) {
        return add_behind_tail(list, new_data);
    }
    else
    {
        Node* prev = NULL;
        Node* curr = list-> head;
        for (size_t i = 0; i < idx; i++)
        {
            prev = curr;
            curr = curr-> next;
        }
        new_node-> data = new_data;
        new_node-> next = curr;
        prev-> next = new_node;
        list-> size++;
        return true;
    }
}
Node * search_by_idx(LinkedList* list, int idx)
{
    // 参数校验
    if (idx < 0 || idx > = list-> size) {
        printf("Index is not valid.\n");
        return NULL;
    }
    Node* curr = list-> head;
    if (! curr)
    {
        printf("The list is empty.\n");
        return NULL;
    }
    for (size_t i = 0; i < idx; i++)
    {
        curr = curr-> next;
    }
    return curr;
}
Node * search_by_data(LinkedList* list, DataType data)
{
    Node* curr = list-> head;
    if (! curr)
    {
        printf("The list is empty.\n");
        return NULL;
    }
    while (curr) {
        if (curr-> data == data)
        {
            return curr;
        }
        curr = curr-> next;
    }
    return NULL;
}
bool delete_by_data(LinkedList* list, DataType data)
{
    Node* prev = NULL;
    Node* curr = list-> head;
    ERROR_CHECK(curr, NULL, "The list is empty.\n");
    while (curr) {
        if (curr-> data == data)
        {
            if (curr == list-> head)
            {
                if (curr-> next) { // 删除的第一个节点有后继节点
                    list-> head = curr-> next;
                    free(curr);
                }
                else { // 删除的第一个节点无后继节点
                    list-> head = NULL;
                    list-> tail = NULL;
                    free(curr);
                }
            }
            else
            {
                prev-> next = curr-> next;
                if (curr == list-> tail) {
                    list-> tail = prev;
                }
                free(curr);
            }
            list-> size--;
            return true;
        }
        prev = curr;
        curr = curr-> next;
    }
    return false;
}
bool delete_by_idx(LinkedList* list, int idx)
{
    // 参数校验
    if (idx < 0 || idx > = list-> size) {
        printf("Index is not valid.\n");
        return false;
    }
    Node* prev = NULL;
    Node* curr = list-> head;
    ERROR_CHECK(curr, NULL, "The list is empty.\n");
    if (idx == 0)
    {
        if (curr-> next) { // 删除的第一个节点有后继节点
            list-> head = curr-> next;
        }
        else { // 删除的第一个节点无后继节点
            list-> head = NULL;
            list-> tail = NULL;
        }
    }
    else {
        for (size_t i = 0; i < idx; i++)
        {
            prev = curr;
            curr = curr-> next;
        }
        prev-> next = curr-> next;
        if (curr-> next == NULL) {
            list-> tail = prev;
        }       
    }
    free(curr);
    list-> size--;
    return true;
}
// 打印单链表 格式为：1 -> 2 -> 3 ->...
void print_list(LinkedList *list) {
    Node *curr = list-> head;
    // 遍历此单链表
    while (curr != NULL) {
        printf("%d", curr-> data);
        // 如果不是链表的最后一个结点，就打印箭头
        if (curr-> next != NULL) {
            printf(" -> ");
        }
        curr = curr-> next;
    }
    printf("\n");
}
</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> test.c </font>
</summary>
<pre> <code class="language-c">#define _CRT_SECURE_NO_WARNINGS
#include &lt;stdio.h&gt;
#include "linked_list.h"
#define ARR_SIZE(arr) (sizeof(arr)/sizeof(arr [0]))
/*
*/
int main(void) {
    LinkedList* list = create_linked_list();
    add_before_head(list, 4);
    add_before_head(list, 3);
    add_before_head(list, 2);
    add_before_head(list, 1);
    add_behind_tail(list, 5);
    // add_by_idx(list, 0, 0);
    // add_by_idx(list, 3, 111);
    print_list(list);
    //Node* node =  search_by_idx(list, 5);
    //Node* node = search_by_data(list, 7);
    // delete_by_data(list, 4);
    delete_by_idx(list, 4);
    print_list(list);
    destroy_linked_list(list);
    return 0;
}</code>
</pre>
</details>
<hr />
<h2 id="栈">栈</h2>
<h3 id="概述">概述</h3>
<p>相比较于数组和链表，栈是一种操作受限的线性结构。</p>
<p>这种操作受限体现在：</p>
<ol type="1">
<li>栈只能在 <strong>同一端</strong> 添加、删除以及访问元素（栈顶）</li>
<li>另一端无法执行任何操作（栈底），栈底的元素既不能直接增删，也不能直接访问。</li>
</ol>
<p><strong>可以把栈想象成一个杯子，杯底相当于栈底，无法直接进行任何操作。杯口相当于栈顶，是唯一可以进行添加、删除和访问操作的地方。</strong></p>
<p>在栈中，最先添加到栈中的元素总是最后被删除的，遵循
<strong>后进先出（LIFO, Last In First Out）</strong> 的原则。</p>
<p>如下图所示：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202311210848469.png" alt="栈-示意图" style="zoom: 33%;" />
</div>
<p>栈这种数据结构的基本操作主要包括以下几种：</p>
<ol type="1">
<li>入栈/压栈(push)：在栈顶添加一个元素，成为新的栈顶元素，其余元素则被压入栈底。时间复杂度
O(1)</li>
<li>出栈/弹栈(pop)：删除栈顶元素，下一个元素成为新的栈顶元素。时间复杂度
O(1)</li>
<li>访问栈顶元素(peek)：返回栈顶元素的值。时间复杂度 O(1)</li>
<li>判空(is_empty)：检查栈中是否有任何元素。时间复杂度
O(1)，因为只需要检查一下栈顶元素即可。</li>
</ol>
<p><strong>说白了，栈仍然还是一个线性数据结构(数组或链表都可以)，只不过在实现时，只需要提供栈顶的增删访问操作罢了。</strong></p>
<h3 id="栈的优点">栈的优点</h3>
<p>栈是一种操作受限的线性结构，既然如此，那么为什么非要使用栈呢？直接使用链表或数组不好吗？</p>
<p>使用栈主要有以下好处：</p>
<ol type="1">
<li>高效率。由于栈的操作仅限于顶部，栈的所有基本操作（push、pop、peek）通常都具有
O(1) 的时间复杂度，这使得栈在性能上非常高效。</li>
<li>简化代码、增强代码可读性。由于操作受限，编程时的考虑因素更少，更容易实现。也有助于写出更清晰、更易于维护的代码。</li>
<li>非常适合用于解决特定问题。在实际的某些应用场景中，如函数调用管理、括号匹配问题、表达式求值、浏览器的前进后退功能等，自然而然地符合
“先进后出” 的特点，天然就适合使用栈来解决。</li>
</ol>
<p><strong>在数据结构的选择上，关键在于根据具体的应用需求和场景来决定最合适的结构。</strong></p>
<h3 id="栈的实现方式">栈的实现方式</h3>
<p>栈的实现有两种方式：</p>
<ol type="1">
<li>基于链表实现</li>
<li>基于数组实现</li>
</ol>
<p>这两种实现方式各有优缺点，总得来说：</p>
<ol type="1">
<li>如果以一个固定长度的数组来实现栈，实现方式非常简洁，依赖于数组随机访问效率特别高。也不需要额外的空间来存储指针。但缺点是栈大小固定，无法扩容。</li>
<li>如果用一个动态数组来实现栈，栈具有更灵活的容量，同样随机访问效率高且不需要额外空间存储指针。但缺点是，重分配内存可能是一个较大的性能开销，拖慢整体栈的效率。</li>
<li>如果以链表来实现，灵活性比数组更强，且扩容不涉及内存重分配，栈整体性能稳定。但缺点是空间占用更多，需要存储额外的指针。当然，链表实现肯定会更复杂一些，这也算个小毛病。</li>
</ol>
<p>总的来说：</p>
<ol type="1">
<li>在已知栈大小限制或不需要频繁调整栈大小时，优先考虑使用固定长度的数组来实现栈，这会提供更好的性能以及更简单的实现以及使用方式。</li>
<li>在栈大小未知或需要频繁调整栈大小时，可以考虑使用动态数组或链表来实现栈，它们的区别是：
<ol type="1">
<li>动态数组实现的缺点是如果需要频繁调整大小，那么性能会非常差。所以如果不需要频繁的进行扩容操作，且对性能需求高，则优先选择动态数组实现。而且现代很多高级编程语言（比如
C++、Java 等）都已提供了现成的动态数组实现。</li>
<li>如果栈的大小完全不可预测，使用动态数组实现会导致频繁调整大小的操作，那么可以更多的考虑使用链表实现。</li>
</ol></li>
</ol>
<h3 id="设计思路-2">设计思路</h3>
<h4 id="链式栈模型">链式栈模型</h4>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20240920115219055.png" alt="链式栈模型" style="zoom: 67%;" />
</div>
<h4 id="动态数组栈模型">动态数组栈模型</h4>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20240921112648213.png" alt="动态数组栈模型" style="zoom: 67%;" />
</div>
<h3 id="代码实现-2">代码实现</h3>
<h4 id="链式栈模型-1">链式栈模型</h4>
<details>
<summary>
<font size="4" color="orange"> linked_stack.h </font>
</summary>
<pre> <code class="language-c">#ifndef LINKED_STACK_H
#define LINKED_STACK_H
#include &lt;stdbool.h&gt;
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
typedef int ElementType;
// 栈的一个结点栈帧, 类型定义
typedef struct node_s {
    ElementType data;
    struct node_s* next;
}StackFrame;
typedef struct {
    StackFrame* top;    // 栈顶指针
}LinkedStack;
// 基本操作
// 创建链式栈
LinkedStack* stack_create();
// 销毁链式栈
void stack_destroy(LinkedStack* stack);
// 判空
bool is_empty(LinkedStack* stack);
// 入栈
void stack_push(LinkedStack* stack, ElementType data);
// 出栈并返回栈顶元素
ElementType stack_pop(LinkedStack* stack);
// 访问栈顶元素
ElementType stack_peek(LinkedStack* stack);
#endif // ! LINKED_STACK_H
</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> linked_stack.c </font>
</summary>
<pre> <code class="language-c">#include "linked_stack.h"
// 新建一个空栈
LinkedStack *stack_create() {
    // calloc 可以不用手动初始化空指针
    return calloc(1, sizeof(LinkedStack));
}
// 对于链式栈而言，销毁栈就是销毁链表
void stack_destroy(LinkedStack *stack) {
    // 相当于遍历链表(出栈)然后在遍历中逐个 free 结点
    StackFrame *curr = stack-> top;
    while (curr != NULL) {
        StackFrame *tmp = curr-> next;   // 保存后继结点
        free(curr);
        curr = tmp;
    }
    // 最后 free 栈结构体
    free(stack);
}
bool is_empty(LinkedStack *stack) {
    return stack-> top == NULL;
}
// 相当于链表头插法插入结点，栈顶指针相当于链表的头指针
void stack_push(LinkedStack *stack, ElementType data) {
    // 1.新建一个栈帧结点
    StackFrame *new_frame = malloc(sizeof(StackFrame));
    if (new_frame == NULL) {
        printf("malloc failed in stack_push.\n");
        exit(1);
    }
    // 2.初始化新栈帧
    new_frame-> data = data;
    new_frame-> next = stack-> top;
    // 3.更新栈顶指针
    stack-> top = new_frame;
}
// 相当于链表在头部删除第一个结点, 栈顶指针相当于链表的头指针
ElementType stack_pop(LinkedStack *stack) {
    // 1.栈判空处理
    if (is_empty(stack)) {
        printf("error: stack is empty.\n");
        exit(1);
    }
    // 2.出栈返回栈顶元素, 并 free 结点, 更新栈顶指针
    StackFrame *curr = stack-> top;
    ElementType data = curr-> data;
    stack-> top = curr-> next;
    free(curr);
    return data;
}
ElementType stack_peek(LinkedStack *stack) {
    if (is_empty(stack)) {
        printf("error: stack is empty.\n");
        exit(1);
    }
    return stack-> top-> data;
}</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> test.c </font>
</summary>
<pre> <code class="language-c">#define _CRT_SECURE_NO_WARNINGS
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;string.h&gt;
#include "linked_stack.h"
int main(void) {
    // 创建一个空栈
    LinkedStack *stack = stack_create();
    if (stack == NULL) {
        printf("Failed to create stack.\n");
        return -1;
    }
    // 入栈操作
    stack_push(stack, 1);
    stack_push(stack, 2);
    stack_push(stack, 3);
    // 查看栈顶元素
    printf("当前栈顶元素是: %d\n", stack_peek(stack)); // 应该输出 3
    // 出栈操作，并打印出栈的元素
    printf("依次出栈以下元素:\n");
    while (! is_empty(stack)) {
        printf("%d\n", stack_pop(stack)); // 应该依次输出 3, 2, 1
    }
    // 再次尝试查看栈顶元素，预期会触发错误提示
    printf("栈为空时, 尝试访问栈顶元素:\n");
    stack_peek(stack);  // 应该输出错误信息并退出
    // 销毁栈
    stack_destroy(stack);   return 0;
}</code>
</pre>
</details>
<h4 id="动态数组栈模型-1">动态数组栈模型</h4>
<details>
<summary>
<font size="4" color="orange"> dynamic_stack.h </font>
</summary>
<pre> <code class="language-c">#ifndef DYNAMIC_STACK_H
#define DYNAMIC_STACK_H
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;stdbool.h&gt;
typedef int ElementType;
typedef struct {
    ElementType* elements;   // 指向动态数组首元素的指针
    int size;   // 元素的个数
    int capacity; // 数组的容量，也就是栈的容量
} DynamicStack;
// 创建动态数组栈
DynamicStack* stack_create();
// 销毁动态数组栈
void stack_destroy(DynamicStack* s);
// 入栈
void stack_push(DynamicStack* s, ElementType val);
// 出栈并返回栈顶元素
ElementType stack_pop(DynamicStack* s);
// 访问栈顶元素
ElementType  stack_peek(DynamicStack* s);
// 判空
bool is_empty(DynamicStack* s);
#endif // ! DYNAMIC_ARR_STACK_H </code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> dynamic_stack.c </font>
</summary>
<pre> <code class="language-c">#include "dynamic_stack.h"
#define DEFAULT_CAPACITY 8  // 动态栈的默认容量
#define THRESHOLD 1024  // 扩容阈值
// 实现扩容机制
static void grow_capacity(DynamicStack* s) {
    // 扩容策略: 超过阈值则 1.5 倍的扩容，否则 2 倍的扩容
    int new_capacity = (s-> capacity > THRESHOLD) ?
        (s-> capacity + (s-> capacity >> 1)) :
        (s-> capacity << 1);
    // 使用 realloc 函数实现扩容，重新分配内存
    ElementType * new_arr = realloc(s-> elements, new_capacity * sizeof(ElementType));
    if (new_arr == NULL) {
        printf("Error: realloc failed in grow_capacity\n");
        exit(1);
    }
    // 更新动态数组栈结构体的信息
    s-> elements = new_arr;
    s-> capacity = new_capacity;
}
// 创建动态数组栈
DynamicStack* stack_create() {
    DynamicStack* stack = calloc(1, sizeof(DynamicStack));
    if (stack == NULL)
    {
        printf("Error: malloc failed in stack_create.\n");
        return NULL;
    }
    stack-> size = 0;
    stack-> capacity = DEFAULT_CAPACITY;
    stack-> elements = malloc(DEFAULT_CAPACITY * sizeof(ElementType));
    if (stack-> elements == NULL)
    {
        free(stack);
        printf("Error: malloc failed in stack_create.\n");
        return NULL;
    }
    return stack;
}
// 销毁动态数组栈
void stack_destroy(DynamicStack* s) {
    // 先释放动态数组
    free(s-> elements);
    // 再释放栈结构体
    free(s);
}
// 入栈
void stack_push(DynamicStack* s, ElementType val) {
    if (s-> size >= s-> capacity)
    {
        grow_capacity(s);
    }
    s-> elements [s-> size] = val;
    s-> size++;
}
// 出栈并返回栈顶元素
ElementType stack_pop(DynamicStack* s) {
    if (is_empty(s)) {
        printf("Error: stack is empty\n");
        exit(1);
    }
    s-> size--;
    return s-> elements [s-> size];
}
// 访问栈顶元素
ElementType  stack_peek(DynamicStack* s) {
    if (is_empty(s)) {
        printf("Error: stack is empty\n");
        exit(1);
    }
    return s-> elements [s-> size - 1];
}
// 判空
bool is_empty(DynamicStack* s) {
    return s-> size == 0;
}</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> test.c </font>
</summary>
<pre> <code class="language-c">#define _CRT_SECURE_NO_WARNINGS
#include &lt;stdio.h&gt;
#include "dynamic_stack.h"
int main(void) {
    DynamicStack* s = stack_create();
    // 入栈 1000 个元素
    for (int i = 0; i < 1000; i++) {
        stack_push(s, i);
    }
    // 出栈直到栈为空
    while (! is_empty(s)) {
        ElementType ele = stack_peek(s);
        printf("%d ", ele); // 预期会将 999-0 倒着输出打印
        stack_pop(s);
    }
    printf("\n");
    // 再次尝试出栈, 此时会打印错误提示信息
    stack_pop(s);
    // 销毁栈
    stack_destroy(s);
    return 0;
}</code>
</pre>
</details>
<h3 id="栈的应用场景">栈的应用场景</h3>
<p>栈特别适用那些存在 “后进先出”
逻辑的场景，在实际的开发，栈很常用。栈至少可以应用于以下领域：</p>
<ol type="1">
<li>函数调用机制</li>
<li>括号匹配问题</li>
<li>表达式求值问题</li>
<li>浏览器历史记录前进后退功能</li>
<li>深度优先遍历算法（一般直接用函数调用者递归实现）</li>
<li>…</li>
</ol>
<hr />
<h2 id="队列">队列</h2>
<h3 id="概述-1">概述</h3>
<p>队列是另一种操作受限的线性结构。但它的受限和栈是不同的：</p>
<ol type="1">
<li>队列只能在一端添加元素（队尾）</li>
<li>在另一端访问、删除元素（队头）</li>
</ol>
<p>队列就不用想象了，它就是我们生活中排队的场景，你只能在队尾插入一个元素，在队头删除一个元素。</p>
<p>在队列中，最先添加到队尾的元素总是最先在队头删除，遵循
<strong>先进先出（FIFO，First In First Out）</strong> 的原则。</p>
<p>如下图所示：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202311211052492.png" alt="队列-概念模型" style="zoom: 80%;" />
</div>
<p>队列这种数据结构的基本操作主要包括以下几种：</p>
<ol type="1">
<li>入队：在队尾添加一个元素，新元素成为新的队尾元素。</li>
<li>出队：在队头删除一个元素，第二个元素称为新的队头元素。</li>
<li>访问队头元素</li>
<li>判空</li>
<li>判满</li>
</ol>
<h3 id="设计思路-3">设计思路</h3>
<h4 id="数组队列模型">数组队列模型</h4>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202311231053386.png" alt="数组队列-方案三" style="zoom: 33%;" />
</div>
<p>这种方案，最核心的问题就是：如何移动两个索引，使得它们能够在数组内部循环移动呢？</p>
<p>使用取余运算符 “%” 即可：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">rear = (rear + <span class="number">1</span>) % N</span><br><span class="line">front = (front + <span class="number">1</span>) % N</span><br></pre></td></tr></table></figure>
<p>解释： 对于数组的下标运算，几乎都是要涉及对数组长度 N
的取余运算，现在希望往后移动索引，而且希望从最后一个索引向后移动是第一个索引，所以就这样计算。</p>
<p>这样设计就实现了对数组前面空间的利用，避免了空间浪费，同时出入队的效率也很高。下面思考一个问题：</p>
<p>这种设计方案，如何进行队列判空和判满呢？</p>
<p>实际上如果就单纯按照上述方案设计循环队列，那么它们的判空和判满条件是一样的：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">rear == front</span><br></pre></td></tr></table></figure>
<p>所以对于这两个操作我们还需要进行一点小优化，有两个选择：</p>
<p>（1）牺牲一个存储单元不存储元素，<strong>只要 rear 指向 front
的前一个位置就表示队列满了。</strong> 如下图所示：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202311231058261.png" alt="" style="zoom: 50%;" />
</div>
<p>在这种情况下：</p>
<ul>
<li><strong>队列满了的条件是：(rear + 1) % N == front</strong></li>
<li>队列为空的条件是：rear == front</li>
</ul>
<p>这种办法是教科书中讲数据结构经常使用的方案，在实际开发中可以用一个更简单粗暴，更易于实现和使用的方式——<strong>计数器</strong>。</p>
<p>（2）使用一个额外的计数器变量来统计数组中元素的个数，这样判空和判满都十分简洁了。具体而言可以参考下图的实现：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20240415202036317.png" alt="数组队列-方案三改进图2" style="zoom: 50%;" />
</div>
<p>基于上述图中的模型，实现一个
<strong>固定长度的数组队列</strong>，需要注意的是：</p>
<ul>
<li><p><strong>front
用于记录队头元素，出队时就将该元素出队。</strong></p></li>
<li><p><strong>rear
用于指示下一个元素入队的索引，也就是说入队操作时直接将元素插入 rear
索引位置就可以了。</strong></p></li>
<li><p><strong>规定 front(包含)和
rear(不包含)索引之间的元素就是队列元素，出队操作时，直接向后移动索引
front 就可以了，不需要任何赋值操作。</strong></p></li>
<li><p><strong>front 向后移动的方式是：(front + 1) % N</strong></p></li>
<li><p><strong>rear 向后移动的方式是：(rear + 1) % N</strong></p></li>
</ul>
<h4 id="链表队列模型">链表队列模型</h4>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20240921154854107.png" alt="链表队列模型" style="zoom: 67%;" />
</div>
<h3 id="代码实现-3">代码实现</h3>
<h4 id="动态数组队列模型">动态数组队列模型</h4>
<details>
<summary>
<font size="4" color="orange"> dynamic_queue.h </font>
</summary>
<pre> <code class="language-c">#ifndef DYNAMIC_QUEUE_H
#define DYNAMIC_QUEUE_H
#include &lt;stdbool.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;stdio.h&gt;
typedef int ElementType;    // 该队列当前存储 int 元素
#define DEFAULT_CAPACITY 10 // 数组队列的初始长度是 10
#define THRESHOLD 1000  // 超过阈值每次扩容 1.5 倍扩，否则 2 倍的扩
// 定义队列结构体
typedef struct {
    ElementType* data;   // 动态数组存储队列元素
    int front;           // 标记队头元素的索引
    int rear;            // 标记队尾元素下一个位置的索引
    int size;            // 当前队列中元素数量
    int capacity;        // 队列容量
} DynamicQueue;
// 队列基本操作函数声明
// 创建动态数组队列
DynamicQueue* create_queue();
// 销毁动态数组队列
void destroy_queue(DynamicQueue* q);
// 判空
bool is_empty(DynamicQueue* q);
// 判满
bool is_full(DynamicQueue* q);
// 入队列
bool enqueue(DynamicQueue* q, ElementType data);
// 出队列并且返回队头元素
ElementType dequeue(DynamicQueue* q);
#endif // ! DYNAMIC_QUEUE_H </code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> dynamic_queue.c </font>
</summary>
<pre> <code class="language-c">#include "dynamic_queue.h"
// 创建并初始化队列
DynamicQueue *create_queue() {
    DynamicQueue *q = calloc(1, sizeof(DynamicQueue));
    if (q == NULL) {
        printf("error: calloc failed in create_queue.\n");
        return NULL;
    }
    // front、rear、size 自动初始化 0 值，无需再手动初始化了
    q-> data = calloc(DEFAULT_CAPACITY, sizeof(ElementType));    // 使用 calloc 避免随机值
    if (q-> data == NULL) {
        printf("error: calloc failed in create_queue.\n");
        free(q);
        return NULL;
    }
    q-> capacity = DEFAULT_CAPACITY;
    return q;
}
// 销毁队列
void destroy_queue(DynamicQueue *q) {
    free(q-> data);
    free(q);
}
// 检查队列是否为空
bool is_empty(DynamicQueue *q) {
    return q-> size == 0;
}
// 检查队列是否已满
bool is_full(DynamicQueue *q) {
    return q-> size == q-> capacity;
}
/*
    这里采用一种简单粗暴的扩容手段:
    直接分配新容量的内存块
    然后遍历旧内存块中的队列元素, 将这些元素全部从头开始复制到新内存块中
    这样的操作在完成后, 需要更新 front 索引和 rear 索引
*/
static bool resize_queue(DynamicQueue *q) {
    int old_capacity = q-> capacity;
    int new_capacity = (old_capacity < THRESHOLD) ?
        (old_capacity << 1) :
    (old_capacity + (old_capacity >> 1));
    // 重新分配一个新的, 更长的动态数组
    ElementType *new_data = malloc(new_capacity * sizeof(ElementType));
    if (new_data == NULL) {
        printf("error: realloc failed in resize_queue.\n");
        return false;
    }
    int curr = q-> front;    // curr 索引用于遍历整个队列中的元素
    int index = 0;
    while (index < q-> size) {
        new_data [index] = q-> data [curr];
        curr = (curr + 1) % q-> capacity;
        index++;
    } // while 循环结束时, new_data 就从头开始包含了队列的所有元素
    free(q-> data);
    q-> data = new_data;
    q-> front = 0;
    q-> rear = q-> size;
    q-> capacity = new_capacity;
    return true;
}
// 入队操作
bool enqueue(DynamicQueue *q, ElementType data) {
    if (is_full(q)) {
        if (! resize_queue(q)) {
            printf("error: 扩容失败.\n");
            return false; // 队列扩容失败, 入队也同步失败
        }
    }
    q-> data [q-> rear] = data;
    q-> rear = (q-> rear + 1) % q-> capacity;  // 循环队列
    q-> size++;
    return true;
}
// 出队操作
ElementType dequeue(DynamicQueue *q) {
    if (is_empty(q)) {
        printf("error: 队列为空，无法出队。\n");
        exit(1);
    }
    ElementType item = q-> data [q-> front];
    q-> front = (q-> front + 1) % q-> capacity; // 循环队列
    q-> size--;
    return item;
}</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> test.c </font>
</summary>
<pre> <code class="language-c">#define _CRT_SECURE_NO_WARNINGS
#include &lt;stdio.h&gt;
#include "dynamic_queue.h"
int main(void) {
    // 创建队列
    DynamicQueue* queue = create_queue();
    if (queue == NULL) {
        printf("创建队列失败。\n");
        return 1;
    }
    // 入队测试
    for (int i = 0; i < 15; i++) {
        if (enqueue(queue, i)) {
            printf("已入队: %d\n", i);
        }
        else {
            printf("入队失败: %d\n", i);
        }
    }
    // 打印队列容量和大小
    printf("队列容量: %d, 队列大小: %d\n", queue-> capacity, queue-> size);
    // 出队测试
    printf("正在出队...\n");
    while (! is_empty(queue)) {
        printf("已出队: %d\n", dequeue(queue));
    }
    // 销毁队列
    destroy_queue(queue);
    return 0;
}</code>
</pre>
</details>
<h4 id="链表队列模型-1">链表队列模型</h4>
<details>
<summary>
<font size="4" color="orange"> list_queue.h </font>
</summary>
<pre> <code class="language-c">#ifndef LIST_QUEUE_H
#define LIST_QUEUE_H
#include &lt;stdbool.h&gt;
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
// 定义队列中的元素类型
typedef int ElementType;
// 队列节点的结构
typedef struct node_s {
    ElementType data;
    struct node_s* next;
} QueueNode;
// 队列的结构
typedef struct {
    QueueNode* front;  // 队头结点指针
    QueueNode* rear;   // 队尾结点指针
} LinkedListQueue;
// 函数声明
// 创建链式队列
LinkedListQueue* create_queue();
// 销毁链式队列
void destroy_queue(LinkedListQueue* q);
// 入队列
bool enqueue(LinkedListQueue* q, ElementType element);
// 出队列并返回队头元素
ElementType dequeue(LinkedListQueue* q);
// 访问队头元素
ElementType peek_queue(LinkedListQueue* q);
// 判空
bool is_empty(LinkedListQueue* q);
#endif // ! LIST_QUEUE_H </code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> list_queue.c </font>
</summary>
<pre> <code class="language-c">#include "list_queue.h"
// 创建链式队列
LinkedListQueue* create_queue() {
    return calloc(1, sizeof(LinkedListQueue));
}
// 销毁链式队列
void destroy_queue(LinkedListQueue* q)
{
    QueueNode* curr = q-> front;
    while (curr != NULL) {
        QueueNode* temp = curr-> next;
        free(curr);
        curr = temp;
    }
    free(q);
}
// 入队列
bool enqueue(LinkedListQueue* q, ElementType element)
{
    QueueNode* new_node = calloc(1, sizeof(QueueNode));
    if (new_node == NULL)
    {
        printf("Error: malloc failed in enqueue.\n");
        return false;
    }
    new_node-> data = element;
    new_node-> next = NULL;
    if (q-> front == NULL)
    {
        q-> front = new_node;
        q-> rear = new_node;
    }
    else
    {
        q-> rear-> next = new_node;
        q-> rear = new_node;
    }
    return true;
}
// 出队列并返回队头元素
ElementType dequeue(LinkedListQueue* q)
{
    if (is_empty(q))
    {
        printf("The queue is empty.\n");
        exit(1);
    }
    ElementType item = q-> front-> data;
    QueueNode* temp = q-> front;
    q-> front = temp-> next;
    if (q-> front == NULL)
    {
        q-> rear == NULL;
    }
    free(temp);
    return item;
}
// 访问队头元素
ElementType peek_queue(LinkedListQueue* q)
{
    if (is_empty(q))
    {
        printf("The queue is empty.\n");
        exit(1);
    }
    return q-> front-> data;
}
// 判空
bool is_empty(LinkedListQueue* q)
{
    return q-> front == NULL;
}</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> test.c </font>
</summary>
<pre> <code class="language-c">#define _CRT_SECURE_NO_WARNINGS
#include "list_queue.h"
int main(void) {
    LinkedListQueue* q = create_queue();
    // 逐一入队列
    enqueue(q, 1);
    enqueue(q, 2);
    enqueue(q, 3);
    enqueue(q, 4);
    enqueue(q, 5);
    enqueue(q, 6);
    // 先进先出，出队列全部元素直到队列为空
    while (! is_empty(q)) {
        int val = peek_queue(q);
        printf("%d ", val); // 预期出队列顺序: 1 2 3 4 5 6
        dequeue(q);
    }
    destroy_queue(q); // 销毁队列
    return 0;
}</code>
</pre>
</details>
<h3 id="队列的应用场景">队列的应用场景</h3>
<p>队列是一种非常实用的数据结构，由于其先进先出的特性，队列特别适用于那些需要按顺序处理元素的场景。比如：</p>
<ol type="1">
<li>操作系统的任务调度，进程/线程按照到达顺序来排队等待 CPU
执行权。</li>
<li>各种数据处理系统中的缓冲区/缓存机制。比如 stdin/stdout
缓冲区，先输入缓冲区的数据，总是先从缓冲区输出。</li>
<li>打印机的任务管理，当多个打印任务同时到达时，它们在队列中排队，按照提交的顺序进行打印。</li>
<li>后端应用系统中的消息队列。</li>
<li>广度优先搜索(比如二叉搜索树的层次遍历)</li>
<li>…</li>
</ol>
<hr />
<h2 id="哈希表">哈希表</h2>
<h3 id="哈希相关概念">哈希相关概念</h3>
<p>哈希（Hash）是一种将 <strong>任意长度大小</strong> 的输入转换处理成
<strong>固定长度大小</strong> 输出的过程：</p>
<ol type="1">
<li>输入可以是一个任意数字或字符串、也可以是一个任意文件等等。<strong>（一般来说同一个哈希过程，输入的类型应该是一致的，但长度大小不限制）</strong></li>
<li>普遍来说，<strong>哈希过程的输出会是一个固定长度的整数</strong>，这个整数就是常说的
<strong>哈希值</strong>。</li>
<li>实现哈希的过程中，代表转换处理规则的就是
<strong>哈希函数</strong>。</li>
</ol>
<p>哈希函数（Hash Function）是实现哈希过程的关键，负责将输入的任意数据
<strong>映射</strong> 到一个哈希值（一般是一个固定长度的整数）上。</p>
<p>哈希函数具有以下特点：</p>
<ol type="1">
<li><strong>同一个输入多次调用哈希函数，必须映射到同一输出上。映射不允许出现一对多。</strong></li>
<li>不同输入多次调用哈希函数，不要求映射到不同输出上。<strong>映射不要求完全一一对应，允许出现多对一，这就是哈希冲突。</strong></li>
</ol>
<p><strong>有关哈希的概念，总结如下：</strong></p>
<p>哈希(过程)：</p>
<ol type="1">
<li>哈希是一种可以接受各种类型、大小的输入，输出一个固定长度整数的过程。</li>
<li><strong>可以将哈希理解成一种特殊的映射，哈希映射，将一个理论无限的集合
A 映射到有限整数集合 B 上。</strong></li>
</ol>
<p><strong>哈希函数</strong>：哈希函数是哈希过程的核心，它决定了哈希映射过程的规则。</p>
<p><strong>哈希冲突</strong>：哈希是一种化无限为有限的映射，允许出现多对一，但绝不允许出现一对多。若映射中出现多对一，就是哈希冲突。哈希冲突可以减少，但绝不可能没有。</p>
<h3 id="哈希表相关概念">哈希表相关概念</h3>
<p>哈希表是一种基于 <strong>“键值对”</strong>
的存储方式的数据结构，能够提供快速的数据查找、插入和删除操作。</p>
<p>哈希表是能够存储键值对元素的数据结构，具体到存储的机制是：</p>
<ol type="1">
<li>先利用哈希函数计算键值对元素中 key <strong>(哈希表的 key
是唯一的)</strong> 对应的哈希值</li>
<li>然后根据哈希值的不同决定这个元素在哈希表中的存储位置。</li>
</ol>
<p>由于哈希冲突的存在，所以哈希表在具体存储元素时，不得不把多个元素存储在同一个位置上，这样就出现了
<strong>哈希桶</strong> 的概念：</p>
<ol type="1">
<li><strong>哈希桶是哈希表存储数据的基本单位，一个哈希表由多个哈希桶组成。</strong></li>
<li><strong>当一个新的键值对需要加入哈希表时，会使用哈希函数计算该键的哈希值，然后根据这个哈希值决定将键值对存储在哪个哈希桶中。</strong></li>
<li><strong>一般而言，每个哈希桶都可以存储一个或多个元素。</strong></li>
</ol>
<p>总之，一个哈希表的概念模型可以用下图描述(不涉及实现)：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202311281546792.png" alt="哈希表的概念模型图" style="zoom: 33%;" />
</div>
<p>根据这个理论模型，我们不难发现：</p>
<p>假设在完美情况下，哈希冲突不出现，任何一个哈希桶都只用存储一个元素，那么这个哈希表就是一个数组。</p>
<p>也就是说在理想、完美的情况下，哈希表就是一个数组。当然这种情况，在现实里并不存在，哈希冲突是必然存在的。</p>
<h3 id="哈希函数">哈希函数</h3>
<p>哈希函数是整个哈希过程的核心，那么应该如何设计一个哈希函数呢？什么样的哈希函数是一个优秀的哈希函数呢？</p>
<p>在编程领域，哈希函数的最常见应用就是实现哈希表。为了哈希表的性能考虑，此哈希函数应该：</p>
<ol type="1">
<li>能够快速计算。在实现哈希表时，对安全性没有非常高的要求，但如果哈希函数的效率很差势必影响哈希表的性能。</li>
<li>均匀映射，尽量减少哈希冲突。哈希函数应该将输入均匀地映射到输出当中。这减少了哈希冲突的概率，意味着不同的输入值更有可能得到不同的哈希值。</li>
</ol>
<p>但如果将哈希函数运用到其他领域，比如文件校验，密码存储等(密码学场景)，对哈希函数的要求就会更高：</p>
<ol type="1">
<li>几乎没有哈希冲突。使得哈希值能够像指纹一样，可以作为输入的唯一标识。</li>
<li>通过哈希值逆向破解输入几乎不可能。</li>
<li>对输入的任何一点修改，都最好得到一个完全不同的哈希值。</li>
</ol>
<p>在非密码学场景(追求高效计算、不追求极致减少哈希冲突)，比较常用的哈希算法有：<strong>MurmurHash</strong>，该算法以高运行效率和分布均匀性著称，在实现哈希表时选择它非常合适。</p>
<p>在密码学场景中(追求极致减少哈希冲突，追求安全性)，比较常用的算法有：MD5，SHA1，SHA-256，SHA-3
等。MD5 和 SHA1 由于存在明显的安全漏洞，已基本被淘汰。SHA-256
是目前最常见、最推荐的密码学哈希算法。</p>
<p>在后续实现哈希表时，这里给出一个 MurmurHash 哈希算法在 C
语言中的实现函数：</p>
<details>
<summary>
<font size="4" color="orange"> Show Code </font>
</summary>
<pre> <code class="language-c">/* murmur_hash2 */
static uint32_t hash(const void* key, int len, uint32_t seed) {
    const uint32_t m = 0x5bd1e995;
    const int r = 24;
    uint32_t h = seed ^ len;
    const unsigned char * data = (const unsigned char*)key;
    while (len >= 4) {
        uint32_t k = *(uint32_t*)data;
        k *= m;
        k ^= k >> r;
        k *= m;
        h *= m;
        h ^= k;
        data += 4;
        len -= 4;
    }
    switch (len) {
        case 3: h ^= data [2] << 16;
        case 2: h ^= data [1] << 8;
        case 1: h ^= data [0];
            h *= m;
    };
    h ^= h >> 13;
    h *= m;
    h ^= h >> 15;
    return h;
}</code>
</pre>
</details>
<p>MurmurHash
是一种非加密哈希函数，适用于一般的哈希检索操作。它以高运行速度和分布均匀性而闻名。其中
uint32_t 表示无符号 32 位整型，要想使用它需要包含 <strong>头文件
&lt;stdint.h&gt;</strong>。</p>
<p>该函数调用需要三个参数：</p>
<ol type="1">
<li><p><strong>void* key，也就是需要计算哈希值的 key 元素。</strong>
此形参的类型是
void*，这意味着它可以传入任何类型的数据，提高了函数的通用性。</p>
<ul>
<li><p>如果 key 是基本数据类型，那就传入指向基本数据类型的指针</p></li>
<li><p>如果 key
本身就是一个指针类型，比如字符串，那么就直接传入这个指针就可以了。</p></li>
</ul></li>
<li><p>int len，表示哈希函数要处理的 key 的大小长度。key
若是字符串可以使用函数 strlen 计算，若是其它类型可以使用 sizeof
计算。</p></li>
<li><p>uint32_t seed，种子值。</p>
<ul>
<li><p>此哈希函数会根据 key 和 seed 共同生成一个哈希值</p></li>
<li><p>不同的种子值会导致相同的数据产生不同的哈希值。<strong>需要注意的是，在程序一次运行中，同一个哈希表应该具有相同的种子值！</strong></p></li>
<li><p>最常见的设置种子值的方式就是用时间戳，也就是
<code>time(NULL);</code> 函数调用。</p></li>
</ul></li>
</ol>
<h3 id="哈希表的核心操作">哈希表的核心操作</h3>
<p>对于一个哈希表而言，最核心的操作是三个：</p>
<ol type="1">
<li><p>插入一个新的键值对。<strong>(哈希表中的 key
是唯一的)</strong></p>
<ul>
<li><p>如果 key 已存在重复，更新对应 value</p></li>
<li><p>如果 key 不存在，将新的键值对添加到哈希表中。</p></li>
</ul></li>
<li><p>根据 key 查找目标 value。</p></li>
<li><p>根据 key 删除目标键值对元素。</p></li>
</ol>
<p>一般来说，不管采用何种方式实现哈希表，都要尽量保证这三个基本操作的时间复杂度接近
O(1)</p>
<p>除此之外，遍历哈希表也是一个常见操作。</p>
<h3 id="如何实现一个哈希表">如何实现一个哈希表？</h3>
<p>在实现一个哈希表的过程中，最需要关注的是两个问题：</p>
<ol type="1">
<li>选择哪一个哈希函数？这一点我们在上面已经给出了答案。</li>
<li>如何设计哈希桶呢？由于哈希冲突一定存在，哈希桶的设计要保证能够处理这种冲突导致的多个元素要存储在一个哈希桶中。所以换句话说，这个问题就是——如何处理哈希冲突呢？</li>
</ol>
<p>关于这个话题，这里给出两种常见办法。</p>
<h4 id="开放寻址法">开放寻址法</h4>
<p>哈希表是一种增查频繁的数据结构，这让我们想起了数组，因为数组根据索引增查效率很高，于是直接将一个数组当成哈希表。那怎么处理冲突问题呢？</p>
<p>一旦出现哈希冲突，就采取下列办法找到一个空位置：</p>
<ol type="1">
<li>线性探测法，逐一探测下一个位置是否为空位置，直到找到。</li>
<li>平方探测法，每次探测都会检查当前位置索引加上探测次数的平方（1, 4, 9,
16, …）的位置。</li>
<li>再哈希法。使用多个哈希函数共同实现哈希表，当插入一个元素时，首先使用第一个哈希函数计算其位置，若冲突，则继续调用后续函数，直到不冲突为止。</li>
<li>…</li>
</ol>
<p>这种解决哈希冲突的方式，就是常说到的
<strong>“开放寻址法”</strong>。</p>
<p>开放寻址法听起来实现简单，利用数组的随机访问，内存连续效率高，但实际上：</p>
<ol type="1">
<li><p>删除操作实现非常困难，复杂。</p>
<ul>
<li><p>删除操作如果只是简单修改数据，那么后续查找可能出现误访问，于是可能需要特殊标记以表示元素被删除。这样实现很麻烦，而且会拖累查询的效率。</p></li>
<li><p>如果通过整体挪动数组元素来表示删除，那删除效率就太差了，也不太可取。</p></li>
</ul></li>
<li><p>数据量较大时哈希冲突难以解决。</p></li>
<li><p>开放寻址法必然意味着，需要经常进行数组扩容。</p>
<ul>
<li><p>这个过程，往往需要重分配内存</p></li>
<li><p>然后将原数组中所有的元素再哈希，拷贝到新数组中。</p></li>
<li><p>这样的一套操作下来，成本过于高昂。</p></li>
</ul></li>
</ol>
<p>但也不是说这种方式完全一无是处，当你有以下场景时：</p>
<ol type="1">
<li>数据量比较少，不需要频繁扩容数组。</li>
<li>删除操作几乎不做，只做增查操作。</li>
</ol>
<p>这时倒也不妨可以使用 “开放寻址法” 来实现哈希表。</p>
<p>但对于大多一般开发场景而言，哈希表的实现基本不会采用开放寻址法。</p>
<h4 id="拉链法常用">拉链法(常用)</h4>
<p>解决哈希冲突，设计实现哈希桶，在开发中最常见的方式还是拉链法，也就是采用
“数组 + 链表” 的方式来实现哈希桶。具体而言：以一个
<strong>键值对结点指针数组</strong>
为哈希表的基本结构，此数组的每一个存储单元都指向一个键值对结点链表。</p>
<p>这种设计下哈希表的基本操作思路如下(大致)：</p>
<ol type="1">
<li><p>进行插入操作时，计算 key
的哈希值以确定存储的哈希桶位置，接下来：</p>
<ul>
<li><p>若哈希桶为空，则此结点成为哈希桶的第一个元素。</p></li>
<li><p>若哈希桶不为空，则需要遍历哈希桶，如果 “Key” 已存在，则更新
Value。若 “Key”
不存在，则再向哈希桶中添加一个元素。此时哈希桶中的多个元素，是以链表的形式挂起来的。</p></li>
</ul></li>
<li><p>进行查找操作时：同样先计算 key
的哈希值，然后在对应索引的链表中遍历查找这个键。</p></li>
<li><p>进行删除操作时：也是先计算 key
的哈希值，然后在对应链表中找到并移除这个键值对。</p></li>
</ol>
<p>拉链法实现的哈希表，实际上就是数组的高访问效率和链表的灵活性的综合应用，是最常见的哈希表实现方式。如下图所示：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202311262031612.png" alt="拉链法-实现哈希表" style="zoom: 33%;" />
</div>
<p>下面，基于 C
语言，以拉链法来实现一个固定容量的哈希表。（若以动态数组实现哈希表，就可实现扩容）</p>
<h3 id="代码实现-4">代码实现</h3>
<details>
<summary>
<font size="4" color="orange"> hash_map.h </font>
</summary>
<pre> <code class="language-c">#ifndef HASH_MAP_H
#define HASH_MAP_H
#include &lt;stdint.h&gt; // 包含它是为了使用别名 uint32_t
#include &lt;stdbool.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;stdio.h&gt;
#include &lt;string.h&gt;
#define HASHMAP_CAPACITY 10 // 哈希表中数组的长度固定是 10
// 此哈希表用于存储字符串类型的键值对
typedef char* KeyType;
typedef char* ValueType;
// 键值对结点
typedef struct node_s {
    KeyType key;
    ValueType val;
    struct node_s* next;
} KeyValueNode;
typedef struct {
    // 哈希桶
    KeyValueNode* buckets [HASHMAP_CAPACITY];    // 直接给定哈希桶的数量是 10 个
    // 哈希函数需要的种子值
    uint32_t hash_seed;
} HashMap;
// 创建一个固定容量的哈希表
HashMap* hashmap_create();
// 销毁一个哈希表
void hashmap_destroy(HashMap* map);
// 插入一个键值对
ValueType hashmap_put(HashMap* map, KeyType key, ValueType val);
// 查询一个键值对
ValueType hashmap_get(HashMap* map, KeyType key);
// 删除某个键值对
bool hashmap_remove(HashMap* map, KeyType key);
#endif // ! HASH_MAP_H </code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> hash_map.c </font>
</summary>
<pre> <code class="language-c">#include "hash_map.h"
/* murmur_hash2 */
static uint32_t hash(const void* key, int len, uint32_t seed) {
    const uint32_t m = 0x5bd1e995;
    const int r = 24;
    uint32_t h = seed ^ len;
    const unsigned char * data = (const unsigned char*)key;
    while (len >= 4) {
        uint32_t k = *(uint32_t*)data;
        k *= m;
        k ^= k >> r;
        k *= m;
        h *= m;
        h ^= k;
        data += 4;
        len -= 4;
    }
    switch (len) {
    case 3: h ^= data [2] << 16;
    case 2: h ^= data [1] << 8;
    case 1: h ^= data [0];
        h *= m;
    };
    h ^= h >> 13;
    h *= m;
    h ^= h >> 15;
    return h;
}
// 创建一个固定容量的哈希表
HashMap* hashmap_create() {
    HashMap* map = calloc(1, sizeof(HashMap));
    if (map == NULL) {
        printf("error: calloc failed in hashmap_create.\n");
        exit(-1);
    }
    map-> hash_seed = time(NULL);
    return map;
}
// 销毁一个哈希表
void hashmap_destroy(HashMap* map) {
    // 遍历数组, 然后销毁哈希桶中的链表, 最后销毁 HashMap 结构体
    for (size_t i = 0; i < HASHMAP_CAPACITY; i++) {
        KeyValueNode* curr = map-> buckets [i];
        while (curr != NULL) {
            KeyValueNode* tmp = curr-> next;
            free(curr);
            curr = tmp;
        }
    }
    free(map);
}
// 插入一个键值对
ValueType hashmap_put(HashMap* map, KeyType key, ValueType val) {
    // 1.计算哈希值确定哈希桶的位置
    int idx = hash(key, strlen(key), map-> hash_seed) % HASHMAP_CAPACITY;
    // 2.遍历哈希桶，查找 Key 是否重复
    KeyValueNode* curr = map-> buckets [idx];
    while (curr != NULL) {
        // 比较字符串
        if (strcmp(key, curr-> key) == 0) {
            // key 已存在, 更新 value, 并返回旧 value
            ValueType old_val = curr-> val;
            curr-> val = val;
            return old_val;
        }
        curr = curr-> next;
    }
    // 3.只要 Key 不重复肯定都要插入，于是无脑使用链表头插法插入结点
    KeyValueNode* new_node = malloc(sizeof(KeyValueNode));
    if (new_node == NULL) {
        printf("Error: malloc failed in hashmap_put.\n");
        exit(1);
    }
    new_node-> key = key;    // key 和 val 都是指针类型，可以直接 "=" 赋值
    new_node-> val = val;
    // 链表头插法
    new_node-> next = map-> buckets [idx]; // 新结点指向原本的第一个结点
    map-> buckets [idx] = new_node;   // 更新头指针
    // 没有更新键值对返回空指针
    return NULL;
}
// 查询一个键值对
ValueType hashmap_get(HashMap* map, KeyType key) {
    // 1.计算哈希值确定哈希桶的位置
    int idx = hash(key, strlen(key), map-> hash_seed) % HASHMAP_CAPACITY;
    // 2.遍历哈希桶，查找 Key 是否存在
    KeyValueNode* curr = map-> buckets [idx];
    while (curr != NULL) {
        // 比较字符串
        if (strcmp(key, curr-> key) == 0) {
            // 已找到目标键值对
            return curr-> val;
        }
        curr = curr-> next;
    }
    // 目标键值对不存在
    return NULL;
}
// 删除某个键值对
bool hashmap_remove(HashMap* map, KeyType key) {
    // 1.计算哈希值确定哈希桶的位置
    int idx = hash(key, strlen(key), map-> hash_seed) % HASHMAP_CAPACITY;
    // 2.初始化两个指针, 用于删除结点
    KeyValueNode* curr = map-> buckets [idx];
    KeyValueNode* prev = NULL;
    // 3.遍历链表查找目标 Key
    while (curr != NULL) {
        // 比较字符串
        if (strcmp(key, curr-> key) == 0) {
            // 已找到目标键值对
            if (prev == NULL) {
                // 删除的是第一个结点
                map-> buckets [idx] = curr-> next;
            }
            else
            {
                // 删除的不是第一个结点
                prev-> next = curr-> next;
            }
            // free 结点
            free(curr);
            return true;    // 删除成功
        }
        prev = curr;        // 更新 prev 为当前节点
        curr = curr-> next;  // 当前结点向后移动
    }
    // 没找到目标键值对, 删除失败
    return false;
}</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> test.c </font>
</summary>
<pre> <code class="language-c">#include "hash_map.h"
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;string.h&gt;
int main(void) {
    // 创建哈希表
    HashMap* map = hashmap_create();
    if (map == NULL) {
        printf("哈希表创建失败。\n");
        return 1;
    }
    // 插入键值对
    hashmap_put(map, "key1", "value1");  // 首次插入，应成功
    hashmap_put(map, "key2", "value2");  // 再插一个新键值对，应成功
    char* old_value = hashmap_put(map, "key1", "value3");  // 更新已存在的键值对，应返回旧值 "value1"
    printf("更新键值对时返回的旧值应为 value1，实际返回：%s\n", old_value);
    // 查询键值对
    char* value = hashmap_get(map, "key1");
    printf("期待得到 value3，实际得到：%s\n", value);  // 预期输出 value3
    value = hashmap_get(map, "key2");
    printf("期待得到 value2，实际得到：%s\n", value);  // 预期输出 value2
    value = hashmap_get(map, "key3");
    if (value == NULL) {
        printf("如预期，key3 未找到\n");  // 预期找不到 key3
    }
    // 删除键值对
    bool deleted = hashmap_remove(map, "key1");  // 删除 key1, 预期删除成功
    if (deleted) {
        printf("key1 成功删除。\n");
    }
    deleted = hashmap_remove(map, "key3");  // 尝试删除不存在的 key3, 预期删除失败
    if (! deleted) {
        printf("key3 未找到且未删除，符合预期。\n");
    }
    // 销毁哈希表
    hashmap_destroy(map);
    return 0;
}</code>
</pre>
</details>
<h3 id="性能分析">性能分析</h3>
<p>拉链法解决哈希冲突实现的哈希表，在选择合适的哈希算法，键值对较为平均分布的前提下，其性能取决于哈希表中链表的
<strong>平均长度 L</strong>：</p>
<ol type="1">
<li>在 <strong>最佳情况</strong>
下，即哈希函数均匀分布且冲突较少时，哈希表的插入、查询和删除操作的效率非常高，接近
O(1)。</li>
<li>在 <strong>最坏情况</strong>
下，特别是哈希冲突很多导致链表过长时，这些操作的效率会降低，接近
O(L)，因为此时的哈希表访问几乎相当于链表的访问。</li>
</ol>
<p>为了保证哈希表的性能，就需要控制链表的长度不能太长，也就是要
<strong>控制哈希表不能太满</strong>，要始终留出一定的空位，这样才不会出现过多的元素用链表存储的情况。</p>
<p>为了衡量一个哈希表满的程度，提出了 <strong>“负载因子(Load Factor
)”</strong> 的概念：</p>
<p><span class="math display">\[
\mathrm{LoadFactor} = \frac{n}{m}
\]</span></p>
<p>其中，<span class="math inline">\(n\)</span>
表示哈希表中的键值对总数，<span class="math inline">\(m\)</span>
表示哈希表底层数组的长度。</p>
<p>一般来说，负载因子在 0.7/0.75
以下时，哈希表处在健康、性能优秀的状态。但一旦超出这个阈值，就意味着哈希冲突会增多，链表平均长度变长，从而导致性能下降。</p>
<p>此时，为了维持哈希表的高效性能，通常采取的措施是“扩容”和“重新哈希”。在拉链法的哈希表中也就是
<strong>对数组进行扩容，然后将所有元素重新哈希再映射到新哈希表中</strong>。</p>
<p>既然要扩容，那就需要使用动态数组实现哈希桶的底层，涉及到内存重分配、以及全部元素重新哈希确定存储的哈希桶，显然是一个非常耗费性能的操作。因此，这个过程应该尽量避免频繁发生。</p>
<h3 id="实现动态哈希表">实现动态哈希表</h3>
<h4 id="设计思路-4">设计思路</h4>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202403011402087.png" alt="动态哈希表-概念模型图" style="zoom: 33%;" />
</div>
<h4 id="代码实现-5"><strong>代码实现</strong></h4>
<details>
<summary>
<font size="4" color="orange"> dynamic_hashmap.h </font>
</summary>
<pre> <code class="language-c">#ifndef DYNAMIC_HASHMAP_H
#define DYNAMIC_HASHMAP_H
#include &lt;stdint.h&gt;
#include &lt;stdbool.h&gt;
#include &lt;string.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;stdio.h&gt;
typedef char* KeyType;
typedef char* ValueType;
typedef struct node_s {
    KeyType key;
    ValueType val;
    struct node_s* next;
} KeyValueNode;
typedef struct {
    KeyValueNode** buckets;     // 此时哈希桶是一个动态数组，指向 char*元素的指针，所以是一个二级指针
    int size;               // 键值对的个数
    int capacity;           // 数组的长度
    uint32_t hash_seed;     // 哈希函数需要的种子值
} DynamicHashMap;
// 创建一个固定容量的哈希表
DynamicHashMap* hashmap_create();
// 销毁一个哈希表
void hashmap_destroy(DynamicHashMap* map);
// 插入一个键值对
ValueType hashmap_put(DynamicHashMap* map, KeyType key, ValueType val);
// 查询一个键值对
ValueType hashmap_get(DynamicHashMap* map, KeyType key);
// 删除某个键值对
bool hashmap_remove(DynamicHashMap* map, KeyType key);
#endif // ! DYNAMIC_HASHMAP_H </code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> dynamic_hashmap.c </font>
</summary>
<pre> <code class="language-c">#include "dynamic_hashmap.h"
#define DEFAULT_CAPACITY 8  // 哈希表的初始默认容量是 8
#define LOAD_FACTOR_THRESHOLD  0.75     // 负载因子的阈值
#define CAPACITY_THRESHOLE 1024     // 数组容量的阈值
/* murmur_hash2 */
static uint32_t hash(const void* key, int len, uint32_t seed) {
    const uint32_t m = 0x5bd1e995;
    const int r = 24;
    uint32_t h = seed ^ len;
    const unsigned char * data = (const unsigned char*)key;
    while (len >= 4) {
        uint32_t k = *(uint32_t*)data;
        k *= m;
        k ^= k >> r;
        k *= m;
        h *= m;
        h ^= k;
        data += 4;
        len -= 4;
    }
    switch (len) {
    case 3: h ^= data [2] << 16;
    case 2: h ^= data [1] << 8;
    case 1: h ^= data [0];
        h *= m;
    };
    h ^= h >> 13;
    h *= m;
    h ^= h >> 15;
    return h;
}
// 创建一个固定容量的哈希表
DynamicHashMap* hashmap_create() {
    DynamicHashMap* map = calloc(1, sizeof(DynamicHashMap));
    if (map == NULL)
    {
        printf("Error: malloc failed in hashmap_create.\n");
        exit(1);
    }
    map-> buckets = calloc(DEFAULT_CAPACITY, sizeof(KeyValueNode*));
    if (map-> buckets == NULL)
    {
        printf("Error: malloc failed in hashmap_create.\n");
        free(map);
        exit(1);
    }
    map-> capacity = DEFAULT_CAPACITY;
    map-> hash_seed = time(NULL);
    return map;
}
// 销毁一个哈希表
void hashmap_destroy(DynamicHashMap* map) {
    // 1.先遍历动态数组销毁链表的每一个结点
    for (int i = 0; i < map-> capacity; i++) {
        KeyValueNode* curr = map-> buckets [i];
        while (curr != NULL) {
            KeyValueNode* tmp = curr-> next;
            free(curr);
            curr = tmp;
        }
    }
    // 2.再销毁动态数组
    free(map-> buckets);
    // 3.最后销毁哈希表结构体
    free(map);
}
static void rehash(KeyValueNode * curr, KeyValueNode** new_table, int new_capacity, uint32_t seed) {
    int len = strlen(curr-> key);
    int idx = hash(curr-> key, len, seed) % new_capacity;
    curr-> next = new_table [idx];
    new_table [idx] = curr;
}
// 对哈希表进行扩容操作
static void grow_capacity(DynamicHashMap* map) {
    /*
    * 扩容策略：
    * 1.如果容量没有达到阈值，那就每次将长度扩大为原先的 2 倍
    * 2.如果容量达到阈值，此时哈希表已经很长了，为了避免扩容过程性能损耗过大
    *   所以扩容保守一些，每次只扩容阈值长度的容量
    *
    * 扩容的过程：
    * 1.每个键值对结点都要重新计算哈希值，重新映射到新哈希桶中(新数组)
    * 2.原先的动态数组的链表很复杂，难以进行重哈希操作，建议直接丢弃它
    * 重新创建一个新动态数组
    */
    int new_capacity = (map-> capacity <= CAPACITY_THRESHOLE)
        ? (map-> capacity << 1) : (map-> capacity + CAPACITY_THRESHOLE);
    KeyValueNode** new_buckets = calloc(new_capacity, sizeof(KeyValueNode*));
    if (new_buckets == NULL) {
        printf("Error: calloc failed in grow_capacity\n");
        exit(1);
    }
    uint32_t seed = time(NULL);
    for (size_t i = 0; i < map-> capacity; i++)
    {
        KeyValueNode* curr = map-> buckets [i];
        while (curr != NULL) {
            KeyValueNode* next = curr-> next;
            rehash(curr, new_buckets, new_capacity, seed);
            curr = next;
        }
    }
    // 将旧动态数组 free，但是注意不要 free 键值对结点，结点已经被链接到新数组中了。
    free(map-> buckets);
    // 更新 HashMap 的信息
    map-> buckets = new_buckets;
    map-> capacity = new_capacity;
    map-> hash_seed = seed;
}
// 插入一个键值对
// 1. 如果 key 不存在，则添加键值对结点
// 2. 如果 key 存在，则更新 val，将旧的 val 返回
ValueType hashmap_put(DynamicHashMap* map, KeyType key, ValueType val) {
    // 计算 key 的哈希值确定存储位置
    int idx = hash(key, strlen(key), map-> hash_seed) % (map-> capacity);
    // 遍历链表
    KeyValueNode* curr = map-> buckets [idx];
    while (curr != NULL) {
        if (strcmp(key, curr-> key) == 0) {
            // 更新 key 关联的 val, 并将旧的 value 返回
            ValueType old_val = curr-> val;
            curr-> val = val;
            return old_val;
        }
        curr = curr-> next;
    } // while 循环结束时, curr 是一个 NULL
    // 键值对不存在，即需要将键值对插入
    // 插入操作前需要计算当前哈希表的负载因子
    double load_factor = 1.0 * map-> size / map-> capacity;
    if (load_factor >= LOAD_FACTOR_THRESHOLD)
    {
        // 当前哈希表负载因子已达到阈值，将动态数组进行扩容
        grow_capacity(map);
        // 数组长度已变，需要再哈希确定当前键值对的存储位置
        idx = hash(key, strlen(key), map-> hash_seed) % (map-> capacity);
    }
    // 开始插入新键值对
    KeyValueNode* new_node = calloc(1, sizeof(KeyValueNode));
    if (new_node == NULL)
    {
        printf("Error: calloc failed in grow_capacity\n");
        exit(1);
    }
    // 初始化结点
    new_node-> key = key;
    new_node-> val = val;
    // 链表的头插法
    new_node-> next = map-> buckets [idx];
    map-> buckets [idx] = new_node;
    // 不要忘记更新 size
    map-> size++;
    return NULL;
    }
// 查询一个键值对
ValueType hashmap_get(DynamicHashMap* map, KeyType key) {
    // 计算 key 的哈希值确定位置
    int idx = hash(key, strlen(key), map-> hash_seed) % (map-> capacity);
    // 遍历链表
    KeyValueNode* curr = map-> buckets [idx];
    while (curr != NULL) {
        if (strcmp(key, curr-> key) == 0) {
            return curr-> val;
        }
        curr = curr-> next;
    }
    return NULL;
}
// 删除某个键值对
bool hashmap_remove(DynamicHashMap* map, KeyType key) {
    // 计算 key 的哈希值确定位置
    int idx = hash(key, strlen(key), map-> hash_seed) % (map-> capacity);
    // 遍历链表
    KeyValueNode* curr = map-> buckets [idx];
    KeyValueNode* prev = NULL;
    while (curr != NULL) {
        if (strcmp(key, curr-> key) == 0) {
            if (prev == NULL)
            {
                map-> buckets [idx] = curr-> next;
            }
            else
            {
                prev-> next = curr-> next;
            }
            free(curr);
            map-> size--;
            return true;
        }
        prev = curr;
        curr = curr-> next;
    }
return false;
}</code>
    </pre>
</details>
<details>
<summary>
<font size="4" color="orange"> test.c </font>
</summary>
<pre> <code class="language-c">#define _CRT_SECURE_NO_WARNINGS
#include "dynamic_hashmap.h"
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;string.h&gt;
#define N 10 // 初始容量是 8，设置为 10 足以触发一次扩容
int main() {
    // 创建哈希表
    DynamicHashMap* map = hashmap_create();
    if (map == NULL) {
        printf("哈希表创建失败。\n");
        return 1;
    }
    // 插入多个键值对以触发扩容
    char tmp_key [1024], tmp_val [1024];
    for (int i = 0; i < N; i++) {
        /*
            sprintf 是一个将格式化的数据输出到一个字符数组中，并最终保存为字符串的标准库函数
            第一个参数是输出的目的地字符串
            后面的参数则和 printf 使用方式一样
        */
        sprintf(tmp_key, "key%d", i);
        sprintf(tmp_val, "value%d", i);
        // 使用 calloc 分配和复制键值对
        char* key = calloc(strlen(tmp_key) + 1, sizeof(char));
        char* val = calloc(strlen(tmp_val) + 1, sizeof(char));
        strcpy(key, tmp_key);
        strcpy(val, tmp_val);
        hashmap_put(map, key, val);
        printf("插入 %s: %s\n", key, val);
    }
    // 检查扩容前后的数据一致性
    bool flag = true;
    for (int i = 0; i < N; i++) {
        sprintf(tmp_key, "key%d", i);
        ValueType ret_val = hashmap_get(map, tmp_key);
        sprintf(tmp_val, "value%d", i);
        if (ret_val == NULL || strcmp(ret_val, tmp_val) != 0) {
            printf("错误：数据不一致，键 %s 应有的值为 %s，检索得到的值为 %s\n",
                tmp_key, tmp_val,
                ((ret_val != NULL) ? ret_val : "null"));
            flag = false;   // 若数据插入有问题，则修改标志为 false
        }
    }
    if (flag) {
        printf("所有数据在扩容后仍正确无误。\n");
    }
    // free 所有堆上分配的字符串
    for (int i = 0; i < map-> capacity; i++) {
        KeyValueNode* curr = map-> buckets [i];
        while (curr != NULL) {
            free(curr-> key);   // 释放键字符串
            free(curr-> val);   // 释放值字符串
            curr = curr-> next;
        }
    }
    // 销毁哈希表
    hashmap_destroy(map);
    return 0;
}</code>
</pre>
</details>
<h3 id="哈希表的应用场景">哈希表的应用场景</h3>
<p>哈希表的应用十分广泛，任何编程语言的程序在日常开发中，哈希表都是必须要使用的数据结构。它大致有以下常见的用途：</p>
<ol type="1">
<li>用作容器，存储键值对元素。用作容器是哈希表最常见的使用场景之一，毕竟键值对数据在开发中出现的频率很高，比如
“用户名-密码”
等。常见的高级编程语言，往往都会提供现成的哈希表库实现，比如：
<ol type="1">
<li>C++ 中的 <code>unordered_map</code> 和 <code>unordered_set</code>。
<ul>
<li>map
是是一种存储键值对的哈希表容器，提供了高效的插入，查询，删除等操作。</li>
<li>set
是一种基于哈希表的集合容器，它存储唯一元素，并提供高效的成员插入、查找和删除操作。</li>
</ul></li>
<li>Java 中的 <code>HashMap</code> 和 <code>HashSet</code> 以及
<code>HashTable</code>，它们都是基于哈希表实现的集合容器。</li>
</ol></li>
<li>在算法领域，哈希表还经常用于以下场景中：
<ol type="1">
<li><strong>查找重复或唯一元素</strong>：哈希表提供了快速查询和插入的功能，且
Key 唯一使得它在查重或查找唯一元素时特别好用。</li>
<li><strong>计数器应用</strong>：统计频率次数，也是哈希表经常应用的场景。</li>
</ol></li>
<li>哈希表可以用于实现数据库的索引，提高数据库的访问效率。</li>
<li>哈希表还常用于实现缓存，利用其快速查询的特点，用于快速查询缓存的消息。比如目前业界最流行的高速缓存中间件——Redis，它有个别名叫：键值对内存数据库，哈希表是
Redis 最常用的类型之一。</li>
</ol>
<hr />
<h2 id="二叉搜索树">二叉搜索树</h2>
<h3 id="二叉树">二叉树</h3>
<p>树是一种层次化的数据结构，它在现实生活和计算机科学中广泛存在并发挥重要作用。比如：</p>
<ol type="1">
<li>族谱(Family Tree)</li>
<li>组织架构</li>
<li>计算机文件系统的目录结构</li>
<li>…</li>
</ol>
<p>树这种数据结构的特点，是从一个单一的根结点开始，分支出多个结点，每个结点又可能有自己的子结点，形成一个分层次的树状结构。</p>
<p>不过在众多的树形结构中，我们只学习二叉树，原因很简单，它很重要！</p>
<p>它有多重要呢？</p>
<p>单就面试而言，在 LeetCode 中二叉树相关的题目共有 300
多道，占据总题目的近三分之一。同时，二叉树在整个算法板块中还起到承上启下的作用：它不但是数组和链表的延伸，又可以作为图的基础。总之，非常重要！</p>
<p>那什么是 <strong>二叉树(BinaryTree)</strong> 呢？</p>
<p><strong>二叉树的定义：二叉树是一棵树，并且二叉树的每个结点最多有两棵子树。二叉树的子树又分为左子树和右子树。</strong></p>
<p>从根结点开始，每个结点最多有两棵子树，这就是一棵
“二叉树”。二叉树有两种比较独特的形态：完全二叉树和满二叉树。</p>
<p>完全二叉树，若二叉树的深度为 h，除第 h
层外，其它各层(1～h-1)的结点数目都达到最大值，第 h
层的结点都连续排列在最左边，这样的二叉树就是完全二叉树。</p>
<p>满二叉树，每一层的结点数目都达到最大值(包括最下面一层)。</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202311300942801.png" alt="完全二叉树和满二叉树-示意图" style="zoom: 67%;" />
</div>
<h3 id="二叉搜索树-1">二叉搜索树</h3>
<p><strong>二叉搜索树是一种特殊的二叉树，其中每个结点的左子树只包含小于当前结点的值，右子树只包含大于当前结点的值。</strong></p>
<p>当然二叉搜索树的定义非常适合使用递归：</p>
<p>二叉搜索树是一种特殊的二叉树，从根结点开始，其每个结点都满足：</p>
<ol type="1">
<li>左子树中的每个结点的值都小于该结点的值，并且左子树本身也是二叉搜索树</li>
<li>右子树中的每个结点的值都大于该结点的值，并且右子树本身也是二叉搜索树</li>
</ol>
<p>所以二叉搜索树在定义时就已经和递归绑定了，我们在下面处理二叉搜索树时，递归是常用的手段。</p>
<p>如下图所示：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202312011624046.png" alt="二叉搜索树-示意图" style="zoom: 33%;" />
</div>
<p><strong>需要注意的是，二叉搜索树只是在二叉树的基础上规定了结点取值的大小关系，二叉搜索树并不一定就是完全二叉树、满二叉树。</strong></p>
<h3 id="设计思路-5">设计思路</h3>
<p>可以用一个结构体描述二叉搜索树的某个结点：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">int</span> KeyType;</span><br><span class="line"><span class="comment">// 二叉搜索树的结点类型</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">node</span> &#123;</span></span><br><span class="line"> KeyType key;       <span class="comment">// 结点 key 值, 具有唯一性</span></span><br><span class="line"> <span class="class"><span class="keyword">struct</span> <span class="title">node</span> *<span class="title">left</span>;</span> <span class="comment">// 左子树</span></span><br><span class="line"> <span class="class"><span class="keyword">struct</span> <span class="title">node</span> *<span class="title">right</span>;</span>    <span class="comment">// 右子树</span></span><br><span class="line">&#125; TreeNode;</span><br></pre></td></tr></table></figure>
<p>其中 key
决定了结点在树中的存储位置，也是搜索的凭证。所以出于简化操作的目的，大多二叉搜索树都会规定
<strong>key 是唯一的</strong>。</p>
<p>二叉搜索树最重要、基础的操作有以下：</p>
<ol type="1">
<li>插入。向树中添加一个新的结点。这个操作需要保持二叉搜索树的性质，即插入的结点必须放置在保持左子结点小于父结点，且右子结点大于父结点的正确位置上。</li>
<li>搜索。根据 key
查找目标结点。在二叉搜索树中，搜索操作可以高效进行，通过比较目标值与结点值来决定向左子树或右子树移动。搜索操作的实现是比较简单的。</li>
<li><strong>删除。根据 key
从树中删除某个结点。这是二叉搜索树中最复杂的操作。</strong></li>
<li>遍历。遍历树中的所有结点。常见的遍历方式有前序遍历、中序遍历、后序遍历以及层次遍历。其中，中序遍历二叉搜索树会得到一个有序的结点序列。</li>
</ol>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202401112321181.png" alt="二叉搜索树实现-概念模型" style="zoom: 50%;" />
</div>
<h3 id="代码实现-6">代码实现</h3>
<details>
<summary>
<font size="4" color="orange"> bst.h </font>
</summary>
<pre> <code class="language-c">#ifndef BST_H
#define BST_H
#include &lt;stdbool.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;stdio.h&gt;
typedef int KeyType;
typedef struct node {
    KeyType key;        // 结点 key 值, 具有唯一性
    struct node* left;  // 左子树
    struct node* right; // 右子树
} TreeNode;
// 推荐定义一个二叉搜索树的结构体, 这样更便于扩展
typedef struct {
    TreeNode* root; // 根结点指针
}BST;
// 基础操作
// 创建一棵空的 BST
BST* bst_create();
// 销毁一棵 BST
void bst_destroy(BST* tree);
// 根据 key 插入一个新结点
bool bst_insert(BST* tree, KeyType key);
// 根据 key 搜索某个结点并返回
TreeNode * bst_search(BST* tree, KeyType key);
// 根据 key 删除一个结点
bool bst_delete(BST* tree, KeyType key);
// 深度优先-先序遍历
void bst_preorder(BST* tree);
// 深度优先-中序遍历
void bst_inorder(BST* tree);
// 深度优先-后序遍历
void bst_postorder(BST* tree);
// 广度优先-层次遍历
void bst_levelorder(BST* tree);
#endif // ! BST_H
</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> bst.c </font>
</summary>
<pre> <code class="language-c">#include "bst.h"
#include "list_queue.h"
BST* bst_create()
{
    return  calloc(1, sizeof(BST));
}
/*
* 向二叉搜索树中新增一个结点
* 1.若 key 已存在，则不新增，插入失败
* 2.若 key 不存在，则进行新增，插入成功
*/
bool bst_insert(BST* tree, KeyType key) {
    // 1.遍历找到插入的位置
    // 初始化两个指针, 一个用于找到插入位置, 一个用于指向它的父结点
    TreeNode* parent = NULL;
    TreeNode* curr = tree-> root;
    int diff;   // while 循环一定执行, 不用担心 diff 未初始化
    while (curr != NULL) {  // 此遍历就是为了查找 NULL, curr 最终等于 NULL 说明查找到了插入位置
        diff = key - curr-> key;
        if (diff > 0) {
            // 待插入结点的 key 大于当前结点 key
            // 向右遍历查找
            parent = curr;
            curr = curr-> right;
        }
        else if (diff < 0)
        {
            // 待插入结点的 key 小于当前结点 key
            // 向左遍历查找
            parent = curr;
            curr = curr-> left;
        }
        else
        {
            // 待插入结点的 key 等于当前结点 key
            // key 重复, 插入失败
            return false;
        }
    }   // 循环结束, 说明 curr 是 NULL, 即 key 不重复, 找到了插入的位置, parent 指针指向待插入位置的父结点
    // 2.新建一个结点, 初始化结点
    // 重要建议: 在二叉搜索树中新建结点, 请一律使用 calloc, 安全第一!
    TreeNode* new_node = calloc(1, sizeof(TreeNode));
    if (new_node == NULL) {
        printf("calloc failed in bst_insert.\n");
        exit(1);
    }
    new_node-> key = key;
    // 3.将新结点链接到二叉树上(链表的尾插)
    if (parent == NULL) {
        // 说明此时树是一个空树
        tree-> root = new_node;  // 新结点成为根结点
        return true;    // 插入成功
    }
    // 插入的结点不是第一个结点
    if (diff < 0) {
        // 插入左子树
        parent-> left = new_node;
    }
    else {
        // 插入右子树
        parent-> right = new_node;
    }
    return true;
}
TreeNode * bst_search(BST* tree, KeyType key)
{
    TreeNode* curr = tree-> root;
    while (curr != NULL) {
        if (curr-> key == key)
        {
            return curr;
        }
        else  if (curr-> key > key)
        {
            curr = curr-> left;
        }
        else
        {
            curr = curr-> right;
        }
    }
    return NULL;
}
// 根据 key 删除一个结点
bool bst_delete(BST* tree, KeyType key) {
    // 1.利用 curr 和 parent 两个指针找到待删除结点以及它的父节点
    TreeNode* curr = tree-> root;
    TreeNode* parent = NULL;
    // 2.根据 key 值的大小关系, 来遍历这棵树的结点
    while (curr != NULL) {  // 这一点模拟了单链表的遍历, 因为搜索路径就是一条单链表
        int diff = key - curr-> key;
        if (diff > 0) {
            // 待删除 key 值比当前结点 key 值要大, 于是去右子树中遍历查找
            parent = curr;
            curr = curr-> right;
        }
        else if (diff < 0) {
            // 待删除 key 值比当前结点 key 值要小, 于是去左子树中遍历查找
            parent = curr;
            curr = curr-> left;
        }
        else {
            // 找到待删除结点, 下面就开始删除该结点的操作
            break;
        }
    }
    /*
        while 循环结束, 有两种可能性:
        1.找到了目标 key 结点, 依赖 break 结束循环
            此时 curr 指针指向待删除结点, parent 指向它的父结点
        2.没找到目标 key 结点, 依赖 curr 变成 NULL 结束循环
            此时 curr 指针是 NULL 相当于找到了这个不存在 key 结点的插入位置
            而 parent 就指向插入位置的父结点
        以上两种情况, 如果是第一种情况, 我们就继续删除
        但如果是第二种情况, 目标结点不存在, 删除就失败了
    */
    if (curr == NULL) {
        return false;
    }
    // 2.根据度不同来进行不同的处理, 使得待删除结点从树结构中断开
    // 2.1 判断结点的度是否为 2, 如果度为 2, 进行降级处理
    if (curr-> left != NULL && curr-> right != NULL) {
        // 删除的结点是度为 2 的, 于是将它降低为删除度为 0 或 1
        TreeNode* min_node = curr-> right;   // 寻找待删除结点的右子树最小结点, 从 curr 的右子树结点开始
        TreeNode* min_parent = curr;    // 寻找待删除结点的右子树最小结点的父结点
        while (min_node-> left != NULL) {
            min_parent = min_node;
            min_node = min_node-> left;
        }   // while 循环结束时, min_node 指向右子树最小结点, min_parent 指向 min_node 结点的父结点
        // 将右子树最小结点的值替换给 curr 结点, 并且后续将删除右子树最小结点
        curr-> key = min_node-> key;
        // 后续的删除操作都是基于 curr 指针和 parent 指针的, 所以这里要进行一步赋值操作
        curr = min_node;
        parent = min_parent;
    }
    // 接下来待删除的结点就是 curr 结点, 它的父结点就是 parent, 而且这个结点的度一定是 0 或 1
    // 2.2 统一处理度为 0 或 1 结点的删除操作
    // 核心操作: parent -> left/right = curr -> left/right
    // 先确定 parent 指向待删除结点的左子树还是右子树
    TreeNode* child = (curr-> left != NULL) ? curr-> left : curr-> right;
    /*
        考虑特殊情况:
        删除的结点是度为 0 或 1 的根结点
        此时需要更新根结点指针
    */
    if (parent == NULL) {
        // 删除的结点是根结点, 此根结点度为 0 或 1
        tree-> root = child;
    }
    else {
        // 删除的结点不是根结点
        // 执行核心操作: parent -> left/right = child;
        if (parent-> left == curr) {
            // 待删除结点是父结点的左子树, 那么就让父结点的左子树指向 child
            parent-> left = child;
        }
        else {
            // 待删除结点是父结点的右子树, 那么就让父结点的右子树指向 child
            parent-> right = child;
        }
    }
    // 3.free 这个结点
    free(curr); // 删除成功
    return true;
}
/*
  利用递归实现 BST 的先序遍历
  root 参数表示此一轮递归调用中的根结点
  先序遍历是: DLR
  分解的思路是:
  对于任何一棵树 root 来说:
  1.总是先输出根结点的值
  2.然后递归遍历它的左子树
  3.最后递归遍历它的右子树
  preoder(root) = D + preorder(root-> left) + preorder(root-> right)
  这就是递归体
  这样的分解不会无休止进行, 如果分解成一棵空树, 也就是 root 是空指针时, 返回函数
  这就是递归的出口
*/
static void preorder(TreeNode* root) {
    if (root == NULL)
    {
        return;
    }
    printf("%d ", root-> key);
    preorder(root-> left);
    preorder(root-> right);
}
// 深度优先-先序遍历
void bst_preorder(BST* tree)
{
    if (tree == NULL) {
        // 树为空直接返回
        return;
    }
    // 利用辅助函数递归实现 BST 的先序遍历
    preorder(tree-> root);
    printf("\n");
}
/*
  利用递归实现 BST 的中序遍历
  root 参数表示此一轮递归调用中的根结点
  中序遍历是: LDR
  分解的思路是:
  对于任何一棵树 root 来说:
  1.总是先递归遍历它的左子树
  1.然后再输出根结点的值
  3.最后递归遍历它的右子树
  inorder(root) = inorder(root-> left) + D + inorder(root-> right)
  这就是递归体
  这样的分解不会无休止进行, 如果分解成一棵空树, 也就是 root 是空指针时, 返回函数
  这就是递归的出口
*/
static void inorder(TreeNode* root) {
    if (root == NULL)
    {
        return;
    }
    inorder(root-> left);
    printf("%d ", root-> key);
    inorder(root-> right);
}
// 深度优先-中序遍历
void bst_inorder(BST* tree)
{
    if (tree == NULL) {
        // 树为空直接返回
        return;
    }
    // 利用辅助函数递归实现 BST 的中序遍历
    inorder(tree-> root);
    printf("\n");
}
/*
  利用递归实现 BST 的后序遍历
  root 参数表示此一轮递归调用中的根结点
  中序遍历是: LRD
  分解的思路是:
  对于任何一棵树 root 来说:
  1.总是先递归遍历它的左子树
  3.然后递归遍历它的右子树
  3.最后再输出根结点的值
  postorder(root) = postorder(root-> left) + postorder(root-> right) + D
  这就是递归体
  这样的分解不会无休止进行, 如果分解成一棵空树, 也就是 root 是空指针时, 返回函数
  这就是递归的出口
*/
static void postorder(TreeNode* root) {
    // 1.递归的出口
    if (root == NULL) {
        return;
    }
    // 2.递归体
    postorder(root-> left);
    postorder(root-> right);
    printf("%d ", root-> key);
}
void bst_postorder(BST* tree) {
    if (tree == NULL) {
        // 树为空直接返回
        return;
    }
    // 利用辅助函数递归实现 BST 的后序遍历
    postorder(tree-> root);
    printf("\n");
}
static void destroy(TreeNode* root) {
    // 1.递归的出口
    if (root == NULL) {
        return;
    }
    // 2.递归体
    destroy(root-> left);
    destroy(root-> right);
    free(root);
}
void bst_destroy(BST* tree)
{
    if (tree == NULL) {
        // 树为空直接返回
        return;
    }
    // 利用辅助函数递归实现 BST 的销毁
    destroy(tree-> root);
    free(tree);
}
void bst_levelorder(BST* tree) {
    LinkedListQueue* q = create_queue();
    enqueue(q, tree-> root);
    while (! is_empty(q)) {
        int level_size = q-> size;
        for (int i = 0; i < level_size; i++)
        {
            TreeNode* node = dequeue(q);
            printf("%d ", node-> key);
            if (node-> left != NULL)
            {
                enqueue(q, node-> left);
            }
            if (node-> right != NULL)
            {
                enqueue(q, node-> right);
            }
        }
        printf("\n");
    }
    destroy_queue(q); // 完成遍历后，销毁队列释放资源
    printf("\n");
}</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> list_queue.h </font>
</summary>
<pre> <code class="language-c">#ifndef LIST_QUEUE_H
#define LIST_QUEUE_H
#include &lt;stdbool.h&gt;
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#include "bst.h"
// 定义队列中的元素类型
typedef TreeNode* ElementType;
// 队列节点的结构
typedef struct node_s {
    ElementType data;
    struct node_s* next;
} QueueNode;
// 队列的结构
typedef struct {
    QueueNode* front;  // 队头结点指针
    QueueNode* rear;   // 队尾结点指针
    int size;   // 队列中元素的个数
} LinkedListQueue;
// 函数声明
// 创建链式队列
LinkedListQueue* create_queue();
// 销毁链式队列
void destroy_queue(LinkedListQueue* q);
// 入队列
bool enqueue(LinkedListQueue* q, ElementType element);
// 出队列并返回队头元素
ElementType dequeue(LinkedListQueue* q);
// 访问队头元素
ElementType peek_queue(LinkedListQueue* q);
// 判空
bool is_empty(LinkedListQueue* q);
#endif // ! LIST_QUEUE_H </code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> list_queue.c </font>
</summary>
<pre> <code class="language-c">#include "list_queue.h"
// 函数声明
LinkedListQueue* create_queue() {
    return calloc(1, sizeof(LinkedListQueue));
}
void destroy_queue(LinkedListQueue* q) {
    // 从队头开始遍历链表，销毁每一个结点
    QueueNode* current = q-> front;
    while (current != NULL) {
        QueueNode* temp = current-> next;
        free(current);
        current = temp;
    }
    // 销毁队列结构体
    free(q);
}
bool is_empty(LinkedListQueue* q) {
    // 队头指针是空指针，即表示空队列
    return q-> front == NULL;
}
// 入队操作: 只能在队尾插入一个结点
// 由于已存在尾指针，所以这里的操作就是链表尾插
bool enqueue(LinkedListQueue* q, ElementType element) {
    QueueNode* new_node = malloc(sizeof(QueueNode));
    if (new_node == NULL) {
        printf("Error: malloc failed in enqueue.\n");
        return false;
    }
    // 初始化新结点
    new_node-> data = element;
    new_node-> next = NULL;
    // 开始进行尾插法插入一个结点
    // 分两种情况：如果尾插插入的是第一个结点需要同步更新头指针，否则仅需要更新尾指针
    if (q-> front == NULL) {
        // 插入的是第一个结点
        q-> front = new_node;
        q-> rear = new_node;
    }
    else {
        // 插入的不是第一个结点
        q-> rear-> next = new_node;
        q-> rear = new_node;
    }
    q-> size++;
    return true;
}
// 出队，在队头删除一个结点。也就是在删除链表的第一个结点
ElementType dequeue(LinkedListQueue* q) {
    if (is_empty(q)) {
        printf("Error: queue is empty.\n");
        exit(1);
    }
    QueueNode* tmp = q-> front;
    // 将出队的结点数据保存
    ElementType remove_data = tmp-> data;
    // 更新队头指针
    q-> front = tmp-> next;
    if (q-> front == NULL) {
        // 如果队头更新后, 队列为空, 说明出队的就是最后一个元素
        // 于是同步更新队尾指针
        q-> rear = NULL;
    }
    free(tmp);
    q-> size--;
    return remove_data;
}
// 访问队头元素但不出队
ElementType peek_queue(LinkedListQueue* q) {
    if (is_empty(q)) {
        printf("Error: queue is empty.\n");
        exit(1);
    }
    return q-> front-> data;
}</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange"> test.c </font>
</summary>
<pre> <code class="language-c">#define _CRT_SECURE_NO_WARNINGS
#include &lt;stdio.h&gt;
#include "bst.h"
int main(void) {
    // 创建二叉搜索树
    BST* tree = bst_create();
    if (! tree) {
        printf("二叉搜索树创建失败。\n");
        return 1;
    }
    // 插入元素
    int elements [] = { 50, 30, 70, 20, 40, 60, 80, 30 }; // 包含重复元素 30
    printf("正在插入元素: ");
    int arr_len = sizeof(elements) / sizeof(elements [0]);
    for (int i = 0; i < arr_len; i++) {
        if (bst_insert(tree, elements [i])) {
            printf("%d ", elements [i]); // 打印成功插入的元素
        }
        else {
            printf("(重复 %d) ", elements [i]); // 打印重复元素的信息
        }
    }
    printf("\n");
    // 预期结果：重复的元素不应该被插入到树中。
    // 中序遍历，应该输出有序序列
    printf("中序遍历输出(预期有序): ");
    bst_inorder(tree);
    printf("\n");
    // 预期结果：20, 30, 40, 50, 60, 70, 80
    // 先序遍历输出 预期结果：50 30 20 40 70 60 80
    printf("先序遍历输出: ");
    bst_preorder(tree);
    printf("\n");
    // 后序遍历输出 预期结果：20 40 30 60 80 70 50
    printf("后序遍历输出: ");
    bst_postorder(tree);
    printf("\n");
    // 层次遍历输出 预期结果：50 30 70 20 40 60 80
    printf("层次遍历输出: ");
    bst_levelorder(tree);
    printf("\n");
    // 测试搜索存在和不存在的情况 预期结果：找到了
    int searchKey = 40;
    printf("搜索键 %d 的结果: %s\n", searchKey, bst_search(tree, searchKey) ? "找到了" : "未找到");
    // 预期结果：未找到
    searchKey = 100;
    printf("搜索键 %d 的结果: %s\n", searchKey, bst_search(tree, searchKey) ? "找到了" : "未找到");
    // 测试删除操作
    printf("删除元素 70 和 20\n");
    bst_delete(tree, 70);
    bst_delete(tree, 20);
    // 再次进行中序遍历验证删除操作 // 预期结果：30, 40, 50, 60, 80
    printf("删除元素后中序遍历输出: ");
    bst_inorder(tree);
    printf("\n");
    // 销毁二叉搜索树
    bst_destroy(tree);
    return 0;
}</code>
</pre>
</details>
<hr />
<h2 id="总结">总结</h2>
<p>以下是关于动态数组、链表、栈、队列、哈希表和二叉搜索树的特点和使用场景的总结：</p>
<table>
<colgroup>
<col style="width: 8%" />
<col style="width: 22%" />
<col style="width: 25%" />
<col style="width: 16%" />
<col style="width: 28%" />
</colgroup>
<thead>
<tr class="header">
<th>数据结构</th>
<th>特点</th>
<th>时间复杂度（平均情况）</th>
<th>时间复杂度（最坏情况）</th>
<th>使用场景</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>动态数组</td>
<td>连续内存、支持随机访问、可扩容</td>
<td>访问：O(1)，插入末尾：O(1)<em>，删除末尾：O(1)</em></td>
<td>插入：O(n)，删除：O(n)</td>
<td>适用于元素个数动态变化、频繁随机访问的场景（如
ArrayList、Vector）</td>
</tr>
<tr class="even">
<td>链表（单/双）</td>
<td>非连续内存、插入删除快、访问慢（需遍历）</td>
<td>插入：O(1)，删除：O(1)，访问：O(n)</td>
<td>插入/删除：O(n)（需查找位置）</td>
<td>多用于频繁插入/删除的场景，如实现栈、队列、LRU 缓存</td>
</tr>
<tr class="odd">
<td>栈</td>
<td>后进先出（LIFO），一般基于数组或链表实现</td>
<td>插入（push）：O(1)，删除（pop）：O(1)，访问栈顶：O(1)</td>
<td>O(1)</td>
<td>用于函数调用、表达式求值、括号匹配、撤销操作等场景</td>
</tr>
<tr class="even">
<td>队列</td>
<td>先进先出（FIFO），一般基于数组或链表实现</td>
<td>入队：O(1)，出队：O(1)，访问队首/尾：O(1)</td>
<td>O(1)</td>
<td>用于任务调度、异步数据处理、消息队列等场景</td>
</tr>
<tr class="odd">
<td>哈希表</td>
<td>快速查找和插入，基于哈希函数实现，可能有哈希冲突</td>
<td>查找/插入/删除：O(1)</td>
<td>查找/插入/删除：O(n)（大量冲突时）</td>
<td>用于快速查找，如字典、缓存、数据去重等</td>
</tr>
<tr class="even">
<td>二叉搜索树（BST）</td>
<td>左小右大，有序结构，支持范围查找</td>
<td>查找/插入/删除：O(log n)（平衡时）</td>
<td>查找/插入/删除：O(n)（退化为链表）</td>
<td>适用于需要有序数据结构，支持范围查询、排序等（如
TreeMap、TreeSet）</td>
</tr>
</tbody>
</table>
<p>*说明：动态数组在扩容时，插入末尾的操作可能引起整体复制，因此最坏情况为
O(n)，但摊还复杂度为 O(1)。</p>
]]></content>
      <categories>
        <category>数据结构与算法（DSA）</category>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
        <tag>数组</tag>
        <tag>链表</tag>
        <tag>栈</tag>
        <tag>队列</tag>
        <tag>哈希表</tag>
        <tag>二叉搜索树</tag>
      </tags>
  </entry>
  <entry>
    <title>递归算法设计与实现</title>
    <url>/2024/04/29/programming/data-structure-algorithm/recursion/</url>
    <content><![CDATA[<p>递归（Recursion）是一种通过函数调用自身来解决问题的方法。要正确设计和实现递归算法，必须满足
<strong>递归三要素</strong>，否则可能导致无限递归、栈溢出或逻辑错误。</p>
<span id="more"></span>
<hr />
<h2 id="递归的三个核心要素">递归的三个核心要素</h2>
<h3 id="递归终止条件base-case">1. 递归终止条件（Base Case）</h3>
<ul>
<li><p><strong>作用</strong>：明确递归何时结束，防止无限调用。<br />
</p></li>
<li><p><strong>要求</strong>：</p>
<ul>
<li>必须有至少一个终止条件（即可以直接求解的简单情况）。<br />
</li>
<li>终止条件通常是问题的最小规模或边界条件。<br />
</li>
</ul></li>
<li><p><strong>示例</strong>（阶乘计算）：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">factorial</span>(<span class="params">n</span>):</span><br><span class="line">    <span class="keyword">if</span> n == <span class="number">0</span>:  <span class="comment"># Base Case: 0! = 1</span></span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> n * factorial(n - <span class="number">1</span>)</span><br></pre></td></tr></table></figure></li>
</ul>
<hr />
<h3 id="递归调用recursive-case">2. 递归调用（Recursive Case）</h3>
<ul>
<li><p><strong>作用</strong>：将原问题分解为更小的同类子问题，通过调用自身解决。<br />
</p></li>
<li><p><strong>要求</strong>：</p>
<ul>
<li>每次递归调用必须向终止条件逼近（即问题规模减小）。<br />
</li>
<li>子问题的解能组合成原问题的解。<br />
</li>
</ul></li>
<li><p><strong>示例</strong>（斐波那契数列）：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">fibonacci</span>(<span class="params">n</span>):</span><br><span class="line">    <span class="keyword">if</span> n &lt;= <span class="number">1</span>:  <span class="comment"># Base Case: fib(0)=0, fib(1)=1</span></span><br><span class="line">        <span class="keyword">return</span> n</span><br><span class="line">    <span class="keyword">return</span> fibonacci(n - <span class="number">1</span>) + fibonacci(n - <span class="number">2</span>)  <span class="comment"># Recursive Case</span></span><br></pre></td></tr></table></figure></li>
</ul>
<hr />
<h3 id="问题可分解性progress-toward-base-case">3. 问题可分解性（Progress
Toward Base Case）</h3>
<ul>
<li><p><strong>作用</strong>：确保每次递归调用后，问题规模严格缩小，最终达到终止条件。<br />
</p></li>
<li><p><strong>要求</strong>：</p>
<ul>
<li>递归调用的参数必须比原问题的参数更接近终止条件。<br />
</li>
<li>避免“原地递归”或扩大问题规模（否则会导致无限递归）。<br />
</li>
</ul></li>
<li><p><strong>错误示例</strong>（缺少问题规模缩小）：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">infinite_recursion</span>(<span class="params">n</span>):</span><br><span class="line">    <span class="keyword">if</span> n == <span class="number">0</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> infinite_recursion(n)  <span class="comment"># 参数未改变，无限递归！</span></span><br></pre></td></tr></table></figure></li>
</ul>
<hr />
<h2 id="递归执行过程分析">递归执行过程分析</h2>
<p>以计算 <code>factorial(3)</code> 为例：</p>
<ol type="1">
<li><code>factorial(3)</code> → <code>3 * factorial(2)</code><br />
</li>
<li><code>factorial(2)</code> → <code>2 * factorial(1)</code><br />
</li>
<li><code>factorial(1)</code> → <code>1 * factorial(0)</code><br />
</li>
<li><code>factorial(0)</code> → 返回 <code>1</code>（终止条件）<br />
</li>
<li>逐层返回结果：<code>1</code> → <code>1*1=1</code> →
<code>2*1=2</code> → <code>3*2=6</code>。</li>
</ol>
<hr />
<h2 id="常见递归问题类型">常见递归问题类型</h2>
<ol type="1">
<li><strong>分治问题（divide-and-conquer）</strong>（如归并排序、快速排序）。<br />
</li>
<li><strong>树/图遍历</strong>（如二叉树的前序/中序/后序遍历）。<br />
</li>
<li><strong>回溯算法</strong>（如八皇后问题、迷宫求解）。</li>
</ol>
<hr />
<h2 id="注意事项">注意事项</h2>
<ul>
<li><strong>栈溢出风险</strong>：递归深度过大时（如数万层），需改用
<strong>尾递归优化</strong> 或 <strong>迭代</strong>。<br />
</li>
<li><strong>重复计算问题</strong>：如斐波那契数列的朴素递归效率极低，需配合
<strong>备忘录（Memoization）</strong> 优化。</li>
</ul>
<p>递归的核心在于
<strong>将大问题拆解为小问题</strong>，理解三要素后，可以更安全地设计递归算法。</p>
]]></content>
      <categories>
        <category>数据结构与算法（DSA）</category>
        <category>算法</category>
      </categories>
      <tags>
        <tag>递归</tag>
      </tags>
  </entry>
  <entry>
    <title>计算机硬件基础知识</title>
    <url>/2025/12/08/programming/computer-system/basic-knowledge-of-computer-hardware/</url>
    <content><![CDATA[<p>本文介绍一些计算机硬件方面的基础知识，旨在为电脑组装、配置升级、检测维修等提供一些帮助。</p>
<span id="more"></span>
<hr />
<h2 id="内存">内存</h2>
<h3 id="内存检测工具">内存检测工具</h3>
<h4 id="memtest86"><span class="exturl" data-url="aHR0cHM6Ly93d3cubWVtdGVzdDg2LmNvbS8=">MemTest86<i class="fa fa-external-link-alt"></i></span></h4>
<h5 id="制作启动盘">制作启动盘</h5>
<p>下载并解压，执行<code>*.exe</code>制作启动盘。制作启动盘步骤如下：</p>
<blockquote>
<p>For Windows:</p>
<ul>
<li>Plug in your USB drive.</li>
<li>Launch the included ImageUSB application.</li>
<li>Select your USB drive from the list (Step 1).</li>
<li>Select ‘Write image to USB drive’ (Step 2)</li>
<li>If it is not already selected, select the included image file (Step
3).</li>
<li>Click ‘Write’ (Step 4).</li>
</ul>
<p>After accepting a few more prompts this should give you a working
bootable USB drive.</p>
</blockquote>
<p>新硬盘测试一轮就可以，二手或旧的可以默认执行4轮。</p>
<h5 id="测试时间">测试时间</h5>
<p>两条镁光8GB DDR5 1R PC5-38400 4800MT/s 1.1V， <u>4轮耗时：
1:43:44</u></p>
<h5 id="测试通过报告">测试通过报告</h5>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20251209160346454.png"
alt="MemTest86测试通过报告" />
<figcaption aria-hidden="true">MemTest86测试通过报告</figcaption>
</figure>
<hr />
<h2 id="显示屏">显示屏</h2>
<p>✏ ✏ ✏</p>
]]></content>
      <categories>
        <category>计算机系统</category>
      </categories>
      <tags>
        <tag>计算机系统</tag>
      </tags>
  </entry>
  <entry>
    <title>有符号整数的三种编码方式：原码、反码和补码</title>
    <url>/2024/12/04/programming/computer-system/code-original-inverse-complement/</url>
    <content><![CDATA[<p>在计算机中，<strong>原码、反码、补码</strong>
是用于表示有符号整数的三种编码方式，主要用于解决二进制数的
<strong>正负表示</strong> 和 <strong>加减运算</strong>
问题。它们的核心区别在于 <strong>符号位的处理</strong> 和
<strong>负数的表示方法</strong>。</p>
<span id="more"></span>
<hr />
<h2 id="原码sign-magnitude">原码（Sign-Magnitude）</h2>
<h3 id="定义">定义</h3>
<ul>
<li>最高位（最左边的一位）表示符号：
<ul>
<li><strong><code>0</code> 表示正数</strong>（如 <code>+5</code>）</li>
<li><strong><code>1</code> 表示负数</strong>（如 <code>-5</code>）</li>
</ul></li>
<li>其余位表示数值的绝对值。</li>
</ul>
<h3 id="示例8位二进制">示例（8位二进制）</h3>
<table>
<thead>
<tr class="header">
<th>十进制</th>
<th>原码表示（8位）</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>+5</td>
<td><code>0000 0101</code></td>
</tr>
<tr class="even">
<td>-5</td>
<td><code>1000 0101</code></td>
</tr>
</tbody>
</table>
<h3 id="特点">特点</h3>
<p>✅ <strong>直观</strong>：和人类书写习惯一致（正负号+数值）。<br />
❌ <strong>问题</strong>：</p>
<ol type="1">
<li><strong><code>+0</code> 和 <code>-0</code> 不唯一</strong>：
<ul>
<li><code>+0</code> = <code>0000 0000</code></li>
<li><code>-0</code> = <code>1000 0000</code>（计算机无法区分）</li>
</ul></li>
<li><strong>加减运算复杂</strong>：
<ul>
<li>需要额外判断符号位，硬件实现麻烦。</li>
</ul></li>
</ol>
<hr />
<h2 id="反码ones-complement">反码（Ones’ Complement）</h2>
<h3 id="定义-1">定义</h3>
<ul>
<li><strong>正数</strong>：和原码相同。</li>
<li><strong>负数</strong>：在原码基础上，<strong>符号位不变，数值位取反</strong>（<code>0</code>→<code>1</code>，<code>1</code>→<code>0</code>）。</li>
</ul>
<h3 id="示例8位二进制-1">示例（8位二进制）</h3>
<table>
<thead>
<tr class="header">
<th>十进制</th>
<th>反码表示（8位）</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>+5</td>
<td><code>0000 0101</code></td>
</tr>
<tr class="even">
<td>-5</td>
<td><code>1111 1010</code></td>
</tr>
</tbody>
</table>
<table>
<colgroup>
<col style="width: 13%" />
<col style="width: 63%" />
<col style="width: 23%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: center;">十进制运算</th>
<th style="text-align: center;">反码计算</th>
<th style="text-align: center;">反码计算结果（十进制）</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: center;">1 +（-1）= 0</td>
<td style="text-align: center;">[0000 0001]反 + [1111 1110]反 = [1111
1111]反 = [1000 0000]原</td>
<td style="text-align: center;">-0</td>
</tr>
<tr class="even">
<td style="text-align: center;">1 +（-2）= -1</td>
<td style="text-align: center;">[0000 0001]反 + [1111 1101]反 = [1111
1110]反 = [1000 0001]原</td>
<td style="text-align: center;">-1</td>
</tr>
<tr class="odd">
<td style="text-align: center;">-1 +（2）= 1</td>
<td style="text-align: center;">[1111 1110]反 + [0000 0010]反 = [0000
0000]反<strong>（溢出）</strong> = [0000 0000]原</td>
<td style="text-align: center;">0（<strong>溢出归0</strong>）</td>
</tr>
</tbody>
</table>
<p>从上表可以看出，如果用反码直接进行计算，虽然有所改善，但仍有问题，所以计算机中的运算也不能直接用反码。</p>
<h3 id="特点-1">特点</h3>
<p><strong>仍然存在 <code>+0</code> 和 <code>-0</code> 问题</strong></p>
<ul>
<li><code>+0</code> = <code>0000 0000</code></li>
<li><code>-0</code> = <code>1111 1111</code>（仍然无意义）</li>
</ul>
<p><strong>运算仍需调整</strong>：</p>
<ul>
<li>加减法后可能需要 <strong>“循环进位”</strong>（如
<code>1 + (-1)</code> = <code>0000 0001</code> + <code>1111 1110</code>
= <code>1111 1111</code>（<code>-0</code>），需要额外加 <code>1</code>
修正）。</li>
</ul>
<hr />
<h2 id="补码twos-complement现代计算机标准">补码（Two’s
Complement）⭐（现代计算机标准）</h2>
<h3 id="定义-2">定义</h3>
<ul>
<li><strong>正数</strong>：和原码相同。</li>
<li><strong>负数</strong>：在反码基础上 <strong>+1</strong>（即
<code>原码取反 + 1</code>）。</li>
</ul>
<h3 id="示例8位二进制-2">示例（8位二进制）</h3>
<table>
<thead>
<tr class="header">
<th>十进制</th>
<th>补码表示（8位）</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>+5</td>
<td><code>0000 0101</code></td>
</tr>
<tr class="even">
<td>-5</td>
<td><code>1111 1011</code></td>
</tr>
</tbody>
</table>
<h3 id="计算--5-的补码">计算 <code>-5</code> 的补码</h3>
<ol type="1">
<li><code>+5</code> 原码 = <code>0000 0101</code></li>
<li><strong>取反</strong> = <code>1111 1010</code>（反码）</li>
<li><strong>+1</strong> = <code>1111 1011</code>（补码）</li>
</ol>
<p>举个例子，如果是8位的二进制数：</p>
<blockquote>
<p>[+1] = [0000 0001]原 = [0000 0001]反 = [0000 0001]补</p>
<p>[-1] = [1000 0001]原 = [1111 1110]反 = [1111 1111]补</p>
<p>[-2] = [1000 0010]原 = [1111 1101]反 = [1111 1110]补</p>
</blockquote>
<table>
<colgroup>
<col style="width: 13%" />
<col style="width: 63%" />
<col style="width: 23%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: center;">十进制运算</th>
<th style="text-align: center;">补码计算</th>
<th style="text-align: center;">补码计算结果（十进制）</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: center;">1 +（-1）= 0</td>
<td style="text-align: center;">[0000 0001]补 + [1111 1111]补 = [0000
0000]补 = [0000 0000]原</td>
<td style="text-align: center;">0（<strong>溢出位舍弃</strong>）</td>
</tr>
<tr class="even">
<td style="text-align: center;">1 +（-2）= -1</td>
<td style="text-align: center;">[0000 0001]补 + [1111 1110]补 = [1111
1111]补 = [1111 1110]反 = [1000 0001]原</td>
<td style="text-align: center;">-1</td>
</tr>
<tr class="odd">
<td style="text-align: center;">-1 +（2）= 1</td>
<td style="text-align: center;">[1111 1111]补 + [0000 0010]补 = [0000
0001]补 = [0000 0001]原</td>
<td style="text-align: center;">1（<strong>溢出位舍弃</strong>）</td>
</tr>
</tbody>
</table>
<p>从上表可以看出，如果使用补码进行计算，就可以正常的进行加法算术运算了。</p>
<h3 id="特点-2">特点</h3>
<p>✅ <strong>解决所有问题</strong>：</p>
<ol type="1">
<li><p><strong><code>+0</code> 和 <code>-0</code> 统一</strong>：</p>
<ul>
<li><code>+0</code> = <code>0000 0000</code></li>
<li><code>-0</code> ：原码 <code>1000 0000</code> → 反码
<code>1111 1111</code> → 补码 <code>0000 0000</code>（和 <code>+0</code>
相同）。</li>
</ul></li>
<li><p><strong>加减法直接运算</strong>：</p>
<ul>
<li><code>5 + (-3)</code> = <code>0000 0101</code> +
<code>1111 1101</code> =
<code>0000 0010</code>（<code>2</code>，正确）。</li>
</ul></li>
<li><p><strong>表示范围更大</strong>：</p>
<ul>
<li>8位补码范围：<code>-128</code>（<code>1000 0000</code>）~
<code>+127</code>（<code>0111 1111</code>），比原码/反码多一个数（<code>-128</code>）。</li>
</ul></li>
</ol>
<p>那么至此，总结两个非常重要的结论：</p>
<ol type="1">
<li><strong>正数的原码反码补码是一致的，负数的反码是原码除符号位取反，补码是反码+1。</strong></li>
<li><strong>计算机中有符号整数的存储方式都是以补码的形式存储的，运算也是以补码的形式计算的。</strong></li>
</ol>
<hr />
<h2 id="三种编码对比8位二进制">三种编码对比（8位二进制）</h2>
<table>
<thead>
<tr class="header">
<th>十进制</th>
<th>原码</th>
<th>反码</th>
<th>补码</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>+5</td>
<td><code>0000 0101</code></td>
<td><code>0000 0101</code></td>
<td><code>0000 0101</code></td>
</tr>
<tr class="even">
<td>-5</td>
<td><code>1000 0101</code></td>
<td><code>1111 1010</code></td>
<td><code>1111 1011</code></td>
</tr>
<tr class="odd">
<td>+0</td>
<td><code>0000 0000</code></td>
<td><code>0000 0000</code></td>
<td><code>0000 0000</code></td>
</tr>
<tr class="even">
<td>-0</td>
<td><code>1000 0000</code></td>
<td><code>1111 1111</code></td>
<td><code>0000 0000</code></td>
</tr>
<tr class="odd">
<td>-128</td>
<td>无法表示</td>
<td>无法表示</td>
<td><code>1000 0000</code></td>
</tr>
</tbody>
</table>
<hr />
<h2 id="为什么计算机使用补码">为什么计算机使用补码</h2>
<blockquote>
<ol type="1">
<li><strong>统一 <code>+0</code> 和
<code>-0</code></strong>，避免歧义。</li>
<li><strong>加减法可以直接运算</strong>，无需额外判断符号。</li>
<li><strong>硬件实现简单</strong>，CPU 只需加法器，无需额外电路。</li>
<li><strong>表示范围更大</strong>（如 8 位补码可表示
<code>-128</code>~<code>127</code>，而原码/反码只能
<code>-127</code>~<code>127</code>）。</li>
</ol>
</blockquote>
<hr />
<h2 id="常见问题">常见问题</h2>
<h3 id="补码-1000-0000-代表多少">补码 <code>1000 0000</code>
代表多少</h3>
<ul>
<li>在 8 位补码中，<code>1000 0000</code> 表示
<code>-128</code>（没有对应的原码/反码）。</li>
</ul>
<h3 id="补码的运算溢出怎么办">补码的运算溢出怎么办</h3>
<ul>
<li>例如 <code>127 + 1</code> = <code>0111 1111</code> +
<code>0000 0001</code> =
<code>1000 0000</code>（<code>-128</code>，溢出）。</li>
<li>CPU 会设置 <strong>溢出标志位（OF）</strong>，由程序员处理。</li>
</ul>
<hr />
<h2 id="总结"><strong>总结</strong></h2>
<table>
<colgroup>
<col style="width: 10%" />
<col style="width: 30%" />
<col style="width: 31%" />
<col style="width: 27%" />
</colgroup>
<thead>
<tr class="header">
<th>编码</th>
<th>优点</th>
<th>缺点</th>
<th>适用场景</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>原码</strong></td>
<td>直观</td>
<td><code>±0</code> 问题，运算复杂</td>
<td>早期计算机（已淘汰）</td>
</tr>
<tr class="even">
<td><strong>反码</strong></td>
<td>改进负数表示</td>
<td><code>±0</code> 仍存在，运算需调整</td>
<td>过渡方案（基本不用）</td>
</tr>
<tr class="odd">
<td><strong>补码</strong></td>
<td>无 <code>±0</code> 问题，运算简单</td>
<td>负数计算稍复杂</td>
<td><strong>现代计算机标准</strong></td>
</tr>
</tbody>
</table>
<p>补码是计算机存储和处理有符号整数的
<strong>最优方案</strong>，理解它对于学习
<strong>计算机组成原理、编程（如整数溢出）、逆向工程</strong>
都至关重要！</p>
]]></content>
      <categories>
        <category>计算机系统</category>
      </categories>
      <tags>
        <tag>编码</tag>
      </tags>
  </entry>
  <entry>
    <title>七大排序算法总结</title>
    <url>/2024/01/30/programming/data-structure-algorithm/sort-algorithm-summary/</url>
    <content><![CDATA[<p>本文介绍了七种经典的基于比较的排序算法，其中基础排序算法包括冒泡排序、选择排序和插入排序三种，高级排序包括快速排序、归并排序、堆排序和希尔排序四种。文中首先对每个排序算法分析了其设计思想，接着使用C语言进行代码实现，然后从时间复杂度、空间复杂度和稳定性三个方面对各个算法进行分析，最后对这七种排序算法的性能和适用场景方面进行总结。</p>
<span id="more"></span>
<hr />
<h2 id="概述">概述</h2>
<p>排序算法，就是将一个无序数组内的全部元素，按照递增或递减的顺序重新排列起来的程序实现方法。在结构体中，还有针对结构体数组排序的排序算法。对于多种属性数据对象集合，按照其中的某一个或某一些属性的大小进行排序。例如：对一个公司的员工信息排序时，可以分别按工号、姓名或年龄等属性进行排序。</p>
<p>基础排序算法包括冒泡排序、选择排序和插入排序三种，高级排序包括快速排序、归并排序、堆排序和希尔排序等等。七种常见的、经典的基于”比较”的排序算法：</p>
<ol type="1">
<li>冒泡排序</li>
<li>选择排序</li>
<li>插入排序</li>
<li>希尔排序</li>
<li>快速排序</li>
<li>归并排序</li>
<li>堆排序</li>
</ol>
<p>这些排序算法各有各的特点、适用场景，没有一种算法总是最优解。但相对而言比较重要、更常用的是：快速排序、归并排序、堆排序以及插入排序。</p>
<p><strong>如何评估一个排序算法？</strong></p>
<p>从三个维度去分析一个排序算法：<strong>时间复杂度、空间复杂度和稳定性。</strong></p>
<p>下面这张图展示了算法随着数据规模的增大，指令数量和额外内存空间占用，在不同级别下的增加趋势：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202401151901805.png" alt="不同级别大O表示法的增加趋势-图" style="zoom: 50%;" />
</div>
<p>其中，常数级别<span
class="math inline">\(O(1)\)</span>和对数级别<span
class="math inline">\(O(\log
n)\)</span>被认为是特别优秀的，稳定且高效。</p>
<p>而从<span
class="math inline">\(O(n^2)\)</span>开始，就表示算法随着数据规模的增大而迅速变得低效，在实际应用中要尽量避免选择这样的算法去进行大数据集的处理。</p>
<hr />
<h2 id="冒泡排序">冒泡排序</h2>
<h3 id="算法思想">算法思想</h3>
<ol type="1">
<li><strong>第一轮冒泡排序：</strong>从数组的第一个元素开始，比较相邻的元素。如果第一个元素比第二个元素大，则交换它们的位置。然后，移动到下一对相邻元素，重复这个过程，直到比较最后一对元素。每一轮冒泡排序都会使当前比较序列的最大值到达数组末尾，随后第二轮排序过程中，需要比较的元素就减1。(将尾部最大的元素减去不再排序)</li>
<li><strong>第二轮冒泡排序：</strong>重复第一轮的过程，但这次只比较和交换直到倒数第二个元素（因为最后一个元素已经是最大的了）。在这一轮结束时，倒数第二大的元素会被“冒泡”到倒数第二的位置。</li>
<li>…</li>
<li><strong>结束条件：</strong>
<ol type="1">
<li>在不设置任何额外结束条件的前提下，冒泡排序每一轮都会将未排序序列的最大值”冒泡”到末尾。冒泡排序需要进行固定的(n
- 1)轮！</li>
<li>但实际上在这(n -
1)轮冒泡排序的过程中，只要某一轮完全不存在元素的交换，就说明数组已经完全有序了，排序就可以结束了。</li>
<li>所以我们可以设定一个布尔值来标记此轮冒泡排序是否存在元素交换，如果没有元素交换，直接结束整个排序。这种做法可以优化冒泡排序的性能，尤其是当原数组已基本有序时。</li>
</ol></li>
</ol>
<h3 id="代码实现">代码实现</h3>
<details>
<summary>
<font size="4" color="orange">冒泡排序算法C语言实现</font>
</summary>
<pre><code class="language-c">
#include &lt;stdio.h&gt;
#include &lt;stdbool.h&gt;
#define ARR_LEN(arr) (sizeof(arr) / sizeof(arr[0]))
#define SWAP(arr, i, j ) {      \
    int tmp = arr[i];           \
    arr[i] = arr[j];            \
    arr[j] = tmp;               \
}
void print_arr(int arr[], int len) {
    for (int i = 0; i < len; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}
// 冒泡排序
void bubble_sort(int arr[], int len) {
    for (size_t i = 0; i < len - 1; i++)
    {
        // 标记在这一次冒泡排序中有没有交换,false表示没有交换
        bool swapped = false;
        for (size_t j = 0; j < len - i - 1; j++) {
            if (arr[j]>arr[j+1])
            {
                SWAP(arr, j, j+1);
                // 发生了交换改变标记
                swapped = true;
            }
        }
        // 在一轮冒泡排序中没有任何交换,则排序已经完成,终止循环
        if (!swapped) {
            break;
        }
        // 打印一轮冒泡排序后数组的元素排列
        print_arr(arr, len);
    }
}
int main(void) {
    // 测试冒泡排序
    int arr[] = { 1,10,2,5,3,4,5,6,3,2 };
    int len = ARR_LEN(arr);
    bubble_sort(arr, len);
    return 0;
}</code>
</pre>
</details>
<h3 id="性能分析">性能分析</h3>
<p><strong>时间复杂度分析：</strong></p>
<p>最佳情况：由于使用了”swapped”标记，所以在最佳情况下，即输入的数组已经是有序的情况下，算法仅需要一次数组遍历就可以确定排序完成。</p>
<p>即：</p>
<p>比较次数： (n - 1)</p>
<p>交换次数： 0</p>
<p><strong>所以最佳情况下的时间复杂度是 <span
class="math inline">\(O(n)\)</span>。</strong></p>
<p>最坏情况：</p>
<p>在最坏的情况下（当数组完全逆序时），冒泡排序第一轮需要执行( n - 1)
次比较和交换，第二次执行( n - 2 )次，依此类推，直到最后一次执行 1
次。所以，总的执行次数是等差数列求和：n * ( n - 1) /
2。所以<strong>最坏情况下的时间复杂度是<span
class="math inline">\(O(n^2)\)</span> 。</strong></p>
<p>平均情况：<strong>在平均情况下，时间复杂度也是<span
class="math inline">\(O(n^2)\)</span>
。</strong>虽然不是所有的元素都需要交换，但总体而言，性能接近最坏情况。</p>
<p><strong>空间复杂度分析：</strong></p>
<p>冒泡排序是一种原地排序算法，不需要占用额外内存空间。空间复杂度是<span
class="math inline">\(O(1)\)</span>。</p>
<p><strong>稳定性分析：</strong></p>
<p>冒泡排序显然是一种稳定的排序算法，因为交换的过程中不会交换任何两个相同的元素。</p>
<hr />
<h2 id="选择排序">选择排序</h2>
<h3 id="算法思想-1">算法思想</h3>
<p>①先假设第一个数是最小的，然后拿这个数去和后面的n-1个数依次进行比较；</p>
<p>②如果发现有比最小值还小的数，则通过中间变量记录下下标和值；</p>
<p>③然后继续拿这个最小值往后进行比较，如果又发现比最小值还小的值，就执行第②步,直到比完最后一个元素；</p>
<p>④最后把存在中间变量的最小值和最小值的下标
和假设的那个最小值进行交换。</p>
<h3 id="代码实现-1">代码实现</h3>
<details>
<summary>
<font size="4" color="orange">选择排序算法C语言实现</font>
</summary>
<pre><code class="language-c">
#include &lt;stdio.h&gt;
#define ARR_LEN(arr) (sizeof(arr) / sizeof(arr[0]))
#define SWAP(arr, i, j ) {      \
    int tmp = arr[i];           \
    arr[i] = arr[j];            \
    arr[j] = tmp;               \
}
void print_arr(int arr[], int len) {
    for (int i = 0; i < len; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}
// 选择排序
void selection_sort(int arr[], int len) {
    /*
    * i表示未排序序列的开头元素
    * 最后一轮选择排序时, 未排序序列的开头元素是数组倒数第二个元素
    * i的每个取值都表示一轮选择排序
    * 也就是选择排序一共执行9趟
    */
    for (int i = 0; i < len - 1; i++) {
        // 不妨直接假设未排序序列的开头i位置元素就是最小值
        int min_index = i;
        // 遍历未排序数组序列,找出真正的最小值下标,此时应遍历最后一个元素
        for (int j = i + 1; j < len; j++) {
            if (arr[j] < arr[min_index]) {
                min_index = j;  // 记录较小值的下标
            }
        }   // for循环结束时，未排序序列的最小值下标就是min_index
        // 交换min_index和下标i的元素
        SWAP(arr, min_index, i);
        // 选择排序一趟打印一次数组
        print_arr(arr, len);
    }
}
int main(void) {
    // 测试选择排序
    int arr[] = { 1,10,2,5,3,4,5,6,3,2 };
    int len = ARR_LEN(arr);
    selection_sort(arr, len);
    return 0;
}</code>
</pre>
</details>
<h3 id="性能分析-1">性能分析</h3>
<p><strong>时间复杂度分析：</strong></p>
<p>在分析以比较为核心的排序算法的时间复杂度时，重点关注两点：</p>
<ol type="1">
<li>比较次数</li>
<li>交换元素次数</li>
</ol>
<p>在选择排序当中，无论什么情况下，比较次数和交换元素次数都不变：</p>
<ol type="1">
<li>比较次数：每一轮的比较次数会随着未排序序列减少递减，即一共比较(n -
1) + (n - 2) + … + 1 = n * (n - 1) / 2 次（等差数列求和）</li>
<li>交换元素次数：数组一共有n个元素，除了末尾元素不需要交换，其它元素都需要交换，固定交换(n
- 1)次。</li>
</ol>
<p>将两者加起来就是总的指令执行数量。</p>
<p>除此之外，选择排序，在数组已经有序、逆序还是完全无序的各种情况下，这些比较和交换都不会减少。</p>
<p>所以用大O表示法，<strong>选择排序的时间复杂度是<span
class="math inline">\(O(n^2)\)</span>，任何情况下都一样。</strong></p>
<p><strong>空间复杂度分析：</strong></p>
<p>选择排序是一种原地排序算法，不需要占用额外内存空间。空间复杂度是<span
class="math inline">\(O(1)\)</span>。</p>
<p><strong>稳定性分析：</strong></p>
<p>选择排序稳定吗？答：不稳定！</p>
<p>选择排序在每轮中选择最小元素，并与未排序部分的第一个元素交换。如果存在相等的元素，选择排序可能会改变它们的相对顺序。所以选择排序不是稳定的排序算法。</p>
<p>这样的例子随手可举，比如对于一个数组：</p>
<blockquote>
<p>[3, 5, 3’, 2, 8]</p>
</blockquote>
<p>两个3是相同的元素，相对位置是3在前，3’在后。在选择排序后，得出结果是：</p>
<blockquote>
<p>[2, 3’, 3 , 5, 8]</p>
</blockquote>
<p>变成3’在前，3在后。</p>
<hr />
<h2 id="插入排序">插入排序</h2>
<h3 id="算法思想-2">算法思想</h3>
<ol type="1">
<li><strong>以数组的首元素为初始状态：</strong>这个初始状态相当于抓到的第一张牌，它默认就是有序的。</li>
<li><strong>从数组的第二个元素开始遍历：</strong>相当于抓一张牌，然后从小到大整理手牌。</li>
<li><strong>比较与交换：</strong>将新插入的元素和前面的元素逐一比较，如果新插入元素较小，则交换两个元素，直到完全不可交换，则完成一轮排序。</li>
<li><strong>重复步骤2和3，直到步骤2遍历到最后一个元素。</strong></li>
</ol>
<h3 id="代码实现-2">代码实现</h3>
<details>
<summary>
<font size="4" color="orange">插入排序算法C语言实现</font>
</summary>
<pre><code class="language-c">
#include &lt;stdio.h&gt;
#define ARR_LEN(arr) (sizeof(arr) / sizeof(arr[0]))
#define SWAP(arr, i, j ) {      \
    int tmp = arr[i];           \
    arr[i] = arr[j];            \
    arr[j] = tmp;               \
}
void print_arr(int arr[], int len) {
    for (int i = 0; i < len; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}
// 插入排序
void insert_sort(int arr[], int len) {
    for (int i = 1; i < len; i++)
    {
        for (int j = i - 1; j >= 0; j--) {
            if (arr[j] > arr[j + 1])
                SWAP(arr, j, j + 1);
            else
                break;
        }
        // 打印一轮插入排序后数组的元素排列
        print_arr(arr, len);
    }
}
// 插入排序 优化: 用向后移动腾出插入位置,然后插入实现插入排序
void insertion_sort2(int arr[], int len) {
    // 现在第一个元素就是第一张手牌,从第二个元素开始就是每一次要摸的牌
    // 外层for循环代表每一轮摸到的新手牌, 也就是每一轮插入排序
    for (int i = 1; i < len; i++) {
        // 先记录一下新手牌的值, 便于后续的插入操作
        int tmp = arr[i];
        int j = i - 1;
        for (; j >= 0; j--) {
            if (arr[j] > tmp) { // 注意:不能加=,加了就不是稳定排序算法了
                arr[j + 1] = arr[j];    // 将旧手牌中大于新手牌的所有牌都向后移
            }
            else
                break;  // 只要发现一张旧手牌更小或相等, 就说明已经找到新手牌的插入位置了
        }
        /*
            现在还有一件事情没做:新手牌要插入,需要确定插入位置
            分析: for循环什么时候结束?
            两种情况:
            1.j=-1时,循环结束,说明新手牌是最小的,所以插入到0这个位置,也就是j+1
            2.arr[j] <= tmp 也就是旧手牌更小或相等,此时新手牌放在j+1的位置
        */
        arr[j + 1] = tmp;
        print_arr(arr, len);    // 每一轮摸牌后查看排序后的数组
    }
}
int main(void) {
    // 测试插入排序
    int arr[] = { 1,10,2,5,3,4,5,6,3,2 };
    int len = ARR_LEN(arr);
    //insert_sort(arr, len);
    insert_sort2(arr, len);
    return 0;
}</code>
</pre>
</details>
<h3 id="性能分析-2">性能分析</h3>
<p>以版本2为选择排序的实现，假设n为数组的长度，分析以下复杂度：</p>
<p><strong>时间复杂度分析：</strong></p>
<p>最佳情况：</p>
<ol type="1">
<li>当输入的数组已经是排序好的，此时不需要移动任何元素，但比较还是需要的。</li>
<li>比较次数(外层for循环次数)：(n - 1)</li>
<li>移动次数：0</li>
</ol>
<p><strong>所以最佳情况下的时间复杂度是 <span
class="math inline">\(O(n)\)</span>。</strong></p>
<p>最坏情况：</p>
<ol type="1">
<li>在最坏的情况下，即数组完全逆序时。</li>
<li>比较和交换的次数是相同的，第一轮需要比较和移动1次，第二轮需要比较和移动2次….最后一个元素需要比较和移动
(n - 1)次。</li>
<li>等比数列求和：需要比较和移动各n(n-1)/2次，加起来就是<strong><span
class="math inline">\(O(n^2 - n)\)</span></strong>。</li>
</ol>
<p>所以<strong>最坏情况下的时间复杂度是$ O(n^2)$。</strong></p>
<p>平均情况：</p>
<ol type="1">
<li>在平均情况下，可以粗略的认为移动次数和比较次数是最坏情况除以2；</li>
<li><strong>所以在平均情况下，时间复杂度也是$ O(n^2)$。</strong></li>
</ol>
<p><strong>空间复杂度分析：</strong></p>
<p>插入排序是一种原地排序算法，不需要占用额外内存空间。空间复杂度是<span
class="math inline">\(O(1)\)</span>。</p>
<p><strong>稳定性分析：</strong></p>
<p>插入排序显然也是一种稳定的排序算法，因为对于两个相同的元素，我们始终都不会交换它们的相对位置。</p>
<p>注意：如果判断的条件改成<code>arr[j] &gt;= arr[j + 1]</code>，即在前后元素相等时也交换/移动元素，算法就会变成不稳定的。</p>
<hr />
<h2 id="基础排序算法总结">基础排序算法总结</h2>
<p><strong>选择排序、冒泡排序和插入排序都是基础的排序算法，同样的基于比较而实现，同样的简单直观，同样的原地算法。</strong></p>
<p>这里对它们做一个总结分析：</p>
<ol type="1">
<li><strong>选择排序</strong>在任何情况下时间复杂度都是<span
class="math inline">\(O(n^2)\)</span>，且它是一个不稳定的排序算法。比起作为算法去使用，它更具有教学意义，一般不推荐去使用它。</li>
<li><strong>冒泡排序</strong>和<strong>插入排序</strong>看起来很类似，它们都有最坏和平均情况下<span
class="math inline">\(O(n^2)\)</span>的时间复杂度，都有最优情况下<span
class="math inline">\(O(n)\)</span>的时间复杂度。但<strong>在小数据集或者数据集基本有序的情况下，仍然更推荐使用插入排序。</strong></li>
</ol>
<p>这是因为：</p>
<ol type="1">
<li><strong>插入排序的移动操作比冒泡排序的交换操作效率更高。</strong>在冒泡排序中，每轮冒泡都可能涉及大量交换元素操作，而插入排序用移动操作替代了交换。从指令执行数量上来说，移动操作效率更高。</li>
<li><strong>在数据基本有序的情况下，插入排序往往表现要更好。</strong>这是因为在数据基本有序的情况下，插入排序往往只需要挪动元素很少的次数，而冒泡排序可能还需要，慢慢的一步步的两两交换多次。这里同样存在移动操作和交换操作指令数量上的差异！</li>
</ol>
<hr />
<h2 id="希尔排序">希尔排序</h2>
<h3 id="算法思想-3">算法思想</h3>
<p><strong>希尔排序(Shell
Sort)</strong>是美国计算机科学家，<strong>唐纳德·希尔</strong>在1959年提出的排序算法。</p>
<p>希尔排序是插入排序的一种改进和扩展，它基于插入排序的两个特点来进行改进：</p>
<ol type="1">
<li>插入排序的缺点就是当较小元素处于末尾时，需要移动元素的次数过多，希尔排序通过比较和交换不相邻的元素来改进插入排序，可以一次性将后面的小元素向前面移动多个位置，从而提高效率。</li>
<li>插入排序在排序基本有序的序列时，效率很高。<strong>可以将希尔排序理解成——先通过几次分组的、较小的组间插入排序将原数组变得有序，最后再进行一次序列基本有序的完整插入排序。</strong></li>
</ol>
<p>整个希尔排序的过程，可以看成多个人轮流抓牌 –&gt; 整理手牌 –&gt;
抓牌，然后再把手牌合并变成更少的人抓牌，直到回归一个人抓牌(直到变成插入排序)。</p>
<h3 id="代码实现-3">代码实现</h3>
<details>
<summary>
<font size="4" color="orange">希尔排序算法C语言实现</font>
</summary>
<pre><code class="language-c">
#include &lt;stdio.h&gt;
#define ARR_LEN(arr) (sizeof(arr) / sizeof(arr[0]))
#define SWAP(arr, i, j ) {      \
    int tmp = arr[i];           \
    arr[i] = arr[j];            \
    arr[j] = tmp;               \
}
void print_arr(int arr[], int len) {
    for (int i = 0; i < len; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}
// 希尔排序: 缩小增量排序, 其实就是多人摸牌, 逐渐减少摸牌人数
// 希尔排序中, 增量序列的设计非常重要,这里采取简单的gap序列: 长度减半..一直减半,直到为1
// gap为1时就是一个在数组元素基本有序情况下的,插入排序
void shell_sort(int arr[], int len) {
    // 第一个元素是第一个人的初始手牌,一直到第gap个元素都是初始手牌
    int gap = len >> 1;
    while (gap > 0) {
        // 外层for的i仍然代表新摸到的手牌的下标,i从gap开始,直到摸完整个数组元素
        for (int i = gap; i < len; i++) {
            // 先记录一下新手牌的值, 便于后续的插入操作
            int tmp = arr[i];
            int j = i - gap;    // 代表每一个人旧手牌的最后一张牌
            for (; j >= 0; j -= gap) {
                // 内层for代表 每个人每摸到一张新手牌,都会和原本的旧手牌比较,但由于gap存在,所以需要减去gap
                if (arr[j] > tmp) { // 注意:不能加=,加了就不稳定了
                    arr[j + gap] = arr[j];  // 将旧手牌中大于新手牌的所有牌都向后移
                }
                else
                    break;  // 只要发现一张旧手牌更小或相等, 就说明已经找到新手牌的插入位置了
            }
            arr[j + gap] = tmp;
        }
        print_arr(arr, len);    // 每一轮希尔排序后查看数组排序结果
        gap >>= 1; // 每一轮希尔排序,增量都减半
    }
}
int main(void) {
    // 测试希尔排序
    int arr[] = { 16, 1, 45, 23, 99, 2, 18, 67, 42, 10 };
    int len = ARR_LEN(arr);
    shell_sort(arr, len);
    return 0;
}</code>
</pre>
</details>
<h3 id="性能分析-3">性能分析</h3>
<p><strong>时间复杂度：</strong></p>
<p>希尔排序的时间复杂度，和选择的增量序列有密切的关联：</p>
<p>若使用希尔本人提出的减半序列，时间复杂度通常会小于<span
class="math inline">\(O(n^2)\)</span>，但在最坏情况也会接近<span
class="math inline">\(O(n^2)\)</span>。</p>
<p><strong>空间复杂度分析：</strong></p>
<p>希尔排序是一种原地排序算法，不需要占用额外内存空间。空间复杂度是<span
class="math inline">\(O(1)\)</span>。</p>
<p><strong>稳定性分析：</strong></p>
<p>希尔排序显然不是一种稳定的排序算法，因为它先分组再插入排序的方式，使得相同元素可能会由于分组不同改变位置。</p>
<p>比如：</p>
<p>假设我们有一个数组 [4a, 3, 4b, 2, 1]，选择的增量是3…1</p>
<p>那么第一轮希尔排序后的结果就是：[2, 1, 4b, 4a,
3]，很明显这不是稳定的排序算法。</p>
<blockquote>
<p><strong>希尔排序的地位如何？</strong></p>
<p>希尔排序的实现比起插入排序更加复杂，思路更加不直观，并且还牺牲了算法的稳定性，从而换取了一定的性能提升。</p>
<p>但总得来说，希尔排序处在一共尴尬的境地：在小数据集的情况下，插入排序的性能和希尔排序相当甚至会更优。</p>
<p>在大数据集的情况下，希尔排序的性能又不及归并、快排等高级排序算法。希尔排序更像是一个介于简单排序算法和高级排序算法之间的一个桥梁，不需要作为重点去学习。</p>
</blockquote>
<hr />
<h2 id="归并排序">归并排序</h2>
<h3 id="算法思想-4">算法思想</h3>
<p>归并排序的分治策略思路大体上如下：</p>
<ol type="1">
<li>分解大问题：将一个大数组分解成两个或更多的子数组，直到每个子数组足够小，通常是直到每个子数组只包含一个元素或者是空数组。</li>
<li>解决子问题：数组小到只有一个元素或者没有元素，那自然是”有序数组”，所以这些子问题可以视为被解决了。</li>
<li>合并：归并排序的核心在于合并步骤，也可以叫”归并”操作，它会将两个有序的子数组合并成一个大的有序数组。这个过程通常需要反复比较元素，比较复杂。</li>
</ol>
<p><strong>分解过程：</strong></p>
<p>可以采用递归作为手段来实现分治的过程。首先存在一个递归分解的过程，递归的思路参考如下：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202312151131058.png" alt="归并排序-递归过程" style="zoom: 50%;" />
</div>
<p>那么这个递归的递归体和递归的出口是什么呢？</p>
<p>很明显，递归分解的过程会不停地将大数组分解成两个小的子数组，这个分解的过程会根据大数组的左右界限求一个中间索引，然后将大数组尽量等分为两份。所以，递归分解的函数，至少需要三个参数：</p>
<ol type="1">
<li>递归分解的数组arr</li>
<li>数组分解的左下标left</li>
<li>数组分解的右下标right</li>
</ol>
<p>此递归分解的函数会将arr数组的[left, right]区间分解成两个小数组。</p>
<p><strong>于是递归的出口就很明显了是：left &gt;=
right，这表示子数组缩小到只包含一个元素或没有元素时，递归将停止。</strong></p>
<p>在计算中索引时，我们将采用一种优化的方案：</p>
<ol type="1">
<li>一般情况下，可以直接使用 <code>(left + right) &gt;&gt; 1</code>
来直接求中间索引。</li>
<li>但C语言中int类型可能只占2个字节，此时int类型取值范围较小，上面的表达式可能会出现数据溢出失真。为避免这种情况发生，我们可以采用表达式<code>left + (right - left &gt;&gt; 1)</code>去求中间索引。</li>
</ol>
<p><strong>合并过程：</strong></p>
<p>合并操作的思路是：</p>
<ol type="1">
<li>从左到右轮流比较待合并子数组中的元素，把比较过程中的较小元素存入临时数组中，直到某个子数组元素为空。</li>
<li>然后再将存在剩余元素的子数组中的所有元素，轮流放入临时数组中。</li>
<li>最后把临时数组中的元素，复制回原数组。</li>
</ol>
<p>注：临时数组的长度和原数组是一致的，且合并过程共有同一套下标索引。</p>
<h3 id="代码实现-4">代码实现</h3>
<details>
<summary>
<font size="4" color="orange">归并排序算法C语言实现</font>
</summary>
<pre><code class="language-c">
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#define ARR_LEN(arr) (sizeof(arr) / sizeof(arr[0]))
// 打印数组的函数
void print_arr(int arr[], int left, int right) {
    for (int i = left; i <= right; i++)
        printf("%d ", arr[i]);
    printf("\n");
}
/*
* 合并的思路:
* 1.把左右子数组中元素按照顺序合并到临时数组中,过程类似"穿针引线"
* 2.将排好序的临时数组元素按照下标赋值给原数组
* 注：临时数组和原数组共有一套下标
* 传入函数逻辑上的左右子数组是有序的，相当于合并两个有序的左右子数组
*/
static void merge(int arr[], int left, int mid, int right, int* temp) {
    /*
    * tmp_idx: 用于存放合并结果的临时数组的开始下标
    * left_idx: 左子数组的开始下标
    * right_idx: 右子数组的开始下标
    */
    int temp_idx = left, left_idx = left, right_idx = mid + 1;
    // 只要左右子数组同时还有元素
    while (left_idx <= mid && right_idx <= right) {
        // 逐对比较左右子数组的元素, 按照从小到大放入临时数组
        // <=判断不会改变相同元素的相对位置,是稳定算法。反之则不是稳定算法
        if (arr[left_idx]<=arr[right_idx])
        {
            temp[temp_idx++] = arr[left_idx++];
        }
        else
        {
            temp[temp_idx++] = arr[right_idx++];
        }
    }
    // while结束时，左右子数组必然有一个没有元素了,此时另一个数组必然还有元素
    // 也就是说只会有一个数组是空的
    // 但我们无法确定是哪个数组没有元素了
    // 所以我们都判断一下将左右子数组还剩余的元素取出来
    while (left_idx <= mid) {
        temp[temp_idx++] = arr[left_idx++];
    }
    while (right_idx <= right) {
        temp[temp_idx++] = arr[right_idx++];
    }
    // 将临时数组中已排序好的元素复制到原始数组中
    for (int i = left; i <= right; i++)
        arr[i] = temp[i];
    // 打印此一轮归并排序的元素
    print_arr(arr, left, right);
}
/*
* 辅助函数,实现对[left, right]范围内的数组递归分解合并
* left表示递归分解的区间起点，right表示递归分解区间的终点，是一个闭区间
* 递归分解的思路是:
* 对[left, right]区间元素的排序,可以分解成:
* [left, mid]区间,和[mid + 1, right]区间的排序合并
* 递归的出口是:
* 如果区间仅有一个元素或没有元素,递归结束
*/
static void divide_merge(int arr[], int left, int right, int* temp) {
    // 递归的出口
    if (left >= right)
        return;
    // 递归体
    // 计算中间索引
    int mid = left + (right - left >> 1);
    divide_merge(arr, left, mid, temp);
    divide_merge(arr, mid + 1, right, temp);
    /*
     * 归并，归并排序的核心操作
     * 需要一个临时数组完成此操作
     * 这个临时数组至少要和原先的数组一般大
     * 有两种方案：
     * 1.用全局变量数组或局部变量,该方式简洁易实现,无需考虑内存回收
     *   但缺点是
     *   a.必须编译时期确定数组长度,无法运行时期动态分配
     *   b.栈区和数据段都无法创建长数组,在大数据集下容易产生溢出错误
     * 为了解决这两个缺点，可以在堆上动态分配数组
     *   但同样也有缺点:
     *   a.内存管理风险
     *   b.动态分配数组会带来额外性能开销
    */
    merge(arr, left, mid, right, temp);
}
// 归并排序
void merge_sort(int arr[], int len) {
    int* temp = calloc(len, sizeof(int));
    if (temp == NULL)
    {
        printf("Error: malloc failed in merge_sort.\n");
        exit(1);
    }
    // 将整个数组进行递归分解合并,即完成归并排序
    divide_merge(arr, 0, len - 1, temp);
    // 不要忘记free释放资源
    free(temp);
}
int main(void) {
    // 测试归并排序
    int arr[] = { 8, 3, 2, 6, 9, 7, 1, 0, 4, 5 };
    int len = ARR_LEN(arr);
    merge_sort(arr, len);
    return 0;
}</code>
</pre>
</details>
<h3 id="性能分析-4">性能分析</h3>
<p><strong>时间复杂度：</strong></p>
<p>无论原始数组处在什么状态，归并排序都会按照既定步骤分解、合并。所以<strong>在最好，最坏，平均情况下，归并排序的时间复杂度都是一样的，都是<span
class="math inline">\(O(n \log n)\)</span>。</strong></p>
<p>归并排序的时间复杂度分析需要考虑它的两个主要操作，分解和合并：</p>
<ol type="1">
<li>分解过程也就是递归调用的过程，这个过程大概分解了<span
class="math inline">\(\log_{2}n\)</span>次（每次都将数组折半，也就是递归的深度）</li>
<li>在合并的过程中，需要遍历并比较子数组的元素，然后将它们按顺序复制回原数组。每次合并操作的时间复杂度都是<span
class="math inline">\(O(n)\)</span>，因为它涉及到整个数组的遍历。合并的次数和分解的次数是一样的，都是<span
class="math inline">\(\log_{2}n\)</span>次，所以对于合并操作，总的时间复杂度是<span
class="math inline">\(O(n\log n)\)</span>。</li>
</ol>
<p>综合起来，时间复杂度就是<span class="math inline">\(O(n\log
n)\)</span>。</p>
<p><strong>空间复杂度：</strong></p>
<p>归并排序显然不是一个原地算法。它需要额外的内存空间：</p>
<ol type="1">
<li>需要一个与原始数组大小相同的，长度是<span
class="math inline">\(n\)</span>的辅助数组来进行合并操作。</li>
<li>递归调用，占用额外的栈空间。因为每次递归调用都会将数组分为两个大致相等的部分，所以每次都将问题的规模减半。递归深度大致是<span
class="math inline">\(\log_{2}{n}\)</span>。</li>
</ol>
<p><strong>所以空间复杂度是<span
class="math inline">\(O(n)\)</span>。</strong></p>
<p><strong>稳定性：</strong></p>
<p><strong>归并排序是稳定的排序算法。</strong>这是因为如果两个元素相等，归并排序不会改变它们的相对顺序。</p>
<hr />
<h2 id="快速排序">快速排序</h2>
<p><strong>快速排序(Quick
Sort)</strong>是一种建立在<strong>分区(partition)操作</strong>上的，非常高效的排序算法。快速排序同样是分治思想的典型应用，由于其优秀的性能和较为简单的实现，它通常是实际应用中，<strong>大数据集下</strong>首选的排序算法。</p>
<p>通过分区，快速排序实现了对数组的分解，大体思路是：</p>
<ol type="1">
<li>分解大问题：挑选一个基准值(pivot)，然后通过一轮遍历将数组分成两个子数组：一个包含所有小于等于基准的元素，另一个包含所有大于基准的元素。这个过程就是分区操作，该过程结束后，基准值一定位于其排序最终位置上。</li>
<li>解决子问题：递归地对两个子数组进行快速排序。由于这两个子数组都比原始数组小，这就是解决子问题的过程。如果子数组足够小，可以直接认定它就是有序的。</li>
<li>合并：快速排序的操作都是在原数组上直接进行的，所以快速排序的合并步骤实际上并不需要做任何操作，只有解决子问题，原数组就是直接排好序的。</li>
</ol>
<p>在具体到实现时，快速排序根据分区操作的不同，可以分为两种：</p>
<ol type="1">
<li>单向分区</li>
<li>双向分区</li>
</ol>
<p>其中双向分区泛用性最强，也最常用。</p>
<h3 id="单向分区">单向分区</h3>
<p><strong>所谓单向分区，指的是快速排序算法在分区的过程中，元素比较和交换的操作是单向进行的，也就是从数组的一端进行到另外一端。</strong></p>
<p>单向分区快速排序算法，具体而言，它的思路是：</p>
<ol type="1">
<li>选择一个基准值（pivot），可以是随机选择，也可以是直接选首尾元素。选定基准值后，一般会将pivot交换到数组末尾，这样做可以简化分区操作。</li>
<li>设置一个索引(比如叫idx)来追踪小于基准值的元素应该插入的位置，一开始idx索引指向数组的首元素。</li>
<li><strong>遍历数组进行分区操作：</strong>
<ol type="1">
<li>从数组首元素开始遍历整个数组</li>
<li>如果元素小于基准值，则将该元素与idx位置的元素交换，idx索引加1。</li>
<li>如果元素大于或等于基准值，则不做任何操作，继续遍历下一个元素。</li>
</ol></li>
<li>当遍历到最后一个元素，也就是pivot时，遍历结束：
<ol type="1">
<li>最后将pivot元素和此时idx索引元素进行交换，完成这一轮分区操作。</li>
<li>此时pivot左侧的元素一定都是小于基准值的。</li>
<li>pivot右侧的元素一定都是大于等于基准值的。</li>
</ol></li>
<li>对基准值左右两边的子数组递归地执行以上步骤，直到每个子数组的大小减少到1或0，此时数组就被完全排序了。</li>
</ol>
<h3 id="双向分区">双向分区</h3>
<p>比起单向分区，双向分区是更常用的快排分区策略，<strong>一般而言当提起快速排序，指的都是双向分区策略的快速排序。</strong></p>
<p><strong>所谓双向分区，指的是在分区过程中，元素比较和交换操作的方向是，同时从数组的两端向中间逼近的。</strong></p>
<p>双向分区快速排序算法，具体而言，它的思路是：</p>
<ol type="1">
<li><p>选择基准值pivot，基准值可以是一个随机元素，也可以选择一个固定元素。然后将基准值元素和首元素交换，这样做的目的是为了将交换元素操作优化成一个赋值操作。<strong>并且要将基准值存储起来。</strong></p></li>
<li><p>设置两个索引 low 和 high ：</p>
<ul>
<li><p>索引 low
一开始指向数组首元素，它的含义是指示小于基准值的元素应该置于的位置。</p></li>
<li><p>索引 high
一开始指向数组尾元素，它的含义是指示大于等于基准值的元素应该置于的位置。</p></li>
</ul></li>
<li><p>率先移动索引high，它从尾元素开始向左移动，目标是找到第一个小于基准值的元素：</p>
<ul>
<li><p>找到该元素后，直接将该元素赋值给low索引位置，也就是覆盖掉基准值。</p></li>
<li><p><strong>赋值结束后，low索引和high索引都不需要移动。</strong></p></li>
</ul></li>
<li><p>然后向右移动索引 low，找到第一个大于等于基准值的元素：</p>
<ul>
<li>找到该元素后，直接将该元素赋值给high索引位置</li>
<li><strong>赋值结束后，low索引和high索引都不需要移动。</strong></li>
</ul></li>
<li><p>重复过程3和4，直到索引high和low相遇。最后将基准值放入它们相遇的位置。</p></li>
<li><p>于是分区就结束了，基准值到达了排序的最终位置，基准值左边都是小于基准值的元素，右边都是大于等于基准值的元素。</p></li>
<li><p>对基准值左右两边的子数组递归地执行以上步骤，直到每个子数组的大小减少到1或0，此时数组就被完全排序了。</p></li>
</ol>
<h3 id="代码实现-5">代码实现</h3>
<details>
<summary>
<font size="4" color="orange">快速排序算法单向分区C语言实现</font>
</summary>
<pre><code class="language-c">#include &lt;stdio.h&gt;
#include &lt;time.h&gt;
#include &lt;stdlib.h&gt;
#define ARR_SIZE(arr) (sizeof(arr) / sizeof(arr[0]))
#define SWAP(arr, i, j) {   \
    int tmp = arr[i];       \
    arr[i] = arr[j];        \
    arr[j] = tmp;           \
}
// 打印数组的函数
void print_arr(int arr[], int left, int right) {
    for (int i = left; i <= right; i++)
        printf("%d ", arr[i]);
    printf("\n");
}
// 分区核心操作实现,返回一轮快排选择的pivot的下标
int partition(int arr[], int left, int right) {
    // 1.随机选择一个基准值,然后把它先放到数组末尾
    int pivot_idx = left + rand() % (right - left + 1); // 得到一个[left, right]范围内的随机索引
    int pivot = arr[pivot_idx];
    SWAP(arr, pivot_idx, right);
    // 2.设置一个partition_idx索引,指示小于pivot的元素应该插入的位置
    // 同时该索引最终表示分区的界限索引,所以命名为partition_idx
    int partition_idx = left;
    // 3.遍历整个数组,当元素小于pivot时,将它和partition_idx位置元素交换,partition_idx加1
    // 希望遍历结束时，i指向数组末尾的pivot，所以i < right
    for (int i = left; i < right; i++) {
        if (arr[i] < pivot) {
            SWAP(arr, i, partition_idx);
            partition_idx++;
        }
    }
    // 4.遍历结束后,将pivot元素(最后一个元素)交换到partition_idx位置
    SWAP(arr, right, partition_idx);
    printf("此一轮分区操作,选择的pivot是: %d\n分区结束后的数组是: ", pivot);
    print_arr(arr, left, right);
    // 5.返回基准值的位置索引
    return partition_idx;
}
/*
* 辅助函数
* 用于对[left, right]区间中的元素进行递归分区操作
*/
void partition_recursion(int arr[], int left, int right) {
    // 递归出口
    if (left >= right) {
        return;
    }
    // 递归体
    int idx = partition(arr, left, right);  // 分区操作,找到pivot元素的下标位置
    partition_recursion(arr, left, idx - 1);
    partition_recursion(arr, idx + 1, right);
}
void quick_sort_one_way(int arr[], int len) {
    // 初始化随机数生成器,time(NULL)获取当前时间戳
    // 用于生成随机索引
    srand(time(NULL));
    // 调用辅助函数进行递归分解
    partition_recursion(arr, 0, len - 1);
}
int main(void) {
    // 测试单向分区快速排序
    int arr[] = { 8,3,2,6,9,5 };
    int len = ARR_SIZE(arr);
    quick_sort_one_way(arr, len);
    return 0;
}</code>
</pre>
</details>
<details>
<summary>
<font size="4" color="orange">快速排序算法双向分区C语言实现</font>
</summary>
<pre><code class="language-c">#include &lt;stdio.h&gt;
#define ARR_SIZE(arr) (sizeof(arr) / sizeof(arr[0]))
// 打印数组的函数
void print_arr(int arr[], int left, int right) {
    for (int i = left; i <= right; i++)
        printf("%d ", arr[i]);
    printf("\n");
}
// 快速排序的核心操作: 双向分区, 也就是确定pivot的最终位置
// 挑选一个基准值,通过双向分区操作,决定最终的位置,最终位置就是基准值排好序的位置
static int partition(int arr[], int left, int right) {
    // 1.为了简化实现,直接挑选首元素为基准值(因为基准值要交换到开头,所以直接挑选首元素作为基准值,可以减少一步交换)
    int pivot = arr[left];
    // 2.初始化两个索引low和high,分别指向数组两端
    int low = left, high = right;
    // 3.循环遍历这个数组区间
    while (low < high) {    // 两个索引没有相遇就继续循环
        // 在两个索引没有相遇的情况下,high索引用于寻找比基准值小的元素
        while (low < high && arr[high] >= pivot) {
            high--;
        }   // while循环结束时,要么两个索引相遇了,要么high索引已经找到了一个比基准值小的元素
        arr[low] = arr[high];   // 将这个比基准值小的元素覆盖到low位置
        //low++;    该行语句不能加，因为若此时两个索引相遇结束while，low++将导致相遇的索引不再相遇
        //
        // 在两个索引没有相遇的情况下,low索引用于寻找比基准值大和相等的元素
        while (low < high && arr[low] < pivot) {
            low++;
        }   // while循环结束时,要么两个索引相遇了,要么low索引已经找到了一个比基准值大或相等的元素
        arr[high] = arr[low];   // 将这个比基准值大或相等的元素覆盖到high位置
        //high--;   该行语句不能加，因为若此时两个索引相遇结束while，high--将导致相遇的索引不再相遇
        //
    }   // while循环结束时,说明low和high索引相遇,此时该位置就是pivot应该放置的位置
    arr[low] = pivot;
    printf("此一轮分区操作选择的pivot = %d\n", pivot);
    print_arr(arr, left, right);
    return low;
}
// 对[left, right]区间进行递归分区操作
void partition_recursion(int arr[], int left, int right) {
    // 递归出口
    if (left >= right)
        return;
    // 递归体
    int idx = partition(arr, left, right);  // 分区操作,找到pivot下标位置
    partition_recursion(arr, left, idx - 1);
    partition_recursion(arr, idx + 1, right);
}
void quick_sort_two_way(int arr[], int len) {
    partition_recursion(arr, 0, len - 1);
}
int main(void) {
    int arr[] = { 8,3,2,6,9,5 };
    int len = ARR_SIZE(arr);
    printf("Before quick sort, the array is: ");
    print_arr(arr, 0, len -1);
    // 测试双向分区-快速排序
    quick_sort_two_way(arr, len);
    printf("After quick sort, the array is: ");
    print_arr(arr, 0, len -1);
    return 0;
}</code>
</pre>
</details>
<h3 id="性能分析-5">性能分析</h3>
<p><strong>时间复杂度：</strong></p>
<p>一般提起快速排序算法，不管采取单双向哪一种分区策略，都只会说它的时间复杂度是<span
class="math inline">\(O(n\log n)\)</span>级别。</p>
<p><strong>空间复杂度：</strong></p>
<p>快速排序，无论什么分区策略，都不需要额外的数组空间来辅助排序，但考虑到需要使用递归来实现算法。</p>
<p><strong>在最佳和平均情况下，递归深度大约是<span
class="math inline">\(\log_{2}{n}\)</span>，空间复杂度是<span
class="math inline">\(O(\log n)\)</span></strong>。</p>
<p>但如果是在最坏情况下，递归深度接近<span
class="math inline">\(n\)</span>，此时空间复杂度为<span
class="math inline">\(O(n)\)</span>。</p>
<p><strong>稳定性：</strong></p>
<p>快速排序是一种不稳定的排序算法，无论选取什么分区方案，快速排序都有可能改变两个相同元素的相对位置。</p>
<hr />
<h2 id="堆排序">堆排序</h2>
<p><strong>堆排序(HeapSort)</strong>是一种高效的排序算法，它利用了堆这种数据结构来实现排序。</p>
<p>堆是一种特殊的完全二叉树，它满足以下性质：</p>
<ol type="1">
<li>大顶堆(最大堆)：每个父节点的值都大于或等于其子节点的值<strong>(递归定义)</strong>。这意味着树的根节点（堆顶）是所有节点中的最大值。</li>
<li>小顶堆(最小堆)：每个父节点的值都小于或等于其子节点的值<strong>(递归定义)</strong>。这意味着树的根节点（堆顶）是所有节点中的最小值。</li>
</ol>
<p>在实现堆排序时，一般选择使用大顶堆。</p>
<h3 id="算法思想-5">算法思想</h3>
<p>堆排序的过程可以分为两个主要步骤：</p>
<ol type="1">
<li>构建一个大顶堆</li>
<li>堆排序过程</li>
</ol>
<p>下面分别解释一下这两个过程：</p>
<h4 id="步骤一构建一个大顶堆">步骤一：构建一个大顶堆</h4>
<p>首先，堆排序基于一个大顶堆来实现排序并不意味着需要借助额外的数据结构，而是只需要把原数组在逻辑上看成是一个大顶堆即可。</p>
<p>在这个过程中，数组首元素(下标为0的元素)就是完全二叉树的根结点，而一个下标为i的元素，它的两个子结点是：</p>
<ol type="1">
<li><strong>2i + 1</strong></li>
<li><strong>2i + 2</strong></li>
</ol>
<p>比如：根结点元素下标为0，它的两个子结点就是下标为1和2的元素。而下标为1的元素，它的两个子结点就是下标为3和4的元素。</p>
<p>以数组[4, 10, 3, 5,
1]为例，这个数组可以构建下面一棵完全二叉树<strong>(还不是大顶堆，因为结点数据大小关系不对)</strong>：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202312121727197.png" alt="数组-构建完全二叉树" style="zoom: 50%;" />
</div>
<p>构建好这样一棵完全二叉树后，就需要调整元素的位置，使得它成为一个大顶堆。具体来说需要这么做：</p>
<p>从数组尾元素开始，找到第一个非叶子结点<strong>（因为叶子结点可以视为已经是大顶堆了）</strong>。遍历这个非叶子结点的左右子树，比较元素的大小关系，以决定是否需要调整元素位置。这个过程会自下而上的将一棵完全二叉树调整成为一个大顶堆，这个过程就是<strong>堆化(heapify)</strong>。</p>
<p><strong>注意：在堆化的过程中，一旦进行了子树结点和父节点的交换操作以后，就需要再判断以该子树结点为父节点的二叉树是否为大顶堆，从而决定是否再次进行调整！！！</strong></p>
<p>那么哪一个元素是这棵树的最后一个非叶子结点（从尾元素开始的第一个非叶子结点）？</p>
<p>我们可以假设最后一个非叶子结点的下标是<code>i</code>，它一定有左子树，且左子树的下标是<code>2i + 1</code>，这个下标不能超出数组的索引界限，假如数组长度是<code>len</code>，下标最大是<code>len-1</code>。</p>
<p>计算过程如下：</p>
<p><code>2i + 1 &lt;= len - 1</code>，即
<code>i &lt;= (len - 2) / 2</code></p>
<p>最后一个非叶子结点就是取等号的位置，也就是<code>(len - 2 ) / 2</code>索引位置，就是数组这棵树的最后一个非叶子结点。</p>
<p>比如在本案例中：</p>
<ol type="1">
<li>第一个非叶子结点是下标为1的元素10，遍历它的左右子树即下标为3和4的结点，发现符合大顶堆的定义。此父结点不需要调整。</li>
<li>第二个非叶子结点就是根结点，下标为0的元素4，遍历它的左右子树即下标为1和2的结点，发现左子树的10比4大，于是交换它们。这样交换后的数组就变成了：[10,
4, 3, 5, 1]。</li>
<li>但这样不算完，下标为0的父节点和下标为1的左子树交换了元素，所以就需要再次判断下标为1的左子树是否还符合大顶堆定义，此时发现已经不符合了。所以再次调整，交换元素4和5。此时被交换元素的结点已经是叶子结点了，没有左右子树，这样整个数组才完全构建成了一个大顶堆。数组堆化结束最终为：[10,
5, 3, 4, 1]</li>
</ol>
<p>经过上述调整，此时该数组就可以在逻辑上看成一个大顶堆。如下图所示：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202312122153960.png" alt="数组-构建大顶堆过程示意图" style="zoom: 50%;" />
</div>
<p>这样第一步，构建大顶堆就完成了。</p>
<h4 id="步骤二堆排序过程">步骤二：堆排序过程</h4>
<p>基于一个大顶堆，知道此时数组的首元素(也就是堆顶元素)，一定是此数组的最大值，那这就好办了。</p>
<p>所谓堆排序的过程，也就是通过不断将堆顶元素移动到数组末尾，再重构大顶堆(重新堆化)，再移动堆顶元素….重复的过程。</p>
<p>具体来说步骤就是：</p>
<ol type="1">
<li>将堆顶元素和堆末尾的元素交换，第一次也就是数组的首尾元素进行交换，这样数组的最大值就到了数组的末尾。于是数组就变成了[1,
5, 3, 4, 10]</li>
<li>将堆的逻辑大小减少1，数组就变成[1, 5, 3, 4]</li>
<li>重构大顶堆(重新堆化)。
<ol type="1">
<li>在上述过程中，数组末尾元素被去掉了，首元素是新赋值的。所以如果新数组逻辑上不是一个大顶堆，只可能是首元素违反了规则。</li>
<li>此时只需要比较首元素和左右子树结点的大小，然后进行调整就可以重新堆化。</li>
<li><strong>不要忘记上述过程如果出现了元素交换，那么就需要去判断被交换的左右子树是否还符合大顶堆的规则。</strong></li>
<li>在本案例中，首先需要将元素5和元素1交换，将1交换到左子树后发现左子树不再是一个大顶堆，于是就继续交换元素1和4。最终数组重新堆化，变成了[5,
4, 3, 1]</li>
</ol></li>
<li>重复过程1~3，直到堆的大小减少为1，堆排序结束。</li>
</ol>
<p>这一过程，可以用下图来描述：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202312122257230.png" alt="堆排序过程示意-图" style="zoom: 50%;" />
</div>
<p><strong>于是第二步堆排序过程就完成了，最终就在原地得到了一个排序好的数组。</strong></p>
<p>以上两个步骤合起来，就是一个完整的堆排序过程，下面就可以基于这个思路来实现堆排序。</p>
<h3 id="实现思路">实现思路</h3>
<p>上述堆排序算法在具体实现时，要分为两个步骤：</p>
<ol type="1">
<li>将待排序的原始数组，在逻辑上进行第一次堆化的操作；</li>
<li>将大顶堆的根结点元素移到数组末尾，交换首尾元素，逻辑上堆大小减1，以新的根结点进行堆化操作。</li>
</ol>
<p>这两个步骤中的核心操作逻辑都是——堆化。</p>
<p>于是我们把堆化这个操作，提取出一个函数，那么这个函数应该给什么参数呢？怎么写呢？</p>
<p>堆化的过程，其实就是自父结点开始，向下检查左右子树和这个父结点大小关系的过程：</p>
<ol type="1">
<li>如果左子树大于父结点，那么交换左子树和父结点；</li>
<li>如果右子树大于父结点，那么交换右子树和父结点；</li>
<li><strong>如果出现了交换，那么被交换的左子树或右子树就要重新进行堆化操作；</strong></li>
<li>如果根结点已经是最大值(相等的最大值也算)，没有交换，那么堆化结束。</li>
</ol>
<p>为什么这么设计堆化函数呢？为了让堆排序两个过程，都能复用这个函数：</p>
<ol type="1">
<li>在第一个步骤中，只需要从原数组的最后一个非叶子结点开始，将每一个非叶子结点作为父结点，逐一堆化。最终将根结点作为父结点传入，完成整个堆化过程。</li>
<li>在第二个步骤中，会将已生成的大顶堆的首尾元素交换，并将尾元素(最大值)移除，堆结点数量减1。此时如果这个堆不是大顶堆了，只可能是首元素(原本的尾元素)影响了大顶堆的性质。于是我们就以这个首元素为父结点，重新进行堆化。</li>
</ol>
<p>那么这个堆化函数的声明就应该如下所示：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">heapify</span><span class="params">(<span class="type">int</span> arr[], <span class="type">int</span> heap_len, <span class="type">int</span> idx)</span>;</span><br></pre></td></tr></table></figure>
<p>解释一下：</p>
<ol type="1">
<li><strong>arr参数：</strong>待堆化的原数组</li>
<li><strong>heap_len参数：</strong>逻辑上堆结点的数量。
<ol type="1">
<li>一开始它就是数组的长度</li>
<li>每一轮堆排序会去掉一个最大值，逐渐减少</li>
<li>直到堆的逻辑大小减少为1，堆排序就结束了，原数组排序完成。</li>
</ol></li>
<li><strong>idx参数：</strong>可能违反大顶堆规则的父结点的下标。</li>
</ol>
<p>把这个核心逻辑函数分析清楚了，代码实现就很容易了。</p>
<h3 id="代码实现-6">代码实现</h3>
<details>
<summary>
<font size="4" color="orange">堆排序算法C语言实现</font>
</summary>
<pre><code class="language-c">#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#define ARR_SIZE(arr) (sizeof(arr) / sizeof(arr[0]))
#define SWAP_ELEMENT(arr, i, j){    \
    int tmp = arr[i];       \
    arr[i] = arr[j];        \
    arr[j] = tmp;       \
}
void print_arr(int arr[], int n) {
    for (int i = 0; i < n; i++)
        printf("%d ", arr[i]);
    printf("\n");
}
// 该函数会把以root_idx索引元素为根结点的逻辑长度是tree_len的一棵完全二叉树arr，构建成一个大顶堆
static void heapify(int arr[], int tree_len, int root_idx) {
    /*
        堆化操作必然是需要循环来完成的
        如果对于某个循环,既不清楚循环的次数,循环结束的条件也不太好找到
        那么可以先写一个死循环, 然后具体到代码中再用break,return等结束循环
    */
    while (1) {
        // 根据根节点的下标,先计算出左右子树的下标
        int lchild_idx = (root_idx << 1) + 1;
        int rchild_idx = (root_idx << 1) + 2;
        int max_idx = root_idx;     // 先假设根节点就是最大值
        if (lchild_idx < tree_len && arr[lchild_idx] > arr[max_idx]) {
            // 如果左子树存在且左子树值比假设的最大值要大,那么左子树下标就是新的最大值下标
            max_idx = lchild_idx;
        }
        if (rchild_idx < tree_len && arr[rchild_idx] > arr[max_idx]) {
            // 如果右子树存在且右子树值比假设的最大值要大,那么右子树下标就是新的最大值下标
            max_idx = rchild_idx;
        }
        if (max_idx != root_idx) {
            // 交换左右子树较大者和根节点的值
            SWAP_ELEMENT(arr, max_idx, root_idx);
            // 此时max_idx结点的值就是以前根节点的值,此时由于数据发生了改变,max_idx结点的树就不一定是大顶堆了
            // 所以接下来要以max_idx为根节点,继续构建大顶堆
            root_idx = max_idx;
        }
        else {
            // 不需要交换了,说明以root_idx为根节点的树已经是大顶堆了
            break;
        }
    }
}
// 第一次将数组构建成大顶堆，自下而上将每一个非叶子结点构建大顶堆
static void first_build_heap(int arr[], int len) {
    int last_idx = len - 2 >> 1;    //最后一个非叶子结点的下标
    for (int i = last_idx; i >= 0; i--) {
        heapify(arr, len, i);
    }
    printf("第一次堆化后数组为: \n");
    print_arr(arr, len);
}
void heap_sort(int arr[], int len) {
    // 1.将原arr数组构建成大顶堆，第一次构建大顶堆
    first_build_heap(arr, len);
    // 2.反复移除根结点元素，然后再重建大顶堆
    int heap_len = len; // 堆逻辑上的长度，一开始就是数组长度，随着反复移除重建大顶堆，这个长度会一直减少1
    while (heap_len > 1) {  // 只要堆还有两个元素就需要继续构建移除
        SWAP_ELEMENT(arr, 0, heap_len - 1);
        heap_len--;
        /*
            堆排序的核心操作：
            重新构建大顶堆
        */
        heapify(arr, heap_len, 0);  // 堆排序核心操作：堆化
        printf("重新构建大顶堆后: \n");
        print_arr(arr, heap_len);
    }
}
int main(void) {
    int arr[] = { 4, 10, 3, 5, 1 };
    int len = ARR_SIZE(arr);
    printf("Before heap sort, the array is: ");
    print_arr(arr, len);
    // 测试堆排序
    heap_sort(arr, len);
    printf("After heap sort, the array is: ");
    print_arr(arr, len);
    return 0;
}</code>
</pre>
</details>
<h3 id="性能分析-6">性能分析</h3>
<p><strong>时间复杂度：</strong></p>
<p><strong>堆排序在任何情况下，时间复杂度都是<span
class="math inline">\(O(n\log n)\)</span></strong>。</p>
<p><strong>空间复杂度：</strong></p>
<p>堆排序显然是一个原地算法，不需要任何额外内存空间，空间复杂度是O(1)</p>
<p><strong>稳定性：</strong></p>
<p>堆排序是一种不稳定的排序算法，在堆化的过程需要交换父节点和左右子树结点，这个过程非常容易出现改变相同元素位置的情况。</p>
<hr />
<h2 id="排序算法总结">排序算法总结</h2>
<div data-align="center">
<table>
<tr>
<th>
</th>
<th>
</th>
<th>
选择排序
</th>
<th>
冒泡排序
</th>
<th>
插入排序
</th>
<th>
希尔排序
</th>
<th>
归并排序
</th>
<th>
快速排序
</th>
<th>
堆排序
</th>
</tr>
<tr>
<td rowspan="3">
时间复杂度
</td>
<td>
最佳情况
</td>
<td>
<i>O(n<sup>2</sup>)</i>
</td>
<td>
<b>O(n)</b>
</td>
<td>
<b>O(n)</b>
</td>
<td>
&lt;<i>O(n<sup>2</sup>)</i>
</td>
<td>
<i>O(nlogn)</i>
</td>
<td>
<i>O(nlogn)</i>
</td>
<td>
<i>O(nlogn)</i>
</td>
</tr>
<tr>
<td>
平均情况
</td>
<td>
<i>O(n<sup>2</sup>)</i>
</td>
<td>
<i>O(n<sup>2</sup>)</i>
</td>
<td>
<i>O(n<sup>2</sup>)</i>
</td>
<td>
&lt;<i>O(n<sup>2</sup>)</i>
</td>
<td>
<i>O(nlogn)</i>
</td>
<td>
<i>O(nlogn)</i>
</td>
<td>
<i>O(nlogn)</i>
</td>
</tr>
<tr>
<td>
最坏情况
</td>
<td>
<i>O(n<sup>2</sup>)</i>
</td>
<td>
<i>O(n<sup>2</sup>)</i>
</td>
<td>
<i>O(n<sup>2</sup>)</i>
</td>
<td>
接近<i>O(n<sup>2</sup>)</i>
</td>
<td>
<i>O(nlogn)</i>
</td>
<td>
<i>O(n<sup>2</sup>)</i>
</td>
<td>
<i>O(nlogn)</i>
</td>
</tr>
<tr>
<td colspan="2">
空间复杂度
</td>
<td>
<i>O</i>(1)
</td>
<td>
<i>O</i>(1)
</td>
<td>
<i>O</i>(1)
</td>
<td>
<i>O</i>(1)
</td>
<td>
<i>O</i>(n)
</td>
<td>
<i>O(logn)~O(n)</i>
</td>
<td>
<b>O(1)</b>
</td>
</tr>
<tr>
<td colspan="2">
稳定性
</td>
<td>
不稳定
</td>
<td>
稳定
</td>
<td>
稳定
</td>
<td>
不稳定
</td>
<td>
<b>稳定</b>
</td>
<td>
不稳定
</td>
<td>
不稳定
</td>
</tr>
</table>
</div>
<p>对于这七种排序算法，同样可以在适用场景上，对它们做一个总结：</p>
<ol type="1">
<li>选择排序：建议任何情况都不用。</li>
<li>冒泡排序：建议任何情况都不用。</li>
<li><strong>插入排序：适合小数据集，尤其当数据已基本有序时非常好用。</strong></li>
<li>希尔排序：一般不使用。</li>
<li><strong>归并排序：大数据集的场景下，需要稳定排序算法时使用。</strong></li>
<li><strong>快速排序：大数据集的场景下，通用的排序算法，效率高，但不稳定。</strong></li>
<li><strong>堆排序：大数据集的场景下，性能均衡的不稳定排序算法，优点是不占用额外内存空间。</strong></li>
</ol>
<p><strong>除此之外，还可以总结记忆排序算法的稳定和不稳定性的特点：</strong></p>
<p>只要排序算法涉及到<strong>大范围的、远距离的元素交换</strong>，那么该算法肯定不是稳定算法，因为远距离的数据交换没办法控制元素的相对位置不变。</p>
<p>举例：</p>
<ol type="1">
<li>选择排序：选择最小（或最大）元素放在排序数组的起始位置，涉及远距离交换。</li>
<li><strong>希尔排序</strong>：通过使用间隔序列，它在远距离进行元素比较和交换。</li>
<li><strong>快速排序</strong>：通过分区操作，导致可能出现远距离的元素交换。</li>
<li><strong>堆排序</strong>：可能出现将根结点元素一直交换到叶子结点，这是远距离交换元素。</li>
</ol>
<p>这些排序算法，都是因为存在不相邻元素的远距离交换，所以是不稳定的；而<strong>冒泡排序、插入排序以及归并排序，由于总是交换相邻元素，它们就是稳定的。</strong></p>
]]></content>
      <categories>
        <category>数据结构与算法（DSA）</category>
        <category>算法</category>
      </categories>
      <tags>
        <tag>排序</tag>
      </tags>
  </entry>
  <entry>
    <title>从 ASCII 到 Unicode：字符编码的发展历程</title>
    <url>/2026/02/01/programming/computer-system/from-ascii-to-unicode-character-code-development/</url>
    <content><![CDATA[<p>字符编码看起来像是一个“文本存储格式”问题，但它实际上贯穿了<strong>计算机输入、存储、传输、显示、编程语言运行时、文件系统、终端和操作系统接口</strong>的各个层面。很多开发者第一次真正意识到字符编码的重要性，往往不是在学习理论的时候，而是在遇到“乱码”时：同一个文件在一个系统里正常、在另一个系统里却变成了问号、方框或者一串莫名其妙的符号。</p>
<span id="more"></span>
<p>从历史上看，字符编码的发展，本质上是计算机从“只处理英文字符”逐步走向“处理全球文字系统”的过程；从工程上看，它也是从“各自为政”逐步走向“统一标准”的过程。本文尝试从时间演进、标准设计和不同操作系统的实现差异三个角度，系统梳理字符编码的来龙去脉。</p>
<hr />
<h2 id="一为什么计算机需要字符编码">一、为什么计算机需要字符编码</h2>
<p>计算机底层只能存储和处理 <strong>0</strong> 和
<strong>1</strong>。而人类使用的文本则由字母、数字、标点、汉字、假名、阿拉伯文、表情符号等组成。为了让计算机理解这些字符，就必须建立一套映射规则：</p>
<blockquote>
<p><strong>把“字符”映射为某种整数值，再把整数值表示为二进制字节序列。</strong></p>
</blockquote>
<p>这套映射规则，广义上就叫<strong>字符编码</strong>。</p>
<p>这里需要区分几个容易混淆的概念：</p>
<ul>
<li><strong>字符（Character）</strong>：抽象符号，例如
<code>A</code>、<code>中</code>、<code>€</code>。</li>
<li><strong>码点（Code Point）</strong>：字符在某个字符集中的编号。</li>
<li><strong>字符集（Character
Set）</strong>：规定“有哪些字符、它们的编号是什么”。</li>
<li><strong>编码方式（Encoding）</strong>：规定“这些编号如何编码成字节流”。</li>
</ul>
<p>在早期系统中，这几个概念经常被混在一起使用；到了 Unicode
时代，它们才逐渐被严格区分开。</p>
<hr />
<h2 id="二早期阶段从电报码到-ascii">二、早期阶段：从电报码到 ASCII</h2>
<p>在现代计算机之前，通信系统已经存在对字符进行编码的需求。最早可以追溯到电报时代的<strong>莫尔斯码</strong>。不过莫尔斯码并不是计算机意义上的定长二进制编码，它更像是一套人工与电信号之间的约定。</p>
<p>进入电子计算机时代后，字符编码开始朝着适合机器处理的方向发展。</p>
<h3 id="baudotebcdic-等早期方案">1. Baudot、EBCDIC 等早期方案</h3>
<p>早期字符编码标准并不统一，不同设备厂商和系统常常采用自己的方案。</p>
<ul>
<li><strong>Baudot Code</strong>：5
位编码，容量非常有限，常用于早期电传系统。</li>
<li><strong>EBCDIC</strong>：IBM 推出的 8
位编码体系，主要用于大型机生态。</li>
</ul>
<p>这些编码方案有一个共同特点：<strong>强依赖具体硬件与厂商生态</strong>。这意味着不同系统之间交换文本数据时，兼容性很差。</p>
<h3 id="ascii-的出现">2. ASCII 的出现</h3>
<p>真正影响深远的里程碑是 <strong>ASCII</strong>（American Standard Code
for Information Interchange）。</p>
<p>ASCII 使用 <strong>7 位</strong> 表示字符，总共可表示 <strong>128
个符号</strong>，包括：</p>
<ul>
<li>26 个英文字母大小写</li>
<li>数字 <code>0</code>~<code>9</code></li>
<li>常见标点符号</li>
<li>一系列控制字符，如：
<ul>
<li><code>LF</code>（换行）</li>
<li><code>CR</code>（回车）</li>
<li><code>TAB</code></li>
<li><code>ESC</code></li>
</ul></li>
</ul>
<p>ASCII
的价值不只是“能表示英文”，更重要的是它成为了一个<strong>跨平台的最小公共文本标准</strong>。许多后来的编码，不管设计差异多大，通常都会保留
ASCII 兼容区。</p>
<h3 id="ascii-的局限">3. ASCII 的局限</h3>
<p>ASCII 最大的问题很明显：</p>
<ul>
<li>只能覆盖英文及少量符号</li>
<li>无法表示重音字母</li>
<li>无法表示中文、日文、韩文等非拉丁文字</li>
</ul>
<p>当计算机开始走向全球化时，ASCII 很快就不够用了。</p>
<hr />
<h2 id="三8-位时代扩展-ascii-与本地代码页">三、8 位时代：扩展 ASCII
与本地代码页</h2>
<p>随着硬件从 7 位、6 位逐渐转向 8
位字节体系，人们自然想到：既然一个字节能表示 <code>0~255</code> 共 256
个值，那么是不是可以在 ASCII 的基础上扩展出更多字符？</p>
<p>于是出现了大量 <strong>8 位扩展编码</strong>。</p>
<h3 id="iso-8859-系列">1. ISO-8859 系列</h3>
<p>ISO-8859 是一组面向不同语言区域的单字节编码标准，例如：</p>
<ul>
<li><strong>ISO-8859-1</strong>：西欧语言常用</li>
<li><strong>ISO-8859-5</strong>：西里尔字母</li>
<li><strong>ISO-8859-6</strong>：阿拉伯文</li>
</ul>
<p>这类编码保留了前 128 个 ASCII 字符不变，把 <code>128~255</code>
区间分配给不同语言字符。</p>
<h3 id="代码页code-page体系">2. 代码页（Code Page）体系</h3>
<p>除了 ISO 标准，许多操作系统厂商还提出了自己的“代码页”方案，最典型的是
DOS/Windows 体系。</p>
<p>比如：</p>
<ul>
<li><strong>CP437</strong>：早期 IBM PC / DOS 常见代码页</li>
<li><strong>CP936</strong>：简体中文 Windows 常见代码页，本质上对应 GBK
生态</li>
<li><strong>CP1252</strong>：西欧 Windows 常见 ANSI 代码页</li>
</ul>
<p>所谓“代码页”，本质上就是某个平台选择的一套字节到字符的映射表。</p>
<h3 id="问题同一个字节不同系统含义不同">3.
问题：同一个字节，不同系统含义不同</h3>
<p>8 位时代最经典的问题就是：</p>
<blockquote>
<p><strong>同样的字节序列，在不同代码页下可能代表完全不同的字符。</strong></p>
</blockquote>
<p>这带来几个严重后果：</p>
<ul>
<li>文件跨系统传输容易乱码</li>
<li>软件需要根据地区配置切换编码</li>
<li>一个程序如果假设编码错误，字符串处理结果就会完全错乱</li>
</ul>
<p>这也是“乱码”成为上世纪软件开发常见问题的根本原因。</p>
<hr />
<h2
id="四东亚编码的出现多字节字符集时代">四、东亚编码的出现：多字节字符集时代</h2>
<p>对中文、日文、韩文来说，单字节 256
个位置远远不够。于是出现了各种<strong>多字节编码</strong>。</p>
<h3 id="中文编码的发展">1. 中文编码的发展</h3>
<p>中文字符数量大，必须采用双字节甚至变长方式来编码。</p>
<p>常见中文编码包括：</p>
<ul>
<li><strong>GB2312</strong>：中国大陆较早期的简体中文标准</li>
<li><strong>GBK</strong>：对 GB2312 的扩展，覆盖更多汉字</li>
<li><strong>GB18030</strong>：进一步扩展，兼容 Unicode
的国家标准实现要求</li>
<li><strong>Big5</strong>：繁体中文环境，尤其是早期香港、台湾地区常见</li>
</ul>
<h3 id="日文和韩文编码">2. 日文和韩文编码</h3>
<ul>
<li>日文常见有 <strong>Shift_JIS</strong>、<strong>EUC-JP</strong></li>
<li>韩文常见有 <strong>EUC-KR</strong></li>
</ul>
<p>这些编码往往设计复杂，既要兼容
ASCII，又要表示本国语言字符，有时还混用了单字节与双字节区间。</p>
<h3 id="多字节编码带来的工程复杂性">3. 多字节编码带来的工程复杂性</h3>
<p>多字节编码虽然解决了字符容量问题，但也引入了更多技术难题：</p>
<ul>
<li>一个字符不再等于一个字节</li>
<li>字符串长度不再等于字节长度</li>
<li>截断字节流时可能截断在字符中间</li>
<li>排序、搜索、子串截取都变复杂</li>
</ul>
<p>也就是说，程序员不能再简单认为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">字符串长度 = 占用字节数</span><br></pre></td></tr></table></figure>
<p>这个看似微小的变化，对编程语言库、操作系统
API、终端工具的设计都产生了深远影响。</p>
<hr />
<h2 id="五统一梦想unicode-的诞生">五、统一梦想：Unicode 的诞生</h2>
<p>到了 1980~1990 年代，软件行业逐渐意识到一个问题：</p>
<blockquote>
<p>如果每个国家、每个平台、每个厂商都维护自己的编码体系，那么全球化软件开发将长期陷入混乱。</p>
</blockquote>
<p>于是，<strong>Unicode</strong> 应运而生。</p>
<h3 id="unicode-的核心目标">1. Unicode 的核心目标</h3>
<p>Unicode 的目标很明确：</p>
<ul>
<li>为全球各种文字系统建立统一字符集合</li>
<li>给每个字符分配一个唯一编号</li>
<li>让不同平台共享同一个字符语义标准</li>
</ul>
<p>例如：</p>
<ul>
<li><code>A</code> 的码点是 <code>U+0041</code></li>
<li><code>中</code> 的码点是 <code>U+4E2D</code></li>
<li>😀 的码点是 <code>U+1F600</code></li>
</ul>
<p>注意：Unicode
首先定义的是<strong>字符与码点的对应关系</strong>，并不等于某一种具体字节编码方式。</p>
<h3 id="unicode-不是单一编码格式">2. Unicode 不是单一编码格式</h3>
<p>这是学习字符编码时最容易误解的一点。</p>
<p>很多人把 Unicode 理解成“一个编码格式”，其实更准确地说：</p>
<blockquote>
<p><strong>Unicode 是统一字符集标准，而 UTF-8、UTF-16、UTF-32
是它的具体实现编码方式。</strong></p>
</blockquote>
<p>Unicode 负责解决“字符编号统一”的问题；UTF
系列负责解决“如何把码点编码成字节”的问题。</p>
<hr />
<h2 id="六utf-家族统一字符集下的不同实现">六、UTF
家族：统一字符集下的不同实现</h2>
<h3 id="utf-32最直观但空间开销大">1. UTF-32：最直观，但空间开销大</h3>
<p>UTF-32 为每个字符固定使用 4 个字节。</p>
<p>优点：</p>
<ul>
<li>实现简单</li>
<li>码点到存储形式转换直接</li>
<li>理论上按“码点”索引方便</li>
</ul>
<p>缺点：</p>
<ul>
<li>太浪费空间</li>
<li>对英文文本尤其不经济</li>
</ul>
<p>因此 UTF-32 更多存在于内部处理场景，而不是文本交换主流格式。</p>
<h3 id="utf-16折中方案">2. UTF-16：折中方案</h3>
<p>UTF-16 以 2 字节为基本单元，常见字符通常占 2
字节，超出基本多文种平面（BMP）的字符则使用 <strong>代理对（Surrogate
Pair）</strong> 表示，占 4 字节。</p>
<p>UTF-16 曾长期在很多系统和语言运行时中扮演重要角色，例如：</p>
<ul>
<li>Windows 宽字符 API</li>
<li>Java 内部字符串历史实现</li>
<li>JavaScript 内部字符串编码模型（以 UTF-16 代码单元为基础）</li>
</ul>
<p>UTF-16 的问题在于：</p>
<ul>
<li>“一个字符”不一定是 2 字节</li>
<li>也不一定是一个 16 位单元</li>
<li>涉及代理对时，字符串遍历和长度计算容易出错</li>
</ul>
<h3 id="utf-8现代事实标准">3. UTF-8：现代事实标准</h3>
<p>UTF-8 是目前最成功的 Unicode 编码方式。</p>
<p>它的特点是：</p>
<ul>
<li><strong>变长编码</strong>，1~4 字节表示一个 Unicode 码点</li>
<li><strong>完全兼容 ASCII</strong>：ASCII 范围字符在 UTF-8
下仍是原来的单字节值</li>
<li>适合网络传输与文件存储</li>
<li>没有字节序问题</li>
</ul>
<p>UTF-8 之所以最终胜出，原因非常现实：</p>
<ol type="1">
<li>对英文世界兼容成本最低</li>
<li>对 Unix/Linux 传统字节流生态最友好</li>
<li>在 Web 时代极其适合跨平台传输</li>
<li>逐渐获得主流编程语言、编辑器、数据库和操作系统支持</li>
</ol>
<p>今天我们说“文本文件默认编码”，大多数场景下实际默认指向的就是
UTF-8。</p>
<hr />
<h2 id="七乱码究竟是怎么来的">七、乱码究竟是怎么来的</h2>
<p>所谓乱码，本质上通常不是“数据坏了”，而是：</p>
<blockquote>
<p><strong>写入时使用了一种编码，读取时却按另一种编码解释。</strong></p>
</blockquote>
<p>例如：</p>
<ul>
<li>文件实际是 GBK 编码</li>
<li>编辑器却按 UTF-8 解码</li>
</ul>
<p>此时字节并没有变化，但字符解释规则变了，于是就会显示异常。</p>
<p>乱码问题常见于：</p>
<ul>
<li>跨操作系统复制文本文件</li>
<li>老旧数据库迁移到新系统</li>
<li>终端环境变量设置错误</li>
<li>Web 页面响应头与实际内容编码不一致</li>
<li>程序把“字节长度”误当成“字符长度”处理</li>
</ul>
<p>所以字符编码问题的关键不只是“选哪种编码”，而是要保证：</p>
<blockquote>
<p><strong>输入、处理、存储、传输、输出链路上的编码假设一致。</strong></p>
</blockquote>
<hr />
<h2
id="八不同操作系统下的字符编码演变">八、不同操作系统下的字符编码演变</h2>
<p>字符编码的发展，不只是标准本身的历史，也是不同操作系统生态各自演进的历史。</p>
<h3 id="unix-linux从字节流传统走向-utf-8">1. Unix /
Linux：从字节流传统走向 UTF-8</h3>
<p>Unix
的哲学是“一切皆字节流”，它并不强制内核理解“文本字符”的高级语义。传统
Unix 工具更多是对字节进行处理，再结合 locale 决定如何解释这些字节。</p>
<p>这带来了两个重要特点：</p>
<ul>
<li>系统底层对编码较“中立”</li>
<li>用户态工具和 locale 决定文本行为</li>
</ul>
<p>在早期 Unix 和 Linux 系统中，常见过：</p>
<ul>
<li>ASCII</li>
<li>ISO-8859 系列</li>
<li>EUC 系列</li>
<li>Shift_JIS 等本地化编码</li>
</ul>
<p>但随着互联网和开源生态全球化，<strong>UTF-8 最终成为 Linux/Unix
世界的事实标准</strong>。</p>
<p>今天在 Linux 中，常见可以看到：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">locale</span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$LANG</span></span><br></pre></td></tr></table></figure>
<p>输出通常类似：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">zh_CN.UTF-8</span><br><span class="line">en_US.UTF-8</span><br></pre></td></tr></table></figure>
<p>这说明当前终端、排序规则、消息本地化等都建立在 UTF-8 locale
之上。</p>
<h4 id="unixlinux-下的几个典型现象">Unix/Linux 下的几个典型现象</h4>
<ol type="1">
<li><p><strong>文件名本质上是字节序列</strong><br />
内核并不真正“理解 UTF-8 文件名”，更多是用户空间约定这些字节按 UTF-8
来解释。</p></li>
<li><p><strong>终端编码必须匹配 locale</strong><br />
如果终端、shell、编辑器和程序的编码假设不一致，很容易出现乱码。</p></li>
<li><p><strong>很多老工具按字节工作</strong><br />
比如某些脚本工具在处理多字节 UTF-8
字符时，长度统计可能与“字符数”不一致。</p></li>
</ol>
<h3 id="windows从代码页体系走向-unicode">2. Windows：从代码页体系走向
Unicode</h3>
<p>Windows 的字符编码历史比 Unix 更复杂，因为它长期深受
<strong>代码页（Code Page）</strong> 体系影响。</p>
<h4 id="早期-dos-windows-阶段">早期 DOS / Windows 阶段</h4>
<p>在 DOS 时代，控制台和文本文件往往依赖 OEM 代码页；进入 Windows
图形界面时代后，又存在 ANSI 代码页。于是同一台机器上，常常并存：</p>
<ul>
<li>控制台代码页</li>
<li>系统 ANSI 代码页</li>
<li>OEM 代码页</li>
<li>应用程序自己的编码假设</li>
</ul>
<p>这也是 Windows 早期乱码问题非常常见的重要原因。</p>
<p>在中文 Windows 环境中，开发者经常会接触：</p>
<ul>
<li><strong>GB2312</strong></li>
<li><strong>GBK</strong></li>
<li><strong>CP936</strong></li>
</ul>
<p>很多历史遗留文本文件至今仍是这种编码。</p>
<h4 id="windows-nt-及-unicode-api">Windows NT 及 Unicode API</h4>
<p>从 Windows NT 系列开始，微软逐步构建了完整的 Unicode
支持体系。Windows API 常见分成两套：</p>
<ul>
<li><strong>A 版本 API</strong>：基于窄字符，通常依赖当前代码页</li>
<li><strong>W 版本 API</strong>：基于宽字符，使用 UTF-16</li>
</ul>
<p>例如：</p>
<ul>
<li><code>CreateFileA</code></li>
<li><code>CreateFileW</code></li>
</ul>
<p>这套双轨设计保证了对旧程序的兼容，也让新程序能逐步迁移到
Unicode。</p>
<h4 id="现代-windows-的变化">现代 Windows 的变化</h4>
<p>近些年 Windows 对 UTF-8 的支持明显增强：</p>
<ul>
<li>Windows Terminal 默认更现代化</li>
<li>PowerShell / 新版控制台对 UTF-8 更友好</li>
<li>许多编辑器与开发工具默认 UTF-8</li>
<li>新项目普遍倾向统一使用 Unicode 接口</li>
</ul>
<p>不过 Windows 生态中仍有一些历史包袱：</p>
<ol type="1">
<li>老程序可能仍依赖 ACP（ANSI Code Page）</li>
<li>控制台编码、文件编码、源码编码未必一致</li>
<li>某些本地化软件仍以 GBK/GB18030 为主要兼容目标</li>
</ol>
<p>因此在 Windows 上处理中文文本时，经常仍需明确区分：</p>
<ul>
<li>终端当前代码页是什么</li>
<li>文件实际保存为什么编码</li>
<li>程序内部字符串使用什么表示</li>
</ul>
<h3 id="macos现代-unix-基础上的-unicode-统一">3. macOS：现代 Unix
基础上的 Unicode 统一</h3>
<p>macOS 继承了 Unix 家族的许多特征，整体上与 Linux 类似，现代环境中也以
<strong>UTF-8</strong> 为主。</p>
<p>不过 macOS 在文件系统层面还有一个常被忽视的问题：</p>
<ul>
<li>某些场景下，文件名字符会涉及 <strong>Unicode
规范化</strong>（Normalization）差异</li>
</ul>
<p>也就是说，看起来相同的字符，在底层码点序列上可能不完全一样。这在跨平台同步文件、比较字符串、处理带重音字符文件名时，偶尔会造成“看起来一样但比较结果不同”的问题。</p>
<p>因此，macOS 的编码问题虽然比早期 Windows 简单，但在 Unicode
细节层面并不意味着完全没有坑。</p>
<hr />
<h2
id="九编程语言与运行时中的字符编码差异">九、编程语言与运行时中的字符编码差异</h2>
<p>操作系统之外，编程语言也会对编码问题产生很大影响。</p>
<h3 id="c-c">1. C / C++</h3>
<p>C
语言历史上对“字符串”的理解更接近<strong>字节数组</strong>。<code>char *</code>
很多时候并不意味着“字符字符串”，而只是某种编码文本的字节序列。</p>
<p>这使得 C 在处理文本时非常灵活，但也非常容易出错：</p>
<ul>
<li>需要程序员自己管理编码</li>
<li><code>strlen()</code> 返回的是字节数，不是字符数</li>
<li>多字节编码下截断、遍历都容易有问题</li>
</ul>
<h3 id="java-c-javascript">2. Java / C# / JavaScript</h3>
<p>这些语言诞生得更晚，设计时就更重视 Unicode。</p>
<ul>
<li>Java / C# 通常内部以 Unicode 字符串模型工作</li>
<li>JavaScript 字符串以 UTF-16 代码单元为核心</li>
</ul>
<p>优点是跨平台文本处理方便得多；缺点是当遇到代理对、组合字符、emoji
等复杂情况时，“长度”“索引”“一个字符”的定义依然不简单。</p>
<h3 id="python">3. Python</h3>
<p>Python 3 明确区分：</p>
<ul>
<li><code>str</code>：Unicode 文本</li>
<li><code>bytes</code>：原始字节序列</li>
</ul>
<p>这是一种非常重要的设计改进。它强迫开发者在“文本”与“字节”之间做出明确区分，也大大减少了
Python 2 时代常见的乱码问题。</p>
<hr />
<h2 id="十web-时代为什么-utf-8-几乎统一了世界">十、Web 时代为什么 UTF-8
几乎统一了世界</h2>
<p>字符编码真正大一统，离不开 Web 的推动。</p>
<p>互联网要求：</p>
<ul>
<li>浏览器能正确显示全球网页</li>
<li>服务器与客户端跨地区通信</li>
<li>HTML、CSS、JavaScript、JSON、URL、数据库之间尽可能无缝协作</li>
</ul>
<p>在这种需求下，UTF-8 的优势被无限放大：</p>
<ul>
<li>对英文兼容好</li>
<li>对多语言支持完整</li>
<li>字节序问题少</li>
<li>网络传输友好</li>
<li>工具链支持极其广泛</li>
</ul>
<p>所以今天常见的现代技术栈里：</p>
<ul>
<li>HTML 通常声明 <code>UTF-8</code></li>
<li>JSON 默认就是 Unicode 文本表示</li>
<li>大多数源码文件默认 UTF-8</li>
<li>Linux 终端默认 UTF-8 locale</li>
<li>Git 仓库中文本文件通常优先 UTF-8</li>
</ul>
<p>可以说，UTF-8
已经从“一个优秀编码方案”变成了“现代软件工程的默认共识”。</p>
<hr />
<h2
id="十一今天我们仍然需要关心字符编码吗">十一、今天我们仍然需要关心字符编码吗</h2>
<p>很多人会觉得：既然现在大家都用
UTF-8，那字符编码是不是已经不是问题了？</p>
<p>答案是：<strong>没有以前那么痛，但依然必须理解。</strong></p>
<p>原因主要有三点。</p>
<h3 id="历史遗留系统仍然大量存在">1. 历史遗留系统仍然大量存在</h3>
<p>现实工作中仍然经常遇到：</p>
<ul>
<li>Windows 下的 GBK 文本</li>
<li>老数据库中的非 UTF-8 字段</li>
<li>某些设备协议使用私有编码</li>
<li>与老旧系统对接时必须兼容代码页</li>
</ul>
<h3 id="unicode-统一了字符集但没有消灭所有复杂性">2. Unicode
统一了字符集，但没有消灭所有复杂性</h3>
<p>即便都使用 Unicode，也仍然存在：</p>
<ul>
<li>UTF-8 / UTF-16 / UTF-32 的差异</li>
<li>代理对问题</li>
<li>组合字符问题</li>
<li>规范化问题</li>
<li>“字节长度”“码点数量”“用户感知字符数”三者不同</li>
</ul>
<h3 id="编码问题本质上是系统边界问题">3.
编码问题本质上是系统边界问题</h3>
<p>只要存在系统边界，就存在编码问题：</p>
<ul>
<li>程序与文件之间</li>
<li>程序与终端之间</li>
<li>前端与后端之间</li>
<li>应用与数据库之间</li>
<li>Windows 与 Linux 之间</li>
</ul>
<p>因此，字符编码并没有“消失”，只是从“人人都在报乱码”变成了“谁做底层、跨平台、数据迁移、国际化，谁就必须认真理解”。</p>
<hr />
<h2
id="十二实践建议现代开发中如何减少编码问题">十二、实践建议：现代开发中如何减少编码问题</h2>
<p>如果从工程实践角度给出建议，通常可以总结为以下几条：</p>
<h3 id="优先统一使用-utf-8">1. 优先统一使用 UTF-8</h3>
<p>除非必须兼容历史系统，否则：</p>
<ul>
<li>源码文件用 UTF-8</li>
<li>文本配置文件用 UTF-8</li>
<li>接口数据用 UTF-8</li>
<li>数据库存储优先 UTF-8 / utf8mb4</li>
</ul>
<h3 id="明确区分字节和文本">2. 明确区分“字节”和“文本”</h3>
<p>尤其在网络编程、文件处理、系统编程中，要时刻问自己：</p>
<ul>
<li>现在处理的是字节流，还是字符文本？</li>
<li>当前这段字节是按什么编码解释的？</li>
</ul>
<h3 id="在系统边界处显式指定编码">3. 在系统边界处显式指定编码</h3>
<p>例如：</p>
<ul>
<li>读写文件时显式指定 UTF-8</li>
<li>HTTP 响应头明确字符集</li>
<li>数据导入导出时确认原始编码</li>
<li>终端工具中检查 locale / code page</li>
</ul>
<h3 id="理解你的操作系统与运行时">4. 理解你的操作系统与运行时</h3>
<p>同样一段文本处理代码，在不同平台行为可能不同，尤其是：</p>
<ul>
<li>Windows 的代码页与 UTF-16 API</li>
<li>Linux 的 locale 与终端 UTF-8 环境</li>
<li>macOS 的 Unicode 规范化细节</li>
</ul>
<p>理解这些差异，往往比死记编码表更重要。</p>
<hr />
<h2 id="总结">总结</h2>
<p>字符编码的发展史，本质上是计算机从局部、单语种、硬件绑定的文本表示方式，逐步走向全球统一字符体系的历史。</p>
<p>它经历了：</p>
<ul>
<li><strong>ASCII 时代</strong>：解决英文世界基础字符交换</li>
<li><strong>扩展 ASCII /
代码页时代</strong>：应对区域语言需求，但兼容性差</li>
<li><strong>多字节本地编码时代</strong>：解决东亚文字问题，但工程复杂度上升</li>
<li><strong>Unicode
时代</strong>：统一字符集，建立全球文本处理基础设施</li>
<li><strong>UTF-8 普及时代</strong>：成为现代跨平台软件开发事实标准</li>
</ul>
<p>从操作系统视角看：</p>
<ul>
<li><strong>Unix/Linux</strong> 倾向字节流与 UTF-8 locale 生态</li>
<li><strong>Windows</strong> 则从代码页逐步迁移到以 Unicode/UTF-16
为核心，并不断加强 UTF-8 支持</li>
<li><strong>macOS</strong> 基本拥抱 UTF-8，但在 Unicode
规范化等细节上仍有自己的特点</li>
</ul>
<p>理解字符编码，不只是为了避免乱码，更是为了真正理解：<strong>文本在计算机内部究竟是如何被表示、传输和解释的。</strong>
这对于系统编程、网络编程、跨平台开发、数据库迁移和国际化软件开发，都是一项非常基础但极其重要的能力。</p>
]]></content>
      <categories>
        <category>计算机系统</category>
      </categories>
      <tags>
        <tag>编码</tag>
      </tags>
  </entry>
  <entry>
    <title>开始学习C++（二）</title>
    <url>/2024/06/23/notes-reading/computer/cpp-primer-plus/02-cpp-begin-study/</url>
    <content><![CDATA[<p>本文记录了没有main的例外程序以及命名空间的几种使用方法。</p>
<span id="more"></span>
<hr />
<h2 id="没有main-的例外程序">“没有”<code>main()</code> 的例外程序</h2>
<ul>
<li>在 Windows 编程中，可以编写一个动态链接库 (DLL) 模块，这是其他
Windows程序可以使用的代码。由于 DLL 模块不是独立的程序，因此不需要
<code>main()</code>；</li>
<li>一些专用环境的程序，例如机器人中的控制器芯片，可能不需要
<code>main()</code>；</li>
<li>有些编程环境提供一个框架程序（MFC等），该程序调用一些非标准函数，如<code>_tmain()</code>。这种情况下，有一个隐藏的
<code>main()</code>，它调用 <code>_tmain()</code>。</li>
</ul>
<h2 id="命名空间的几种使用方法">命名空间的几种使用方法</h2>
<p>让标准命名空间（std）的元素在程序中可用的几种不同选择如下：</p>
<ol type="1">
<li><p>将以下内容放在文件中的<strong>所有函数定义上方</strong>，这样std命名空间中的所有内容就对该文件中的每个函数都可用了：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br></pre></td></tr></table></figure></li>
<li><p>将以下内容放在<strong>特定的函数定义中</strong>，使std命名空间的所有内容可用于该特定函数：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br></pre></td></tr></table></figure></li>
<li><p>在特定的函数定义中放置如下所示的<strong>using声明</strong>，使某个特定元素（例如<code>cout</code>）可用于该函数
（或者放在文件中的<strong>所有函数定义上方</strong>，特定元素（例如<code>cout</code>）就对该文件中的每个函数都可用）：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">using</span> std::cout;</span><br></pre></td></tr></table></figure></li>
<li><p>完全省略using指令和声明，并且在使用std命名空间中的元素时，始终使用<code>std::</code>前缀：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">std::cout &lt;&lt; <span class="string">&quot;I’m using cout and endl from the std namespace&quot;</span> &lt;&lt; std::endl;</span><br></pre></td></tr></table></figure></li>
</ol>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>C++ Primer Plus</tag>
      </tags>
  </entry>
  <entry>
    <title>变量与基本类型（三）</title>
    <url>/2024/06/29/notes-reading/computer/cpp-primer-plus/03-cpp-variables-and-basic-types/</url>
    <content><![CDATA[<p>本文记录了C++中与 变量与基本类型 相关的容易遗忘的一些知识。</p>
<span id="more"></span>
<hr />
<h2 id="变量">变量</h2>
<p>✏</p>
<p>✏</p>
<p>✏</p>
<hr />
<h2 id="基本类型">基本类型</h2>
<h3 id="整数类型">整数类型</h3>
<h4 id="short-int-long-long-long"><code>short</code>, <code>int</code>,
<code>long</code>, <code>long long</code></h4>
<p>C++标准规定：</p>
<ul>
<li>short 至少 16 位；</li>
<li>int至少与short一样大；</li>
<li>long至少32位并且至少和int一样大；</li>
<li>long long至少64位且至少和int一样大。</li>
</ul>
<h4 id="初始化">初始化</h4>
<p>C++ 有不同与C的初始化语法：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> owls = <span class="number">101</span>; <span class="comment">// traditional C initialization, sets owls to 101</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">wrens</span><span class="params">(<span class="number">432</span>)</span></span>; <span class="comment">// alternative C++ syntax, set wrens to 432</span></span><br></pre></td></tr></table></figure>
<p>如果不对<strong>函数内部定义的变量进行初始化</strong>，该变量的<strong>值就是不确定</strong>的。这意味着其值取决于在该变量创建之前恰好存放在那个内存位置的数据。</p>
<p>C++11初始化的其他方式：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> emus&#123;<span class="number">7</span>&#125;; <span class="comment">// set emus to 5</span></span><br><span class="line"><span class="type">int</span> rheas = &#123;<span class="number">12</span>&#125;; <span class="comment">// set rheas to 12</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> rocs = &#123;&#125;; <span class="comment">// set rocs to 0</span></span><br><span class="line"><span class="type">int</span> psychics&#123;&#125;; <span class="comment">// set psychicsto 0</span></span><br></pre></td></tr></table></figure>
<h4 id="通用字符名universal-character-names">通用字符名（Universal
Character Names）</h4>
<p>使用通用字符名称类似于使用转义序列。通用字符名称以<code>\u</code>或<code>\U</code>开头。<code>\u</code>形式后接8个十六进制数字，<code>\U</code>形式后接16个十六进制数字。这些数字代表该字符的ISO
10646编码点。</p>
<p>如果你的实现支持扩展字符，你可以在标识符、字符常量和字符串中使用通用字符名。例如，以下代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> k\u00F6rper;</span><br><span class="line">cout &lt;&lt; <span class="string">&quot;Let them eat g\u00E2teau.\n&quot;</span>;</span><br></pre></td></tr></table></figure>
<p>ö的ISO
10646编码点是00F6，â的编码点是00E2。因此，这段C++代码会将变量name设置为körper，并显示以下输出：</p>
<p><code>Let them eat gâteau.</code></p>
<h4 id="wchar_t"><code>wchar_t</code></h4>
<p><code>cin</code>和<code>cout</code>将输入和输出视为由字符（char）流组成，因此它们不适合处理<code>wchar_t</code>类型。<code>iostream</code>头文件提供了<code>wcin</code>和<code>wcout</code>的形式，用于处理<code>wchar_t</code>流。此外，你可以在宽字符常量或字符串前加上<code>L</code>来表示它们。下面的代码将字母<code>P</code>的<code>wchar_t</code>版本存储在变量<code>bob</code>中，并显示单词<code>tall</code>的<code>wchar_t</code>版本：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">wchar_t</span> bob = <span class="string">L&#x27;P&#x27;</span>; <span class="comment">// a wide-character constant</span></span><br><span class="line">wcout &lt;&lt; <span class="string">L&quot;tall&quot;</span> &lt;&lt; endl; <span class="comment">// outputting a wide-character string</span></span><br></pre></td></tr></table></figure>
<h4 id="char16_t-和-char32_t"><code>char16_t</code> 和
<code>char32_t</code></h4>
<p>C++11 对 <code>char16_t</code> 字符和字符串常量使用 <code>u</code>
前缀，例如 <code>u'C'</code> 和 <code>u"be good"</code>。同样，它对
<code>char32_t</code> 常量使用 <code>U</code> 前缀，例如
<code>U'R'</code> 和 <code>U"dirty rat"</code>。<code>char16_t</code>
类型自然匹配 <code>\u00F6</code> 形式的通用字符名，而
<code>char32_t</code> 类型自然匹配 <code>\U0000222B</code>
形式的通用字符名。</p>
<p>前缀<code>u</code>和<code>U</code>分别用于表示<code>char16_t</code>和<code>char32_t</code>类型的字符字面量：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">char16_t</span> ch1 = <span class="string">u&#x27;q&#x27;</span>; <span class="comment">// basic character in 16-bit form</span></span><br><span class="line"><span class="type">char32_t</span> ch2 = <span class="string">U&#x27;\U0000222B&#x27;</span>; <span class="comment">// universal character name in 32-bit form</span></span><br></pre></td></tr></table></figure>
<p>与<code>wchar_t</code>类似，<code>char16_t</code>和<code>char32_t</code>各自都有一个底层类型，该类型是内置整数类型之一。但在一个系统上，其底层类型可能与另一个系统上的不同。</p>
<h3 id="浮点数类型">浮点数类型</h3>
<p>✏</p>
<p>✏</p>
<p>✏</p>
<hr />
<h2 id="const-修饰符">const 修饰符</h2>
<h3
id="const之于define的优势"><code>const</code>之于<code>#define</code>的优势</h3>
<ul>
<li><p>它允许显式地指定类型。</p></li>
<li><p>你可以利用C++的作用域规则将定义限制在特定的函数或文件中。</p></li>
<li><p>可以将const用于更复杂的类型，如数组和结构体。</p></li>
</ul>
<hr />
<h2 id="类型转换">类型转换</h2>
<h3 id="自动类型转换发生时机">自动类型转换发生时机</h3>
<ul>
<li><p>当你将一种算术类型的值赋给另一种算术类型的变量时，C++会进行值的转换。</p></li>
<li><p>当你在表达式中组合混合类型时，C++会进行值的转换。</p></li>
<li><p>当你向函数传递参数时，C++会对值进行转换。</p></li>
</ul>
<h3 id="显示类型转换">显示类型转换</h3>
<p>两种形式：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">(typeName) value <span class="comment">// converts value to typeName type</span></span><br><span class="line"><span class="built_in">typeName</span> (value) <span class="comment">// converts value to typeName type</span></span><br></pre></td></tr></table></figure>
<p>第一种形式是纯C语言形式。第二种形式是纯C++形式。这种新形式背后的理念是让类型转换看起来像函数调用。这使得内置类型的类型转换看起来与你可以为用户定义的类设计的类型转换相似。</p>
<p>四种运算符：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">static_cast</span>&lt;typeName&gt; (value) <span class="comment">// converts value to typeName type</span></span><br></pre></td></tr></table></figure>
<h3 id="数组转换为指针的隐式转换">数组转换为指针的隐式转换</h3>
<p><strong>数组转换为指针</strong>：在大多数用到数组的表达式中，数组自动转换成指向数组首元素的指针：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> ia[<span class="number">10</span>];</span><br><span class="line"><span class="type">int</span> *ip = ia;      <span class="comment">// ia转换成指向数组首元素的指针</span></span><br></pre></td></tr></table></figure>
<p><strong>不会发生转换的情况：</strong>
当数组被用作<code>decltype</code>关键字的参数，或者作为取地址符<code>&amp;</code>、<code>sizeof</code>及<code>typeid</code>等运算符的运算对象时，用一个引用来初始化数组时。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>C++ Primer Plus</tag>
      </tags>
  </entry>
  <entry>
    <title>复合类型（四）</title>
    <url>/2024/07/06/notes-reading/computer/cpp-primer-plus/04-cpp-compound-types/</url>
    <content><![CDATA[<p>本文记录了C++中与 复合类型 相关的容易遗忘的一些知识。</p>
<span id="more"></span>
<hr />
<h2 id="内置数组">内置数组</h2>
<h3 id="初始化">初始化</h3>
<p>只有在定义数组时才能使用初始化形式。之后不能使用，也不能将一个数组整体赋值给另一个数组：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> cards[<span class="number">4</span>] = &#123;<span class="number">3</span>, <span class="number">6</span>, <span class="number">8</span>, <span class="number">10</span>&#125;; <span class="comment">// okay</span></span><br><span class="line"><span class="type">int</span> hand[<span class="number">4</span>]; <span class="comment">// okay</span></span><br><span class="line">hand[<span class="number">4</span>] = &#123;<span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>, <span class="number">9</span>&#125;; <span class="comment">// not allowed</span></span><br><span class="line">hand = cards; <span class="comment">// not allowed</span></span><br></pre></td></tr></table></figure>
<p>C++11内置数组特性：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// drop the = sign when initializing an array:</span></span><br><span class="line"><span class="type">double</span> earnings[<span class="number">4</span>] &#123;<span class="number">1.2e4</span>, <span class="number">1.6e4</span>, <span class="number">1.1e4</span>, <span class="number">1.7e4</span>&#125;; <span class="comment">// okay with C++11</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// use empty braces to set all the elements to 0:</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> counts[<span class="number">10</span>] = &#123;&#125;; <span class="comment">// all elements set to 0</span></span><br><span class="line"><span class="type">float</span> balances[<span class="number">100</span>] &#123;&#125;; <span class="comment">// all elements set to 0</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="字符串">字符串</h2>
<h3 id="逐行读取字符串输入">逐行读取字符串输入</h3>
<p>对于<code>istream</code>类（<code>cin</code>就是该类的一个实例），它有一些面向行的类成员函数：<code>getline()</code>和<code>get()</code>。这两个函数都会读取一整行输入——也就是说，一直读到换行符为止。不过，<code>getline()</code>会随后丢弃这个换行符，而<code>get()</code>则会将其留在输入队列中。</p>
<h4 id="getline"><code>getline()</code></h4>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">cin.<span class="built_in">getline</span>(name,<span class="number">20</span>);</span><br></pre></td></tr></table></figure>
<p><code>getline()</code>通过标记行尾的换行符读取输入，但不会保存换行符。相反，在存储字符串时，它会将换行符替换为空字符。</p>
<h4 id="get"><code>get()</code></h4>
<p><code>istream</code>类有另一个成员函数<code>get()</code>，它有好几种变体。其中一种变体的作用和<code>getline()</code>很像。它接受相同的参数，以相同的方式解析这些参数，并读取到行尾。但与读取并丢弃换行符不同，<code>get()</code>会将该字符留在输入队列中。假设你连续两次调用<code>get()</code>：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">cin.<span class="built_in">get</span>(name, ArSize);</span><br><span class="line">cin.<span class="built_in">get</span>(dessert, Arsize); <span class="comment">// a problem</span></span><br></pre></td></tr></table></figure>
<p>因为第一次调用会在输入队列中留下换行符，所以这个换行符是第二次调用看到的第一个字符。因此，<code>get()</code>函数会认为自己已经到达行尾，却没有找到任何可读取的内容。如果没有帮助，<code>get()</code>函数根本无法跳过那个换行符。</p>
<p>有一种<code>get()</code>的变体可以提供帮助。调用<code>cin.get()</code>（不带任何参数）会读取下一个单个字符，即便是换行符也不例外，所以你可以用它来处理换行符，为下一行输入做好准备。也就是说，下面这个序列是有效的：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">cin.<span class="built_in">get</span>(name, ArSize); <span class="comment">// read first line</span></span><br><span class="line">cin.<span class="built_in">get</span>(); <span class="comment">// read newline</span></span><br><span class="line">cin.<span class="built_in">get</span>(dessert, Arsize); <span class="comment">// read second line</span></span><br></pre></td></tr></table></figure>
<p>使用<code>get()</code>的另一种方法是将两个类成员函数连接起来，如下所示：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">cin.<span class="built_in">get</span>(name, ArSize).<span class="built_in">get</span>(); <span class="comment">// concatenate member functions</span></span><br></pre></td></tr></table></figure>
<p>之所以能做到这一点，是因为<code>cin.get(name, ArSize)</code>会返回<code>cin</code>对象，然后该对象被用作调用<code>get()</code>函数的对象。同样地，下面的语句会将连续的两行输入读入数组<code>name1</code>和<code>name2</code>，这相当于两次分别调用<code>cin.getline()</code>：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">cin.<span class="built_in">getline</span>(name1, ArSize).<span class="built_in">getline</span>(name2, ArSize);</span><br></pre></td></tr></table></figure>
<h4
id="为什么有了getline还要使用get">为什么有了<code>getline()</code>还要使用<code>get()</code></h4>
<p>为什么有了<code>getline()</code>还要使用<code>get()</code>呢？首先，较旧的实现可能没有<code>getline()</code>。其次，<code>get()</code>能让你更谨慎一些。例如，假设你用<code>get()</code>将一行内容读入一个数组。怎样判断它是读取了整行内容，还是因为数组已满而停止读取的呢？看看下一个输入字符就知道了。<strong>如果是换行符，那就说明读取了整行；如果不是换行符，那就意味着该行还有更多输入内容</strong>。简而言之，<code>getline()</code>使用起来稍简单一些，但<code>get()</code>让<strong>错误检查</strong>更简便。</p>
<hr />
<h2 id="结构体">结构体</h2>
<h3 id="结构体中的位域">结构体中的位域</h3>
<p>和C语言一样，C++允许你指定占据特定位数的结构成员。这在创建与某些硬件设备上的寄存器相对应的数据结构时会很方便。位域的类型应该是整数类型或枚举类型，冒号后面跟一个数字表示要使用的实际位数。你可以使用未命名的位域来提供间距。每个成员都被称为位域。下面是一个例子：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">torgle_register</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> SN : <span class="number">4</span>; <span class="comment">// 4 bits for SN value</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> : <span class="number">4</span>; <span class="comment">// 4 bits unused</span></span><br><span class="line"><span class="type">bool</span> goodIn : <span class="number">1</span>; <span class="comment">// valid input (1 bit)</span></span><br><span class="line"><span class="type">bool</span> goodTorgle : <span class="number">1</span>; <span class="comment">// successful torgling</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>你可以用常规方式初始化这些字段，并且可以使用标准的结构表示法来访问位字段：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">torgle_register tr = &#123; <span class="number">14</span>, <span class="literal">true</span>, <span class="literal">false</span> &#125;;</span><br><span class="line">...</span><br><span class="line"><span class="keyword">if</span> (tr.goodIn) <span class="comment">// if statement covered in Chapter 6</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="联合体">联合体</h2>
<h3 id="定义">定义</h3>
<p>联合体是一种数据格式，它可以存储不同的数据类型，但每次只能存储一种类型。也就是说，结构体可以同时存储例如一个<code>int</code>、一个<code>long</code>和一个<code>double</code>，而联合体只能存储一个<code>int</code>、或一个<code>long</code>或者一个<code>double</code>。共用体的语法与结构体类似，但含义不同。例如，请看下面的声明：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">union</span> <span class="title class_">one4all</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> int_val;</span><br><span class="line">    <span class="type">long</span> long_val;</span><br><span class="line">    <span class="type">double</span> double_val;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>由于联合体一次只能存储一个值，所以它必须有足够的空间来容纳其最大的成员。因此，<strong>联合体的大小等于其最大成员的大小</strong>。</p>
<hr />
<h2 id="枚举">枚举</h2>
<h3 id="定义-1">定义</h3>
<p>C++的枚举功能为创建符号常量提供了一种替代const的方式。它还允许定义新类型，但方式相当受限。枚举的语法类似于结构体语法。例如，考虑以下语句：</p>
<p><code>enum spectrum {red, orange, yellow, green, blue, violet, indigo, ultraviolet};</code></p>
<blockquote>
<p>注意：成员值之间分隔符用的是逗号，结构体和联合用的是引号<code>;</code>。</p>
</blockquote>
<p>该语句有两个作用：</p>
<ul>
<li>使<code>spectrum</code>成为一种新类型的名称；<code>spectrum</code>被称为枚举，就像结构体变量被称为结构体一样。</li>
<li>将<code>red</code>、<code>orange</code>、<code>yellow</code>等确立为整数值0到7的符号常量。这些常量被称为枚举量。</li>
</ul>
<hr />
<h2 id="指针">指针</h2>
<h3 id="指针与数组名的区别">指针与数组名的区别</h3>
<p>第一个区别是，你可以改变指针的值，而数组名是一个常量：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">pointername = pointername + <span class="number">1</span>; <span class="comment">// 有效</span></span><br><span class="line">arrayname = arrayname + <span class="number">1</span>; <span class="comment">// 不允许</span></span><br></pre></td></tr></table></figure>
<p>第二个区别是，对数组名应用<code>sizeof</code>运算符会得到数组的大小，但对指针应用<code>sizeof</code>会得到指针的大小，即使该指针指向数组。这是C++不将数组名解释为地址的一种情况。</p>
<blockquote>
<p><strong>数组的地址</strong></p>
<p>对数组取地址是数组名称不被解释为其地址的另一种情况。数组的名称难道不是被解释为数组的地址吗？并不完全是——数组的名称被解释为数组首元素的地址，而使用地址运算符则会得到整个数组的地址：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">short</span> tell[<span class="number">10</span>]; <span class="comment">// tell是一个20字节的数组</span></span><br><span class="line">cout &lt;&lt; tell &lt;&lt; endl; <span class="comment">// 显示&amp;tell[0]</span></span><br><span class="line">cout &lt;&lt; &amp;tell &lt;&lt; endl; <span class="comment">// 显示整个数组的地址</span></span><br></pre></td></tr></table></figure>
<p>从数值上看，这两个地址是相同的，但从概念上讲，<code>&amp;tell[0]</code>（<code>tell</code>也是）是一个2字节内存块的地址，而<code>&amp;tell</code>是一个20字节内存块的地址。所以表达式<code>tell + 1</code>会使地址值增加2，而<code>&amp;tell + 1</code>会使地址值增加20。换一种说法，<code>tell</code>的类型是指向<code>short</code>的指针（即<code>short *</code>），而<code>&amp;tell</code>的类型是指向包含20个<code>short</code>的数组的指针（即<code>short (*)[20]）</code>。</p>
<p>现在你可能会好奇最后那个类型描述是怎么来的。首先，下面是如何声明并初始化该类型的指针：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">short</span> (*pas)[<span class="number">20</span>] = &amp;tell; <span class="comment">// pas指向包含20个short的数组</span></span><br></pre></td></tr></table></figure>
<p>如果省略括号，根据优先级规则，<code>[20]</code>会先与<code>pas</code>关联，使<code>pas</code>成为一个包含20个指向<code>short</code>的指针的数组，所以括号是必需的。其次，如果你想描述一个变量的类型，可以以该变量的声明为指导，并去掉变量名。因此，<code>pas</code>的类型是<code>short (*)[20]</code>。另外需要注意的是，由于<code>pas</code>被赋值为<code>&amp;tell</code>，所以<code>*pas</code>等同于<code>tell</code>，因此<code>(*pas)[0]</code>就是<code>tell</code>数组的第一个元素。</p>
</blockquote>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>C++ Primer Plus</tag>
      </tags>
  </entry>
  <entry>
    <title>循环、表达式和语句（五）</title>
    <url>/2024/07/14/notes-reading/computer/cpp-primer-plus/05-cpp-loops-expressions-statements/</url>
    <content><![CDATA[<p>本文记录了C++中与 循环、表达式和语句 相关的容易遗忘的一些知识。</p>
<span id="more"></span>
<hr />
<h2 id="表达式和语句">表达式和语句</h2>
<h3 id="定义">定义</h3>
<ul>
<li><p>一个C++表达式是一个值，或者是值与运算符的组合，并且每个C++表达式都有一个值。从C++的构建方式来看，</p></li>
<li><p>对表达式进行求值是主要作用，改变变量是副作用。</p></li>
<li><p>语句没有值。不能将语句赋值给变量，比如for语句、声明语句等。</p></li>
<li><p>从表达式到语句只是一小步，只需加一个分号。</p></li>
</ul>
<h3 id="打破规则">打破规则</h3>
<p>C++ 为 C 语言的循环增加了一项特性，这需要对 for
循环的语法进行一些巧妙的调整。以下是原来的语法：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (expression; expression; expression)</span><br><span class="line">  statement</span><br></pre></td></tr></table></figure>
<p>特别是，for结构的控制部分由三个表达式组成，这些表达式用分号分隔。不过，C++循环允许你做如下之类的事情：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++)</span><br></pre></td></tr></table></figure>
<p>也就是说，你可以在for循环的初始化区域声明一个变量。这可能很方便，但它不符合原有的语法，因为声明不是表达式。</p>
<p>for语句的语法已修改为如下形式：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">for</span>-init-statement condition; expression)</span><br><span class="line">  statement</span><br></pre></td></tr></table></figure>
<p>乍一看，这似乎有些奇怪，因为这里只有一个分号，而不是两个。但这是没问题的，因为for初始化语句被认定为一个语句，而一个语句本身就带有分号。至于<code>for-init-statement</code>，它被识别为表达式语句或声明。此语法规则用一个自带分号的语句取代了后跟分号的表达式。</p>
<p>归根结底，这意味着C++程序员希望能够在for循环的初始化部分声明并初始化一个变量，而且他们会<strong>不惜对C++语法乃至英语语言做出任何必要的改动</strong>来实现这一点。</p>
<h3 id="逗号表达式">逗号表达式</h3>
<ul>
<li>逗号表达式本身是序列点</li>
<li>且有最低的运算符优先级</li>
<li>逗号表达式的值是该表达式第二部分的值</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">i = <span class="number">20</span>, j = <span class="number">2</span> * i <span class="comment">// i set to 20, then j set to 40</span></span><br><span class="line">cata = <span class="number">17</span>,<span class="number">240</span>;   <span class="comment">// read as (cats = 17), 240; cata = 17  240 do nothing</span></span><br><span class="line">cats = (<span class="number">17</span>,<span class="number">240</span>); <span class="comment">// cats = 240</span></span><br></pre></td></tr></table></figure>
<h3 id="空语句">空语句</h3>
<ol type="1">
<li>使用空语句时应该加上注释，从而让读这段代码的人知道该语句是有意省略的。</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span>(cin &gt;&gt;s &amp;&amp; s != sought )</span><br><span class="line">    ;  <span class="comment">// 空语句</span></span><br></pre></td></tr></table></figure>
<h3 id="跳转语句">跳转语句</h3>
<ol type="1">
<li><p>C++提供了4种跳转语句：<strong>break</strong>、<strong>continue</strong>、<strong>goto</strong>和<strong>return</strong>。</p></li>
<li><p>break语句负责终止<strong>离它最近</strong>的while、do
while、for或switch语句，并从这些语句之后的第一条语句开始继续执行。</p></li>
<li><p>continue语句终止<strong>最近的循环</strong>中的当前迭代并立即开始下一次迭代。和break语句类似的是，出现在嵌套循环中的continue语句仅作用于离它最近的循环。和break语句不同的是，只有当switch语句嵌套在迭代语句内部时，才能在switch里使用continue。</p></li>
<li><p><code>goto</code>语句无条件跳转到同一函数内的另一条语句。语法形式为：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">goto</span> label;</span><br></pre></td></tr></table></figure>
<p><code>label</code>是作用于标识一条语句的标示符。带标签语句是一种特殊的语句，在它之前有一个标示符以及一个冒号：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">end: <span class="keyword">return</span>;  <span class="comment">// 带标签语句，可以作为goto的目标</span></span><br></pre></td></tr></table></figure>
<p>标签标示符独立于变量或其他标示符的名字，因此，标签标示符可以和程序中其他实体的标示符使用同一个名字而不会相互干扰。goto语句和控制权转向的那条带标签的语句必须位于同一个函数之内。</p>
<p><strong>尽量不要在程序中使用goto语句，因为它使得程序既难理解又难修改。</strong></p></li>
</ol>
<hr />
<h2 id="循环">循环</h2>
<h3 id="前缀与后缀的效率">前缀++与后缀++的效率</h3>
<p>如果将值用于某种用途（例如作为函数参数或赋值给变量），那么使用前缀形式还是后缀形式会有所不同。但是，如果递增或递减表达式的值未被使用，情况会怎样呢？例如，<code>x++;</code>和<code>++x;</code>有什么区别呢？</p>
<p>从逻辑上讲，在这两种情况下，使用前缀形式还是后缀形式并没有区别。表达式的值并未被使用，因此唯一的影响就是副作用。这里使用这些运算符的表达式都是完整表达式，所以到程序执行下一步时，对<code>x</code>进行递增的副作用肯定已经完成；前缀形式和后缀形式会产生相同的最终结果。</p>
<p>不过，尽管前缀形式和后缀形式的选择对程序行为没有影响，但这种选择可能会对执行速度产生微小影响。对于内置类型和现代编译器来说，这似乎不是什么问题。但C++允许为类定义这些运算符。在这种情况下，用户定义的前缀函数的工作方式是先递增一个值，然后返回该值。而<strong>后缀版本的工作方式是先保存该值的一个副本，对该值进行递增，然后返回保存的副本</strong>。因此，<strong>对于类来说，前缀版本比后缀版本效率要高一些</strong>。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> i = <span class="number">0</span>, j;</span><br><span class="line">j = ++i;      <span class="comment">// j = 1, i = 1 ：前置版本得到递增之后的值</span></span><br><span class="line">j = i++;      <span class="comment">// j = 1, i = 2 ：后置版本得到递增之前的值</span></span><br></pre></td></tr></table></figure>
<h3 id="类型别名">类型别名</h3>
<p>C++有两种方法可以为一种类型建立新名称作为别名。一种是使用预处理器（在编译时进行<strong>宏替换</strong>）：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> BYTE char <span class="comment">// 预处理器将BYTE替换为char</span></span></span><br></pre></td></tr></table></figure>
<p>第二种方法是使用C++（以及C语言）的关键字<code>typedef</code>来创建别名。例如，要让<code>byte</code>成为<code>char</code>的别名，可以这样写：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">char</span> byte; <span class="comment">// 使byte成为char的别名</span></span><br></pre></td></tr></table></figure>
<p>其一般形式如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> typeName aliasName;</span><br></pre></td></tr></table></figure>
<p>换句话说，如果你想让别名成为某一特定类型的别名，你可以先像声明该类型的变量一样声明这个别名，然后在声明前面加上<code>typedef</code>关键字。例如，要让<code>byte_pointer</code>成为指向<code>char</code>的指针的别名，你可以先将<code>byte_pointer</code>声明为指向<code>char</code>的指针，然后在前面加上<code>typedef</code>：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">char</span> * byte_pointer; <span class="comment">// 指向char类型的指针</span></span><br></pre></td></tr></table></figure>
<h3 id="基于范围的for循环">基于范围的for循环</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">double</span> prices[<span class="number">5</span>] = &#123;<span class="number">4.99</span>, <span class="number">10.99</span>, <span class="number">6.87</span>, <span class="number">7.99</span>, <span class="number">8.49</span>&#125;;</span><br><span class="line"><span class="keyword">for</span> (<span class="type">double</span> x : prices)</span><br><span class="line">  cout &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 修改数组中的值要用引用</span></span><br><span class="line"><span class="keyword">for</span> (<span class="type">double</span> &amp;x : prices) </span><br><span class="line">  x = x * <span class="number">0.80</span>; <span class="comment">//20% off sale</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 用于初始化列表</span></span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> x : &#123;<span class="number">3</span>, <span class="number">5</span>, <span class="number">2</span>, <span class="number">8</span>, <span class="number">6</span>&#125;)</span><br><span class="line">  cout &lt;&lt; x &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br><span class="line">cout &lt;&lt; ‘\n’;</span><br></pre></td></tr></table></figure>
<h3 id="键盘模拟eof">键盘模拟EOF</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Ctrl + Z // Windows</span><br><span class="line">Ctrl + D // Unix Linux</span><br></pre></td></tr></table></figure>
<p>请注意，在Unix及类Unix系统（包括Linux和Cygwin）中，<code>Ctrl+Z</code>会<strong>暂停</strong>程序的执行；<code>fg</code>命令可使执行恢复。</p>
<h3 id="常用的字符输入习语">常用的字符输入习语</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (cin.<span class="built_in">get</span>(ch)) <span class="comment">// while input is successful</span></span><br><span class="line">&#123;</span><br><span class="line">... <span class="comment">// do stuff</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>首先必须执行对<code>cin.get(ch)</code>的调用，若调用成功，就会将一个值存入<code>ch</code>中。然后程序会从该函数调用中获取返回值，这个返回值就是<code>cin</code>。接着，程序会对<code>cin</code>进行<code>bool</code>类型转换，如果输入成功，转换结果为<code>true</code>，否则为<code>false</code>。三条准则（确定终止条件、初始化条件以及更新条件）都被压缩到了一个循环测试条件中。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>C++ Primer Plus</tag>
      </tags>
  </entry>
  <entry>
    <title>函数（六）</title>
    <url>/2024/07/21/notes-reading/computer/cpp-primer-plus/06-cpp-functions/</url>
    <content><![CDATA[<p>本文记录了C++中与 函数 相关的容易遗忘的一些知识。</p>
<span id="more"></span>
<hr />
<h2 id="函数综述">函数综述</h2>
<h3 id="为什么需要函数原型">为什么需要函数原型？</h3>
<p>原型向编译器描述函数接口。也就是说，它告诉编译器该函数的返回值类型（如果有的话），以及函数参数的数量和类型。</p>
<table>
<thead>
<tr class="header">
<th>原因</th>
<th>解释</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>使编译器在调用前知道函数存在</td>
<td>否则调用时报错</td>
</tr>
<tr class="even">
<td>类型检查</td>
<td>检查参数数量、类型、返回值</td>
</tr>
<tr class="odd">
<td>支持多文件工程</td>
<td>头文件机制依赖函数原型</td>
</tr>
<tr class="even">
<td>支持函数重载</td>
<td>参数列表匹配需要原型</td>
</tr>
<tr class="odd">
<td>限制危险的隐式函数声明</td>
<td>C++ 禁止隐式声明</td>
</tr>
</tbody>
</table>
<p><strong>函数原型让编译器能正确、安全地生成函数调用，不然编译器根本不知道怎么调用。</strong></p>
<hr />
<h2 id="递归函数">递归函数</h2>
<p>参考<span class="exturl" data-url="aHR0cHM6Ly93d3cuY25ibG9ncy5jb20vSW52aW5jLVovcC8xOTMwMzc4NS9yZWN1cnNpb24=">递归算法设计与实现<i class="fa fa-external-link-alt"></i></span></p>
<hr />
<h2 id="函数指针pointers-to-functions">函数指针（Pointers to
Functions）</h2>
<h3 id="定义">定义</h3>
<p>函数指针，即指向函数的指针（地址）。函数和数据项一样，都有地址。<strong>函数的地址是存储该函数的机器语言代码开始的内存地址</strong>。</p>
<p>编写一个以另一个函数的地址作为参数的函数是可行的。这使得第一个函数能够找到第二个函数并运行它。这种方法比直接让第一个函数调用第二个函数要更麻烦一些，但它保留了在不同时间传递不同函数地址的可能性。<u>这意味着第一个函数可以在不同时间使用不同的函数</u>。</p>
<h3 id="基本使用">基本使用</h3>
<ol type="1">
<li><p>获取函数地址：函数名即为函数地址。</p></li>
<li><p>声明一个指向函数的指针：要声明一个指向特定类型函数的指针，可以先为所需类型的常规函数编写一个原型，然后将函数名替换为<code>(*pf)</code>形式的表达式。在这种情况下，<code>pf</code>就是指向该类型函数的指针。</p></li>
<li><p>使用指针调用函数：一种表示，<code>(*pf)</code>与函数名等价。另一种表示，C++也允许像使用函数名一样使用<code>pf</code>。使用第一种形式虽然更难看，但它能提供强烈的视觉提示，让人意识到代码正在使用函数指针。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">double</span> <span class="title">pam</span><span class="params">(<span class="type">int</span>)</span></span>;</span><br><span class="line"><span class="built_in">double</span> (*pf)(<span class="type">int</span>);</span><br><span class="line">pf = pam; <span class="comment">// pf now points to the pam() function</span></span><br><span class="line"><span class="type">double</span> x = <span class="built_in">pam</span>(<span class="number">4</span>); <span class="comment">// call pam() using the function name</span></span><br><span class="line"><span class="type">double</span> y = (*pf)(<span class="number">5</span>); <span class="comment">// call pam() using the pointer pf</span></span><br><span class="line"><span class="type">double</span> y = <span class="built_in">pf</span>(<span class="number">5</span>); <span class="comment">// also call pam() using the pointer pf</span></span><br></pre></td></tr></table></figure></li>
</ol>
<hr />
<h2 id="引用变量">引用变量</h2>
<p>C++为该语言新增了一种复合类型——引用变量。引用是一个作为变量别名的名称。引用变量的主要用途是作为函数的形参。如果你使用引用作为参数，函数操作的是原始数据，而不是副本。对于在函数中处理大型结构来说，引用提供了一种比指针更便捷的替代方式。</p>
<h3 id="引用的本质">引用的本质</h3>
<p>引用有点像常量指针；在创建它时就必须对其进行初始化，而且当引用宣誓效忠于某个特定变量后，它就会坚守这个誓言。也就是说，</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> &amp; rodents = rats;</span><br></pre></td></tr></table></figure>
<p>本质上是一种伪装的表示法，类似于：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> * <span class="type">const</span> pr = &amp;rats;</span><br></pre></td></tr></table></figure>
<p>在这里，引用<code>rodents</code>所扮演的角色和表达式<code>*pr</code>是一样的。</p>
<h3 id="匿名变量">匿名变量</h3>
<p>如果函数调用的<strong>实参不是左值</strong>，<strong>或者与对应的常量引用形参类型不匹配</strong>，C++会创建一个类型正确的匿名变量，将函数调用实参的值赋给该匿名变量，并让形参引用这个变量。</p>
<p>这里还有一个注意的点是形参必须是常量引用，不然的话修改的其实是匿名变量的值，而实际传的实参值没有改变，这与不加<code>const</code>的本来的修改实参的意图相悖。所以C++禁止这样做。</p>
<h3 id="函数中的引用变量">函数中的引用变量</h3>
<p>返回引用的函数实际上是所引用变量的别名。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span>&amp; <span class="title">getValue</span><span class="params">(<span class="type">int</span>&amp; x)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x;   <span class="comment">// 返回 x 的引用，也就是 x 本身</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="type">int</span>&amp; b = <span class="built_in">getValue</span>(a); <span class="comment">// 这里整个函数表达式相当于是a或x的别名</span></span><br></pre></td></tr></table></figure>
<h3
id="普通函数返回值的赋值为何经常会出现两次-copy">普通函数返回值的赋值为何经常会出现“两次
copy”</h3>
<p>这是一个编译器实现与 ABI（Application Binary
Interface）相关的问题。</p>
<p><strong>因为大多数 ABI 的函数返回机制要求：</strong></p>
<ol type="1">
<li><strong>函数返回时先把返回值写入一个临时位置（通常是寄存器或隐藏指针指向的空间）</strong></li>
<li><strong>然后在赋值语句中再从这个临时位置 copy
到最终变量</strong></li>
</ol>
<p>所以就出现了 <strong>“函数内部一次 copy → 赋值语句又一次
copy”</strong>。</p>
<p><strong>2 次 copy 的根本原因（关键）：</strong></p>
<ol type="1">
<li><strong>ABI 要求 struct 返回值由调用方分配空间</strong> →
函数必须将内部的返回对象 copy 到调用方提供的缓冲区。</li>
<li><strong>赋值语句又需要从返回缓冲区 copy 到目标变量</strong> →
出现第二次 copy。</li>
</ol>
<h3
id="返回引用类型的函数在赋值时通常只发生一次-copy">返回引用类型的函数在赋值时通常只发生一次
copy</h3>
<p>返回引用不会产生返回值 copy，赋值语句本身才是唯一的 copy 来源。</p>
<p>为什么没有返回值 copy？</p>
<p>因为引用（<code>T&amp;</code>）只是一个“地址”，返回时只需把地址放进寄存器，不构造临时对象，不使用隐藏返回缓冲区。</p>
<p>与此相反，返回值（T）必须经由隐藏返回缓冲区 → 可能两次 copy。</p>
<p>不过，有一个重要的补充：</p>
<p><strong>如果写的是 <code>T y = func();</code>，那么确实只有一次
copy（把 func 返回的对象内容 copy 给 y）。</strong>但如果你写的是
<strong>给引用本身赋值</strong><code>T&amp; y = func();</code> 或
<strong>持续使用引用</strong>，可能根本没有 copy。</p>
<hr />
<h2 id="函数重载">函数重载</h2>
<p><strong>函数签名（参数列表）而非函数返回值类型决定函数重载</strong>。例如，以下两个声明是不兼容的：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">long</span> <span class="title">gronk</span><span class="params">(<span class="type">int</span> n, <span class="type">float</span> m)</span></span>; </span><br><span class="line"><span class="function"><span class="type">double</span> <span class="title">gronk</span><span class="params">(<span class="type">int</span> n, <span class="type">float</span> m)</span></span>;</span><br></pre></td></tr></table></figure>
<p>所以，C++不允许以这种方式重载 <code>gronk()</code>
函数。可以有不同的返回类型，但前提是签名也不同：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">long</span> <span class="title">gronk</span><span class="params">(<span class="type">int</span> n, <span class="type">float</span> m)</span></span>;</span><br><span class="line"><span class="function"><span class="type">double</span> <span class="title">gronk</span><span class="params">(<span class="type">float</span> n, <span class="type">float</span> m)</span></span>;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="函数模板">函数模板</h2>
<h3 id="一般形式">一般形式</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt; <span class="comment">// or class T</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Swap</span><span class="params">(T &amp;a, T &amp;b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    T temp; <span class="comment">// temp a variable of type T</span></span><br><span class="line">    temp = a;</span><br><span class="line">    a = b;</span><br><span class="line">    b = temp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="注意事项">注意事项</h3>
<p>需要注意的是，函数模板并不会缩短可执行程序的长度。而且最终的代码中不会包含任何模板，只包含为程序生成的实际函数。模板的好处在于，它们使得生成多个函数定义变得更简单、更可靠。更常见的做法是，将模板放在头文件中，然后在使用它们的文件中包含该头文件。</p>
<h3 id="模板特化">模板特化</h3>
<p>非模板函数、模板函数以及显式特化 函数原型如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// non template function prototype</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Swap</span><span class="params">(job &amp;, job &amp;)</span></span>;</span><br><span class="line"><span class="comment">// template prototype</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Swap</span><span class="params">(T &amp;, T &amp;)</span></span>;</span><br><span class="line"><span class="comment">// explicit specialization for the job type</span></span><br><span class="line"><span class="keyword">template</span> &lt;&gt; <span class="type">void</span> <span class="built_in">Swap</span>&lt;job&gt;(job &amp;, job &amp;);</span><br></pre></td></tr></table></figure>
<p><code>Swap&lt;job&gt;</code> 中的 <code>&lt;job&gt;</code>
是可选的，因为函数参数类型表明这是针对 job
的特化版本。因此，该原型也可以这样写：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;&gt; <span class="function"><span class="type">void</span> <span class="title">Swap</span><span class="params">(job &amp;, job &amp;)</span></span>; <span class="comment">// 更简洁的形式</span></span><br></pre></td></tr></table></figure>
<h3 id="实例化与特化">实例化与特化</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">Swap</span> <span class="params">(T &amp;, T &amp;)</span></span>; <span class="comment">// template prototype</span></span><br><span class="line"><span class="keyword">template</span> &lt;&gt; <span class="type">void</span> <span class="built_in">Swap</span>&lt;job&gt;(job &amp;, job &amp;); <span class="comment">// explicit specialization for job</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">(<span class="type">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">template</span> <span class="type">void</span> <span class="built_in">Swap</span>&lt;<span class="type">char</span>&gt;(<span class="type">char</span> &amp;, <span class="type">char</span> &amp;); <span class="comment">// explicit instantiation for char</span></span><br><span class="line">    <span class="type">short</span> a, b;</span><br><span class="line">    ...</span><br><span class="line">        <span class="built_in">Swap</span>(a,b); <span class="comment">// implicit template instantiation for short</span></span><br><span class="line">    job n, m;</span><br><span class="line">    ...</span><br><span class="line">        <span class="built_in">Swap</span>(n, m); <span class="comment">// use explicit specialization for job</span></span><br><span class="line">    <span class="type">char</span> g, h;</span><br><span class="line">    ...</span><br><span class="line">        <span class="built_in">Swap</span>(g, h); <span class="comment">// use explicit template instantiation for char</span></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="decltype关键字"><code>decltype</code>关键字</h3>
<p>编译器必须通过一个检查清单来确定类型。假设我们有以下内容：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">decltype</span>(expression) var;</span><br></pre></td></tr></table></figure>
<p>下面是该清单的一个略作简化的版本。</p>
<p>阶段1：如果<code>expression</code>是一个不带括号的标识符（即没有额外的括号），那么<code>var</code>与该标识符的类型相同，包括const等限定符：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">double</span> x = <span class="number">5.5</span>;</span><br><span class="line"><span class="type">double</span> y = <span class="number">7.9</span>;</span><br><span class="line"><span class="type">double</span> &amp;rx = x;</span><br><span class="line"><span class="type">const</span> <span class="type">double</span> * pd;</span><br><span class="line"><span class="keyword">decltype</span>(x) w; <span class="comment">// w is type double</span></span><br><span class="line"><span class="keyword">decltype</span>(rx) u = y; <span class="comment">// u is type double &amp;</span></span><br><span class="line"><span class="keyword">decltype</span>(pd) v; <span class="comment">// v is type const double *</span></span><br></pre></td></tr></table></figure>
<p>阶段2：如果<code>expression</code>是一个函数调用，那么变量具有该函数返回类型：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">long</span> <span class="title">indeed</span><span class="params">(<span class="type">int</span>)</span></span>;</span><br><span class="line"><span class="keyword">decltype</span> (<span class="built_in">indeed</span>(<span class="number">3</span>)) m; <span class="comment">// m is type int</span></span><br></pre></td></tr></table></figure>
<p>阶段3：如果<code>expression</code>是左值，那么<code>var</code>就是该表达式类型的引用。这似乎意味着像<code>w</code>这样的早期示例应该是引用类型，因为<code>x</code>是左值。然而，请记住这种情况在阶段1中已经涵盖了。要应用此阶段，<code>expression</code>不能是未加括号的标识符。那么它可以是什么呢？一个明显的可能性是<strong>加括号的标识符</strong>：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">double</span> xx = <span class="number">4.4</span>;</span><br><span class="line"><span class="keyword">decltype</span> ((xx)) r2 = xx; <span class="comment">// r2 is double &amp;</span></span><br><span class="line"><span class="keyword">decltype</span>(xx) w = xx; <span class="comment">// w is double (Stage 1 match)</span></span><br></pre></td></tr></table></figure>
<p>阶段4：如果前面的特殊情况都不适用，则<code>var</code>与<code>expression</code>的类型相同：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> j = <span class="number">3</span>;</span><br><span class="line"><span class="type">int</span> &amp;k = j</span><br><span class="line"><span class="type">int</span> &amp;n = j;</span><br><span class="line"><span class="keyword">decltype</span>(j<span class="number">+6</span>) i1; <span class="comment">// i1 type int</span></span><br><span class="line"><span class="keyword">decltype</span>(<span class="number">100L</span>) i2; <span class="comment">// i2 type long</span></span><br><span class="line"><span class="keyword">decltype</span>(k+n) i3; <span class="comment">// i3 type int;</span></span><br></pre></td></tr></table></figure>
<p>如果您需要不止一个声明，可以将<code>typedef</code>与<code>decltype</code>一起使用：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">class</span> T1, <span class="keyword">class</span> T2&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">ft</span><span class="params">(T1 x, T2 y)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="function"><span class="keyword">typedef</span> <span class="title">decltype</span><span class="params">(x + y)</span> xytype</span>;</span><br><span class="line">    xytype xpy = x + y;</span><br><span class="line">    xytype arr[<span class="number">10</span>];</span><br><span class="line">    xytype &amp; rxy = arr[<span class="number">2</span>]; <span class="comment">// rxy a reference</span></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="尾置返回类型">尾置返回类型</h3>
<p><code>decltype</code>机制本身留下了另一个相关的问题未解决。考虑这个不完整的模板函数：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>, <span class="keyword">class</span> <span class="title class_">T2</span>&gt;</span><br><span class="line">?type? <span class="built_in">gt</span>(T1 x, T2 y)</span><br><span class="line">&#123;</span><br><span class="line">  ...</span><br><span class="line">  <span class="keyword">return</span> x + y;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>同样，事先并不知道x加y会得到什么类型的结果。似乎可以使用<code>decltype(x + y)</code>来作为返回类型。但遗憾的是，在代码中的那个位置，参数x和y尚未声明，因此它们不在作用域内（对编译器而言是不可见且无法使用的）。<code>decltype</code>说明符必须出现在参数声明之后。为了实现这一点，C++11允许一种新的函数声明和定义语法。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">class</span> T1, <span class="keyword">class</span> T2&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">gt</span><span class="params">(T1 x, T2 y)</span> -&gt; <span class="title">decltype</span><span class="params">(x + y)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  ...</span><br><span class="line"><span class="keyword">return</span> x + y;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>-&gt; decltype(x + y)</code>是尾置返回类型，<code>auto</code>是为尾置返回类型提供的类型占位符。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>C++ Primer Plus</tag>
      </tags>
  </entry>
  <entry>
    <title>内存模型和命名空间（七）</title>
    <url>/2024/07/30/notes-reading/computer/cpp-primer-plus/07-cpp-memory-models-namespaces/</url>
    <content><![CDATA[<p>本文记录了C++中与 内存模型和命名空间 相关的容易遗忘的一些知识。</p>
<span id="more"></span>
<hr />
<h2 id="存储周期作用域和链接性">存储周期、作用域和链接性</h2>
<h3
id="在无命名空间时变量存储类别特性">在无命名空间时变量存储类别特性</h3>
<table>
<colgroup>
<col style="width: 28%" />
<col style="width: 9%" />
<col style="width: 5%" />
<col style="width: 8%" />
<col style="width: 48%" />
</colgroup>
<thead>
<tr class="header">
<th>Storage Description</th>
<th>Duration</th>
<th>Scope</th>
<th>Linkage</th>
<th>How Declared</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Automatic</td>
<td>Automatic</td>
<td>Block</td>
<td>None</td>
<td>In a block</td>
</tr>
<tr class="even">
<td>Register</td>
<td>Automatic</td>
<td>Block</td>
<td>None</td>
<td>In a block with the keyword <code>register</code></td>
</tr>
<tr class="odd">
<td>Static with no linkage</td>
<td>Static</td>
<td>Block</td>
<td>None</td>
<td>In a block with the keyword <code>static</code></td>
</tr>
<tr class="even">
<td>Static with external linkage</td>
<td>Static</td>
<td>File</td>
<td>External</td>
<td>Outside all functions</td>
</tr>
<tr class="odd">
<td>Static with internal linkage</td>
<td>Static</td>
<td>File</td>
<td>Internal</td>
<td>Outside all functions with the keyword <code>static</code></td>
</tr>
</tbody>
</table>
<h3 id="语言链接性">语言链接性</h3>
<p>链接器需要为每个不同的函数分配一个不同的符号名称。在C语言中，这很容易实现，因为对于一个给定的名称，只能有一个C函数。因此，出于内部目的，C编译器可能会将诸如<code>spiff</code>之类的C函数名翻译为<code>_spiff</code>。C语言的处理方式称为C语言链接。然而，C++中可以有多个具有相同名称的函数，这些函数必须被转换为不同的符号名称。因此，C++编译器会进行名称修饰（或称为名称改编）过程，为重载函数生成不同的符号名称。例如，它可能会将<code>spiff(int)</code>转换为诸如<code>_spiff_i</code>之类的名称，将<code>spiff(double, double)</code>转换为<code>_spiff_d_d</code>。这种C++的处理方式称为C++语言链接。</p>
<p>当链接器查找与C++函数调用相匹配的函数时，它所使用的查找方法与查找匹配C函数调用的方法不同。但假设你想在C++程序中使用来自C库的预编译函数，该怎么办呢？例如，假设你有这样的代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">spiff</span>(<span class="number">22</span>); <span class="comment">// want spiff(int) from a C library</span></span><br></pre></td></tr></table></figure>
<p>它在C库文件中的假设符号名称是<code>_spiff</code>，但对于我们假设的链接程序来说，C++的查找约定是查找符号名称<code>_spiff_i</code>。为了解决这个问题，可以使用函数原型来指明要使用的协议：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="function"><span class="type">void</span> <span class="title">spiff</span><span class="params">(<span class="type">int</span>)</span></span>; <span class="comment">// use C protocol for name look-up</span></span><br><span class="line"><span class="function"><span class="keyword">extern</span> <span class="type">void</span> <span class="title">spoff</span><span class="params">(<span class="type">int</span>)</span></span>; <span class="comment">// use C++ protocol for name look-up</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C++&quot;</span> <span class="function"><span class="type">void</span> <span class="title">spaff</span><span class="params">(<span class="type">int</span>)</span></span>; <span class="comment">// use C++ protocol for name look-up</span></span><br></pre></td></tr></table></figure>
<p>这里的第一个示例使用C语言链接。第二个和第三个示例使用C++语言链接；第二个示例默认采用C++语言链接，第三个示例则显式采用C++语言链接。</p>
<h3 id="new运算符函数和替换函数">new：运算符、函数和替换函数</h3>
<p><code>new</code>和<code>new[]</code>运算符会调用两个函数：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> * <span class="keyword">operator</span> <span class="title">new</span><span class="params">(std::<span class="type">size_t</span>)</span></span>; <span class="comment">// 被new使用</span></span><br><span class="line"><span class="type">void</span> * <span class="keyword">operator</span> <span class="keyword">new</span>[](std::<span class="type">size_t</span>); <span class="comment">// 被new[]使用</span></span><br></pre></td></tr></table></figure>
<p>这些被称为分配函数，它们属于全局命名空间。同样地，有供<code>delete</code>和<code>delete[]</code>使用的释放函数：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="type">void</span> *)</span></span>;</span><br><span class="line"><span class="type">void</span> <span class="keyword">operator</span> <span class="keyword">delete</span>[](<span class="type">void</span> *);</span><br></pre></td></tr></table></figure>
<p>像这样的基本语句：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> * pi = <span class="keyword">new</span> <span class="type">int</span>;</span><br></pre></td></tr></table></figure>
<p>会被转换为类似这样的形式：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> * pi = <span class="built_in">new</span>(<span class="built_in">sizeof</span>(<span class="type">int</span>));</span><br></pre></td></tr></table></figure>
<p>而语句</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> * pa = <span class="keyword">new</span> <span class="type">int</span>[<span class="number">40</span>];</span><br></pre></td></tr></table></figure>
<p>会被转换为类似这样的形式：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> * pa = <span class="built_in">new</span>(<span class="number">40</span> * <span class="built_in">sizeof</span>(<span class="type">int</span>));</span><br></pre></td></tr></table></figure>
<p>有趣的是，C++将这些函数称为可替换的。这意味着如果你有足够的专业知识和需求，你可以提供<code>new</code>和<code>delete</code>的替换函数，并根据自己的特定要求进行定制。例如，一种选择是在类作用域中定义替换函数，以便它们能满足特定类的分配需求。你的代码会像往常一样使用<code>new</code>运算符，但此时<code>new</code>运算符会调用替换的<code>new()</code>函数。</p>
<h3 id="定位new运算符">定位new运算符</h3>
<p>要使用定位new特性，首先需要包含
<code>&lt;new&gt;</code>，该头文件提供了这种版本<code>new</code>的原型。然后，使用带参数的<code>new</code>，该参数提供预期的地址。除了这个参数外，其语法与常规<code>new</code>相同。特别是，定位<code>new</code>既可以不带括号使用，也可以带括号使用。</p>
<p>就像常规的<code>new</code>调用带一个参数的<code>new</code>函数一样，标准的定位new会调用带两个参数的<code>new</code>函数：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span> buffer[BUF]; <span class="comment">// chunk of memory</span></span><br><span class="line"><span class="type">int</span> * pi = <span class="keyword">new</span> <span class="type">int</span>;       <span class="comment">// invokes new(sizeof(int))</span></span><br><span class="line"><span class="type">int</span> * p2 = <span class="built_in">new</span>(buffer) <span class="type">int</span>;   <span class="comment">// invokes new(sizeof(int), buffer)</span></span><br><span class="line"><span class="type">int</span> * p3 = <span class="built_in">new</span>(buffer) <span class="type">int</span>[<span class="number">40</span>]; <span class="comment">// invokes new(40*sizeof(int), buffer)</span></span><br></pre></td></tr></table></figure>
<p>定位<code>new</code>函数不可替换，但可以重载。它至少需要两个参数，第一个参数始终是一个<code>std::size_t</code>参数，用于指定请求的字节数。任何此类重载函数都被称为定位<code>new</code>，即使额外的参数没有指定位置也是如此。</p>
<hr />
<h2 id="命名空间">命名空间</h2>
<p>命名空间可以位于全局级别，也可以位于其他命名空间内部，但不能放在块中。因此，在命名空间中声明的名称默认具有外部链接（除非它指向常量，常量自动具有内部链接）。</p>
<p>除了用户定义的命名空间外，还有一个命名空间，即全局命名空间。它对应于文件级别的声明区域，因此以前被称为全局变量的东西现在被描述为全局命名空间的一部分。</p>
<h3 id="using-声明和-using-指令">using 声明和 using 指令</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;  <span class="comment">// using 指令</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> std::cout;    <span class="comment">// using 声明</span></span><br><span class="line"><span class="keyword">using</span> std::cin;</span><br><span class="line"><span class="keyword">using</span> std::endl;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>[!NOTE]</p>
<p>假设一个命名空间和一个声明区域都定义了相同的名称。如果尝试使用using声明将该命名空间名称引入到声明区域中，这两个名称会产生冲突，从而导致错误。如果使用using指令将该命名空间名称引入到声明区域中，则该名称的局部版本会隐藏命名空间版本（using指令会将命名空间名称视为在函数外部声明的，但这并不会使这些名称对文件中的其他函数可用）。</p>
</blockquote>
<h3 id="未命名命名空间">未命名命名空间</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> <span class="comment">// unnamed namespace</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> ice;</span><br><span class="line">    <span class="type">int</span> bandycoot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>不能在包含未命名命名空间声明的文件以外的其他文件中使用该未命名命名空间中的名称。这提供了一种替代方案，可用于替代具有内部链接的静态变量。</p>
<h3 id="命名空间惯用法">命名空间惯用法</h3>
<ul>
<li>在命名空间中使用变量，而不是使用外部全局变量。</li>
<li>使用未命名命名空间中的变量，而不是使用静态全局变量。</li>
<li>如果你开发一个函数或类的库，请将它们放在命名空间中。</li>
<li>仅将using指令用作将旧代码转换为命名空间使用方式的临时手段。</li>
<li>不要在头文件中使用using指令；一方面，这样做会隐藏哪些名称被公开了。此外，头文件的顺序可能会影响程序的行为。如果要使用using指令，请将其放在所有预处理<code>#include</code>指令之后。</li>
<li>通过使用作用域解析运算符<code>::</code>或using声明来优先导入名称。</li>
<li>使用using声明时，优先使用局部作用域而非全局作用域。</li>
</ul>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>C++ Primer Plus</tag>
      </tags>
  </entry>
  <entry>
    <title>对象和类（八）</title>
    <url>/2024/08/07/notes-reading/computer/cpp-primer-plus/08-cpp-objects-classes/</url>
    <content><![CDATA[<p>本文记录了C++中与 对象和类 相关的容易遗忘的一些知识。</p>
<span id="more"></span>
<hr />
<blockquote>
<p>面向对象编程（OOP）最重要的特性：</p>
<ul>
<li>抽象</li>
<li>封装与数据隐藏</li>
<li>多态性</li>
<li>继承</li>
<li>代码的可重用性</li>
</ul>
</blockquote>
<hr />
<h2 id="抽象和类">抽象和类</h2>
<p>指定一种基本类型会起到三个作用：</p>
<ul>
<li>它决定了一个数据对象需要多少内存。</li>
<li>它决定了内存中的位如何被解释。（长整数和浮点数在内存中可能使用相同数量的位，但它们被转换为数值的方式不同。）</li>
<li>它决定了可以使用该数据对象执行哪些操作或方法。</li>
</ul>
<p>将实现细节整合在一起并将它们与抽象分离开来，这被称为封装。</p>
<p>类的基本思想是数据抽象和封装。数据抽象是一种依赖于接口和实现分离的编程（以及设计）技术。<strong>类的接口包括用户所能执行的操作</strong>；类的实现则包括类的数据成员、负责接口实现的函数体以及定义类所需的各种私有函数。</p>
<hr />
<h2 id="类作用域">类作用域</h2>
<h3 id="有作用域枚举">有作用域枚举</h3>
<p>为了解决如下形式的处于同一作用域，枚举值名称冲突：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="title class_">egg</span> &#123;Small, Medium, Large, Jumbo&#125;;</span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">t_shirt</span> &#123;Small, Medium, Large, Xlarge&#125;;</span><br></pre></td></tr></table></figure>
<p>C++11提供了一种新的枚举形式，其枚举器具有类作用域，从而避免了这一问题。这种形式的声明如下所示：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum class</span> <span class="title class_">egg</span> &#123;Small, Medium, Large, Jumbo&#125;;</span><br><span class="line"><span class="keyword">enum class</span> <span class="title class_">t_shirt</span> &#123;Small, Medium, Large, Xlarge&#125;;</span><br></pre></td></tr></table></figure>
<p>现在需要使用枚举名称来限定枚举数：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">egg choice = egg::Large; <span class="comment">// the Large enumerator of the egg enum</span></span><br><span class="line">t_shirt Floyd = t_shirt::Large; <span class="comment">// the Large enumerator of the t_shirt enum</span></span><br></pre></td></tr></table></figure>
<p>C++11还加强了作用域枚举的类型安全性。常规枚举在某些情况下会自动转换为整数类型，例如赋值给<code>int</code>变量或在比较表达式中使用，但作用域枚举不会隐式转换为整数类型。</p>
<p>默认情况下，C++11 作用域枚举的底层类型是
<code>int</code>，此外，有一种语法用于表示不同的选择：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// underlying type for pizza is short</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">class</span> : <span class="type">short</span> pizza &#123;Small, Medium, Large, XLarge&#125;;</span><br></pre></td></tr></table></figure>
<p><code>: short</code> 指定基础类型为
<code>short</code>。基础类型必须是整数类型。在 C++11
标准下，也可以使用这种语法来指定无作用域枚举的基础类型，但如果你不选择该类型，编译器所做的选择则取决于具体实现。</p>
<hr />
<h2 id="运算符重载">运算符重载</h2>
<h3 id="重载限制">重载限制</h3>
<ol type="1">
<li><p>重载的运算符必须至少有一个操作数是用户定义的类型。这会阻止你对标准类型重载运算符。</p></li>
<li><p>不能以违反原始运算符语法规则的方式使用运算符。例如，不能重载取模运算符（%），使其可以用于单个操作数。</p></li>
<li><p>不能创建新的运算符符号。例如，不能定义一个
<code>operator**()</code> 函数来表示幂运算。</p></li>
<li><p>以下运算符不能重载：</p>
<table>
<thead>
<tr class="header">
<th>Operator</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>sizeof</code></td>
<td>The sizeof operator</td>
</tr>
<tr class="even">
<td><code>.</code></td>
<td>The membership operator</td>
</tr>
<tr class="odd">
<td><code>.*</code></td>
<td>The pointer-to-member operator</td>
</tr>
<tr class="even">
<td><code>::</code></td>
<td>The scope-resolution operator</td>
</tr>
<tr class="odd">
<td><code>?:</code></td>
<td>The conditional operator</td>
</tr>
<tr class="even">
<td><code>typeid</code></td>
<td>An RTTI operator</td>
</tr>
<tr class="odd">
<td><code>const_cast</code></td>
<td>A type cast operator</td>
</tr>
<tr class="even">
<td><code>dynamic_cast</code></td>
<td>A type cast operator</td>
</tr>
<tr class="odd">
<td><code>reinterpret_cast</code></td>
<td>A type cast operator</td>
</tr>
<tr class="even">
<td><code>static_cast</code></td>
<td>A type cast operator</td>
</tr>
</tbody>
</table></li>
<li><p>只能使用成员函数（不支持非成员函数）来重载以下运算符：</p>
<table>
<thead>
<tr class="header">
<th>Operator</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>=</code></td>
<td>Assignment operator</td>
</tr>
<tr class="even">
<td><code>()</code></td>
<td>Function call operator</td>
</tr>
<tr class="odd">
<td><code>[]</code></td>
<td>Subscripting operator</td>
</tr>
<tr class="even">
<td><code>-&gt;</code></td>
<td>Class member access by pointer operator</td>
</tr>
</tbody>
</table></li>
</ol>
<hr />
<h2 id="类型转换">类型转换</h2>
<blockquote>
<p>应当谨慎使用隐式转换函数。通常，显式调用的函数才是最佳选择。</p>
</blockquote>
<h3 id="类型转换构造函数">类型转换构造函数</h3>
<p>只有一个参数的类构造函数充当着将参数类型的值转换为该类类型的指令。</p>
<p>关键字<code>explicit</code>，用于关闭自动转换功能，即在构造函数声明前加<code>explicit</code>。</p>
<h3 id="转换函数">转换函数</h3>
<p>一种被称为转换函数的特殊类成员运算符函数，充当着将类对象转换为其他某种类型的指令。</p>
<p>要转换为<code>typeName</code>类型，需使用以下形式的转换函数：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">operator</span> <span class="title">typeName</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>
<p>当你将类对象赋值给该类型<code>typeName</code>的变量，或者使用该类型<code>typeName</code>的强制类型转换运算符时，这个转换函数会被自动调用。</p>
<p>请注意以下几点：</p>
<ul>
<li>转换函数必须是类方法。</li>
<li>转换函数不得指定返回类型。</li>
<li>转换函数不得有参数。</li>
</ul>
<p>例如，一个转换为<code>double</code>类型的函数会有这样的原型：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">operator</span> <span class="title">double</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>
<p>关键字<code>explicit</code>，用于关闭隐式转换功能。</p>
<hr />
<h2 id="特殊成员函数">特殊成员函数</h2>
<p>C++会自动提供以下成员函数：</p>
<ul>
<li>如果你没有定义任何构造函数，会有一个默认构造函数</li>
<li>如果你没有定义的话，会有一个默认的析构函数</li>
<li>如果你不定义一个拷贝构造函数的话</li>
<li>如果你不定义赋值运算符的话，系统会提供一个赋值运算符</li>
<li>如果你不定义地址运算符的话</li>
</ul>
<h3 id="默认构造函数">默认构造函数</h3>
<p>默认构造函数是指没有参数的构造函数，或者所有参数都有默认值的构造函数。如果你没有定义任何构造函数，编译器会为你定义一个默认构造函数。它的存在使你能够创建对象。</p>
<p>自动默认构造函数还有一个作用，就是为任何基类以及任何属于其他类对象的成员调用默认构造函数。</p>
<p>此外，如果你在编写派生类构造函数时，没有在成员初始化列表中显式调用基类构造函数，编译器会使用基类的默认构造函数来构造新对象中的基类部分。如果不存在基类默认构造函数，在这种情况下就会出现编译时错误。</p>
<p>如果你定义了任何类型的构造函数，编译器都不会为你定义默认构造函数。在这种情况下，如果需要默认构造函数，就需要你自己来提供。</p>
<h3 id="拷贝构造函数">拷贝构造函数</h3>
<p>类的拷贝构造函数是一种以该类类型的对象作为参数的构造函数。通常，其声明的参数是对该类类型的常量引用。</p>
<p>类的拷贝构造函数用于以下情况：</p>
<ul>
<li>当一个新对象被初始化为同一类的对象时</li>
<li>当对象通过值传递给函数时</li>
<li>当函数通过值返回对象时</li>
<li>当编译器生成临时对象时</li>
</ul>
<p>调用时机：拷贝构造函数在每当<strong>创建新对象</strong>并将其初始化为同一类型的现有对象时被调用。</p>
<p>如果一个程序没有（显式或隐式地）使用拷贝构造函数，编译器会提供一个原型，但不会提供函数定义。否则，程序会定义一个执行成员逐一初始化的拷贝构造函数。也就是说，新对象的每个成员都会被初始化为原始对象对应成员的值。如果某个成员本身是一个类对象，那么成员逐一初始化会使用为该特定类定义的拷贝构造函数。</p>
<p>在某些情况下，成员逐一初始化并不可取。例如，用<code>new</code>初始化的成员指针通常需要进行深拷贝。或者，某个类可能包含一个需要修改的静态变量。在这些情况下，你需要定义自己的拷贝构造函数。</p>
<h3 id="赋值运算符">赋值运算符</h3>
<p>默认的赋值运算符用于处理将一个对象赋值给同一个类的另一个对象。不要将赋值与初始化混淆。如果一条语句创建了一个新对象，那它使用的是初始化；如果一条语句更改了现有对象的值，那它就是赋值。</p>
<p>默认赋值使用逐成员赋值。如果某个成员本身是类对象，那么默认的逐成员赋值会使用为该特定类定义的赋值运算符。如果需要显式定义复制构造函数，出于同样的原因，也需要显式定义赋值运算符。</p>
<p>注意事项：</p>
<ul>
<li>由于目标对象可能已经引用了先前分配的数据，该函数应使用<code>delete []</code>来释放之前的内存占用。</li>
<li>该函数应防止将对象赋值给自身；否则，前面描述的内存释放操作可能会在对象内容被重新赋值之前就将其清除。</li>
<li>该函数返回对调用对象的引用。</li>
</ul>
<p>示例：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">StringBad &amp; StringBad::<span class="keyword">operator</span>=(<span class="type">const</span> StringBad &amp; st)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span> == &amp;st) <span class="comment">// object assigned to itself</span></span><br><span class="line">        <span class="keyword">return</span> *<span class="keyword">this</span>; <span class="comment">// all done</span></span><br><span class="line">    <span class="keyword">delete</span> [] str; <span class="comment">// free old string</span></span><br><span class="line">    len = st.len;</span><br><span class="line">    str = <span class="keyword">new</span> <span class="type">char</span> [len + <span class="number">1</span>]; <span class="comment">// get space for new string</span></span><br><span class="line">    std::<span class="built_in">strcpy</span>(str, st.str); <span class="comment">// copy the string</span></span><br><span class="line">    <span class="keyword">return</span> *<span class="keyword">this</span>; <span class="comment">// return reference to invoking object</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>赋值不会创建新对象，因此不必调整静态数据成员<code>num_strings</code>的值。</p>
<hr />
<h2 id="动态内存分配">动态内存分配</h2>
<p>通常情况下，在程序运行时决定许多事情（例如使用多少存储空间）要比在编译时决定好得多。</p>
<p>静态数据成员在类声明中声明，并在包含类方法的文件中初始化。初始化时使用作用域运算符来指明该静态成员属于哪个类。不过，如果静态成员是<code>const</code>整数类型或枚举类型，则可以在类声明中直接初始化。</p>
<h3 id="构造函数中使用new">构造函数中使用new</h3>
<p>在使用<code>new</code>来初始化对象的指针成员时，必须格外小心。具体来说，应该执行以下操作：</p>
<ul>
<li>如果你在构造函数中使用<code>new</code>来初始化指针成员，那么你应该在析构函数中使用<code>delete</code>。</li>
<li><code>new</code>和<code>delete</code>的使用应当兼容。你应当将<code>new</code>与<code>delete</code>配对使用，将<code>new []</code>与<code>delete []</code>配对使用。</li>
<li>如果存在多个构造函数，所有构造函数都应采用相同的方式使用<code>new</code>（要么都带括号，要么都不带括号）。析构函数只有一个，因此所有构造函数都必须与该析构函数兼容。不过，在一个构造函数中用<code>new</code>初始化指针，而在另一个构造函数中用空指针（<code>0</code>，或者在C++11中用<code>nullptr</code>）初始化指针是允许的，因为对空指针执行<code>delete</code>操作（带或不带括号）都是可行的。</li>
<li>应该定义一个拷贝构造函数，通过深拷贝将一个对象初始化为另一个对象。</li>
<li>应该定义一个赋值运算符，通过深拷贝将一个对象复制到另一个对象。</li>
</ul>
<p>示例：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">StringBad::<span class="built_in">StringBad</span>(<span class="type">const</span> StringBad &amp; st)</span><br><span class="line">&#123;</span><br><span class="line">    num_strings++; <span class="comment">// handle static member update</span></span><br><span class="line">    len = st.len; <span class="comment">// same length</span></span><br><span class="line">    str = <span class="keyword">new</span> <span class="type">char</span> [len + <span class="number">1</span>]; <span class="comment">// allot space</span></span><br><span class="line">    std::<span class="built_in">strcpy</span>(str, st.str); <span class="comment">// copy string to new location</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="成员变量初始化">成员变量初始化</h2>
<h3 id="构造函数初始化列表">构造函数初始化列表</h3>
<p>如果<strong>成员是<code>const</code>、引用，或者属于某种未提供默认构造函数</strong>的类类型，必须通过构造函数初始值列表为这些成员提供初值。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 成员初始化列表语法</span></span><br><span class="line">TableTennisPlayer::<span class="built_in">TableTennisPlayer</span> (<span class="type">const</span> string &amp; fn, <span class="type">const</span> string &amp; ln, <span class="type">bool</span> ht) : <span class="built_in">firstname</span>(fn),</span><br><span class="line"><span class="built_in">lastname</span>(ln), <span class="built_in">hasTable</span>(ht) &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 函数体内赋值</span></span><br><span class="line">TableTennisPlayer::<span class="built_in">TableTennisPlayer</span> (<span class="type">const</span> string &amp; fn,</span><br><span class="line">                                      <span class="type">const</span> string &amp; ln, <span class="type">bool</span> ht)</span><br><span class="line">&#123;</span><br><span class="line">    firstname = fn;</span><br><span class="line">    lastname = ln;</span><br><span class="line">    hasTable = ht;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>成员初始化列表语法通过直接使用拷贝构造函数初始化，只用一个步骤。效率更高。</p>
<p>函数体内赋值是先为类内成员调用默认的构造函数构造出一个对象，然后调用赋值运算符对齐赋值。多了一个赋值的动作。</p>
<p>对于基本类型的成员而言没有区别。</p>
<h3 id="类内初始化">类内初始化</h3>
<p>C++11允许类内初始化（即在类定义中进行初始化）：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Queue</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    ...</span><br><span class="line">    Node * front = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">enum</span> &#123;Q_SIZE = <span class="number">10</span>&#125;;</span><br><span class="line">    Node * rear = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="type">int</span> items = <span class="number">0</span>;</span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> qsize = Q_SIZE;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>这相当于使用成员初始化列表。不过，任何使用成员初始化列表的构造函数都会覆盖相应的类内初始化。</p>
<hr />
<h2 id="友元">友元</h2>
<p>C++控制对类对象私有部分的访问。通常，公共类方法是唯一的访问途径，但有时这种限制对于特定的编程问题来说过于严格。在这种情况下，C++提供了另一种访问形式：友元。友元分为三种类型：</p>
<ul>
<li>友元函数</li>
<li>友元类</li>
<li>友元成员函数</li>
</ul>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>C++ Primer Plus</tag>
      </tags>
  </entry>
  <entry>
    <title>类的继承（九）</title>
    <url>/2024/08/14/notes-reading/computer/cpp-primer-plus/09-cpp-class-inheritance/</url>
    <content><![CDATA[<p>本文记录了C++中与 类的继承 相关的容易遗忘的一些知识。</p>
<span id="more"></span>
<hr />
<h2 id="构造与析构">构造与析构</h2>
<p>创建派生类的对象时，程序会先调用基类的构造函数，然后再调用派生类的构造函数。</p>
<p>基类的构造函数负责初始化继承的数据成员。派生类的构造函数负责初始化任何新增的数据成员。</p>
<p>派生类的构造函数总会调用基类的构造函数。你可以使用初始化列表语法来指定要使用哪个基类构造函数。否则，将使用默认的基类构造函数。</p>
<p>当派生类的对象销毁时，程序会先调用派生类的析构函数，然后再调用基类的析构函数。</p>
<hr />
<h2 id="静态绑定与动态绑定">静态绑定与动态绑定</h2>
<p>C++规定了虚函数应该如何运行，但将其实现方式留给了编译器编写者。不需要了解实现方法就能使用虚函数，但了解它是如何实现的或许能帮助你更好地理解相关概念，下面是一种实现方式。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20251226140536133.png" alt="虚函数机制" style="zoom:67%;" /></p>
<p>使用虚函数在内存和执行速度方面会产生以下适度的开销：</p>
<ul>
<li>每个对象的大小都会增加，增加的量为容纳一个地址所需的空间。</li>
<li>编译器会为每个类创建一个（数组形式的）虚函数地址表。</li>
<li>对于每次函数调用，都会多一个步骤：去表中查找地址。</li>
</ul>
<p>要记住，尽管非虚函数比虚函数效率略高，但它们不提供动态绑定。</p>
<hr />
<h2 id="抽象基类">抽象基类</h2>
<p>C++有一种通过使用纯虚函数来提供未实现函数的方法。纯虚函数在其声明的末尾带有
<code>= 0</code>，正如<code>Area()</code>方法所示：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">BaseEllipse</span> <span class="comment">// abstract base class</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">double</span> x; <span class="comment">// x-coordinate of center</span></span><br><span class="line">    <span class="type">double</span> y; <span class="comment">// y-coordinate of center</span></span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">BaseEllipse</span>(<span class="type">double</span> x0 = <span class="number">0</span>, <span class="type">double</span> y0 = <span class="number">0</span>) : <span class="built_in">x</span>(x0),<span class="built_in">y</span>(y0) &#123;&#125;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">BaseEllipse</span>() &#123;&#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">Move</span><span class="params">(<span class="type">int</span> nx, ny)</span> </span>&#123; x = nx; y = ny; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">double</span> <span class="title">Area</span><span class="params">()</span> <span class="type">const</span> </span>= <span class="number">0</span>; <span class="comment">// a pure virtual function</span></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当一个类声明包含纯虚函数时，就不能创建该类的对象。其理念是，包含纯虚函数的类仅作为基类存在。</p>
<p>简而言之，原型中的
<code>=0</code>表明该类是一个抽象基类，并且该类不一定非要定义这个函数（也可以定义）。<code>= 0</code>
的作用是告诉编译器
“这个函数是纯虚的，该类是抽象类”，它只是一个标识，而非 “禁止定义”
的指令。编译器允许你为纯虚函数编写函数体，只是函数体不能写在类的内部，必须写在类外部。</p>
<h3 id="abc哲学">ABC哲学</h3>
<p>ABC方法是一种系统性和条理性更强的处理继承的方式。在设计ABC之前，首先必须构建一个模型，明确需要哪些类来表示一个编程问题，以及这些类之间是如何相互关联的。一种观点认为，如果设计了一个类的继承层次结构，那么唯一的具体类应该是那些永远不会作为基类的类。这种方法往往能产生更简洁的设计，减少复杂性。</p>
<p>一种理解ABC的方式是将其视为对接口的一种强制实施。ABC要求其纯虚函数必须在任何具体的派生类中被重写，这就迫使派生类遵守ABC所设定的接口规则。这种模式在基于组件的编程范式中很常见，在这种范式下，ABC的使用使组件设计者能够创建一份“接口契约”，所有从该ABC派生的组件都能保证至少支持ABC所规定的通用功能。</p>
<hr />
<h2 id="成员函数属性总结">成员函数属性总结</h2>
<table>
<colgroup>
<col style="width: 15%" />
<col style="width: 10%" />
<col style="width: 16%" />
<col style="width: 20%" />
<col style="width: 14%" />
<col style="width: 22%" />
</colgroup>
<thead>
<tr class="header">
<th>Function</th>
<th>Inherited</th>
<th>Member or Friend</th>
<th>Generated by Default</th>
<th>Can Be Virtual</th>
<th>Can Have a Return Type</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Constructor</td>
<td>No</td>
<td>Member</td>
<td>Yes</td>
<td>No</td>
<td>No</td>
</tr>
<tr class="even">
<td>Destructor</td>
<td>No</td>
<td>Member</td>
<td>Yes</td>
<td>Yes</td>
<td>No</td>
</tr>
<tr class="odd">
<td><code>=</code></td>
<td>No</td>
<td>Member</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr class="even">
<td><code>&amp;</code></td>
<td>Yes</td>
<td>Either</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr class="odd">
<td>Conversion</td>
<td>Yes</td>
<td>Member</td>
<td>No</td>
<td>Yes</td>
<td>No</td>
</tr>
<tr class="even">
<td><code>()</code></td>
<td>Yes</td>
<td>Member</td>
<td>No</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr class="odd">
<td><code>[]</code></td>
<td>Yes</td>
<td>Member</td>
<td>No</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr class="even">
<td><code>-&gt;</code></td>
<td>Yes</td>
<td>Member</td>
<td>No</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr class="odd">
<td><code>op=</code></td>
<td>Yes</td>
<td>Either</td>
<td>No</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr class="even">
<td><code>new</code></td>
<td>Yes</td>
<td>Static member</td>
<td>No</td>
<td>No</td>
<td>void*</td>
</tr>
<tr class="odd">
<td><code>delete</code></td>
<td>Yes</td>
<td>Static member</td>
<td>No</td>
<td>No</td>
<td>void</td>
</tr>
<tr class="even">
<td>Other operators</td>
<td>Yes</td>
<td>Either</td>
<td>No</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr class="odd">
<td>Other members</td>
<td>Yes</td>
<td>Member</td>
<td>No</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr class="even">
<td>Friends</td>
<td>No</td>
<td>Friend</td>
<td>No</td>
<td>No</td>
<td>Yes</td>
</tr>
</tbody>
</table>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>C++ Primer Plus</tag>
      </tags>
  </entry>
  <entry>
    <title>代码复用（十）</title>
    <url>/2024/08/21/notes-reading/computer/cpp-primer-plus/10-cpp-reusing-code/</url>
    <content><![CDATA[<p>本文记录了C++中与 代码复用 相关的容易遗忘的一些知识。</p>
<span id="more"></span>
<hr />
<blockquote>
<p>C++的主要目标之一是促进代码的重用。代码复用的机制有以下几种：</p>
<ul>
<li><p>公有继承是实现这一目标的一种机制，但并非唯一机制。</p></li>
<li><p>一种技术是使用本身就是 另一个类的对象
的类成员，这被称为包含、组合或分层。</p></li>
<li><p>另一种选择是使用私有继承或保护继承。</p></li>
<li><p>包含、私有继承和保护继承通常用于实现“有一个”（has-a）关系。</p></li>
<li><p>类（函数）模板，提供了另一种代码重用的方式。</p></li>
</ul>
</blockquote>
<hr />
<h2 id="包含对象成员的类">包含对象成员的类</h2>
<h3 id="接口与实现">接口与实现</h3>
<p>通过公有继承，一个类会继承接口，或许还会继承实现。（基类中的纯虚函数可以提供接口而不提供实现。）获得接口是“是一个”关系的一部分。另一方面，通过组合，一个类会获得实现但不会获得接口。不继承接口是“有一个”关系的一部分。</p>
<h3 id="成员初始化列表顺序">成员初始化列表顺序</h3>
<p>当你有一个成员初始化列表来初始化多个项时，这些项的初始化顺序是它们在类中被声明的顺序，而不是它们在初始化列表中或构造函数参数的书写顺序。</p>
<p>例如，假设编写一个Student构造函数：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Student</span>(<span class="type">const</span> <span class="type">char</span> * str, <span class="type">const</span> <span class="type">double</span> * pd, <span class="type">int</span> n)</span><br><span class="line">    : <span class="built_in">scores</span>(pd, n), <span class="built_in">name</span>(str) &#123;&#125;</span><br></pre></td></tr></table></figure>
<p>由于<code>name</code>成员在类定义中是首先声明的，所以它仍会首先被初始化。对于这个例子来说，确切的初始化顺序并不重要，但如果代码将一个成员的值用作另一个成员的初始化表达式的一部分，那么初始化顺序就会很重要。</p>
<hr />
<h2 id="私有保护继承">私有/保护继承</h2>
<h3 id="私有继承">私有继承</h3>
<p>通过私有继承，基类的公有成员和保护成员会成为派生类的私有成员。这意味着基类的方法不会成为派生类对象的公有接口的一部分。不过，它们可以在派生类的成员函数内部使用。</p>
<p>在公有继承中，基类的公有方法会成为派生类的公有方法。简而言之，派生类继承了基类的接口。这是“是一个”关系的一部分。而在私有继承中，基类的公有方法会成为派生类的私有方法。简而言之，派生类不会继承基类的接口。正如在包含对象的情况中所看到的，这种不继承接口是“有一个”关系的一部分。</p>
<p>包含是将一个对象作为命名的成员对象添加到类中，而私有继承是将一个对象作为未命名的继承对象添加到类中。</p>
<p>因此，私有继承提供了与包含相同的特性：获得实现，但不获得接口。所以，它也可以用来实现“有一个”关系。</p>
<h3 id="保护继承">保护继承</h3>
<p>受保护继承是私有继承的一种变体。受保护继承和私有继承之间的主要区别在于，当从派生类再派生出另一个类时。在私有继承的情况下，这个第三代类无法在内部使用基类的接口。这是因为基类的公有方法会变成在派生类中是私有的，并且私有成员和方法不能被下一级派生类直接访问。在保护继承的情况下，基类的公共方法在第二代中会变成受保护的，因此可被下一级派生类内部使用。</p>
<p>一般来说，应该使用包含来建模“有一个”（has-a）关系。如果新类需要访问原始类中的受保护成员，或者需要重定义虚函数，那么应该使用私有继承。</p>
<h3 id="继承的种类">继承的种类</h3>
<p>下表总结了公有继承、私有继承和保护继承。术语
<code>implicit upcasting</code>
指的是无需使用显式类型转换，就可以让基类指针或引用指向派生类对象。</p>
<table style="width:100%;">
<colgroup>
<col style="width: 14%" />
<col style="width: 28%" />
<col style="width: 28%" />
<col style="width: 28%" />
</colgroup>
<thead>
<tr class="header">
<th>Property</th>
<th>Public Inheritance</th>
<th>Protected Inheritance</th>
<th>Private Inheritance</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Public members become</td>
<td>Public members of the derived class</td>
<td>Protected members of the derived class</td>
<td>Private members of the derived class</td>
</tr>
<tr class="even">
<td>Protected members become</td>
<td>Protected members of the derived class</td>
<td>Protected members of the derived class</td>
<td>Private members of the derived class</td>
</tr>
<tr class="odd">
<td>Private members become</td>
<td>Accessible only through the base-class interface</td>
<td>Accessible only through the base-class interface</td>
<td>Accessible only through the base-class interface</td>
</tr>
<tr class="even">
<td>Implicit upcasting</td>
<td>Yes</td>
<td>Yes (but only within the derived class)</td>
<td>No</td>
</tr>
</tbody>
</table>
<h3 id="用using重定义访问方式">用<code>using</code>重定义访问方式</h3>
<p>当使用保护派生或私有派生时，基类的公有成员会变成保护成员或私有成员。假设希望让基类的某个特定方法在派生类中可以公开使用，一种选择是定义一个派生类方法来调用该基类方法。例如，假设你希望<code>Student</code>类能够使用<code>valarray</code>的<code>sum()</code>方法，可以在类声明中声明一个<code>sum()</code>方法，然后像这样定义该方法：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">double</span> <span class="title">Student::sum</span><span class="params">()</span> <span class="type">const</span> <span class="comment">// 公有Student方法</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> std::valarray&lt;<span class="type">double</span>&gt;::<span class="built_in">sum</span>(); <span class="comment">// 使用私有继承的方法</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>一种替代方案可以不用将一个函数调用嵌套在另一个函数调用中，那就是使用<code>using</code>声明（就像在命名空间中使用的那些声明一样）来表明，即使派生是私有继承的，派生类也可以使用某个特定的基类成员。例如，假设你希望能够将<code>valarray</code>的<code>min()</code>和<code>max()</code>方法在<code>Student</code>中使用，在这种情况下可以，在公共部分添加<code>using</code>声明：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> : <span class="keyword">private</span> std::string, <span class="keyword">private</span> std::valarray&lt;<span class="type">double</span>&gt;</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">using</span> std::valarray&lt;<span class="type">double</span>&gt;::min;</span><br><span class="line">    <span class="keyword">using</span> std::valarray&lt;<span class="type">double</span>&gt;::max;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p><code>using</code>声明使得<code>valarray&lt;double&gt;::min()</code>和<code>valarray&lt;double&gt;::max()</code>方法可用，就好像它们是公共的<code>Student</code>方法一样。</p>
<blockquote>
<p><code>using</code>声明方法仅适用于继承，不适用于包含。</p>
</blockquote>
<hr />
<h2 id="多重继承">多重继承</h2>
<p>多重继承的两个主要问题是：从两个不同的基类继承具有相同名称的不同方法，以及通过两个或多个相关的直接基类继承一个类的多个实例。</p>
<hr />
<h2 id="类模板">类模板</h2>
<h3 id="模板特化">模板特化</h3>
<h4 id="隐式实例化">隐式实例化</h4>
<h4 id="显式实例化">显式实例化</h4>
<h4 id="显式特化">显式特化</h4>
<h4 id="部分特化">部分特化</h4>
<h3 id="模板类与友元">模板类与友元</h3>
<p>模板类声明也可以有友元。可以将模板的友元分为三类：</p>
<ul>
<li>非模板友元</li>
<li>绑定模板友元，指的是当类被实例化时，友元的类型由该类的类型决定</li>
<li>非绑定模板友元，指的是友元的所有特化版本都是该类每个特化版本的友元</li>
</ul>
<h4 id="非模板友元">非模板友元</h4>
<h4 id="绑定模板友元">绑定模板友元</h4>
<h4 id="非绑定模板友元">非绑定模板友元</h4>
<h3 id="模板别名">模板别名</h3>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>C++ Primer Plus</tag>
      </tags>
  </entry>
  <entry>
    <title>友元和异常（十一）</title>
    <url>/2024/08/29/notes-reading/computer/cpp-primer-plus/11-cpp-friends-exceptions/</url>
    <content><![CDATA[<p>本文记录了C++中与 友元和异常 相关的容易遗忘的一些知识。</p>
<span id="more"></span>
<hr />
<h2 id="友元">友元</h2>
<h3 id="友元类">友元类</h3>
<h3 id="友元成员函数">友元成员函数</h3>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>C++ Primer Plus</tag>
      </tags>
  </entry>
  <entry>
    <title>CS:APP学习笔记之计算机系统漫游（一）</title>
    <url>/2024/05/19/notes-reading/computer/cs-app/01-computer-system-tour/</url>
    <content><![CDATA[<p>本文记录《深入理解计算机系统》第3版中第1章计算机系统漫游的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="第1章-计算机系统漫游">第1章 计算机系统漫游</h2>
<h3 id="信息就是位上下文">信息就是位+上下文</h3>
<p>系统中所有的信息——包括磁盘文件、内存中的程序、内存中存放的用户数据以及网络上传送的数据，都是由一串比特表示的。区分不同数据对象的唯一方法是我们读到这些数据对象时的上下文。比如，在不同的上下文中，一个同样的字节序列可能表示一个整数、浮点数、字符串或者机器指令。</p>
<h3
id="程序被其他程序翻译成不同的格式">程序被其他程序翻译成不同的格式</h3>
<p>编译系统：执行这四个阶段的程序（预处理器、编译器、汇编器和链接器）一起构成了编译系统（compilation
system）。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250916095053216.png"
alt="编译系统" />
<figcaption aria-hidden="true">编译系统</figcaption>
</figure>
<h3 id="了解编译系统如何工作的好处">了解编译系统如何工作的好处</h3>
<ul>
<li>优化程序性能；</li>
<li>理解链接时出现的错误；</li>
<li>避免安全漏洞。</li>
</ul>
<h3
id="处理器读并解释储存在内存中的指令">处理器读并解释储存在内存中的指令</h3>
<h4 id="系统的硬件组成">系统的硬件组成</h4>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250916102405678.png" alt="典型系统的硬件组成" style="zoom:67%;" /></p>
<p>指令集架构描述的是每条机器代码指令的效果；而微体系结构描述的是处理器实际上是如何实现的。</p>
<h4 id="运行hello程序过程">运行Hello程序过程</h4>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250916103148522.png" alt="从键盘读取hello命令" style="zoom:67%;" /></p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250916103210822.png" alt="从磁盘加载可执行文件到内存" style="zoom:67%;" /></p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250916103236418.png" alt="将输出字符串从存储器写到显示器" style="zoom:67%;" /></p>
<h3 id="高速缓存至关重要">高速缓存至关重要</h3>
<p>程序执行时，会有大量的从磁盘-主存-寄存器复制的开销，拖慢了程序的真正工作。针对这种处理器与主存之间的差异，系统设计者采用了更小更快的存储设备，称为高速缓存存储器（
cache memory ，简称为 cache
或高速缓存），作为暂时的集结区域，存放处理器近期可能会需要的信息。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250916105632322.png"
alt="高速缓存存储器" />
<figcaption aria-hidden="true">高速缓存存储器</figcaption>
</figure>
<h3 id="存储设备形成层次结构">存储设备形成层次结构</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250916111332135.png" alt="存储器层次结构" style="zoom:67%;" /></p>
<p>存储器层次结构的主要思想是上一层的存储器作为低一层存储器的高速缓存。</p>
<h3 id="操作系统管理硬件">操作系统管理硬件</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250916112102287.png" alt="计算机系统分层视图和操作系统提供的抽象表示" style="zoom:67%;" /></p>
<p>操作系统有两个基本功能：</p>
<ul>
<li><p>防止硬件被失控的应用程序滥用；</p></li>
<li><p>向应用程序提供简单一致的机制来控制复杂而又通大不相同的低级硬件设备。</p></li>
</ul>
<p>操作系统通过几个基本的抽象概念（进程、虚拟内存和文件）来实现这两个功能。文件是对I/O设备的抽象表示，虚拟内存是对主存和磁盘I/O设备的抽象表示，进程则是对处理器、主存和I/O设备的抽象表示。</p>
<h4 id="进程">进程</h4>
<p>操作系统保持跟踪进程运行所需的所有状态信息。这种状态，也就是上下文，包括许多信息，比如
PC
和寄存器文件的当前值，以及主存的内容。在任何一个时刻，单处理器系统都只能执行一个进程的代码。当操作系统决定要把控制权从当前进程转移到某个新进程时，就会进行上下文切换，即保存当前进程的上下文、恢复新进程的上下文，然后将控制权传递到新进程。新进程就会从它上次停止的地方开始。下图展示了示例
hello 程序运行场景的基本理念。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250916134021655.png" alt="进程的上下文切换" style="zoom:67%;" /></p>
<h4 id="线程">线程</h4>
<p>在现代系统中，一个进程实际上可以由多个称为线程的执行单元组成，每个线程都运行在进程的上下文中，并共享同样的代码和全局数据。由于网络服务器中对<strong>并行处理</strong>的需求，线程成为越来越重要的编程模型，因为多线程之间比多进程之间更容易共享数据，也因为线程一般来说都比进程更高效。</p>
<h4 id="虚拟内存">虚拟内存</h4>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250916135139816.png" alt="进程虚拟地址空间" style="zoom:67%;" /></p>
<p>虚拟内存的运作需要硬件和操作系统软件之间精密复杂的交互，包括对处理器生成的每个地址的硬件翻译。基本思想是把<strong>一个进程虚拟内存的内容存储在磁盘上</strong>，然后用<strong>主存作为磁盘的高速缓存</strong>。</p>
<h4 id="文件">文件</h4>
<p>文件就是字节序列，仅此而已。每个I/O设备，包括磁盘、键盘、显示器，甚至网络，都可以看成是文件。</p>
<h3 id="系统之间利用网络通信">系统之间利用网络通信</h3>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250916140322557.png"
alt="利用telnet通过网络远程运行hello" />
<figcaption
aria-hidden="true">利用telnet通过网络远程运行hello</figcaption>
</figure>
<h3 id="重要主题">重要主题</h3>
<h4 id="amdahl定律">Amdahl定律</h4>
<p>该定律的主要思想是，当我们对系统的某个部分加速时，其对系统整体性能的影响取决于该部分的重要性和加速程度。若系统执行某应用程序需要时间为<span
class="math inline">\(T_\mathrm{old}\)</span>，假设系统某部分所需执行时间与该时间的比例为<span
class="math inline">\(\alpha\)</span>，而该部分性能提升比例为<span
class="math inline">\(k\)</span>。即该部分初始所需时间为<span
class="math inline">\(\alpha
T_{\mathrm{old}}\)</span>，现在所需时间为<span
class="math inline">\(\alpha
T_{\mathrm{old}}/k\)</span>。因此，总的执行时间应为</p>
<p><span class="math display">\[
T_{\mathrm{new}} = (1-\alpha)T_{\mathrm{old}}+\alpha T_{\mathrm{old}}/k
= T_{\mathrm{old}}[(1-\alpha)+\alpha/k]
\]</span></p>
<p>由此，可以计算加速比<span
class="math inline">\(S=T_\mathrm{old}/T_\mathrm{new}\)</span>为</p>
<p><span class="math display">\[
S = \frac{1}{(1-\alpha) + \alpha/k}
\]</span></p>
<p>同时，在并行计算领域可将<span
class="math inline">\(\alpha\)</span>理解为程序中可并行化的比例，<span
class="math inline">\(k\)</span>为处理器（核心）数量，<span
class="math inline">\((1-\alpha)\)</span>为必须串行执行的比例。</p>
<table>
<thead>
<tr class="header">
<th>场景</th>
<th>加速效果</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><span class="math inline">\(\alpha=1\)</span></td>
<td>完美并行，加速比 <span
class="math inline">\(S=k\)</span>（线性加速）</td>
</tr>
<tr class="even">
<td><span class="math inline">\(\alpha=0\)</span></td>
<td>完全串行，加速比 <span
class="math inline">\(S=1\)</span>（无法加速）</td>
</tr>
<tr class="odd">
<td><span class="math inline">\(\alpha\)</span> 较小</td>
<td>即使 <span class="math inline">\(k\)</span>
很大，加速比仍受串行部分<span
class="math inline">\((1-\alpha)\)</span>限制</td>
</tr>
</tbody>
</table>
<blockquote>
<p><strong>“并行计算的加速比受限于程序中必须串行执行的部分。”</strong></p>
</blockquote>
<h4 id="并发与并行">并发与并行</h4>
<p>书中用的术语并发（ concurrency
）是一个通用的概念（战略层面），指<strong>一个同时具有多个活动的系统</strong>；而术语并行（
parallelism
）指的是用并发来使一个系统运行得更快。并行可以在计算机系统的多个抽象层次上运用（战术层面）。在此，我们按照系统层次结构中由高到低的顺序重点强调三个层次。</p>
<h5 id="线程级并发">线程级并发</h5>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250916142428270.png" alt="多核处理器的组织结构" style="zoom:67%;" /></p>
<p>多处理器的使用可以从两方面提高系统性能。首先，它减少了在执行多个任务时模拟并发的需要。正如前面提到的，即使是只有一个用户使用的个人计算机也需要并发地执行多个活动。其次，它可以使应用程序运行得更快，当然，这必须要求程序是以多线程方式来书写的，这些线程可以并行地高效执行。</p>
<h5 id="指令级并行">指令级并行</h5>
<p>在较低的抽象层次上，现代处理器可以同时执行多条指令的属性称为指令级并行。早期的微处理器，如1978年的lntel
8086 ，需要多个（通常是 3 、 10
个）时钟周期来执行一条指令。最近的处理器可以保持每个时钟周期 2 ～ 4
条指令的执行速率。其实每条指令从开始到结束需要长得多的时间，大约 20
个或者更多周期，但是处理器使用了非常多的聪明技巧来同时处理多达 100
条指令。</p>
<h5 id="单指令多数据并行">单指令、多数据并行</h5>
<p>在最低层次上，许多现代处理器拥有特殊的硬件，允许一条指令产生多个可以并行执行的操作，这种方式称为单指令、多数据，即
SIMD 并行。例如，较新几代的 lntel 和AMD 处理器都具有并行地对 8
对单精度浮点数（C 数据类型 float）做加法的指令。</p>
<p>提供这些 SIMD
指令多是为了提高处理影像、声音和视频数据应用的执行速度。虽然有些编译器会试图从
C 程序中自动抽取 SIMD
并行性，但是更可靠的方法是用编译器支持的特殊的向量数据类型来写程序，比如
GCC 就支持向量数据类型。</p>
<h4 id="计算机系统中抽象的重要性">计算机系统中抽象的重要性</h4>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250916144008212.png"
alt="计算机系统提供的一些抽象" />
<figcaption aria-hidden="true">计算机系统提供的一些抽象</figcaption>
</figure>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>CS:APP</tag>
      </tags>
  </entry>
  <entry>
    <title>CS:APP学习笔记之信息的表示和处理（二）</title>
    <url>/2024/05/25/notes-reading/computer/cs-app/02-info-representation-manipulation/</url>
    <content><![CDATA[<p>本文记录《深入理解计算机系统》第3版中第2章信息的表示和处理的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="第2章-信息的表示和处理">第2章 信息的表示和处理</h2>
<h3 id="信息存储">信息存储</h3>
<h4 id="or-64位程序">32 or 64位程序</h4>
<p>大多数 64 位机器也可以运行为 32
位机器编译的程序，这是一种向后兼容。因此，举例来说，当程序用如下伪指令编译后</p>
<p><code>gcc -m32 prog.c</code></p>
<p>该程序就可以在 32 位或 64
位机器上正确运行。另一方面，若程序用下述伪指令编译</p>
<p><code>gcc -m64 prog.c</code></p>
<p>那就只能在 64 位机器上运行。</p>
<p>因此，将程序称为“ 32 位程序”或“ 64
位程序”时，区别在于该程序是如何编译的，而不是其运行的机器类型。</p>
<h4 id="大端法和小端法">大端法和小端法</h4>
<p>最低有效字节在最前面的方式，称为小端法（little endian）
。最高有效字节在最前面的方式，称为大端法（big endian）。假设变量 x
的类型为int，位于地址 0x100 处，它的十六进制值为 0x01234567 。地址范围
0x100 ～ 0x103 的字节顺序依赖于机器的类型：</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250917181525809.png"
alt="大端法和小端法" />
<figcaption aria-hidden="true">大端法和小端法</figcaption>
</figure>
<h4 id="逻辑右移与算术右移">逻辑右移与算术右移</h4>
<p>一般而言，机器支持两种形式的右移：逻辑右移和算术右移。逻辑右移在左端补个
0 。算术右移是在左端补个最高有效位的值。</p>
<p>C
语言标准并没有明确定义对于有符号数应该使用哪种类型的右移一一算术右移或者逻辑右移都可以。不幸地，这就意味着任何假设一种或者另一种右移形式的代码都可能会遇到可移植性问题。然而，实际上，<strong>几乎所有的编译器/机器组合都对有符号数使用算术右移</strong>，且许多程序员也都假设机器会使用这种右移。另一方面，对于无符号数，右移必须是逻辑的。</p>
<h3 id="整数表示">整数表示</h3>
<h4 id="位整型取值范围">64位整型取值范围</h4>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250917201612125.png" alt="64位程序整型取值范围" style="zoom:67%;" /></p>
<h4 id="编码方式">编码方式</h4>
<p>不同数值类型编码方式是不一样的，尤其是<strong>无符号整数和有符号整数之间</strong></p>
<h5 id="无符号整数编码">无符号整数编码</h5>
<p>无符号整数用无符号整数编码，无符号数编码定义如下：</p>
<p>对向量<span class="math inline">\(\vec{x} = [x_{w-1}, x_{w-2},
\cdots, x_{0}]\)</span>:</p>
<p><span class="math display">\[
B2U_{w}(\vec{x})  \doteq \sum_{i=0}^{w-1} x_{i} 2^{i}
\]</span></p>
<p><span class="math inline">\(B2U_{4}([1111]) = 1\cdot 2^3 + 1\cdot 2^2
+ 1\cdot 2^1 + 1\cdot 2^0 = 8 + 4 + 2 + 1 = 15\)</span></p>
<p><span class="math inline">\(w\)</span>位的无符号整数最大值为<span
class="math inline">\(U\max_w \doteq \sum_{i=0}^{w-1} 2^{i} = 2^w
-1\)</span>。</p>
<h5 id="补码编码">补码编码</h5>
<p>最常见的有符号数的计算机表示方式就是补码形式（Two’s
complement）。在这个定义中，将字的最高有效位解释为负权。补码编码的定义如下：</p>
<p>对向量<span class="math inline">\(\vec{x} = [x_{w-1}, x_{w-2},
\cdots, x_{0}]\)</span>:</p>
<p><span class="math display">\[
B2T_{w}(\vec{x})  \doteq - x_{w-1}2^{w-1} + \sum_{i=0}^{w-2} x_{i} 2^{i}
\]</span></p>
<p>最高有效位<span
class="math inline">\(x_{w-1}\)</span>称为符号位。</p>
<p><span class="math inline">\(B2T_{4}([1111]) = -1\cdot 2^3 + 1\cdot
2^2 + 1\cdot 2^1 + 1\cdot 2^0 = -8 + 4 + 2 + 1 = -1\)</span></p>
<p><span class="math inline">\(w\)</span>位的有符号整数最小值为<span
class="math inline">\(T\min_w \doteq -2^{w-1}\)</span>，最大值为<span
class="math inline">\(T\max_w \doteq \sum_{i=0}^{w-2} 2^{i} = 2^{w-1}
-1\)</span>。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250917220553171.png"
alt="重要数字" />
<figcaption aria-hidden="true">重要数字</figcaption>
</figure>
<h5 id="反码和原码">反码和原码</h5>
<p>有符号数还有另外两种标准表示方法（现代机器基本不用这两种）。</p>
<p><strong>反码</strong>（Ones’ Complement）：除了最高有效位的权是<span
class="math inline">\(-(2^{w-1} - 1)\)</span>而不是<span
class="math inline">\(-2^{w-1}\)</span>，和补码相同：</p>
<p><span class="math display">\[
B2O_{w}(\vec{x})  \doteq - x_{w-1}(2^{w-1} - 1) + \sum_{i=0}^{w-2} x_{i}
2^{i}
\]</span></p>
<p><strong>原码</strong>（Sign-Magnitude）：最高有效位是符号位，用来确定剩下的位应该取负权还是正权：</p>
<p><span class="math display">\[
B2S_{w}(\vec{x})  \doteq - 1^{x^{w-1}} + \sum_{i=0}^{w-2} x_{i} 2^{i}
\]</span></p>
<p>这两种表示方法都有一个奇怪的属性，那就是对于数字 0
有两种不同的编码方式。这两种表示方法，把[ 00…0 ]都解释为+ 0 。而值-0
在原码中表示为[10…0 ]，在反码中表示为[
11…1]。虽然过去生产过基于反码表示的机器，但是<strong>几乎所有的现代机器都使用补码</strong>。</p>
<p>注意补码（<strong>Two’s</strong>
Complement）和反码（<strong>Ones’</strong>
complement）的名称中的撇号的位置是不同的（<strong>理解这个英文的区别能更好的理解补码和反码</strong>）。术语补码来源于这样一个情况，对于非负数<span
class="math inline">\(x\)</span>，用<span class="math inline">\(2^w -
x\)</span>来计算<span
class="math inline">\(-x\)</span>的表示。术语反码来源于这样一个属性，用<span
class="math inline">\([ 111\cdots1] - x\)</span>来计算<span
class="math inline">\(-x\)</span>的反码表示。</p>
<h3 id="整数计算">整数计算</h3>
<h4 id="无符号数的逆元">无符号数的逆元</h4>
<p>对满足<span class="math inline">\(0 \leq x &lt;
2^w\)</span>的任意<span class="math inline">\(x\)</span>，其<span
class="math inline">\(w\)</span>位的无符号逆元<span
class="math inline">\(- _{w}^{u}x\)</span> 由下式给出：</p>
<p><span class="math display">\[
- _{w}^{u}x = \left\{
\begin{array}{ll}
x, &amp; x = 0 \\
2^w - x, &amp; x &gt; 0
\end{array} \right.
\]</span></p>
<p>意思就是任何一个数，都能找到另外一个数让他们两加起来的位全为0。</p>
<h4 id="补码的非">补码的非</h4>
<p>对满足 <span class="math inline">\(TMin_w \leq x \leq
TMax_w\)</span>的<span class="math inline">\(x\)</span>，其补码的非
<span class="math inline">\(- _{w}^{t}x\)</span>由下式给出：</p>
<p><span class="math display">\[
- _{w}^{t}x = \left\{
\begin{array}{ll}
TMin_w, &amp; x = TMin_w \\
-x, &amp; x &gt; TMin_w
\end{array} \right.
\]</span></p>
<p>意思就是任何一个数，都能找到另外一个数让他们两加起来的位全为0。</p>
<p>对C来说，任何一个数都满足 <code>-x = ~x + 1</code>。</p>
<h3 id="浮点数">浮点数</h3>
<h4 id="ieee浮点表示">IEEE浮点表示</h4>
<p>IEEE 浮点标准用<span class="math inline">\(V = (-1) ^{s} \times M
\times 2^E\)</span>的形式来表示一个数:</p>
<ul>
<li>符号 (sign) <span
class="math inline">\(s\)</span>决定这数是负数。</li>
<li>尾数 (significand) <span class="math inline">\(M\)</span>
是一个二进制小数，它的范围是<span class="math inline">\(1 \sim
2-\epsilon\)</span>，或者是 <span class="math inline">\(0 \sim 1 -
\epsilon\)</span> 。</li>
<li>阶码（exponent）<span class="math inline">\(E\)</span>
的作用是对浮点数加权，这个权重是 2 的 <span
class="math inline">\(E\)</span> 次幂（可能是负数）。</li>
</ul>
<p>将浮点数的位表示划分为三个字段，分别对这些值进行编码：</p>
<ul>
<li>一个单独的符号位<span class="math inline">\(s\)</span>；</li>
<li><span class="math inline">\(k\)</span>位的阶码字段<span
class="math inline">\(exp = e_{k-1}\cdots
e_{1}e_{0}\)</span>编码阶码<span class="math inline">\(E\)</span>；</li>
<li><span class="math inline">\(n\)</span>位小数字段<span
class="math inline">\(frac = f_{n-1}\cdots f_{1}
f_{0}\)</span>编码尾数<span class="math inline">\(M\)</span>，
编码出来的值也依赖于阶码字段的值是否等于0。</li>
</ul>
<p>两种最常见的格式如下图。在单精度浮点格式（ <code>float</code>）中，
<span class="math inline">\(s\)</span>、 <span
class="math inline">\(exp\)</span> 和 <span
class="math inline">\(frac\)</span> 字段分别为 1 位、 8 位和 23
位，得到一个 32 位的表示。在双精度浮点格式（ <code>double</code>)
中，<span class="math inline">\(s\)</span>、 <span
class="math inline">\(exp\)</span> 和 <span
class="math inline">\(frac\)</span> 字段分别为 1 位、11 位和 52
位，得到一个 64 位的表示。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250922153808973.png"
alt="标准浮点格式" />
<figcaption aria-hidden="true">标准浮点格式</figcaption>
</figure>
<p>给定位表示，根据<span
class="math inline">\(exp\)</span>的值，被编码的值可以分为三种不同的情况（最后一种情况有两个变种）。下图说明了对单精度格式的情况。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250923090242967.png"
alt="单精度浮点数值的分类" />
<figcaption aria-hidden="true">单精度浮点数值的分类</figcaption>
</figure>
<h5 id="情况1规格化的值">情况1：规格化的值</h5>
<p>这是最普遍的情况。当 <span class="math inline">\(exp\)</span>
的位模式既不全为 0 （数值 0 ），也不全为 1 （单精度数值为255
，双精度数值为 2047
）时，都属于这类情况。在这种情况中，<strong>阶码字段被解释为以偏置（
biased ）形式表示的有符号整数</strong>。也就是说，阶码的值是 <span
class="math inline">\(E=e—Bias\)</span> ，其中 <span
class="math inline">\(e\)</span> 是无符号数，其位表示为<span
class="math inline">\(e_{k-1}\cdots e_{1}e_{0}\)</span> ，而<span
class="math inline">\(Bias\)</span>是一个等于<span
class="math inline">\(2^{k-1} - 1\)</span>（单精度是 127 ，双精度是 1023
）的偏置值。由此产生指数的取值范围，对于单精度是<span
class="math inline">\(-126 \sim +127\)</span> ，而对于双精度是<span
class="math inline">\(-1022\sim+1023\)</span> 。</p>
<p>小数字段<span class="math inline">\(frac\)</span>
被解释为描述小数值<span class="math inline">\(f\)</span>，其中 <span
class="math inline">\(0 \leq f &lt; 1\)</span> ，其二进制表示为<span
class="math inline">\(0.f_{n-1}\cdots f_{1}
f_{0}\)</span>，也就是二进制小数点在最高有效位的左边。尾数定义为 <span
class="math inline">\(M = 1 + f\)</span>。有时，这种方式也叫做隐含的以 1
开头的 (implied leading 1) 表示，因为可以把 <span
class="math inline">\(M\)</span> 看成一个二进制表达式为 <span
class="math inline">\(1.f_{n-1}f_{n-2}\cdots f_{0}\)</span>
的数字。既然我们总是能够调整阶码 <span class="math inline">\(E\)</span>
，使得尾数 <span class="math inline">\(M\)</span> 在范围 <span
class="math inline">\(1 \leq M &lt; 2\)</span>
之中（假设没有溢出），那么这种表示方法是一种轻松获得一个额外精度位的技巧。既然第一位总是等于
1 ，那么就不需要显式地表示它。</p>
<h5 id="情况2非规格化的值">情况2：非规格化的值</h5>
<p>当阶码域为全 0
时，所表示的数是非规格化形式。这种情况下，<strong>阶码值是</strong>
<span class="math inline">\(E = 1 - Bias\)</span> ，而尾数的值是<span
class="math inline">\(M =
f\)</span>，也就是小数字段的值，不包含隐含的开头的 1 。</p>
<p>非规格化数有两个用途。首先，它们提供了一种表示数值 0
的方法，因为使用规格化数，我们必须总是使<span class="math inline">\(M
\geq 1\)</span>，因此就不能表示 0 。实际上，<span
class="math inline">\(+0.0\)</span>的浮点表示的位模式为全 0 ：符号位是 0
，阶码字段全为 0 （表明是一个非规格化值），而小数域也全为 0
，这就得到<span class="math inline">\(M = f =
0\)</span>。令人奇怪的是，当符号位为 1 ，而其他域全为 0 时，得到值<span
class="math inline">\(-0.0\)</span> 。根据IEEE浮点格式，值<span
class="math inline">\(+0.0\)</span>和<span
class="math inline">\(-0.0\)</span>在某些方面被认为是不同的，而在其他方面是相同的。</p>
<p>非规格化数另外一个功能是表示那些非常接近于<span
class="math inline">\(0.0\)</span>的数。它们提供了一种属性，称为逐渐溢出
(gradual underflow) ，其中，可能的数值分布均匀地接近于<span
class="math inline">\(0.0\)</span>。</p>
<h5 id="情况3特殊值">情况3：特殊值</h5>
<p>最后一类数值是当指阶码全为 1 的时候出现的。当小数域全为 0
时，得到的值表示无穷。当<span class="math inline">\(s =
0\)</span>时是<span class="math inline">\(+\infty\)</span>，<span
class="math inline">\(s = 1\)</span>时是<span
class="math inline">\(-\infty\)</span>。当我们把两个非常大的数相乘，或者除以零，无穷能够表示<em>溢出</em>的结果。当小数域为非零时，结果值被称为
“NaN”，即“不是一个数 (Not a Number)
”的缩写。一些运算的结果不能是实数或无穷，就会返回这样的 NaN
值，比如当计算<span class="math inline">\(\sqrt{-1}\)</span>或<span
class="math inline">\(\infty -
\infty\)</span>时。在某些应用中，表示未初始化的数据时，它们也很有用处。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>CS:APP</tag>
      </tags>
  </entry>
  <entry>
    <title>CS:APP学习笔记之程序的机器级表示（三）</title>
    <url>/2024/06/02/notes-reading/computer/cs-app/03-machine-level-representation-of-programs/</url>
    <content><![CDATA[<p>本文记录《深入理解计算机系统》第3版中第3章程序的机器级表示的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="第3章-程序的机器级表示">第3章 程序的机器级表示</h2>
<h3 id="程序编码">程序编码</h3>
<h4 id="两种抽象">两种抽象</h4>
<p>对于机器级编程来说，其中两种抽象尤为重要。</p>
<p>第一种是由<strong>指令集体系结构或指令集架构（Instruction Set
Architecture,
ISA）</strong>来定义机器级程序的格式和行为，它定义了处理器状态、指令的格式，以及每条指令对状态的影响。大多数ISA，包括x86-64，将程序的行为描述成好像每条指令都是按顺序执行的，一条指令结束后，下一条再开始。处理器的硬件远比描述的精细复杂，它们并发地执行许多指令，但是可以采取措施保证整体行为与ISA指定的顺序执行的行为完全一致。</p>
<p>第二种抽象是，<strong>机器级程序使用的内存地址是虚拟地址</strong>，提供的内存模型看上去是一个非常大的字节数组。存储器系统的实际实现是将多个硬件存储器和操作系统软件组合起来。</p>
<h4 id="处理器状态">处理器状态</h4>
<p>x86-64
的机器代码和原始的C代码差别非常大。一些通常对C语言程序员隐藏的处理器状态都是可见的：</p>
<ul>
<li><strong>程序计数器</strong>（通常称为PC，在x86-64中用<code>%rip</code>表示）给出将要执行的下一条指令在内存中的地址。</li>
<li><strong>整数寄存器</strong>文件包含16个命名的位置，分别存储64位的值。这些寄存器可以存储地址（对应于C语言的指针）或整数数据。有的寄存器被用来记录某些重要的程序状态，而其他的寄存器用来保存临时数据，例如过程的参数和局部变量，以及函数的返回值。</li>
<li><strong>条件码寄存器</strong>保存着最近执行的算术或逻辑指令的状态信息。它们用来实现控制或数据流中的条件变化，比如说用来实现<code>if</code>
和 <code>while</code> 语句。</li>
<li><strong>一组向量寄存器</strong>可以存放一个或多个整数或浮点数值。</li>
</ul>
<h4 id="编译和调试命令">编译和调试命令</h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 编译c文件，编译选项-Og高速编译器使用会生成符合源代码结构的机器代码的优化等级</span></span><br><span class="line">gcc -Og -o p p1.c p2.c</span><br><span class="line"></span><br><span class="line"><span class="comment"># 产生汇编文件mstore.s</span></span><br><span class="line">gcc -Og -S mstore.c</span><br><span class="line"></span><br><span class="line"><span class="comment"># 反汇编，查看机器代码文件的内容</span></span><br><span class="line">objdump -d mstore.o</span><br><span class="line"></span><br><span class="line"><span class="comment"># 产生Intel格式的汇编文件mstore.s</span></span><br><span class="line">gcc -Og -S -masm=intel mstore.c</span><br></pre></td></tr></table></figure>
<h3 id="数据格式">数据格式</h3>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250924120648172.png"
alt="C语言数据类型在x86-64中的大小" />
<figcaption
aria-hidden="true">C语言数据类型在x86-64中的大小</figcaption>
</figure>
<p>如图所示，大多数GCC生成的汇编代码指令都有一个字符的后缀，表明操作数的大小。例如，数据传送指令有四个变种：
movb（传送字节）、movw（传送字）、 movl（传送双字）和
movq（传送四字）。后缀<code>l</code>用来表示双字，因为32位数被看成是“长字
(long word) ”。注意，汇编代码也使用后缀<code>l</code>
来表示4字节整数和8字节双精度浮点数。这不会产生歧义，因为浮点数使用的是一组完全不同的指令和寄存器。</p>
<h3 id="访问信息">访问信息</h3>
<h4 id="整数寄存器">整数寄存器</h4>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250924124841070.png"
alt="整数寄存器" />
<figcaption aria-hidden="true">整数寄存器</figcaption>
</figure>
<h4 id="操作数指示符">操作数指示符</h4>
<p>大多数指令有一个或多个操作数，指示出执行一个操作中要使用的源数据值，以及放置结果的目的位置。
x86-64支持多种操作数格式。源数据值可以以<strong>常数形式</strong>给出，或是从<strong>寄存器</strong>或<strong>内存</strong>中读出。结果可以存放在<strong>寄存器</strong>或<strong>内存</strong>中。因此，各种不同的操作数的可能性被分为三种类型。</p>
<p>第一种类型是<strong>立即数</strong> (immediate)
，用来表示常数值。</p>
<p>第二种类型是<strong>寄存器</strong>（ register
），它表示某个寄存器的内容。</p>
<p>第三类操作数是<strong>内存引用</strong>，它会根据计算出来的地址（通常称为有效地址）访问某个内存位置。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250924132328736.png"
alt="操作数格式" />
<figcaption aria-hidden="true">操作数格式</figcaption>
</figure>
<h4 id="数据传送指令">数据传送指令</h4>
<p>源操作数指定的值是一个立即数，存储在寄存器中或者内存中。目的操作数指定一个位置，一个寄存器或者是一个内存地址。
x86-64加了一条限制，传送指令的两个操作数不能都指向内存位置。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250924164313407.png"
alt="简单数据传送指令" />
<figcaption aria-hidden="true">简单数据传送指令</figcaption>
</figure>
<p>图3-5和图3-6记录的是两类数据移动指令，在将较小的源值复制到较大的目的时使用。所有这些指令都把数据从源（<strong>在寄存器或内存中</strong>）复制到目的寄存器。
MOVZ类中的指令把目的中剩余的字节填充为0，而MOVS类中的指令通过符号扩展来填充，把源操作的最高位进行复制。可以观察到，每条指令名字的最后两个字符都是大小指示符：第一个字符指定源的大小，而第二个指明目的的大小。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250924164222798.png"
alt="零扩展和符号扩展数据传送指令" />
<figcaption aria-hidden="true">零扩展和符号扩展数据传送指令</figcaption>
</figure>
<h4 id="压入和弹出栈数据">压入和弹出栈数据</h4>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250924172306794.png"
alt="入栈和出栈指令" />
<figcaption aria-hidden="true">入栈和出栈指令</figcaption>
</figure>
<p>在x86-64中，程序栈存放在内存中某个区域。如下图所示，栈向下增长，这样一来，栈顶元素的地址是所有栈中元素地址中最低的。栈指针<code>%rsp</code>保存着栈顶元素的地址。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250924172342532.png"
alt="栈操作说明" />
<figcaption aria-hidden="true">栈操作说明</figcaption>
</figure>
<h3 id="算术和逻辑操作">算术和逻辑操作</h3>
<p>x86-64的一些整数和逻辑操作可分为四组：加载有效地址、一元操作、二元操作和移位。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250924172957728.png"
alt="整数算术操作" />
<figcaption aria-hidden="true">整数算术操作</figcaption>
</figure>
<h4 id="加载有效地址">加载有效地址</h4>
<p><code>leaq</code>是 x86-64
汇编中一个非常重要且”独特”的指令。它的全称是 <strong>Load Effective
Address to Register</strong>（将有效地址加载到寄存器）。</p>
<hr />
<blockquote>
<p>核心概念：它不访问内存！</p>
</blockquote>
<p>这是理解 <code>leaq</code> 最关键的一点：<strong><code>leaq</code>
计算一个内存地址，但它并不去读取这个地址里的数据！它只是将计算出的地址值本身存入寄存器。</strong></p>
<p>基本语法</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">leaq SRC, DEST</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>SRC</strong>：一个内存操作数（寻址模式）</li>
<li><strong>DEST</strong>：一个寄存器</li>
<li><strong>效果</strong>：<code>DEST = address_of(SRC)</code></li>
</ul>
<table>
<colgroup>
<col style="width: 20%" />
<col style="width: 52%" />
<col style="width: 26%" />
</colgroup>
<thead>
<tr class="header">
<th>特性</th>
<th><code>leaq</code></th>
<th><code>mov</code>（内存到寄存器）</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>主要功能</strong></td>
<td>计算地址</td>
<td>从内存加载数据</td>
</tr>
<tr class="even">
<td><strong>是否访问内存</strong></td>
<td>❌ 否</td>
<td>✅ 是</td>
</tr>
<tr class="odd">
<td><strong>实际用途</strong></td>
<td>1. 地址计算<br>2. 整数运算<br>3. 指针运算</td>
<td>数据传输</td>
</tr>
</tbody>
</table>
<p><strong>简单记忆：<code>leaq</code>
是”披着内存操作外衣的算术指令”——它穿着内存寻址的”衣服”（语法），但干的是地址计算或普通算术的”活儿”。</strong></p>
<p>这个指令的巧妙之处在于硬件设计者提供了一个强大的算术单元（用于地址计算），而软件开发者发现可以”借用”这个单元来做通用的数学运算，从而获得了性能优势。</p>
<h4 id="一元和二元操作">一元和二元操作</h4>
<p>一元操作只有一个操作数，既是源又是目的。这个操作数可以是一个寄存器，也可以是一个内存位置。比如说，指令<code>incq (%rsp)</code>会使栈顶的8字节元素加
1 。这种语法让人想起 C 语言中的加1运算符（++）和减1运算符（–）。</p>
<p>二元操作中第二个操作数既是源又是目的。这种语法让人想起C语言中的赋值运算符，例如
x -=y
。例如，指令<code>subq %rax, %rdx</code>使寄存器<code>%rdx</code>的值减去<code>%rax</code>中的值。（将指令解读成从
<code>%rdx</code> 中减去打 <code>%rax</code>
会有所帮助。）第一个操作数可以是立即数、寄存器或是内存位置。第二个操作数可以是寄存器或是内存位置。注意，当第二个操作数为内存地址时，处理器必须从内存读出值，执行操作，再把结果写回内存。</p>
<h4 id="移位操作">移位操作</h4>
<p>移位操作先给出移位量，然后第二项给出的是要移位的数。可以进行算术和逻辑右移。移位量可以是一个立即数，或者放在单字节寄存器<code>%cl</code>中。（这些指令很特别，因为只允许以<strong>这个特定的寄存器作为操作数</strong>）</p>
<h3 id="控制">控制</h3>
<h4 id="条件码寄存器">条件码寄存器</h4>
<p>除了整数寄存器， CPU还维护着一组单个位的条件码 (condition code)
寄存器，它们描述了最近的算术或逻辑操作的属性。可以检测这些寄存器来执行条件分支指令。最常用的条件码有：</p>
<ul>
<li>CF
：进位标志。最近的操作使最高位产生了进位。可用来检查无符号操作的溢出。</li>
<li>ZF：零标志。最近的操作得出的结果为0。</li>
<li>SF ：符号标志。最近的操作得到的结果为负数。</li>
<li>OF ：溢出标志。</li>
</ul>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250925145110152.png"
alt="比较和测试指令" />
<figcaption aria-hidden="true">比较和测试指令</figcaption>
</figure>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250925145149737.png"
alt="SET指令" />
<figcaption aria-hidden="true">SET指令</figcaption>
</figure>
<h4 id="跳转指令">跳转指令</h4>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250925145646612.png"
alt="跳转指令" />
<figcaption aria-hidden="true">跳转指令</figcaption>
</figure>
<h4 id="条件控制的汇编">条件控制的汇编</h4>
<p>C语言中<code>if-else</code>语句的通用形式模板如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(test-expr)</span><br><span class="line">    then-statement</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">    <span class="keyword">else</span>-statement</span><br></pre></td></tr></table></figure>
<p>对应的汇编实现通常会使用下面的形式，这里用C语法来描述控制流：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">  t = test-expr;</span><br><span class="line">  <span class="keyword">if</span>(!t)</span><br><span class="line">      <span class="keyword">goto</span> <span class="literal">false</span>;</span><br><span class="line">  then-statement</span><br><span class="line">  <span class="keyword">goto</span> done;</span><br><span class="line"><span class="literal">false</span>:</span><br><span class="line">  <span class="keyword">else</span>-statement</span><br><span class="line">done:</span><br></pre></td></tr></table></figure>
<h4 id="条件传送的汇编">条件传送的汇编</h4>
<p>条件传送一般只用于分支语句比较简单的情况。</p>
<p>为了理解如何通过条件数据传输来实现条件操作，考虑下面的条件表达式和赋值的通用形式：</p>
<p><code>v = test-expr ? then-expr : else-expr;</code></p>
<p>用条件控制转移的标准方法来编译这个表达式会得到如下形式：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">  <span class="keyword">if</span>(!test-expr)</span><br><span class="line">      <span class="keyword">goto</span> <span class="literal">false</span>;</span><br><span class="line">  v = then-statement</span><br><span class="line">  <span class="keyword">goto</span> done;</span><br><span class="line"><span class="literal">false</span>:</span><br><span class="line">  v = <span class="keyword">else</span>-statement</span><br><span class="line">done:</span><br></pre></td></tr></table></figure>
<p>这段代码包含两个代码序列：一个对<code>then-expr</code>求值，另一个对<code>else-expr</code>求值。</p>
<p>基于条件传送的代码，会对<code>then-expr</code>和<code>else-expr</code>都求值，最终值的选择基于对<code>test-expr</code>的求值。可以用下面的抽象代码描述：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">vt = then-expr;</span><br><span class="line">ve = <span class="keyword">else</span>-expr;</span><br><span class="line">t = test-expr;</span><br><span class="line"><span class="keyword">if</span>(!t) v = ve;</span><br></pre></td></tr></table></figure>
<p>这个序列中的最后一条语句是用条件传送实现的–只有当测试条件<code>t</code>满足时，<code>vt</code>
的值才会被复制到<code>v</code>中。</p>
<h4 id="do-while的汇编">do-while的汇编</h4>
<p><code>do-while</code>语句的通用形式如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">do</span></span><br><span class="line">    body-statement</span><br><span class="line">    <span class="title function_">while</span><span class="params">(test-expr)</span>;</span><br></pre></td></tr></table></figure>
<p>这种通用形式可以被翻译成如下所示的条件和<code>goto</code>语句：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">loop:</span><br><span class="line">  body-statement</span><br><span class="line">     t = test-expr;</span><br><span class="line">  <span class="keyword">if</span>(t)</span><br><span class="line">        <span class="keyword">goto</span> loop;</span><br></pre></td></tr></table></figure>
<h4 id="while的汇编">while的汇编</h4>
<p><code>while</code>语句的通用形式如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span>(test-expr)</span><br><span class="line">    body-statement</span><br></pre></td></tr></table></figure>
<p>GCC在代码翻译中使用两种方法，跳转到中间（jump to middle）和
guarded-do。</p>
<h5 id="jump-to-middle">jump to middle</h5>
<p>用以下模板表示这种方法：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">  <span class="keyword">goto</span> test;</span><br><span class="line">loop:</span><br><span class="line">  body-statement</span><br><span class="line">test:</span><br><span class="line">  t = test-expr;</span><br><span class="line">  <span class="keyword">if</span>(t)</span><br><span class="line">        <span class="keyword">goto</span> loop;</span><br></pre></td></tr></table></figure>
<h5 id="guarded-do">guarded-do</h5>
<p>首先用条件分支，如果初始条件不成立就过循环，把代码变换为
<code>do-while</code>
循环。当使用较高优化等级编译时，例如使用命令行<code>O1</code>， GCC
会采用这种策略。可以用如下模板来表达这种方法，把通用的
<code>while</code> 循环翻译成<code>do-while</code>循环：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">t = test-expr;</span><br><span class="line"><span class="keyword">if</span>(!t)</span><br><span class="line">    <span class="keyword">goto</span> done;</span><br><span class="line"><span class="keyword">do</span></span><br><span class="line">    body-statement;</span><br><span class="line">  <span class="keyword">while</span>(test-expr);</span><br><span class="line">done:</span><br></pre></td></tr></table></figure>
<p>翻译成<code>goto</code>代码如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">t = test-expr;</span><br><span class="line"><span class="keyword">if</span>(!t)</span><br><span class="line">    <span class="keyword">goto</span> done;</span><br><span class="line">loop:</span><br><span class="line">  body-statement</span><br><span class="line">    t = test-expr;</span><br><span class="line">  <span class="keyword">if</span>(t)</span><br><span class="line">        <span class="keyword">goto</span> loop;</span><br><span class="line">done:</span><br></pre></td></tr></table></figure>
<h4 id="for循环的汇编">for循环的汇编</h4>
<p><code>for</code>循环的通用形式如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(init-expr; test-expr; update-expr)</span><br><span class="line">    body-statement</span><br></pre></td></tr></table></figure>
<p>可将其转换为<code>while</code>循环：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">init-expr;</span><br><span class="line"><span class="keyword">while</span>(test-expr)&#123;</span><br><span class="line">    body-statement</span><br><span class="line">    update-expr;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>GCC为<code>for</code>循环翻译的汇编代码是<code>while</code>循环的两种翻译之一，这取决于优化的等级。</p>
<h3 id="过程">过程</h3>
<h4 id="什么是过程">什么是过程</h4>
<p>不同编程语言中，过程的形式多样：函数、方法、子例程、处理函数等，但是它们都有一些共有特性。</p>
<p>要提供对过程的机器级支持，必须处理不同属性。为讨论方便，假设过程P调用过程Q，Q执行后返回到P。这些动作包括下面一个或多个机制：</p>
<p><strong>传递控制</strong>。在进入过程Q时，程序计数器必须被设置为Q的代码的起始地址，然后在返回时，要把程序计数器设置为P中调用Q后面那条指令的地址。</p>
<p><strong>传递数据</strong>。P必须能够向Q提供一个或多个参数，Q必须能够向P返回一个值。</p>
<p><strong>分配和释放内存</strong>。在开始时，Q可能需要为局部变量分配空间，而在返回前，又必须释放这些存储空间。</p>
<h4 id="运行时栈">运行时栈</h4>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250929142946797.png" alt="通用的栈帧结构" style="zoom:67%;" /></p>
<p>在x86 - 64架构的System V
ABI规范下，对于超过6个的函数参数，压栈时是先压<strong>后面的参数</strong>，局部变量也是先压后面的参数。</p>
<h3
id="在机器级程序中将控制与数据结合起来">在机器级程序中将控制与数据结合起来</h3>
<h4 id="对抗缓冲区溢出攻击的方法">对抗缓冲区溢出攻击的方法</h4>
<h5 id="栈随机化">栈随机化</h5>
<p><strong>栈随机化的思想使得栈的位置在程序每次运行时都有变化</strong>。</p>
<p>在 Linux
系统中，栈随机化已经变成了标准行为。它是更大的一类技术中的一种，这类技术称为地址空间布局随机化
(Address-Space Layout Randomization) ，或者简称 ASLR。采用 ASLR
，每次运行时程序的不同部分，包括程序代码、库代码、栈、全局变量和堆数据，都会被加载到内存的不同区域。这就意味着在一台机器上运行一个程序，与在其他机器上运行同样的程序，它们的地址映射大相径庭。这样才能够对抗一些形式的攻击。</p>
<h5 id="栈破坏检测">栈破坏检测</h5>
<p>最近的 GCC 版本在产生的代码中加入了一种栈保护者（ stack protector)
机制，来检测缓冲区越界。<strong>其思想是在栈帧中任何局部缓冲区与栈状态之间存储一个特殊的金丝雀值（canary）</strong>。这个金丝雀值，也称为哨兵值(guard
value)
，是在程序每次运行时随机产生的，因此，攻击者没有简单的办法能够知道它是什么。在恢复寄存器状态和从函数返回之前，程序检查这个金丝雀值是否被该函数的某个操作或者该函数调用的某个函数的某个操作改变了。如果是的，那么程序异常中止。</p>
<p>栈保护很好地防止了缓冲区溢出攻击破坏存储在程序栈上的状态。它只会带来很小的能损失，特别是因为
GCC 只在函数中有局部 char 类型缓冲区的时候才插人这样的代码。</p>
<h5 id="限制可执行代码区域">限制可执行代码区域</h5>
<p>限制哪些内存区域能够存放可执行代码。在典型的程序中，只有保存编译器产生的代码的那部分内存才需要是可执行的。其他部分可以被限制为只允许读和写。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>CS:APP</tag>
      </tags>
  </entry>
  <entry>
    <title>lshort笔记：用 LaTeX 排版文字（二）</title>
    <url>/2025/01/06/notes-reading/computer/lshort/lshort-02-typeset-text/</url>
    <content><![CDATA[<blockquote>
<p>本文内容来自<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">一份
(不太) 简短的 LATEX2ε 介绍<i class="fa fa-external-link-alt"></i></span>。</p>
</blockquote>
<p>文字是排版的基础。本章主要介绍如何在 <span
class="math inline">\(\LaTeX{}\)</span>
中输入各种文字符号，包括标点符号、连字符、重音等，以及控制文字断行和断页的方式。本章简要介绍了在
<span class="math inline">\(\LaTeX{}\)</span> 中排版中文的方法。随着
<span class="math inline">\(\LaTeX{}\)</span> 和底层 <span
class="math inline">\(\TeX{}\)</span>引擎的发展，旧方式（<code>CCT</code>、<code>CJK</code>等）日渐退出舞台，<code>xelatex</code>
和 <code>lualatex</code> 编译命令配合 <code>ctex</code>
宏包/文档类的方式成为当前的主流中文排版支持方式。</p>
<span id="more"></span>
<hr />
<h2 id="latex中的字符"><span
class="math inline">\(\LaTeX{}\)</span>中的字符</h2>
<h3 id="空格和分段">空格和分段</h3>
<p>空格键和 Tab
键输入的空白字符视为“空格”。连续的若干个空白字符视为一个空格。一行开头的空格忽略不计。行末的换行符视为一个空格；但连续两个换行符，也就是空行，会将文字分段。多个空行被视为一个空行。也可以在行末使用
<code>\par</code>命令分段。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114165512239.png" alt="image-20250114165512239" style="zoom: 50%;" /></p>
<h3 id="注释">注释</h3>
<ul>
<li>用 <code>%</code>
字符作为注释。在这个字符之后直到行末，所有的字符都被忽略，行末的换行符也不引入空格。</li>
<li>texstudio快捷键 <code>Ctrl + T</code>
快速注释/取消注释光标所在行。</li>
<li><code>\iffalse</code> 和 <code>\fi</code>
是一个常用的方法，适合注释大段文字或代码。</li>
</ul>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\iffalse</span></span><br><span class="line">这里是被注释掉的内容。</span><br><span class="line">无论是文字、公式还是代码，都会被忽略。</span><br><span class="line"><span class="keyword">\fi</span></span><br></pre></td></tr></table></figure>
<ul>
<li><code>comment</code>
宏包提供了方便的注释方式，可以直接注释任意内容。在导言区引入宏包：<code>\usepackage{comment}</code></li>
</ul>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;comment&#125;</span><br><span class="line">这里是大段注释内容。</span><br><span class="line">你可以注释多行文本或代码。</span><br><span class="line"><span class="keyword">\end</span>&#123;comment&#125;</span><br></pre></td></tr></table></figure>
<h3 id="特殊字符">特殊字符</h3>
<p>以下字符有特殊用途，如 <code>%</code> 表示注释，
<code>$</code>、<code>^</code> 、<code>_</code>
等用于排版数学公式，<code>&amp;</code>
用于排版表格，等等。直接输入这些字符得不到对应的符号，还往往会出错：</p>
<p><code># $ % &amp; { } _ ^ ~ \</code></p>
<p>想要输入以上符号，需要使用以下带反斜线的形式输入，类似编程语言里的“转义”符号：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114170715995.png" alt="image-20250114170715995" style="zoom: 50%;" /></p>
<p>这些“转义”符号事实上是一些 <span
class="math inline">\(\LaTeX{}\)</span> 命令。其中 <code>\^</code> 和
<code>\~</code>两个命令需要一个参数，加一对花括号的写法相当于提供了空的参数，否则它们可能会将后面的字符作为参数，形成重音效果。<code>\\</code>被直接定义成了手动换行的命令，输入反斜线就需要用
<code>\textbackslash</code>。</p>
<h3 id="连字">连字</h3>
<p>西文排版中经常会出现连字（ligatures），常见的有
ff/fi/fl/ffi/ffl。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114171445216.png" alt="image-20250114171445216" style="zoom:67%;" /></p>
<h3 id="标点符号">标点符号</h3>
<p>中文的标点符号（绝大多数为非 ASCII
字符）使用中文输入法输入即可，一般不需要过多留意。而输入西文标点符号时，有不少地方需要留意。</p>
<h4 id="引号">引号</h4>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114172016408.png" alt="image-20250114172016408" style="zoom:67%;" /></p>
<h4 id="连字号和破折号">连字号和破折号</h4>
<p>有三种长度的“横线”可用：连字号（hyphen）、短破折号（en-dash）和长破折号（em-dash）。
它们分别有不同的用途：连字号 <code>-</code> 用来组成复合词；短破折号
<code>--</code> 用来连接数字表示范围；长破折号 <code>---</code>
用来连接单词，语义上类似中文的破折号。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114172526612.png" alt="image-20250114172526612" style="zoom:67%;" /></p>
<h4 id="省略号">省略号</h4>
<p>提供了 <code>\ldots</code>
命令表示省略号，相对于直接输入三个点的方式更为合理。<code>\dots</code>
与 <code>\ldots</code> 命令等效。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114172739640.png" alt="image-20250114172739640" style="zoom:67%;" /></p>
<h4 id="拉丁文扩展与重音">拉丁文扩展与重音</h4>
<p>支持用命令输入西欧语言中使用的各种拉丁文扩展字符，主要为带重音的字母：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114173207428.png" alt="image-20250114173207428" style="zoom:67%;" /></p>
<p>更多可用的符号和重音见下表。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114173234392.png" alt="image-20250114173234392" style="zoom:67%;" /></p>
<h4 id="latex-标志"><span class="math inline">\(\LaTeX{}\)</span>
标志</h4>
<p>我们见到的所有错落有致的 <span
class="math inline">\(\LaTeX{}\)</span> 标志都是由以下命令输入的：</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114173523673.png"
alt="image-20250114173523673" />
<figcaption aria-hidden="true">image-20250114173523673</figcaption>
</figure>
<p>更多的 <span class="math inline">\(\LaTeX{}\)</span> 标志可以使用
<code>hologo</code> 宏包提供的 <code>\hologo</code> 命令输入。</p>
<hr />
<h2 id="断行和断页">断行和断页</h2>
<p><span class="math inline">\(\LaTeX{}\)</span>
将文字段落在合适的位置进行断行，尽可能做到每行的疏密程度匀称，单词间距不会过宽或过窄。文字段落和公式、图表等内容从上到下顺序排布，并在合适的位置断页，分割成匀称的页面。在绝大多数时候，我们无需自己操心断行和断页。但偶尔会遇到需要手工调整的地方。</p>
<h3 id="单词间距">单词间距</h3>
<p>文字在单词间的“空格”处断行时，“空格”生成的间距随之舍去。可以使用字符
<code>~</code> 输入一个<strong>不会断行的空格</strong>（高德纳称之为
tie，“带子”），通常用在<strong>英文人名、图表名称</strong>等上下文环境：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114174307171.png" alt="image-20250114174307171" style="zoom:67%;" /></p>
<h3 id="手动断行和断页">手动断行和断页</h3>
<p>如果我们确实需要手动断行，可使用如下命令：</p>
<p><code>\\[⟨length⟩]</code></p>
<p><code>\\*[⟨length⟩]</code></p>
<p><code>\newline</code></p>
<p>它们有两点区别：一是 <code>\\</code>
可以带可选参数<code>&lt;length&gt;</code>，用于在断行处向下增加垂直间距，而
<code>\newline</code> 不带可选参数；二是 <code>\\</code>
也在表格、公式等地方用于换行，而 <code>\newline</code>
只用于文本段落中。带星号的 <code>\\</code> 表示禁止在断行处分页。</p>
<p>断页的命令有两个：</p>
<p><code>\newpage</code></p>
<p><code>\clearpage</code></p>
<p>通常情况下两个命令都起到另起一页的作用，区别在于：第一，在双栏排版模式中
<code>\newpage</code> 起到另起一栏的作用，<code>\clearpage</code>
则能够另起一页；第二，在涉及浮动体的排版上行为不同。</p>
<p>有时候我们不满足于默认的断行和断页位置，需要进行微调，可以用以下命令告诉
<span class="math inline">\(\LaTeX{}\)</span>
哪些地方适合断行或断页，哪些地方不适合：</p>
<p><code>\linebreak[⟨n⟩]</code> <code>\nolinebreak[⟨n⟩]</code>
<code>\pagebreak[⟨n⟩]</code> <code>\nopagebreak[⟨n⟩]</code></p>
<p>以上命令都带一个可选参数，用数字
<code>n</code>代表适合/不适合的程度，取值范围为
0-4，不带可选参数时，缺省为4。 比如 <code>\linebreak</code> 或者
<code>\linebreak[4]</code>
意味着此处需要强行断行；<code>\nopagebreak</code> 或
<code>\nopagebreak[4]</code>意味着禁止在此处断页。</p>
<p>以上命令适合给出优先考虑断行断页/禁止断行断页的位置，但不适合直接拿来断行或断页，使用
<code>\newline</code> 或 <code>\newpage</code> 等命令是更好的选择。因为
<code>\newline</code> 和 <code>\newpage</code>
会在断行/断页位置填充适当的间距，但 <code>\linebreak</code> 和
<code>\pagebreak</code>
不能，使用这些命令强行断行/断页可能会制造出糟糕的排版效果，并导致 <span
class="math inline">\(\LaTeX{}\)</span> 报 <span
class="math inline">\(\texttt{Underfull \\hbox}\)</span> 等警告。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>lshort</tag>
      </tags>
  </entry>
  <entry>
    <title>lshort笔记：文档元素（三）</title>
    <url>/2025/01/12/notes-reading/computer/lshort/lshort-03-document-elements/</url>
    <content><![CDATA[<blockquote>
<p>本文内容来自<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">一份
(不太) 简短的 LATEX2ε 介绍<i class="fa fa-external-link-alt"></i></span>。</p>
</blockquote>
<p>在知道了如何输入文字后，本章介绍一个结构化的文档所依赖的各种元素——章节、目录、列表、图表、交叉引用、脚注等等。</p>
<span id="more"></span>
<hr />
<h2 id="章节和目录">章节和目录</h2>
<h3 id="章节标题">章节标题</h3>
<p>一篇结构化的、条理清晰文档一定是层次分明的，通过不同的命令分割为章、节、小节。三个标准文档类
<code>article</code>、<code>report</code> 和 <code>book</code>
提供了划分章节的命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\chapter</span>&#123;⟨title⟩&#125; <span class="keyword">\section</span>&#123;⟨title⟩&#125; <span class="keyword">\subsection</span>&#123;⟨title⟩&#125;</span><br><span class="line"><span class="keyword">\subsubsection</span>&#123;⟨title⟩&#125; <span class="keyword">\paragraph</span>&#123;⟨title⟩&#125; <span class="keyword">\subparagraph</span>&#123;⟨title⟩&#125;</span><br></pre></td></tr></table></figure>
<p>其中 <code>\chapter</code> <strong>只在 <code>report</code> 和
<code>book</code>
文档类有定义</strong>。这些命令生成章节标题，并能够自动编号。除此之外
<span class="math inline">\(\LaTeX{}\)</span> 还提供了
<code>\part</code> 命令，用来将整个文档分割为大的分块，但不影响
<code>\chapter</code> 或 <code>\section</code> 等的编号。</p>
<p>上述命令除了生成带编号的标题之外，还向目录中添加条目，并影响页眉页脚的内容。每个命令有两种变体：</p>
<ul>
<li><p>带可选参数的变体：<code>\section[&lt;short title&gt;]{&lt;title&gt;}</code>
，标题使用 <code>&lt;title&gt;</code> 参数，在目录和页眉页脚中使用
<code>&lt;short title&gt;</code>参数；</p></li>
<li><p>带星号的变体：<code>\section*{&lt;title&gt;}</code>
，标题不带编号，也不生成目录项和页眉页脚。</p></li>
</ul>
<p>较低层次如 <code>\paragraph</code> 和 <code>\subparagraph</code>
即使不用带星号的变体，生成的标题默认也不带编号，事实上，除
<code>\part</code> 外：</p>
<ul>
<li><code>article</code> 文档类带编号的层级为
<code>\section</code>、<code>\subsection</code>、<code>\subsubsection</code>
三级；</li>
<li><code>report</code> 和 <code>book</code> 文档类带编号的层级为
<code>\chapter</code>、<code>\section</code>、<code>\subsection</code>
三级。</li>
</ul>
<h3 id="目录">目录</h3>
<p>生成目录非常容易，只需在合适的地方使用命令：<code>\tableofcontents</code></p>
<p>这个命令会生成单独的一章（<code>report</code>/<code>book</code>）或一节（<code>article</code>），标题默认为“Contents”，可定制标题。这种生成的章节默认不写入目录（<code>\section*</code>
或 <code>\chapter*</code>），可使用 <code>tocbibind</code>
等宏包修改设置。</p>
<p>正确生成目录项，一般需要 <strong>编译两次源代码</strong>。</p>
<p>有时使用了 <code>\chapter*</code> 或 <code>\section*</code>
这样不生成章节编号和目录项的章节标题命令，而又想手动生成该章节的目录项，可以在标题命令后面使用：</p>
<p><code>\addcontentsline{toc}{&lt;level&gt;}{&lt;title&gt;}</code></p>
<p>其中 <code>&lt;level&gt;</code> 为章节层次 <code>chapter</code> 或
<code>section</code> 等，<code>&lt;title&gt;</code>
为出现于目录项的章节标题。</p>
<p><code>titletoc</code>、<code>tocloft</code>
等宏包提供了具体定制目录项格式的功能，详情请参考宏包的帮助文档。</p>
<h3 id="文档结构的划分">文档结构的划分</h3>
<p>所有标准文档类都提供了一个 <code>\appendix</code>
命令将正文和附录分开，使用 <code>\appendix</code>
后，最高一级章节改为使用拉丁字母编号，从 A 开始。</p>
<p><code>book</code> 文档类还提供了前言、正文、后记结构的划分命令：</p>
<ul>
<li><code>\frontmatter</code> 前言部分，页码使用小写罗马数字；其后的
<code>\chapter</code> 不编号。</li>
<li><code>\mainmatter</code> 正文部分，页码使用阿拉伯数字，从 1
开始计数；其后的章节编号正常。</li>
<li><code>\backmatter</code>
后记部分，页码格式不变，继续正常计数；其后的 <code>\chapter</code>
不编号。</li>
</ul>
<p>以上三个命令还可和 <code>\appendix</code>
命令结合，生成有前言、正文、附录、后记四部分的文档。</p>
<p>下面的源代码是一份完整的文档结构。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\documentclass</span>&#123;book&#125;</span><br><span class="line"><span class="comment">% 导言区，加载宏包和各项设置，包括参考文献、索引等</span></span><br><span class="line"><span class="keyword">\usepackage</span>&#123;makeidx&#125;        <span class="comment">% 调用 makeidx 宏包，用来处理索引</span></span><br><span class="line"><span class="keyword">\makeindex</span>                  <span class="comment">% 开启索引的收集</span></span><br><span class="line"><span class="keyword">\bibliographystyle</span>&#123;plain&#125;   <span class="comment">% 指定参考文献样式为 plain</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">\title</span>&#123;Languages&#125;</span><br><span class="line"><span class="keyword">\author</span>&#123;someone&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">\frontmatter</span>         <span class="comment">% 前言部分</span></span><br><span class="line"><span class="keyword">\maketitle</span>           <span class="comment">% 标题页</span></span><br><span class="line"><span class="keyword">\include</span>&#123;preface&#125;    <span class="comment">% 前言章节 preface.tex</span></span><br><span class="line"><span class="keyword">\tableofcontents</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">\mainmatter</span>          <span class="comment">% 正文部分</span></span><br><span class="line"><span class="keyword">\include</span>&#123;chapter1&#125;   <span class="comment">% 第一章 chapter1.tex</span></span><br><span class="line"><span class="keyword">\include</span>&#123;chapter2&#125;   <span class="comment">% 第二章 chapter2.tex</span></span><br><span class="line">...</span><br><span class="line"><span class="keyword">\appendix</span>            <span class="comment">% 附录</span></span><br><span class="line"><span class="keyword">\include</span>&#123;appendixA&#125;  <span class="comment">% 附录 A appendixA.tex</span></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="keyword">\backmatter</span>          <span class="comment">% 后记部分</span></span><br><span class="line"><span class="keyword">\include</span>&#123;epilogue&#125;   <span class="comment">% 后记 epilogue.tex</span></span><br><span class="line"><span class="keyword">\bibliography</span>&#123;books&#125; <span class="comment">% 利用 BibTeX 工具从数据库文件 books.bib 生成参考文献</span></span><br><span class="line"><span class="keyword">\printindex</span>          <span class="comment">% 利用 makeindex 工具生成索引</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="标题页">标题页</h2>
<p><span class="math inline">\(\LaTeX{}\)</span>
支持生成简单的标题页。首先需要给定标题和作者等信息：</p>
<p><code>\title{&lt;title&gt;}</code>
<code>\author{&lt;author&gt;}</code>
<code>\date{&lt;date&gt;}</code></p>
<p>其中前两个命令是必须的（不用 <code>\title</code> 会报错；不用
<code>\author</code> 会警告），<code>\date</code> 命令可选。<span
class="math inline">\(\LaTeX{}\)</span> 还提供了一个 <code>\today</code>
命令自动生成当前日期，<code>\date</code> 默认使用 <code>today</code>。在
<code>\title</code>、<code>\author</code> 等命令内可以使用
<code>\thanks</code> 命令生成标题页的脚注，用 <code>\and</code>
隔开多个人名。</p>
<p>在信息给定后，就可以使用 <code>\maketitle</code>
命令生成一个简单的标题页了。下面的源代码给出了一个标题页的示例和大致效果。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\title</span>&#123;Test title&#125;</span><br><span class="line"><span class="keyword">\author</span>&#123; Mary<span class="keyword">\thanks</span>&#123;E-mail:*****@***.com&#125;</span><br><span class="line">  <span class="keyword">\and</span> Ted<span class="keyword">\thanks</span>&#123;Corresponding author&#125;</span><br><span class="line">  <span class="keyword">\and</span> Louis&#125;</span><br><span class="line"><span class="keyword">\date</span>&#123;<span class="keyword">\today</span>&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114201008937.png" alt="image-20250114201008937" style="zoom:67%;" /></p>
<p><code>article</code> 文档类的标题默认不单独成页，而
<code>report</code> 和 <code>book</code> 默认单独成页。可在
<code>\documentclass</code> 命令调用文档类时指定 <code>titlepage</code>
或 <code>notitlepage</code>选项以修改默认的行为。</p>
<p><span class="math inline">\(\LaTeX{}\)</span>
标准类还提供了一个简单的 <code>titlepage</code>
环境，生成不带页眉页脚的一页。用户可以在这个环境中使用各种排版元素自由发挥，生成自定义的标题页以替代
<code>\maketitle</code> 命令。甚至可以利用 <code>titlepage</code>
环境重新定义 <code>\maketitle</code>：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\renewcommand</span>&#123;<span class="keyword">\maketitle</span>&#125;&#123;<span class="keyword">\begin</span>&#123;titlepage&#125;</span><br><span class="line">... <span class="comment">% 用户自定义命令</span></span><br><span class="line"><span class="keyword">\end</span>&#123;titlepage&#125;&#125;</span><br></pre></td></tr></table></figure>
<p>以上是 <span class="math inline">\(\LaTeX{}\)</span>
标准文档类的标题页相关命令用法。在各种文档模板中经常有自定义的标题页，有可能需要除了
<code>\title</code> 和 <code>\author</code>
以外的命令给定信息，用法也可能与标准文档类的不一致（甚至有些模板可能没有定义
<code>titlepage</code> 等环境）。</p>
<p><strong>使用文档模板前一定要仔细阅读文档模板的帮助文档。</strong></p>
<hr />
<h2 id="交叉引用">交叉引用</h2>
<p>交叉引用是 <span class="math inline">\(\LaTeX{}\)</span>
强大的自动排版功能的体现之一。在能够被交叉引用的地方，如章节、公式、图表、定理等位置使用
<code>\label</code> 命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\label</span>&#123;&lt;label-name&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>之后可以在别处使用 <code>\ref</code> 或 <code>\pageref</code>
命令，分别生成交叉引用的编号和页码：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\ref</span>&#123;&lt;label-name&gt;&#125;  <span class="keyword">\pageref</span>&#123;&lt;label-name&gt;&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114202135903.png" alt="image-20250114202135903" style="zoom:67%;" /></p>
<p>为了生成正确的交叉引用，一般也需要
<strong>多次编译源代码</strong>。</p>
<p><code>label</code>
命令可用于记录各种类型的交叉引用，使用位置分别为：</p>
<ul>
<li><strong>章节标题</strong> 在章节标题命令 <code>\section</code>
等之后紧接着使用。</li>
<li><strong>行间公式</strong>
单行公式在公式内任意位置使用；多行公式在每一行公式的任意位置使用。</li>
<li><strong>有序列表</strong> 在 <code>enumerate</code> 环境的每个
<code>\item</code> 命令之后、下一个 <code>\item</code>
命令之前任意位置使用。</li>
<li><strong>图表标题</strong> 在图表标题命令 <code>\caption</code>
之后紧接着使用。</li>
<li><strong>定理环境</strong> 在定理环境内部任意位置使用。</li>
</ul>
<p>在使用不记编号的命令形式（<code>\section*</code>、<code>\caption*</code>、带可选参数的
<code>\item</code> 命令等）时不要使用 <code>\label</code>
命令，否则生成的引用编号不正确。</p>
<hr />
<h2 id="脚注和边注">脚注和边注</h2>
<p>使用 <code>\footnote</code>
命令可以在页面底部生成一个脚注。假如输入以下文字和命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">“ 天地玄黄，宇宙洪荒。日月盈昃，辰宿列张。” <span class="keyword">\footnote</span>&#123;出自《千字文》。&#125;</span><br></pre></td></tr></table></figure>
<p>在正文中则为：“天地玄黄，宇宙洪荒。日月盈昃，辰宿列张。”<a
href="#fn1" class="footnote-ref" id="fnref1"
role="doc-noteref"><sup>1</sup></a></p>
<p>有些情况下（比如在表格环境、各种盒子内）使用 <code>\footnote</code>
并不能正确生成脚注。可以分两步进行，先使用 <code>\footnotemark</code>
为脚注计数，再在合适的位置用 <code>\footnotetext</code>
生成脚注。比如：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;l&#125;</span><br><span class="line"><span class="keyword">\hline</span></span><br><span class="line">“天地玄黄，宇宙洪荒。日月盈昃，辰宿列张。”<span class="keyword">\footnotemark</span> <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br><span class="line"><span class="keyword">\footnotetext</span>&#123;表格里的名句出自《千字文》。&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114210530107.png" alt="image-20250114210530107" style="zoom:67%;" /></p>
<p>使用 <code>\marginpar</code> 命令可在边栏位置生成边注：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\marginpar</span>[⟨left-margin⟩]&#123;⟨right-margin⟩&#125;</span><br></pre></td></tr></table></figure>
<p>如果只给定了
<code>right-margin</code>，那么边注在奇偶数页文字相同；如果同时给定了
<code>left-margin</code>，则偶数页使用 <code>left-margin</code>
的文字。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114211653969.png" alt="image-20250114211653969" style="zoom:67%;" /></p>
<hr />
<h2 id="特殊环境">特殊环境</h2>
<h3 id="列表">列表</h3>
<p><span class="math inline">\(\LaTeX{}\)</span>
提供了基本的有序和无序列表环境 <code>enumerate</code> 和
<code>itemize</code>，两者用法类似，都用 <code>\item</code>
标明每个列表项。<code>enumerate</code> 环境会自动对列表项编号。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;enumerate&#125;</span><br><span class="line"><span class="keyword">\item</span> …</span><br><span class="line"><span class="keyword">\end</span>&#123;enumerate&#125;</span><br></pre></td></tr></table></figure>
<p>其中 <code>\item</code>
可带一个可选参数，将有序列表的计数或者无序列表的符号替换成自定义的符号。列表可以嵌套使用，最多嵌套四层。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;enumerate&#125;</span><br><span class="line">  <span class="keyword">\item</span> An item.</span><br><span class="line">  <span class="keyword">\begin</span>&#123;enumerate&#125;</span><br><span class="line">    <span class="keyword">\item</span> A nested item.<span class="keyword">\label</span>&#123;itref&#125;</span><br><span class="line">    <span class="keyword">\item</span>[*] A starred item.</span><br><span class="line">  <span class="keyword">\end</span>&#123;enumerate&#125;</span><br><span class="line">  <span class="keyword">\item</span> Reference(<span class="keyword">\ref</span>&#123;itref&#125;).</span><br><span class="line"><span class="keyword">\end</span>&#123;enumerate&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114212322245.png" alt="image-20250114212322245" style="zoom:67%;" /></p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;itemize&#125;</span><br><span class="line">  <span class="keyword">\item</span> An item.</span><br><span class="line">  <span class="keyword">\begin</span>&#123;itemize&#125;</span><br><span class="line">    <span class="keyword">\item</span> A nested item.</span><br><span class="line">    <span class="keyword">\item</span>[+] A `plus&#x27; item.</span><br><span class="line">    <span class="keyword">\item</span> Another item.</span><br><span class="line">  <span class="keyword">\end</span>&#123;itemize&#125;</span><br><span class="line">  <span class="keyword">\item</span> Go back to upper level.</span><br><span class="line"><span class="keyword">\end</span>&#123;itemize&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114212430376.png" alt="image-20250114212430376" style="zoom:67%;"/></p>
<p>关键字环境 <code>description</code> 的用法与以上两者类似，不同的是
<code>\item</code>
后的可选参数用来写关键字，以粗体显示，一般是必填的：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;description&#125;</span><br><span class="line"><span class="keyword">\item</span>[⟨item title⟩] …</span><br><span class="line"><span class="keyword">\end</span>&#123;description&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;description&#125;</span><br><span class="line">  <span class="keyword">\item</span>[Enumerate] Numbered list.</span><br><span class="line">  <span class="keyword">\item</span>[Itemize] Non-numbered list.</span><br><span class="line"><span class="keyword">\end</span>&#123;description&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114212752393.png" alt="image-20250114212752393" style="zoom:67%;" /></p>
<p>各级无序列表的符号由命令 <code>\labelitemi</code> 到
<code>\labelitemiv</code> 定义，可以简单地重新定义它们：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\renewcommand</span>&#123;<span class="keyword">\labelitemi</span>&#125;&#123;<span class="keyword">\ddag</span>&#125;</span><br><span class="line"><span class="keyword">\renewcommand</span>&#123;<span class="keyword">\labelitemii</span>&#125;&#123;<span class="keyword">\dag</span>&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;itemize&#125;</span><br><span class="line">  <span class="keyword">\item</span> First item</span><br><span class="line">  <span class="keyword">\begin</span>&#123;itemize&#125;</span><br><span class="line">    <span class="keyword">\item</span> Subitem</span><br><span class="line">    <span class="keyword">\item</span> Subitem</span><br><span class="line">  <span class="keyword">\end</span>&#123;itemize&#125;</span><br><span class="line">  <span class="keyword">\item</span> Second item</span><br><span class="line"><span class="keyword">\end</span>&#123;itemize&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114213005038.png" alt="image-20250114213005038" style="zoom:67%;" /></p>
<p>有序列表的符号由命令 <code>\labelenumi</code> 到
<code>\labelenumiv</code>
定义，重新定义这些命令需要用到计数器相关命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\renewcommand</span>&#123;<span class="keyword">\labelenumi</span>&#125;<span class="comment">%</span></span><br><span class="line">  &#123;<span class="keyword">\Alph</span>&#123;enumi&#125;&gt;&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;enumerate&#125;</span><br><span class="line">  <span class="keyword">\item</span> First item</span><br><span class="line">  <span class="keyword">\item</span> Second item</span><br><span class="line"><span class="keyword">\end</span>&#123;enumerate&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114213232649.png" alt="image-20250114213232649" style="zoom:67%;" /></p>
<p>默认的列表间距比较宽，<span class="math inline">\(\LaTeX{}\)</span>
本身也未提供方便的定制功能，可用 <code>enumitem</code>
宏包定制各种列表间距。<code>enumitem</code>
宏包还提供了对列表标签、引用等的定制。有兴趣的读者可参考其帮助文档。</p>
<h3 id="对齐环境">对齐环境</h3>
<p><code>center</code>、<code>flushleft</code> 和
<code>flushright</code>
环境分别用于生成居中、左对齐和右对齐的文本环境。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;center&#125; … <span class="keyword">\end</span>&#123;center&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;flushleft&#125; … <span class="keyword">\end</span>&#123;flushleft&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;flushright&#125; … <span class="keyword">\end</span>&#123;flushright&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;center&#125;</span><br><span class="line">Centered text using a</span><br><span class="line"><span class="keyword">\verb</span>|<span class="string">center</span>| environment.</span><br><span class="line"><span class="keyword">\end</span>&#123;center&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;flushleft&#125;</span><br><span class="line">Left-aligned text using a</span><br><span class="line"><span class="keyword">\verb</span>|<span class="string">flushleft</span>| environment.</span><br><span class="line"><span class="keyword">\end</span>&#123;flushleft&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;flushright&#125;</span><br><span class="line">Right-aligned text using a</span><br><span class="line"><span class="keyword">\verb</span>|<span class="string">flushright</span>| environment.</span><br><span class="line"><span class="keyword">\end</span>&#123;flushright&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114214234722.png" alt="image-20250114214234722" style="zoom:67%;" /></p>
<p>除此之外，还可以用以下命令直接改变文字的对齐方式：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\centering</span>   <span class="keyword">\raggedright</span> 	<span class="keyword">\raggedleft</span></span><br></pre></td></tr></table></figure>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\centering</span></span><br><span class="line">Centered text paragraph.</span><br><span class="line"></span><br><span class="line"><span class="keyword">\raggedright</span></span><br><span class="line">Left-aligned text paragraph.</span><br><span class="line"></span><br><span class="line"><span class="keyword">\raggedleft</span></span><br><span class="line">Right-aligned text paragraph.</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114214436517.png" alt="image-20250114214436517" style="zoom:67%;" /></p>
<p>三个命令和对应的环境经常被误用，有直接用所谓 <code>\flushleft</code>
命令或者 <code>raggedright</code>
环境的，都是不甚严格的用法（即使它们可能有效）。有一点可以将两者区分开来：<code>center</code>
等<strong>环境会在上下文产生一个额外间距</strong>，而
<code>\centering</code>
等<strong>命令不产生，只是改变对齐方式</strong>。比如在浮动体环境
<code>table</code> 或 <code>figure</code> 内实现居中对齐，用
<code>\centering</code> 命令即可，没必要再用 <code>center</code>
环境。</p>
<h3 id="引用环境">引用环境</h3>
<p><span class="math inline">\(\LaTeX{}\)</span>
提供了两种引用的环境：<code>quote</code>
用于引用较短的文字，首行不缩进；<code>quotation</code>
用于引用若干段文字，首行缩进。引用环境较一般文字有额外的左右缩进。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">Francis Bacon says:</span><br><span class="line"><span class="keyword">\begin</span>&#123;quote&#125;</span><br><span class="line">Knowledge is power.</span><br><span class="line"><span class="keyword">\end</span>&#123;quote&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114215152828.png" alt="image-20250114215152828" style="zoom:67%;" /></p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">《木兰诗》：</span><br><span class="line"><span class="keyword">\begin</span>&#123;quotation&#125;</span><br><span class="line">万里赴戎机，关山度若飞。</span><br><span class="line">朔气传金柝，寒光照铁衣。</span><br><span class="line">将军百战死，壮士十年归。</span><br><span class="line"></span><br><span class="line">归来见天子，天子坐明堂。</span><br><span class="line">策勋十二转，赏赐百千强。... ...</span><br><span class="line"><span class="keyword">\end</span>&#123;quotation&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114215308777.png" alt="image-20250114215308777" style="zoom:67%;" /></p>
<p><code>verse</code> 用于排版诗歌，与 <code>quotation</code>
恰好相反，<code>verse</code> 是首行悬挂缩进的。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">Rabindranath Tagore&#x27;s short poem:</span><br><span class="line"><span class="keyword">\begin</span>&#123;verse&#125;</span><br><span class="line">Beauty is truth&#x27;s smile</span><br><span class="line">when she beholds her own face in</span><br><span class="line">a perfect mirror.</span><br><span class="line"><span class="keyword">\end</span>&#123;verse&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114215501957.png" alt="image-20250114215501957" style="zoom:67%;" /></p>
<h3 id="摘要环境">摘要环境</h3>
<p>摘要环境 <code>abstract</code> 默认只在标准文档类中的
<code>article</code> 和 <code>report</code> 文档类可用，一般用于紧跟
<code>\maketitle</code> 命令之后介绍文档的摘要。如果文档类指定了
<code>titlepage</code>
选项，则单独成页；反之，单栏排版时相当于一个居中的小标题加一个
<code>quotation</code> 环境，双栏排版时相当于 <code>\section*</code>
定义的一节。</p>
<h3 id="代码环境">代码环境</h3>
<p>有时需要将一段代码原样转义输出，这就要用到代码环境
<code>verbatim</code>，它以等宽字体排版代码，回车和空格也分别起到换行和空位的作用；
带星号的版本更进一步将空格显示成 <code>␣</code>。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;verbatim&#125;<span class="string"></span></span><br><span class="line"><span class="string">#include &lt;iostream&gt;</span></span><br><span class="line"><span class="string">int main()</span></span><br><span class="line"><span class="string">&#123;</span></span><br><span class="line"><span class="string">  std::cout &lt;&lt; &quot;Hello, world!&quot;</span></span><br><span class="line"><span class="string">            &lt;&lt; std::endl;</span></span><br><span class="line"><span class="string">  return 0;</span></span><br><span class="line"><span class="string">&#125;</span></span><br><span class="line"><span class="string"></span><span class="keyword">\end</span>&#123;verbatim&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114220451842.png" alt="image-20250114220451842" style="zoom:67%;" /></p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;verbatim*&#125;<span class="string"></span></span><br><span class="line"><span class="string">for (int i=0; i&lt;4; ++i)</span></span><br><span class="line"><span class="string">  printf(&quot;Number %d\n&quot;,i);</span></span><br><span class="line"><span class="string"></span><span class="keyword">\end</span>&#123;verbatim*&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114220636123.png" alt="image-20250114220636123" style="zoom:67%;" /></p>
<p>要排版简短的代码或关键字，可使用 <code>\verb</code> 命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\verb</span>⟨<span class="string">delim⟩</span>⟨code⟩⟨delim⟩</span><br></pre></td></tr></table></figure>
<p><code>&lt;delim&gt;</code>
标明代码的分界位置，前后必须一致，除字母、空格或星号外，可任意选择使得不与代码本身冲突，习惯上使用
<span class="math inline">\(\texttt|\)</span> 符号。</p>
<p>同 <code>verbatim</code> 环境，<code>\verb</code>
后也可以带一个星号，以显示空格：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\verb</span>|<span class="string">\LaTeX</span>| <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\verb</span>+<span class="string">(a || b)</span>+ <span class="keyword">\verb</span>*<span class="string">+(a || b)+</span></span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114220953291.png" alt="image-20250114220953291" style="zoom:67%;" /></p>
<p><code>\verb</code>
命令对符号的处理比较复杂，一般<strong>不能用在其它命令的参数里</strong>，否则多半会出错。</p>
<p><code>verbatim</code> 宏包优化了 <code>verbatim</code>
环境的内部命令，并提供了 <code>\verbatiminput</code>
命令用来直接读入文件生成代码环境。 <code>fancyvrb</code>
宏包提供了可定制格式的 <code>Verbatim</code> 环境；<code>listings</code>
和 <code>minted</code>
宏包更进一步，可生成关键字高亮的代码环境，支持各种程序设计语言的语法和关键字。详情请参考各自的帮助文档。</p>
<hr />
<h2 id="表格">表格</h2>
<p>排版表格最基本的 <code>tabular</code> 环境用法为：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;[⟨align⟩]&#123;⟨column-spec⟩&#125;</span><br><span class="line">⟨item1⟩ <span class="built_in">&amp;</span> ⟨item2⟩ <span class="built_in">&amp;</span> … <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\hline</span></span><br><span class="line">⟨item1⟩ <span class="built_in">&amp;</span> ⟨item2⟩ <span class="built_in">&amp;</span> … <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p>其中 <code>&lt;column-spec&gt;</code>
是列格式标记，在接下来的内容将仔细介绍；<code>&amp;</code>
用来分隔单元格；<code>\\</code> 用来换行；<code>\hline</code>
用来在行与行之间绘制横线。</p>
<p>直接使用 <code>tabular</code> 环境的话，会
<strong>和周围的文字混排</strong>。此时可用一个可选参数
<code>&lt;align&gt;</code> 控制垂直对齐：<code>t</code> 和
<code>b</code>
分别表示按表格顶部、底部对齐，其他参数或省略不写（默认）表示居中对齐。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|&#125;</span><br><span class="line">  center-<span class="keyword">\\</span> aligned <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;,</span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;[t]&#123;|c|&#125;</span><br><span class="line">  top-<span class="keyword">\\</span> aligned <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;,</span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;[b]&#123;|c|&#125;</span><br><span class="line">  bottom-<span class="keyword">\\</span> aligned<span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125; tabulars.</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114222501332.png" alt="image-20250114222501332" style="zoom:67%;" /></p>
<p>但是通常情况下 <code>tabular</code>
环境很少与文字直接混排，而是会放在 <code>table</code> 浮动体环境中，并用
<code>\caption</code> 命令加标题。</p>
<h3 id="列格式">列格式</h3>
<p><code>tabular</code> 环境使用
<code>&lt;column-spec&gt;</code>参数指定表格的列数以及每列的格式。基本的列格式见下表。</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;"><strong>列格式</strong></th>
<th style="text-align: left;"><strong>说明</strong></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><code>l/c/r</code></td>
<td style="text-align: left;">单元格内容左对齐/居中/右对齐，不折行</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>p\{&lt;width&gt;\}</code></td>
<td style="text-align: left;">单元格宽度固定为
<code>&lt;width&gt;</code>，可自动折行</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\mid\)</span></td>
<td style="text-align: left;">绘制竖线</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>@\{&lt;string&gt;\}</code></td>
<td style="text-align: left;">自定义内容
<code>&lt;string&gt;</code></td>
</tr>
</tbody>
</table>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;lcr|p&#123;6em&#125;&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  left <span class="built_in">&amp;</span> center <span class="built_in">&amp;</span> right</span><br><span class="line">       <span class="built_in">&amp;</span> par box with fixed width<span class="keyword">\\</span></span><br><span class="line">  L    <span class="built_in">&amp;</span> C      <span class="built_in">&amp;</span> R     <span class="built_in">&amp;</span> P <span class="keyword">\\</span></span><br><span class="line"> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115130809432.png" alt="image-20250115130809432" style="zoom:67%;" /></p>
<p>表格中每行的单元格数目不能多于列格式里 <code>l/c/r/p</code>
的总数（可以少于这个总数），否则出错。</p>
<p><code>@</code>
格式可在单元格前后插入任意的文本，但同时它也消除了单元格前后额外添加的间距。<code>@</code>
格式可以适当使用以充当“竖线”。特别地，<code>@\{\}</code>
可直接用来消除单元格前后的间距：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;@&#123;&#125; r@&#123;:&#125;lr @&#123;&#125;&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  1  <span class="built_in">&amp;</span> 1 <span class="built_in">&amp;</span> one <span class="keyword">\\</span></span><br><span class="line">  11 <span class="built_in">&amp;</span> 3 <span class="built_in">&amp;</span> eleven <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115131115056.png" alt="image-20250115131115056" style="zoom:67%;" /></p>
<p>另外还提供了简便的将格式参数重复的写法
<code>*{&lt;n&gt;}{&lt;columnspec&gt;}</code>，比如以下两种写法是等效的：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|c|c|c|c|p&#123;4em&#125;|p&#123;4em&#125;|&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|*&#123;5&#125;&#123;c|&#125;*&#123;2&#125;&#123;p&#123;4em&#125;|&#125;&#125;</span><br></pre></td></tr></table></figure>
<p>有时需要为整列修饰格式，比如整列改变为粗体，如果每个单元格都加上
<code>\bfseries</code> 命令会比较麻烦。<code>array</code>
宏包提供了辅助格式 <span class="math inline">\(\texttt&gt;\)</span> 和
<span
class="math inline">\(\texttt&lt;\)</span>，用于给列格式前后加上修饰命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;array&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;&gt;&#123;<span class="keyword">\itshape</span>&#125;r&lt;&#123;*&#125;l&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  italic <span class="built_in">&amp;</span> normal <span class="keyword">\\</span></span><br><span class="line">  column <span class="built_in">&amp;</span> column <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115131903387.png" alt="image-20250115131903387" style="zoom:67%;" /></p>
<p>辅助格式甚至支持插入 <code>\centering</code> 等命令改变
<code>p</code> 列格式的对齐方式，一般还要加额外的命令
<code>\arraybackslash</code> 以免出错。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;array&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;<span class="comment">%</span></span><br><span class="line">&#123;&gt;&#123;<span class="keyword">\centering</span><span class="keyword">\arraybackslash</span>&#125;p&#123;9em&#125;&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  Some center-aligned long text. <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115132303588.png" alt="image-20250115132303588" style="zoom:67%;" /></p>
<p><code>array</code> 宏包还提供了类似 <code>p</code> 格式的
<code>m</code> 格式和 <code>b</code>
格式，三者分别在垂直方向上靠顶端对齐、居中以及底端对齐。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;array&#125;</span></span><br><span class="line"><span class="keyword">\newcommand</span><span class="keyword">\txt</span>&#123;a b c d e f g h i&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;cp&#123;2em&#125;m&#123;2em&#125;b&#123;2em&#125;&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  pos <span class="built_in">&amp;</span> <span class="keyword">\txt</span> <span class="built_in">&amp;</span> <span class="keyword">\txt</span> <span class="built_in">&amp;</span> <span class="keyword">\txt</span> <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115132526382.png" alt="image-20250115132526382" style="zoom:67%;" /></p>
<h3 id="列宽">列宽</h3>
<p>在控制列宽方面，<span class="math inline">\(\LaTeX{}\)</span>
表格有着明显的不足：<code>l/c/r</code>
格式的列宽是由文字内容的自然宽度决定的，而 <code>p</code>
格式给定了列宽却不好控制对齐（可用 <code>array</code>
宏包的辅助格式），更何况列与列之间通常还有间距，所以直接生成给定总宽度的表格并不容易。</p>
<p><span class="math inline">\(\LaTeX{}\)</span> 本身提供了
<code>tabular*</code> 环境用来排版定宽表格，但是不太方便使用，比如要用到
<code>@</code> 格式插入额外命令，令单元格之间的间距为
<code>\fill</code>，但即使这样仍然有瑕疵：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular*&#125;&#123;14em&#125;<span class="comment">%</span></span><br><span class="line">&#123;@&#123;<span class="keyword">\extracolsep</span>&#123;<span class="keyword">\fill</span>&#125;&#125;|c|c|c|c|&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  A <span class="built_in">&amp;</span> B <span class="built_in">&amp;</span> C <span class="built_in">&amp;</span> D <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line">  a <span class="built_in">&amp;</span> b <span class="built_in">&amp;</span> c <span class="built_in">&amp;</span> d <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular*&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115132953911.png" alt="image-20250115132953911" style="zoom:67%;" /></p>
<p><code>tabularx</code> 宏包为我们提供了方便的解决方案。它引入了一个
<code>X</code> 列格式，类似 <code>p</code>
列格式，不过会根据表格宽度自动计算列宽，多个 <code>X</code>
列格式平均分配列宽。 <code>X</code> 列格式也可以用 <code>array</code>
里的辅助格式修饰对齐方式：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;array,tabularx&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;tabularx&#125;&#123;14em&#125;<span class="comment">%</span></span><br><span class="line">&#123;|*&#123;4&#125;&#123;&gt;&#123;<span class="keyword">\centering</span><span class="keyword">\arraybackslash</span>&#125;X|&#125;&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  A <span class="built_in">&amp;</span> B <span class="built_in">&amp;</span> C <span class="built_in">&amp;</span> D <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line">  a <span class="built_in">&amp;</span> b <span class="built_in">&amp;</span> c <span class="built_in">&amp;</span> d <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabularx&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115133221441.png" alt="image-20250115133221441" style="zoom:67%;" /></p>
<h3 id="横线">横线</h3>
<p>在之前的例子见过许多次绘制表格线的 <code>\hline</code> 命令。另外
<code>\cline{&lt;i&gt;-&lt;j&gt;}</code>
用来绘制跨越部分单元格的横线：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|c|c|&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  4 <span class="built_in">&amp;</span> 9 <span class="built_in">&amp;</span> 2 <span class="keyword">\\</span> </span><br><span class="line">  <span class="keyword">\cline</span>&#123;2-3&#125;</span><br><span class="line">  3 <span class="built_in">&amp;</span> 5 <span class="built_in">&amp;</span> 7 <span class="keyword">\\</span> </span><br><span class="line">  <span class="keyword">\cline</span>&#123;1-1&#125;</span><br><span class="line">  8 <span class="built_in">&amp;</span> 1 <span class="built_in">&amp;</span> 6 <span class="keyword">\\</span> </span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115133628644.png"
alt="image-20250115133628644" />
<figcaption aria-hidden="true">image-20250115133628644</figcaption>
</figure>
<p>在科技论文排版中广泛应用的表格形式是三线表，形式干净简明。三线表由
<code>booktabs</code> 宏包支持，它提供了
<code>\toprule</code>、<code>\midrule</code> 和 <code>\bottomrule</code>
命令用以排版三线表的三条线，以及和 <code>\cline</code> 对应的
<code>\cmidrule</code>。除此之外，最好不要用其它横线以及竖线：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;booktabs&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;cccc&#125;</span><br><span class="line">  <span class="keyword">\toprule</span></span><br><span class="line">   <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;3&#125;&#123;c&#125;&#123;Numbers&#125; <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\cmidrule</span>&#123;2-4&#125;</span><br><span class="line">           <span class="built_in">&amp;</span> 1 <span class="built_in">&amp;</span> 2 <span class="built_in">&amp;</span> 3 <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\midrule</span></span><br><span class="line">  Alphabet <span class="built_in">&amp;</span> A <span class="built_in">&amp;</span> B <span class="built_in">&amp;</span> C <span class="keyword">\\</span></span><br><span class="line">  Roman    <span class="built_in">&amp;</span> I <span class="built_in">&amp;</span> II<span class="built_in">&amp;</span> III <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\bottomrule</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115140704162.png" alt="image-20250115140704162" style="zoom:67%;" /></p>
<h3 id="合并单元格">合并单元格</h3>
<p>横向合并单元格由 <code>\multicolumn</code> 命令实现：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\multicolumn</span>&#123;⟨n⟩&#125;&#123;⟨column-spec⟩&#125;&#123;⟨item⟩&#125;</span><br></pre></td></tr></table></figure>
<p>其中 <code>&lt;n&gt;</code>
为要合并的列数，<code>&lt;column-spec&gt;</code>
为合并单元格后的列格式，只允许出现一个 <code>l/c/r</code> 或
<code>p</code> 格式。如果合并前的单元格前后带表格线 <span
class="math inline">\(\texttt|\)</span>，合并后的列格式也要带 <span
class="math inline">\(\texttt|\)</span> 以使得表格的竖线一致。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|c|c|&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  1 <span class="built_in">&amp;</span> 2 <span class="built_in">&amp;</span> Center <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line">  <span class="keyword">\multicolumn</span>&#123;2&#125;&#123;|c|&#125;&#123;3&#125; <span class="built_in">&amp;</span></span><br><span class="line">  <span class="keyword">\multicolumn</span>&#123;1&#125;&#123;r|&#125;&#123;Right&#125; <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line">  4 <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;2&#125;&#123;c|&#125;&#123;C&#125; <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115142430321.png" alt="image-20250115142430321" style="zoom:67%;" /></p>
<p><strong>纵向合并单元格</strong> 需要用到 <code>multirow</code>
宏包提供的 <code>\multirow</code> 命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\multirow</span>&#123;⟨n⟩&#125;&#123;⟨width⟩&#125;&#123;⟨item⟩&#125;</span><br></pre></td></tr></table></figure>
<p><width> 为合并后单元格的宽度，可以填 <code>*</code>
以使用自然宽度。</p>
<p>我们看一个结合 <code>\cline</code>、<code>\multicolumn</code> 和
<code>\multirow</code> 命令的例子：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;multirow&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;ccc&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  <span class="keyword">\multirow</span>&#123;2&#125;&#123;*&#125;&#123;Item&#125; <span class="built_in">&amp;</span></span><br><span class="line">    <span class="keyword">\multicolumn</span>&#123;2&#125;&#123;c&#125;&#123;Value&#125; <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\cline</span>&#123;2-3&#125;</span><br><span class="line">    <span class="built_in">&amp;</span> First <span class="built_in">&amp;</span> Second <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line">  A <span class="built_in">&amp;</span> 1     <span class="built_in">&amp;</span> 2 <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115142135335.png" alt="image-20250115142135335" style="zoom:67%;" /></p>
<h3 id="嵌套表格">嵌套表格</h3>
<p>在以下的例子中，注意要用 <code>\multicolumn</code> 命令配合
<code>@\{\}</code>
格式把单元格的额外边距去掉，使得嵌套的表格线能和外层的表格线正确相连：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|c|c|&#125;</span><br><span class="line"> <span class="keyword">\hline</span></span><br><span class="line"> a <span class="built_in">&amp;</span> b <span class="built_in">&amp;</span> c <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"> a <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;1&#125;&#123;@&#123;&#125;c@&#123;&#125;|&#125;</span><br><span class="line"> &#123;<span class="keyword">\begin</span>&#123;tabular&#125;&#123;c|c&#125;</span><br><span class="line">   e <span class="built_in">&amp;</span> f <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line">   e <span class="built_in">&amp;</span> f <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\end</span>&#123;tabular&#125;&#125;</span><br><span class="line">       <span class="built_in">&amp;</span> c <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"> a <span class="built_in">&amp;</span> b <span class="built_in">&amp;</span> c <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115142824913.png" alt="image-20250115142824913" style="zoom:67%;" /></p>
<p>如果不需要为“拆分的单元格”画线，并且只在垂直方向“拆分”的话，<code>makecell</code>
宏包提供的 <code>\makecell</code> 命令是一个简单的解决方案：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;makecell&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|c|&#125;</span><br><span class="line"> <span class="keyword">\hline</span></span><br><span class="line"> a <span class="built_in">&amp;</span> <span class="keyword">\makecell</span>&#123;d1 <span class="keyword">\\</span> d2&#125; <span class="keyword">\\</span></span><br><span class="line"> <span class="keyword">\hline</span></span><br><span class="line"> b <span class="built_in">&amp;</span> c <span class="keyword">\\</span></span><br><span class="line"> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115143114195.png" alt="image-20250115143114195" style="zoom:67%;" /></p>
<h3 id="行距控制">行距控制</h3>
<p><span class="math inline">\(\LaTeX{}\)</span>
生成的表格看起来通常比较紧凑。修改参数 <code>\arraystretch</code>
可以得到行距更加宽松的表格。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\renewcommand</span><span class="keyword">\arraystretch</span>&#123;1.8&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  Really loose <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line">  tabular rows.<span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;example&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115143416039.png" alt="image-20250115143416039" style="zoom:67%;" /></p>
<p>另一种增加间距的办法是给换行命令 <code>\\</code>
添加可选参数，在这一行下面加额外的间距，适合用于在行间不加横线的表格：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;c&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  Head lines <span class="keyword">\\</span>[6pt]</span><br><span class="line">  tabular lines <span class="keyword">\\</span></span><br><span class="line">  tabular lines <span class="keyword">\\</span> </span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115143537544.png" alt="image-20250115143537544" style="zoom:67%;" /></p>
<p>但是这种换行方式的存在导致了一个缺陷——从第二行开始，表格的首个单元格不能直接使用中括号
<code>[]</code>，否则 <code>\\</code>
往往会将下一行的中括号当作自己的可选参数，因而出错。如果要使用中括号，应当放在花括号
<code>{}</code> 里面。或者也可以选择将换行命令写成
<code>\\\\[0pt]</code>。</p>
<hr />
<h2 id="图片">图片</h2>
<p><span class="math inline">\(\LaTeX{}\)</span>
本身不支持插图功能，需要由 <code>graphicx</code> 宏包辅助支持。</p>
<p>使用 <code>latex + dvipdfmx</code> 编译命令时，调用
<code>graphicx</code> 宏包时要指定 <code>dvipdfmx</code> 选项；而使用
<code>pdflatex</code> 或 <code>xelatex</code> 命令编译时不需要。</p>
<p>不同编译命令支持的图片格式种类各异，见下表。这个表格也能解答诸如“为什么
<code>.eps</code> 格式图片在 <code>pdflatex</code>
编译命令下出错”之类的问题。本表格也再一次说明，使用 <code>xelatex</code>
命令是最推荐的方式。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115144839982.png" alt="image-20250115144839982" style="zoom:67%;" /></p>
<p>在调用了 <code>graphicx</code> 宏包以后，就可以使用
<code>\includegraphics</code> 命令加载图片了：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\includegraphics</span>[⟨options⟩]&#123;⟨filename⟩&#125;</span><br></pre></td></tr></table></figure>
<p>其中 <code>filename</code>
为图片文件名，文件名可能需要用相对路径或绝对路径表示。图片文件的扩展名一般可不写。另外一定要注意，<strong>文件名里既不要有空格也不要有多余的英文点号</strong>，否则宏包在解析文件名的过程中会出错。</p>
<p>另外 <code>graphicx</code> 宏包还提供了 <code>graphicspath</code>
命令，用于声明一个或多个图片文件存放的目录，使用这些目录里的图片时可不用写路径：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% 假设主要的图片放在 figures 子目录下，标志放在 logo 子目录下</span></span><br><span class="line"><span class="keyword">\graphicspath</span>&#123;&#123;figures/&#125;&#123;logo/&#125;&#125;</span><br></pre></td></tr></table></figure>
<p>在 <code>\includegraphics</code> 命令的可选参数
<code>&lt;options&gt;</code> 中可以使用
<code>&lt;key&gt;=&lt;value&gt;</code> 的形式，常用的参数如下：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115145631351.png" alt="image-20250115145631351" style="zoom:67%;" /></p>
<p><code>graphicx</code> 宏包也支持
<code>draft</code>/<code>final</code> 选项。当 <code>graphicx</code>
宏包或文档类指定 <code>draft</code>
选项时，图片将不会被实际插入，取而代之的是一个包含文件名的与原图片等大的方框。(<strong>加快编译过程</strong>)</p>
<hr />
<h2 id="盒子">盒子</h2>
<p>盒子是 <span class="math inline">\(\LaTeX{}\)</span>
排版的基础单元，虽然解释略有抽象：每一行是一个盒子，里面的文字从左到右依次排列；每一页也是一个盒子，各行文字从上到下依次排布……颇有一些活字印刷术的味道。</p>
<p>不管如何，<span class="math inline">\(\LaTeX{}\)</span>
提供了一些命令让我们手动生成一些有特定用途的盒子。</p>
<h3 id="水平盒子">水平盒子</h3>
<p>生成水平盒子的命令如下：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\mbox</span>&#123;…&#125;</span><br><span class="line"><span class="keyword">\makebox</span>[⟨width⟩][⟨align⟩]&#123;…&#125;</span><br></pre></td></tr></table></figure>
<p><code>\mbox</code>
生成一个基本的水平盒子，内容只有一行，不允许分段（除非嵌套其它盒子，比如后文的垂直盒子）。外表看上去，<code>\mbox</code>
的内容与正常的文本无二，不过断行时文字不会从盒子里断开。</p>
<p><code>\makebox</code> 更进一步，可以加上可选参数用于控制盒子的宽度
<code>&lt;width&gt;</code>，以及内容的对齐方式
<code>&lt;align&gt;</code>，可选居中 <code>c</code>（默认值）、左对齐
<code>l</code>、右对齐 <code>r</code> 和分散对齐 <code>s</code>。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">|<span class="keyword">\mbox</span>&#123;Test some words.&#125;|<span class="keyword">\\</span></span><br><span class="line">|<span class="keyword">\makebox</span>[10em]&#123;Test some words.&#125;|<span class="keyword">\\</span></span><br><span class="line">|<span class="keyword">\makebox</span>[10em][l]&#123;Test some words.&#125;|<span class="keyword">\\</span></span><br><span class="line">|<span class="keyword">\makebox</span>[10em][r]&#123;Test some words.&#125;|<span class="keyword">\\</span></span><br><span class="line">|<span class="keyword">\makebox</span>[10em][s]&#123;Test some words.&#125;|</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115150814856.png" alt="image-20250115150814856" style="zoom:67%;" /></p>
<h3 id="带框的水平盒子">带框的水平盒子</h3>
<p><code>\fbox</code> 和 <code>\framebox</code>
让我们可以为水平盒子添加边框。使用的语法与 <code>\mbox</code> 和
<code>\makebox</code> 一模一样：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\fbox</span>&#123;…&#125;</span><br><span class="line"><span class="keyword">\framebox</span>[⟨width⟩][⟨align⟩]&#123;…&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\fbox</span>&#123;Test some words.&#125;<span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\framebox</span>[10em][r]&#123;Test some words.&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115151302157.png" alt="image-20250115151302157" style="zoom:67%;" /></p>
<p>可以通过 <code>\setlength</code> 命令调节边框的宽度
<code>\fboxrule</code> 和内边距 <code>\fboxsep</code>：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\framebox</span>[10em][r]&#123;Test box&#125;<span class="keyword">\\</span>[1ex]</span><br><span class="line"><span class="keyword">\setlength</span>&#123;<span class="keyword">\fboxrule</span>&#125;&#123;1.6pt&#125;</span><br><span class="line"><span class="keyword">\setlength</span>&#123;<span class="keyword">\fboxsep</span>&#125;&#123;1em&#125;</span><br><span class="line"><span class="keyword">\framebox</span>[10em][r]&#123;Test box&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115151733062.png" alt="image-20250115151733062" style="zoom:67%;" /></p>
<h3 id="垂直盒子">垂直盒子</h3>
<p>如果需要排版一个文字可以换行的盒子，<span
class="math inline">\(\LaTeX{}\)</span> 提供了两种方式：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\parbox</span>[⟨align⟩][⟨height⟩][⟨inner-align⟩]&#123;⟨width⟩&#125;&#123;…&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;minipage&#125;[⟨align⟩][⟨height⟩][⟨inner-align⟩]&#123;⟨width⟩&#125;</span><br><span class="line">…</span><br><span class="line"><span class="keyword">\end</span>&#123;minipage&#125;</span><br></pre></td></tr></table></figure>
<p>其中 <code>&lt;align&gt;</code>
为盒子和周围文字的对齐情况；<code>&lt;height&gt;</code> 和
<code>&lt;inner-align&gt;</code>
设置盒子的高度和内容的对齐方式，<code>&lt;inner-align&gt;</code>
接受的参数是顶部 <code>t</code>、底部 <code>b</code>、居中
<code>c</code> 和分散对齐 <code>s</code>。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">三字经：<span class="keyword">\parbox</span>[t]&#123;3em&#125;<span class="comment">%</span></span><br><span class="line">&#123;人之初 性本善 性相近 习相远&#125;</span><br><span class="line"><span class="keyword">\quad</span></span><br><span class="line">千字文：</span><br><span class="line"><span class="keyword">\begin</span>&#123;minipage&#125;[b][8ex][t]&#123;4em&#125;</span><br><span class="line">天地玄黄 宇宙洪荒</span><br><span class="line"><span class="keyword">\end</span>&#123;minipage&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115152828516.png" alt="image-20250115152828516" style="zoom:67%;" /></p>
<p>如果在 <code>minipage</code> 里使用 <code>\footnote</code>
命令，生成的脚注会出现在盒子底部，编号是独立的，并且使用小写字母编号。这也是
<code>minipage</code> 环境之被称为“迷你页”（Mini-page）的原因。而在
<code>\parbox</code> 里无法正常使用 <code>\footnote</code>
命令，只能在盒子里使用 <code>\footnotemark</code>，在盒子外使用
<code>\footnotetext</code>。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\fbox</span>&#123;<span class="keyword">\begin</span>&#123;minipage&#125;&#123;15em&#125;<span class="comment">%</span></span><br><span class="line">  这是一个垂直盒子的测试。</span><br><span class="line">  <span class="keyword">\footnote</span>&#123;脚注来自 minipage。&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;minipage&#125;&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115153544989.png" alt="image-20250115153544989" style="zoom:67%;" /></p>
<h3 id="标尺盒子">标尺盒子</h3>
<p><code>\rule</code>
命令用来画一个实心的矩形盒子，也可适当调整以用来画线（标尺）：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\rule</span>[⟨raise⟩]&#123;⟨width⟩&#125;&#123;⟨height⟩&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">Black <span class="keyword">\rule</span>&#123;12pt&#125;&#123;4pt&#125; box.</span><br><span class="line"></span><br><span class="line">Upper <span class="keyword">\rule</span>[4pt]&#123;6pt&#125;&#123;8pt&#125; and</span><br><span class="line">lower <span class="keyword">\rule</span>[-4pt]&#123;6pt&#125;&#123;8pt&#125; box.</span><br><span class="line"></span><br><span class="line">A <span class="keyword">\rule</span>[-.4pt]&#123;3em&#125;&#123;.4pt&#125; line.</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115153851284.png" alt="image-20250115153851284" style="zoom:67%;" /></p>
<hr />
<h2 id="浮动体">浮动体</h2>
<p>内容丰富的文章或者书籍往往包含许多图片和表格等内容。这些内容的尺寸往往太大，导致分页困难。<span
class="math inline">\(\LaTeX{}\)</span>
为此引入了浮动体的机制，令大块的内容可以脱离上下文，放置在合适的位置。
<span class="math inline">\(\LaTeX{}\)</span> 预定义了两类浮动体环境
<code>figure</code> 和 <code>table</code>。习惯上 <code>figure</code>
里放图片，<code>table</code>
里放表格，但并没有严格限制，可以在任何一个浮动体里放置文字、公式、表格、图片等等任意内容。</p>
<p>以 <code>table</code> 环境的用法举例，<code>figure</code> 同理：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;table&#125;[⟨placement⟩]</span><br><span class="line">…</span><br><span class="line"><span class="keyword">\end</span>&#123;table&#125;</span><br></pre></td></tr></table></figure>
<p><code>&lt;placement&gt;</code>
参数提供了一些符号用来表示浮动体允许排版的位置，如 <code>hbp</code>
允许浮动体排版在当前位置、底部或者单独成页。<code>table</code> 和
<code>figure</code> 浮动体的默认设置为 <code>tbp</code>。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115155105789.png" alt="image-20250115155105789" style="zoom:67%;" /></p>
<p>双栏排版环境下，<span class="math inline">\(\LaTeX{}\)</span> 提供了
<code>table*</code> 和 <code>figure*</code> 环境用来排版
<strong>跨栏</strong> 的浮动体。用法与 <code>table</code> 和
<code>figure</code> 一样，不同之处为双栏的
<code>&lt;placement&gt;</code> 参数只能用 <code>tp</code> 两个位置。</p>
<p>浮动体的位置选取受到先后顺序的限制。如果某个浮动体由于参数限制、空间限制等原因在当前页无法放置，就要推迟到之后处理，并使得之后的同类浮动体一并推迟。<code>\clearpage</code>
命令会在另起一页之前，先将所有推迟处理的浮动体排版成页，此时
<code>htbp</code> 等位置限制被完全忽略。</p>
<p><code>float</code> 宏包为浮动体提供了 <code>H</code> 位置参数，不与
<code>htbp</code> 及 <code>!</code> 混用。使用 <code>H</code>
位置参数时，会取消浮动机制，将浮动体视为一般的盒子插入当前位置。这在一些特殊情况下很有用（如使用
<code>multicol</code>
宏包排版分栏内容的时候），但尺寸过大的浮动体可能使得分页比较困难。</p>
<h3 id="浮动体的标题">浮动体的标题</h3>
<p>图表等浮动体提供了 <code>\caption</code>
命令加标题，并且自动给浮动体编号，可以用带星号的命令
<code>\caption*</code>(需加载相关宏包，如 <code>caption</code>)
生成不带编号的标题，也可以使用带可选参数的形式
<code>\caption[...]{...}</code>，使得在目录里使用短标题。命令之后还可以紧跟
<code>\label</code> 命令标记交叉引用。</p>
<p><code>\caption</code> 生成的标题形如“Figure 1: …”或“Table 1:
…”可通过修改 <code>\figurename</code> 和 <code>\tablename</code>
的内容来修改标题的前缀。标题样式的定制功能由 <code>caption</code>
宏包提供，详见该宏包的帮助文档，在此不作赘述。</p>
<p><code>table</code> 和 <code>figure</code>
两种浮动体分别有各自的生成目录的命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\listoftables</span></span><br><span class="line"><span class="keyword">\listoffigures</span></span><br></pre></td></tr></table></figure>
<p>它们类似 <code>\tableofcontents</code> 生成单独的章节。</p>
<h3 id="并排和子图表">并排和子图表</h3>
<p>我们时常有在一个浮动体里面放置多张图的用法。最简单的用法就是直接并排放置，也可以通过分段或者换行命令
<code>\\</code>
排版多行多列的图片。以下为示意代码，效果大致如图所示。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;figure&#125;[htbp]</span><br><span class="line">  <span class="keyword">\centering</span></span><br><span class="line">  <span class="keyword">\includegraphics</span>[width=...]&#123;...&#125;</span><br><span class="line">  <span class="keyword">\qquad</span></span><br><span class="line">  <span class="keyword">\includegraphics</span>[width=...]&#123;...&#125; <span class="keyword">\\</span>[...pt]</span><br><span class="line">  <span class="keyword">\includegraphics</span>[width=...]&#123;...&#125;</span><br><span class="line">  <span class="keyword">\caption</span>&#123;...&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;figure&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115160703778.png" alt="image-20250115160703778" style="zoom:67%;" /></p>
<p>由于标题是横跨一行的，用 <code>\caption</code>
命令为每个图片单独生成标题就需要借助前文提到的 <code>parbox</code> 或者
<code>minipage</code> 环境，将标题限制在盒子内。效果见图所示。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;figure&#125;[htbp]</span><br><span class="line">  <span class="keyword">\centering</span></span><br><span class="line">  <span class="keyword">\begin</span>&#123;minipage&#125;&#123;...&#125;</span><br><span class="line">    <span class="keyword">\centering</span></span><br><span class="line">    <span class="keyword">\includegraphics</span>[width=...]&#123;...&#125;</span><br><span class="line">    <span class="keyword">\caption</span>&#123;...&#125;</span><br><span class="line">  <span class="keyword">\end</span>&#123;minipage&#125;</span><br><span class="line">  <span class="keyword">\qquad</span></span><br><span class="line">  <span class="keyword">\begin</span>&#123;minipage&#125;&#123;...&#125;</span><br><span class="line">    <span class="keyword">\centering</span></span><br><span class="line">    <span class="keyword">\includegraphics</span>[width=...]&#123;...&#125;</span><br><span class="line">    <span class="keyword">\caption</span>&#123;...&#125;</span><br><span class="line">  <span class="keyword">\end</span>&#123;minipage&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;figure&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115161039696.png" alt="image-20250115161039696" style="zoom:67%;" /></p>
<p>当需要更进一步，给每个图片定义小标题时，就要用到
<code>subcaption</code>
宏包的功能了。这里仅举一例，效果见图。更详细的用法请参考
<code>subcaption</code> 宏包的帮助文档。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;figure&#125;[htbp]</span><br><span class="line">  <span class="keyword">\centering</span></span><br><span class="line">  <span class="keyword">\begin</span>&#123;subfigure&#125;&#123;...&#125;</span><br><span class="line">    <span class="keyword">\centering</span></span><br><span class="line">    <span class="keyword">\includegraphics</span>[width=...]&#123;...&#125;</span><br><span class="line">    <span class="keyword">\caption</span>&#123;...&#125;</span><br><span class="line">  <span class="keyword">\end</span>&#123;subfigure&#125;</span><br><span class="line">  <span class="keyword">\qquad</span></span><br><span class="line">  <span class="keyword">\begin</span>&#123;subfigure&#125;&#123;...&#125;</span><br><span class="line">    <span class="keyword">\centering</span></span><br><span class="line">    <span class="keyword">\includegraphics</span>[width=...]&#123;...&#125;</span><br><span class="line">    <span class="keyword">\caption</span>&#123;...&#125;</span><br><span class="line">  <span class="keyword">\end</span>&#123;subfigure&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;figure&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115161220429.png" alt="image-20250115161220429" style="zoom:67%;" /></p>
<p><code>subcaption</code> 依赖上文提到过的 <code>caption</code>
宏包，因此也支持子图表标题样式的定制。并排子图表的功能也可通过
<code>subfig</code> 宏包的 <code>subfloat</code>
命令实现，具体请参考宏包文档。</p>
<aside id="footnotes" class="footnotes footnotes-end-of-document"
role="doc-endnotes">
<hr />
<ol>
<li id="fn1"><p>出自《千字文》。<a href="#fnref1" class="footnote-back"
role="doc-backlink">↩︎</a></p></li>
</ol>
</aside>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>lshort</tag>
      </tags>
  </entry>
  <entry>
    <title>lshort笔记：排版样式设定（五）</title>
    <url>/2025/01/30/notes-reading/computer/lshort/lshort-05-layout-style/</url>
    <content><![CDATA[<blockquote>
<p>本文内容来自<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">一份
(不太) 简短的 LATEX2ε 介绍<i class="fa fa-external-link-alt"></i></span>。</p>
</blockquote>
<p>本章内容将一览如何修改 <span class="math inline">\(\LaTeX{}\)</span>
的排版样式，具体包括设置字体和字号，修饰和强调文字，调整段落格式和间距，设定页面参数和分栏，以及如何更改页眉页脚的样式和内容。</p>
<span id="more"></span>
<hr />
<h2 id="字体和字号">字体和字号</h2>
<p><span class="math inline">\(\LaTeX{}\)</span>
根据文档的逻辑结构（章节、脚注等）来选择默认的字体样式以及字号。需要更改字体样式或字号的话，可以使用
下表 中列出的命令。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250121100704497.png" alt="image-20250121100704497" style="zoom:67%;" /></p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250121101016109.png" alt="image-20250121101016109" style="zoom:67%;" /></p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250121101037202.png" alt="image-20250121101037202" style="zoom:67%;" /></p>
<h3 id="字体样式">字体样式</h3>
<p><span class="math inline">\(\LaTeX{}\)</span>
提供了两组修改字体的命令，见上表。其中诸如 <code>\bfseries</code>
形式的命令将会影响之后所有的字符，如果想要让它在局部生效，需要用花括号<strong>分组</strong>，也就是写成<code>\bfseries &lt;some text&gt;</code>
这样的形式；对应的 <code>\textbf</code>
形式带一个参数，只改变参数内部的字体，更为常用。</p>
<p>在公式中，直接使用<code>\textbf</code>
等命令不会起效，甚至报错。<span class="math inline">\(\LaTeX{}\)</span>
提供了修改数学字母样式的命令，如 <code>\mathbf</code> 等。</p>
<h3 id="字号">字号</h3>
<p>字号命令实际大小依赖于所使用的文档类及其选项。下表列出了这些命令在标准文档类中的绝对大小，单位为
pt。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250121102202770.png" alt="image-20250121102202770" style="zoom:67%;" /></p>
<p>使用字号命令的时候，通常也需要用花括号进行分组，如同
<code>\rmfamily</code> 那样。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250121102848593.png" alt="image-20250121102848593" style="zoom:67%;" /></p>
<p><span class="math inline">\(\LaTeX{}\)</span> 还提供了一个基础的命令
<code>\fontsize</code> 用于设定任意大小的字号：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\fontsize</span>&#123;&lt;size&gt;&#125;&#123;&lt;base line-skip&gt;&#125;</span><br></pre></td></tr></table></figure>
<p><code>\fontsize</code> 用到两个参数，<code>&lt;size&gt;</code>
为字号，<code>&lt;base lineskip&gt;</code> 为基础行距。表 5.3
中的命令也都各自设定了与字号对应的基础行距，大小为字号的 1.2
倍。如果不是在导言区，<code>\fontsize</code> 的设定需要
<code>\selectfont</code> 命令才能立即生效，而表 5.2
的字号设定都是立即生效的。</p>
<h3 id="选用字体宏包">选用字体宏包</h3>
<p>下表
列出了较为常用的字体宏包，其中相当多的宏包还配置了数学字体，或者文本、数学字体兼而有之。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250121105449196.png" alt="image-20250121105449196" style="zoom:67%;" /></p>
<h3 id="字体编码">字体编码</h3>
<p>常见的正文字体编码有 <code>OT1</code> 和 <code>T1</code> 等。<span
class="math inline">\(\LaTeX{}\)</span> 默认使用兼容 plainTeX 的
<code>OT1</code> 编码，使用起来有诸多限制：高德纳在设计 Computer Modern
字体时认为一些符号，如大于号、小于号等，原则上都应该在公式里出现，所以在正文字体里这些符号所在的位置被其它符号所占据（
<code>OT1</code> 字体编码、<code>rmfamily</code> 和 <code>sffamly</code>
字体族下， <code>&lt;</code> 和 <code>&gt;</code> 排版 <span
class="math inline">\(!\)</span> 和 <span
class="math inline">\(¿\)</span>
两个倒立的标点符号，正常的大于号和小于号可用命令
<code>\textgreater</code> 和 <code>\textless</code>
输入；<code>ttfamily</code> 字体族下是正常的大于号和小于号）。扩展的
<code>T1</code> 字体编码则更加靠近 ASCII
文本编码，不会出现上述的大于号、小于号的问题。</p>
<p>切换字体编码要用到 <code>fontenc</code> 宏包：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\usepackage</span>[T1]&#123;fontenc&#125;</span><br></pre></td></tr></table></figure>
<p>如果使用 <code>xelatex</code> 编译方式，并使用 <code>fontspec</code>
宏包调用 <code>ttf</code> 或 <code>otf</code> 格式字体，就不要再使用
<code>fontenc</code> 宏包。</p>
<h3 id="使用-fontspec-宏包更改字体">使用 <code>fontspec</code>
宏包更改字体</h3>
<p><code>xelatex</code> 和 <code>lualatex</code>
编译命令能够支持直接调用系统和 <span
class="math inline">\(\TeX{}\)</span> 发行版中的 <code>.ttf</code> 或
<code>.otf</code> 格式字体(Linux 下的 TeXLive 为了令 XeTeX 使用 OpenType
字体，需要额外的配置）。相比于前文介绍的字体宏包，我们有了更多自由修改字体的余地。</p>
<p><code>xelatex</code> 和 <code>lualatex</code>
命令下支持用户调用字体的宏包是
<code>fontspec</code>。宏包提供了几个设置全局字体的命令，设置
<code>\rmfamily</code> 等对应命令的默认字体：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\setmainfont</span>&#123;&lt;font name&gt;&#125;[&lt;font features&gt;] <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\setsansfont</span>&#123;&lt;font name&gt;&#125;[&lt;font features&gt;] <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\setmonofont</span>&#123;&lt;font name&gt;&#125;[&lt;font features&gt;]</span><br></pre></td></tr></table></figure>
<p>其中 <code>&lt;font name&gt;</code>
使用字体的文件名（带扩展名）或者字体的英文名称。<code>&lt;font features&gt;</code>
用来手动配置对应的粗体或斜体，比如为 Windows 下的无衬线字体 Arial
配置粗体和斜体（通常情况下自动检测并设置对应的粗体和斜体，无需手动指定）：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\setsansfont</span>&#123;Arial&#125;[BoldFont=&#123;Arial Bold&#125;, ItalicFont=&#123;Arial Italic&#125;]</span><br></pre></td></tr></table></figure>
<p><code>&lt;font features&gt;</code>
还能配置字体本身的各种特性，这里不再赘述，感兴趣的读者请参考
<code>fontspec</code> 宏包的帮助文档。</p>
<p>需要注意的是，<code>fontspec</code> 宏包会覆盖数学字体设置。需要调用
上表 中列出的一些数学字体宏包时，应当在调用 <code>fontspec</code>
宏包时指定 <code>no-math</code> 选项。<code>fontspec</code>
宏包可能被其它宏包或文档类（如 <code>ctex</code>
文档类）自动调用时，则在文档开头的 <code>documentclass</code> 命令里指定
<code>no-math</code> 选项。</p>
<h3 id="在-ctex-宏包或文档类中更改中文字体">在 <code>ctex</code>
宏包或文档类中更改中文字体</h3>
<p>前文已经介绍过的 <code>ctex</code>
宏包或文档类提供了和<code>fontspec</code>
宏包非常类似的语法设置中文字体(使用 <code>xelatex</code>
编译时，这几个命令实际上由 <code>xeCJK</code> 宏包提供；使用
<code>lualatex</code> 编译时，则是由 <code>ctex</code> 宏包或文档类对
<code>luatexja</code> 宏包提供的类似命令进行额外封装)。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\setCJKmainfont</span>&#123;&lt;font name&gt;&#125;[&lt;font features&gt;] <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\setCJKsansfont</span>&#123;&lt;font name&gt;&#125;[&lt;font features&gt;] <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\setCJKmonofont</span>&#123;&lt;font name&gt;&#125;[&lt;font features&gt;] </span><br></pre></td></tr></table></figure>
<p>由于中文字体少有对应的粗体或斜体，<code>&lt;font features&gt;</code>
里多用其他字体来配置，比如在 Windows 中设定基本字体为宋体，并设定对应的
<code>BoldFont</code> 为黑体，<code>ItalicFont</code> 为楷体：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\setCJKmainfont</span>&#123;SimSun&#125;[BoldFont=SimHei, ItalicFont=KaiTi]</span><br></pre></td></tr></table></figure>
<h3 id="使用-unicode-math-宏包配置-unicode-数学字体">使用
<code>unicode-math</code> 宏包配置 Unicode 数学字体</h3>
<p>Unicode 数学字体是一类 OpenType 字体，包含了 Unicode
字符集中的数学符号部分，字体中也设定了数学公式排版所需的一些参数。在
<code>xelatex</code> 或者 <code>lualatex</code> 编译命令下，借助
<code>unicode-math</code> 宏包可以调用 Unicode
数学字体配置数学公式的字体风格。</p>
<p>在导言区使用<code>\usepackage{unicode-math}</code> 后，使用
<code>\setmathfont</code> 命令即可：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\setmathfont</span>&#123;&lt;font name&gt;&#125;[&lt;font features&gt;] </span><br></pre></td></tr></table></figure>
<p>绝大多数时候，只需要给定字体名称 <code>&lt;font name&gt;</code>
即可。Unicode 数学字体相比于正文字体的选择余地不多。下表
给出了较为常用的 Unicode 数学字体。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125120819345.png" alt="image-20250125120819345" style="zoom:67%;" /></p>
<hr />
<h2 id="文字装饰和强调">文字装饰和强调</h2>
<p>强调文字的方法，或者是添加下划线等装饰物，或者是改变文字的字体。</p>
<p><span class="math inline">\(\LaTeX{}\)</span> 定义了
<code>\underline</code> 命令用来为文字添加下划线：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">An <span class="keyword">\underline</span>&#123;underlined&#125; text.</span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
An \  \underline{underlined} \  text.
\]</span></p>
<p><code>\underline</code>
命令生成下划线的样式不够灵活，不同的单词可能生成高低各异的下划线，并且无法换行。<code>ulem</code>
宏包提供了更灵活的解决方案，它提供的 <code>\uline</code>
命令能够轻松生成自动换行的下划线：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125121752045.png" alt="image-20250125121752045" style="zoom:67%;" /></p>
<p>前一节介绍了 <code>\emph</code>
命令，它将文字变为斜体以示强调，而如果在已强调的文字中嵌套使用
<code>\emph</code> 命令，命令内则使用直立体文字：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">Some <span class="keyword">\emph</span>&#123;emphasized words,</span><br><span class="line">including <span class="keyword">\emph</span>&#123;double-emphasized&#125;</span><br><span class="line">words&#125;, are shown here.</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125121906215.png" alt="image-20250125121906215" style="zoom:67%;" /></p>
<hr />
<h2 id="段落格式和间距">段落格式和间距</h2>
<h3 id="长度和长度变量">长度和长度变量</h3>
<p>在本节首先统一介绍长度和长度变量。长度的数值
<code>&lt;length&gt;</code> 由数字和单位组成。常用的单位见下表。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125124002209.png" alt="image-20250125124002209" style="zoom:67%;" /></p>
<p>在一些情况下还会用到可伸缩的“弹性长度”，如
<code>12pt plus 2pt minus 3pt</code> 表示基础长度为
<code>12pt</code>，可以伸展到 <code>14pt</code>，也可以收缩到
<code>9pt</code>。 也可只定义 <code>plus</code> 或者 <code>minus</code>
的部分，如 `0pt plus 5pt。</p>
<p>长度的数值还可以用长度变量本身或其倍数来表达，如
<code>2.5\\parindent</code> 等。</p>
<p><span class="math inline">\(\LaTeX{}\)</span>
预定义了大量的长度变量用于控制版面格式。如页面宽度和高度、首行缩进、段落间距等。如果需要自定义长度变量，需使用如下命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\newlength</span>&#123;<span class="keyword">\&lt;</span>length command&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>长度变量可以用 <code>\setlength</code> 赋值，或用
<code>\addtolength</code> 增加长度：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\setlength</span>&#123;&lt;length command&gt;&#125;&#123;&lt;length&gt;&#125; </span><br><span class="line"><span class="keyword">\addtolength</span>&#123;&lt;length command&gt;&#125;&#123;&lt;length&gt;&#125;</span><br></pre></td></tr></table></figure>
<h3 id="行距">行距</h3>
<p>前文中提到过 <code>\fontsize</code>
命令可以为字号设定对应的行距，但很少那么用。更常用的办法是在导言区使用
<code>\linespread</code> 命令。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\linespread</span>&#123;&lt;factor&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>其中 <code>&lt;factor&gt;</code>
作用于基础行距而不是字号。缺省的基础行距是 1.2 倍字号大小（参考
<code>\fontsize</code> 命令），因此使用 <code>\linespread{1.5}</code>
意味着最终行距为 <strong>1.8 倍</strong>的字号大小。</p>
<p>如果不是在导言区全局修改，而想要<strong>局部地</strong>改变某个段落的行距，需要用
<code>\selectfont</code> 命令使 <code>\linespread</code>
命令的改动立即生效：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125130505191.png" alt="image-20250125130505191" style="zoom:67%;" /></p>
<p>字号的改变是即时生效的，而行距的改变直到文字<strong>分段</strong>时才生效。如果需要改变某一部分文字的行距，那么不能简单地将文字包含在花括号内。注意下面两个例子中
<code>\par</code> 命令的位置：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125130826941.png" alt="image-20250125130826941" style="zoom:67%;" /></p>
<h3 id="段落格式">段落格式</h3>
<p>以下长度分别为段落的左缩进、右缩进和首行缩进：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\setlength</span>&#123;<span class="keyword">\leftskip</span>&#125;&#123;&lt;length&gt;&#125;  </span><br><span class="line"><span class="keyword">\setlength</span>&#123;<span class="keyword">\rightskip</span>&#125;&#123;&lt;length&gt;&#125;</span><br><span class="line"><span class="keyword">\setlength</span>&#123;<span class="keyword">\parindent</span>&#125;&#123;&lt;length&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>它们和设置行距的命令一样，在分段时生效。</p>
<p>控制段落缩进的命令为：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\indent</span></span><br><span class="line"><span class="keyword">\noindent</span></span><br></pre></td></tr></table></figure>
<p><span class="math inline">\(\LaTeX{}\)</span>
默认在段落开始时缩进，长度为用上述命令设置的
<code>parindent</code>。如果需要在某一段不缩进，可在段落开头使用<code>\noindent</code>
命令。相反地，<code>\indent</code>
命令强制开启一段首行缩进的段落。在段落开头使用多个 <code>\indent</code>
命令可以累加缩进量。</p>
<p><span class="math inline">\(\LaTeX{}\)</span> 还默认在
<code>\chapter</code>、<code>\section</code>
等章节标题命令之后的第一段不缩进(<code>ctex</code>
宏包和文档类默认按照中文习惯保持标题后第一段的首行缩进)。如果不习惯这种设定，可以调用
<code>indentfirst</code> 宏包，令第一段的首行缩进照常。</p>
<p>段落间的垂直间距为 <code>parskip</code>，如设置段落间距在
<code>0.8ex</code> 到 <code>1.5ex</code> 变动：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\setlength</span>&#123;<span class="keyword">\parskip</span>&#125;&#123;1ex plus 0.5ex minus 0.2ex&#125;</span><br></pre></td></tr></table></figure>
<h3 id="水平间距">水平间距</h3>
<p><span class="math inline">\(\LaTeX{}\)</span>
默认为将单词之间的“空格”转化为水平间距。如果需要在文中手动插入额外的水平间距，可使用
<code>\hspace</code> 命令：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125143708280.png" alt="image-20250125143708280" style="zoom:67%;" /></p>
<p><code>\hspace</code>
命令生成的水平间距如果位于一行的开头或末尾，则有可能因为断行而被舍弃。可使用
<code>\hspace*</code> 命令代替<code>\hspace</code>
命令得到不会因断行而消失的水平间距。</p>
<p>命令 <code>\stretch{&lt;n&gt;}</code> 生成一个特殊弹性长度，参数
<code>&lt;n&gt;</code> 为权重。它的基础长度为
0pt，但可以无限延伸，如果同一行内出现多个
<code>\stretch{&lt;n&gt;}</code>，这一行的所有可用空间将按每个
<code>\stretch</code> 命令给定的权重 <code>&lt;n&gt;</code>
进行分配。</p>
<p>命令 <code>\fill</code> 相当于 <code>\stretch{1}</code>(注意不要用
<code>1.5\fill</code> 这样的用法，它生成的长度只有基础长度 0pt
而没有延伸部分)。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125160138870.png" alt="image-20250125160138870" style="zoom:67%;" /></p>
<p>在正文中用 <code>\hspace</code> 命令生成水平间距时，往往使用
<code>em</code> 作为单位，生成的间距随字号大小而变。在数学公式中见过
<code>\quad</code> 和 <code>\qquad</code>
命令，它们也可以用于文本中，分别相当于 <code>\hspace{1em}</code> 和
<code>\hspace{2em}</code>：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125160343041.png" alt="image-20250125160343041" style="zoom:67%;" /></p>
<h3 id="垂直间距">垂直间距</h3>
<p>在页面中，段落、章节标题、行间公式、列表、浮动体等元素之间的间距是
<span class="math inline">\(\LaTeX{}\)</span> 预设的。比如
<code>\parskip</code>，默认设置为 <code>0pt plus 1pt</code>。</p>
<p>如果我们想要人为地增加段落之间的垂直间距，可以在两个段落之间的位置使用
<code>\vspace</code> 命令：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125160614418.png" alt="image-20250125160614418" style="zoom:67%;" /></p>
<p><code>\vspace</code>
命令生成的垂直间距在一页的顶端或底端可能被“吞掉”，类似
<code>\hspace</code>
在一行的开头和末尾那样。对应地，<code>\vspace*</code>
命令产生不会因断页而消失的垂直间距。<code>\vspace</code> 也可用
<code>\stretch}</code>设置无限延伸的垂直长度。</p>
<p>在段落内的两行之间增加垂直间距，一般通过给断行命令 <code>\\</code>
加可选参数，如 <code>\\[6pt]</code> 或 <code>\\*[6pt]</code>。
<code>\vspace</code> 也可以在段落内使用，区别在于 <code>\vspace</code>
只引入垂直间距而不断行：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125161039548.png" alt="image-20250125161039548" style="zoom:67%;" /></p>
<p>另外 <span class="math inline">\(\LaTeX{}\)</span>
还提供了<code>\bigskip</code>, <code>\medskip</code>,
<code>\smallskip</code> 来增加预定义长度的垂直间距。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125161301100.png" alt="image-20250125161301100" style="zoom:67%;" /></p>
<hr />
<h2 id="页面和分栏">页面和分栏</h2>
<p><span class="math inline">\(\LaTeX{}\)</span>
允许用户通过为文档类指定选项来控制纸张的大小，包括
<code>a4paper</code>、<code>letterpaper</code>
等等，并配合字号设置了适合的页边距。</p>
<p>控制页边距的参数由 下图 里给出的各种长度变量控制。可以用
<code>\setlength</code>
命令修改这些长度变量，以达到调节页面尺寸和边距的作用；反之也可以利用这些长度变量来决定排版内容的尺寸，如在
<code>tabularx</code> 环境或 <code>\includegraphics</code>
命令的参数里，设置图片或表格的宽度为 <code>0.8\textwidth</code>。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125162000736.png" alt="image-20250125162000736" style="zoom:67%;" /></p>
<p>页边距等比较直观的参数则必须间接设置。根据 上图
将各个方向的页边距计算公式给出（以奇数页为例）：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125162309991.png" alt="image-20250125162309991" style="zoom:67%;" /></p>
<p>如果需要设置合适的 <code>&lt;left-margin&gt;</code> 和
<code>&lt;right-margin&gt;</code>，就要通过上述方程组把<code>\oddsidemargin</code>
和 <code>\textwidth</code> 等参数解出来！</p>
<p>幸好 <code>geometry</code>
宏包提供了设置页边距等参数的简便方法，能够帮我们完成背后繁杂的计算。</p>
<h3 id="利用-geometry-宏包设置页面参数">利用 <code>geometry</code>
宏包设置页面参数</h3>
<p><code>geometry</code> 宏包的调用方式类似于 <code>graphicx</code>，在
<code>latex</code> + <code>dvipdfmx</code> 命令下需要指定选项
<code>dvipdfm</code> 或 <code>dvipdfmx</code>；<code>pdflatex</code> 和
<code>xelatex</code> 编译命令下不需要。</p>
<p>既可以调用 <code>geometry</code> 宏包，然后用其提供的
<code>\geometry</code>命令设置页面参数：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\usepackage</span>&#123;geometry&#125;</span><br><span class="line"><span class="keyword">\geometry</span>&#123;&lt;eometry-settings&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>也可以直接在宏包选项中设置：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\usepackage</span>[&lt;geometry-settings&gt;]&#123;geometry&#125;</span><br></pre></td></tr></table></figure>
<p>其中 <code>&lt;geometry-settings&gt;</code> 多以
<code>&lt;key&gt;=&lt;value&gt;</code> 的形式组织。</p>
<p>比如，符合 Microsoft Word 习惯的页面设定是 A4 纸张，上下边距 1
英寸，左右边距 1.25
英寸，于是我们可以通过如下两种等效的方式之一设定页边距：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\geometry</span>&#123;a4paper,left=1.25in,right=1.25in,top=1in,bottom=1in&#125;</span><br><span class="line"><span class="comment">% or like this:</span></span><br><span class="line"><span class="keyword">\geometry</span>&#123;a4paper,hmargin=1.25in,vmargin=1in&#125;</span><br></pre></td></tr></table></figure>
<p>又比如，需要设定周围的边距一致为 1.25 英寸，可以用更简单的语法：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\geometry</span>&#123;margin=1.25in&#125;</span><br></pre></td></tr></table></figure>
<p>对于书籍等双面文档，习惯上奇数页右边、偶数页左边留出较大的页边距，而靠近书脊一侧的奇数页左边、偶数页右边页边距较小。可以这样设定：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\geometry</span>&#123;inner=1in,outer=1.25in&#125;</span><br></pre></td></tr></table></figure>
<p>需要指出的是，通过 <code>geometry</code> 宏包设置的纸张大小是输出 PDF
文件的真实大小，而在文档类选项中设置的参数实际上只影响输出区域。<code>geometry</code>
宏包还能够修改页眉页脚高度、边注宽度等参数，并且能比较好地处理各参数之间的依赖关系。更详细的用法不再赘述，感兴趣的用户可查阅其帮助文档。</p>
<h3 id="页面内容的垂直对齐">页面内容的垂直对齐</h3>
<p><span class="math inline">\(\LaTeX{}\)</span>
默认将页面内容在垂直方向分散对齐。对于有大量图表的文档，许多时候想要做到排版匀称的页面很困难，垂直分散对齐会造成某些页面的垂直间距过宽，还可能报大量的
<code>Underfull \\vbox</code> 警告。<span
class="math inline">\(\LaTeX{}\)</span>
还提供了另一种策略：将页面内容向顶部对齐，给底部留出高度不一的空白。</p>
<p>以下命令分别令页面在垂直方向向顶部对齐/分散对齐：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\raggedbottom</span></span><br><span class="line"><span class="keyword">\flushbottom</span></span><br></pre></td></tr></table></figure>
<h3 id="分栏">分栏</h3>
<p><span class="math inline">\(\LaTeX{}\)</span>
支持简单的单栏或双栏排版。标准文档类的全局选项
<code>onecolumn</code>、<code>twocolumn</code>
可控制全文分单栏或双栏排版。<span
class="math inline">\(\LaTeX{}\)</span>
也提供了切换单/双栏排版的命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\onecolumn</span></span><br><span class="line"><span class="keyword">\twocolumn</span>[&lt;one-column top material&gt;]</span><br></pre></td></tr></table></figure>
<p><code>\twocolumn</code>
支持带一个可选参数，用于排版双栏之上的一部分单栏内容。</p>
<p>切换单/双栏排版时总是会另起一页（<code>\clearpage</code>）。在双栏模式下使用
<code>\newpage</code> 会换栏而不是换页；<code>\clearpage</code>
则能够换页。</p>
<p>双栏排版时每一栏的宽度为
<code>\columnwidth</code>，它由<code>\textwidth</code> 减去
<code>\columnsep</code> 的差除以 2 得到。两栏之间还有一道竖线，宽度为
<code>\columnseprule</code>，默认为零，也就是看不到竖线。</p>
<p>一个比较好用的分栏解决方案是 <code>multicol</code>，它提供了简单的
<code>multicols</code> 环境自动产生分栏，如以下环境将内容分为 3 栏：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;multicols&#125;&#123;3&#125;</span><br><span class="line">...</span><br><span class="line"><span class="keyword">\end</span>&#123;multicols&#125;</span><br></pre></td></tr></table></figure>
<p><code>multicol</code>
宏包能够在一页之中切换单栏/多栏，也能处理跨页的分栏，且各栏的高度分布平衡。但代价是<strong>在
<code>multicols</code> 环境中无法正常使用 <span
class="math inline">\(\texttt{table}\)</span> 和 <code>figure</code>
等浮动体环境</strong>，它会直接让浮动体丢失。<code>multicols</code>
环境中只能用跨栏的 <code>table*</code> 和 <code>figure*</code>
环境，或者用 <code>float</code> 宏包提供的 <code>H</code>
参数固定浮动体的位置。</p>
<hr />
<h2 id="页眉页脚">页眉页脚</h2>
<h3 id="基本的页眉页脚样式">基本的页眉页脚样式</h3>
<p><span class="math inline">\(\LaTeX{}\)</span> 中提供了命令
<code>\pagestyle</code> 来修改页眉页脚的样式：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\pagestyle</span>&#123;&lt;page-style&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>命令 <code>\thispagestyle</code> 只影响当页的页眉页脚样式：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\thispagestyle</span>&#123;&lt;page-style&gt;&#125;</span><br></pre></td></tr></table></figure>
<p><code>&lt;page-style&gt;</code> 参数为样式的名称，在 <span
class="math inline">\(\LaTeX{}\)</span> 里预定义了四类样式，见
下表。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125170414339.png" alt="image-20250125170414339" style="zoom:67%;" /></p>
<p>其中 <code>headings</code> 的情况较为复杂：</p>
<ul>
<li><code>article</code> 文档类， <code>twoside</code> 选项:
偶数页为页码和节标题，奇数页为小节标题和页码；</li>
<li><code>article</code> 文档类， <code>oneside</code> 选项:
页眉为节标题和页码；</li>
<li><code>report</code> 和 <code>book</code>
文档类，<code>twoside</code> 选项:
偶数页为页码和章标题，奇数页为节标题和页码；</li>
<li><code>report</code> 和 <code>book</code>
文档类，<code>oneside</code> 选项: 页眉为章标题和页码。</li>
</ul>
<p><code>\pagenumbering</code>
命令令我们能够改变页眉页脚中的页码样式：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\pagenumbering</span>&#123;&lt;style&gt;&#125;</span><br></pre></td></tr></table></figure>
<p><code>&lt;style&gt;</code> 为页码样式，默认为
<code>arabic</code>（阿拉伯数字），还可修改为
<code>roman</code>（小写罗马数字）、<code>Roman</code>（大写罗马数字）等。注意使用
<code>\pagenumbering</code> 命令后会将页码重置为 1。<code>book</code>
文档类的 <code>\frontmatter</code> 和 <code>\mainmatter</code>
内部就使用了 <code>\pagenumbering</code> 命令切换页码样式。</p>
<h3 id="手动更改页眉页脚的内容">手动更改页眉页脚的内容</h3>
<p>对于 <code>headings</code> 或者 <code>myheadings</code> 样式，<span
class="math inline">\(\LaTeX{}\)</span>
允许用户使用命令手动修改页眉上面的内容，特别是因为使用了
<code>\chapter*</code> 等命令而无法自动生成页眉页脚的情况：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\markright</span>&#123;&lt;right-mark&gt;&#125;</span><br><span class="line"><span class="keyword">\markboth</span>&#123;&lt;left-mark&gt;&#125;&#123;&lt;right-mark&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>在双面排版、<code>headings</code> 或 <code>myheadings</code>
页眉页脚样式下，<code>&lt;left-mark&gt;</code> 和
<code>&lt;right-mark&gt;</code>
的内容分别预期出现在左页（偶数页）和右页（奇数页）。 事实上
<code>\chapter</code> 和 <code>\section</code> 等章节命令内部也使用
<code>\markboth</code> 或者 <code>\markright</code> 生成页眉。</p>
<p><span class="math inline">\(\LaTeX{}\)</span>
默认将页眉的内容都转为大写字母。如需要保持字母的大小写，可以尝试以下代码（但是这不能改变页眉的斜体样式<code>\slshape</code>，斜体定义在
<code>headings</code> 样式里的。如不喜欢斜体，可在 <code>markboth</code>
等命令参数里先使用
<code>\normalfont</code>，再使用想要的字体样式命令，或直接尝试使用
<code>fancyhdr</code> 宏包)：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\renewcommand</span><span class="keyword">\chaptermark</span>[1]&#123;<span class="comment">%</span></span><br><span class="line">  <span class="keyword">\markboth</span>&#123;Chapter <span class="keyword">\thechapter</span><span class="keyword">\quad</span> <span class="params">#1</span>&#125;&#123;&#125;&#125;</span><br><span class="line"><span class="keyword">\renewcommand</span><span class="keyword">\sectionmark</span>[1]&#123;<span class="comment">%</span></span><br><span class="line">  <span class="keyword">\markright</span>&#123;<span class="keyword">\thesection</span><span class="keyword">\quad</span> <span class="params">#1</span>&#125;&#125;</span><br></pre></td></tr></table></figure>
<p>其中 <code>\thechapter</code>、<code>\thesection</code>
等命令为章节计数器的数值。注意以上代码适用于 <code>report</code> 和
<code>book</code> 文档类；对于 <code>article</code>
文档类，与两个页眉相关的命令分别为 <code>\sectionmark</code> 和
<code>\subsectionmark</code>。</p>
<h3 id="fancyhdr-宏包"><code>fancyhdr</code> 宏包</h3>
<p><code>fancyhdr</code>
宏包改善了页眉页脚样式的定义方式，允许将内容自由安置在页眉和页脚的左、中、右三个位置，还为页眉和页脚各加了一条横线。</p>
<p><code>fancyhdr</code> 自定义了样式名称 <code>fancy</code>。使用
<code>fancyhdr</code> 宏包定义页眉页脚之前，通常先用
<code>\pagestyle{fancy}</code> 调用这个样式。在
<code>fancyhdr</code>定义页眉页脚的命令为：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\fancyhf</span>[&lt;position&gt;]&#123;...&#125;</span><br><span class="line"><span class="keyword">\fancyhead</span>[&lt;position&gt;]&#123;...&#125;</span><br><span class="line"><span class="keyword">\fancyfoot</span>[&lt;position&gt;]&#123;...&#125;</span><br></pre></td></tr></table></figure>
<p>其中 <code>&lt;position&gt;</code> 为 L（左）/ C（中）/ R（右）以及与
O（奇数页）/ E（偶数页）字母的组合。<code>\fancyhf</code>
用于同时定义页眉和页脚，习惯上使用 <code>\fancyhf{}</code>
来清空页眉页脚的设置。</p>
<p>下面的源代码 给出了 <code>fancyhdr</code>
基础用法的一个示例，效果为将章节标题放在和 headings
一致的位置，但使用加粗格式；页码都放在页脚正中；修改横线宽度，“去掉”页脚的横线。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250125174255995.png" alt="image-20250125174255995" style="zoom:67%;" /></p>
<p><code>fancyhdr</code> 还支持用 <code>\fancypagestyle</code>
为自定义的页眉页脚样式命名，或者重新定义已有的样式如 <code>plain</code>
等：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% 自定义 myfancy 样式</span></span><br><span class="line"><span class="keyword">\fancypagestyle</span>&#123;myfancy&#125;&#123;<span class="comment">%</span></span><br><span class="line">  <span class="keyword">\fancyhf</span>&#123;&#125;</span><br><span class="line">  <span class="keyword">\fancyhead</span>&#123;...&#125;</span><br><span class="line">  <span class="keyword">\fancyfoot</span>&#123;...&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">% 使用样式</span></span><br><span class="line"><span class="keyword">\pagestyle</span>&#123;myfancy&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>lshort</tag>
      </tags>
  </entry>
  <entry>
    <title>lshort笔记：排版数学公式（四）</title>
    <url>/2025/01/18/notes-reading/computer/lshort/lshort-04-math-equation/</url>
    <content><![CDATA[<blockquote>
<p>本文内容来自<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">一份
(不太) 简短的 LATEX2ε 介绍<i class="fa fa-external-link-alt"></i></span>。</p>
</blockquote>
<p>本章将见识到 <span class="math inline">\(\LaTeX{}\)</span>
闻名的强项——排版数学公式。</p>
<span id="more"></span>
<hr />
<h2 id="textrmams-宏集"><span
class="math inline">\(\textrm{AMS}\)</span> 宏集</h2>
<p>在介绍数学公式排版之前，简单介绍一下 <span
class="math inline">\(\textrm{AMS}\)</span> 宏集。<span
class="math inline">\(\textrm{AMS}\)</span>
宏集合是美国数学学会(American Mathematical Society)提供的对<span
class="math inline">\(\LaTeX{}\)</span>
原生的数学公式排版的扩展，其核心是 <code>amsmath</code>
宏包，对多行公式的排版提供了有力的支持。此外，<code>amsfonts</code>
宏包以及基于它的 <code>amssymb</code>
宏包提供了丰富的数学符号；<code>amsthm</code> 宏包扩展了 <span
class="math inline">\(\LaTeX{}\)</span> 定理证明格式。</p>
<p>以下示例都假定了导言区中写有 <code>\usepackage{amsmath}</code> 。</p>
<hr />
<h2 id="公式排版基础">公式排版基础</h2>
<h3 id="行内和行间公式">行内和行间公式</h3>
<p>数学公式有两种排版方式：其一是与文字混排，称为<strong>行内公式</strong>；其二是单独列为一行排版，称为<strong>行间公式</strong>。</p>
<p>行内公式由一对 <code>$$</code> 符号包裹：The Pythagorean theorem is
<span class="math inline">\(a^2 + b^2 = c^2\)</span>
(<code>$a^2 + b^2 = c^2$</code>)。</p>
<p>单独成行的<strong>行间公式</strong>由 <code>equation</code>
环境包裹。 <code>equation</code>
环境为公式自动生成一个编号，这个编号可以用<code>\label</code> 和
<code>\ref</code> 生成交叉引用，<code>amsmath</code> 的
<code>\eqref</code>
命令甚至为引用<strong>自动加上圆括号</strong>；还可以用<code>\tag</code>
命令手动修改公式的编号，或者用<code>\notag</code>
命令取消为公式编号（与之基本等效的命令是 <code>\nonumber</code>）。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250116220746833.png" alt="image-20250116220746833" style="zoom:67%;" /></p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250116220821758.png" alt="image-20250116220821758" style="zoom:67%;" /></p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;equation&#125;</span><br><span class="line">1 + 1 = 3 <span class="keyword">\tag</span>&#123;dumb&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;equation&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;equation&#125;</span><br><span class="line">1 + 1 = 4 <span class="keyword">\notag</span></span><br><span class="line"><span class="keyword">\end</span>&#123;equation&#125;</span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{equation}
1 + 1 = 3 \tag{dumb}
\end{equation}
\]</span></p>
<p><span class="math display">\[
\begin{equation}
1 + 1 = 4 \notag
\end{equation}
\]</span></p>
<p>如果需要直接使用不带编号的行间公式，则将公式用命令 <code>\[</code> 和
<code>\]</code> 包裹，与之等效的是 <code>displaymath</code>
环境。有的人更喜欢 <code>equation*</code>
环境，体现了带星号和不带星号的环境之间的区别。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250116221231100.png" alt="image-20250116221231100" style="zoom:67%;" /></p>
<p>通过一个例子展示行内公式和行间公式的对比。为了与文字相适应，行内公式在排版大的公式元素（分式、巨算符等）时显得很“局促”：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250116221343609.png" alt="image-20250116221343609" style="zoom:67%;" /></p>
<p>行间公式的对齐、编号位置等性质由文档类选项控制，文档类的
<code>fleqn</code> 选项令行间公式左对齐；<code>leqno</code>
选项令编号放在公式左边。</p>
<h3 id="数学模式">数学模式</h3>
<p>当用户使用 <code>$</code> 开启行内公式输入，或是使用 <code>\[</code>
命令、<code>equation</code> 环境时，<span
class="math inline">\(\LaTeX{}\)</span>
就进入了<strong>数学模式</strong>。</p>
<p>数学模式相比于文本模式有以下特点：</p>
<blockquote>
<ol type="1">
<li>数学模式中输入的空格被忽略。数学符号的间距默认由符号的性质（关系符号、运算符等）决定。需要人为引入间距时，<code>\quad</code>
和 <code>\qquad</code> 等命令。</li>
<li><strong>不允许有空行（分段）</strong>。行间公式中也无法用
<code>\\</code>命令手动换行。</li>
<li>所有的字母被当作数学公式中的变量处理，字母间距与文本模式不一致，也无法生成单词之间的空格。如果想在数学公式中<strong>输入正体</strong>的文本，简单情况下可用
<code>\mathrm</code> 命令。或者用 <code>\amsmath</code> 提供的
<code>\text</code> 命令。</li>
</ol>
</blockquote>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span>x<span class="built_in">^</span>&#123;2&#125;  <span class="keyword">\geq</span> 0 <span class="keyword">\qquad</span></span><br><span class="line"><span class="keyword">\text</span>&#123;for <span class="keyword">\textbf</span>&#123;all&#125; &#125;</span><br><span class="line">x<span class="keyword">\in</span> <span class="keyword">\mathbb</span>&#123;R&#125;<span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{gather*}
x^{2}  \geq 0 \qquad
\text{for \textbf{all} }
x\in \mathbb{R}
\end{gather*}
\]</span></p>
<hr />
<h2 id="数学符号">数学符号</h2>
<p><span class="math inline">\(\LaTeX{}\)</span>​
默认提供了常用的数学符号，<code>amssymb</code>
宏包提供了一些次常用的符号。更多符号可在命令行输入<code>texdoc symbols-a4</code>
<span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL0NUQU4vaW5mby9zeW1ib2xzL2NvbXByZWhlbnNpdmUvc3ltYm9scy1hNC5wZGY=">查阅<i class="fa fa-external-link-alt"></i></span>
。</p>
<h3 id="一般符号">一般符号</h3>
<p>希腊字母符号的名称就是其英文名称，如 <span
class="math inline">\(\alpha\)</span> (<code>\alpha</code>)、<span
class="math inline">\(\beta\)</span>
(<code>\beta</code>)等等。大写的希腊字母为首字母大写的命令 <span
class="math inline">\(\Gamma\)</span> (<code>\Gamma</code>) <span
class="math inline">\(\Delta\)</span>
(<code>\Delta</code>)。无穷大符号为 <span
class="math inline">\(\infty\)</span> (<code>\infty</code>)。</p>
<p>省略号有 <span class="math inline">\(\dots\)</span>
(<code>\dots</code>) 和 <span class="math inline">\(\cdots\)</span>
(<code>\cdots</code>) 两种形式。它们有各自合适的用途：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span>a<span class="built_in">_</span>1, a<span class="built_in">_</span>2, <span class="keyword">\dots</span>, a<span class="built_in">_</span>n<span class="built_in">$</span> <span class="keyword">\\</span></span><br><span class="line"><span class="built_in">$</span>a<span class="built_in">_</span>1 + a<span class="built_in">_</span>2 + <span class="keyword">\cdots</span> + a<span class="built_in">_</span>n<span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{gather*}
a_1, a_2, \dots, a_n \\
a_1 + a_2 + \cdots + a_n
\end{gather*}
\]</span></p>
<p><code>\ldots</code> 和 <code>\dots</code>
是完全等效的，它们既能用在公式中，也用来在文本里作为省略号。
除此之外，在矩阵中可能会用到竖排的 <span
class="math inline">\(\vdots\)</span> (<code>\votes</code>)和斜排的
<span class="math inline">\(\ddots\)</span> (<code>\ddots</code>)。</p>
<h3 id="指数上下标和导数">指数、上下标和导数</h3>
<p>用<code>^</code> 和
<code>_</code>标明上下标。注意上下标的内容（子公式）一般需要<strong>用花括号包裹</strong>，否则上下标只对后面的一个符号起作用。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span>p<span class="built_in">^</span>3<span class="built_in">_</span>&#123;ij&#125; <span class="keyword">\qquad</span></span><br><span class="line">m<span class="built_in">_</span><span class="keyword">\mathrm</span>&#123;Knuth&#125;<span class="keyword">\qquad</span></span><br><span class="line"><span class="keyword">\sum</span><span class="built_in">_</span>&#123;k=1&#125;<span class="built_in">^</span>3 k <span class="built_in">$</span><span class="keyword">\\</span>[5pt]</span><br><span class="line"><span class="built_in">$</span>a<span class="built_in">^</span>x+y <span class="keyword">\neq</span> a<span class="built_in">^</span>&#123;x+y&#125;<span class="keyword">\qquad</span></span><br><span class="line">e<span class="built_in">^</span>&#123;x<span class="built_in">^</span>2&#125; <span class="keyword">\neq</span> &#123;e<span class="built_in">^</span>x&#125;<span class="built_in">^</span>2<span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{gather*}
p^3_{ij} \qquad
m_\mathrm{Knuth}\qquad
\sum_{k=1}^3 k \\[5pt]
a^x+y \neq a^{x+y}\qquad
e^{x^2} \neq {e^x}^2
\end{gather*}
\]</span></p>
<p>导数符号<code>'</code>(<span
class="math inline">\({}&#39;\)</span>)是一类特殊的上标，可连续使用，但只能在<strong>其后</strong>添加其他上标：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span>f(x) = x<span class="built_in">^</span>2 <span class="keyword">\quad</span> f&#x27;(x) = 2x <span class="keyword">\quad</span> f&#x27;&#x27;<span class="built_in">^</span>&#123;2&#125;(x) = 4<span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
f(x) = x^2 \quad f&#39;(x) = 2x \quad f&#39;&#39;^{2}(x) = 4
\]</span></p>
<h3 id="分式和根式">分式和根式</h3>
<p>分式使用 <code>\frac{分子}{分母}</code>
来书写。分式的大小在行间公式中是正常大小，而在行内被极度压缩。
<code>amsmath</code> 提供了方便的命令 <code>\dfrac</code> 和
<code>\tfrac</code>，令用户能够在行内使用正常大小的分式，或是反过来。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">In display style:</span><br><span class="line"><span class="keyword">\[</span></span><br><span class="line">3/8 <span class="keyword">\qquad</span> <span class="keyword">\frac</span>&#123;3&#125;&#123;8&#125;</span><br><span class="line"><span class="keyword">\qquad</span> <span class="keyword">\tfrac</span>&#123;3&#125;&#123;8&#125;</span><br><span class="line"><span class="keyword">\]</span></span><br><span class="line">In text style:</span><br><span class="line"><span class="built_in">$</span>1<span class="keyword">\frac</span>&#123;1&#125;&#123;2&#125;<span class="built_in">$</span>~hours <span class="keyword">\qquad</span></span><br><span class="line"><span class="built_in">$</span>1<span class="keyword">\dfrac</span>&#123;1&#125;&#123;2&#125;<span class="built_in">$</span>~hours</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250118195233817.png" alt="image-20250118195233817" style="zoom:67%;" /></p>
<p>一般的根式使用 <code>\sqrt{...}</code>；表示 <span
class="math inline">\(n\)</span> 次方根时写成
<code>\sqrt[n]{...}</code>。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span><span class="keyword">\sqrt</span>&#123;x&#125; <span class="keyword">\Leftrightarrow</span> x<span class="built_in">^</span>&#123;1/2&#125;</span><br><span class="line"><span class="keyword">\quad</span> <span class="keyword">\sqrt</span>[3]&#123;2&#125;</span><br><span class="line"><span class="keyword">\quad</span> <span class="keyword">\sqrt</span>&#123;x<span class="built_in">^</span>&#123;2&#125; + <span class="keyword">\sqrt</span>&#123;y&#125;&#125;<span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\sqrt{x} \Leftrightarrow x^{1/2}
\quad \sqrt[3]{2}
\quad \sqrt{x^{2} + \sqrt{y}}
\]</span></p>
<p>特殊的分式形式，如二项式结构，由 <code>amsmath</code> 宏包的
<code>\binom</code> 命令生成：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\[</span></span><br><span class="line"><span class="keyword">\binom</span>&#123;n&#125;&#123;k&#125; =<span class="keyword">\binom</span>&#123;n-1&#125;&#123;k&#125;</span><br><span class="line">+ <span class="keyword">\binom</span>&#123;n-1&#125;&#123;k-1&#125;</span><br><span class="line"><span class="keyword">\]</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\binom{n}{k} =\binom{n-1}{k} + \binom{n-1}{k-1}
\]</span></p>
<h3 id="关系符">关系符</h3>
<p>常见的关系符号除了可以直接输入的 <span
class="math inline">\(=\)</span>，<span
class="math inline">\(&gt;\)</span>，<span
class="math inline">\(&lt;\)</span>，其它符号用命令输入，常用的有 <span
class="math inline">\(\ne\)</span> (<code>\ne</code>)、<span
class="math inline">\(\ge\)</span> (<code>\ge</code>)、<span
class="math inline">\(\le\)</span> (<code>\le</code>)、<span
class="math inline">\(\approx\)</span> （<code>\approx</code>）、 等价
<span class="math inline">\(\equiv\)</span> (<code>\equiv</code>)、正比
<span class="math inline">\(\propto\)</span>
(<code>\propto</code>)、相似 <span
class="math inline">\(\sim\)</span><code>\sim</code> 等等。</p>
<p><span class="math inline">\(\LaTeX{}\)</span>
还提供了自定义二元关系符的命令
<code>\stackrel</code>，用于将一个符号叠加在原有的二元关系符之上：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span>f<span class="built_in">_</span>n(x) <span class="keyword">\stackrel</span>&#123;*&#125;&#123;<span class="keyword">\approx</span>&#125; 1<span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
f_n(x) \stackrel{*}{\approx} 1
\]</span></p>
<h3 id="运算符">运算符</h3>
<p><span class="math inline">\(\LaTeX{}\)</span>
中的算符大多数是二元算符，除了直接用键盘可以输入的 <span
class="math inline">\(+\)</span>、<span
class="math inline">\(-\)</span>、<span
class="math inline">\(*\)</span>、<span
class="math inline">\(/\)</span>，其它符号用命令输入，常用的有乘号 <span
class="math inline">\(\times\)</span> (<code>\times</code>)、 除号 <span
class="math inline">\(÷\)</span>（<code>\div</code>）、点乘 <span
class="math inline">\(\cdot\)</span> (<code>\cdot</code>)、加减号<span
class="math inline">\(\pm\)</span> (<code>\pm</code>) / <span
class="math inline">\(\mp\)</span> (<code>\mp</code>)等等。</p>
<p><span class="math inline">\(\nabla\)</span> (<code>\nabla</code>) 和
<span class="math inline">\(\partial\)</span> (<code>\partial</code>)
也是常用的算符，虽然它们不属于二元算符。</p>
<p><span class="math inline">\(\LaTeX{}\)</span>
将数学函数的名称作为一个算符排版，字体为直立字体。其中有一部分符号在上下位置可以书写一些内容作为条件，
类似于后文所叙述的巨算符。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250118202405498.png" alt="image-20250118202405498" style="zoom:67%;" /></p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span><span class="keyword">\lim</span><span class="built_in">_</span>&#123;x <span class="keyword">\rightarrow</span> 0&#125; <span class="keyword">\frac</span>&#123;<span class="keyword">\sin</span> x&#125;&#123;x&#125; = 1<span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\lim_{x \rightarrow 0} \frac{\sin x}{x} = 1
\]</span></p>
<p>对于求模表达式，<span class="math inline">\(\LaTeX{}\)</span> 提供了
<code>\bmod</code>和 <code>\pmod</code>
命令，前者相当于一个二元运算符，后者作为同余表达式的后缀：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span>a<span class="keyword">\bmod</span> b <span class="keyword">\\</span></span><br><span class="line"> x<span class="keyword">\equiv</span> a <span class="keyword">\pmod</span>&#123;b&#125;<span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{gather*}
a\bmod b \\
x\equiv a \pmod{b}
\end{gather*}
\]</span></p>
<p>如果上表中的算符不够用的话，<code>amsmath</code> 允许用户在导言区用
<code>\DeclareMathOperator</code>定义自己的算符，其中带星号的命令定义带上下限的算符：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\DeclareMathOperator</span>&#123;<span class="keyword">\argh</span>&#125;&#123;argh&#125;</span><br><span class="line"><span class="keyword">\DeclareMathOperator</span>*&#123;<span class="keyword">\nut</span>&#125;&#123;Nut&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">\[</span><span class="keyword">\argh</span> 3 = <span class="keyword">\nut</span><span class="built_in">_</span>&#123;x=1&#125; 4x<span class="keyword">\]</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\DeclareMathOperator{\argh}{argh}
\DeclareMathOperator*{\nut}{Nut}
\argh 3 = \nut_{x=1} 4x
\]</span></p>
<h3 id="巨算符">巨算符</h3>
<p>积分号 <span class="math inline">\(\int\)</span>
(<code>\int</code>)、求和号 <span class="math inline">\(\sum\)</span>
(<code>\sum</code>)
等符号称为<strong>巨算符</strong>。巨算符在行内公式和行间公式的大小和形状有区别。行内:<span
class="math inline">\(\sum_{i=1}^n \quad \int_0^{\frac{\pi}{2}} \quad
\oint_0^{\frac{\pi}{2}} \quad \prod_\epsilon\)</span> ，行间：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\[</span><span class="keyword">\sum</span><span class="built_in">_</span>&#123;i=1&#125;<span class="built_in">^</span>n <span class="keyword">\quad</span></span><br><span class="line"><span class="keyword">\int</span><span class="built_in">_</span>0<span class="built_in">^</span>&#123;<span class="keyword">\frac</span>&#123;<span class="keyword">\pi</span>&#125;&#123;2&#125;&#125; <span class="keyword">\quad</span></span><br><span class="line"><span class="keyword">\oint</span><span class="built_in">_</span>0<span class="built_in">^</span>&#123;<span class="keyword">\frac</span>&#123;<span class="keyword">\pi</span>&#125;&#123;2&#125;&#125; <span class="keyword">\quad</span></span><br><span class="line"><span class="keyword">\prod</span><span class="built_in">_</span><span class="keyword">\epsilon</span> <span class="keyword">\]</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\sum_{i=1}^n \quad
\int_0^{\frac{\pi}{2}} \quad
\oint_0^{\frac{\pi}{2}} \quad
\prod_\epsilon
\]</span></p>
<p>巨算符的上下标位置可由 <code>\limits</code> 和
<code>\nolimits</code>调整，前者令巨算符类似 <span
class="math inline">\(\lim\)</span> 或求和算符 <span
class="math inline">\(\sum\)</span>，上下标位于上下方；后者令巨算符类似积分号，上下标位于右上方和右下方。行内:<span
class="math inline">\(\sum\limits_{i=1}^n \quad
\int\limits_0^{\frac{\pi}{2}} \quad \prod\limits_\epsilon\)</span> ;
行间:</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\[</span><span class="keyword">\sum</span><span class="keyword">\nolimits</span><span class="built_in">_</span>&#123;i=1&#125;<span class="built_in">^</span>n <span class="keyword">\quad</span></span><br><span class="line"><span class="keyword">\int</span><span class="keyword">\limits</span><span class="built_in">_</span>0<span class="built_in">^</span>&#123;<span class="keyword">\frac</span>&#123;<span class="keyword">\pi</span>&#125;&#123;2&#125;&#125; <span class="keyword">\quad</span></span><br><span class="line"><span class="keyword">\prod</span><span class="keyword">\nolimits</span><span class="built_in">_</span><span class="keyword">\epsilon</span> <span class="keyword">\]</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\sum\nolimits_{i=1}^n \quad
\int\limits_0^{\frac{\pi}{2}} \quad
\prod\nolimits_\epsilon
\]</span></p>
<p><code>amsmath</code> 宏包还提供了
<code>\substack</code>，能够在下限位置书写多行表达式；<span
class="math inline">\(\texttt{subarray}\)</span>
环境更进一步，令多行表达式可选择居中 (c) 或左对齐 (l)：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;amssymb&#125;</span></span><br><span class="line"><span class="keyword">\[</span></span><br><span class="line"><span class="keyword">\sum</span><span class="built_in">_</span>&#123;<span class="keyword">\substack</span>&#123;0<span class="keyword">\le</span> i<span class="keyword">\le</span> n <span class="keyword">\\</span></span><br><span class="line">  j<span class="keyword">\in</span> <span class="keyword">\mathbb</span>&#123;R&#125;&#125;&#125;</span><br><span class="line">P(i,j) = Q(n)</span><br><span class="line"> <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\sum</span><span class="built_in">_</span>&#123;<span class="keyword">\begin</span>&#123;subarray&#125;&#123;l&#125;</span><br><span class="line">  0<span class="keyword">\le</span> i<span class="keyword">\le</span> n <span class="keyword">\\</span></span><br><span class="line">  j<span class="keyword">\in</span> <span class="keyword">\mathbb</span>&#123;R&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;subarray&#125;&#125;</span><br><span class="line">P(i,j) = Q(n)</span><br><span class="line"><span class="keyword">\]</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{gather*}
\sum_{\substack{0\le i\le n \\
  j\in \mathbb{R}}}
P(i,j) = Q(n)
\\
\sum_{\begin{subarray}{l}
  0\le i\le n \\
  j\in \mathbb{R}
\end{subarray}}
P(i,j) = Q(n)
\end{gather*}
\]</span></p>
<h3 id="数学重音和上下括号">数学重音和上下括号</h3>
<p>数学符号可以像文字一样加重音，比如求导符号 <span
class="math inline">\(\dot{r}\)</span> （<code>\dot{r}</code>）、 <span
class="math inline">\(\ddot{r}\)</span>
(<code>\ddot{r}</code>)、表示向量的箭头 <span
class="math inline">\(\vec{r}\)</span> (<code>\vec{r}</code>)
、表示单位向量的符号 <span
class="math inline">\(\hat{\mathbf{e}}\)</span>
(<code>\hat{mathbf{e}}</code>)等。使用时要注意重音符号的作用区域，一般应当对<strong>某个符号</strong>而不是“符号加下标”使用重音：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span><span class="keyword">\bar</span>&#123;x<span class="built_in">_</span>0&#125; <span class="keyword">\quad</span> <span class="keyword">\bar</span>&#123;x&#125;<span class="built_in">_</span>0<span class="built_in">$</span><span class="keyword">\\</span>[5pt]</span><br><span class="line"><span class="built_in">$</span><span class="keyword">\vec</span>&#123;x<span class="built_in">_</span>0&#125; <span class="keyword">\quad</span> <span class="keyword">\vec</span>&#123;x&#125;<span class="built_in">_</span>0<span class="built_in">$</span><span class="keyword">\\</span>[5pt]</span><br><span class="line"><span class="built_in">$</span><span class="keyword">\hat</span>&#123;<span class="keyword">\mathbf</span>&#123;e&#125;<span class="built_in">_</span>x&#125; <span class="keyword">\quad</span></span><br><span class="line"> <span class="keyword">\hat</span>&#123;<span class="keyword">\mathbf</span>&#123;e&#125;&#125;<span class="built_in">_</span>x<span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{gather*}
\bar{x_0} \quad \bar{x}_0\\[5pt]
\vec{x_0} \quad \vec{x}_0\\[5pt]
\hat{\mathbf{e}_x} \quad
\hat{\mathbf{e}}_x
\end{gather*}
\]</span></p>
<p>也能为多个字符加重音，包括直接画线的 <code>\overline</code> 和
<code>\underline</code> 命令（可叠加使用）、宽重音符号
<code>\widehat</code>、表示向量的箭头 <code>\overrightarrow</code>
等。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span>0.<span class="keyword">\overline</span>&#123;3&#125; = <span class="keyword">\underline</span>&#123;<span class="keyword">\underline</span>&#123;1/3&#125;&#125;<span class="built_in">$</span> <span class="keyword">\\</span>[5pt]</span><br><span class="line"><span class="built_in">$</span><span class="keyword">\hat</span>&#123;XY&#125; <span class="keyword">\qquad</span> <span class="keyword">\widehat</span>&#123;XY&#125;<span class="built_in">$</span><span class="keyword">\\</span>[5pt]</span><br><span class="line"><span class="built_in">$</span><span class="keyword">\vec</span>&#123;AB&#125; <span class="keyword">\qquad</span> <span class="keyword">\overrightarrow</span>&#123;AB&#125;<span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{gather*}
0.\overline{3} = \underline{\underline{1/3}} \\[5pt]
\hat{XY} \qquad \widehat{XY}\\[5pt]
\vec{AB} \qquad \overrightarrow{AB}
\end{gather*}
\]</span></p>
<p><code>\overbrace</code> 和 <code>\underbrace</code>
命令用来生成上/下括号，各自可带一个上/下标公式。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span><span class="keyword">\underbrace</span>&#123;<span class="keyword">\overbrace</span>&#123;(a+b+c)&#125;<span class="built_in">^</span>6</span><br><span class="line"><span class="keyword">\cdot</span> <span class="keyword">\overbrace</span>&#123;(d+e+f)&#125;<span class="built_in">^</span>7&#125;</span><br><span class="line"><span class="built_in">_</span><span class="keyword">\text</span>&#123;meaning of life&#125; = 42<span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\underbrace{\overbrace{(a+b+c)}^6
\cdot \overbrace{(d+e+f)}^7}
_\text{meaning of life} = 42
\]</span></p>
<h3 id="箭头">箭头</h3>
<p>常用的箭头包括 <code>\rightarrow</code>(<span
class="math inline">\(\rightarrow\)</span>，或
<code>\to</code>)、<code>\leftarrow</code>（<span
class="math inline">\(\leftarrow\)</span>，或<code>\gets</code>）等。</p>
<p><code>amsmath</code> 的 <code>xleftarrow</code> 和
<code>xrightarrow</code>
命令提供了长度可以伸展的箭头，并且可以为箭头增加上下标：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\[</span> a<span class="keyword">\xleftarrow</span>&#123;x+y+z&#125; b <span class="keyword">\]</span> <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\[</span> c<span class="keyword">\xrightarrow</span>[x&lt;y]&#123;a*b*c&#125;d <span class="keyword">\]</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{gather*}
a\xleftarrow{x+y+z} b \\
c\xrightarrow[x&lt;y]{a*b*c}d
\end{gather*}
\]</span></p>
<h3 id="括号和定界符">括号和定界符</h3>
<p><span class="math inline">\(\LaTeX{}\)</span>
提供了多种括号和定界符表示公式块的边界，如小括号 <span
class="math inline">\(()\)</span>、中括号 <span
class="math inline">\([]\)</span>、大括号 <span
class="math inline">\(\{\}\)</span> (<code>\{ \}</code>)、尖括号 <span
class="math inline">\(\langle \rangle\)</span>（<code>\langle</code>
<code>\rangle</code>）等。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span>&#123;a,b,c&#125; <span class="keyword">\neq</span> <span class="keyword">\&#123;</span>a,b,c<span class="keyword">\&#125;</span><span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
{a,b,c} \neq \{a,b,c\}
\]</span></p>
<p>使用 <code>\left</code> 和 <code>\right</code>
命令可令括号（定界符）的大小可变，在行间公式中常用。会自动根据括号内的公式大小决定定界符大小。
<code>\left</code> 和 <code>\right</code>
必须成对使用。需要使用单个定界符时，另一个定界符写成 <code>\left.</code>
或<code>\right.</code>。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\[</span>1 + <span class="keyword">\left</span>(<span class="keyword">\frac</span>&#123;1&#125;&#123;1-x<span class="built_in">^</span>&#123;2&#125;&#125;</span><br><span class="line"><span class="keyword">\right</span>)<span class="built_in">^</span>3 <span class="keyword">\qquad</span></span><br><span class="line"><span class="keyword">\left</span>.<span class="keyword">\frac</span>&#123;<span class="keyword">\partial</span> f&#125;&#123;<span class="keyword">\partial</span> t&#125;</span><br><span class="line"><span class="keyword">\right</span>|<span class="built_in">_</span>&#123;t=0&#125;<span class="keyword">\]</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
1 + \left(\frac{1}{1-x^{2}}
\right)^3 \qquad
\left.\frac{\partial f}{\partial t}
\right|_{t=0}
\]</span></p>
<p>有时我们不满意于 <span class="math inline">\(\LaTeX{}\)</span>
自动调节的定界符大小。这时还可以用 <code>\big</code>、<code>bigg</code>
等命令生成固定大小的定界符。 更常用的形式是类似 <code>\left</code> 的
<code>\bigl</code>、<code>\biggl</code> 等，以及类似 <code>\right</code>
的 <code>\bigr</code>、<code>\biggr</code> 等（<code>\bigl</code> 和
<code>\bigr</code> 不必成对出现）。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span><span class="keyword">\Bigl</span>((x+1)(x-1)<span class="keyword">\Bigr</span>)<span class="built_in">^</span>&#123;2&#125;<span class="built_in">$</span><span class="keyword">\\</span></span><br><span class="line"><span class="built_in">$</span><span class="keyword">\bigl</span>( <span class="keyword">\Bigl</span>( <span class="keyword">\biggl</span>( <span class="keyword">\Biggl</span>( <span class="keyword">\quad</span></span><br><span class="line"><span class="keyword">\bigr</span><span class="keyword">\&#125;</span> <span class="keyword">\Bigr</span><span class="keyword">\&#125;</span> <span class="keyword">\biggr</span><span class="keyword">\&#125;</span> <span class="keyword">\Biggr</span><span class="keyword">\&#125;</span> <span class="keyword">\quad</span></span><br><span class="line"><span class="keyword">\big</span><span class="keyword">\|</span> <span class="keyword">\Big</span><span class="keyword">\|</span> <span class="keyword">\bigg</span><span class="keyword">\|</span> <span class="keyword">\Bigg</span><span class="keyword">\|</span> <span class="keyword">\quad</span></span><br><span class="line"><span class="keyword">\big</span><span class="keyword">\Downarrow</span> <span class="keyword">\Big</span><span class="keyword">\Downarrow</span></span><br><span class="line"><span class="keyword">\bigg</span><span class="keyword">\Downarrow</span> <span class="keyword">\Bigg</span><span class="keyword">\Downarrow</span><span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{gather*}
\Bigl((x+1)(x-1)\Bigr)^{2}\\
\bigl( \Bigl( \biggl( \Biggl( \quad
\bigr\} \Bigr\} \biggr\} \Biggr\} \quad
\big\| \Big\| \bigg\| \Bigg\| \quad
\big\Downarrow \Big\Downarrow
\bigg\Downarrow \Bigg\Downarrow
\end{gather*}
\]</span></p>
<p>使用 <code>\big</code> 和 <code>\bigg</code>
等命令的另外一个好处是：用<code>\left</code> 和 <code>\right</code>
分界符包裹的公式块是不允许断行的（下文提到的 <span
class="math inline">\(\texttt{array}\)</span> 或者 <span
class="math inline">\(\texttt{aligned}\)</span>
等环境视为一个公式块），所以也不允许在多行公式里跨行使用，而
<code>\big</code> 和 <code>\bigg</code> 等命令不受限制。</p>
<hr />
<h2 id="多行公式">多行公式</h2>
<h3 id="长公式折行">长公式折行</h3>
<p>通常来讲应当避免写出超过一行而需要折行的长公式。如果一定要折行的话，习惯上优先在等号之前折行，其次在加号、减号之前，再次在乘号、除号之前。
其它位置应当避免折行。</p>
<p><code>amsmath</code> 宏包的 <code>multline</code>
环境提供了书写折行长公式的方便环境。它允许用 <code>\\</code>
折行，将公式编号放在最后一行。多行公式的首行左对齐，末行右对齐，其余行居中。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;multline&#125;</span><br><span class="line">a + b + c + d + e + f</span><br><span class="line">+ g + h + i <span class="keyword">\\</span></span><br><span class="line">= j + k + l + m + n<span class="keyword">\\</span></span><br><span class="line">= o + p + q + r + s<span class="keyword">\\</span></span><br><span class="line">= t + u + v + x + z</span><br><span class="line"><span class="keyword">\end</span>&#123;multline&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120103429233.png" alt="image-20250120103429233" style="zoom:67%;" /></p>
<p>与表格不同的是，<strong>公式的最后一行不写
<code>\\</code>，如果写了，反倒会产生一个多余的空行</strong>。</p>
<p>类似 <code>equation*</code>，<code>multline*</code>
环境排版不带编号的折行长公式。</p>
<h3 id="多行公式-1">多行公式</h3>
<p>更多的情况是，需要罗列一系列公式，并令其按照等号对齐。 目前最常用的是
<code>align</code> 环境，它将公式用 <code>&amp;</code>
隔为两部分并对齐。分隔符通常放在等号左边：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;align&#125;</span><br><span class="line">a <span class="built_in">&amp;</span> = b + c <span class="keyword">\\</span></span><br><span class="line"><span class="built_in">&amp;</span> = d + e</span><br><span class="line"><span class="keyword">\end</span>&#123;align&#125;</span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{align}
a &amp; = b + c \\
&amp; = d + e
\end{align}
\]</span></p>
<p><code>align</code> 环境会给每行公式都编号。仍然可以用
<code>\notag</code> 去掉某行的编号。
在以下的例子，为了对齐等号，将分隔符放在右侧，并且此时需要在等号后添加一对括号
<code>{}</code> 以产生正常的间距：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;align&#125;</span><br><span class="line">a =&#123;&#125; <span class="built_in">&amp;</span> b + c <span class="keyword">\\</span></span><br><span class="line">  =&#123;&#125; <span class="built_in">&amp;</span> d + e + f + g + h + i + j + k + l <span class="keyword">\notag</span> <span class="keyword">\\</span></span><br><span class="line">            <span class="built_in">&amp;</span> + m + n + o <span class="keyword">\\</span></span><br><span class="line">    =&#123;&#125; <span class="built_in">&amp;</span> p + q + r + s</span><br><span class="line"><span class="keyword">\end</span>&#123;align&#125;</span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{align}
a ={} &amp; b + c \\
  ={} &amp; d + e + f + g + h + i + j + k + l \notag \\
      &amp; + m + n + o \\
  ={} &amp; p + q + r + s
\end{align}
\]</span></p>
<p><code>align</code> 还能够对齐多组公式，除等号前的 <code>&amp;</code>
之外, 公式之间也用 <code>&amp;</code> 分隔：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;align&#125;</span><br><span class="line">a <span class="built_in">&amp;</span>=1  <span class="built_in">&amp;</span>  b <span class="built_in">&amp;</span>=2   <span class="built_in">&amp;</span> c <span class="built_in">&amp;</span>=3   <span class="keyword">\\</span></span><br><span class="line">d <span class="built_in">&amp;</span>=-1 <span class="built_in">&amp;</span>  e <span class="built_in">&amp;</span>=-2  <span class="built_in">&amp;</span> f <span class="built_in">&amp;</span>=-5</span><br><span class="line"><span class="keyword">\end</span>&#123;align&#125;</span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{align}
a &amp;=1  &amp;  b &amp;=2   &amp; c &amp;=3   \\
d &amp;=-1 &amp;  e &amp;=-2  &amp; f &amp;=-5
\end{align}
\]</span></p>
<p>如果不需要按等号对齐，只需罗列数个公式，<code>gather</code>
将是一个很好用的环境：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;gather&#125;</span><br><span class="line">a = b + c <span class="keyword">\\</span></span><br><span class="line">d = e + f + g <span class="keyword">\\</span></span><br><span class="line">h + i = j + k <span class="keyword">\notag</span> <span class="keyword">\\</span></span><br><span class="line">l + m = n</span><br><span class="line"><span class="keyword">\end</span>&#123;gather&#125;</span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{gather}
a = b + c \\
d = e + f + g \\
h + i = j + k \notag \\
l + m = n
\end{gather}
\]</span></p>
<p>有对应的不带编号的版本 <code>align*</code> 和
<code>gather*</code>。</p>
<h3 id="公用编号的多行公式">公用编号的多行公式</h3>
<p>另一个常见的需求是将多个公式组在一起公用一个编号，编号位于公式的居中位置。为此，<code>amsmath</code>
宏包提供了诸如 <code>aligned</code>、<code>gathered</code> 等环境，与
<code>equation</code> 环境套用。以 <code>-ed</code>
结尾的环境用法与前一节不以 <code>-ed</code> 结尾的环境用法一一对应。仅以
<code>aligned</code> 举例：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;equation&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;aligned&#125;</span><br><span class="line">a <span class="built_in">&amp;</span>= b + c <span class="keyword">\\</span></span><br><span class="line">d <span class="built_in">&amp;</span>= e + f + g <span class="keyword">\\</span></span><br><span class="line">h + i <span class="built_in">&amp;</span>= j + k <span class="keyword">\\</span></span><br><span class="line">l + m <span class="built_in">&amp;</span>= n</span><br><span class="line"><span class="keyword">\end</span>&#123;aligned&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;equation&#125;</span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{equation}
\begin{aligned}
a &amp;= b + c \\
d &amp;= e + f + g \\
h + i &amp;= j + k \\
l + m &amp;= n
\end{aligned}
\end{equation}
\]</span></p>
<p><code>split</code> 环境和 <code>aligned</code> 环境用法类似，也用于和
<code>equation</code> 环境套用，区别是 <code>split</code>
只能将每行的一个公式分两栏，<code>aligned</code>
允许每行多个公式多栏。</p>
<hr />
<h2 id="数组和矩阵">数组和矩阵</h2>
<p>为了排版二维数组，<span class="math inline">\(\LaTeX{}\)</span>
提供了 <code>array</code> 环境，用法与 <code>tabular</code>
环境极为类似，也需要定义列格式，并用 <code>\\</code>
换行。数组可作为一个公式块，在外套用
<code>\left</code>、<code>\right</code> 等定界符：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\[</span> <span class="keyword">\mathbf</span>&#123;X&#125; = <span class="keyword">\left</span>(</span><br><span class="line"><span class="keyword">\begin</span>&#123;array&#125;&#123;cccc&#125;</span><br><span class="line">x<span class="built_in">_</span>&#123;11&#125; <span class="built_in">&amp;</span> x<span class="built_in">_</span>&#123;12&#125; <span class="built_in">&amp;</span> <span class="keyword">\ldots</span> <span class="built_in">&amp;</span> x<span class="built_in">_</span>&#123;1n&#125;<span class="keyword">\\</span></span><br><span class="line">x<span class="built_in">_</span>&#123;21&#125; <span class="built_in">&amp;</span> x<span class="built_in">_</span>&#123;22&#125; <span class="built_in">&amp;</span> <span class="keyword">\ldots</span> <span class="built_in">&amp;</span> x<span class="built_in">_</span>&#123;2n&#125;<span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\vdots</span> <span class="built_in">&amp;</span> <span class="keyword">\vdots</span> <span class="built_in">&amp;</span> <span class="keyword">\ddots</span> <span class="built_in">&amp;</span> <span class="keyword">\vdots</span><span class="keyword">\\</span></span><br><span class="line">x<span class="built_in">_</span>&#123;n1&#125; <span class="built_in">&amp;</span> x<span class="built_in">_</span>&#123;n2&#125; <span class="built_in">&amp;</span> <span class="keyword">\ldots</span> <span class="built_in">&amp;</span> x<span class="built_in">_</span>&#123;nn&#125;<span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\end</span>&#123;array&#125; <span class="keyword">\right</span>) <span class="keyword">\]</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\mathbf{X} = \left(
\begin{array}{cccc}
x_{11} &amp; x_{12} &amp; \ldots &amp; x_{1n}\\
x_{21} &amp; x_{22} &amp; \ldots &amp; x_{2n}\\
\vdots &amp; \vdots &amp; \ddots &amp; \vdots\\
x_{n1} &amp; x_{n2} &amp; \ldots &amp; x_{nn}\\
\end{array} \right)
\]</span></p>
<p>值得注意的是，上一节末尾介绍的 <code>aligned</code>
等环境也可以用定界符包裹。</p>
<p>还可以利用空的定界符排版出这样的效果：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\[</span> |x| = <span class="keyword">\left</span><span class="keyword">\&#123;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;array&#125;&#123;rl&#125;</span><br><span class="line">-x <span class="built_in">&amp;</span> <span class="keyword">\text</span>&#123;if &#125; x &lt; 0,<span class="keyword">\\</span></span><br><span class="line">0 <span class="built_in">&amp;</span> <span class="keyword">\text</span>&#123;if &#125; x = 0,<span class="keyword">\\</span></span><br><span class="line">x <span class="built_in">&amp;</span> <span class="keyword">\text</span>&#123;if &#125; x &gt; 0.</span><br><span class="line"><span class="keyword">\end</span>&#123;array&#125; <span class="keyword">\right</span>. <span class="keyword">\]</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
|x| = \left\{
\begin{array}{rl}
-x &amp; \text{if } x &lt; 0,\\
0 &amp; \text{if } x = 0,\\
x &amp; \text{if } x &gt; 0.
\end{array} \right.
\]</span></p>
<p>不过上述例子可以用 <code>amsmath</code> 提供的 <code>cases</code>
环境更轻松地完成：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\[</span> |x| =</span><br><span class="line"><span class="keyword">\begin</span>&#123;cases&#125;</span><br><span class="line">-x <span class="built_in">&amp;</span> <span class="keyword">\text</span>&#123;if &#125; x &lt; 0,<span class="keyword">\\</span></span><br><span class="line">0 <span class="built_in">&amp;</span> <span class="keyword">\text</span>&#123;if &#125; x = 0,<span class="keyword">\\</span></span><br><span class="line">x <span class="built_in">&amp;</span> <span class="keyword">\text</span>&#123;if &#125; x &gt; 0.</span><br><span class="line"><span class="keyword">\end</span>&#123;cases&#125; <span class="keyword">\]</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
|x| =
\begin{cases}
-x &amp; \text{if } x &lt; 0,\\
0 &amp; \text{if } x = 0,\\
x &amp; \text{if } x &gt; 0.
\end{cases}
\]</span></p>
<p><code>amsmath</code>
宏包还直接提供多种排版矩阵的环境，包括不带定界符的
<code>matrix</code>，以及带各种定界符的矩阵 <code>pmatrix</code>（<span
class="math inline">\(\bigl(\)</span>）、<code>bmatrix</code>（<span
class="math inline">\(\bigl[\)</span>）、<code>Bmatrix</code>（<span
class="math inline">\(\bigl\{\)</span>）、<code>vmatrix</code>（<span
class="math inline">\(\bigl\vert\)</span>）、<code>Vmatrix</code>（<span
class="math inline">\(\bigl\Vert\)</span>）。使用这些环境时，无需给定列格式（事实上这些矩阵内部也是用
<code>array</code> 环境生成的。）</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\[</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;matrix&#125;</span><br><span class="line">1 <span class="built_in">&amp;</span> 2 <span class="keyword">\\</span> 3 <span class="built_in">&amp;</span> 4</span><br><span class="line"><span class="keyword">\end</span>&#123;matrix&#125; <span class="keyword">\qquad</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;bmatrix&#125;</span><br><span class="line">x<span class="built_in">_</span>&#123;11&#125; <span class="built_in">&amp;</span> x<span class="built_in">_</span>&#123;12&#125; <span class="built_in">&amp;</span> <span class="keyword">\ldots</span> <span class="built_in">&amp;</span> x<span class="built_in">_</span>&#123;1n&#125;<span class="keyword">\\</span></span><br><span class="line">x<span class="built_in">_</span>&#123;21&#125; <span class="built_in">&amp;</span> x<span class="built_in">_</span>&#123;22&#125; <span class="built_in">&amp;</span> <span class="keyword">\ldots</span> <span class="built_in">&amp;</span> x<span class="built_in">_</span>&#123;2n&#125;<span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\vdots</span> <span class="built_in">&amp;</span> <span class="keyword">\vdots</span> <span class="built_in">&amp;</span> <span class="keyword">\ddots</span> <span class="built_in">&amp;</span> <span class="keyword">\vdots</span><span class="keyword">\\</span></span><br><span class="line">x<span class="built_in">_</span>&#123;n1&#125; <span class="built_in">&amp;</span> x<span class="built_in">_</span>&#123;n2&#125; <span class="built_in">&amp;</span> <span class="keyword">\ldots</span> <span class="built_in">&amp;</span> x<span class="built_in">_</span>&#123;nn&#125;<span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\end</span>&#123;bmatrix&#125;</span><br><span class="line"><span class="keyword">\]</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{matrix}
1 &amp; 2 \\ 3 &amp; 4
\end{matrix} \qquad
\begin{bmatrix}
x_{11} &amp; x_{12} &amp; \ldots &amp; x_{1n}\\
x_{21} &amp; x_{22} &amp; \ldots &amp; x_{2n}\\
\vdots &amp; \vdots &amp; \ddots &amp; \vdots\\
x_{n1} &amp; x_{n2} &amp; \ldots &amp; x_{nn}\\
\end{bmatrix}
\]</span></p>
<p>在矩阵中的元素里排版分式时，要用到<code>\dfrac</code> 等命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\[</span></span><br><span class="line"><span class="keyword">\mathbf</span>&#123;H&#125;=</span><br><span class="line"><span class="keyword">\begin</span>&#123;bmatrix&#125;</span><br><span class="line"><span class="keyword">\dfrac</span>&#123;<span class="keyword">\partial</span><span class="built_in">^</span>2 f&#125;&#123;<span class="keyword">\partial</span> x<span class="built_in">^</span>2&#125; <span class="built_in">&amp;</span></span><br><span class="line"><span class="keyword">\dfrac</span>&#123;<span class="keyword">\partial</span><span class="built_in">^</span>2 f&#125;</span><br><span class="line">      &#123;<span class="keyword">\partial</span> x <span class="keyword">\partial</span> y&#125; <span class="keyword">\\</span>[8pt]</span><br><span class="line"><span class="keyword">\dfrac</span>&#123;<span class="keyword">\partial</span><span class="built_in">^</span>2 f&#125;</span><br><span class="line">      &#123;<span class="keyword">\partial</span> x <span class="keyword">\partial</span> y&#125; <span class="built_in">&amp;</span></span><br><span class="line"><span class="keyword">\dfrac</span>&#123;<span class="keyword">\partial</span><span class="built_in">^</span>2 f&#125;&#123;<span class="keyword">\partial</span> y<span class="built_in">^</span>2&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;bmatrix&#125;</span><br><span class="line"><span class="keyword">\]</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\mathbf{H}=
\begin{bmatrix}
\dfrac{\partial^2 f}{\partial x^2} &amp;
\dfrac{\partial^2 f}
      {\partial x \partial y} \\[8pt]
\dfrac{\partial^2 f}
      {\partial x \partial y} &amp;
\dfrac{\partial^2 f}{\partial y^2}
\end{bmatrix}
\]</span></p>
<hr />
<h2 id="公式中的间距">公式中的间距</h2>
<p>绝大部分时候，数学公式中各元素的间距是根据符号类型自动生成的，需要手动调整的情况极少。已经认识了两个生成间距的命令
<code>\quad</code> 和
<code>\qquad</code>。在公式中可能用到的间距还包括<code>\,</code>、<code>\:</code>、<code>\;</code>
以及负间距 <code>\!</code>。文本中的 <code>\␣</code>
也能使用在数学公式中。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120153652472.png" alt="image-20250120153652472" style="zoom:67%;" /></p>
<p>一个常见的用途是修正积分的被积函数 <span
class="math inline">\(f(x)\)</span> 和微元 <span
class="math inline">\(\mathrm{d}x\)</span> 之间的距离。注意微元里的
<span class="math inline">\(\mathrm{d}\)</span> 用的是直立体：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\[</span></span><br><span class="line"><span class="keyword">\int</span><span class="built_in">_</span>a<span class="built_in">^</span>b f(x)<span class="keyword">\mathrm</span>&#123;d&#125;x</span><br><span class="line"><span class="keyword">\qquad</span></span><br><span class="line"><span class="keyword">\int</span><span class="built_in">_</span>a<span class="built_in">^</span>b f(x)<span class="keyword">\,</span><span class="keyword">\mathrm</span>&#123;d&#125;x</span><br><span class="line"><span class="keyword">\]</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\int_a^b f(x)\mathrm{d}x
\qquad
\int_a^b f(x)\,\mathrm{d}x
\]</span></p>
<p>另一个用途是生成多重积分号。如果直接连写两个
<code>\int</code>，之间的间距将会过宽，此时可以使用负间距
<code>\!</code> 修正之。不过 <code>amsmath</code>
提供了更方便的多重积分号，如二重积分
<code>\iint</code>、三重积分<code>\iiint</code> 等。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\newcommand</span><span class="keyword">\diff</span>&#123;<span class="keyword">\,</span><span class="keyword">\mathrm</span>&#123;d&#125;&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;gather*&#125;</span><br><span class="line"><span class="keyword">\int</span><span class="keyword">\int</span> f(x)g(y) <span class="keyword">\diff</span> x <span class="keyword">\diff</span> y <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\int</span><span class="keyword">\!</span><span class="keyword">\!</span><span class="keyword">\!</span><span class="keyword">\int</span> f(x)g(y) <span class="keyword">\diff</span> x <span class="keyword">\diff</span> y <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\iint</span> f(x)g(y) <span class="keyword">\diff</span> x <span class="keyword">\diff</span> y <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\iint</span><span class="keyword">\quad</span> <span class="keyword">\iiint</span><span class="keyword">\quad</span> <span class="keyword">\idotsint</span></span><br><span class="line"><span class="keyword">\end</span>&#123;gather*&#125;</span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\newcommand\diff{\,\mathrm{d}}
\begin{gather*}
\int\int f(x)g(y)
\diff x \diff y \\
\int\!\!\!\int
f(x)g(y) \diff x \diff y \\
\iint f(x)g(y) \diff x \diff y \\
\iint\quad \iiint\quad \idotsint
\end{gather*}
\]</span></p>
<hr />
<h2 id="数学符号的字体控制">数学符号的字体控制</h2>
<h3 id="数学字母字体">数学字母字体</h3>
<p><span class="math inline">\(\LaTeX{}\)</span>
允许一部分数学符号切换字体，主要是拉丁字母、数字、大写希腊字母以及重音符号等。下表给出了切换字体的命令。某些命令需要字体宏包支持。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;amssymb&#125;</span></span><br><span class="line"><span class="built_in">$</span><span class="keyword">\mathcal</span>&#123;R&#125; <span class="keyword">\quad</span> <span class="keyword">\mathfrak</span>&#123;R&#125; <span class="keyword">\quad</span> <span class="keyword">\mathbb</span>&#123;R&#125;<span class="built_in">$</span></span><br><span class="line"><span class="keyword">\[</span><span class="keyword">\mathcal</span>&#123;L&#125; = -<span class="keyword">\frac</span>&#123;1&#125;&#123;4&#125;F<span class="built_in">_</span>&#123;<span class="keyword">\mu</span><span class="keyword">\nu</span>&#125;F<span class="built_in">^</span>&#123;<span class="keyword">\mu</span><span class="keyword">\nu</span>&#125;<span class="keyword">\]</span></span><br><span class="line"><span class="built_in">$</span><span class="keyword">\mathfrak</span>&#123;su&#125;(2)<span class="built_in">$</span> and <span class="built_in">$</span><span class="keyword">\mathfrak</span>&#123;so&#125;(3)<span class="built_in">$</span> Lie algebra</span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
% \usepackage{amssymb}
\begin{gather*}
\mathcal{R} \quad \mathfrak{R} \quad \mathbb{R} \\
\mathcal{L} = -\frac{1}{4}F_{\mu\nu}F^{\mu\nu} \\
\mathfrak{su}(2) \  and \  \mathfrak{so}(3) \  Lie \  algebra
\end{gather*}
\]</span></p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120160607188.png" alt="image-20250120160607188" style="zoom:67%;" /></p>
<p>一般来说，不同的数学字体往往带有不同的语义，如矩阵、向量等常会使用粗体或粗斜体，而数集常会使用
<code>\mathbb</code>
表示。出于内容与格式分离以及方便书写的考虑，可以为它们定义新的命令。</p>
<h3 id="加粗的数学符号">加粗的数学符号</h3>
<p>上表中的 <code>\mathbf</code>
命令只能获得直立、加粗的字母。如果想得到粗斜体（国内使用粗斜体符号表示矢量，见
GB/T 3102.11—1993）。可以使用 <code>amsmath</code> 宏包提供的
<code>\boldsymbol</code> 命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span><span class="keyword">\mu</span>, M <span class="keyword">\qquad</span> <span class="keyword">\boldsymbol</span>&#123;<span class="keyword">\mu</span>&#125;, <span class="keyword">\boldsymbol</span>&#123;M&#125;<span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\mu, M \qquad \boldsymbol{\mu}, \boldsymbol{M}
\]</span></p>
<p>也可以使用 <code>bm</code> 宏包提供的 <code>\bm</code> 命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;bm&#125;</span></span><br><span class="line"><span class="built_in">$</span><span class="keyword">\mu</span>, M <span class="keyword">\qquad</span> <span class="keyword">\bm</span>&#123;<span class="keyword">\mu</span>&#125;, <span class="keyword">\bm</span>&#123;M&#125;<span class="built_in">$</span></span><br></pre></td></tr></table></figure>
<p>在 <span class="math inline">\(\LaTeX{}\)</span>
默认的数学字体中，一些符号本身并没有粗体版本，使用
<code>\boldsymbol</code> 也得不到粗体。此时 <code>\bm</code>
命令会生成“伪粗体”，尽管效果比较粗糙，但在某些时候也不失为一种解决方案。</p>
<h3 id="数学符号的尺寸">数学符号的尺寸</h3>
<p>数学符号按照符号排版的位置规定尺寸，从大到小包括行间公式尺寸、行内公式尺寸、上下标尺寸、次级上下标尺寸。除了字号有别之外，行间和行内公式尺寸下的巨算符也使用不一样的大小。
为每个数学尺寸指定了一个切换的命令，见 下表。</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">命令</th>
<th style="text-align: left;">尺寸</th>
<th style="text-align: left;">示例</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><code>\displaystyle</code></td>
<td style="text-align: left;">行间公式尺寸</td>
<td style="text-align: left;"><span
class="math inline">\(\displaystyle\sum a\)</span></td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>\textstyle</code></td>
<td style="text-align: left;">行内公式尺寸</td>
<td style="text-align: left;"><span class="math inline">\(\textstyle\sum
a\)</span></td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>\scriptstyle</code></td>
<td style="text-align: left;">上下标尺寸</td>
<td style="text-align: left;"><span class="math inline">\(\scriptstyle
a\)</span></td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>\scriptscriptstyle</code></td>
<td style="text-align: left;">次级上下标尺寸</td>
<td style="text-align: left;"><span
class="math inline">\(\scriptscriptstyle a\)</span></td>
</tr>
</tbody>
</table>
<p>通过以下示例对比行间公式和行内公式的区别。在分式中，<strong>分子分母默认为行内公式尺寸</strong>，示例中将分母切换到行间公式尺寸：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\[</span></span><br><span class="line">r = <span class="keyword">\frac</span></span><br><span class="line">  &#123;<span class="keyword">\sum</span><span class="built_in">_</span>&#123;i=1&#125;<span class="built_in">^</span>n (x<span class="built_in">_</span>i- x)(y<span class="built_in">_</span>i- y)&#125;</span><br><span class="line">  &#123;<span class="keyword">\displaystyle</span> <span class="keyword">\left</span>[</span><br><span class="line">    <span class="keyword">\sum</span><span class="built_in">_</span>&#123;i=1&#125;<span class="built_in">^</span>n (x<span class="built_in">_</span>i-x)<span class="built_in">^</span>2</span><br><span class="line">    <span class="keyword">\sum</span><span class="built_in">_</span>&#123;i=1&#125;<span class="built_in">^</span>n (y<span class="built_in">_</span>i-y)<span class="built_in">^</span>2</span><br><span class="line">  <span class="keyword">\right</span>]<span class="built_in">^</span>&#123;1/2&#125; &#125;</span><br><span class="line"><span class="keyword">\]</span></span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
r = \frac
  {\sum_{i=1}^n (x_i- x)(y_i- y)}
  {\displaystyle \left[
    \sum_{i=1}^n (x_i-x)^2
    \sum_{i=1}^n (y_i-y)^2
  \right]^{1/2} }
\]</span></p>
<hr />
<h2 id="定理环境">定理环境</h2>
<h3 id="latex-原始的定理环境"><span
class="math inline">\(\LaTeX{}\)</span> 原始的定理环境</h3>
<p>使用 <span class="math inline">\(\LaTeX{}\)</span>
排版数学和其他科技文档时，会接触到大量的定理、证明等内容。<span
class="math inline">\(\LaTeX{}\)</span> 提供了一个基本的命令
<code>\newtheorem</code> 提供定理环境的定义：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\newtheorem</span>&#123;theorem environment&#125;&#123;title&#125;[section-level] <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\newtheorem</span>&#123;theorem environment&#125;[counter]&#123;title&#125;</span><br></pre></td></tr></table></figure>
<p><code>theorem environment</code> 为定理环境的名称。原始的 <span
class="math inline">\(\LaTeX{}\)</span>
里<strong>没有现成的定理环境</strong>，不加定义而直接使用很可能会出错。<code>title</code>
是定理环境的标题（“定理”，“公理”等）。</p>
<p>定理的序号由两个可选参数之一决定，它们<strong>不能同时使用</strong>：</p>
<ul>
<li><code>section-level</code> 为章节级别，如
<code>chapter</code>、<code>section</code>
等，定理序号成为章节的下一级序号；</li>
<li><code>counter</code> 为用 <code>\newcounter</code>
自定义的计数器名称，定理序号由这个计数器管理。</li>
</ul>
<p>如果两个可选参数都不用的话，则使用默认的与定理环境同名的计数器。</p>
<p>在以下示例代码中，定义了一个 <code>mythm</code> 环境，其序号设为
<code>section</code> 的下一级序号。注意 <code>mythm</code>
环境的可选参数以及 <code>\label</code> 的用法：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\newtheorem</span>&#123;mythm&#125;&#123;My Theorem&#125;[section]</span><br><span class="line"><span class="keyword">\begin</span>&#123;mythm&#125;<span class="keyword">\label</span>&#123;thm:light&#125;</span><br><span class="line">The light speed in vacuum</span><br><span class="line">is <span class="built_in">$</span>299,792,458<span class="keyword">\,</span><span class="keyword">\mathrm</span>&#123;m/s&#125;<span class="built_in">$</span>.</span><br><span class="line"><span class="keyword">\end</span>&#123;mythm&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;mythm&#125;[Energy-momentum relation]</span><br><span class="line">The relationship of energy,</span><br><span class="line">momentum and mass is</span><br><span class="line"><span class="keyword">\[</span>E<span class="built_in">^</span>2 = m<span class="built_in">_</span>0<span class="built_in">^</span>2 c<span class="built_in">^</span>4 + p<span class="built_in">^</span>2 c<span class="built_in">^</span>2<span class="keyword">\]</span></span><br><span class="line">where <span class="built_in">$</span>c<span class="built_in">$</span> is the light speed</span><br><span class="line">described in theorem <span class="keyword">\ref</span>&#123;thm:light&#125;.</span><br><span class="line"><span class="keyword">\end</span>&#123;mythm&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120165121095.png" alt="image-20250120165121095" style="zoom:67%;" /></p>
<h3 id="amsthm-宏包"><code>amsthm</code> 宏包</h3>
<p><span class="math inline">\(\LaTeX{}\)</span>
默认的定理环境格式为粗体标签、斜体内容、定理名用小括号包裹。如果需要修改格式，则要依赖其它的宏包，如
<code>amsthm</code>、<code>ntheorem</code> 等等。本小节简单介绍一下
<code>amsthm</code> 的用法。</p>
<p><code>amsthm</code> 提供了 <code>\theoremstyle</code>
命令支持定理格式的切换，在用 <code>\newtheorem</code>
命令定义定理环境之前使用。<code>amsthm</code>
预定义了三种格式用于<code>\theoremstyle</code>：<span
class="math inline">\(\texttt{plain}\)</span> 和 <span
class="math inline">\(\LaTeX{}\)</span> 原始的格式一致；<span
class="math inline">\(\texttt{definition}\)</span>
使用粗体标签、正体内容；<span
class="math inline">\(\texttt{remark}\)</span>
使用斜体标签、正体内容。</p>
<p>另外 <code>amsthm</code> 还支持用带星号的 <code>\newtheorem*</code>
定义不带序号的定理环境：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\theoremstyle</span>&#123;definition&#125; <span class="keyword">\newtheorem</span>&#123;law&#125;&#123;Law&#125;</span><br><span class="line"><span class="keyword">\theoremstyle</span>&#123;plain&#125; <span class="keyword">\newtheorem</span>&#123;jury&#125;[law]&#123;Jury&#125;</span><br><span class="line"><span class="keyword">\theoremstyle</span>&#123;remark&#125; <span class="keyword">\newtheorem</span>*&#123;mar&#125;&#123;Margaret&#125;</span><br></pre></td></tr></table></figure>
<p>以上例子定义的 <code>jury</code> 环境与 <code>law</code>
环境共用编号，<code>mar</code> 环境不编号：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\theoremstyle</span>&#123;definition&#125; <span class="keyword">\newtheorem</span>&#123;law&#125;&#123;Law&#125;</span><br><span class="line"><span class="keyword">\theoremstyle</span>&#123;plain&#125; <span class="keyword">\newtheorem</span>&#123;jury&#125;[law]&#123;Jury&#125;</span><br><span class="line"><span class="keyword">\theoremstyle</span>&#123;remark&#125; <span class="keyword">\newtheorem</span>*&#123;mar&#125;&#123;Margaret&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;law&#125;<span class="keyword">\label</span>&#123;law:box&#125;</span><br><span class="line">Don&#x27;t hide in the witness box.</span><br><span class="line"><span class="keyword">\end</span>&#123;law&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;jury&#125;[The Twelve]</span><br><span class="line">It could be you! So beware and</span><br><span class="line">see law~<span class="keyword">\ref</span>&#123;law:box&#125;.<span class="keyword">\end</span>&#123;jury&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;jury&#125;</span><br><span class="line">You will disregard the last</span><br><span class="line">statement.<span class="keyword">\end</span>&#123;jury&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;mar&#125;No, No, No<span class="keyword">\end</span>&#123;mar&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;mar&#125;Denis!<span class="keyword">\end</span>&#123;mar&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120171711806.png" alt="image-20250120171711806" style="zoom:67%;" /></p>
<p><code>amsthm</code> 还支持使用 <code>\newtheoremstyle</code>
命令自定义定理格式，更为方便使用的是 <code>ntheorem</code>
宏包。感兴趣的读者可参阅它们的帮助文档。</p>
<h3 id="证明环境和证毕符号">证明环境和证毕符号</h3>
<p><code>amsthm</code> 还提供了一个 <code>proof</code>
环境用于排版定理的证明过程。环境末尾自动加上一个 □ 证毕符号：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;proof&#125;</span><br><span class="line">For simplicity, we use</span><br><span class="line"><span class="keyword">\[</span></span><br><span class="line">E=mc<span class="built_in">^</span>2</span><br><span class="line"><span class="keyword">\]</span></span><br><span class="line">That&#x27;s it.</span><br><span class="line"><span class="keyword">\end</span>&#123;proof&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120173549574.png" alt="image-20250120173549574" style="zoom:67%;" /></p>
<p>如果行末是一个不带编号的公式，□ 符号会另起一行，这时可使用
<code>\qedhere</code> 命令将 □ 符号放在公式末尾：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120173828551.png" alt="image-20250120173828551" style="zoom:67%;" /></p>
<p><code>\qedhere</code> 对于 <code>align*</code> 等环境也有效：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;proof&#125;</span><br><span class="line">Assuming <span class="built_in">$</span><span class="keyword">\gamma</span></span><br><span class="line">= 1/<span class="keyword">\sqrt</span>&#123;1-v<span class="built_in">^</span>2/c<span class="built_in">^</span>2&#125;<span class="built_in">$</span>, then</span><br><span class="line"><span class="keyword">\begin</span>&#123;align*&#125;</span><br><span class="line">E <span class="built_in">&amp;</span>= <span class="keyword">\gamma</span> m<span class="built_in">_</span>0 c<span class="built_in">^</span>2  <span class="keyword">\\</span></span><br><span class="line">p <span class="built_in">&amp;</span>= <span class="keyword">\gamma</span> m<span class="built_in">_</span>0v <span class="keyword">\qedhere</span></span><br><span class="line"><span class="keyword">\end</span>&#123;align*&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;proof&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120174116569.png" alt="image-20250120174116569" style="zoom:67%;" /></p>
<p>在使用带编号的公式时，最好<strong>不要在公式末尾使用</strong>
<code>\qedhere</code> 命令。对带编号的公式使用<code>\qedhere</code>
命令会使 □ 符号放在一个难看的位置，紧贴着公式：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120174348875.png" alt="image-20250120174348875" style="zoom:67%;" /></p>
<p>在 <code>align</code> 等环境中使用 <code>\qedhere</code> 命令会使 □
盖掉公式的编号；使用 <code>equation</code> 嵌套 <code>aligned</code>
等环境时，<code>\qedhere</code> 命令会将 □
直接放在公式后。这些位置都不太正常。</p>
<p>证毕符号 □ 本身被定义在命令 <code>\qedsymbol</code>
中，如果有使用实心符号作为证毕符号的需求，需要自行用
<code>\renewcommand}</code>命令修改。可以利用标尺盒子来生成一个适当大小的“实心矩形”：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\renewcommand</span>&#123;<span class="keyword">\qedsymbol</span>&#125;<span class="comment">%</span></span><br><span class="line">  &#123;<span class="keyword">\rule</span>&#123;1ex&#125;&#123;1.5ex&#125;&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;proof&#125;</span><br><span class="line">For simplicity, we use</span><br><span class="line"><span class="keyword">\[</span></span><br><span class="line">E=mc<span class="built_in">^</span>2 <span class="keyword">\qedhere</span></span><br><span class="line"><span class="keyword">\]</span></span><br><span class="line"><span class="keyword">\end</span>&#123;proof&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120174901879.png" alt="image-20250120174901879" style="zoom:67%;" /></p>
<hr />
<h2 id="符号表">符号表</h2>
<p><span class="exturl" data-url="aHR0cHM6Ly93d3cuY25ibG9ncy5jb20vSW52aW5jLVovcC8xODY4MjUyMC9sc2hvcnQtc3ltYm9sLXRhYmxlcw==">Here<i class="fa fa-external-link-alt"></i></span>。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>lshort</tag>
      </tags>
  </entry>
  <entry>
    <title>lshort笔记：特色工具和功能（六）</title>
    <url>/2025/02/05/notes-reading/computer/lshort/lshort-06-features-tools/</url>
    <content><![CDATA[<blockquote>
<p>本文内容来自<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">一份
(不太) 简短的 LATEX2ε 介绍<i class="fa fa-external-link-alt"></i></span>。</p>
</blockquote>
<p>本文介绍一些特色的 <span class="math inline">\(\LaTeX{}\)</span>
辅助功能。前两个功能 BibTeX 和 makeindex
依靠一些辅助程序自动生成参考文献、索引等；后面使用颜色、超链接等则生成美观易用的电子文档。</p>
<span id="more"></span>
<hr />
<h2 id="参考文献和-textttbibtex-工具">参考文献和 <span
class="math inline">\(\texttt{BibTeX}\)</span> 工具</h2>
<h3 id="基本的参考文献和引用">基本的参考文献和引用</h3>
<p><span class="math inline">\(\LaTeX{}\)</span>
提供的参考文献和引用方式比较原始，需要用户自行书写参考文献列表（包括格式），因此较难直接使用。相关的命令只作最简单的介绍。</p>
<p><span class="math inline">\(\LaTeX{}\)</span> 提供了最基本的
<code>\cite</code> 命令用于在正文中引用参考文献：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\cite</span>&#123;&lt;citation&gt;&#125;</span><br></pre></td></tr></table></figure>
<p><code>&lt;citation&gt;</code> 为引用的参考文献的标签，类似
<code>\ref</code> 里的参数；<code>\cite</code>
带一个可选参数，为引用的编号后加上额外的内容，如
<code>\cite[page 22]{Paper2013}</code>可能得到形如
<code>[13, page 22]</code> 这样的引用。</p>
<p>参考文献由 <span
class="math inline">\(\texttt{thebibliography}\)</span>
环境包裹。每条参考文献由 <code>\bibitem</code>
开头，其后是参考文献本身的内容：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;thebibliography&#125;&#123;&lt;widest label&gt;&#125;</span><br><span class="line">  <span class="keyword">\bibitem</span>[&lt;item number&gt;&#125;&#123;&lt;citation&gt;&#125; ...</span><br><span class="line"><span class="keyword">\end</span>&#123;thebibliography&#125;</span><br></pre></td></tr></table></figure>
<p>其中 <code>&lt;citation&gt;</code> 是 <code>\cite</code>
使用的文献标签，<code>&lt;item number&gt;</code>
自定义参考文献的序号，如果省略，则按自然排序给定序号。<code>&lt;widest label&gt;</code>
用以限制参考文献序号的宽度，如 99
意味着不超过两位数字。通常设定为与参考文献的数目一致。</p>
<p>在 <span class="math inline">\(\texttt{article}\)</span>
文档类，<span class="math inline">\(\texttt{thebibliography}\)</span>
环境自动生成不带编号的一节，标题默认为<span
class="math inline">\(\texttt{“References”}\)</span>；而在 <span
class="math inline">\(\texttt{report}\)</span> 或 <span
class="math inline">\(\texttt{book}\)</span>
文档类中，则会生成不带编号的一章，标题默认为<span
class="math inline">\(\texttt{“Bibliography”}\)</span>。</p>
<p>以下为一个使用 <span
class="math inline">\(\texttt{thebibliography}\)</span>
排版参考文献的例子：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\documentclass</span>&#123;article&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line"><span class="keyword">\section</span>&#123;Introduction&#125;</span><br><span class="line">Partl~<span class="keyword">\cite</span>&#123;germenTeX&#125; has proposed that <span class="keyword">\ldots</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">\begin</span>&#123;thebibliography&#125;&#123;99&#125;</span><br><span class="line"><span class="keyword">\bibitem</span>&#123;germenTeX&#125; H.~Partl: <span class="keyword">\emph</span>&#123;German <span class="keyword">\TeX</span>&#125;,</span><br><span class="line">  TUGboat Volume~9, Issue~1 (1988)</span><br><span class="line"><span class="keyword">\end</span>&#123;thebibliography&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure>
<h3 id="textttbibtex-数据库"><span
class="math inline">\(\texttt{BibTeX}\)</span> 数据库</h3>
<p><span class="math inline">\(\texttt{BibTeX}\)</span>
是最为流行的参考文献数据组织格式之一。它的出现让我们摆脱手写参考文献条目的麻烦。还可以通过参考文献样式的支持，让同一份
<span class="math inline">\(\texttt{BibTeX}\)</span>
数据库生成不同样式的参考文献列表。</p>
<p><span class="math inline">\(\texttt{BibTeX}\)</span> 数据库以 <span
class="math inline">\(\texttt{.bib}\)</span>
作为扩展名，其内容是若干个文献条目，每个条目的格式为：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250126152050667.png" alt="image-20250126152050667" style="zoom:67%;" /></p>
<p>其中 <code>&lt;type&gt;</code> 为文献的类别，如 <span
class="math inline">\(\texttt{article}\)</span> 为学术论文，<span
class="math inline">\(\texttt{book}\)</span> 为书籍，<span
class="math inline">\(\texttt{incollection}\)</span>
为论文集中的某一篇，等等。<code>&lt;citation&gt;</code> 为
<code>\cite</code> 命令使用的文献标签。在<code>&lt;citation&gt;</code>
之后为条目里的各个字段，以 <code>&lt;key&gt; = &lt;value&gt;</code>
的形式组织。</p>
<p>在此简单列举学术论文里使用较多的 <span
class="math inline">\(\texttt{BibTeX}\)</span> 文献条目类别：</p>
<ul>
<li><span class="math inline">\(\texttt{article}\)</span>
<strong>学术论文</strong>，必需字段有 <span
class="math inline">\(\textrm{author}\)</span>, <span
class="math inline">\(\textrm{title}\)</span>, <span
class="math inline">\(\textrm{journal}\)</span>, <span
class="math inline">\(\textrm{year}\)</span>; 可选字段包括 <span
class="math inline">\(\textrm{volume}\)</span>, <span
class="math inline">\(\textrm{number}\)</span>, <span
class="math inline">\(\textrm{pages}\)</span>, <span
class="math inline">\(\textrm{doi}\)</span> 等；</li>
<li><span class="math inline">\(\texttt{book}\)</span>
<strong>书籍</strong>，必需字段有 <span
class="math inline">\(\textrm{author/editor}\)</span>, <span
class="math inline">\(\textrm{title}\)</span>, <span
class="math inline">\(\textrm{publisher}\)</span>, <span
class="math inline">\(\textrm{publisher}\)</span>; 可选字段包括 <span
class="math inline">\(\textrm{volume/number}\)</span>, <span
class="math inline">\(\textrm{series}\)</span>, <span
class="math inline">\(\textrm{address}\)</span> 等；</li>
<li><span class="math inline">\(\texttt{incollection}\)</span>
<strong>论文集中的一篇</strong>，必需字段有 <span
class="math inline">\(\textrm{author}\)</span>, <span
class="math inline">\(\textrm{title}\)</span>, <span
class="math inline">\(\textrm{booktitle}\)</span>, <span
class="math inline">\(\textrm{publisher}\)</span>, <span
class="math inline">\(\textrm{publisher}\)</span>; 可选字段包括 <span
class="math inline">\(\textrm{editor}\)</span>, <span
class="math inline">\(\textrm{volume/number}\)</span>, <span
class="math inline">\(\textrm{chapter}\)</span>, <span
class="math inline">\(\textrm{pages}\)</span>, <span
class="math inline">\(\textrm{address}\)</span> 等；</li>
<li><span class="math inline">\(\texttt{inbook}\)</span>
<strong>书中的一章</strong>，必需字段有 <span
class="math inline">\(\textrm{author/editor}\)</span>, <span
class="math inline">\(\textrm{title}\)</span>, <span
class="math inline">\(\textrm{chapter/pages}\)</span>, <span
class="math inline">\(\textrm{publisher}\)</span>, <span
class="math inline">\(\textrm{year}\)</span>; 可选字段包括 <span
class="math inline">\(\textrm{volume/number}\)</span>, <span
class="math inline">\(\textrm{series}\)</span>, <span
class="math inline">\(\textrm{address}\)</span> 等。</li>
</ul>
<p>例如 <span class="math inline">\(\texttt{article}\)</span>
类别的参考文献数据条目写法如下：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">@article&#123;Alice13,</span><br><span class="line">  title = &#123;Demostration of bibliography items&#125;,</span><br><span class="line">  author = &#123;Alice Axford and Bob Birkin and Charlie Copper and Danny Dannford&#125;,</span><br><span class="line">  year = &#123;2013&#125;,</span><br><span class="line">  month = &#123;Mar&#125;,</span><br><span class="line">  journal = &#123;Journal of <span class="keyword">\TeX</span> perts&#125;,</span><br><span class="line">  volume = &#123;36&#125;,</span><br><span class="line">  number = &#123;7&#125;,</span><br><span class="line">  pages = &#123;114-120&#125;&#125;</span><br></pre></td></tr></table></figure>
<p>多数时候，我们无需自己手写 <span
class="math inline">\(\texttt{BibTeX}\)</span> 文献条目。从 Google
Scholar 或者期刊/数据库的网站上都能够导出 <span
class="math inline">\(\texttt{BibTeX}\)</span>文献条目，老牌的文献管理软件
EndNote 也支持生成 <span class="math inline">\(\texttt{BibTeX}\)</span>
格式的数据库。开源软件 JabRef 甚至支持 <span
class="math inline">\(\texttt{BibTeX}\)</span>
文献条目的导入、导出和管理。下面是<strong>从谷歌学术镜像获取bib</strong>的步骤：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250422160552215.png" alt="image-20250422160552215" style="zoom:50%;" /></p>
<h3 id="textttbibtex-样式"><span
class="math inline">\(\texttt{BibTeX}\)</span> 样式</h3>
<p>参考文献的写法在不同文献里千差万别，包括作者、标题、年份等各项的顺序和字体样式、文献在列表中的排序规则等。<span
class="math inline">\(\texttt{BibTeX}\)</span> 用样式（<span
class="math inline">\(\texttt{style}\)</span>）来管理参考文献的写法。<span
class="math inline">\(\texttt{BibTeX}\)</span>
提供了几个预定义的样式，如 <span
class="math inline">\(\texttt{plain}\)</span>, <span
class="math inline">\(\texttt{unsrt}\)</span>, <span
class="math inline">\(\texttt{alpha}\)</span>
等。如果使用期刊模板的话，可能会提供自用的样式。样式文件以 <span
class="math inline">\(\texttt{.bst}\)</span> 为扩展名。
使用样式文件的方法是在源代码内（一般在导言区）使用<code>\bibliographystyle</code>
命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\bibliographystyle</span>&#123;&lt;bst-name&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>这里 <code>&lt;bst-name&gt;</code> 为 <span
class="math inline">\(\texttt{.bst}\)</span>
样式文件的名称，<strong>不要带 <span
class="math inline">\(\texttt{.bst}\)</span> 扩展名</strong>。</p>
<p>以 上一小节给出的数据条目为例，使用 <code>\bibliographystyle</code>
命令选择不同的参考文献样式，效果大致如下表所示。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250127144405131.png" alt="image-20250127144405131" style="zoom:67%;" /></p>
<h3 id="使用-textttbibtex-排版参考文献">使用 <span
class="math inline">\(\texttt{BibTeX}\)</span> 排版参考文献</h3>
<p>现在我们来看如何利用 <span
class="math inline">\(\texttt{BibTeX}\)</span>
数据库生成参考文献和引用。</p>
<p>第一步：准备一份 <span class="math inline">\(\texttt{BibTeX}\)</span>
数据库，假设数据库文件名为 <span
class="math inline">\(\texttt{books.bib}\)</span>，和 <span
class="math inline">\(\LaTeX{}\)</span>
源代码<strong>一般位于同一个目录下</strong>。</p>
<p>第二步：在源代码中添加必要的命令。假设源代码名为 <span
class="math inline">\(\texttt{demo.tex}\)</span>（见下面的源代码）。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\documentclass</span>&#123;article&#125;</span><br><span class="line"><span class="keyword">\bibliographystyle</span>&#123;plain&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line"><span class="keyword">\section</span>&#123;Some words&#125;</span><br><span class="line">Some excellent books, for example, <span class="keyword">\cite</span>&#123;citation1&#125; and <span class="keyword">\cite</span>&#123;citation2&#125; <span class="keyword">\ldots</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">\bibliography</span>&#123;books&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure>
<ol type="1">
<li><p>首先需要使用命令 <code>\bibliographystyle</code>
设定参考文献的格式。</p></li>
<li><p>其次，在正文中引用参考文献。<span
class="math inline">\(\texttt{BibTeX}\)</span>
程序在生成参考文献列表的时候，通常只列出用了 <code>\cite</code>
命令引用的那些。如果需要列出未被引用的文献，则需要<code>\nocite{&lt;citation&gt;}</code>
命令；而 <code>\nocite{*}</code> 则让所有未被引用的文献都列出。</p></li>
<li><p>再次，在需要列出参考文献的位置，使用<code>\bibliography</code>
命令代替 <span class="math inline">\(\texttt{thebibliography}\)</span>
环境：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\bibliography</span>&#123;&lt;bib-name&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>其中<code>&lt;bib-name&gt;</code> 是 <span
class="math inline">\(\texttt{BibTeX}\)</span>
数据库的文件名，<strong>不要带 <span
class="math inline">\(\texttt{.bib}\)</span> 扩展名</strong>。</p></li>
</ol>
<p>注意：<code>\bibliographystyle</code> 和 <code>\bibliography</code>
命令缺一不可，没有这两个命令，使用 <span
class="math inline">\(\texttt{BibTeX}\)</span>
生成参考文献列表的时候会报错。</p>
<p>第三步：写好了以上两个文件之后，就可以开始编译了。</p>
<ol type="1">
<li>首先使用 <span class="math inline">\(\texttt{pdflatex}\)</span> 或
<span class="math inline">\(\texttt{xelatex}\)</span> 等命令编译 <span
class="math inline">\(\LaTeX{}\)</span> 源代码 <span
class="math inline">\(\texttt{demo.tex}\)</span>；</li>
<li>接下来用 <span class="math inline">\(\texttt{bibtex}\)</span>
命令处理 <span class="math inline">\(\texttt{demo.aux}\)</span>
辅助文件记录的参考文献格式、引用条目等信息。<span
class="math inline">\(\texttt{bibtex}\)</span> 命令处理完毕后会生成
<span class="math inline">\(\texttt{demo.bbl}\)</span>
文件，内容就是一个 <code>\thebibliography</code> 环境；</li>
<li>再使用 <span class="math inline">\(\texttt{pdflatex}\)</span> 或
<span class="math inline">\(\texttt{xelatex}\)</span> 等命令把源代码
<span class="math inline">\(\texttt{demo.tex}\)</span>
编译<strong>两遍</strong>，读入参考文献并正确生成引用。</li>
</ol>
<p>整个过程使用的命令如下（可以略去扩展名）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">xelatex demo</span><br><span class="line">bibtex demo</span><br><span class="line">xelatex demo</span><br><span class="line">xelatex demo</span><br></pre></td></tr></table></figure>
<p>使用 <span class="math inline">\(\texttt{latex + dvipdfmx}\)</span>
命令编译时，则 <span class="math inline">\(\texttt{dvipdfmx}\)</span>
命令放在最后，相当于先后使用<span
class="math inline">\(\texttt{latex}\)</span>, <span
class="math inline">\(\texttt{bibtex}\)</span>, <span
class="math inline">\(\texttt{latex}\)</span>, <span
class="math inline">\(\texttt{latex}\)</span>, <span
class="math inline">\(\texttt{dvipdfmx}\)</span>。</p>
<h3 id="textttnatbib-宏包"><span
class="math inline">\(\texttt{natbib}\)</span> 宏包</h3>
<p>时下许多学术期刊比较喜欢使用人名——年份的引用方式，形如(<span
class="math inline">\({Axford~et~al.},
2013\)</span>)。<code>natbib</code>
宏包提供了对这种“自然”引用方式的处理。</p>
<p>除了 <code>\cite</code> 之外，<code>natbib</code>
宏包在正文中支持两种引用方式：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\citep</span>&#123;&lt;citation&gt;&#125; </span><br><span class="line"><span class="keyword">\citet</span>&#123;&lt;citation&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>它们分别生成形如(<span class="math inline">\(Axford~et~al.,
2013\)</span>) 和 <span class="math inline">\(Axford~et~al.
(2013)\)</span> 的人名——年份引用。正确排版人名——年份引用还依赖于特定的
<span class="math inline">\(\texttt{BibTeX}\)</span>
样式。<code>natbib</code> 提供了与 <span
class="math inline">\(\LaTeX{}\)</span> 预定义样式相对应的几个样式，包括
<span class="math inline">\(\texttt{plainnat}\)</span>、<span
class="math inline">\(\texttt{abbrvnat}\)</span> 和 <span
class="math inline">\(\texttt{unsrtnat}\)</span>。学术论文模板是否支持
<code>natbib</code>，需要参考其帮助文档。</p>
<p><code>natbib</code>
宏包同样也支持数字引用，并且支持将引用的序号压缩，例如：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\usepackage</span>[numbers,sort<span class="built_in">&amp;</span>compress]&#123;natbib&#125;</span><br></pre></td></tr></table></figure>
<p>调用 <code>natbib</code>
宏包时指定以上选项后，连续引用多篇文献时，会生成形如 <span
class="math inline">\((3-7)\)</span> 的引用而不是 <span
class="math inline">\((3, 4, 5, 6, 7)\)</span>。</p>
<p><code>natbib</code>
宏包还有更多选项和用法，比如默认的引用是用小括号包裹的，可指定 <span
class="math inline">\(\texttt{square}\)</span> 选项改为中括号；再比如
<code>\citep</code>
命令也支持可选参数，为引用前后都添加额外内容。这里不再赘述，请参考
<code>natbib</code> 宏包的帮助文档。</p>
<h3 id="textttbiblatex-宏包"><span
class="math inline">\(\texttt{biblatex}\)</span> 宏包</h3>
<p>本节的末尾简单介绍一下基于 <code>biblatex</code>
宏包排版参考文献的方式。<code>biblatex</code> 宏包是一套基于 <span
class="math inline">\(\LaTeX{}\)</span>
宏命令的参考文献解决方案，提供了便捷的格式控制和强大的排序、分类、筛选、多文献表等功能。<code>biblatex</code>
宏包也因其对 UTF-8 和中文参考文献的良好支持，被国内较多 <span
class="math inline">\(\LaTeX{}\)</span> 模板采用。</p>
<p>基于 <code>biblatex</code> 宏包的方式与基于 <span
class="math inline">\(\texttt{BibTeX}\)</span>
的传统方式有一定区别，下面从文档结构和命令、编译方式、样式选择等方面逐一介绍：</p>
<h4 id="文档结构和-biblatex-相关命令">文档结构和 <code>biblatex</code>
相关命令</h4>
<ol type="1">
<li>首先是在导言区调用 <code>biblatex</code> 宏包。宏包支持以
<code>&lt;key&gt;=&lt;value&gt;</code> 形式指定选项，包括参考文献样式
style、参考文献著录排序的规则 sorting 等。</li>
<li>接着在导言区使用 <code>\addbibresource</code> 命令为
<code>biblatex</code> 引入参考文献数据库。与基于 <span
class="math inline">\(\texttt{BibTeX}\)</span>
的传统方式不同的是，这里需要写完整的文件名。</li>
<li>在正文中使用 <code>\cite</code>
命令引用参考文献。除此之外还可以使用丰富的命令达到不同的引用效果，如
<code>\citeauthor</code> 和 <code>\citeyear</code>
分别单独引用作者和年份，<code>\textcite</code> 和
<code>\parencite</code> 分别类似 <code>natbib</code> 宏包提供的
<code>\citet</code> 和 <code>\citep</code> 命令，以及脚注式引用
<code>\footcite</code> 等。</li>
<li>最后在需要排版参考文献的位置使用命令
<code>\printbibliography</code>。</li>
</ol>
<h4 id="编译方式">编译方式</h4>
<p>与基于 <span class="math inline">\(\texttt{BibTeX}\)</span>
的传统方式不同的是，<code>biblatex</code> 宏包使用 <span
class="math inline">\(\texttt{biber}\)</span>
程序处理参考文献。因此上述文档的编译步骤为：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">xelatex demo</span><br><span class="line">biber demo</span><br><span class="line">xelatex demo</span><br><span class="line">xelatex demo</span><br></pre></td></tr></table></figure>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% File: egbibdata.bib</span></span><br><span class="line">@book&#123;caimin2006,</span><br><span class="line">  title      = &#123;UML基础和Rose建模教程&#125;,</span><br><span class="line">  address    = &#123;北京&#125;,</span><br><span class="line">  author     = &#123;蔡敏 and 徐慧慧 and 黄柄强&#125;,</span><br><span class="line">  publisher  = &#123;人民邮电出版社&#125;,</span><br><span class="line">  year       = &#123;2006&#125;,</span><br><span class="line">  month      = &#123;1&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">% File: demo.tex</span></span><br><span class="line"><span class="keyword">\documentclass</span>&#123;ctexart&#125;</span><br><span class="line"><span class="comment">% 使用符合 GB/T 7714-2015 规范的参考文献样式</span></span><br><span class="line"><span class="keyword">\usepackage</span>[style=gb7714-2015]&#123;biblatex&#125;</span><br><span class="line"><span class="comment">% 注意加 .bib 扩展名</span></span><br><span class="line"><span class="keyword">\addbibresource</span>&#123;egbibdata.bib&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line"></span><br><span class="line">见文献<span class="keyword">\cite</span>&#123;caimin2006&#125;。</span><br><span class="line"></span><br><span class="line"><span class="keyword">\printbibliography</span></span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure>
<h4 id="biblatex-样式和其它选项"><code>biblatex</code>
样式和其它选项</h4>
<p><code>biblatex</code> 使用的参考文献样式分为著录样式（<span
class="math inline">\(\texttt{bibliography
style}\)</span>）和引用样式（<span
class="math inline">\(\texttt{citation style}\)</span>），分别以 <span
class="math inline">\(\texttt{.bbx}\)</span> 和 <span
class="math inline">\(\texttt{.cbx}\)</span>
为扩展名。参考文献的样式在调用宏包时使用 <span
class="math inline">\(\texttt{style}\)</span> 选项指定，或者使用 <span
class="math inline">\(\texttt{bibstyle}\)</span> 或 <span
class="math inline">\(\texttt{citestyle}\)</span> 分别指定：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% 同时调用 gb7714-2015.bbx 和 gb7714-2015.cbx</span></span><br><span class="line"><span class="keyword">\usepackage</span>[style=gb7714-2015]&#123;biblatex&#125;</span><br><span class="line"><span class="comment">% 著录样式调用 gb7714-2015.bbx，引用样式调用 biblatex 宏包自带的 authoryear</span></span><br><span class="line"><span class="keyword">\usepackage</span>[bibstyle=gb7714-2015,citestyle=authoryear]&#123;biblatex&#125;</span><br></pre></td></tr></table></figure>
<p>以下总结一些常用的参考文献样式，除 <code>biblatex</code>
宏包自带的样式外，许多样式以单独的宏包在发行版内发布。</p>
<ul>
<li><span class="math inline">\(\texttt{authoryear}\)</span>
<code>biblatex</code>自带样式，类似 <code>natbib</code>
默认的引用样式效果。</li>
<li><span class="math inline">\(\texttt{authortitle}\)</span>
<code>biblatex</code>自带样式，采用作者-题名（shorttitle
字段）的引用样式。</li>
<li><span class="math inline">\(\texttt{verbose}\)</span>
<code>biblatex</code>
自带样式，引用样式中包含作者、题名、书名、页码等字段的信息。</li>
<li><span class="math inline">\(\texttt{alphabetic}\)</span>
<code>biblatex</code>自带样式，著录样式与 <span
class="math inline">\(\texttt{BibTeX}\)</span> 的 <span
class="math inline">\(\texttt{alpha}\)</span> 样式类似。</li>
<li><span class="math inline">\(\texttt{trad-alpha}\)</span>
<code>biblatex-trad</code> 样式包，移植自 <span
class="math inline">\(\texttt{BibTeX}\)</span> 默认的 <span
class="math inline">\(\texttt{alpha}\)</span> 样式。另外还包括 <span
class="math inline">\(\texttt{trad-abbrv}\)</span>、<span
class="math inline">\(\texttt{trad-plain}\)</span> 和 $。</li>
<li><span class="math inline">\(\texttt{gb7714-2015}\)</span>
符合中文文献著录标准 GB/T 7714—2015
的样式，著录按顺序编码排版。另外还包括按作者—年份顺序排版著录的样式
<span class="math inline">\(\texttt{gb7714-2015ay}\)</span>。</li>
<li><span class="math inline">\(\texttt{caspervector}\)</span>
以中文文献著录标准 GB/T 7714—2015 为基础的一个样式。</li>
<li><span class="math inline">\(\texttt{ieee}\)</span> 兼容
<code>IEEEtran</code>
风格的样式，著录按顺序编码排版。另外还包括按作者—年份顺序排版著录的样式
<span class="math inline">\(\texttt{ieee-alphabetic}\)</span>。</li>
</ul>
<hr />
<h2 id="索引和-textrmmakeindex-工具">索引和 <span
class="math inline">\(\textrm{makeindex}\)</span> 工具</h2>
<p>书籍和大文档通常用索引来归纳关键词，方便用户查阅。<span
class="math inline">\(\LaTeX{}\)</span> 借助配套的 <span
class="math inline">\(\textrm{makeindex}\)</span>
程序完成对索引的排版。</p>
<h3 id="使用-textrmmakeindex-工具的方法">使用 <span
class="math inline">\(\textrm{makeindex}\)</span> 工具的方法</h3>
<p>要使用索引，须经过这么几个步骤（仍设源代码名为 <span
class="math inline">\(\texttt{demo.tex}\)</span>）：</p>
<p>第一步，在 <span class="math inline">\(\LaTeX{}\)</span>
源代码的导言区调用 <code>makeidx</code> 宏包，并使用
<code>\makeindex</code> 命令开启索引的收集：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\usepackage</span>&#123;makeidx&#125;</span><br><span class="line"><span class="keyword">\makeindex</span></span><br></pre></td></tr></table></figure>
<p>第二步，在正文中需要索引的地方使用 <code>\index</code>
命令。在需要输出索引的地方（如所有章节之后）使用
<code>\printindex</code> 命令。</p>
<p>第三步，编译过程：</p>
<ol type="1">
<li>首先用 <span class="math inline">\(\texttt{xelatex}\)</span>
等命令编译源代码 <span
class="math inline">\(\texttt{demo.tex}\)</span>。编译过程中产生索引记录文件
<span class="math inline">\(\texttt{demo.idx}\)</span>；</li>
<li>用 <span class="math inline">\(\texttt{makeindex}\)</span> 程序处理
<span
class="math inline">\(\texttt{demo.idx}\)</span>，生成用于排版的索引列表文件
<span class="math inline">\(\texttt{demo.ind}\)</span>；</li>
<li>再次编译源代码 <span
class="math inline">\(\texttt{demo.tex}\)</span>，正确生成索引列表。</li>
</ol>
<h3 id="索引项的写法">索引项的写法</h3>
<p>添加索引项的命令为：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\index</span>&#123;&lt;index entry&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>其中 <code>&lt;index entry&gt;</code> 为索引项，写法由 下表
汇总。其中 <span class="math inline">\(\texttt!\)</span>、<span
class="math inline">\(\texttt @\)</span> 和 <span
class="math inline">\(\texttt|\)</span>
为特殊符号，如果要向索引项直接输出这些符号，需要加前缀 <span
class="math inline">\(\texttt&quot;\)</span>；而 <span
class="math inline">\(\texttt&quot;\)</span> 需要输入两个引号 <span
class="math inline">\(\texttt{&quot;&quot;}\)</span>
才能输出到索引项。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250127204208420.png" alt="image-20250127204208420" style="zoom:67%;" /></p>
<p>读者可以钻研一下以下给出的一个较为复杂的，结合多级索引、索引格式、页码格式等的用法示例。但在自己使用时，最好还是遵循“简单的就是最好的”原则，尽量使用
上表 中的写法。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">Test index.</span><br><span class="line"><span class="keyword">\index</span>&#123;Test@<span class="keyword">\textsf</span>&#123;&quot;&quot;Test&#125;|(textbf&#125;</span><br><span class="line"><span class="keyword">\index</span>&#123;Test@<span class="keyword">\textsf</span>&#123;&quot;&quot;Test&#125;!sub@&quot;|sub&quot;||see&#123;Test&#125;&#125;</span><br><span class="line"><span class="keyword">\newpage</span></span><br><span class="line">Test index.</span><br><span class="line"><span class="keyword">\index</span>&#123;Test@<span class="keyword">\textsf</span>&#123;&quot;&quot;Test&#125;|)textbf&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="使用颜色">使用颜色</h2>
<p>原始的 <span class="math inline">\(\LaTeX{}\)</span>
不支持使用各种颜色。<code>color</code> 宏包或者 <code>xcolor</code>
宏包提供了对颜色的支持，给 PDF 输出生成颜色的特殊指令。</p>
<h3 id="颜色的表达方式">颜色的表达方式</h3>
<p>调用 <code>color</code> 或 <code>xcolor</code>
宏包后，我们就可以用如下命令切换颜色：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\color</span>[&lt;color-mode&gt;]&#123;&lt;code&gt;&#125;</span><br><span class="line"><span class="keyword">\color</span>&#123;&lt;color-name&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>颜色的表达方式有两种，其一是使用色彩模型和色彩代码，代码用 <span
class="math inline">\(0\sim1\)</span>
的数字代表成分的比例。<code>color</code> 宏包支持 <span
class="math inline">\(\texttt{rgb}\)</span>、<span
class="math inline">\(\texttt{cmyk}\)</span> 和 <span
class="math inline">\(\texttt{gray}\)</span> 模型，<code>xcolor</code>
支持更多的模型如 <span class="math inline">\(\texttt{hsb}\)</span>
等。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250127205538936.png" alt="image-20250127205538936" style="zoom:67%;" /></p>
<p>其二是直接使用名称代表颜色，前提是已经定义了颜色名称（没定义的话会报错）：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250127205710585.png" alt="image-20250127205710585" style="zoom:67%;" /></p>
<p><code>color</code> 宏包仅定义了 8 种颜色名称，<code>xcolor</code>
补充了一些，总共有 19 种，见下表。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250127205821233.png" alt="image-20250127205821233" style="zoom:67%;" /></p>
<p><code>xcolor</code> 还支持将颜色通过表达式混合或互补：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250127205941911.png" alt="image-20250127205941911" style="zoom:67%;" /></p>
<p>我们还可以通过命令自定义颜色名称，注意这里的 <code>color-mode</code>
是必选参数：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\definecolor</span>&#123;&lt;color-name&gt;&#125;&#123;&lt;color-mode&gt;&#125;&#123;&lt;code&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>如果调用 <code>color</code> 或 <code>xcolor</code> 宏包时指定 <span
class="math inline">\(\texttt{dvipsnames}\)</span> 选项，就有额外的 68
种颜色名称可用。<code>xcolor</code>
宏包还支持通过指定其它选项载入更多颜色名称。限于篇幅不展开介绍，详情请参考
<code>xcolor</code> 宏包的手册。</p>
<h3 id="带颜色的文本和盒子">带颜色的文本和盒子</h3>
<p>原始的 <code>\color</code> 命令类似于字体命令
<code>\bfseries</code>，它使之后排版的内容全部变成指定的颜色，所以直接使用时通常要加花括号分组。<code>color</code>
和 <code>xcolor</code> 宏包都定义了一些方便用户使用的带颜色元素。</p>
<p>输入带颜色的文本可以用类似 <code>\textbf</code> 的命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\textcolor</span>[&lt;color-mode&gt;]&#123;&lt;code&gt;&#125;&#123;&lt;text&gt;&#125;</span><br><span class="line"><span class="keyword">\textcolor</span>&#123;color-name&#125;&#123;&lt;text&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>以下命令构造一个带背景色的盒子，<code>&lt;material&gt;</code>
为盒子中的内容：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\colorbox</span>[&lt;color-mode&gt;]&#123;&lt;code&gt;&#125;&#123;&lt;material&gt;&#125;</span><br><span class="line"><span class="keyword">\colorbox</span>&#123;&lt;color-name&gt;&#125;&#123;&lt;material&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>以下命令构造一个带背景色和有色边框的盒子，<code>&lt;fcode&gt;</code>
或 <code>&lt;fcolor-name&gt;</code> 用于设置边框颜色：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\fcolorbox</span>[&lt;color-mode&gt;]&#123;&lt;fcode&gt;&#125;&#123;&lt;code&gt;&#125;&#123;&lt;material&gt;&#125; </span><br><span class="line"><span class="keyword">\fcolorbox</span>&#123;&lt;fcolor-name&gt;&#125;&#123;&lt;color-name&gt;&#125;&#123;&lt;material&gt;&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250127211405408.png" alt="image-20250127211405408" style="zoom:67%;" /></p>
<p><code>\fcolorbox</code> 也可以像 <code>\fbox</code> 那样调节
<code>\fboxrule</code> 和 <code>\fboxsep</code>；对于
<code>\colorbox</code>，调整 <code>\fboxsep</code> 是有效的。</p>
<hr />
<h2 id="使用超链接">使用超链接</h2>
<p><span class="math inline">\(\LaTeX{}\)</span> 中实现超链接功能的是
<code>hyperref</code> 宏包。</p>
<h3 id="hyperref-宏包"><code>hyperref</code> 宏包</h3>
<p><code>hyperref</code> 宏包涉及的链接遍布 <span
class="math inline">\(\LaTeX{}\)</span>
的每一个角落——目录、引用、脚注、索引、参考文献等等都被封装成超链接。但这也使得它与其它宏包发生冲突的可能性大大增加，虽然宏包已经尽力解决各方面的兼容性，但仍不能面面俱到。为减少可能的冲突，习惯上将
<code>hyperref</code> 宏包<strong>放在其它宏包之后调用</strong>。</p>
<p>与 <code>graphicx</code> 宏包类似，<span
class="math inline">\(\texttt{latex} + \texttt{dvipdfmx}\)</span>
命令下调用 <code>hyperref</code> 宏包时，需要指定选项 <span
class="math inline">\(\texttt{dvipdfmx}\)</span>；而 <span
class="math inline">\(\texttt{pdflatex}\)</span> 或 <span
class="math inline">\(\texttt{xelatex}\)</span> 命令下不需要。</p>
<p><code>hyperref</code> 宏包提供了命令 <code>\hypersetup</code>
配置各种参数。参数也可以作为宏包选项，在调用宏包时指定：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\hypersetup</span>&#123;&lt;option1&gt;,&lt;option2&gt;=&lt;value&gt;,...&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>[&lt;option1&gt;,&lt;option2&gt;=&lt;value&gt;,...]&#123;hyperref&#125;</span><br></pre></td></tr></table></figure>
<p>当选项值为 <span class="math inline">\(\texttt{true}\)</span>
时，可以省略“<span
class="math inline">\(\texttt{=true}\)</span>”不写。可用的参数见
下表。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250128001606010.png" alt="image-20250128001606010" style="zoom:67%;" /></p>
<h3 id="超链接">超链接</h3>
<p><code>hyperref</code> 宏包提供了直接书写超链接的命令，用于在 PDF
中生成 URL：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\url</span>&#123;<span class="link">&lt;url&gt;</span>&#125; </span><br><span class="line"><span class="keyword">\nolinkurl</span>&#123;&lt;url&gt;&#125;</span><br></pre></td></tr></table></figure>
<p><code>\url</code> 和 <code>\nolinkurl</code> 都像抄录命令
<code>\verb</code> 一样输出一个 URL，区别是前者还为 URL
加上了超链接，后者没有。在 <code>\url</code> 等命令的参数
<code>&lt;url&gt;</code> 里，可直接输入如
<code>%</code>、<code>&amp;</code> 这样的特殊符号。</p>
<p>也可以像 HTML 中的超链接一样，把一段文字作为超链接：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\href</span>&#123;<span class="link">url</span>&#125;&#123;text&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250128002508111.png" alt="image-20250128002508111" style="zoom:67%;" /></p>
<p>使用 <code>hyperref</code>
宏包后，文档中所有的引用、参考文献、索引等等都转换为超链接。用户也可对某个
<code>\label</code> 命令定义的标签 <code>&lt;label&gt;</code>
作超链接（注意这里的 <code>&lt;label&gt;</code>
虽然是可选参数的形式，但通常是<strong>必填的</strong>：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\hyperref</span>[&lt;label&gt;]&#123;&lt;text&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>默认的超链接在文字外边加上一个带颜色的边框（在打印 PDF
时边框不会打印），可指定 <span
class="math inline">\(\texttt{colorlinks}\)</span>
参数修改为将文字本身加上颜色，或修改 <span
class="math inline">\(\texttt{pdfborder}\)</span>
参数调整边框宽度以“去掉”边框；<span
class="math inline">\(\texttt{hidelinks}\)</span>
参数则令超链接既不变色也不加边框。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\hypersetup</span>&#123;hidelinks&#125;</span><br><span class="line"><span class="comment">% or:</span></span><br><span class="line"><span class="keyword">\hypersetup</span>&#123;pdfborder=&#123;0 0 0&#125;&#125;</span><br></pre></td></tr></table></figure>
<h3 id="pdf-书签">PDF 书签</h3>
<p><code>hyperref</code> 宏包另一个强大的功能是为 PDF
生成书签。对于章节命令 <code>\chapter</code>、<code>\section</code>
等，默认情况下会为 PDF
自动生成书签。和交叉引用、索引等类似，生成书签也需要多次编译源代码，第一次编译将书签记录写入
<span class="math inline">\(\texttt{.out}\)</span>
文件，第二次编译才正确生成书签。</p>
<p>书签的一些属性见 上表。使用 <code>CJK</code>
宏包时，为了防止中文书签出现乱码，需要进行繁琐的设置；但在使用
<code>ctex</code> 宏包和文档类、且使用 <span
class="math inline">\(\texttt{xelatex}\)</span> 或 <span
class="math inline">\(\texttt{lualatex}\)</span>
编译的情况下，无需用户额外干预，即可正确生成中文书签。</p>
<p><code>hyperref</code> 还提供了手动生成书签的命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\pdfbookmark</span>[&lt;level&gt;]&#123;&lt;bookmark&gt;&#125;&#123;&lt;anchor&gt;&#125;</span><br></pre></td></tr></table></figure>
<p><code>&lt;bookmark&gt;</code> 为书签名称，<code>&lt;anchor&gt;</code>
为书签项使用的锚点（类似交叉引用的标签）。可选参数<code>&lt;level&gt;</code>为书签的层级，默认为
0。</p>
<p>章节命令里往往有 <span class="math inline">\(\LaTeX{}\)</span>
命令甚至数学公式，而 PDF
书签是纯文本，对命令和公式的处理很困难，有出错的风险。<code>hyperref</code>
宏包已经为我们处理了许多常见命令，如 <code>\LaTeX</code> 和字体命令
<code>\textbf</code>
等，对于未被处理的命令或数学公式，就要在章节标题中使用如下命令，分别提供
<span class="math inline">\(\LaTeX{}\)</span> 代码和 PDF
书签可用的纯文本：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\texorpdfstring</span>&#123;&lt;LaTeX code&gt;&#125;&#123;&lt;PDF bookmark text&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>比如在章节名称里使用公式 <span
class="math inline">\(E=mc^2\)</span>，而书签则使用纯文本形式的
E=mc^2：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\section</span>&#123;质能公式 <span class="keyword">\texorpdfstring</span>&#123;<span class="built_in">$</span>E=mc<span class="built_in">^</span>2<span class="built_in">$</span>&#125;&#123;E=mc<span class="keyword">\textasciicircum</span> 2&#125;&#125;</span><br></pre></td></tr></table></figure>
<h3 id="pdf-文档属性">PDF 文档属性</h3>
<p><code>hyperref</code> 宏包还提供了一些参数用于改变 PDF
文档的属性，部分见 上表。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>lshort</tag>
      </tags>
  </entry>
  <entry>
    <title>lshort笔记：绘图功能（七）</title>
    <url>/2025/02/11/notes-reading/computer/lshort/lshort-07-graphic/</url>
    <content><![CDATA[<blockquote>
<p>本文内容来自<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">一份
(不太) 简短的 LATEX2ε 介绍<i class="fa fa-external-link-alt"></i></span>。</p>
</blockquote>
<p>除了排版文字，<span class="math inline">\(\LaTeX{}\)</span>
也支持用代码表示图形。不同的扩展已极大丰富了 <span
class="math inline">\(\LaTeX{}\)</span> 的图形功能，<code>TikZ</code>
就是其中之一。本章将带你了解一些基本的绘图功能。</p>
<span id="more"></span>
<hr />
<h2 id="绘图语言简介">绘图语言简介</h2>
<p><span class="math inline">\(\LaTeX{}\)</span> 提供了原始的 <span
class="math inline">\(\texttt{picture}\)</span>
环境，能够绘制一些基本的图形如点、线、矩形、圆、B'ezier
曲线等等，不过受制于 <span class="math inline">\(\LaTeX{}\)</span>
本身，它的绘图功能极为有限，效果也不够美观。</p>
<p>当前较为流行的、用于 <span class="math inline">\(\LaTeX{}\)</span>
的绘图宏包/程序主要有：</p>
<ul>
<li><p><span class="math inline">\(\textrm{PSTricks}\)</span></p>
<p>以 <span class="math inline">\(\textrm{PostSciprt}\)</span>
语法为基础的绘图宏包，具有优秀的绘图能力。它对老式的 <span
class="math inline">\(\texttt{latex + dvips}\)</span>
编译命令支持最好，而现在的几种编译命令下使用起来都不够方便。</p></li>
<li><p><span class="math inline">\(\textrm{TikZ}\)</span> <span
class="math inline">\(\&amp;\)</span> <span
class="math inline">\(\textrm{pgf}\)</span></p>
<p>德国的 Till Tantau 教授在开发著名的 <span
class="math inline">\(\LaTeX{}\)</span> 幻灯片文档类 <span
class="math inline">\(\texttt{beamer}\)</span> 时一并开发了绘图宏包
<code>pgf</code>，目的是令其能够在 <span
class="math inline">\(\texttt{pdflatex}\)</span> 或 <span
class="math inline">\(\texttt{xelatex}\)</span>
等不同的编译命令下都能使用。<span
class="math inline">\(\textrm{TikZ}\)</span> 是在 <code>pgf</code>
基础上封装的一个宏包，采用了类似 <span
class="math inline">\(\texttt{METAPOST}\)</span>
的语法，提供了方便的绘图命令，绘图能力不输 <span
class="math inline">\(\textrm{PSTricks}\)</span> 。</p></li>
<li><p><span class="math inline">\(\textrm{METAPOST}\)</span> <span
class="math inline">\(\&amp;\)</span> <span
class="math inline">\(\textrm{Asymptote}\)</span></p>
<p><span class="math inline">\(\textrm{METAPOST}\)</span> 脱胎于高德纳为
<span class="math inline">\(\TeX{}\)</span> 配套开发的字体生成程序 <span
class="math inline">\(\textrm{METAFONT}\)</span>，具有优秀的绘图能力，并能够调用
<span class="math inline">\(\TeX{}\)</span>
引擎向图片中插入文字和公式。<span
class="math inline">\(\textrm{Asymptote}\)</span> 在 <span
class="math inline">\(\textrm{METAPOST}\)</span>
的基础上更进一步，具有一定的类似 C
语言的编程能力，支持三维图形的绘制。</p></li>
</ul>
<p>它们作为独立的程序，通常的用法是将代码写在单独的文件里，编译生成图片供
<span class="math inline">\(\LaTeX{}\)</span>
引用，也可以借助特殊的宏包在 <span
class="math inline">\(\LaTeX{}\)</span> 代码里直接使用。</p>
<hr />
<h2 id="textrmtikz-绘图语言"><span
class="math inline">\(\textrm{TikZ}\)</span> 绘图语言</h2>
<p>在导言区调用 <span class="math inline">\(\textrm{TikZ}\)</span>
宏包，就可以用以下命令和环境使用 <span
class="math inline">\(\textrm{TikZ}\)</span> 的绘图功能了（<span
class="math inline">\(\texttt{latex + dvipdfmx}\)</span> 编译方式要在
<span class="math inline">\(\textrm{TikZ}\)</span> 宏包之前调用
<code>graphicx</code> 宏包并指定 <span
class="math inline">\(\texttt{dvipdfmx}\)</span> 选项。）：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250128171123106.png" alt="image-20250128171123106" style="zoom:67%;" /></p>
<p>前一种用法为 <code>\tikz</code>
带单条绘图命令，以分号结束，一般用于在文字之间插入简单的图形；
后两种用法较为常见，使用多条绘图命令，可以在 <span
class="math inline">\(\texttt{figure}\)</span> 等浮动体中使用。</p>
<h3 id="textrmtikz-坐标和路径"><span
class="math inline">\(\textrm{TikZ}\)</span> 坐标和路径</h3>
<p><span class="math inline">\(\textrm{TikZ}\)</span>
用直角坐标系或者极坐标系描述点的位置。</p>
<ul>
<li>直角坐标下，点的位置写作 <code>(&lt;x&gt;,&lt;y&gt;)</code>，坐标
<code>&lt;x&gt;</code> 和 <code>&lt;y&gt;</code> 可以用 <span
class="math inline">\(\LaTeX{}\)</span> 支持的任意单位表示， 缺省为
<span class="math inline">\(\texttt{cm}\)</span>；</li>
<li>极坐标下，点的位置写作 <span
class="math inline">\((\theta:r)\)</span>。<span
class="math inline">\(\theta\)</span> 为极角，单位是度。</li>
</ul>
<p>还可以为某个点命名：<code>\coordinate (A) at (&lt;coordinate&gt;)</code>，然后就可以使用
<span class="math inline">\(\texttt{(A)}\)</span> 作为点的位置了。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span> (0,0) -- (30:1);</span><br><span class="line"><span class="keyword">\draw</span> (1,0) -- (2,1);</span><br><span class="line"><span class="keyword">\coordinate</span> (S) at (0,1);</span><br><span class="line"><span class="keyword">\draw</span> (S) -- (1,1);</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250128175453993.png" alt="image-20250128175453993" style="zoom:67%;" /></p>
<p>坐标的表示形式还包括“垂足”形式：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\coordinate</span> (S) at (2,2);</span><br><span class="line"><span class="keyword">\draw</span>[gray] (-1,2) -- (S);</span><br><span class="line"><span class="keyword">\draw</span>[gray] (2,-1) -- (S);</span><br><span class="line"><span class="keyword">\draw</span>[red] (0,0) -- (0,0 -| S);</span><br><span class="line"><span class="keyword">\draw</span>[blue] (0,0) -- (0,0 |- S);</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250128175922953.png" alt="image-20250128175922953" style="zoom:67%;" /></p>
<p><span class="math inline">\(\textrm{TikZ}\)</span>
最基本的路径为两点之间连线，如 <span class="math inline">\((x_1,y_1) --
(x_2,y_2)\)</span>，可以连用表示多个连线（折线）。连续使用连线时，可以使用
<span class="math inline">\(\texttt{cycle}\)</span>
令路径回到起点，生成闭合的路径。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span> (0,0) -- (1,1) -- (2,0) -- cycle;</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250128180317382.png" alt="image-20250128180317382" style="zoom:67%;" /></p>
<p><strong>多条路径</strong>可用于同一条画图命令中，以<strong>空格分隔</strong>：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span> (0,0) -- (0,1)</span><br><span class="line">      (1,0) -- (1,1) -- (2,0) -- cycle;</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250128180447083.png" alt="image-20250128180447083" style="zoom:67%;" /></p>
<p>其它常用的路径还包括：</p>
<ul>
<li><p>矩形、圆和椭圆：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span> (0,0) rectangle (1.5,1);</span><br><span class="line"><span class="keyword">\draw</span> (2.5,0.5) circle [radius=0.5];</span><br><span class="line"><span class="keyword">\draw</span> (4.5,0.5) ellipse</span><br><span class="line">    [x radius=1,y radius=0.5];</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250128180748916.png" alt="image-20250128180748916" style="zoom:67%;" /></p></li>
<li><p>直角、圆弧、椭圆弧</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span> (0,0) |- (1,1);</span><br><span class="line"><span class="keyword">\draw</span> (1,0) -| (2,1);</span><br><span class="line"><span class="keyword">\draw</span> (4,0) arc (0:135:1);</span><br><span class="line"><span class="keyword">\draw</span> (6,0) arc (0:135:1 and 0.5);</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250128180937724.png" alt="image-20250128180937724" style="zoom:67%;" /></p></li>
<li><p>正弦、余弦曲线（1/4 周期）：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span> (0,0) sin (1,1);</span><br><span class="line"><span class="keyword">\draw</span> (0,1) sin (1,0);</span><br><span class="line"><span class="keyword">\draw</span> (2,1) cos (3,0);</span><br><span class="line"><span class="keyword">\draw</span> (2,0) cos (3,1);</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250128181343493.png" alt="image-20250128181343493" style="zoom:67%;" /></p></li>
<li><p>抛物线，用 <span class="math inline">\(\texttt{bend}\)</span>
控制顶点：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span> (0,0) parabola (1,2);</span><br><span class="line"><span class="keyword">\draw</span> (2,0) parabola</span><br><span class="line">      bend (2.25,-0.25) (3,2);</span><br><span class="line"><span class="keyword">\draw</span> (4,0) parabola</span><br><span class="line">      bend (4.75,2.25) (5,2);</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250128181601221.png" alt="image-20250128181601221" style="zoom:67%;" /></p></li>
<li><p>二次和三次 B'ezier 曲线，分别使用一个和两个控制点：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span> (0,0) .. controls</span><br><span class="line">  (2,1) and (3,1) .. (3,0);</span><br><span class="line"><span class="keyword">\draw</span> (4,0) .. controls</span><br><span class="line">  (5,1) .. (5,0);</span><br><span class="line"><span class="keyword">\draw</span>[help lines] (0,0)</span><br><span class="line">  -- (2,1) -- (3,1) -- (3,0)</span><br><span class="line">  (4,0) -- (5,1) -- (5,0);</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250128181655169.png" alt="image-20250128181655169" style="zoom:67%;" /></p></li>
<li><p>网格、函数图像，网格可用 <span
class="math inline">\(\texttt{step}\)</span>
参数控制网格大小，函数图像用 <span
class="math inline">\(\texttt{domain}\)</span> 参数控制定义域：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span>[help lines,step=0.5]</span><br><span class="line">     (-1,-1) grid (1,1);</span><br><span class="line"><span class="keyword">\draw</span>[-&gt;] (-1.5,0) -- (1.5,0);</span><br><span class="line"><span class="keyword">\draw</span>[-&gt;] (0,-1.5) -- (0,1.5);</span><br><span class="line"><span class="keyword">\draw</span>[domain=-1:1]</span><br><span class="line">     plot(<span class="keyword">\x</span>,&#123;<span class="keyword">\x</span>*<span class="keyword">\x</span>*2 -1&#125;);</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250128181832752.png" alt="image-20250128181832752" style="zoom:67%;" /></p></li>
</ul>
<h3 id="textrmtikz-绘图命令和参数"><span
class="math inline">\(\textrm{TikZ}\)</span> 绘图命令和参数</h3>
<p>除了 <code>\draw</code> 命令之外，<span
class="math inline">\(\textrm{TikZ}\)</span> 还提供了 <code>\fill</code>
命令用来填充图形，<code>\filldraw</code>
命令则同时填充和描边。除了矩形、圆等现成的闭合图形外，<code>\fill</code>
和 <code>\filldraw</code> 命令也能够填充人为构造的闭合路径。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250128182617607.png" alt="image-20250128182617607" style="zoom:67%;" /></p>
<p>绘图参数可作为可选参数用在 <span
class="math inline">\(\texttt{tikzpicture}\)</span> 环境或
<code>\tikz</code>
命令时，参数会影响到所有具体的绘图命令；用在单个绘图命令
<code>\draw</code>、<code>\filldraw</code> 等时，只对这个命令起效。</p>
<p><span class="math inline">\(\textrm{TikZ}\)</span>
有数不清的绘图参数，这些参数令 <span
class="math inline">\(\textrm{TikZ}\)</span>
能够绘制丰富多彩的图像，同时也令其难以精通。以下示例常用的一些绘图参数。</p>
<ul>
<li><p><span
class="math inline">\(\texttt{color/draw/fill=&lt;color&gt;}\)</span> 为
<code>\draw</code> 或 <code>\fill</code> 等命令指定颜色。<span
class="math inline">\(\texttt{draw}\)</span> 和 <span
class="math inline">\(\texttt{fill}\)</span>
分别指定描边和填充的颜色，而 <span
class="math inline">\(\texttt{color}\)</span> 同时指定，也可以省略 <span
class="math inline">\(\texttt{color=}\)</span> 直接写颜色名称。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;[thick]</span><br><span class="line"><span class="keyword">\draw</span>[blue] (0,0) rectangle (1,1);</span><br><span class="line"><span class="keyword">\filldraw</span>[fill=yellow,draw=red]</span><br><span class="line">  (2,0.5) circle [radius=0.5];</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129092336978.png" alt="image-20250129092336978"  style="zoom:67%;" /></p></li>
<li><p><span
class="math inline">\(\texttt{thick=&lt;length&gt;/thin/semithick/...}\)</span>
指定线条的粗细。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span>[ultra thin] (0,0)--(0,2);</span><br><span class="line"><span class="keyword">\draw</span>[very thin] (0.5,0)--(0.5,2);</span><br><span class="line"><span class="keyword">\draw</span>[thin] (1,0)--(1,2);</span><br><span class="line"><span class="keyword">\draw</span>[semithick] (1.5,0)--(1.5,2);</span><br><span class="line"><span class="keyword">\draw</span>[thick] (2,0)--(2,2);</span><br><span class="line"><span class="keyword">\draw</span>[very thick] (2.5,0)--(2.5,2);</span><br><span class="line"><span class="keyword">\draw</span>[ultra thick] (3,0)--(3,2);</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129092537257.png" alt="image-20250129092537257" style="zoom:67%;" /></p></li>
<li><p><span class="math inline">\(\texttt{solid/dashed/dotted/dash
dot/dash dot dot}\)</span> 指定线条类型（实线、虚线、点划线等）。与
<span class="math inline">\(\texttt{dashed}\)</span> 对应地有 <span
class="math inline">\(\texttt{densely dashed}\)</span> 和 <span
class="math inline">\(\texttt{loosely
dashed}\)</span>，后三种类型同理。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span>[dashed] (0,0) -- (0,2);</span><br><span class="line"><span class="keyword">\draw</span>[dotted] (0.5,0) -- (0.5,2);</span><br><span class="line"><span class="keyword">\draw</span>[dash dot] (1,0) -- (1,2);</span><br><span class="line"><span class="keyword">\draw</span>[dash dot dot] (1.5,0) -- (1.5,2);</span><br><span class="line"><span class="keyword">\draw</span>[densely dotted]</span><br><span class="line">   (2,0) -- (3,2) -- (4,0) -- cycle;</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129092948158.png" alt="image-20250129092948158" style="zoom:67%;" /></p></li>
<li><p><span class="math inline">\(\texttt{arrow - arrow}\)</span>
指定线条首尾的箭头形式。复杂的箭头形式需要在导言区使用
<code>\usetikzlibrary{arrows.meta}</code>。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;[thick]</span><br><span class="line"><span class="keyword">\draw</span>[-&gt;] (0,4) -- (3,4);</span><br><span class="line"><span class="keyword">\draw</span>[-&gt;&gt;] (0,3.5) -- (3,3.5);</span><br><span class="line"><span class="keyword">\draw</span>[-&gt;|] (0,3) -- (3,3);</span><br><span class="line"><span class="keyword">\draw</span>[&lt;-] (0,2.5) -- (3,2.5);</span><br><span class="line"><span class="keyword">\draw</span>[&lt;-&gt;] (0,2) -- (3,2);</span><br><span class="line"><span class="keyword">\draw</span>[&gt;-&gt;|] (0,1.5) -- (3,1.5);</span><br><span class="line"><span class="keyword">\draw</span>[-stealth] (0,1) -- (3,1);</span><br><span class="line"><span class="keyword">\draw</span>[-latex] (0,0.5) -- (3,0.5);</span><br><span class="line"><span class="keyword">\draw</span>[-to] (0,0) -- (3,0);</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129093329315.png" alt="image-20250129093329315" style="zoom:67%;" /></p></li>
<li><p><span class="math inline">\(\texttt{rounded
corners[=&lt;radius&gt;]/sharp corners}\)</span>
将路径转向处绘制成圆角/直角。可选参数<code>&lt;radius&gt;</code>
控制圆角的半径。可对某一段路径直接使用。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span>[rounded corners]</span><br><span class="line">  (0,0) rectangle (1,1);</span><br><span class="line"><span class="keyword">\draw</span> (2,0) -- (2,1)</span><br><span class="line">  [rounded corners=.3cm]</span><br><span class="line">  -- (3,1) -- (3.5,0)</span><br><span class="line">  [sharp corners] -- cycle;</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129093732376.png" alt="image-20250129093732376" style="zoom:67%;" /></p></li>
<li><p><span
class="math inline">\(\texttt{scale/xshift/yshift/xslant/yslant/rotate}\)</span>
设定图形的缩放、位移和旋转。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span>[help lines](0,0) rectangle (1,1);</span><br><span class="line"><span class="keyword">\draw</span>[scale=1.5] (0,0) rectangle (1,1);</span><br><span class="line"><span class="keyword">\draw</span>[rotate=30] (0,0) rectangle (1,1);</span><br><span class="line"><span class="keyword">\draw</span>[help lines](2,0) rectangle (3,1);</span><br><span class="line"><span class="keyword">\draw</span>[yshift=4pt](2,0) rectangle (3,1);</span><br><span class="line"><span class="keyword">\draw</span>[help lines](4,0) rectangle (5,1);</span><br><span class="line"><span class="keyword">\draw</span>[xslant=0.4](4,0) rectangle (5,1);</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129094034719.png" alt="image-20250129094034719" style="zoom:67%;" /></p></li>
</ul>
<p>为了重复利用绘图参数，减少代码冗余，<span
class="math inline">\(\textrm{TikZ}\)</span>
引入了“样式”的概念，可以定义一个样式包含绘图参数，然后将样式作为一个参数用于绘图：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line">  [myarrow/.style=&#123;blue,thick,-&gt;&#125;]</span><br><span class="line"><span class="keyword">\draw</span> (0,0)--(0,1)--(2,1);</span><br><span class="line"><span class="keyword">\draw</span>[myarrow] (0,0)--(2,1);</span><br><span class="line"><span class="keyword">\draw</span>[myarrow,dotted]</span><br><span class="line">  (0,0)--(2,0)--(2,1);</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129094345438.png" alt="image-20250129094345438" style="zoom:67%;" /></p>
<p><span class="math inline">\(\textrm{TikZ}\)</span> 还提供了<span
class="math inline">\(\texttt{scope}\)</span>
环境，令绘图参数或样式在局部生效：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span> (0,0) rectangle (2.5, 2.5);</span><br><span class="line"><span class="keyword">\begin</span>&#123;scope&#125;[thick,scale=0.5]</span><br><span class="line"><span class="keyword">\draw</span> (0,0) rectangle (2.5, 2.5);</span><br><span class="line"><span class="keyword">\end</span>&#123;scope&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129094453595.png" alt="image-20250129094453595" style="zoom:67%;" /></p>
<h3 id="textrmtikz-文字结点"><span
class="math inline">\(\textrm{TikZ}\)</span> 文字结点</h3>
<p><span class="math inline">\(\textrm{TikZ}\)</span> 用
<code>\node</code> 命令绘制文字结点：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\node</span>[&lt;options&gt;] (&lt;name&gt;) at (&lt;coordinate&gt;) &#123;&lt;text&gt;&#125;;</span><br></pre></td></tr></table></figure>
<p><span class="math inline">\(\texttt{(&lt;name&gt;)}\)</span>
为结点命名，类似 <code>\coordinate</code>；<span
class="math inline">\(\texttt{at (&lt;coordinate&gt;)}\)</span>
指定结点的位置。这两者和前面的 <code>&lt;options&gt;</code>
都可以省略，只有 <code>&lt;text&gt;</code> 是必填的。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\node</span> (A) at (0,0) &#123;A&#125;;</span><br><span class="line"><span class="keyword">\node</span> (B) at (1,0) &#123;B&#125;;</span><br><span class="line"><span class="keyword">\node</span> (C) at (60:1) &#123;C&#125;;</span><br><span class="line"><span class="keyword">\draw</span> (A) -- (B) -- (C) -- (A);</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129095143645.png" alt="image-20250129095143645" style="zoom:67%;" /></p>
<p>上一 小节中的参数可用于 <code>\node</code>
命令的配置。除此之外，<code>\node</code> 还有一些特定的参数：</p>
<ul>
<li><p><span
class="math inline">\(\texttt{anchor=&lt;position&gt;}\)</span>
令结点的某个角落 <code>&lt;position&gt;</code> 与
<code>&lt;coordinate&gt;</code> 对应。</p></li>
<li><p><span class="math inline">\(\texttt{centered}\)</span>/<span
class="math inline">\(\texttt{above}\)</span>/<span
class="math inline">\(\texttt{below}\)</span>/<span
class="math inline">\(\texttt{left}\)</span>/<span
class="math inline">\(\texttt{right}\)</span>/<span
class="math inline">\(\texttt{above left}\)</span>/<span
class="math inline">\(\ldots [=&lt;length&gt;]\)</span> 与 <span
class="math inline">\(\texttt{anchor}\)</span> 等效的选项。可选的
<code>&lt;length&gt;</code> 为结点相对于 <code>&lt;coordinate&gt;</code>
的距离。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\coordinate</span> (A) at (1,1);</span><br><span class="line"><span class="keyword">\fill</span> (A) circle[radius=2pt];</span><br><span class="line"><span class="keyword">\node</span>[draw,anchor=south] at (A) &#123;a&#125;;</span><br><span class="line"><span class="keyword">\node</span>[draw,below right=4pt] at (A) &#123;b&#125;;</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129100019973.png" alt="image-20250129100019973" style="zoom:67%;" /></p></li>
<li><p><span class="math inline">\(\texttt{shape=&lt;shape&gt;}\)</span>
结点的形状，默认可用 <span
class="math inline">\(\texttt{rectangle}\)</span> 和 <span
class="math inline">\(\texttt{circle}\)</span>，可省略 <span
class="math inline">\(\texttt{shape=}\)</span>
直接写。在导言区使用命令<code>\usetikzlibrary{shapes.geometric}</code>
可用更多的形状。</p></li>
<li><p><span class="math inline">\(\texttt{text=&lt;color&gt;}\)</span>
结点文字的颜色。</p></li>
<li><p><span class="math inline">\(\texttt{node font={&lt;font
command&gt;}}\)</span>结点文字的字体，形如 <code>\bfseries</code> 或
<code>\itshape</code> 等。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\node</span>[circle,fill=blue,text=white,</span><br><span class="line">  node font=&#123;<span class="keyword">\bfseries</span>&#125;]</span><br><span class="line">  (A) at (0,0) &#123;A node&#125;;</span><br><span class="line"><span class="keyword">\node</span>[rectangle,rounded corners,</span><br><span class="line">  draw=gray,</span><br><span class="line">  node font=&#123;<span class="keyword">\sffamily</span><span class="keyword">\slshape</span>&#125;]</span><br><span class="line">  (B) at (2,0) &#123;B node&#125;;</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129111754629.png" alt="image-20250129111754629" style="zoom:67%;" /></p></li>
<li><p><span class="math inline">\(\texttt{inner sep= &lt;length&gt;} /
\texttt{outer sep=&lt;length&gt;}\)</span>
结点边界向外和向内的额外距离。</p></li>
<li><p><span class="math inline">\(\texttt{minimum
size=&lt;length&gt;}\)</span> / <span
class="math inline">\(\texttt{minimum height=&lt;length&gt;}\)</span>
/<span class="math inline">\(\texttt{minimum
width=&lt;length&gt;}\)</span>结点的最小大小或最小高度/宽度。</p></li>
</ul>
<p><code>\node</code> 命令不仅为文字结点的位置命名，在
<code>\draw</code>
等命令中还可以使用某个结点的相对位置，以“东南西北”的方式命名：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span> (0,0) circle[radius=1];</span><br><span class="line"><span class="keyword">\fill</span> (0,0) circle[radius=2pt];</span><br><span class="line"><span class="keyword">\node</span>[draw] (P) at (15:2) &#123;center&#125;;</span><br><span class="line"><span class="keyword">\draw</span>[dotted] (0,0) -- (P.west);</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129112355158.png" alt="image-20250129112355158" style="zoom:67%;" /></p>
<p><code>\node</code> 命令的一种等效用法是在 <code>\draw</code>
等命令的路径中使用 <span
class="math inline">\(\texttt{node}\)</span>，不仅可以对某个位置标记结点，还能够对线标记：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span> (2,1.5) node[above] &#123;<span class="built_in">$</span>A<span class="built_in">$</span>&#125;</span><br><span class="line">       -- node[above left]  &#123;<span class="built_in">$</span>c<span class="built_in">$</span>&#125;</span><br><span class="line">    (0,0) node[left]  &#123;<span class="built_in">$</span>B<span class="built_in">$</span>&#125;</span><br><span class="line">       -- node[below]       &#123;<span class="built_in">$</span>a<span class="built_in">$</span>&#125;</span><br><span class="line">  (2.5,0) node[right] &#123;<span class="built_in">$</span>C<span class="built_in">$</span>&#125;</span><br><span class="line">       -- node[above right] &#123;<span class="built_in">$</span>b<span class="built_in">$</span>&#125;</span><br><span class="line">       cycle;</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129112621479.png" alt="image-20250129112621479" style="zoom:67%;" /></p>
<p>在此举一个较为复杂的例子，综合前面介绍过的各种路径、形状、文字结点和参数设置，见下面的源代码。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129112859971.png" alt="image-20250129112859971" style="zoom:67%;" /></p>
<h3 id="在-textrmtikz-中使用循环">在 <span
class="math inline">\(\textrm{TikZ}\)</span> 中使用循环</h3>
<p><span class="math inline">\(\textrm{TikZ}\)</span> 通过
<code>pgffor</code> 功能宏包实现了简单的循环功能，语法为：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\foreach</span> <span class="keyword">\a</span> in &#123;&lt;list&gt;&#125; &#123;&lt;commands&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>上述语法定义了 <code>\a</code> 为变量，在
<code>&lt;commands&gt;</code> 中使用 <code>\a</code> 完成循环。</p>
<p><code>&lt;list&gt;</code> 可以直接将所有值写出来，如 <span
class="math inline">\(\texttt{1,2,3,4}\)</span>；也可以写成省略形式，如
<span class="math inline">\(\texttt{1,2,...,10}\)</span>。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\draw</span> (0,0)--(5,0);</span><br><span class="line"><span class="keyword">\foreach</span> <span class="keyword">\i</span> in &#123;0.0,0.1,...,5.0&#125;</span><br><span class="line">  &#123;<span class="keyword">\draw</span>[very thin]</span><br><span class="line">     (<span class="keyword">\i</span>,0)--(<span class="keyword">\i</span>,0.15);&#125;</span><br><span class="line"><span class="keyword">\foreach</span> <span class="keyword">\I</span> in &#123;0,1,2,3,4,5&#125;</span><br><span class="line">  &#123;<span class="keyword">\draw</span> (<span class="keyword">\I</span>,0)--(<span class="keyword">\I</span>,0.25)</span><br><span class="line">     node[above] &#123;<span class="keyword">\I</span>&#125;;&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129113731045.png" alt="image-20250129113731045" style="zoom:67%;" /></p>
<p><code>\foreach</code> 还可使用变量对参与循环：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tikzpicture&#125;</span><br><span class="line"><span class="keyword">\foreach</span> <span class="keyword">\n</span>/<span class="keyword">\t</span> in</span><br><span class="line">  &#123;0/<span class="keyword">\alpha</span>,1/<span class="keyword">\beta</span>,2/<span class="keyword">\gamma</span>&#125;</span><br><span class="line">  &#123;<span class="keyword">\node</span>[circle,fill=lightgray,draw]</span><br><span class="line">    at (<span class="keyword">\n</span>,0) &#123;<span class="built_in">$</span><span class="keyword">\t</span><span class="built_in">$</span>&#125;;&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;tikzpicture&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250129113823045.png" alt="image-20250129113823045" style="zoom:67%;" /></p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>lshort</tag>
      </tags>
  </entry>
  <entry>
    <title>lshort笔记：自定义 LaTeX 命令和功能（八）</title>
    <url>/2025/02/17/notes-reading/computer/lshort/lshort-08-custom/</url>
    <content><![CDATA[<blockquote>
<p>本文内容来自<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">一份
(不太) 简短的 LATEX2ε 介绍<i class="fa fa-external-link-alt"></i></span>。</p>
</blockquote>
<p>如何制作一个简单但像样的毕业论文/书籍/简历模板，每次可以直接套用，而不是再在导言区写一堆代码？</p>
<p>本章的内容将有助于你实现这一个目标，让你能编写可重复利用的模块——宏包和文档类，并在其中自己定义命令和环境。</p>
<span id="more"></span>
<p>不过作为入门手册，这些知识仍是不全面的。如果你不满足于此，需要参考更多资料，比如<a
href="#fn1" class="footnote-ref" id="fnref1"
role="doc-noteref"><sup>1</sup></a> 和 <a href="#fn2"
class="footnote-ref" id="fnref2"
role="doc-noteref"><sup>2</sup></a>。</p>
<hr />
<h2 id="自定义命令和环境">自定义命令和环境</h2>
<h3 id="定义新命令">定义新命令</h3>
<p>使用如下命令可以定义你自己的命令：
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250131110507497.png" alt="image-20250131110507497" style="zoom:67%;" />
<code>\newcommand</code> 的基本用法需要两个必选参数，第一个参数
<code>&lt;name&gt;</code> 是要定义的命令名称（带反斜线），第二个参数
<code>&lt;definition&gt;</code> 是命令的具体定义。方括号里的参数
<code>&lt;num&gt;</code> 是可选的，用于指定新命令所需的参数数目（最多 9
个)。如果缺省可选参数，默认就是 0，也就是新定义的命令不带任何参数。</p>
<p>接下来的两个例子有助于理解。第一个例子定义了一个新的命令
<code>\tnss</code>。这个命令是本手册英文名称“The Not So Short
Introduction to ”
的简写。如果需要在文档中多次使用本手册的名称，那么使用这个命令是一个非常方便的办法。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250131111926918.png" alt="image-20250131111926918" style="zoom:67%;" /></p>
<p>第二个例子演示了如何定义一个带参数的命令。在命令的定义中，标记
<code>#1</code>代表指定的参数。如果想使用多个参数，可以依次使用
<code>#2</code>、……、<code>#9</code>等标记。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250131112252163.png" alt="image-20250131112252163" style="zoom:67%;" /></p>
<p><span class="math inline">\(\LaTeX{}\)</span> 不允许使用
<code>\newcommand</code>
定义一个与现有命令重名的命令。如果需要修改命令定义的话，使用
<code>renewcommand</code> 命令。它使用与命令<code>\newcommand</code>
相同的语法。</p>
<p>在某些情况之下，使用 <code>\providecommand</code>
命令是一种比较理想的方案：在命令未定义时，它相当于
<code>newcommand</code>；在命令已定义时，沿用已有的定义。</p>
<h3 id="定义环境">定义环境</h3>
<p>与 <code>\newcommand</code> 命令类似，可以用
<code>\newenvironment</code> 定义新的环境。它的语法如下所示：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250131141432124.png" alt="image-20250131141432124" style="zoom:67%;" /></p>
<p>同样地，<code>\newenvironment</code> 命令有一个可选的参数。在
<code>&lt;before&gt;</code> 中的内容将在此环境包含的文本之前处理，而在
<code>&lt;after&gt;</code> 中的内容将在遇到
<code>\end{&lt;name&gt;}</code> 命令时处理。</p>
<p>下面的例子演示了 <code>\newenvironment</code> 命令的用法：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250131142005221.png" alt="image-20250131142005221" style="zoom:67%;" /></p>
<p>参数 <code>&lt;num&gt;</code> 的使用方式与 <code>\newcommand</code>
命令相同。<span class="math inline">\(\LaTeX{}\)</span>
还同样保证你不会不小心新建重名的环境。如果你确实希望改变一个现有的环境，你可以使用命令
<code>\renewenvironment</code>，它使用和命令
<code>\newenvironment</code> 相同的语法。</p>
<h3 id="xparse-宏包简介"><code>xparse</code> 宏包简介</h3>
<p>通过 <code>\newcommand</code> 和 <code>\newenvironment</code>
定义的命令或环境格式比较固定。如果需要定义带有多个可选参数、或者带星号的命令或环境，可以使用
<code>xparse</code> 宏包，它提供了 <code>\NewDocumentCommand</code> 和
<code>\NewDocumentEnvironment</code> 等命令，具体语法如下：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250131142701900.png" alt="image-20250131142701900" style="zoom:67%;" /></p>
<p>相比 <code>\newcommand</code> 和
<code>\newenvironment</code>，<code>xparse</code>
通过<code>{&lt;arg spec&gt;}</code>
来指定参数的个数和格式。基本的参数格式见下表。注意<code>{&lt;arg spec&gt;}</code>
中的空格可以忽略。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250131142942039.png" alt="image-20250131142942039" style="zoom:67%;" /></p>
<p>不同输入值在解析后的结果可以见 下表 中的示例。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250131143046148.png" alt="image-20250131143046148" style="zoom:67%;" /></p>
<p><span class="math inline">\(\texttt{-NoValue-}\)</span> 标记可以用
<code>\IfNoValueTF</code> 等命令来判断：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250131143150139.png" alt="image-20250131143150139" style="zoom:67%;" /></p>
<p>举例如下：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250131143225242.png" alt="image-20250131143225242" style="zoom:67%;" /></p>
<p><code>\BooleanTrue</code> 和 <code>\BooleanFalse</code> 则可以用
<code>\IfBooleanTF</code> 等命令来判断：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250131143936824.png" alt="image-20250131143936824" style="zoom:67%;" /></p>
<p>举例如下：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250131143954655.png" alt="image-20250131143954655" style="zoom:67%;" /></p>
<p>需要注意的是，与命令不同，环境在定义时名字里面可以包含
<code>*</code>：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\NewDocumentEnvironment</span> &#123;mytabular&#125;  &#123; o +m &#125; &#123;...&#125; &#123;...&#125;</span><br><span class="line"><span class="keyword">\NewDocumentEnvironment</span> &#123;mytabular*&#125; &#123; m o +m &#125; &#123;...&#125; &#123;...&#125;</span><br></pre></td></tr></table></figure>
<p>用 <code>s</code> 标记的 <code>*</code>
则应该放在<code>\begin{&lt;env&gt;}</code> 的后面：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\NewDocumentEnvironment</span> &#123; envstar &#125; &#123; s &#125;</span><br><span class="line">  &#123;<span class="keyword">\IfBooleanTF</span> &#123;<span class="params">#1</span>&#125; &#123;star&#125; &#123;no star&#125;&#125; &#123;&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;envstar&#125;*</span><br><span class="line"><span class="keyword">\end</span>&#123;envstar&#125;</span><br></pre></td></tr></table></figure>
<p>与 <code>\renewcommand</code>、<code>\providecommand</code>
等命令类似，<code>xparse</code>
宏包也允许在命令或环境已有定义时做出相应的处理，具体见 下表。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250206211617053.png" alt="image-20250206211617053" style="zoom:67%;" /></p>
<hr />
<h2 id="编写自己的宏包和文档类">编写自己的宏包和文档类</h2>
<h3 id="编写简单的宏包">编写简单的宏包</h3>
<p>如果定义了很多新的环境和命令，文档的导言区将变得很长，在这种情况下，可以建立一个新的
<span class="math inline">\(\LaTeX{}\)</span>
宏包来存放所有你自己定义的命令和环境，然后在文档中使用
<code>\usepackage</code> 命令来调用自定义的宏包。</p>
<p>写一个宏包的基本工作就是将原本在文档导言区里很长的内容拷贝到另一个文件中去，这个文件需要以
<span class="math inline">\(\texttt{.sty}\)</span>
作扩展名。还需要加入一个宏包专用的命令：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250207202115559.png" alt="image-20250207202115559" style="zoom:67%;" /></p>
<p>这个命令应该放在你的宏包的最前面，并且一定要注意：<code>&lt;package name&gt;</code>
需要和宏包的文件名一致。<code>\ProvidesPackage</code> 是 LaTeX
中用于声明宏包名称和版本信息的命令，通常放在宏包文件的顶部。它的作用是：</p>
<ol type="1">
<li><strong>声明宏包名称</strong>：告诉 LaTeX 当前文件的宏包名称。</li>
<li><strong>版本信息</strong>：提供宏包的版本号和日期，便于用户了解更新情况。</li>
<li><strong>避免重复加载</strong>：LaTeX
会检查宏包是否已加载，防止重复加载。</li>
</ol>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\ProvidesPackage</span>&#123;&lt;宏包名称&gt;&#125;[&lt;版本信息&gt;]</span><br></pre></td></tr></table></figure>
<ul>
<li><code>&lt;宏包名称&gt;</code>：宏包的名字，通常与文件名一致。</li>
<li><code>&lt;版本信息&gt;</code>：可选，包含版本号和日期。</li>
</ul>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\ProvidesPackage</span>&#123;mypackage&#125;[2023/10/01 v1.0 My custom package]</span><br></pre></td></tr></table></figure>
<p>这表示宏包 <code>mypackage</code> 的版本是 1.0，发布于 2023 年 10 月
1 日。</p>
<p>下面的源代码
给出了一个小的宏包示例，其中包含了之前定义的一些命令。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% Demo Package by Tobias Oetiker</span></span><br><span class="line"><span class="keyword">\ProvidesPackage</span>&#123;demopack&#125;</span><br><span class="line"><span class="keyword">\newcommand</span>&#123;<span class="keyword">\tnss</span>&#125;&#123;The not so Short Introduction</span><br><span class="line">                   to <span class="keyword">\LaTeXe</span>&#125;</span><br><span class="line"><span class="keyword">\newcommand</span>&#123;<span class="keyword">\txsit</span>&#125;[1]&#123;The <span class="keyword">\emph</span>&#123;<span class="params">#1</span>&#125; Short</span><br><span class="line">                       Introduction to <span class="keyword">\LaTeXe</span>&#125;</span><br><span class="line"><span class="keyword">\newenvironment</span>&#123;king&#125;&#123;<span class="keyword">\begin</span>&#123;quote&#125;&#125;&#123;<span class="keyword">\end</span>&#123;quote&#125;&#125;</span><br></pre></td></tr></table></figure>
<h3 id="在宏包中调用其它宏包">在宏包中调用其它宏包</h3>
<p>如果想进一步把各种宏包的功能汇总到一个文件里，而不是在文档的导言区罗列一大堆宏包的话，<span
class="math inline">\(\LaTeX{}\)</span>
允许你在自己编写的宏包中调用其它宏包，命令为
<code>\RequirePackage</code>，用法和 <code>\usepackage</code>一致：
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250207204212557.png" alt="image-20250207204212557" style="zoom:67%;" /></p>
<h3 id="编写自己的文档类">编写自己的文档类</h3>
<p>当你更进一步，需要编写自己的文档类，如论文模板等，问题就稍稍麻烦了一些。首先，自己的文档类以
<span class="math inline">\(\texttt{.cls}\)</span> 作扩展名，开头使用
<code>\ProvidesClass</code> 命令：
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250207210745704.png" alt="image-20250207210745704" style="zoom:67%;" />
<code>{&lt;class name&gt;}</code>也需要和文档类的文件名一致。</p>
<p>但是有了上述命令和和你之前学到的 <code>\newcommand</code>
等，还并不能完成一个文档类的编写，因为诸如
<code>\chapter</code>、<code>\section</code>
等等许多常用的命令都是在文档类中定义的。事实上，许多时候我们只需要像调用宏包那样调用一个基本的文档类，省去许多不必要的麻烦。在你的文档类中调用其它文档类的命令是
<code>\LoadClass</code>，用法和 <code>\documentclass</code> 十分相像：
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250207211020811.png" alt="image-20250207211020811" style="zoom:67%;" /></p>
<h2 id="计数器">计数器</h2>
<p>我们早就见识到了 <span class="math inline">\(\LaTeX{}\)</span>
对文档元素自动计数的能力：章节符号、列表、图表……它们都是依靠 <span
class="math inline">\(\LaTeX{}\)</span> 提供的计数器功能完成的。</p>
<h3 id="定义和修改计数器">定义和修改计数器</h3>
<p>定义一个计数器的方法为：
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250207214349670.png" alt="image-20250207214349670" style="zoom:67%;" /></p>
<p><code>{&lt;counter name&gt;}</code>
为计数器的名称。计数器可以有上下级的关系，可选参数
<code>[&lt;parent counter name&gt;]</code> 定义为
<code>{&lt;counter name&gt;}</code> 的上级计数器。</p>
<p>以下命令修改计数器的数值，<code>\setcounter</code> 将数值设为
<code>&lt;number&gt;</code>；<code>\addtocounter</code> 将数值加上
<code>&lt;number&gt;</code>；<code>\stepcounter</code>
将数值加一，并<span
class="math inline">\(\textbf{将所有下级计数器归零}\)</span>。
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250207220543336.png" alt="image-20250207220543336" style="zoom:67%;" /></p>
<h3 id="计数器的输出格式">计数器的输出格式</h3>
<p>计数器<code>&lt;counter&gt;</code> 的输出格式由
<code>\the&lt;counter&gt;</code>
表示。这个值默认以阿拉伯数字形式输出，如果想改成其它形式，需要重定义
<code>\the&lt;counter&gt;</code>，如将 equation
计数器的格式定义为大写字母：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\renewcommand</span><span class="keyword">\theequation</span>&#123;<span class="keyword">\Alph</span>&#123;equation&#125;&#125;</span><br></pre></td></tr></table></figure>
<p>命令 <code>\Alph</code> 控制计数器 <code>&lt;counter&gt;</code>
的值以大写字母形式显示。下表列出所有可用于修改计数器格式的命令。注意：这些命令<span
class="math inline">\(\textbf{只能用于计数器，不能直接用于数字}\)</span>，如<code>\roman{1}</code>
这样的命令会出错。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250207223910065.png" alt="image-20250207223910065" style="zoom:67%;" /></p>
<p>计数器的输出格式还可以利用其它字符，甚至其它计数器的输出格式与之组合。如标准文档类里对
<code>\subsection</code> 相关的计数器的输出格式的定义相当于：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\renewcommand</span><span class="keyword">\thesubsection</span>&#123;<span class="keyword">\thesection</span>.<span class="keyword">\arabic</span>&#123;subsection&#125;&#125;</span><br></pre></td></tr></table></figure>
<h3 id="latex-中的计数器"><span class="math inline">\(\LaTeX{}\)</span>
中的计数器</h3>
<ul>
<li>所有章节命令 <code>\chapter</code>、<code>\section</code>
等分别对应计数器 <span
class="math inline">\(\textrm{chapter}\)</span>、<span
class="math inline">\(\textrm{section}\)</span>
等等，而且有上下级的关系。而计数器 part 是独立的。</li>
<li>有序列表 <span class="math inline">\(\texttt{enumerate}\)</span>
的各级计数器为 <span class="math inline">\(\textrm{enumi}\)</span>,
<span class="math inline">\(\textrm{enumii}\)</span>, <span
class="math inline">\(\textrm{enumiii}\)</span>, <span
class="math inline">\(\textrm{enumiv}\)</span>，也有上下级的关系。</li>
<li>图表浮动体的计数器就是 <span
class="math inline">\(\textrm{table}\)</span> 和 <span
class="math inline">\(\textrm{figure}\)</span>；公式的计数器为 <span
class="math inline">\(\textrm{equation}\)</span>。这些计数器在 <span
class="math inline">\(\textrm{article}\)</span>
文档类中是独立的，而在<span
class="math inline">\(\textrm{report}\)</span> 和<span
class="math inline">\(\textrm{book}\)</span> 中以<span
class="math inline">\(\textrm{chapter}\)</span> 为上级计数器。</li>
<li>页码、脚注的计数器分别是 <span
class="math inline">\(\textrm{page}\)</span>和 <span
class="math inline">\(\textrm{footnote}\)</span>。</li>
</ul>
<p>我们可以利用前面介绍过的命令，修改计数器的样式以达到想要的效果，比如把页码修改成大写罗马数字，左右加横线，或是给脚注加上方括号：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\renewcommand</span><span class="keyword">\thepage</span>&#123;--~<span class="keyword">\Roman</span>&#123;page&#125;~--&#125;</span><br><span class="line"><span class="keyword">\renewcommand</span><span class="keyword">\thefootnote</span>&#123;[<span class="keyword">\arabic</span>&#123;footnote&#125;]&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;verbatim&#125;</span><br></pre></td></tr></table></figure>
<p>最后介绍两个有用的计数器：</p>
<h4 id="textbfsecnumdepth"><span
class="math inline">\(\textbf{secnumdepth}\)</span></h4>
<p><span class="math inline">\(\LaTeX{}\)</span>
标准文档类对章节划分了层级：</p>
<ul>
<li>在 <span class="math inline">\(\textrm{article}\)</span> 文档类里
<span class="math inline">\(\textrm{part}\)</span> 为 0，<span
class="math inline">\(\textrm{section}\)</span> 为 1，依此类推；</li>
<li><span class="math inline">\(\textrm{report}\)</span> 和 <span
class="math inline">\(\textrm{book}\)</span> 文档类里<span
class="math inline">\(\textrm{part}\)</span>为 -1，<span
class="math inline">\(\textrm{chapter}\)</span> 为 0，<span
class="math inline">\(\textrm{section}\)</span> 为 1，等等。</li>
</ul>
<p><span class="math inline">\(\textrm{secnumdepth}\)</span>
计数器控制章节编号的深度，如果章节的层级大于 <span
class="math inline">\(\textbf{secnumdepth}\)</span>，那么章节的标题、在目录和页眉页脚的标题都不编号（照常生成目录和页眉页脚），章节计数器也不计数。</p>
<p>可以用 <code>\setcounter</code> 命令设置 <span
class="math inline">\(\textrm{secnumdepth}\)</span>
为较大的数使得层级比较深的章节也编号，如设置为 4 令
<code>\paragraph</code> 也编号；或者设置一个较小的数以取消编号，如设置为
-1 令 <code>\chapter</code>
不编号。后者是生成不编号的章节的一个妙招，免去了手动使用
<code>\addcontentsline</code> 和 <code>\markboth</code> 的麻烦。</p>
<p><span class="math inline">\(\textrm{secnumdepth}\)</span> 计数器在
<span class="math inline">\(\textrm{article}\)</span> 文档类里默认为
3（<span class="math inline">\(\textrm{subsubsection}\)</span>
一级）；在 <span class="math inline">\(\textrm{report}\)</span> 和 <span
class="math inline">\(\textrm{book}\)</span> 文档类里默认为 2（<span
class="math inline">\(\textrm{subsection}\)</span> 一级）。</p>
<h4 id="textbftocdepth"><span
class="math inline">\(\textbf{tocdepth}\)</span></h4>
<p><span class="math inline">\(\texttt{tocdepth}\)</span>
计数器控制目录的深度，如果章节的层级大于 <span
class="math inline">\(\texttt{tocdepth}\)</span>，那么章节将不会自动写入目录项。默认值同
<span class="math inline">\(\textrm{secnumdepth}\)</span>。</p>
<hr />
<h2 id="latex-可定制的一些命令和参数"><span
class="math inline">\(LaTeX{}\)</span> 可定制的一些命令和参数</h2>
<p><span class="math inline">\(\LaTeX{}\)</span>
事实上有相当一些可以定制的命令和参数，不过对于修改样式或者开发宏包来说，这些定制项还远远不够。</p>
<p>对于用户来讲，容易定制的是这一些项目：</p>
<ul>
<li><p>标题名称/前后缀等。 下表 列出了标准文档类里可定制的项目，
表中所有的 <span class="math inline">\(\LaTeX{}\)</span> 命令都可以用
<code>\renewcommand</code> 来修改。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250208210859903.png" alt="image-20250208210859903" style="zoom:67%;" /></p></li>
<li><p>长度。前文在叙述各种排版元素时已经介绍过一些，现归纳于
下表。表中所有的长度命令可用 <code>\setlength</code> 来修改。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250208211426192.png" alt="image-20250208211426192" style="zoom:67%;" /></p></li>
</ul>
<aside id="footnotes" class="footnotes footnotes-end-of-document"
role="doc-endnotes">
<hr />
<ol>
<li id="fn1"><p>Frank Mittelbach, Michel Goossens, Johannes Braams,
David Carlisle, Chris Rowley. The LATEX Companion, 2nd edition.
Addison-Wesley, Reading, Massachusetts, 2004, ISBN 0-201-36299-6.<a
href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn2"><p>LATEX Project Team. LATEX 2 for class and package
writers. <code>CTAN://macros/latex/base/clsguide.pdf</code>
(<strong>texdoc clsguide</strong>)<a href="#fnref2"
class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</aside>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>lshort</tag>
      </tags>
  </entry>
  <entry>
    <title>lshort笔记：安装TEX发行版（九）</title>
    <url>/2025/02/25/notes-reading/computer/lshort/lshort-09-install/</url>
    <content><![CDATA[<blockquote>
<p>本文内容来自<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">一份
(不太) 简短的 LATEX2ε 介绍<i class="fa fa-external-link-alt"></i></span>。</p>
</blockquote>
<p>本章会简单介绍如何安装 <span class="math inline">\(\TeX{}\)</span>
发行版，以及保持发行版的内容紧跟最新。后者非常重要，因为 <span
class="math inline">\(\LaTeX{}\)</span> 宏包是不断更新换代的。</p>
<span id="more"></span>
<hr />
<h2 id="tex-发行版简介"><span class="math inline">\(\TeX{}\)</span>
发行版简介</h2>
<p>一个 <strong><span class="math inline">\(\TeX{}\)</span>
发行版</strong> 是 <span class="math inline">\(\TeX{}\)</span>
排版引擎、支持排版的文件（基本格式、<span
class="math inline">\(\LaTeX{}\)</span>
宏包、字体等）以及一些辅助工具的集合。各式各样的 <span
class="math inline">\(\TeX{}\)</span>
发行版经过十多年的发展，大浪淘沙，现今的两个主流发行版为：</p>
<ul>
<li><p><span class="math inline">\(\textbf{TeX Live}\)</span> <span
class="math inline">\(\textrm{TeX Live}\)</span> 由类 UNIX 系统上的
<span class="math inline">\(\textrm{teTEX}\)</span>
发展并取而代之，最终成为跨平台的 <span
class="math inline">\(\TeX{}\)</span> 发行版。<span
class="math inline">\(\textrm{TeX Live}\)</span> 自 2011
年起以年份作为发行版的版本号，保持了一年一更的频率。</p>
<p><span class="math inline">\(\textrm{MacTeX}\)</span> 是 macOS（OS
X）系统下的一个定制化的 <span class="math inline">\(\textrm{TeX
Live}\)</span> 版本，与 <span class="math inline">\(\textrm{TeX
Live}\)</span> 同步更新。</p></li>
<li><p><span class="math inline">\(\textbf{MiKTeX}\)</span> <span
class="math inline">\(\textrm{MiKTeX}\)</span> 是主要用于 Windows
平台的一个稳定发展的 <span class="math inline">\(\TeX{}\)</span>
发行版。在中国大陆曾经发行过“<span
class="math inline">\(\textrm{CTeX}\)</span>
套装”，它是一个经过本地化配置的 <span
class="math inline">\(\textrm{MiKTeX}\)</span>，不过其配置较为过时，也不再有更新支持，使用起来可能有诸多问题，现已不推荐使用。</p></li>
</ul>
<p><span class="math inline">\(\textrm{TeX Live}\)</span> 和 <span
class="math inline">\(\textrm{MiKTeX}\)</span> 都集成了一个简单的 <span
class="math inline">\(\LaTeX{}\)</span> 源代码编辑器
<strong>TeXworks</strong>（<span
class="math inline">\(\textrm{MacTeX}\)</span> 则集成了类似的
TeXShop）。用户在安装完毕后，可直接使用 TeXworks 编写和编译 <span
class="math inline">\(\LaTeX{}\)</span> 源代码。</p>
<h3 id="安装发行版">安装发行版</h3>
<h4 id="textbftex-live"><span class="math inline">\(\textbf{TeX
Live}\)</span></h4>
<p><span class="math inline">\(\textrm{TeX Live}\)</span>
的安装和使用指南可参考<span class="exturl" data-url="aHR0cHM6Ly93d3cudHVnLm9yZy90ZXhsaXZlL2RvYy90ZXhsaXZlLXpoLWNuL3RleGxpdmUtemgtY24ucGRm">文档<i class="fa fa-external-link-alt"></i></span>,
可用 <code>texdoc texlive-zh-cn</code> 在本地打开。</p>
<p><span class="math inline">\(\textrm{TeX Live}\)</span>
的光盘镜像发布于 <span class="exturl" data-url="aHR0cHM6Ly93d3cudHVnLm9yZy90ZXhsaXZlLw==">texlive官网<i class="fa fa-external-link-alt"></i></span>，下载镜像到本地，挂载到虚拟光驱，或者用压缩工具解压后，在其根目录有几个用于安装的脚本：</p>
<ul>
<li><p>用于 <strong>Windows</strong> 的批处理文件：</p>
<ul>
<li><p><span
class="math inline">\(\texttt{install-tl-windows.bat}\)</span>
双击启动图形界面安装程序，可以在图形安装界面的 Advanced
选项中定制安装；</p></li>
<li><p>在命令提示符中输入 <span
class="math inline">\(\texttt{install-tl-windows.bat -no-gui}\)</span>
启动文本界面安装程序。</p></li>
</ul></li>
<li><p>用于 <strong>Linux</strong> 的 Perl 脚本 <span
class="math inline">\(\texttt{install-tl}\)</span> ：</p>
<ul>
<li><p><span class="math inline">\(\texttt{install-tl}\)</span>
启动文本界面安装程序；</p></li>
<li><p><span class="math inline">\(\texttt{install-tl -gui}\)</span>
启动图形界面安装程序。</p></li>
</ul></li>
</ul>
<p>另外也可以下载在线安装程序 <span
class="math inline">\(\texttt{install-tl.zip}\)</span>，包含以上所有安装脚本。安装过程中会从
CTAN 软件源下载所有组件。</p>
<p>Linux 下 <span class="math inline">\(\textrm{TeX Live}\)</span>
安装完毕后，一般还需要在 root 权限下进行以下操作，使得 <span
class="math inline">\(\textrm{XeLaTeX}\)</span> 能正确通过
<code>fontspec</code>等宏包使用字体：</p>
<ol type="1">
<li>将 <span
class="math inline">\(\texttt{texlive-fontconfig.conf}\)</span>
文件复制到 <span
class="math inline">\(\texttt{/etc/fonts/conf.d/09-texlive.conf}\)</span>。</li>
<li>运行 <span class="math inline">\(\texttt{fc-cache
-fsv}\)</span>。</li>
</ol>
<h4 id="textbfmiktex"><span
class="math inline">\(\textbf{MiKTeX}\)</span></h4>
<p>从 <span class="math inline">\(\textrm{MiKTeX}\)</span> <span class="exturl" data-url="aHR0cHM6Ly93d3cubWlrdGV4Lm9yZy8=">官网<i class="fa fa-external-link-alt"></i></span> 下载名为 <span
class="math inline">\(\texttt{basic-miktex-***.exe}\)</span> 的 Windows
安装包。下载后直接双击打开，按照程序的提示进行安装即可。</p>
<hr />
<h2 id="安装和更新宏包">安装和更新宏包</h2>
<p><span class="math inline">\(\textrm{TeX Live}\)</span> 和 <span
class="math inline">\(\textrm{MiKTeX}\)</span>
分别提供了图形界面的宏包管理器 <span class="math inline">\(\textrm{TEX
Live Manager}\)</span> 和 <span class="math inline">\(\textrm{MiKTeX
Package
Manager}\)</span>，用于安装、管理和更新宏包。一般情况下，直接在图形界面的工具下按提示操作即可（<span
class="math inline">\(\textrm{MiKTeX Package Manager}\)</span>
有普通权限和管理员权限的版本，建议<strong>总是使用管理员权限</strong>的版本）。</p>
<p>两者也可以通过各自的命令行工具安装和更新宏包：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">% TeX Live 命令行工具 tlmgr 的使用示例</span><br><span class="line">% 安装/卸载宏包</span><br><span class="line">tlmgr install &lt;package-name&gt;</span><br><span class="line">tlmgr remove &lt;package-name&gt;</span><br><span class="line">% 更新所有宏包（包括 tlmgr 本身）</span><br><span class="line">tlmgr update --all --self</span><br><span class="line">% 列出所有可更新的宏包</span><br><span class="line">tlmgr update --list</span><br><span class="line">% 指定更新源地址</span><br><span class="line">% &lt;CTAN mirrors&gt; 形如 https://mirrors.tuna.tsinghua.edu.cn/CTAN</span><br><span class="line">tlmgr repository <span class="built_in">set</span> &lt;CTAN mirrors&gt;/systems/texlive/tlnet</span><br><span class="line">% 查看宏包信息，加 --list 参数可列出宏包的所有文件</span><br><span class="line">tlmgr info &lt;package-name&gt;</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">% MiKTeX 命令行工具 mpm 的使用示例</span><br><span class="line">% 建议始终加 --admin 参数使用</span><br><span class="line">% 安装/卸载宏包</span><br><span class="line">mpm --admin --install &lt;package-name&gt;</span><br><span class="line">mpm --admin --uninstall &lt;package-name&gt;</span><br><span class="line">% 更新所有宏包</span><br><span class="line">mpm --admin --update</span><br><span class="line">% 列出所有可更新的宏包</span><br><span class="line">mpm --admin --find-updates</span><br><span class="line">% 指定更新源地址</span><br><span class="line">mpm --admin --set-repository=&lt;CTAN mirrors&gt;/systems/win32/miktex/tm/packages</span><br><span class="line">% 查看宏包信息</span><br><span class="line">mpm --admin --print-package-info &lt;package-name&gt;</span><br></pre></td></tr></table></figure>
<p><span class="math inline">\(\textrm{TeX Live}\)</span>
默认安装所有宏包， <span class="math inline">\(\textrm{MiKTeX}\)</span>
的安装程序只包含了 <span class="math inline">\(\LaTeX{}\)</span>
的一些基本宏包。从 <span class="math inline">\(\textrm{TeX
Live}\)</span> 的光盘镜像和 <span
class="math inline">\(\textrm{MiKTeX}\)</span> 的安装包体积可见一斑。
默认情况下，编译过程中如果遇到宏包未安装而报错的情况下，<span
class="math inline">\(\textrm{MiKTeX}\)</span>
会弹出一个对话框，让用户可以选择临时安装宏包，安装成功后继续编译。</p>
<h3 id="手动安装宏包">手动安装宏包</h3>
<p><span
class="math inline">\(\textcolor{red}{如非万不得已，尽量不要手动安装宏包}\)</span>。绝大多数宏包都已打包到
<span class="math inline">\(\textrm{TeX Live}\)</span> 和 <span
class="math inline">\(\textrm{MiKTeX}\)</span>
两大发行版的安装源，可用宏包管理器安装。如果用户知道某个宏包的名称，但不确定是否在发行版中已打包，可在
<span class="math inline">\(\textrm{CTAN}\)</span> 中搜索。</p>
<p>如果确实有手动安装宏包的需要，本小节的内容将有所帮助。在手动安装之前，有必要了解一下
<span class="math inline">\(\TeX{}\)</span> 目录结构（<span
class="math inline">\(\TeX{}\)</span> Directory Structure, TDS）。它是
<span class="math inline">\(\TeX{}\)</span>
发行版中宏包、字体、帮助文档等文件的组织结构。TDS 有时也称为 <span
class="math inline">\(\textrm{TEXMF}\)</span> 树，取 <span
class="math inline">\(\TeX+\textrm{METAFONT}\)</span> 之意。</p>
<p>以 <span class="math inline">\(\textrm{TeX Live}\)</span>
为例，假设系统的 <span class="math inline">\(\textrm{TEXMF}\)</span>
树根目录为 <span
class="math inline">\(\textrm{C:\\texlive\\2020\\texmf-dist}\)</span>，其下有很多子目录，仅举几例：</p>
<table>
<colgroup>
<col style="width: 29%" />
<col style="width: 70%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;"><span
class="math inline">\(\texttt{tex/latex}\)</span></th>
<th style="text-align: left;"><span
class="math inline">\(\LaTeX{}\)</span> 宏包</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{doc/latex}\)</span></td>
<td style="text-align: left;"><span
class="math inline">\(\LaTeX{}\)</span> 宏包的帮助文档</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{source/latex}\)</span></td>
<td style="text-align: left;"><span
class="math inline">\(\LaTeX{}\)</span> 宏包的源代码</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{bibtex}\)</span></td>
<td style="text-align: left;"><span
class="math inline">\(\textrm{BibTeX}\)</span>
工具相关文件，许多宏包配套的 <span
class="math inline">\(\textrm{BibTeX}\)</span> 格式文件位于子目录 <span
class="math inline">\(\texttt{bst}\)</span> 中</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{fonts/tfm}\)</span></td>
<td style="text-align: left;">$ $ 使用的字体文件，<span
class="math inline">\(\textrm{TFM}\)</span> 格式</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{fonts/type1}\)</span></td>
<td style="text-align: left;"><span
class="math inline">\(\textrm{PostScript}\)</span> 字体文件（<span
class="math inline">\(\textrm{Type1}\)</span>），<span
class="math inline">\(\textrm{PFB}\)</span> 格式</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{fonts/opentype}\)</span></td>
<td style="text-align: left;"><span
class="math inline">\(\textrm{OpenType}\)</span> 格式的字体文件</td>
</tr>
</tbody>
</table>
<p>需要手动安装的宏包，一般已经按照上述目录结构打包完成。手动安装时，尽量不要拷贝到系统的
<span class="math inline">\(\textrm{TEXMF}\)</span>
树，而是拷贝到发行版提供的用户 <span
class="math inline">\(\textrm{TEXMF}\)</span> 树，如 <span
class="math inline">\(\textrm{TeX Live}\)</span> 的 <span
class="math inline">\(\textrm{C:\\texlive\\texmf-local}\)</span>。安装完成后，还需<strong>刷新
<span class="math inline">\(\TeX{}\)</span>
发行版的文件名数据库</strong>，令新安装的宏包文件能够被系统找到。<span
class="math inline">\(\textrm{TeX Live}\)</span> 用户须在 Windows
命令行或者 Linux 终端执行命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mktexlsr</span><br></pre></td></tr></table></figure>
<p><span class="math inline">\(\textrm{MiKTeX}\)</span>
用户的命令为：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">initexmf --update-fndb</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>lshort</tag>
      </tags>
  </entry>
  <entry>
    <title>lshort笔记：LaTeX 的基本概念（一）</title>
    <url>/2024/12/31/notes-reading/computer/lshort/lshort-01-basic-concepts/</url>
    <content><![CDATA[<blockquote>
<p>本文内容来自<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">一份
(不太) 简短的 LATEX2ε 介绍<i class="fa fa-external-link-alt"></i></span>。</p>
</blockquote>
<p>本章对 TeX 与 LaTeX 的基本概念和整体结构作了简要介绍。首先说明了 TeX
与 LaTeX 的来源和优缺点。随后通过一个简单的 Hello, World 示例，展示了
LaTeX 文档的基本组成形式。</p>
<p>在此基础上，进一步介绍了 LaTeX
中的命令与环境，以及源代码在逻辑和结构上的组织方式。同时，对文档类与宏包的作用进行了说明，并列举了
LaTeX 编译过程中涉及的常见文件类型与文件组织方式。最后，对 TeX / LaTeX
中常见的术语和相关概念进行了集中说明。</p>
<p>本章旨在帮助读者对 LaTeX
建立整体性的认识，为后续章节中更深入的排版细节与宏包使用奠定基础。</p>
<span id="more"></span>
<hr />
<h2 id="概述">概述</h2>
<h3 id="tex"><span class="math inline">\(\TeX{}\)</span></h3>
<p><span class="math inline">\(\TeX{}\)</span>是<span class="exturl" data-url="aHR0cHM6Ly93d3ctY3MtZmFjdWx0eS5zdGFuZm9yZC5lZHUvfmtudXRoLw==">高德纳 (Donald
E.~Knuth)<i class="fa fa-external-link-alt"></i></span>为排版文字和数学公式而开发的软件。</p>
<h3 id="latex"><span class="math inline">\(\LaTeX{}\)</span></h3>
<p><span class="math inline">\(\LaTeX{}\)</span> 是一种使用 <span
class="math inline">\(\TeX{}\)</span>
程序作为排版引擎的格式，可以粗略地将它理解成是对 <span
class="math inline">\(\TeX{}\)</span> 的一层封装。 <span
class="math inline">\(\LaTeX{}\)</span>
最初的设计目标是分离内容与格式，以便作者能够专注于内容创作而非版式设计，并能以此得到高质量排版的作品。<span
class="math inline">\(\LaTeX{}\)</span> 起初由 Leslie Lamport
博士开发，目前由 <span class="math inline">\(\LaTeX{}\)</span> 工作组<a
href="#fn1" class="footnote-ref" id="fnref1"
role="doc-noteref"><sup>1</sup></a>进行维护。</p>
<h3 id="latex-的优缺点"><span class="math inline">\(\LaTeX{}\)</span>
的优缺点</h3>
<p>优点：</p>
<blockquote>
<ul>
<li>好看。</li>
<li>具有方便而强大的数学公式排版能力。</li>
<li>用户只需专注于组织文档结构的基础命令，无需操心文档的版面设计。</li>
<li>容易生成复杂的专业排版元素，如脚注、交叉引用、参考文献、目录等。</li>
<li>强大的可扩展性。数以千计的 <span
class="math inline">\(\LaTeX{}\)</span> 宏包用于补充和扩展 <span
class="math inline">\(\LaTeX{}\)</span> 的功能。</li>
<li>能够促使用户写出结构良好的文档。</li>
<li><span class="math inline">\(\LaTeX{}\)</span> 和 <span
class="math inline">\(\TeX{}\)</span>
及相关软件是跨平台、免费、开源的。</li>
</ul>
</blockquote>
<p>缺点：</p>
<blockquote>
<ul>
<li>入门门槛高。</li>
<li>不容易排查错误。</li>
<li>不容易定制样式。</li>
<li>相比“所见即所得”的模式需要编译查看文档效果。</li>
</ul>
</blockquote>
<hr />
<h2 id="hello-world">Hello, World</h2>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\documentclass</span>&#123;article&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line">``Hello world!&#x27;&#x27; from <span class="keyword">\LaTeX</span>.</span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure>
<p>这里首先介绍如何编译使用这份源代码，在后续小节中再介绍源代码的细节。可以将这份源代码保存为
<code>helloworld.tex</code>，而后编译。具体来说:</p>
<ul>
<li><p>如果使用 TeXworks 或 TeXstudio
等编辑器，可以使用编辑器提供的“编译”或“排版”按钮。建议使用
<code>pdfLaTeX</code> 或 <code>XeLaTeX</code>
作为默认的编译方式。</p></li>
<li><p>如果使用命令行方式进行编译，打开终端，在源代码所在的目录下输入：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pdflatex helloworld</span><br></pre></td></tr></table></figure>
<p>或者</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">xelatex helloworld</span><br></pre></td></tr></table></figure>
<p>如果编译成功，可以在 <code>helloworld.tex</code> 所在目录看到生成的
<code>helloworld.pdf</code> 以及一些其它文件。</p></li>
</ul>
<p>下面是排版中文的一个最简示例。编译的方式与上一份源代码相同，但需使用
<code>XeLaTeX</code> 编译方式。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\documentclass</span>&#123;ctexart&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line">“你好，世界！”来自<span class="keyword">\LaTeX</span>&#123;&#125; 的问候。</span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="latex-命令和代码结构"><span
class="math inline">\(\LaTeX{}\)</span> 命令和代码结构</h2>
<p><span class="math inline">\(\LaTeX{}\)</span>
的源代码为文本文件。这些文本除了文字本身，还包括各种命令，用在排版公式、划分文档结构、控制样式等等不同的地方。</p>
<h3 id="latex-命令和环境"><span class="math inline">\(\LaTeX{}\)</span>
命令和环境</h3>
<p><span class="math inline">\(\LaTeX{}\)</span> 中命令以反斜线
<code>\</code> 开头，为以下两种形式之一：</p>
<ul>
<li><p>反斜线和后面的一串字母，如
<code>\{LaTeX}</code>。它们以任意非字母符号（空格、数字、标点等）为界限。</p></li>
<li><p>反斜线和后面的单个非字母符号，如 <code>\$</code>。</p></li>
</ul>
<p>注意 <span class="math inline">\(\LaTeX{}\)</span>
命令<strong>对大小写敏感</strong>，比如输入 <code>\{LaTeX}</code>
命令可生成错落有致的 <span class="math inline">\(\LaTeX{}\)</span>
字母组合，但输入 <code>\{Latex}</code> 或 <code>\{LaTex}</code>
什么都得不到，还会报错。</p>
<p>字母形式的 <span class="math inline">\(\LaTeX{}\)</span>
命令忽略其后的所有连续空格。如果要人为引入空格，需要在命令后面加一对花括号阻止其忽略空格:
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250113150429995.png"
alt="image-20250113150429995" /></p>
<p>一些 <span class="math inline">\(\LaTeX{}\)</span>
命令可以接收一些参数，参数的内容会影响命令的效果。<span
class="math inline">\(\LaTeX{}\)</span>
的参数分为可选参数和必选参数。可选参数以方括号 <code>[ ]</code>
包裹；必选参数一般以花括号 <code>{ }</code> 包裹<a href="#fn2"
class="footnote-ref" id="fnref2"
role="doc-noteref"><sup>2</sup></a>。还有些命令可以带一个星号
<code>*</code>，带星号和不带星号的命令效果有一定差异。初次接触这些概念时，可以粗略地把星号看作一种特殊的可选参数。</p>
<p><span class="math inline">\(\LaTeX{}\)</span>
中还包括<strong>环境</strong>，用以令一些效果在局部生效，或是生成特殊的文档元素。<span
class="math inline">\(\LaTeX{}\)</span> 环境的用法为一对命令
<code>\begin</code> 和 <code>\end</code>：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;&lt;environment name&gt;&#125;[&lt;optional arguments&gt;]&#123;&lt;mandatory arguments&gt;&#125;</span><br><span class="line">...</span><br><span class="line"><span class="keyword">\end</span>&#123;&lt;environment name&gt;&#125;</span><br></pre></td></tr></table></figure>
<p>有些命令（如
<code>\bfseries</code>）会对其后所有内容产生作用。若要限制其作用范围，则需要使用<strong>分组</strong>。<span
class="math inline">\(\LaTeX{}\)</span> 使用一对花括号<code>{ }</code>
作为分组，在分组中使用的命令被限制在分组内，不会影响到分组外的内容<a
href="#fn3" class="footnote-ref" id="fnref3"
role="doc-noteref"><sup>3</sup></a>。上文提到的环境隐含了一个分组，在环境中的命令被包裹在分组内。</p>
<h3 id="latex-源代码结构"><span class="math inline">\(\LaTeX{}\)</span>
源代码结构</h3>
<p><span class="math inline">\(\LaTeX{}\)</span> 源代码以一个
<code>\{documentclass}</code>
命令作为开头，它指定了文档使用的<strong>文档类</strong>。<code>document</code>
环境当中的内容是文档正文。 在 <code>\documentclass</code> 和
<code>\begin{document}</code> 之间的位置称为<strong>导言区</strong>。
在导言区中常会使用 <code>\usepackage{}</code>
命令调用<strong>宏包</strong>，还会进行文档的全局设置。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\documentclass</span>&#123;...&#125;  <span class="comment">% ... 为某文档类</span></span><br><span class="line"><span class="comment">% 导言区</span></span><br><span class="line"><span class="keyword">\usepackage</span>&#123;comment&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line"><span class="comment">% 正文内容</span></span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br><span class="line"><span class="comment">% 此后内容会被忽略</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="latex-宏包和文档类"><span
class="math inline">\(\LaTeX{}\)</span> 宏包和文档类</h2>
<h3 id="文档类">文档类</h3>
<p><strong>文档类</strong>规定了 <span
class="math inline">\(\LaTeX{}\)</span>
源代码所要生成的文档的性质——普通文章、书籍、演示文稿、个人简历等。<span
class="math inline">\(\LaTeX{}\)</span>
源代码的开头须用<code>\documentclass</code>指定文档类：
<code>\documentclass[&lt;options&gt;]{&lt;class-name&gt;}</code>
其中<code>&lt;class-name&gt;</code>为文档类的名称，如 <span
class="math inline">\(\LaTeX{}\)</span> 提供的
<code>article</code>、<code>report</code>、<code>book</code>，在其基础上派生的一些文档类，如支持中文排版的
<code>ctexart</code>、<code>ctexrep</code>、<code>ctexbook</code>，或者有其它功能的文档类，如
<code>moderncv</code>、<code>beamer</code> 等。<span
class="math inline">\(\LaTeX{}\)</span>
提供的基础文档类见下表，其中前三个习惯上称为“标准文档类”。</p>
<table>
<colgroup>
<col style="width: 10%" />
<col style="width: 89%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">文档类</th>
<th style="text-align: left;">说明</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><code>article</code></td>
<td
style="text-align: left;">文章格式的文档类，广泛用于科技论文、报告、说明文档等。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>report</code></td>
<td
style="text-align: left;">长篇报告格式的文档类，具有章节结构，用于综述、长篇论文、简单的书籍等。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>book</code></td>
<td
style="text-align: left;">书籍文档类，包含章节结构和前言、正文、后记等结构。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>proc</code></td>
<td style="text-align: left;">基于<code>article</code>
文档类的一个简单的学术文档模板。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>slides</code></td>
<td style="text-align: left;">幻灯格式的文档类，使用无衬线字体。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>minimal</code></td>
<td
style="text-align: left;">一个极其精简的文档类，只设定了纸张大小和基本字号，用作代码测试的最小工作示例。</td>
</tr>
</tbody>
</table>
<p>可选参数 <code>&lt;options&gt;</code>
为文档类指定选项，以全局地规定一些排版的参数，如字号、纸张大小、单双面等等。比如调用
<code>article</code> 文档类排版文章，指定纸张为 A4 大小，基本字号为
11pt，双面排版：
<code>\documentclass[11pt,twoside,a4paper]{article}</code></p>
<p><span class="math inline">\(\LaTeX{}\)</span>
的三个标准文档类可指定的选项包括：</p>
<table>
<colgroup>
<col style="width: 36%" />
<col style="width: 63%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">options</th>
<th style="text-align: left;">含义</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><code>10pt, 11pt, 12pt</code></td>
<td style="text-align: left;">指定文档的基本字号。默认为
<code>10pt</code></td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>a4paper,letterpaper</code></td>
<td style="text-align: left;">指定纸张大小</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>twoside, oneside</code></td>
<td
style="text-align: left;">指定单面/双面排版。双面排版时，奇偶页的页眉页脚、页边距不同。<code>article</code>
和 <code>report</code> 默认为 <code>oneside</code>，<code>book</code>
默认为 <code>twoside</code></td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>onecolumn, twocolumn</code></td>
<td style="text-align: left;">指定单栏/双栏排版。默认为
<code>onecolumn</code></td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>openright, openany</code></td>
<td style="text-align: left;">指定新的一章 <code>chapter</code>
是在奇数页（右侧）开始，还是直接紧跟着上一页开始。<code>report</code>
默认为 <code>openany</code>，<code>book</code> 默认为
<code>openright</code>。对 <code>article</code> 无效</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>landscape</code></td>
<td style="text-align: left;">指定横向排版。默认为纵向</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>titlepage, notitlepage</code></td>
<td style="text-align: left;">指定标题命令 <code>\maketitle</code>
是否生成单独的标题页。<code>article</code>默认为
<code>notitlepage</code>，<code>report</code> 和 <code>book</code>
默认为 <code>titlepage</code></td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>fleqn</code></td>
<td style="text-align: left;">令行间公式左对齐。默认为居中对齐</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>leqno</code></td>
<td style="text-align: left;">将公式编号放在左边。默认为右边</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>draft, final</code></td>
<td style="text-align: left;">指定草稿/终稿模式。默认为
<code>final</code></td>
</tr>
</tbody>
</table>
<h3 id="宏包">宏包</h3>
<p>在使用 <span class="math inline">\(\LaTeX{}\)</span>
时，时常需要依赖一些扩展来增强或补充 <span
class="math inline">\(\LaTeX{}\)</span>
的功能，比如排版复杂的表格、插入图片、增加颜色甚至超链接等等。这些扩展称为<strong>宏包</strong>。调用宏包的方法类似调用文档类的方法：
<code>\usepackage[&lt;options&gt;]{&lt;package-name&gt;}</code></p>
<p><code>\usepackage</code>可以一次性调用多个宏包，在
<code>&lt;package-name&gt;</code>
中用逗号隔开。这种用法一般不要指定选项<a href="#fn4"
class="footnote-ref" id="fnref4"
role="doc-noteref"><sup>4</sup></a>。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% 一次性调用三个排版表格常用的宏包</span></span><br><span class="line"><span class="keyword">\usepackage</span>&#123;tabularx, makecell, multirow&#125;</span><br></pre></td></tr></table></figure>
<p>在使用宏包和文档类之前，一定要首先确认它们是否安装在你的计算机中，否则
<code>\usepackage</code>等命令会报错误。</p>
<p>宏包（包括前面所说的文档类）可能定义了许多命令和环境，或者修改了
<span class="math inline">\(\LaTeX{}\)</span>
已有的命令和环境。它们的用法说明记在相应宏包和文档类的帮助文档。在
Windows 命令提示符或者 Linux 终端下输入命令可查阅相应文档：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">texdoc &lt;pkg-name&gt;</span><br></pre></td></tr></table></figure>
<p>其中 <code>&lt;pkg-name}&gt;</code> 是宏包或者文档类的名称。</p>
<hr />
<h2 id="latex-用到的文件一览"><span
class="math inline">\(\LaTeX{}\)</span> 用到的文件一览</h2>
<p>除了源代码文件 <code>.tex</code> 以外，使用 <span
class="math inline">\(\LaTeX{}\)</span>
时还可能接触到各种格式的文件。本节简单介绍一下经常见到的文件。</p>
<p>每个宏包和文档类都是带特定扩展名的文件，除此之外也有一些文件出现于
<span class="math inline">\(\LaTeX{}\)</span> 模板中：</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;"><code>.sty</code></th>
<th style="text-align: left;">宏包文件。宏包的名称与文件名一致</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><code>.cls</code></td>
<td style="text-align: left;">文档类文件。文档类名称与文件名一致</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>.bib</code></td>
<td style="text-align: left;">参考文献数据库文件</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>.bst</code></td>
<td style="text-align: left;">用到的参考文献格式模板</td>
</tr>
</tbody>
</table>
<p>在编译过程中可能会生成相当多的辅助文件和日志。一些功能如交叉引用、参考文献、目录、索引等，需要先通过编译生成辅助文件，然后再次编译时读入辅助文件得到正确的结果，所以复杂的源代码可能要编译多次。</p>
<table>
<colgroup>
<col style="width: 20%" />
<col style="width: 80%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">中间文件</th>
<th style="text-align: left;">作用</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><code>.log</code></td>
<td
style="text-align: left;">排版引擎生成的日志文件，供排查错误使用</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>.aux</code></td>
<td
style="text-align: left;">生成的主辅助文件，记录交叉引用、目录、参考文献的引用等</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>.toc</code></td>
<td style="text-align: left;">生成的目录记录文件</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>.lof</code></td>
<td style="text-align: left;">生成的图片目录记录文件</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>.lot</code></td>
<td style="text-align: left;">生成的表格目录记录文件</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>.bbl</code></td>
<td style="text-align: left;">BibTeX生成的参考文献记录文件</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>.blg</code></td>
<td style="text-align: left;">BibTeX生成的日志文件</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>.idx</code></td>
<td style="text-align: left;">生成的供 <code>makeindex</code>
处理的索引记录文件</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>.ind</code></td>
<td style="text-align: left;"><code>makeindex</code> 处理
<code>.idx</code> 生成的用于排版的格式化索引文件</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>.ilg</code></td>
<td style="text-align: left;"><code>makeindex</code> 生成的日志文件</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>.out</code></td>
<td style="text-align: left;"><code>hyperref</code> 宏包生成的 PDF
书签记录文件</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="文件的组织方式">文件的组织方式</h2>
<p>当编写长篇文档时，例如当编写书籍、毕业论文时，单个源文件会使修改、校对变得十分困难。将源文件分割成若干个文件，例如将每章内容单独写在一个文件中，会大大简化修改和校对的工作。
<span class="math inline">\(\LaTeX{}\)</span> 提供了命令
<code>\include</code> 用来在源代码里插入文件：</p>
<p><code>\include{&lt;filename&gt;}</code></p>
<p><code>&lt;filename&gt;</code> 为文件名（不带 <code>.tex</code>
扩展名）<a href="#fn5" class="footnote-ref" id="fnref5"
role="doc-noteref"><sup>5</sup></a>，如果和要编译的主文件不在一个目录中，则要加上相对或绝对路径，例如：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\include</span>&#123;chapters/file&#125; <span class="comment">% 相对路径</span></span><br><span class="line"><span class="keyword">\include</span>&#123;/home/Bob/file&#125; <span class="comment">% *nix（包含 Linux、macOS）绝对路径</span></span><br><span class="line"><span class="keyword">\include</span>&#123;D:/file&#125; <span class="comment">% Windows 绝对路径，用正斜线</span></span><br></pre></td></tr></table></figure>
<p>值得注意的是 <code>\include</code>
在读入<filename>之前会<strong>另起一页</strong>。有的时候我们并不需要这样，而是用<code>\input</code>
命令，它<strong>纯粹是把文件里的内容插入</strong>：</p>
<p><code>\input{&lt;filename&gt;}</code></p>
<p>当导言区内容较多时，常常将其单独放置在一个 <code>.tex</code>
文件中，再用 <code>\input</code>
命令插入。复杂的图、表、代码等也会用类似的手段处理。</p>
<p><span class="math inline">\(\LaTeX{}\)</span> 还提供了一个
<code>\includeonly</code>
命令来组织文件，用于导言区，指定只载入某些文件。导言区使用了
<code>\includeonly</code> 后，正文中不在其列表范围的
<code>\include</code> 命令不会起效：</p>
<p><code>\includeonly{&lt;filename1&gt;,&lt;filename2&gt;,...}</code></p>
<p>需要注意的是，使用 <code>\include</code> 和 <code>\input</code>
命令<strong>载入的文件名最好不要加空格和特殊字符，也尽量避免使用中文名，否则很可能会出错</strong>。</p>
<p>最后介绍一个实用的工具宏包<code>syntonly</code>。加载这个宏包后，在导言区使用
<code>\syntaxonly</code> 命令，可令编译后不生成 DVI 或者 PDF
文档，只排查错误，编译速度会快不少：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\usepackage</span>&#123;syntonly&#125;</span><br><span class="line"><span class="keyword">\syntaxonly</span></span><br></pre></td></tr></table></figure>
<p>如果想生成文档，则用 <code>%</code> 注释掉 <code>\syntaxonly</code>
命令即可。</p>
<hr />
<h2 id="latex-和-tex-相关的术语和概念"><span
class="math inline">\(\LaTeX{}\)</span> 和 <span
class="math inline">\(\TeX{}\)</span> 相关的术语和概念</h2>
<p>在本章的最后有必要澄清几个概念：</p>
<ul>
<li><strong>引擎</strong>
全称为排版引擎，是编译源代码并生成文档的程序，如
<code>pdfTeX</code>、<code>XeTeX</code>等
。有时也称为<strong>编译器</strong>。</li>
<li><strong>格式</strong>
是定义了一组命令的代码集（类似于库函数）。<span
class="math inline">\(\LaTeX{}\)</span>
就是最广泛应用的一个格式，高德纳本人还编写了一个简单的 plain TeX
格式，没有定义诸如 <code>\documentclass</code> 和 <code>\section</code>
等命令。</li>
<li><strong>编译命令</strong> 是实际调用的、结合了引擎和格式的命令。如
<code>xelatex</code> 命令是结合 XeTeX引擎和 <span
class="math inline">\(\LaTeX{}\)</span>
格式的一个编译命令（类似于编译并链接库函数的过程）。</li>
</ul>
<p>常见的引擎、格式和编译命令的关系总结于下表。</p>
<table>
<colgroup>
<col style="width: 18%" />
<col style="width: 22%" />
<col style="width: 27%" />
<col style="width: 31%" />
</colgroup>
<thead>
<tr class="header">
<th></th>
<th>文档格式</th>
<th>plain TeX 格式</th>
<th>LaTeX 格式</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>TeX 引擎</td>
<td>DVI</td>
<td>tex</td>
<td>N/A</td>
</tr>
<tr class="even">
<td>pdfTeX 引擎</td>
<td>DVI</td>
<td>etex</td>
<td>latex</td>
</tr>
<tr class="odd">
<td></td>
<td>PDF</td>
<td>pdftex</td>
<td>pdflatex</td>
</tr>
<tr class="even">
<td>XeTeX 引擎</td>
<td>PDF</td>
<td>xetex</td>
<td>xelatex</td>
</tr>
<tr class="odd">
<td>LuaTeX 引擎</td>
<td>PDF</td>
<td>luatex</td>
<td>lualatex</td>
</tr>
</tbody>
</table>
<p><code>latex</code> 编译命令和 <span
class="math inline">\(\LaTeX{}\)</span> 格式往往容易混淆，在讨论关于
<span class="math inline">\(\LaTeX{}\)</span> 的时候需要明确。</p>
<p>在此介绍一下几个编译命令的基本特点：</p>
<table>
<colgroup>
<col style="width: 23%" />
<col style="width: 76%" />
</colgroup>
<thead>
<tr class="header">
<th>编译命令</th>
<th>解释</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>latex</code></td>
<td>虽然名为 <code>latex</code> 命令，底层调用的引擎其实是 pdfTeX。
该命令生成 dvi 格式的文档, 用 <code>dvipdfmx</code> 命令可以将其转为
pdf</td>
</tr>
<tr class="even">
<td><code>pdflatex</code></td>
<td>底层调用的引擎也是 pdfTeX，可以直接生成 pdf 格式的文档</td>
</tr>
<tr class="odd">
<td><code>xelatex</code></td>
<td>底层调用的引擎是 XeTeX，支持 UTF-8 编码和对 TrueType/OpenType
字体的调用。当前较为方便的<strong>中文排版</strong>解决方案基于
<code>xelatex</code></td>
</tr>
<tr class="even">
<td><code>lualatex</code></td>
<td>底层调用的引擎是 LuaTeX。这个引擎在 pdfTeX
引擎基础上发展而来，除了支持 UTF-8 编码和对 TrueType/OpenType
字体的调用外，还支持通过 Lua 语言扩展 <span
class="math inline">\(\TeX{}\)</span> 的功能。 <code>lualatex</code>
编译命令下的中文排版支持需要借助 <code>luatexja</code>宏包</td>
</tr>
</tbody>
</table>
<aside id="footnotes" class="footnotes footnotes-end-of-document"
role="doc-endnotes">
<hr />
<ol>
<li id="fn1"><p><code>https://www.latex-project.org</code><a
href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li
id="fn2"><p>以单个字符作为命令的参数时，可以不加括号。例如，在数学环境下，<code>\frac12</code>
和 <code>\frac{1\}\{2}</code> 的效果是一样的。<a href="#fnref2"
class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li
id="fn3"><p>个别命令在分组内仍然会产生全局作用，例如<code>\setcounter</code>
等命令。<a href="#fnref3" class="footnote-back"
role="doc-backlink">↩︎</a></p></li>
<li
id="fn4"><p>使用多个宏包时指定选项，相当于给每个宏包指定同样的选项。如果有某个宏包不能识别指定的选项，则会出错。<a
href="#fnref4" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn5"><p><span class="math inline">\(\LaTeX{}\)</span> 2020-10-01
版本之后允许添加扩展名。<a href="#fnref5" class="footnote-back"
role="doc-backlink">↩︎</a></p></li>
</ol>
</aside>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>lshort</tag>
      </tags>
  </entry>
  <entry>
    <title>lshort笔记：排除错误和寻求帮助（十）</title>
    <url>/2025/03/03/notes-reading/computer/lshort/lshort-10-error-help/</url>
    <content><![CDATA[<blockquote>
<p>本文内容来自 <span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">一份
(不太) 简短的 LATEX2ε 介绍<i class="fa fa-external-link-alt"></i></span>。</p>
</blockquote>
<p><span class="math inline">\(\LaTeX{}\)</span>
入门用户总会为两大问题头疼：我写的代码到底哪里出错了？如果想要实现某种用法该怎么办？本章首先总结了常见的
<span class="math inline">\(\LaTeX{}\)</span>
错误及应对办法，然后介绍了查找帮助文档的一些方法，最后简单总结了一些常用的宏包。</p>
<span id="more"></span>
<hr />
<h2 id="latex-错误"><span class="math inline">\(\LaTeX{}\)</span>
错误</h2>
<p>当用排版引擎编译 <span class="math inline">\(\LaTeX{}\)</span>
代码时，命令行的窗口（终端）会显示大量信息（TeXworks
等编辑器会有一个区域显示这些信息）。
当编译过程中出现错误时，信息将会停止在出错的地方，等待我们接下来的操作。</p>
<p>比如说我们有一个明显出错的例子：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\documentclass</span>&#123;article&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line">Test <span class="keyword">\LaTEx</span>&#123;&#125; and it&#x27;s friends.</span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure>
<p>编译过程中遇到这个错误将会停顿下来，提示错误，并等待用户输入指令：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line">! Undefined control sequence.</span><br><span class="line">l.3 Test <span class="keyword">\LaTEx</span></span><br><span class="line">             &#123;&#125; and it&#x27;s friends.</span><br></pre></td></tr></table></figure>
<p>这种错误信息分两部分，前一部分提示了错误的信息，后一部分指出了错误发生的行号，以及通过错落的文字告知发生错误的命令所在位置。如上错误显示
<code>\LaTEx</code> 位置发生了错误，错误信息是“未定义的控制序列”，意思是
<code>\LaTEx</code> 是 <span class="math inline">\(\TeX{}\)</span>
编译器无法识别的一个命令，很显然是把 <code>\LaTeX</code>
的大小写写错了。</p>
<h3 id="处理方式">处理方式</h3>
<p>出现错误时，编译过程将暂停，等待用户输入命令。用户可以直接敲回车跳过当前的错误，继续编译，相当于丢掉了写错的命令，将“Test
and it’s
friends.”排版出来。但这个例子过于简单，有些复杂的代码中，有可能会由于一个小问题导致一连串的错误。此时可以选择按
<span class="math inline">\(\textbf{S/R/Q}\)</span>
选择跳过接下来的所有错误，或者按 <span
class="math inline">\(\textbf{X}\)</span>
直接退出编译，将源代码中的错误修改后重新编译。</p>
<h3 id="常见的-latex-错误信息">常见的 <span
class="math inline">\(\LaTeX{}\)</span> 错误信息</h3>
<p>在此总结一些经常发生、问题比较明确的 <span
class="math inline">\(\LaTeX{}\)</span> 错误：</p>
<ul>
<li><p><span style="color:red;">! Undefined control
sequences.</span></p>
<p>使用了未定义的命令。拼写错误是原因之一（标点符号中英文不匹配）。也有可能是没有调用某个宏包，但用了该宏包定义的命令。</p></li>
<li><p><span style="color:red;">! LaTeX error: Environment …
undefined.</span></p>
<p>使用了未定义的环境。</p></li>
<li><p><span style="color:red;">! Missing $ inserted.</span></p>
<p>缺少数学环境的符号 <span
class="math inline">\(\texttt\$\)</span>。多由于将数学符号用在公式之外而导致。</p></li>
<li><p><span style="color:red;">Runaway argument? </span></p>
<p><span style="color:red;"> ! Paragraph ended before … was
complete.</span></p></li>
<li><p><span style="color:red;">! File ended while scanning
definition/use of …</span></p>
<p>这两个错误主要是由于漏写了包裹命令参数的花括号，导致识别参数时出现错误。许多编辑器的括号配对功能有助于检查和消除这类错误。</p>
<p>这类错误还有可能是由于前一次编译中断导致 <span
class="math inline">\(\texttt{.aux}\)</span>
等辅助文件不完整，再次编译读入不完整的文件产生错误。解决办法是删除辅助文件并重新编译。</p></li>
<li><p><span style="color:red;">! Extra alignment tab has been changed
to .</span></p></li>
<li><p><span style="color:red;">! Misplaced .</span></p>
<p>两个错误信息都与表格有关。</p>
<ul>
<li>前者的字面意义是“一行中使用的列分隔符 <span
class="math inline">\(\texttt\&amp;\)</span> 太多”，有时可能确实是 <span
class="math inline">\(\texttt\&amp;\)</span>
的个数和列格式不匹配，但多数情况是漏掉了行尾的 <code>\\</code>
命令。</li>
<li>后者常出现于漏掉了行尾的 <code>\\</code> 命令而接着使用
<code>\hline</code> 命令画横线的时候。</li>
</ul></li>
<li><p><span style="color:red;">! LaTeX Error: Lonely \item – perhaps a
missing list environment.</span></p></li>
<li><p><span style="color:red;">! LaTeX Error: Something’s wrong–perhaps
a missing \item.</span></p>
<p>两个错误信息都与列表环境和 <code>\item</code>
命令有关。前者意味着在没有使用列表环境的情况下用了
<code>\item</code>；后者则相反，是在列表环境中漏了
<code>\item</code>。</p></li>
<li><p><span style="color:red;">! I can’t find file `…’.</span></p></li>
<li><p><span style="color:red;">! LaTeX Error: File `…’ not
found.</span></p>
<p>两个错误都意味着缺少文件。</p>
<ul>
<li>如果使用 <code>\input</code> 或者 <code>\include</code>
命令添加文件，出现上述错误的原因当然是文件不存在或者文件名不对；</li>
<li>如果错误提示里的文件名带 <span
class="math inline">\(\texttt{.sty}\)</span> 或者 <span
class="math inline">\(\texttt{.cls}\)</span> 扩展名，那么很显然，是因为
<strong>没有安装所需的宏包或文档类</strong>。</li>
</ul></li>
<li><p><span style="color:red;">! LaTeX Error: Missing
\begin{document}.</span></p>
<p>字面上是缺少 <code>\begin{document}</code>，实际上往往是由于在
<code>\begin{document}</code>
之前（导言区）输入了文字或某些命令。</p></li>
<li><p><span style="color:red;">! LaTeX Error: Can be used only in
preamble.</span></p>
<p>与上一条相反，由于将必须用于导言区的命令在
<code>\begin{document}</code> 之后使用而产生。</p></li>
<li><p><span style="color:red;">! LaTeX Error:  begin{…} on input line
ended by \end{…}. </span></p>
<p>环境首尾不匹配。比如 <code>\begin{enumerate}</code> 用了
<code>\end{itemize}</code> 结尾。或者也可能是由于漏写了
<code>\begin</code> 或者 <code>\end</code> 命令。</p></li>
<li><p><span style="color:red;">! LaTeX Error: Option clash for package
`…’.</span></p>
<p>以 <strong>不同选项</strong>
重复调用宏包造成冲突。有可能是因为其它宏包内部事先调用了这个宏包，用户再次带选项调用而导致冲突。解决问题的办法是去掉重复调用的宏包。如果宏包允许的话，尽量使用其定义的命令改变设置，减少宏包选项的使用。</p></li>
<li><p><span style="color:red;">! LaTeX Error: Command … already
defined, </span></p>
<p><span style="color:red;">or name \end… illegal … </span></p>
<p>使用 <code>\newcommand</code> 或 <code>\newenvironment</code>
定义已有的命令/环境时产生的错误。如果自己确实作了定义，可考虑用
<code>\renewcommand</code> 或 <code>\renewenvironment</code>
定义；如果是宏包定义的命令产生了这个错误，则属于隐性的宏包冲突。</p>
<p>相比之前的“Option
clash”，隐性宏包冲突是更难以解决的问题，对各种宏包不熟悉的用户，尤其是使用模板的用户而言，往往难以下手。用户可尝试查找引起冲突的宏包的帮助文档。详尽的手册里通常会告知用户这个宏包应当在某个宏包的前面/后面调用，或者不能与某个宏包一起调用。
如果是模板调用了大量宏包导致冲突，可联系模板的作者解决。</p></li>
<li><p><span style="color:red;">! LaTeX Error: Unknown option `…' for
package `…’. </span></p>
<p>调用宏包时指定了不能被其识别的选项。此时应该查找宏包的帮助文档来解决问题。</p></li>
<li><p><span style="color:red;">! Package `…’ error: … </span></p>
<p>宏包或文档类自定义的错误，由于不正确地使用宏包里的命令而导致。此时应该查找宏包的帮助文档来解决问题。</p></li>
</ul>
<hr />
<h2 id="查找帮助文档">查找帮助文档</h2>
<p>无论是 <span class="math inline">\(\textrm{TeX Live}\)</span> 还是
<span
class="math inline">\(\textrm{MiKTeX}\)</span>，都提供了一个命令行模式的程序
<span class="math inline">\(\texttt{texdoc}\)</span>。比如对
<code>fancyhdr</code> 宏包感兴趣，这时在 Windows 命令提示符或者 Linux
终端输入以下命令，则会弹出宏包的帮助文档 <span
class="math inline">\(\texttt{fancyhdr.pdf}\)</span>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">texdoc fancyhdr</span><br></pre></td></tr></table></figure>
<p>除了宏包的帮助文档外，<span class="math inline">\(\TeX{}\)</span>
发行版还包括了各类有用的文档，有一部分在参考文献中给出。</p>
<p>对于初学者，有一个现实而棘手的问题：<strong>某个命令到底是 <span
class="math inline">\(\LaTeX{}\)</span>
自有的，还是哪个宏包提供的？</strong>
很遗憾地说，除了通过慢慢积累、熟悉较多宏包之外，没有很方便的办法解决这个问题，因为
<span class="math inline">\(\LaTeX{}\)</span> 的宏包实在太丰富了。</p>
<p>解决这个问题有几点可行的办法：</p>
<ol type="1">
<li><p>查询一些综述性的资料，如总结所有 <span
class="math inline">\(\LaTeX{}\)</span> 自带命令的文档 <a href="#fn1"
class="footnote-ref" id="fnref1"
role="doc-noteref"><sup>1</sup></a>、、<span
class="math inline">\(\LaTeX{}\)</span> 符号大全 <a href="#fn2"
class="footnote-ref" id="fnref2" role="doc-noteref"><sup>2</sup></a>
等；</p></li>
<li><p>在互联网上搜索自己不清楚的命令；</p></li>
<li><p>在论坛上提问求助有经验的人。</p></li>
</ol>
<hr />
<h2 id="常用宏包简介">常用宏包简介</h2>
<h3 id="文字公式和符号">文字、公式和符号</h3>
<table>
<colgroup>
<col style="width: 28%" />
<col style="width: 71%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">宏包</th>
<th style="text-align: left;">简介</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{amsmath}\)</span></td>
<td style="text-align: left;">AMS 数学公式扩展。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{mathtools}\)</span></td>
<td
style="text-align: left;">数学公式扩展宏包，提供了公式编号定制和更多的符号、矩阵等。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{amsfonts}\)</span></td>
<td style="text-align: left;">AMS 扩展符号的基础字体支持。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{amssymb}\)</span></td>
<td style="text-align: left;">在 <span
class="math inline">\(\texttt{amsfonts}\)</span> 基础上将 AMS
扩展符号定义成命令。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{bm}\)</span></td>
<td style="text-align: left;">提供将数学符号加粗的命令
<code>\bm</code>。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{unicode-math}\)</span></td>
<td style="text-align: left;">使用 Unicode 数学字体。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{nicematrix}\)</span></td>
<td style="text-align: left;">排版复杂矩阵。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{siunitx}\)</span></td>
<td style="text-align: left;">以国际单位规范排版物理量的单位。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{mhchem}\)</span></td>
<td style="text-align: left;">排版化学式和方程式。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{tipa}\)</span></td>
<td style="text-align: left;">排版国际音标。</td>
</tr>
</tbody>
</table>
<h3 id="排版元素">排版元素</h3>
<table>
<colgroup>
<col style="width: 26%" />
<col style="width: 73%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">宏包</th>
<th style="text-align: left;">简介</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{ulem}\)</span></td>
<td style="text-align: left;">提供排版可断行下划线的命令
<code>\uline</code> 以及其它装饰文字的命令。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{endnote}\)</span></td>
<td style="text-align: left;">排版尾注。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{marginnote}\)</span></td>
<td style="text-align: left;">改善的边注排版功能。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{multicol}\)</span></td>
<td style="text-align: left;">提供将内容自由分栏的 <span
class="math inline">\(\texttt{multicols}\)</span> 环境。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{multitoc}\)</span></td>
<td style="text-align: left;">生成多栏排版的目录。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{minitoc}\)</span></td>
<td style="text-align: left;">为章节生成独立的小目录。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{glossaries}\)</span></td>
<td style="text-align: left;">生成词汇表。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{verbatim}\)</span></td>
<td style="text-align: left;">对原始的 <span
class="math inline">\(\texttt{verbatim}\)</span> 环境的改善。提供了命令
<code>\verbatiminput</code> 调用源文件。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{fancyvrb}\)</span></td>
<td style="text-align: left;">提供了代码排版环境 <span
class="math inline">\(\texttt{Verbatim}\)</span>
以及对版式的自定义。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{listings}\)</span></td>
<td style="text-align: left;">提供了排版关键字高亮的代码环境 <span
class="math inline">\(\texttt{lstlisting}\)</span>
以及对版式的自定义。类似宏包有 <span
class="math inline">\(\texttt{minted}\)</span> 等。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{algorithmic}\)</span></td>
<td
style="text-align: left;">一个简单的实现算法排版的宏包。如果要生成浮动体的话，需要搭配
<span class="math inline">\(\texttt{algorithm}\)</span> 宏包使用。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{algorithm2e}\)</span></td>
<td
style="text-align: left;">较为复杂的、可定制的算法排版宏包。类似宏包有
<span class="math inline">\(\texttt{algorithmicx}\)</span> 等。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{amsthm}\)</span></td>
<td style="text-align: left;">定制定理环境。类似宏包包括 <span
class="math inline">\(\texttt{theorem}\)</span>、<span
class="math inline">\(\texttt{ntheorem}\)</span>、<span
class="math inline">\(\texttt{thmtools}\)</span> 等。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{mdframed}\)</span></td>
<td
style="text-align: left;">排版可自动断页的带边框文字段落，提供边框样式的定制功能。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{tcolorbox}\)</span></td>
<td style="text-align: left;">以 <span
class="math inline">\(\texttt{TikZ}\)</span>
为基础提供排版样式丰富的彩色盒子的功能。</td>
</tr>
</tbody>
</table>
<h3 id="图表和浮动体">图表和浮动体</h3>
<table>
<colgroup>
<col style="width: 25%" />
<col style="width: 74%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">宏包</th>
<th style="text-align: left;">简介</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{array}\)</span></td>
<td style="text-align: left;">对表格列格式的扩展。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{booktabs}\)</span></td>
<td style="text-align: left;">排版三线表。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{tabularx}\)</span></td>
<td style="text-align: left;">提供 <span
class="math inline">\(\texttt{tabularx}\)</span>
环境排版定宽表格，支持自动计算宽度的 <span
class="math inline">\(\texttt{X}\)</span> 列格式。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{arydshln}\)</span></td>
<td style="text-align: left;">支持排版虚线表格线。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{colortbl}\)</span></td>
<td style="text-align: left;">支持修改表格的行、列、单元格的颜色。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{multirow}\)</span></td>
<td style="text-align: left;">支持合并多行单元格。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{makecell}\)</span></td>
<td
style="text-align: left;">支持在单元格里排版多行内容（嵌套一个单列的小表格）。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{diagbox}\)</span></td>
<td style="text-align: left;">排版斜线表头。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{longtable}\)</span></td>
<td style="text-align: left;">提供排版跨页长表格的 <span
class="math inline">\(\texttt{longtable}\)</span> 环境。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{ltxtable}\)</span></td>
<td style="text-align: left;">为跨页长表格提供 <span
class="math inline">\(\texttt{tabularx}\)</span> 的 <span
class="math inline">\(\texttt{X}\)</span> 列格式。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{tabularray}\)</span></td>
<td style="text-align: left;">排版复杂表格（基于 <span
class="math inline">\(\LaTeX3\)</span> 实现）。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{graphicx}\)</span></td>
<td style="text-align: left;">支持插图。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{bmpsize}\)</span></td>
<td style="text-align: left;"><span class="math inline">\(\texttt{latex}
+ \texttt{dvipdfmx}\)</span> 命令下支持 BMP/JPG/PNG 等格式的位图。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{epstopdf}\)</span></td>
<td style="text-align: left;"><span
class="math inline">\(\texttt{pdflatex}\)</span> 命令下支持 EPS
格式的矢量图。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{wrapfig}\)</span></td>
<td style="text-align: left;">支持简单的文字在图片周围的绕排。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{caption}\)</span></td>
<td style="text-align: left;">控制浮动体标题的格式。类似宏包有 <span
class="math inline">\(\texttt{keyfloat}\)</span> 等。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{subcaption}\)</span></td>
<td style="text-align: left;">提供子图表和子标题的排版。类似宏包有 <span
class="math inline">\(\texttt{subfigure}\)</span> 和 <span
class="math inline">\(\texttt{subfig}\)</span> 等。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{bicaption}\)</span></td>
<td style="text-align: left;">生成双语浮动体标题。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{float}\)</span></td>
<td style="text-align: left;">为浮动体提供不浮动的 <span
class="math inline">\(\texttt{H}\)</span>
模式；提供自定义浮动体结构的功能。</td>
</tr>
</tbody>
</table>
<h3 id="修改版式">修改版式</h3>
<table>
<colgroup>
<col style="width: 26%" />
<col style="width: 73%" />
</colgroup>
<thead>
<tr class="header">
<th>宏包</th>
<th>简介</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><span class="math inline">\(\texttt{geometry}\)</span></td>
<td>修改页面尺寸、页边距、页眉页脚等参数。</td>
</tr>
<tr class="even">
<td><span class="math inline">\(\texttt{fancyhdr}\)</span></td>
<td>修改页眉页脚格式，令页眉页脚可以左对齐、居中、右对齐。</td>
</tr>
<tr class="odd">
<td><span class="math inline">\(\texttt{titlesec}\)</span></td>
<td>修改章节标题 <code>\chapter</code>、<code>\section</code>
等的格式。</td>
</tr>
<tr class="even">
<td><span class="math inline">\(\texttt{titletoc}\)</span></td>
<td>修改目录中各条目的格式。类似宏包有 <span
class="math inline">\(\texttt{tocloft}\)</span> 等。</td>
</tr>
<tr class="odd">
<td><span class="math inline">\(\texttt{tocbibind}\)</span></td>
<td>支持将目录、参考文献、索引本身写入目录项。</td>
</tr>
<tr class="even">
<td><span class="math inline">\(\texttt{footmisc}\)</span></td>
<td>修改脚注 <code>\footnote</code> 的格式。</td>
</tr>
<tr class="odd">
<td><span class="math inline">\(\texttt{indentfirst}\)</span></td>
<td>令章节标题后的第一段首行缩进。</td>
</tr>
<tr class="even">
<td><span class="math inline">\(\texttt{enumerate}\)</span></td>
<td>提供简单的自定义标签格式的 <span
class="math inline">\(\texttt{enumerate}\)</span> 环境。</td>
</tr>
<tr class="odd">
<td><span class="math inline">\(\texttt{enumitem}\)</span></td>
<td>修改列表环境 <span class="math inline">\(\texttt{enumerate}\)</span>
和 <span class="math inline">\(\texttt{itemize}\)</span> 等的格式。</td>
</tr>
<tr class="even">
<td><span class="math inline">\(\texttt{lettrine}\)</span></td>
<td>生成段落首字母大写的效果。</td>
</tr>
</tbody>
</table>
<aside id="footnotes" class="footnotes footnotes-end-of-document"
role="doc-endnotes">
<hr />
<ol>
<li id="fn1"><p>Karl Berry, Jim Hefferon, Vincent Belaiche. LaTeXe: An
unofficial reference manual. (texdoc latex2e)<a href="#fnref1"
class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn2"><p>Scott Pakin. The Comprehensive LaTeX Symbol List}.
(texdoc symbols-a4)<a href="#fnref2" class="footnote-back"
role="doc-backlink">↩︎</a></p></li>
</ol>
</aside>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>lshort</tag>
      </tags>
  </entry>
  <entry>
    <title>lshort笔记：参考文献（十一）</title>
    <url>/2025/03/09/notes-reading/computer/lshort/lshort-11-reference-books/</url>
    <content><![CDATA[<blockquote>
<p>本文内容来自 <span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">一份
(不太) 简短的 LATEX2ε 介绍<i class="fa fa-external-link-alt"></i></span>。</p>
</blockquote>
<p>本文推荐了一些 <span class="math inline">\(\LaTeX{}\)</span>
的经典文献书籍。</p>
<span id="more"></span>
<hr />
<h3 id="latex-经典书籍"><span class="math inline">\(\LaTeX{}\)</span>
经典书籍</h3>
<p>[1] Leslie Lamport. LaTeX: A Document Preparation System, 2nd
edition. Addison-Wesley, Reading, Massachusetts, 1994,
ISBN~0-201-52983-1.</p>
<p>[2] Frank Mittelbach, Michel Goossens, Johannes Braams, David
Carlisle, Chris Rowley. The LaTeX Companion, 2nd edition.
Addison-Wesley, Reading, Massachusetts, 2004, ISBN~0-201-36299-6.</p>
<p>[3] Michel Goossens, Sebastian Rahtz and Frank Mittelbach. The LaTeX
Graphics Companion, 2nd edition. Addison-Wesley, Reading, Massachusetts,
1997, ISBN~0-301-50892-0.</p>
<p>[4] Helmut Kopka and Patrick~W. Daly, Guide to LaTeX, 4th edition.
Addison-Wesley, Reading, Massachusetts, 2004, ISBN~0-321-17385-6.</p>
<p>[5] 刘海洋. LaTeX 入门. 电子工业出版社, 北京, 2013,
ISBN~978-7-121-20208-7.</p>
<hr />
<h3
id="tex-经典书籍介绍底层的命令和排版方式有志深入学习-latex-者或参与宏包开发者必读有一些书籍已经开放网络资源"><span
class="math inline">\(\TeX{}\)</span>
经典书籍，介绍底层的命令和排版方式，有志深入学习 <span
class="math inline">\(\LaTeX{}\)</span>
者或参与宏包开发者必读。有一些书籍已经开放网络资源</h3>
<p>[6] Donald~E. Knuth. The TeX book, Volume A of Computers and
Typesetting, 2nd edition. Addison-Wesley, Reading, Massachusetts, 1984,
ISBN~0-201-13448-9.</p>
<p>[7] Paul Abrahams, Kathryn A. Hargreaves, Karl Berry. <span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL0NUQU4vaW5mby9pbXBhdGllbnQvYm9vay5wZGY=">TeX
for the impatient, 2nd edition<i class="fa fa-external-link-alt"></i></span>. Addison-Wesley, Reading,
Massachusetts, 1984, ISBN~0-201-51375-7. (<strong>texdoc
impatient</strong>)</p>
<p>[8] Victor Eijkhout. <span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL0NUQU4vaW5mby90ZXhieXRvcGljL1RlWGJ5VG9waWMucGRm">TeX
by Topic, A TeX nician’s Reference<i class="fa fa-external-link-alt"></i></span>. Addison-Wesley, Reading,
Massachusetts, 1992, ISBN~0-201-56882-9. (<strong>texdoc
texbytopic</strong>)</p>
<hr />
<h3
id="tex-发行版内置的资源除宏包帮助手册之外的一些文档可以在本地使用-texttttexdoc-命令查找也可以在-ctan-上找到"><span
class="math inline">\(\TeX{}\)</span>
发行版内置的资源（除宏包帮助手册之外的一些文档），可以在本地使用 <span
class="math inline">\(\texttt{texdoc}\)</span> 命令查找，也可以在 CTAN
上找到</h3>
<p>[9] <span class="math inline">\(\LaTeX{}\)</span> Project Team.
LaTeXe for authors.(<strong>texdoc usrguide</strong>)</p>
<p>[10] <span class="math inline">\(\LaTeX{}\)</span> Project Team.
LaTeXe for class and package writers. (<strong>texdoc
clsguide</strong>)</p>
<p>[11] <span class="math inline">\(\LaTeX{}\)</span> Project Team.
LaTeXe font selection. (<strong>texdoc fntguide</strong>)</p>
<p>[12] Karl Berry, Jim Hefferon, Vincent Belaiche. LaTeXe: An
unofficial reference manual. (<strong>texdoc latex2e</strong>)</p>
<p>[13] Jurgen Fenn. An essential guide to LaTeXe usage: Obsolete
commands and packages (English translation). (<strong>texdoc
l2tabuen</strong>)</p>
<p>[14] Scott Pakin. The Comprehensive LaTeX Symbol List.
(<strong>texdoc symbols-a4</strong>)</p>
<p>[15] Winston Chang. LaTeXe Cheat Sheet. (<strong>texdoc
latexcheat</strong>)</p>
<hr />
<h3 id="ctan-上的其它网络资源">CTAN 上的其它网络资源</h3>
<p>[16] Stephen~G. Hartke. <span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL0NUQU4vaW5mby9GcmVlX01hdGhfRm9udF9TdXJ2ZXkvc3VydmV5LnBkZg==">A
Survey of Free Math Fonts for TeX and LaTeX<i class="fa fa-external-link-alt"></i></span>.</p>
<hr />
<h3 id="其它网络渠道可获得的资源">其它网络渠道可获得的资源</h3>
<p>[17] The TeX FAQ List. <span class="exturl" data-url="aHR0cHM6Ly90ZXhmYXEub3Jn">The TeX
Frequently Asked Question List<i class="fa fa-external-link-alt"></i></span>.</p>
<p>[18] learnlatex.org. <span class="exturl" data-url="aHR0cHM6Ly93d3cubGVhcm5sYXRleC5vcmc=">Learn LaTeX
online<i class="fa fa-external-link-alt"></i></span>.</p>
<p>[19] Palle Jorgensen. <span class="exturl" data-url="aHR0cHM6Ly90dWcub3JnL0ZvbnRDYXRhbG9ndWUv">The
LaTeX font catalogue, a font catalogue of LaTeX font packages<i class="fa fa-external-link-alt"></i></span>.</p>
<p>[20] Indian TeX users group. <span class="exturl" data-url="aHR0cHM6Ly93d3cudHVnLm9yZy90d2cvbWFjdGV4L3R1dG9yaWFscy9sdHhwcmltZXItMS4wLnBkZg==">LaTeX
Tutorials: A primer<i class="fa fa-external-link-alt"></i></span>.</p>
<p>[21] Overleaf. <span class="exturl" data-url="aHR0cHM6Ly93d3cub3ZlcmxlYWYuY29tL2xlYXJu">Overleaf
Documentation<i class="fa fa-external-link-alt"></i></span>.</p>
<p>[22] 黄新刚. <span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2h1YW5neGcvbG5vdGVzL3Jhdy9tYXN0ZXIvbG5vdGVzMi5wZGY=">雷太赫排版系统简介(LaTeX
Notes)<i class="fa fa-external-link-alt"></i></span>, 第二版. （旧版：<strong>texdoc
latex-notes-zh-cn</strong>）</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>lshort</tag>
      </tags>
  </entry>
  <entry>
    <title>lshort笔记：数学符号表</title>
    <url>/2025/01/24/notes-reading/computer/lshort/lshort-symbol-tables/</url>
    <content><![CDATA[<blockquote>
<p>本文内容来自<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">一份
(不太) 简短的 LATEX2ε 介绍<i class="fa fa-external-link-alt"></i></span>。</p>
</blockquote>
<p>本文介绍了在 <span class="math inline">\(\LaTeX{}\)</span>
中常用的符号。</p>
<span id="more"></span>
<hr />
<blockquote>
<p>注意事项：</p>
<ol type="1">
<li>蓝色的命令依赖 <code>amsmath</code> 宏包（非 <code>amssymb</code>
宏包）；</li>
<li>带有角标 <span class="math inline">\(\ell\)</span> 的符号命令依赖
<code>latexsym</code> 宏包。</li>
</ol>
</blockquote>
<h2 id="latex-普通符号"><span class="math inline">\(\LaTeX{}\)</span>
普通符号</h2>
<h3 id="文本数学模式通用符号">文本/数学模式通用符号</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120181959362.png" alt="image-20250120181959362" style="zoom:67%;" /></p>
<h3 id="希腊字母">希腊字母</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182031782.png" alt="image-20250120182031782" style="zoom:67%;" /></p>
<h3 id="二元关系符">二元关系符</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182111315.png" alt="image-20250120182111315" style="zoom:67%;" /></p>
<h3 id="二元运算符">二元运算符</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182201060.png" alt="image-20250120182201060" style="zoom:67%;" /></p>
<h3 id="巨算符">巨算符</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182228062.png" alt="image-20250120182228062" style="zoom:67%;" /></p>
<h3 id="数学重音符号">数学重音符号</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182250603.png" alt="image-20250120182250603" style="zoom:67%;" /></p>
<h3 id="箭头">箭头</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182315227.png" alt="image-20250120182315227" style="zoom:67%;" /></p>
<h3 id="作为重音的箭头符号">作为重音的箭头符号</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182339796.png" alt="image-20250120182339796" style="zoom:67%;" /></p>
<h3 id="定界符">定界符</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182407131.png" alt="image-20250120182407131" style="zoom:67%;" /></p>
<h3 id="用于行间公式的大定界符">用于行间公式的大定界符</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182438387.png" alt="image-20250120182438387" style="zoom:67%;" /></p>
<h3 id="其他符号">其他符号</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182501021.png" alt="image-20250120182501021" style="zoom:67%;" /></p>
<hr />
<h2 id="ams-符号">AMS 符号</h2>
<p>本小节所有符号依赖 <code>amssymb</code> 宏包。</p>
<h3 id="ams希腊字符和希伯来字母">AMS希腊字符和希伯来字母</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182624893.png" alt="image-20250120182624893" style="zoom:67%;" /></p>
<h3 id="ams-二元关系符">AMS 二元关系符</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182650748.png" alt="image-20250120182650748" style="zoom:67%;" /></p>
<h3 id="ams-二元运算符">AMS 二元运算符</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182713010.png" alt="image-20250120182713010" style="zoom:67%;" /></p>
<h3 id="ams-箭头">AMS 箭头</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182742368.png" alt="image-20250120182742368" style="zoom:67%;" /></p>
<h3 id="ams-反义二元关系符和箭头">AMS 反义二元关系符和箭头</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120182804491.png" alt="image-20250120182804491" style="zoom:67%;" /></p>
<h3 id="ams-定界符">AMS 定界符</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120204824904.png" alt="image-20250120204824904" style="zoom:67%;" /></p>
<h3 id="ams-其他符号">AMS 其他符号</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250120204936578.png" alt="image-20250120204936578" style="zoom:67%;" /></p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>lshort</tag>
      </tags>
  </entry>
  <entry>
    <title>Programming Windows 笔记：Text Output（四）</title>
    <url>/2026/03/21/notes-reading/computer/programming-windows/win32-04-text-output/</url>
    <content><![CDATA[<blockquote>
<p>本文为《Programming Windows, Fifth Edition》（Charles Petzold
著）第4章 “Text Output” 的学习笔记总结。</p>
</blockquote>
<span id="more"></span>
<hr />
<h2 id="wm_paint-消息">WM_PAINT 消息</h2>
<p>大多数 Windows 程序在 WinMain
函数初始化过程中会在进入消息循环之前调用 <code>UpdateWindow</code>
函数。 Windows 利用这个机会向窗口过程发送最初的 <code>WM_PAINT</code>
消息。这个消息通知窗口过程绘制客户区。此后，窗口过程需要在几乎任何时间都能够处理新收到的
<code>WM_PAINT</code>
消息，而且能够在必要时重绘整个客户区。在以下任何一个事件发生时，窗口过程都会收到一条
<code>WM_PAINT</code> 消息：</p>
<ul>
<li>用户移动一个窗口，导致原来被遮盖的部分窗口暴露出来。</li>
<li>用户调整窗口的大小（当窗口的类型设定为 <code>CS_HREDRAW</code> 和
<code>CS_VREDRAW</code> 值时）。</li>
<li>程序调用 <code>ScrollWindow</code> 或 <code>ScrollDC</code>
函数滚动客户区。</li>
<li>程序调用 <code>InvalidateRect</code> 或 <code>InvalidateRgn</code>
函数显式生成 <code>WM_PAINT</code> 消息。</li>
</ul>
<p>处理 <code>WM_PAINT</code>
消息需要你改变对于视频输出的概念。<strong>你的程序应该收集并保存所有用于绘制客户区的信息</strong>，但是只在需要时才进行绘制——当收到
<code>WM_PAINT</code> 消息时。如果程序需要在其他时候更新客户区，可以强制
Windows 生成 <code>WM_PAINT</code>
消息。这看上去貌似绕了一个弯，但是程序的结构会因此而受益。</p>
<hr />
<h2 id="获取设备环境句柄">获取设备环境句柄</h2>
<p>Windows
应用程序需要在屏幕上绘图时，通常有两种方法获取设备环境句柄。</p>
<h3 id="方法一beginpaint-和-endpaint">方法一：BeginPaint 和
EndPaint</h3>
<p>通常，处理 WM PAINT 消息的代码如下所示：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> WM_PAINT:</span><br><span class="line">  hdc = BeginPaint(hwnd, &amp;ps);</span><br><span class="line">  <span class="comment">//[ 使用GDI函数]</span></span><br><span class="line">  EndPaint(hwnd, &amp;ps);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br></pre></td></tr></table></figure>
<p><code>hwnd</code>
是窗口的句柄，这是窗口消息处理过程的参数，<code>ps</code> 是一个类型为
<code>PAINTSTRUCT</code> 结构的变量的地址。 函数的返回值
<code>hdc</code> 就是设备环境句柄。</p>
<p>在处理 <code>WM_PAINT</code> 消息时，窗口过程首先调用
<code>BeginPaint</code>
函数。这个函数通常会<strong>擦去无效区域的背景</strong>以便绘图。它同时还会填充
<code>ps</code> 结构的各个字段。</p>
<h3 id="绘制信息结构-paintstruct">绘制信息结构 PAINTSTRUCT</h3>
<p>Windows 将为每个窗口维护一个绘制信息结构
<code>PAINTSTRUCT</code>。定义如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">tagPAINTSTRUCT</span> &#123;</span> <span class="comment">// ps</span></span><br><span class="line">    HDC hdc;</span><br><span class="line">    BOOL fErase;</span><br><span class="line">    RECT rcPaint;</span><br><span class="line">    BOOL fRestore;</span><br><span class="line">    BOOL fIncUpdate;</span><br><span class="line">    BYTE rgbReserved[<span class="number">32</span>];</span><br><span class="line">&#125; PAINTSTRUCT;</span><br></pre></td></tr></table></figure>
<p>当程序调用 BeginPaint 函数时， Windows
将自动填充这个结构中的字段。程序只能够使用前三个字段，其他的供 Windows
内部使用。 <code>hdc</code> 字段是设备环境句柄。 BeginPaint
函数的返回值就是这个设备环境句柄的值（这里表现了 Windows
典型的冗余性）。在大多数情况下， <code>fFrase</code> 字段将被设置为
<code>FALSE</code>。这意味着 Windows 在先前的 BeginPaint
函数中己经擦除了无效区域的背景。（如果想在窗口过程中自定义背景擦除方式，必须自己处理
<code>WM_ERASEBKGND</code> 消息。）在 <code>WINMAIN</code>
初始化时，用于注册窗口类的 <code>WNDCLASS</code> 结构中的
<code>hbrBackground</code> 字段指定了一个画刷， Windows
就使用这个画刷来擦除背景。</p>
<p>但是，当你的程序通过调用 <code>InvalidateRect</code>
函数使客户区中的一个矩形无效时， <code>InvalidateRect</code>
函数的最后一个参数将指定背景是否要被擦除。如果参数是
<code>FALSE</code>，Windows 将不会擦除背景，同时在调用
<code>BeginPaint</code> 函数后， <code>PAINTSTRUCT</code> 结构的
<code>fErase</code> 字段的值将是 <code>TRUE</code>。</p>
<p>即如果 <code>InvalidateRect</code>
函数的最后一个参数为0，不擦除背景，<code>fErase</code>
填充为1。函数的最后一个参数为1，擦除背景，<code>fErase</code>
填充为0。</p>
<p>结构中的 <code>rcPaint</code>
矩形不仅仅是无效矩形，而且还是一个“裁剪”(Clipping) 矩形。这意味着
Windows 将把绘制限制在这个矩形里。更确切地说，如果无效区域不是一个矩形，
Windows 将把绘制限制在这个区域内。</p>
<h3 id="方法二getdc-和-releasedc">方法二：GetDC 和 ReleaseDC</h3>
<p>尽管最好让程序在处理 <code>WM_PAINT</code>
消息时才更新整个客户区，但有时也会发现在处理非 <code>WM_PAINT</code>
消息时绘制部分客户区也是很有用的。有些时候，还需要设备环境句柄用作其他用途，例如获取设备环境的信息。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">hdc = GetDC(hwnd);</span><br><span class="line"><span class="comment">//[使用GDI函数]</span></span><br><span class="line">ReleaseDC(hwnd, hdc);</span><br></pre></td></tr></table></figure>
<p><code>GetDC</code>
返回的设备环境句枘中的裁剪矩形是整个客户区。这意味着你可以在客户区的任意部分绘制，而不仅仅是在无效矩形里，也就是说如果不存在无效矩形也没有关系。与
<code>BeginPaint</code> 不同， <code>GetDC</code>
不会将无效区域有效化。如果需要将整个客户区有效化，可以像下面这样调用函数：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">VaIidateRect (hwnd, <span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure>
<p>通常， <code>GetDC</code> 和 <code>ReleaseDC</code>
函数用于处理键盘消息（例如在字处理程序中）或者鼠标消息（例如绘图程序中）。使用这两个函数，程序可以在收到用户的鍵盘或鼠标输入时及时地绘制客户区，而不必为了生成
<code>WM_PAINT</code>
消息去刻意使客户区的一部分无效化。但是，即使程序在处理非
<code>WM_PAINT</code>
消息时进行了绘制，它<strong>仍然必须收集足够的信息</strong>以便在收到
<code>WM_PAINT</code> 时能更新显示。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>Programming Windows</tag>
      </tags>
  </entry>
  <entry>
    <title>C语言之动态内存分配与释放</title>
    <url>/2024/01/12/programming/languages/C/c-dynamic-memory-allocation-free/</url>
    <content><![CDATA[<p>本文首先介绍了通用指针类型 <code>void</code>
的特点，接着给出了在堆上动态分配内存空间主要依赖的三个函数（<code>malloc</code>、<code>calloc</code>和
<code>realloc</code>）和内存释放函数 <code>free</code>
的使用方法和注意事项。</p>
<span id="more"></span>
<hr />
<h2 id="通用指针类型void">通用指针类型void</h2>
<p>通用类型指针具有以下特点：</p>
<p><strong>类型无关，赋值灵活：</strong>
由于指针本质上是一个存储内存地址的变量，而内存地址是没有类型的，所以
<code>void</code>
指针可以存储任意类型数据的地址，指向任意类型对象。无论是整数、浮点数、字符或数组、结构体等类型都可以用
<code>void</code>
指针指向。表现在代码中就是：<strong>可以将任意类型指针(地址)赋值给
<code>void</code> 指针，这个过程一般是没有风险的。</strong>
如下列代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">42</span>;</span><br><span class="line"><span class="type">void</span>* void_ptr = &amp;a;</span><br><span class="line"></span><br><span class="line"><span class="comment">// void指针转换成其它类型指针,在C++语法中必须加上显式类型转换说明</span></span><br><span class="line"><span class="comment">// 但C语言支持void指针隐式类型转换成各种其它指针类型，所以这个强转语法可加可不加</span></span><br><span class="line"><span class="comment">// float* float_ptr = (float*)void_ptr;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 错误的类型转换</span></span><br><span class="line"><span class="type">float</span>* float_ptr = void_ptr;</span><br><span class="line"><span class="comment">// 解引用产生未定义行为</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%f\n&quot;</span>, *float_ptr);</span><br></pre></td></tr></table></figure>
<p>关于通用指针类型转换成其它指针类型，C语言和C++在语法上会有明显差别：</p>
<ul>
<li>C语言更灵活允许隐式的类型转换，所以强转语法可加可不加。</li>
<li>C++则不支持这类隐式类型转换，必须要加上强转的语法，否则编译无法通过。</li>
</ul>
<p>在C语言中，想要在堆上动态分配内存空间，主要依赖三个函数来完成，它们都声明在头文件<code>&lt;stdlib.h&gt;</code>当中：</p>
<ol type="1">
<li><code>malloc</code></li>
<li><code>calloc</code></li>
<li><code>realloc</code></li>
</ol>
<hr />
<h2 id="内存分配函数malloc">内存分配函数malloc</h2>
<p><strong>函数全名：</strong> memory allocation</p>
<p><strong>函数声明：</strong>
<code>void* malloc(size_t size);</code></p>
<p>函数作用：</p>
<ul>
<li><strong>此函数会在堆空间上分配一片连续的，size个字节大小的内存块。</strong></li>
<li><strong>此函数不会对内存块中的数据进行初始化，内存块中的数据是随机未定义的。</strong></li>
</ul>
<p><strong>函数返回值：</strong></p>
<ul>
<li>如果分配成功，此函数会返回指向该
<strong>内存块地址(首字节地址)</strong>
的指针。<strong>注意返回的指针类型是void指针，在操作之前需要进行转换。</strong></li>
<li>如果分配失败，此函数会返回一个空指针(NULL)。</li>
</ul>
<h3 id="内存泄漏">内存泄漏</h3>
<p>以往我们创建数组，创建结构体都是在栈上完成的，它们是栈数组、栈结构体，它们的内存空间都是由栈自动管理完成的。</p>
<p>但使用动态内存分配函数创建的数组、结构体都被存储在堆上，栈上存储的只不过是它的指针，如下图所示：</p>
<div data-align="center">
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/202311091105358.png" alt="栈上指针指向堆上内存区域-示意图" style="zoom: 33%;" />
</div>
<p>堆上存储的数据由程序员手动管理生命周期，手动申请内存资源，也需要手动释放内存空间。</p>
<p><strong>内存泄漏是指程序在运行过程中，未能适时释放不再使用的内存区域，导致这部分内存在程序的生命周期内始终无法被重用。</strong></p>
<p>内存泄漏在短时间内可能对程序而言，不是巨大的、致命的风险，但：长时间运行或频繁执行的程序中如果存在内存泄漏，随着时间的推移，被泄漏的内存累积会越来越多，最终可能导致程序运行缓慢甚至崩溃，特别是在内存有限的系统中。</p>
<hr />
<h2 id="内存释放函数free">内存释放函数free</h2>
<p>为了避免内存泄漏，在确定动态分配的内存不再使用后，要及时调用
<code>free</code> 函数释放它。</p>
<p><strong>函数声明：</strong> <code>void free(void *ptr);</code></p>
<p><strong>函数参数：</strong>
必须是堆上申请内存块的地址(<strong>首字节地址</strong>)，不能传递别的指针，否则会引发未定义行为。</p>
<p><strong>函数功能：</strong></p>
<ol type="1">
<li><strong><code>free</code>
函数并不会修改它所释放的内存区域中存储的任何数据。</strong>
<code>free</code>
的作用仅仅是告诉操作系统这块内存不再被使用了，可以将其标记为可用状态，以供将来的内存分配请求使用。</li>
<li>释放后的内存区域中的数据一般仍然会继续存在，直到被下一次的内存分配操作覆盖。当然即便free前的原始数据一直存在未被覆盖，这片内存区域也不再可用了，因为你不知道什么时候数据就会被覆盖掉了。</li>
<li><strong>free函数不会修改传入指针指向的内容，更不会对实参指针本身做任何修改。</strong></li>
</ol>
<p><code>free</code> 函数调用后，指针指向的内存块就被释放了。但
<strong>free函数不会改变传入的实参指针本身</strong>，所以free后的实参指针就变成了指向一片已释放区域的指针。这就是
<strong>“悬空指针”</strong>，悬空指针是野指针的一种特例，使用悬空指针同样会引发未定义行为。</p>
<p><strong>为了避免悬空指针为程序安全带来隐患，推荐在free掉指针指向的内存块后，及时将指针置为空指针。</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 伪代码,p是一个指针类型</span></span><br><span class="line">p = <span class="built_in">malloc</span>(...);</span><br><span class="line"><span class="built_in">free</span>(p);</span><br><span class="line">p = <span class="literal">NULL</span>;</span><br></pre></td></tr></table></figure>
<p><strong>总结：</strong></p>
<ol type="1">
<li><p><strong>正确传参free函数。</strong> <code>free</code>
函数需要传入指向动态分配内存块首字节的指针，free之前不妨检查指针是否已被移动。</p></li>
<li><p><strong>在free内存块后，建议立刻将指针设置为NULL。</strong>
这样可以规避一些常见的问题：</p>
<ul>
<li>避免了”double free”的风险。对空指针调用 <code>free</code>
函数是安全的，它不会有任何效果。</li>
<li>减少悬空指针出现的风险。解引用空指针导致程序崩溃，比悬空指针带来的未定义行为要更容易检测和修正。</li>
</ul></li>
<li><p><strong>慎重改变堆区指针的指向。</strong>
指向堆区域的指针，如果需要改变它的指向，在改变之前应当考虑指向的内存块是否需要free。</p></li>
<li><p><strong>多函数共同管理同一块内存区域时，应严格遵循单一原则。</strong>
尤其是，哪个函数用于分配内存，哪个函数用于free释放内存，这两个函数一定要明确单一的职责。</p></li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在堆上动态分配内存拼接两个字符串</span></span><br><span class="line"><span class="type">char</span>* <span class="title function_">dynamic_strcat</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* prefix, <span class="type">const</span> <span class="type">char</span>* suffix)</span> &#123;</span><br><span class="line"><span class="comment">// 计算拼接后字符串的长度</span></span><br><span class="line"> <span class="type">int</span> new_str_len = <span class="built_in">strlen</span>(prefix) + <span class="built_in">strlen</span>(suffix);</span><br><span class="line"><span class="type">char</span> *new_str = <span class="built_in">malloc</span>(new_str_len + <span class="number">1</span>);    <span class="comment">// char在各平台上长度都是1，所以不用乘了</span></span><br><span class="line"></span><br><span class="line"> <span class="keyword">if</span> (new_str == <span class="literal">NULL</span>) &#123;</span><br><span class="line">     <span class="built_in">printf</span>(<span class="string">&quot;ERROR: malloc failed in dynamic_strcat!\n&quot;</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line"> &#125;</span><br><span class="line"><span class="comment">// 长度是精确计算得出的，不用担心越界访问</span></span><br><span class="line"> <span class="built_in">strcat</span>(<span class="built_in">strcpy</span>(new_str, prefix), suffix);</span><br><span class="line"><span class="keyword">return</span> new_str;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line"> <span class="type">char</span> str1[] = <span class="string">&quot;hello&quot;</span>;</span><br><span class="line"><span class="type">char</span> str2[] = <span class="string">&quot; world!&quot;</span>;</span><br><span class="line"> <span class="type">char</span>* result_str = dynamic_strcat(str1, str2);  <span class="comment">// 注意只要涉及动态内存分配，一律用指针类型。这里不能用数组类型</span></span><br><span class="line"><span class="built_in">puts</span>(result_str);</span><br><span class="line"> <span class="comment">// 现在不再使用result字符串了，不要忘记free它</span></span><br><span class="line"><span class="built_in">free</span>(result_str);</span><br><span class="line"> <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="清零内存分配函数calloc">清零内存分配函数calloc</h2>
<p><strong>函数全名：</strong>cleared
allocation，该函数的最大特点是分配内存空间时会<strong>自动初始化0值</strong>。</p>
<p><strong>函数声明：</strong>
<code>void* calloc(size_t num, size_t size);</code>。</p>
<p><strong>函数参数：</strong></p>
<ol type="1">
<li><code>num</code> 表示要分配的元素数量</li>
<li><code>size</code> 表示每个元素的内存大小</li>
</ol>
<p>此函数也会在堆空间上分配一片连续的内存空间，但不同的是，它基于元素的个数以及每个元素的大小来进行内存分配，所以calloc常用于在堆上分配<strong>数组</strong>的内存空间。</p>
<p><strong>函数返回值：</strong>
返回值在分配成功和失败时，和malloc是一致的。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> _CRT_SECURE_NO_WARNINGS</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line"> <span class="comment">// 分配一个长度为10的整数数组</span></span><br><span class="line"> <span class="type">int</span> len = <span class="number">10</span>;</span><br><span class="line"> <span class="type">int</span> *arr = <span class="built_in">calloc</span>(len, <span class="keyword">sizeof</span>(<span class="type">int</span>));</span><br><span class="line"></span><br><span class="line"> <span class="type">int</span>* p = arr;</span><br><span class="line"> <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; len; i++)&#123;</span><br><span class="line">     <span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, *p++);   <span class="comment">// 此时数组中的元素都具有0值，而不是随机未定义的</span></span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 使用完毕，不要忘记free</span></span><br><span class="line"> <span class="built_in">free</span>(arr);</span><br><span class="line"> <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>总得来说，推荐在<strong>动态分配数组内存空间</strong>时，尤其是需要将内存空间初始化为0值时，<strong>使用calloc函数</strong>。</p>
<hr />
<h2 id="内存重分配函数realloc">内存重分配函数realloc</h2>
<p><strong>函数全名：</strong> reallocation，表示内存重新分配。</p>
<p><strong>函数声明：</strong>
<code>void* realloc(void* ptr, size_t new_size);</code></p>
<p><strong>函数参数：</strong></p>
<ol type="1">
<li><code>ptr</code>：指向原来已分配内存的内存块。</li>
<li><code>new_size</code>：新的内存块大小。</li>
</ol>
<p><strong>函数功能：</strong></p>
<p>该函数根据参数取值的不同，可能表现为 <code>malloc</code> 或
<code>free</code> 函数的行为：</p>
<ol type="1">
<li><strong>如果ptr指针是一个空指针，那么该函数的行为和malloc一致——分配new_size字节的内存空间，并且返回该内存块的指针。</strong></li>
<li><strong>如果new_size的取值为0，那么该函数的行为就是free函数，会释放ptr指向的内存块。</strong></li>
</ol>
<p>如果没有出现上述两种特殊情况，<code>realloc</code>
用于重新调整已分配内存块的大小(也就是ptr指针指向的已分配内存块的大小)：</p>
<ol type="1">
<li>当new_size的取值和已分配的内存块大小一致时，此函数不会做任何操作。</li>
<li>当new_size的取值比已分配的内存块小时，会在旧内存块的
<strong>尾部(高地址)</strong>
截断，被截断抛弃的内存块会被自动释放。</li>
<li>当new_size的取值比已分配的内存块大时(新内存块比旧内存块大时)，会尽可能地尝试原地扩大旧内存块(这样效率高)；</li>
<li>如果无法原地进行扩大，则会在<strong>别处</strong>申请空间分配new_size大小的新内存块，并将旧内存块中的数据全部复制进去后，将<strong>旧内存块自动释放</strong>。</li>
<li>不管采用哪种方式扩展旧内存块，<strong>新扩展部分</strong>的内存区域都不会初始化，仍只<strong>具有随机值</strong>。</li>
</ol>
<p><strong>函数返回值：</strong></p>
<p>如果realloc函数分配内存空间成功，它会返回指向新内存块的指针，<strong>若失败，仍会返回空指针，且不会改变旧内存块。</strong></p>
<p>总之，realloc函数适用于调整已分配内存块的大小，特别是在<strong>动态数组或数据结构的大小需要在程序运行时增加或减少时</strong>使用。</p>
<p><strong>惯用法：</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// p和arr_p指针类型一致</span></span><br><span class="line">p = <span class="built_in">realloc</span>(arr_p, new_size);</span><br><span class="line"><span class="keyword">if</span> (p == <span class="literal">NULL</span>)&#123;</span><br><span class="line">   <span class="comment">// 分配失败处理</span></span><br><span class="line">   <span class="comment">// 退出</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 代码运行到这里，p != NULL，realloc分配内存成功</span></span><br><span class="line">arr_p = p;</span><br></pre></td></tr></table></figure>
<p>这样写既避免了 <code>arr_p</code> 成为悬空指针，也不会因为
<code>realloc</code> 分配失败导致内存泄漏。</p>
]]></content>
      <categories>
        <category>编程语言（Languages）</category>
        <category>C</category>
      </categories>
      <tags>
        <tag>动态内存</tag>
      </tags>
  </entry>
  <entry>
    <title>C语言之文件流常用标准库函数</title>
    <url>/2024/01/18/programming/languages/C/c-file-stream-stdlib-func/</url>
    <content><![CDATA[<p>本文总结了在C语言中与文件流相关的标准库函数。</p>
<span id="more"></span>
<hr />
<h2 id="文件流常用标准库函数">文件流常用标准库函数</h2>
<p>使用下列文件流相关函数需要包含头文件<code>stdio.h</code>。</p>
<div data-align="center">
<table>
<thead>
<tr>
<th style="text-align:left">
</th>
<th style="text-align:left">
函数
</th>
<th style="text-align:left">
作用
</th>
<th style="text-align:left">
注意事项
</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left" rowspan="2">
打开或关闭文件流
</td>
<td style="text-align:left">
<code>fopen</code>
</td>
<td style="text-align:left">
打开文件，返回指向FILE的指针
</td>
<td style="text-align:left">
需要指定模式（如“r”, “w”, “a”等），并处理可能的文件打开错误
</td>
</tr>
<tr>
<td style="text-align:left">
<code>fclose</code>
</td>
<td style="text-align:left">
关闭文件流，释放流资源
</td>
<td style="text-align:left">
确保每个打开的文件流最终都被关闭，避免资源泄露
</td>
</tr>
<tr>
<td style="text-align:left" rowspan="2">
读写二进制文件流
</td>
<td style="text-align:left">
<code>fread</code>
</td>
<td style="text-align:left">
从文件流中读取数据
</td>
<td style="text-align:left">
需要检查返回值以确定读取的数据量和是否遇到错误或EOF
</td>
</tr>
<tr>
<td style="text-align:left">
<code>fwrite</code>
</td>
<td style="text-align:left">
向文件流中写入数据
</td>
<td style="text-align:left">
需要检查返回值以确保数据正确写入
</td>
</tr>
<tr>
<td style="text-align:left" rowspan="2">
逐字符读写文本文件流
</td>
<td style="text-align:left">
<code>fgetc</code>
</td>
<td style="text-align:left">
从文件流中读取下一个字符
</td>
<td style="text-align:left">
在达到文件末尾或出现错误时返回EOF
</td>
</tr>
<tr>
<td style="text-align:left">
<code>fputc</code>
</td>
<td style="text-align:left">
将一个字符写入文件流
</td>
<td style="text-align:left">
返回写入的字符，若出现错误则返回EOF
</td>
</tr>
<tr>
<td style="text-align:left" rowspan="2">
逐行读写文本文件流
</td>
<td style="text-align:left">
<code>fgets</code>
</td>
<td style="text-align:left">
从文件流中读取字符串（一行）
</td>
<td style="text-align:left">
确保正确处理缓冲区长度和行尾字符
</td>
</tr>
<tr>
<td style="text-align:left">
<code>fputs</code>
</td>
<td style="text-align:left">
将字符串（不包括NUL终止符）写入文件流
</td>
<td style="text-align:left">
不会自动添加换行符，需手动处理
</td>
</tr>
<tr>
<td style="text-align:left" rowspan="2">
格式化读写文本文件流
</td>
<td style="text-align:left">
<code>fscanf</code>
</td>
<td style="text-align:left">
从文件流中格式化读取数据
</td>
<td style="text-align:left">
与<code>scanf</code>类似，但从文件流读取
</td>
</tr>
<tr>
<td style="text-align:left">
<code>fprintf</code>
</td>
<td style="text-align:left">
格式化输出到文件流
</td>
<td style="text-align:left">
类似于<code>printf</code>，但输出到文件流
</td>
</tr>
<tr>
<td style="text-align:left" rowspan="2">
文件流状态检查
</td>
<td style="text-align:left">
<code>feof</code>
</td>
<td style="text-align:left">
检查文件流的EOF标志
</td>
<td style="text-align:left">
通常用于循环读取数据时判断文件是否结束
</td>
</tr>
<tr>
<td style="text-align:left">
<code>ferror</code>
</td>
<td style="text-align:left">
检查文件流的错误指示器
</td>
<td style="text-align:left">
用于错误处理和调试
</td>
</tr>
<tr>
<td style="text-align:left" rowspan="3">
文件定位
</td>
<td style="text-align:left">
<code>ftell</code>
</td>
<td style="text-align:left">
返回文件流的当前文件位置指示器
</td>
<td style="text-align:left">
配合<code>fseek</code>可实现文件的随机访问
</td>
</tr>
<tr>
<td style="text-align:left">
<code>fseek</code>
</td>
<td style="text-align:left">
设置文件流的文件位置指示器
</td>
<td style="text-align:left">
可用于移动文件指针到特定位置
</td>
</tr>
<tr>
<td style="text-align:left">
<code>rewind</code>
</td>
<td style="text-align:left">
将文件流的文件位置指示器重置到文件开头
</td>
<td style="text-align:left">
等同于<code>fseek(stream, 0, SEEK_SET)</code>
</td>
</tr>
<tr>
<td style="text-align:left">
刷新缓冲区
</td>
<td style="text-align:left">
<code>fflush</code>
</td>
<td style="text-align:left">
刷新输出缓冲区到文件，丢弃输入缓冲区内容
</td>
<td style="text-align:left">
参数为NULL时，刷新所有打开的文件流
</td>
</tr>
</tbody>
</table>
</div>
]]></content>
      <categories>
        <category>编程语言（Languages）</category>
        <category>C</category>
      </categories>
      <tags>
        <tag>标准库</tag>
        <tag>文件流</tag>
      </tags>
  </entry>
  <entry>
    <title>C语言之如何定义一个数据类型</title>
    <url>/2024/05/05/programming/languages/C/c-how-to-define-data-type/</url>
    <content><![CDATA[<p>本文介绍了如何设计和定义一个新的数据类型，具体包括建立抽象、建立接口和实现接口三个部分。总结这三步法：从思考“做什么”（抽象）到规定“怎么做才对”（接口），最后才是“怎么做到”（实现），这是编写健壮、清晰、可维护代码的基石。</p>
<span id="more"></span>
<hr />
<h2 id="引言">引言</h2>
<p>设计一种数据类型包括设计如何储存该数据类型（属性）和设计一系列管理该数据的函数（操作）。</p>
<p>计算机科学领域已开发了一种定义新类型的好方法，用3个步骤完成从抽象到具体的过程。</p>
<blockquote>
<ol type="1">
<li>提供类型属性和相关操作的抽象描述。这些描述既不能依赖特定的实现，也不能依赖特定的编程语言。这种正式的抽象描述被称为抽象数据类型（ADT）。</li>
<li>开发一个实现 ADT
的编程接口。也就是说，指明如何储存数据和执行所需操作的函数。例如在C中，可以提供结构定义和操控该结构的函数原型。这些作用于用户定义类型的函数相当于作用于C基本类型的内置运算符。需要使用该新类型的程序员可以使用这个接口进行编程。</li>
<li>编写代码实现接口。这一步至关重要，但是使用该新类型的程序员无需了解具体的实现细节。</li>
</ol>
</blockquote>
<p>抽象数据类型以面向问题而不是面向语言的方式，把解决问题的方法和数据表示结合起来。设计一个ADT后，可以在不同的环境中复用。理解ADT可以为将来学习面向对象程序设计（OOP）以及C++语言做好准备。</p>
<hr />
<h2 id="建立抽象">建立抽象</h2>
<p>以链表为例，非正式但抽象的链表定义是：链表是一个能储存一系列项且可以对其进行所需操作的数据对象。该定义既未说明链表中可以储存什么项，也未指定是用数组、结构还是其他数据形式来储存项，而且并未规定用什么方法来实现操作（如，查找链表中元素的个数）。这些细节都留给实现完成。</p>
<p>为了让示例尽量简单，一种简化的链表类型总结如下：</p>
<blockquote>
<p>类型名： 简单链表</p>
<p>类型属性： 可以储存一系列项</p>
<p>类型操作： 初始化链表为空</p>
<p>确定链表为空</p>
<p>确定链表已满</p>
<p>确定链表中的项数</p>
<p>在链表末尾添加项</p>
<p>遍历链表，处理链表中的项</p>
<p>清空链表</p>
</blockquote>
<p>下一步是为开发简单链表ADT开发一个C接口。</p>
<hr />
<h2 id="建立接口">建立接口</h2>
<p>这个简单链表的接口有两个部分。第1部分是描述如何表示数据，第2部分是描述实现ADT操作的函数。接口设计应尽量与ADT的描述保持一致。因此，应该用某种通用的Item类型而不是一些特殊类型，如
<code>int</code> 或 <code>struct film</code> 。可以用C的
<code>typedef</code> 功能来定义所需的Item类型：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> TSIZE 45 <span class="comment">/* 储存电影名的数组大小 */</span></span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">film</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">char</span> title[TSIZE];</span><br><span class="line">    <span class="type">int</span> rating;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">film</span> <span class="title">Item</span>;</span></span><br></pre></td></tr></table></figure>
<p>然后，就可以在定义的其余部分使用 Item
类型。如果以后需要其他数据形式的链表，可以重新定义Item类型，不必更改其余的接口定义。定义了
Item
之后，现在必须确定如何储存这种类型的项。实际上这一步属于实现步骤，但是现在决定好可以让示例更简单些。这里采用链节节点结构：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">node</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    Item item;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">node</span> * <span class="title">next</span>;</span></span><br><span class="line">&#125; Node;</span><br><span class="line"><span class="keyword">typedef</span> Node * List;</span><br></pre></td></tr></table></figure>
<p>在链表的实现中，每一个链节叫作节点（node）。每个节点包含形成链表内容的信息和指向下一个节点的指针。为了强调这个术语，我们把node作为节点结构的标记名，并使用typedef把Node作为struct
node结构的类型名。最后，为了管理链表，还需要一个指向链表开始处的指针，我们使用typedef把List作为该类型的指针名。因此，下面的声明：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">List movies;</span><br></pre></td></tr></table></figure>
<p>创建了该链表所需类型的指针movies。
这是否是定义List类型的唯一方法？不是。例如，还可以添加一个变量记录项数，添加第2个指针储存链表的末尾：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">list</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    Node * head; <span class="comment">/* 指向链表头的指针 */</span></span><br><span class="line">    Node * tail; <span class="comment">/* 指向链表尾的指针 */</span></span><br><span class="line">    <span class="type">int</span> size;    <span class="comment">/* 链表中的项数 */</span></span><br><span class="line">&#125; List; <span class="comment">/* List的另一种定义 */</span></span><br></pre></td></tr></table></figure>
<p>现在，还是使用List类型的第1种定义。这里要着重理解下面的声明创建了一个链表，而不一个指向节点的指针或一个结构：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">List movies;</span><br></pre></td></tr></table></figure>
<p>movies代表的确切数据应该是接口层次不可见的实现细节（不好理解的话可以代入上面的包含尾指针和项数的List定义）。</p>
<p>为了指导用户使用，可以在函数原型前面提供以下注释：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 操作：初始化一个链表 */</span></span><br><span class="line"><span class="comment">/* 前提条件：plist指向一个链表*/</span></span><br><span class="line"><span class="comment">/* 后置条件：该链表初始化为空 */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">InitializeList</span><span class="params">(List * plist)</span>;</span><br></pre></td></tr></table></figure>
<p>这里要注意3点。第1，注释中的“前提条件”（precondition）是调用该函数前应具备的条件。例如，需要一个待初始化的链表。第2，注释中的“后置条件”（postcondition）是执行完该函数后的情况。第3，该函数的参数是一个指向链表的指针，而不是一个链表。所以应该这样调用该函数：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">InitializeList(&amp;movies);</span><br></pre></td></tr></table></figure>
<p>C语言把所有类型和函数的信息集合成一个软件包的方法是：把类型定义和函数原型（包括前提条件和后置条件注释）放在一个头文件中。该文件应该提供程序员使用该类型所需的所有信息。</p>
<p>程序清单<code>list.h</code>给出了一个简单链表类型的头文件。该程序定义了一个特定的结构作为Item类型，然后根据Item定义了Node，再根据Node定义了List。然后，把表示链表操作的函数设计为接受Item类型和List类型的参数。如果函数要修改一个参数，那么该参数的类型应是指向相应类型的指针，而不是该类型。在头文件中，把组成函数名的每个单词的首字母大写，以这种方式表明这些函数是接口包的一部分。另外，该文件使用
<code>#ifndef</code> 指令，防止多次包含一个文件。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* list.h -- 简单链表类型的头文件 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifndef</span> LIST_H_</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> LIST_H_</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdbool.h&gt;</span> <span class="comment">/* C99特性 */</span></span></span><br><span class="line"><span class="comment">/* 特定程序的声明 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TSIZE 45 <span class="comment">/* 储存电影名的数组大小 */</span></span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">film</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">char</span> title[TSIZE];</span><br><span class="line">    <span class="type">int</span> rating;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">/* 一般类型定义 */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">film</span> <span class="title">Item</span>;</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">node</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    Item item;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">node</span> * <span class="title">next</span>;</span></span><br><span class="line">&#125; Node;</span><br><span class="line"><span class="keyword">typedef</span> Node * List;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 函数原型 */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*操作： 初始化一个链表 */</span></span><br><span class="line"><span class="comment">/*前提条件：plist指向一个链表 */</span></span><br><span class="line"><span class="comment">/*后置条件：链表初始化为空 */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">InitializeList</span><span class="params">(List * plist)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*操作： 确定链表是否为空定义 */</span></span><br><span class="line"><span class="comment">/*前提条件：plist指向一个已初始化的链表 */</span></span><br><span class="line"><span class="comment">/*后置条件：如果链表为空，该函数返回true；否则返回false */</span></span><br><span class="line"><span class="type">bool</span> <span class="title function_">ListIsEmpty</span><span class="params">(<span class="type">const</span> List *plist)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*操作： 确定链表是否已满 */</span></span><br><span class="line"><span class="comment">/*前提条件：plist指向一个已初始化的链表 */</span></span><br><span class="line"><span class="comment">/*后置条件：如果链表已满，该函数返回真；否则返回假 */</span></span><br><span class="line"><span class="type">bool</span> <span class="title function_">ListIsFull</span><span class="params">(<span class="type">const</span> List *plist)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*操作： 确定链表中的项数 */</span></span><br><span class="line"><span class="comment">/*前提条件：plist指向一个已初始化的链表 */</span></span><br><span class="line"><span class="comment">/*后置条件：该函数返回链表中的项数 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> <span class="title function_">ListItemCount</span><span class="params">(<span class="type">const</span> List *plist)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*操作： 在链表的末尾添加项 */</span></span><br><span class="line"><span class="comment">/*前提条件： item是一个待添加至链表的项, plist指向一个已初始化的链表 */</span></span><br><span class="line"><span class="comment">/*后置条件：如果可以，该函数在链表末尾添加一个项，且返回true；否则返回false */</span></span><br><span class="line"><span class="type">bool</span> <span class="title function_">AddItem</span><span class="params">(Item item, List * plist)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*操作： 把函数作用于链表中的每一项 */</span></span><br><span class="line"><span class="comment">/*前提条件：plist指向一个已初始化的链表 */</span></span><br><span class="line"><span class="comment">/*前提条件：pfun指向一个函数，该函数接受一个Item类型的参数，且无返回值 */</span></span><br><span class="line"><span class="comment">/*后置条件：pfun指向的函数作用于链表中的每一项一次 */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">Traverse</span><span class="params">(<span class="type">const</span> List *plist, <span class="type">void</span>(*pfun)(Item item))</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*操作：释放已分配的内存（如果有的话） */</span></span><br><span class="line"><span class="comment">/*前提条件：plist指向一个已初始化的链表 */</span></span><br><span class="line"><span class="comment">/*后置条件：释放了为链表分配的所有内存，链表设置为空 */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">EmptyTheList</span><span class="params">(List * plist)</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="实现接口">实现接口</h2>
<p>C方法是把函数定义统一放在一个<code>*.c</code>文件中，具体函数实现可参考<a
href="https://invinc-z.com/data-structure-algorithm/common-data-structure/#%E4%BB%A3%E7%A0%81%E5%AE%9E%E7%8E%B0-1">常用数据结构设计与实现</a>。</p>
<hr />
<h2 id="使用接口">使用接口</h2>
<p>在第二步接口建立好之后便可使用接口进行开发了，实际开发中架构师定义接口（第1、2步），工程师并行实现（第3步），以此来提升开发效率。</p>
<hr />
<h2 id="抽象数据类型的优点">抽象数据类型的优点</h2>
<blockquote>
<ul>
<li>关注点分离： 让设计更清晰，减少思维负担。</li>
<li>灵活性/可维护性：接口和实现分离，使得更改内部实现变得非常容易，不会破坏现有代码。</li>
<li>团队协作：架构师定义接口（第1、2步），工程师并行实现（第3步）。</li>
<li>正确性：先定义契约，便于后续编写测试用例（如单元测试）。</li>
</ul>
</blockquote>
]]></content>
      <categories>
        <category>编程语言（Languages）</category>
        <category>C</category>
      </categories>
      <tags>
        <tag>数据类型</tag>
        <tag>抽象</tag>
        <tag>接口</tag>
        <tag>实现</tag>
      </tags>
  </entry>
  <entry>
    <title>C语言之字符串操作</title>
    <url>/2024/01/24/programming/languages/C/c-string-operation/</url>
    <content><![CDATA[<p>C
语言提供了丰富的字符串处理相关的库函数，这些函数基本上，都声明在头文件
<code>string.h</code>
当中，所以使用它们需要包含这个头文件。这里只介绍几种最基本的和最常用的，以及自己实现这些函数的方式。</p>
<span id="more"></span>
<hr />
<h2 id="字符串长度strlen">字符串长度strlen</h2>
<h3 id="strlen">strlen</h3>
<p><strong>函数全名：</strong> string_length</p>
<p><strong>函数声明：</strong>
<code>size_t strlen (const char *s);</code></p>
<p><strong>函数作用：</strong>
返回当前字符串的长度，也就是字符数组中空字符’\0’前面的字符数量。<strong>不包括空字符’\0’!</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> len;</span><br><span class="line"><span class="type">char</span> str[] = <span class="string">&quot;abcd&quot;</span>;</span><br><span class="line"><span class="type">char</span> str2[<span class="number">10</span>] = <span class="string">&quot;12345&quot;</span>;</span><br><span class="line"><span class="type">char</span> str3[<span class="number">5</span>] = &#123; <span class="string">&#x27;a&#x27;</span>,<span class="string">&#x27;\0&#x27;</span>,<span class="string">&#x27;c&#x27;</span> &#125;;</span><br><span class="line">len = <span class="built_in">strlen</span>(<span class="string">&quot;abc&quot;</span>);    <span class="comment">/* len is now 3 */</span></span><br><span class="line">len = <span class="built_in">strlen</span>(<span class="string">&quot;&quot;</span>);       <span class="comment">/* len is now 0 */</span></span><br><span class="line">len = <span class="built_in">strlen</span>(str);      <span class="comment">/* len is now 4 */</span></span><br><span class="line">len = <span class="built_in">strlen</span>(str2);     <span class="comment">/* len is now 5 */</span></span><br><span class="line">len = <span class="built_in">strlen</span>(str3);     <span class="comment">/* len is now 1 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">char</span> str4[<span class="number">4</span>] = <span class="string">&quot;1234&quot;</span>;</span><br><span class="line">len = <span class="built_in">strlen</span>(str4);     <span class="comment">// str4并不能表示一个字符串,该函数调用会引发未定义行为</span></span><br></pre></td></tr></table></figure>
<h3 id="手动实现my_strlen">手动实现my_strlen</h3>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">size_t</span> <span class="title function_">my_strlen1</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* str)</span> &#123;</span><br><span class="line">    <span class="type">size_t</span> len = <span class="number">0</span>; </span><br><span class="line">    <span class="keyword">while</span> (*str != <span class="string">&#x27;\0&#x27;</span>) &#123;    <span class="comment">// 遍历直到遇到空字符</span></span><br><span class="line">        len++;</span><br><span class="line">        str++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> len;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 数组下标形式</span></span><br><span class="line"><span class="type">size_t</span> <span class="title function_">my_strlen2</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* str)</span> &#123;</span><br><span class="line">    <span class="type">size_t</span> len = <span class="number">0</span>; </span><br><span class="line">    <span class="keyword">while</span> (str[len] != <span class="string">&#x27;\0&#x27;</span>) &#123;</span><br><span class="line">        len++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> len;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">size_t</span> <span class="title function_">my_strlen3</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* str)</span> &#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>* p = str;  <span class="comment">// 用一个指针记录数组首元素</span></span><br><span class="line">    <span class="keyword">while</span> (*str) &#123;    <span class="comment">// 惯用法: 遍历直到字符串的末尾，</span></span><br><span class="line">        str++;</span><br><span class="line">    &#125;   <span class="comment">// 循环结束时，str指针指向空字符，p指针指向数组首元素</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 相当于空字符的下标减去首元素下标，结果就是字符串的长度</span></span><br><span class="line">    <span class="keyword">return</span> str - p;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="字符串复制strcpystrncpy">字符串复制strcpy，strncpy</h2>
<h3 id="strcpy">strcpy</h3>
<p><strong>函数全名：</strong> string_copy</p>
<p><strong>函数声明：</strong>
<code>char *strcpy(char *dest, const char *src);</code></p>
<p><strong>函数作用：</strong> 将 <strong>src</strong> 中存储的以空字符
‘\0’ 结束的字符串复制到 <strong>dest</strong>所指向的数组中。</p>
<p>也就是说，会从首字符开始逐个字符地从 src
复制字符，<strong>包括空字符在内</strong>，都会从dest首位置开始，复制到dest当中。</p>
<p>这个过程中，src数组是不会被修改的，所以它被const修饰。总之，该函数调用后，dest
将包含 src 的完整副本。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span> src[] = <span class="string">&quot;hello&quot;</span>;</span><br><span class="line"><span class="type">char</span> dest[<span class="number">10</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用函数后，src不变</span></span><br><span class="line"><span class="comment">// dest数组就会变成&#123;&#x27;h&#x27;,&#x27;e&#x27;,&#x27;l&#x27;,&#x27;l&#x27;,&#x27;o&#x27;,&#x27;\0&#x27;, ....&#125;</span></span><br><span class="line"><span class="comment">// 其中前6个字符是字符串&quot;hello&quot;</span></span><br><span class="line"><span class="built_in">strcpy</span>(dest, src);</span><br></pre></td></tr></table></figure>
<p><strong>函数返回值：</strong></p>
<p>该函数会返回指向目标数组 dest
的指针，一般来说，该函数的返回值没有什么意义，推荐忽略它。</p>
<p>但某些场景下，这个<strong>返回值允许strcpy
函数的调用，可以被嵌套使用或者用于更复杂的表达式中。</strong> 比如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// strcpy返回复制完成后指向 dest 数组的指针</span></span><br><span class="line"><span class="type">char</span> src[] = <span class="string">&quot;hello&quot;</span>;</span><br><span class="line"><span class="type">char</span> dest[<span class="number">10</span>];</span><br><span class="line"><span class="type">char</span> dest2[<span class="number">10</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 可以直接利用printf、puts函数打印复制后的目标数组</span></span><br><span class="line"><span class="built_in">puts</span>(<span class="built_in">strcpy</span>(dest, src));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 更加复杂的函数调用</span></span><br><span class="line"><span class="comment">// 将src复制到dest，再将dest复制给dest2</span></span><br><span class="line"><span class="built_in">strcpy</span>(dest2, <span class="built_in">strcpy</span>(dest, src));</span><br></pre></td></tr></table></figure>
<blockquote>
<p>注意：</p>
<ol type="1">
<li>strcpy函数是不安全的，它并不会检查dest是否真的能够包含src数组。如果dest不够大，就会因数组越界而产生未定义行为。</li>
<li>为了安全起见，可以考虑使用strncpy函数解决这一问题，虽然它可能效率更低一些。</li>
</ol>
</blockquote>
<h3 id="strncpy">strncpy</h3>
<p><strong>函数声明:</strong>
<code>char *strncpy(char *dest, const char *src, size_t n);</code></p>
<p><strong>函数作用：</strong></p>
<p>该函数会将最多n个字符从src中复制到dest数组中：</p>
<ol type="1">
<li>如果<code>n &lt; strlen(src) + 1</code>，也就是 n 小于源字符串的长度
+ 1时，此时strncpy函数就无法将整个源字符串数据都复制到 dest
中，并且此时复制完成的 dest
数组也不会以空字符结尾，无法表示一个字符串。不过好在，它不会引起越界问题，是安全的操作。</li>
<li>如果<code>n = strlen(src) + 1</code>，即 n 恰好是源字符串长度 +
1时，strncpy函数会将src完整复制到dest数组中，包括空字符。</li>
<li>如果<code>n &gt; strlen(src) + 1</code>，即 n 完全大于源字符串长度 +
1时，strncpy函数不仅会完整复制src字符串到dest中，还会将<strong>剩余 (n -
strlen(src) - 1) 个字符</strong>设置为空字符。</li>
</ol>
<p>strncpy函数一定会处理n个字符数据，如果n比较大，在复制完源数组后，它会顺道将
dest 数组中的元素置为空字符。</p>
<p>实际开发中，建议将<strong>n的值设定为
dest长度-1</strong>，并<strong>主动将dest的最后一个元素设置为空字符</strong>，这样总能安全地得到一个字符串。</p>
<p>也就是以下操作代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">strncpy</span>(dest, src, <span class="keyword">sizeof</span>(dest) - <span class="number">1</span>);</span><br><span class="line">dest[<span class="keyword">sizeof</span>(dest) - <span class="number">1</span>] = <span class="string">&#x27;\0&#x27;</span>;</span><br></pre></td></tr></table></figure>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span> src[] = <span class="string">&quot;hello&quot;</span>;</span><br><span class="line"><span class="type">char</span> dest[<span class="number">10</span>];</span><br><span class="line"><span class="comment">// 这个复制是没有问题的</span></span><br><span class="line"><span class="built_in">strncpy</span>(dest, src, <span class="keyword">sizeof</span>(dest) - <span class="number">1</span>);</span><br><span class="line">dest[<span class="keyword">sizeof</span>(dest) - <span class="number">1</span>] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">char</span> dest2[<span class="number">5</span>];</span><br><span class="line"><span class="comment">// 这个复制虽然没有真的将&quot;hello&quot;都复制到dest2，但是一个安全的操作。最终dest2存放字符串&quot;hell&quot;</span></span><br><span class="line"><span class="built_in">strncpy</span>(dest2, src, <span class="keyword">sizeof</span>(dest2) - <span class="number">1</span>);</span><br><span class="line">dest2[<span class="keyword">sizeof</span>(dest2) - <span class="number">1</span>] = <span class="string">&#x27;\0&#x27;</span>;</span><br></pre></td></tr></table></figure>
<p>这样的复制行为，若dest足够装下src，会导致dest剩余部分全都被设置为空字符。</p>
<h3 id="手动实现my_strcpymy_strncpy">手动实现my_strcpy、my_strncpy</h3>
<h4 id="my_strcpy">my_strcpy</h4>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span>* <span class="title function_">my_strcpy1</span><span class="params">(<span class="type">char</span> *dest, <span class="type">const</span> <span class="type">char</span> *src)</span> &#123;</span><br><span class="line">    <span class="type">char</span> *tmp = dest;   <span class="comment">// dest数组的首元素指针要作为返回值,所以要临时保存一下</span></span><br><span class="line">    <span class="keyword">while</span> (*src) &#123;</span><br><span class="line">        *dest++ = *src++;</span><br><span class="line">    &#125;   <span class="comment">// 这个while循环没有复制空字符,所以需要手动来复制空字符</span></span><br><span class="line">    <span class="comment">// while循环结束时,src指针指向空字符, dest指针也指向对应的位置</span></span><br><span class="line">    *dest = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">return</span> tmp;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 简化: 复制字符串的惯用法</span></span><br><span class="line"><span class="type">char</span> *<span class="title function_">my_strcpy2</span><span class="params">(<span class="type">char</span> *dest, <span class="type">const</span> <span class="type">char</span> *src)</span> &#123;</span><br><span class="line">    <span class="type">char</span> *tmp = dest;   <span class="comment">// dest数组的首元素指针要作为返回值,所以要临时保存一下</span></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">        (*dest++ = *src++)</span></span><br><span class="line"><span class="comment">        主要作用: 返回*src的值</span></span><br><span class="line"><span class="comment">        副作用: src和dest都往后挪动指针</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">        当主要作用返回src指针指向的元素, 且返回空字符结束这个while循环时</span></span><br><span class="line"><span class="comment">        =的副作用赋值并不会消失</span></span><br><span class="line"><span class="comment">        所以所以这种写法, 空字符也会连带被复制</span></span><br><span class="line"><span class="comment">        不需要考虑空字符的手动复制</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">while</span> (*dest++ = *src++)</span><br><span class="line">        ;</span><br><span class="line">    <span class="keyword">return</span> tmp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="my_strncpy">my_strncpy</h4>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span> *<span class="title function_">my_strncpy1</span><span class="params">(<span class="type">char</span> *dest, <span class="type">const</span> <span class="type">char</span> *src, <span class="type">size_t</span> n)</span> &#123;</span><br><span class="line">    <span class="type">char</span> *tmp = dest;</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">        复制src到dest，直到达到n个字符或遇到src的终结符</span></span><br><span class="line"><span class="comment">        while循环结束条件是:</span></span><br><span class="line"><span class="comment">        1.n足够长时(n &gt;= strlen(src)+1时)，会将src中直到空字符的数据复制到dest中，然后结束循环</span></span><br><span class="line"><span class="comment">        2.n不够长时(n &lt; strlen(src)+1时)，会将src中的前n个字符复制到dest中,然后结束循环</span></span><br><span class="line"><span class="comment">        此时dest数组不会存储空字符,它不能表示字符串,但不会有越界风险</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">while</span> (n &gt; <span class="number">0</span> &amp;&amp; (*dest++ = *src++)) &#123;</span><br><span class="line">        n--;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">        当n &gt;= strlen(src) + 1时</span></span><br><span class="line"><span class="comment">        上面的while循环依靠(*dest++ = *src++)结束循环</span></span><br><span class="line"><span class="comment">        此时空字符被复制到dest中了</span></span><br><span class="line"><span class="comment">        但循环体没有执行循环就终止了, n--没有执行, 即复制了一个字符但n没有减少1</span></span><br><span class="line"><span class="comment">        所以下面要判断n &gt; 1 而不是n &gt; 0</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">while</span> (n &gt; <span class="number">1</span>) &#123;</span><br><span class="line">        *dest++ = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">        n--;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> tmp;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 如果不将n设置为无符号类型,而设置为int类型</span></span><br><span class="line"><span class="type">char</span> *<span class="title function_">my_strncpy2</span><span class="params">(<span class="type">char</span> *dest, <span class="type">const</span> <span class="type">char</span> *src, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">char</span> *tmp = dest;</span><br><span class="line">    <span class="comment">// 这个循环不用担心无符号0减1变为最大值的情况发生,可以放心使用</span></span><br><span class="line">    <span class="keyword">while</span> (n-- &amp;&amp; (*dest++ = *src++))</span><br><span class="line">        ;</span><br><span class="line">    <span class="comment">// 由于n--正常执行,所以下面仍然可以判断n&gt;0</span></span><br><span class="line">    <span class="keyword">while</span> (n &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        *dest++ = <span class="number">0</span>;</span><br><span class="line">        n--;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> tmp;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 当然为了追求原汁原味, 还是应该保留n的size_t类型</span></span><br><span class="line"><span class="comment">// 同时为了保证n&gt;0的正常使用,我们就需要保证n--正常执行</span></span><br><span class="line"><span class="type">char</span> *<span class="title function_">my_strncpy3</span><span class="params">(<span class="type">char</span> *dest, <span class="type">const</span> <span class="type">char</span> *src, <span class="type">size_t</span> n)</span> &#123;</span><br><span class="line">    <span class="type">char</span> *tmp = dest;</span><br><span class="line">    <span class="keyword">while</span> (n &gt; <span class="number">0</span> &amp;&amp; (*dest = *src)) &#123; </span><br><span class="line">        dest++;</span><br><span class="line">        src++;</span><br><span class="line">        n--;</span><br><span class="line">    &#125;<span class="comment">// while循环结束时，src指针指向指向空字符，dest指针也指向空字符</span></span><br><span class="line">    <span class="comment">// 虽然空字符已经复制结束后，但由于指针仍然指向空字符，下面的while循环就可以n&gt;0执行</span></span><br><span class="line">    <span class="keyword">while</span> (n &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        *dest++ = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">        n--;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> tmp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="字符串拼接strcatstrncat">字符串拼接strcat，strncat</h2>
<h3 id="strcat">strcat</h3>
<p><strong>函数全名：</strong>
string_concat，顾名思义，它用于将一个字符串拼接到另一个字符串的末尾。</p>
<p><strong>函数声明：</strong>
<code>char *strcat(char *dest, const char *src);</code></p>
<p><strong>函数作用：</strong> strcat 函数会将
src（源字符串）中存储的以空字符 ‘\0’ 结束的字符串拼接到
dest（目标字符串）的末尾。</p>
<p>具体来说：</p>
<p>会从 dest 字符串的空字符 ‘\0’
开始，替换这个空字符，然后<strong>将src中表示字符串的字符数据从开头到空字符</strong>，全部拼接到dest末尾。</p>
<p>这个过程中，src 字符串不会被修改，所以它被 const 修饰。</p>
<p><strong>总之，该函数调用后，dest 将包含 dest 和 src
的字符串拼接后的副本。</strong></p>
<p>简单的代码示例：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span> dest[<span class="number">20</span>] = <span class="string">&quot;Hello, &quot;</span>;</span><br><span class="line"><span class="type">char</span> src[] = <span class="string">&quot;World!&quot;</span>;</span><br><span class="line"><span class="comment">// 调用函数后，dest数组变为 &quot;Hello, World!\0&quot;，其余部分填充为空字符</span></span><br><span class="line"><span class="built_in">strcat</span>(dest, src);</span><br><span class="line"><span class="comment">// 输出拼接后的字符串</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>, dest); <span class="comment">// 输出 &quot;Hello, World!&quot;</span></span><br></pre></td></tr></table></figure>
<p><strong>函数返回值：</strong></p>
<p>该函数会返回指向目标数组 dest
的指针。一般来说这个返回值可以忽略，但有些场景中可以利用该返回值，对函数调用进行嵌套处理。</p>
<p>例如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span> dest[<span class="number">64</span>] = <span class="string">&quot;Hello, &quot;</span>;</span><br><span class="line"><span class="type">char</span> src[] = <span class="string">&quot;Everyone!&quot;</span>;</span><br><span class="line"><span class="type">char</span> src2[] = <span class="string">&quot; Have a nice day!&quot;</span>;</span><br><span class="line"><span class="comment">// 嵌套，将src和src2都拼接到dest上</span></span><br><span class="line"><span class="built_in">strcat</span>(<span class="built_in">strcat</span>(dest, src), src2);</span><br><span class="line"><span class="comment">// 输出最终的拼接字符串</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>, dest); <span class="comment">// 输出 &quot;Hello, Everyone! Have a nice day!&quot;</span></span><br></pre></td></tr></table></figure>
<p><strong>注意：</strong></p>
<ol type="1">
<li><strong>src 和
dest两个参数都必须是字符串，即以空字符串结尾的字符数组，否则将引发未定义行为。</strong></li>
<li>strcat 函数与 strcpy 一样，不会检查 dest
数组是否能够容纳拼接后的字符串。如果 dest
不够大，就会产生未定义行为，这是潜在的安全隐患。</li>
<li>可以考虑使用更安全的strncat函数来实现字符串拼接，它允许指定最大拼接的字符数。</li>
</ol>
<hr />
<h3 id="strncat">strncat</h3>
<p><strong>函数声明：</strong>
<code>char *strncat(char *dest, const char *src, size_t n);</code></p>
<p><strong>函数作用：</strong></p>
<p>仍旧是找到dest字符串末尾的空字符，然后从该字符开始，将 src
的首个元素复制到 dest末尾，直到：</p>
<ul>
<li><p>已经复制了 n 个字符。</p></li>
<li><p>或者复制到达了<code>src</code>字符串的结尾，即遇到了<code>src</code>的空字符串。所以该函数<strong>不会把src中的空字符复制到dest中</strong>。</p></li>
</ul>
<p>最后，<code>strncat</code>函数一定会在dest的末尾添加一个空字符，以确保dest能够表示一个字符串。由于这一步操作的存在，该函数仍然具有越界访问的风险，需要程序员自己设定合理的n取值，以规避这种情况。</p>
<p>为了安全的使用此函数，基于函数的特点，我们就可以得出n的计算公式：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n = <span class="keyword">sizeof</span>(dest) - <span class="built_in">strlen</span>(dest) - <span class="number">1</span>;    <span class="comment">// dest数组的长度 - 已存储字符串的长度 - 1(留给存空字符)</span></span><br></pre></td></tr></table></figure>
<p>表达式中<code>-1</code>是必要的，否则会导致数组越界情况发生。</p>
<p>给strncat函数传入这样的一个<code>n</code>参数，就能够保证一定安全的得到一个拼接后的结果字符串。</p>
<p>当然若dest容量不够大，拼接只会截取src的一部分字符数据。但安全是更重要的事情，这样的结果是我们可以接受的。</p>
<p>实际开发中，建议采取以下方式来调用此函数，避免dest空间不足导致溢出：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span> src[] = <span class="string">&quot;world&quot;</span>;</span><br><span class="line"><span class="type">char</span> dest[<span class="number">10</span>] = <span class="string">&quot;hello, &quot;</span>;</span><br><span class="line"><span class="comment">// 确保dest是一个数组类型，而不是一个指针类型才能这样操作</span></span><br><span class="line"><span class="type">int</span> n =  <span class="keyword">sizeof</span>(dest) - <span class="built_in">strlen</span>(dest) - <span class="number">1</span>;</span><br><span class="line"><span class="built_in">strncat</span>(dest, src, n);</span><br></pre></td></tr></table></figure>
<p>dest最多再拼接 <strong>(自身数组长度 - 字符串长度 - 1)</strong>
个字符，最后留一个字节，strncat函数会自动拼接一个空字符表示字符串结束。</p>
<h3 id="手动实现my_strcatmy_strncat">手动实现my_strcat、my_strncat</h3>
<h4 id="my_strcat">my_strcat</h4>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span>* <span class="title function_">my_strcat</span><span class="params">(<span class="type">char</span>* dest, <span class="type">const</span> <span class="type">char</span>* src)</span> &#123;</span><br><span class="line">    <span class="type">char</span>* temp = dest;</span><br><span class="line">    <span class="comment">// 找到dest的末尾，也就是让dest指针指向空字符</span></span><br><span class="line">    <span class="keyword">while</span> (*dest) &#123;</span><br><span class="line">        dest++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将src的字符串数据从开头到空字符都拷贝到dest的末尾</span></span><br><span class="line">    <span class="keyword">while</span> ((*dest++ = *src++))</span><br><span class="line">        ;</span><br><span class="line">    <span class="keyword">return</span> temp;  <span class="comment">// 返回dest的起始地址</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="my_strncat">my_strncat</h4>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="type">char</span>* <span class="title function_">my_strncat1</span><span class="params">(<span class="type">char</span>* dest, <span class="type">const</span> <span class="type">char</span>* src, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">char</span>* temp = dest;</span><br><span class="line">    <span class="comment">// 找到dest的末尾，也就是让dest指针指向空字符</span></span><br><span class="line">    <span class="keyword">while</span> (*dest) &#123;</span><br><span class="line">        dest++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 拷贝最多n个字符从src到dest的末尾</span></span><br><span class="line">    <span class="keyword">while</span> (n-- &amp;&amp; (*src)) &#123;</span><br><span class="line">        *dest++ = *src++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 确保dest以空字符结尾,所以一定会将末尾置为空字符。这里没有判断越界，所以strncpy函数实际仍存在越界风险</span></span><br><span class="line">    <span class="comment">// 这就要求我们传入一个正确合理的n以确保安全    </span></span><br><span class="line">    *dest = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">    <span class="keyword">return</span> temp;  <span class="comment">// 返回dest的起始地址</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="字符串比较大小strcmp">字符串比较大小strcmp</h2>
<h3 id="strcmp">strcmp</h3>
<p><strong>函数全名：</strong>string_compare，用于比较两个字符串的大小。</p>
<p><strong>函数声明：</strong>
<code>int strcmp(const char *str1, const char *str2);</code></p>
<p><strong>函数作用：</strong>
<strong>strcmp函数按照字典顺序比较两个字符串的大小。</strong>当调用
strcmp函数时，它会逐个字符地比较两个字符串 str1 和 str2：</p>
<p>注意：</p>
<ol type="1">
<li><strong>这个函数不会修改任何一个字符串，所以两个参数都被 const
修饰。</strong></li>
<li>不同编译器和环境可能对strcmp函数返回值的表现有所不同。在某些环境中<strong>（比如VS）</strong>，出于简化的目的，可能会将返回值归一化为
-1、0 或 1。但是标准的 C 语言库中 strcmp
的返回值是根据实际字符的ASCII值差异来确定的，而不是简单的 -1、0 或
1。</li>
<li>要确保传入的两个参数数组都表示字符串，都以空字符结尾，否则可能会导致比较越界。</li>
<li>strcmp 是区分大小写的比较，如果需要进行不区分大小写的比较，可以使用
strcasecmp 函数。</li>
<li>由于strcmp是基于ASCII值进行比较的，它在处理非ASCII或多字节字符（如UTF-8编码的文本）时可能不会表现出预期的行为。</li>
</ol>
<h3 id="手动实现strcmp">手动实现strcmp</h3>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">my_strcmp1</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* str1, <span class="type">const</span> <span class="type">char</span>* str2)</span> &#123;</span><br><span class="line">    <span class="keyword">while</span> (*str1 &amp;&amp; *str2) &#123;</span><br><span class="line">        <span class="keyword">if</span> (*str1 != *str2)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> *str1 - *str2;</span><br><span class="line">        &#125;</span><br><span class="line">        str1++;</span><br><span class="line">        str2++;  </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> *str1 - *str2;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这个实现可读性更差，但简洁很多</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">my_strcmp2</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* s1, <span class="type">const</span> <span class="type">char</span>* s2)</span> &#123;</span><br><span class="line">    <span class="keyword">while</span> (*s1 &amp;&amp; (*s1 == *s2)) &#123;</span><br><span class="line">        <span class="comment">// 移动两个指针，比较下一对字符</span></span><br><span class="line">        s1++;</span><br><span class="line">        s2++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    * while循环结束的条件是：</span></span><br><span class="line"><span class="comment">    * 1.s1指针指向了空字符</span></span><br><span class="line"><span class="comment">    * 2.或者s2指针指向了空字符</span></span><br><span class="line"><span class="comment">    * 3.或者s1和s2指针指向的字符不一致</span></span><br><span class="line"><span class="comment">    * 不管是哪一种情况，返回它们的编码值差就是结果</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">return</span> *s1 - *s2;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="字符相关的库函数">字符相关的库函数</h2>
<p>在操作字符串的过程中，经常需要对单个字符做出判断，比如：</p>
<ol type="1">
<li>判断字符是大写字母还是小写</li>
<li>判断字符是不是数字</li>
<li>…</li>
</ol>
<p>C 标准函数库在<strong>头文件 <code>&lt;ctype.h&gt;</code></strong>
中定义了大量此类功能函数。比如：</p>
<p><strong>islower 和 isupper</strong> 函数是 C 标准库中声明在
&lt;ctype.h&gt;
头文件中的函数，它们用于检查给定的字符是否为小写字母或大写字母。</p>
<p>除此之外，<code>&lt;ctype.h&gt;</code>
头文件中还包括了以下类似的库函数：</p>
<ol type="1">
<li><code>isalpha(int c)</code>：检查传入的字符是否为字母（包括大写和小写）。</li>
<li><code>isdigit(int c)</code>：检查传入的字符是否为十进制数字（0到9）。</li>
<li><code>isalnum(int c)</code>：检查传入的字符是否为字母或数字。</li>
<li><code>isspace(int c)</code>：检查传入的字符是否为空白字符，比如空格、制表符、换行符等。</li>
<li><code>isblank(int c)</code>：检查传入的字符是否为空格或制表符。</li>
<li>…</li>
</ol>
]]></content>
      <categories>
        <category>编程语言（Languages）</category>
        <category>C</category>
      </categories>
      <tags>
        <tag>标准库</tag>
        <tag>字符串</tag>
      </tags>
  </entry>
  <entry>
    <title>C语言之可变参数列表</title>
    <url>/2024/02/11/programming/languages/C/c-variable-argument-lists/</url>
    <content><![CDATA[<p>在 C 语言中，可变参数列表（Variable Argument List）通过
<code>stdarg.h</code>
头文件提供的宏和函数来实现。它允许函数接受可变数量的参数，类似于
<code>printf</code> 和 <code>scanf</code>
这样的函数。本文介绍与可变参数列表相关的函数和用法。</p>
<span id="more"></span>
<hr />
<h2 id="核心宏和函数">核心宏和函数</h2>
<p><code>stdarg.h</code> 提供了以下宏和函数来处理可变参数列表：</p>
<table>
<thead>
<tr class="header">
<th>宏/函数</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>va_list</code></td>
<td>定义一个变量，用于存储可变参数列表。</td>
</tr>
<tr class="even">
<td><code>va_start()</code></td>
<td>初始化 <code>va_list</code>，使其指向可变参数列表的第一个参数。</td>
</tr>
<tr class="odd">
<td><code>va_arg()</code></td>
<td>从 <code>va_list</code> 中获取下一个参数，并指定其类型。</td>
</tr>
<tr class="even">
<td><code>va_end()</code></td>
<td>清理 <code>va_list</code>，结束可变参数列表的访问。</td>
</tr>
<tr class="odd">
<td><code>va_copy()</code></td>
<td>复制一个 <code>va_list</code>（C99 标准引入）。</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="使用步骤">使用步骤</h2>
<p>使用可变参数列表的典型步骤如下：</p>
<ol type="1">
<li>定义一个 <code>va_list</code> 类型的变量。</li>
<li>使用 <code>va_start()</code> 初始化 <code>va_list</code>。</li>
<li>使用 <code>va_arg()</code> 逐个获取参数。</li>
<li>使用 <code>va_end()</code> 清理 <code>va_list</code>。</li>
</ol>
<hr />
<h2 id="示例代码">示例代码</h2>
<p>以下是一个简单的示例，演示如何使用可变参数列表实现一个求和函数：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdarg.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义一个可变参数函数，计算任意数量整数的和</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sum</span><span class="params">(<span class="type">int</span> count, ...)</span> &#123;</span><br><span class="line">    <span class="type">int</span> total = <span class="number">0</span>;</span><br><span class="line">    va_list args;  <span class="comment">// 定义 va_list 变量</span></span><br><span class="line"></span><br><span class="line">    va_start(args, count);  <span class="comment">// 初始化 args，使其指向第一个可变参数</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">        <span class="type">int</span> num = va_arg(args, <span class="type">int</span>);  <span class="comment">// 获取下一个 int 类型的参数</span></span><br><span class="line">        total += num;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    va_end(args);  <span class="comment">// 清理 args</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> total;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Sum: %d\n&quot;</span>, sum(<span class="number">3</span>, <span class="number">10</span>, <span class="number">20</span>, <span class="number">30</span>));  <span class="comment">// 输出 60</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Sum: %d\n&quot;</span>, sum(<span class="number">5</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>));  <span class="comment">// 输出 15</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="详细说明">详细说明</h2>
<h3 id="va_list">(1) <code>va_list</code></h3>
<ul>
<li><p>类型：<code>va_list</code>
是一个类型，用于存储可变参数列表。</p></li>
<li><p>示例：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">va_list args;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="va_start">(2) <code>va_start()</code></h3>
<ul>
<li><p>函数原型：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">va_start</span><span class="params">(va_list ap, last_arg)</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>概念实现：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 概念性实现（实际由编译器提供）</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> va_start(ap, last_arg) \</span></span><br><span class="line"><span class="meta">(ap = (va_list)((char*)&amp;last_arg + sizeof(last_arg)))</span></span><br></pre></td></tr></table></figure></li>
<li><p>作用：</p>
<ul>
<li>初始化 <code>va_list</code>，使其指向可变参数列表的第一个参数。</li>
<li><code>last_arg</code> 是可变参数列表前的最后一个固定参数。</li>
</ul></li>
<li><p>示例：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">va_start(args, count);</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="va_arg">(3) <code>va_arg()</code></h3>
<ul>
<li><p>函数原型：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">type <span class="title function_">va_arg</span><span class="params">(va_list ap, type)</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>作用：</p>
<ul>
<li>从 <code>va_list</code> 中获取下一个参数，并指定其类型。</li>
<li><code>type</code> 是参数的类型（如
<code>int</code>、<code>double</code> 等）。</li>
</ul></li>
<li><p>示例：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> num = va_arg(args, <span class="type">int</span>);</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="va_end">(4) <code>va_end()</code></h3>
<ul>
<li><p>函数原型：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">va_end</span><span class="params">(va_list ap)</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>作用：清理
<code>va_list</code>，结束可变参数列表的访问。</p></li>
<li><p>示例：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">va_end(args);</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="va_copyc99-引入">(5) <code>va_copy()</code>（C99 引入）</h3>
<ul>
<li><p>函数原型：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">va_copy</span><span class="params">(va_list dest, va_list src)</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>作用：</p>
<ul>
<li>复制一个 <code>va_list</code> 变量。</li>
<li><code>dest</code> 是目标变量，<code>src</code> 是源变量。</li>
</ul></li>
<li><p>示例：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">va_list args_copy;</span><br><span class="line">va_copy(args_copy, args);</span><br></pre></td></tr></table></figure></li>
</ul>
<hr />
<h2 id="注意事项">注意事项</h2>
<ol type="1">
<li><strong>固定参数</strong>：可变参数函数必须至少有一个固定参数（如
<code>count</code>），用于确定可变参数的数量或类型。</li>
<li><strong>参数类型</strong>：使用 <code>va_arg()</code>
时，必须明确指定参数的类型。如果类型不匹配，会导致未定义行为。</li>
<li><strong>参数数量</strong>：需要确保访问的参数数量不超过实际传递的参数数量，否则会导致未定义行为。</li>
<li><strong>平台依赖性</strong>：可变参数列表的实现依赖于底层平台，不同平台可能有不同的行为。</li>
</ol>
<hr />
<h2 id="更复杂的示例">更复杂的示例</h2>
<p>以下是一个更复杂的示例，实现一个类似 <code>printf</code>
的函数，支持格式化输出：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdarg.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">my_printf</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *format, ...)</span> &#123;</span><br><span class="line">    va_list args;</span><br><span class="line">    va_start(args, format);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (*format) &#123;</span><br><span class="line">        <span class="keyword">if</span> (*format == <span class="string">&#x27;%&#x27;</span>) &#123;</span><br><span class="line">            format++;  <span class="comment">// 跳过 &#x27;%&#x27;</span></span><br><span class="line">            <span class="keyword">switch</span> (*format) &#123;</span><br><span class="line">                <span class="keyword">case</span> <span class="string">&#x27;d&#x27;</span>: &#123;</span><br><span class="line">                    <span class="type">int</span> num = va_arg(args, <span class="type">int</span>);</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>, num);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">case</span> <span class="string">&#x27;f&#x27;</span>: &#123;</span><br><span class="line">                    <span class="type">double</span> num = va_arg(args, <span class="type">double</span>);</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;%f&quot;</span>, num);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">case</span> <span class="string">&#x27;s&#x27;</span>: &#123;</span><br><span class="line">                    <span class="type">char</span> *str = va_arg(args, <span class="type">char</span> *);</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>, str);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">default</span>:</span><br><span class="line">                    <span class="built_in">putchar</span>(*format);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">putchar</span>(*format);</span><br><span class="line">        &#125;</span><br><span class="line">        format++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    va_end(args);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    my_printf(<span class="string">&quot;Integer: %d, Float: %f, String: %s\n&quot;</span>, <span class="number">42</span>, <span class="number">3.14</span>, <span class="string">&quot;Hello&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="总结">总结</h2>
<ul>
<li><code>va_list</code> 是处理可变参数列表的核心类型。</li>
<li><code>va_start()</code>、<code>va_arg()</code> 和
<code>va_end()</code> 是处理可变参数的基本宏。</li>
<li>可变参数函数需要至少一个固定参数，用于确定可变参数的数量或类型。</li>
<li>使用可变参数时需要注意参数类型和数量的匹配，避免未定义行为。</li>
</ul>
]]></content>
      <categories>
        <category>编程语言（Languages）</category>
        <category>C</category>
      </categories>
      <tags>
        <tag>可变参数列表</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：关于本书（零）</title>
    <url>/2025/04/17/notes-reading/computer/apue/apue-00-about-the-book/</url>
    <content><![CDATA[<p>本文记录了 APUE
的相关信息，包括图书基本信息，官网地址，源码地址。</p>
<span id="more"></span>
<hr />
<h2 id="图书信息">图书信息</h2>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">书名：Advanced Programming in the UNIX Environment, 3rd Edition</span><br><span class="line">作者: W. Richard Stevens / Stephen A. Rago</span><br><span class="line">出版社: Addison-Wesley Professional</span><br><span class="line">出版年: 2013-5-24</span><br><span class="line">页数: 1024</span><br><span class="line">定价: USD 69.99</span><br><span class="line">装帧: Paperback</span><br><span class="line">丛书: Addison-Wesley Professional Computing Series</span><br><span class="line">ISBN: 9780321637734</span><br></pre></td></tr></table></figure>
<p>官方网址：<span class="exturl" data-url="aHR0cDovL3d3dy5hcHVlYm9vay5jb20v">http://www.apuebook.com/<i class="fa fa-external-link-alt"></i></span></p>
<p><span class="exturl" data-url="aHR0cHM6Ly9ib29rLmRvdWJhbi5jb20vc3ViamVjdC8xMTkzNzUxMS8=">豆瓣地址<i class="fa fa-external-link-alt"></i></span></p>
<p><span class="exturl" data-url="aHR0cDovL3d3dy5hcHVlYm9vay5jb20vY29kZTNlLmh0bWw=">源码地址<i class="fa fa-external-link-alt"></i></span></p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：UNIX System Overview（一）</title>
    <url>/2025/04/20/notes-reading/computer/apue/apue-01-unix-system-overview/</url>
    <content><![CDATA[<p>本文是《UNIX环境高级编程》第3版 第1章 UNIX System Overview
的一些笔记。
包括UNIX体系结构、登录、文件和目录、输入和输出、程序和进程、出错处理、用户标识、信号、时间值、系统调用和库函数等基本概念。</p>
<span id="more"></span>
<hr />
<h2 id="unix体系结构">UNIX体系结构</h2>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260121200458079.png" alt="Architecture of the Unix operating system" style="zoom: 67%;" /></p>
<hr />
<h2 id="登录">登录</h2>
<h3 id="登录名">登录名</h3>
<p>系统在其口令文件（通常是<code>/etc/passwd</code>文件）中查看登录名。口令文件中的登录项由7个以冒号分隔的字段组成，依次是：登录名、加密口令、用户ID、组ID、注释字段、起始目录以及shell程序。</p>
<p><code>invinc-z:x:1000:1000:Invinc-Z:/home/invinc-z:/bin/bash</code></p>
<p>目前，所有的系统已将加密口令移到另一个文件中（通常是<code>/etc/shadow</code>）。</p>
<h3 id="shell">shell</h3>
<p>shell是一个命令行解释器，它读取用户输入，然后执行命令。shell
的用户输入通常来自于终端（交互式shell），有时则来自于文件（称为shell脚本）。</p>
<p>系统从口令文件中相应用户登录项的最后一个字段中了解到应该为该登录用户执行哪一个shell。</p>
<hr />
<h2 id="文件和目录">文件和目录</h2>
<h3 id="文件系统">文件系统</h3>
<p>UNIX文件系统是目录和文件的一种层次结构，所有东西的起点是称为根（root）的目录，这个目录的名称是一个字符<code>/</code>。</p>
<p>目录（directory）是一个包含目录项的文件。在逻辑上，可以认为每个目录项都包含一个文件名，同时还包含说明该文件属性的信息。文件属性是指文件类型（是普通文件还是目录等）、文件大小、文件所有者、文件权限（其他用户能否访问该文件）以及文件最后的修改时间等。</p>
<h3 id="文件名">文件名</h3>
<p>目录中的各个名字称为文件名。为了可移植性，POSIX.1推荐将文件名限制在以下字符集之内：字母（a～z、A～Z）、数字（0～9）、句点（.）、短横线（-）和下划线（_）。</p>
<h3 id="路径名">路径名</h3>
<p>由斜线分隔的一个或多个文件名组成的序列（也可以是斜线开头）构成路径名，以斜线开头的路径名称为绝对路径名，否则称为相对路径名。相对路径名指向<strong>相对于当前目录</strong>的文件。文件系统根的名字
<code>/</code> 是一个特殊的绝对路径名，它不含文件名。</p>
<h3 id="工作目录">工作目录</h3>
<p>每个进程都有一个工作目录，有时称其为当前工作目录。所有相对路径名都从工作目录开始解释。进程可以用<code>chdir</code>函数更改其工作目录。</p>
<h3 id="起始目录">起始目录</h3>
<p>登录时，工作目录设置为起始目录（home
directory），该起始目录从口令文件（<code>/etc/passwd</code>）中相应用户的登录项中取得。</p>
<p><code>invinc-z:x:1000:1000:Invinc-Z:/home/invinc-z:/bin/bash</code></p>
<hr />
<h2 id="输入和输出">输入和输出</h2>
<h3 id="文件描述符">文件描述符</h3>
<p>文件描述符（file
descriptor）通常是一个小的非负整数，内核用以标识一个特定进程正在访问的文件。当内核打开一个现有文件或创建一个新文件时，它都返回一个文件描述符。在读、写文件时，可以使用这个文件描述符。</p>
<h3 id="标准输入标准输出和标准错误">标准输入、标准输出和标准错误</h3>
<p>每当运行一个新程序时，所有的 shell 都为其打开 3
个文件描述符，即标准输入、标准输出以及标准错误。如果不做特殊处理，例如就像简单的命令<code>ls</code>，则这3个描述符都链接向终端。大多数shell都提供一种方法，使其中任何一个或所有这3个描述符都能重新定向到某个文件，例如：</p>
<p><code>ls &gt; file.list</code></p>
<p>执行ls命令，其标准输出重新定向到名为 <code>file.list</code>
的文件。</p>
<h3 id="不带缓冲的io">不带缓冲的I/O</h3>
<p>函数<code>open</code>、<code>read</code>、<code>write</code>、<code>lseek</code>以及<code>close</code>提供了不带缓冲的I/O。这些函数都使用文件描述符。</p>
<h3 id="标准io">标准I/O</h3>
<p>标准I/O函数为那些不带缓冲的I/O函数提供了一个带缓冲的接口。使用标准I/O函数无需担心如何选取最佳的缓冲区大小。最熟悉的标准I/O函数是<code>printf</code>。在调用<code>printf</code>的程序中，总是包含<code>&lt;stdio.h&gt;</code>，该头文件包括了所有标准I/O函数的原型。</p>
<hr />
<h2 id="程序和进程">程序和进程</h2>
<h3 id="程序">程序</h3>
<p>程序（program）是一个存储在磁盘上某个目录中的可执行文件。内核使用<code>exec</code>函数（7个exec函数之一），将程序读入内存，并执行程序。</p>
<h3 id="进程和进程id">进程和进程ID</h3>
<p>程序的执行实例被称为进程（process）。某些操作系统用任务（task）表示正在被执行的程序。UNIX系统确保每个进程都有一个唯一的数字标识符，称为进程ID（process
ID）。进程ID总是一个非负整数。</p>
<h3 id="进程控制">进程控制</h3>
<p>有3个用于进程控制的主要函数：<code>fork</code>、<code>exec</code>和<code>waitpid</code>。（<code>exec</code>函数有7种变体，但经常把它们统称为exec函数。）</p>
<h3 id="线程和线程id">线程和线程ID</h3>
<p>通常，一个进程只有一个控制线程（thread）–
某一时刻执行的一组机器指令。对于某些问题，如果有多个控制线程分别作用于它的不同部分，那么解决起来就容易得多。另外，多个控制线程也可以充分利用多处理器系统的并行能力。</p>
<p>一个进程内的所有线程共享同一地址空间、文件描述符、栈以及与进程相关的属性。因为它们能访问同一存储区，所以各线程在访问共享数据时需要采取同步措施以避免不一致性。</p>
<p>与进程相同，线程也用ID标识。但是，线程ID只在它所属的进程内起作用。一个进程中的线程
ID
在另一个进程中没有意义。当在一进程中对某个特定线程进行处理时，我们可以使用该线程的ID引用它。</p>
<p>控制线程的函数与控制进程的函数类似，但另有一套。</p>
<hr />
<h2 id="出错处理">出错处理</h2>
<p>当UNIX系统函数出错时，通常会返回一个负值，而且整型变量<code>errno</code>通常被设置为具有特定信息的值。例如，<code>open</code>函数如果成功执行则返回一个非负文件描述符，如出错则返回−1。在<code>open</code>出错时，有大约15种不同的errno值（文件不存在、权限问题等）。而有些函数对于出错则使用另一种约定而不是返回负值。例如，大多数返回指向对象指针的函数，在出错时会返回一个null指针。</p>
<p>文件<code>&lt;errno.h&gt;</code>中定义了errno以及可以赋与它的各种常量。这些常量都以字符E开头。</p>
<p>在Linux中，出错常量在errno(3)手册页中列出。</p>
<p>在支持线程的环境中，进程地址空间会在多个线程之间共享，而每个线程都需要有自己的<code>errno</code>本地副本，以防止一个线程干扰另一个线程。例如，Linux通过将<code>errno</code>定义为：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="type">int</span> *_ _errno_location(<span class="type">void</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">define</span> errno (*_ _errno_location())</span></span><br></pre></td></tr></table></figure>
<p>来支持对它的多线程访问。</p>
<p>C标准定义了两个函数，它们用于打印出错信息。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="type">char</span> *<span class="title function_">strerror</span><span class="params">(<span class="type">int</span> errnum)</span>;</span><br><span class="line"><span class="comment">// 返回值：指向消息字符串的指针</span></span><br></pre></td></tr></table></figure>
<p><code>strerror</code>函数将
errnum（通常就是errno值）映射为一个出错消息字符串，并且返回此字符串的指针。</p>
<p><code>perror</code>函数基于 errno
的当前值，在标准错误上产生一条出错消息，然后返回。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">perror</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *msg)</span>;</span><br></pre></td></tr></table></figure>
<p>它首先输出由 msg 指向的字符串，然后是一个冒号，一个空格，接着是对应于
errno 值的出错消息，最后是一个换行符。</p>
<hr />
<h2 id="用户标识">用户标识</h2>
<h3 id="用户id">用户ID</h3>
<p>口令文件登录项中的用户ID（user
ID）是一个数值，它向系统标识各个不同的用户。</p>
<h3 id="组id">组ID</h3>
<p>口令文件登录项也包括用户的组ID（group
ID），它是一个数值。组ID也是由系统管理员在指定用户登录名时分配的。一般来说，在口令文件中有多个登录项具有相同的组ID。组被用于将若干用户集合到项目或部门中去。这种机制允许同组的各个成员之间共享资源（如文件）。</p>
<p>组文件将组名映射为数值的组ID。组文件通常是<code>/etc/group</code>。</p>
<h3 id="附属组id">附属组ID</h3>
<p>除了在口令文件中对一个登录名指定一个组ID外，大多数UNIX系统版本还允许一个用户属于另外一些组。它允许一个用户属于多至16个其他的组。登录时，读文件<code>/etc/group</code>，寻找列有该用户作为其成员的前16个记录项就可以得到该用户的附属组ID（supplementary
group ID）。</p>
<hr />
<h2 id="信号">信号</h2>
<p>信号（signal）用于通知进程发生了某种情况。例如，若某一进程执行除法操作，其除数为0，则将名为SIGFPE（浮点异常）的信号发送给该进程。进程有以下3种处理信号的方式。</p>
<p>（1）忽略信号。有些信号表示硬件异常，例如，除以0或访问进程地址空间以外的存储单元等，这些异常产生的后果不确定，所以不推荐使用这种处理方式。</p>
<p>（2）按系统默认方式处理。对于除数为0，系统默认方式是终止该进程。</p>
<p>（3）提供一个函数，信号发生时调用该函数，这被称为捕捉该信号。通过提供自编的函数，就能知道什么时候产生了信号，并按期望的方式处理它。</p>
<p>很多情况都会产生信号。终端键盘上有两种产生信号的方法，分别称为中断键（interrupt
key，通常是 <kbd>Delete</kbd> 键或 <kbd>Ctrl</kbd> +
<kbd>C</kbd>）和退出键（quit key，通常是<kbd>Ctrl</kbd> +
<kbd>\</kbd>），它们被用于中断当前运行的进程。另一种产生信号的方法是调用
<code>kill</code>
函数。在一个进程中调用此函数就可向另一个进程发送一个信号。当然这样做也有些限制：当向一个进程发送信号时，我们必须是那个进程的所有者或者是超级用户。</p>
<hr />
<h2 id="时间值">时间值</h2>
<p>历史上，UNIX系统使用过两种不同的时间值。</p>
<p>（1）日历时间。该值是自协调世界时（Coordinated Universal
Time，UTC）1970年1月1日00:00:00这个特定时间以来所经过的秒数累计值。这些时间值可用于记录文件最近一次的修改时间等。</p>
<p>系统基本数据类型<code>time_t</code>用于保存这种时间值。</p>
<p>（2）进程时间。也被称为CPU时间，用以度量进程使用的中央处理器资源。进程时间以时钟滴答计算。每秒钟曾经取为50、60或100个时钟滴答。</p>
<p>系统基本数据类型<code>clock_t</code>保存这种时间值。</p>
<p>当度量一个进程的执行时间时，UNIX系统为一个进程维护了3个进程时间值：</p>
<ul>
<li>时钟时间；</li>
<li>用户CPU时间；</li>
<li>系统CPU时间。</li>
</ul>
<p>时钟时间又称为墙上时钟时间（wall clock
time），它是进程运行的时间总量，其值与系统中同时运行的进程数有关。</p>
<p>用户CPU时间是执行用户指令所用的时间量。</p>
<p>系统CPU时间是为该进程执行内核程序所经历的时间。例如，每当一个进程执行一个系统服务时，如read或write，在内核内执行该服务所花费的时间就计入该进程的系统CPU时间。用户CPU时间和系统CPU时间之和常被称为CPU时间。</p>
<p>可使用<code>time</code>命令取得任一进程的时钟时间、用户时间和系统时间，如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /usr/include</span><br><span class="line"><span class="keyword">time</span> -p grep _POSIX_SOURCE */*.h &gt; /dev/null</span><br></pre></td></tr></table></figure>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">real 1.16</span><br><span class="line">user 0.02</span><br><span class="line">sys 0.36</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="系统调用和库函数">系统调用和库函数</h2>
<p>所有的操作系统都提供多种服务的入口点，由此程序向内核请求服务。各种版本的UNIX实现都提供良好定义、数量有限、直接进入内核的入口点，这些入口点被称为系统调用。</p>
<p>应用程序既可以调用系统调用也可以调用库函数。很多库函数则会调用系统调用。下图显示了这种差别。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260122190220788.png" alt="Difference between C library functions and system calls" style="zoom:50%;" /></p>
<p>系统调用和库函数之间的另一个差别是：<strong>系统调用通常提供一种最小接口，而库函数通常提供比较复杂的功能</strong>。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：UNIX Standardization and Implementations（二）</title>
    <url>/2025/04/26/notes-reading/computer/apue/apue-02-unix-standardization-and-implementations/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第2章 UNIX Standardization and
Implementations 的一些知识点。</p>
<span id="more"></span>
<hr />
<p>所有标准化工作的一个重要部分是规定每种环境实现必须定义的各种限制。</p>
<h2 id="unix-standardization">UNIX Standardization</h2>
<h3 id="iso-c">ISO C</h3>
<p>ISO
C标准的目的是确保符合标准的C程序能够移植到多种操作系统上，而不仅仅是UNIX系统。该标准不仅定义了这门编程语言的语法和语义，还定义了一个标准库。</p>
<h3 id="ieee-posix">IEEE POSIX</h3>
<p>POSIX是由IEEE最初制定的一系列标准。POSIX代表可移植操作系统接口。它最初仅指IEEE
1003.1-1988标准——操作系统接口，但后来扩展到包含许多带有1003编号的标准和标准草案，包括shell和实用程序（1003.2）。</p>
<p>本书特别关注的是1003.1操作系统接口标准，其目标是促进应用程序在各种UNIX系统环境之间的可移植性。</p>
<p>由于1003.1标准规定的是接口而非实现，因此系统调用和库函数之间没有区别。该标准中的所有例程都被称为函数。</p>
<h3 id="the-single-unix-specification">The Single UNIX
Specification</h3>
<p>The Single UNIX Specification, a superset of the POSIX.1 standard,
specifies additional interfaces that extend the functionality provided
by the POSIX.1 specification. POSIX.1 is equivalent to the Base
Specifications portion of the Single UNIX Specification.</p>
<p>Only XSI-conforming implementations can be called UNIX systems.</p>
<h3 id="总结与比较">总结与比较</h3>
<table>
<colgroup>
<col style="width: 12%" />
<col style="width: 29%" />
<col style="width: 29%" />
<col style="width: 29%" />
</colgroup>
<thead>
<tr class="header">
<th><strong>维度</strong></th>
<th><strong>ISO C</strong></th>
<th><strong>POSIX</strong></th>
<th><strong>SUS（Single UNIX Specification）</strong></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>定义主体</strong></td>
<td>国际标准化组织（ISO）</td>
<td>电气电子工程师协会（IEEE）</td>
<td>The Open Group（继承自 X/Open）</td>
</tr>
<tr class="even">
<td><strong>核心目标</strong></td>
<td>统一 C 语言本身的语法、标准库，确保跨编译器兼容</td>
<td>统一类 Unix 系统的接口，确保应用跨系统兼容</td>
<td>定义“合规 Unix 系统”的完整标准，整合并扩展 POSIX</td>
</tr>
<tr class="odd">
<td><strong>覆盖范围</strong></td>
<td>C 语言语法（如关键字、类型）、标准库（如
<code>stdio.h</code>、<code>stdlib.h</code>）</td>
<td>系统调用（如 <code>open</code>、<code>fork</code>）、命令行工具（如
<code>ls</code>）、Shell 接口等</td>
<td>包含 POSIX 全部内容，新增更多接口（如 XSI 扩展）、命令和库函数</td>
</tr>
<tr class="even">
<td><strong>强制性</strong></td>
<td>编译器需完全实现以声称“符合 ISO C 标准”</td>
<td>系统可部分实现（标记为“可选”），但需声明支持范围</td>
<td>若声称“符合 SUS”，必须实现所有规定内容（包括 POSIX 可选部分中的 XSI
必选接口）</td>
</tr>
<tr class="odd">
<td><strong>与 Unix 关联</strong></td>
<td>不绑定特定操作系统，适用于所有支持 C 的平台</td>
<td>专为类 Unix 系统设计（如 Linux、macOS）</td>
<td>直接定义“Unix 系统”的标准，是“Unix 商标”认证的依据</td>
</tr>
<tr class="even">
<td><strong>典型内容</strong></td>
<td><code>printf</code>、<code>malloc</code>、<code>strcmp</code>
等语言级函数</td>
<td><code>read</code>、<code>write</code>、<code>exec</code>
等系统调用；<code>pthread</code> 线程库</td>
<td>包含 POSIX 内容 + XSI 扩展（如 <code>msgctl</code>
消息队列、<code>nl_langinfo</code> 本地化函数）</td>
</tr>
</tbody>
</table>
<ul>
<li>ISO C 是“语言标准”，POSIX 是“类 Unix 系统接口标准”，SUS 是“Unix
系统的完整标准”（基于 POSIX 并扩展，更严格）。<br />
</li>
<li>一个符合 SUS 的系统，必然符合 POSIX 和 ISO C；但符合 POSIX 或 ISO C
的系统，未必符合 SUS。</li>
</ul>
<hr />
<h2 id="unix-system-implementations">UNIX System Implementations</h2>
<h3 id="unix-system-v-release-4">UNIX System V Release 4</h3>
<h3 id="bsd">4.4BSD</h3>
<h3 id="freebsd">FreeBSD</h3>
<h3 id="linux">Linux</h3>
<h3 id="mac-os-x">Mac OS X</h3>
<h3 id="solaris">Solaris</h3>
<hr />
<h2 id="限制">限制</h2>
<p>UNIX
系统实现定义了很多幻数和常量，其中有很多已被硬编码到程序中，或用特定的技术确定。</p>
<p>以下两种类型的限制是必需的。</p>
<p>（1）编译时限制（例如，短整型的最大值是什么？）</p>
<p>（2）运行时限制（例如，文件名有多少个字符？）</p>
<p>编译时限制可在头文件中定义。程序在编译时可以包含这些头文件。但是，运行时限制则要求进程调用一个函数获得限制值。</p>
<p>此外，某些限制在特定的实现中可能是固定的——因此可以在头文件中静态定义——但在另一种实现中可能会有所不同，这就需要调用运行时函数。</p>
<p>为了解决这类问题，提供了以下3种限制。</p>
<p>（1）编译时限制（头文件）。</p>
<p>（2）与文件或目录无关的运行时限制（<code>sysconf</code>函数）。</p>
<p>（3）与文件或目录有关的运行时限制（<code>pathconf</code>和<code>fpathconf</code>函数）。</p>
<h3
id="函数sysconfpathconf和fpathconf">函数sysconf、pathconf和fpathconf</h3>
<p>运行时限制可调用下面3个函数之一获得。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">long</span> <span class="title function_">sysconf</span><span class="params">(<span class="type">int</span> name)</span>;</span><br><span class="line"><span class="type">long</span> <span class="title function_">pathconf</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">int</span> name)</span>;</span><br><span class="line"><span class="type">long</span> <span class="title function_">fpathconf</span><span class="params">(<span class="type">int</span> fd, <span class="type">int</span> name)</span>;</span><br><span class="line"><span class="comment">// 所有函数返回值：若成功，返回相应值；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>具体返回值可参考下面的代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">pr_pathconf</span><span class="params">(<span class="type">char</span> *mesg, <span class="type">char</span> *path, <span class="type">int</span> name)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">long</span> val;</span><br><span class="line">    <span class="built_in">fputs</span>(mesg, <span class="built_in">stdout</span>);</span><br><span class="line">    errno = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">if</span> ((val = pathconf(path, name)) &lt; <span class="number">0</span>) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (errno != <span class="number">0</span>) </span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// set errno to EINVAL if the name isn’t one of the appropriate constants.</span></span><br><span class="line">            <span class="keyword">if</span> (errno == EINVAL) </span><br><span class="line">                <span class="built_in">fputs</span>(<span class="string">&quot; (not supported)\n&quot;</span>, <span class="built_in">stdout</span>);</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                err_sys(<span class="string">&quot;pathconf error, path = %s&quot;</span>, path);</span><br><span class="line">        &#125; </span><br><span class="line">        <span class="comment">// An indeterminate value is indicated by returning −1 and not changing the value of errno.</span></span><br><span class="line">        <span class="keyword">else</span> </span><br><span class="line">            <span class="built_in">fputs</span>(<span class="string">&quot; (no limit)\n&quot;</span>, <span class="built_in">stdout</span>);</span><br><span class="line">    &#125; </span><br><span class="line">    <span class="comment">// return the value of the variable (a return value ≥ 0) </span></span><br><span class="line">    <span class="keyword">else</span> </span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot; %ld\n&quot;</span>, val);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="feature-test-macros">Feature Test Macros</h2>
<p>除了POSIX.1和XSI定义的符号外，大多数实现还可以在这些头文件中添加自己的定义。如果想要编译一个程序，使其仅依赖于POSIX定义，且不与任何实现定义的常量发生冲突，就需要定义<code>_POSIX_C_SOURCE</code>这个常量。所有的POSIX.1头文件都会使用这个常量，当<code>_POSIX_C_SOURCE</code>被定义时，就会排除任何实现自己定义的内容。</p>
<p>两种使用方式：</p>
<ol type="1">
<li><code>cc -D_POSIX_C_SOURCE=200809L file.c</code>
这会导致特性测试宏在C程序包含任何头文件之前被定义。</li>
<li>也可以将源文件的第一行设置为
<code>#define _POSIX_C_SOURCE 200809L</code>。</li>
</ol>
<p>要启用《单一UNIX规范》第4版的XSI选项，需要将常量
<code>_XOPEN_SOURCE</code>
定义为700。除了启用XSI选项外，就POSIX.1功能而言，这与将
<code>_POSIX_C_SOURCE</code> 定义为 200809L 具有相同的效果。</p>
<hr />
<h2 id="基本系统数据类型">基本系统数据类型</h2>
<p>头文件<code>&lt;sys/types.h&gt;</code>中定义了某些与实现有关的数据类型，它们被称为基本系统数据类型。还有很多这种数据类型定义在其他头文件中。在头文件中，这些数据类型都是用C的<code>typedef</code>来定义的。它们绝大多数都以<code>_t</code>结尾。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00045.jpg" alt=" 一些常用的基本系统数据类型" style="zoom:50%;" /></p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：File I/O（三）</title>
    <url>/2025/05/02/notes-reading/computer/apue/apue-03-file-io/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第3章 File I/O 的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="文件描述符">文件描述符</h2>
<p>对于内核而言，所有打开的文件都通过文件描述符引用。文件描述符是一个非负整数。当打开一个现有文件或创建一个新文件时，内核向进程返回一个文件描述符。当读、写一个文件时，使用
<code>open</code> 或 <code>creat</code>
返回的文件描述符标识该文件，将其作为参数传送给 <code>read</code> 或
<code>write</code> 。</p>
<p>按照惯例，UNIX系统shell把文件描述符<strong>0与进程的标准输入</strong>关联，文件描述符<strong>1与标准输出</strong>关联，文件描述符<strong>2与标准错误</strong>关联。这是各种shell以及很多应用程序使用的惯例，与UNIX内核无关。</p>
<p>在符合POSIX.1的应用程序中，幻数0、1、2虽然已被标准化，但应当把它们替换成符号常量
<code>STDIN_FILENO</code>、<code>STDOUT_FILENO</code> 和
<code>STDERR_FILENO</code> 以提高可读性。这些常量都在头文件
<code>&lt;unistd.h&gt;</code> 中定义。</p>
<hr />
<h2 id="函数open和openat">函数open和openat</h2>
<p>调用 <code>open</code> 或 <code>openat</code>
函数可以打开或创建一个文件。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">open</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">int</span> oflag,... <span class="comment">/* mode_t mode */</span>)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">openat</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">char</span> *path, <span class="type">int</span> oflag, ... <span class="comment">/* mode_t mode */</span> )</span>;</span><br><span class="line"><span class="comment">// 两函数的返回值：若成功，返回文件描述符；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>将最后一个参数写为…，ISO
C用这种方法表明余下的参数的数量及其类型是可变的。对于<code>open</code>函数而言，仅当创建新文件时才使用最后这个参数。</p>
<p><code>path</code> 参数是要打开或创建文件的名字。<code>oflag</code>
参数可用来说明此函数的多个选项。用下列一个或多个常量进行“或”运算构成
<code>oflag</code>
参数（这些常量在头文件<code>&lt;fcntl.h&gt;</code>中定义）。</p>
<ul>
<li><p><code>O_RDONLY</code> 只读打开。</p></li>
<li><p><code>O_WRONLY</code> 只写打开。</p></li>
<li><p><code>O_RDWR</code> 读、写打开。</p></li>
<li><p><code>O_EXEC</code> 只执行打开。</p></li>
<li><p><code>O_SEARCH</code> 只搜索打开（应用于目录）。</p></li>
</ul>
<p>在这5个常量中必须指定一个且只能指定一个。下列常量则是可选的。</p>
<ul>
<li><p><code>O_APPEND</code> 每次写时都追加到文件的尾端。</p></li>
<li><p><code>O_CLOEXEC</code>
把<code>FD_CLOEXEC</code>常量设置为文件描述符标志。</p></li>
<li><p><code>O_CREAT</code>
若此文件不存在则创建它。使用此选项时，<code>open</code>函数需同时说明第3个参数
<code>mode</code>（<code>openat</code> 函数需说明第4个参数
<code>mode</code>），用 <code>mode</code>
指定该新文件的访问权限位。</p></li>
<li><p><code>O_DIRECTORY</code> 如果 <code>path</code>
引用的不是目录，则出错。</p></li>
<li><p><code>O_EXCL</code>
如果同时指定了<code>O_CREAT</code>，而文件已经存在，则出错。用此可以测试一个文件是否存在，如果不存在，则创建此文件，这使测试和创建两者成为一个原子操作。</p></li>
<li><p><code>O_NOCTTY</code> 如果 <code>path</code>
引用的是终端设备，则不该将设备分配作为此进程的控制终端。</p></li>
<li><p><code>O_NOFOLLOW</code> 如果 <code>path</code>
引用的是一个符号链接，则出错。</p></li>
<li><p><code>O_NONBLOCK</code> 如果 <code>path</code>
引用的是一个FIFO、一个块特殊文件或一个字符特殊文件，则此选项为文件的本次打开操作和后续的I/O操作设置非阻塞方式。</p></li>
<li><p><code>O_SYNC</code> 使每次 <code>write</code>
等待物理I/O操作完成，包括由该 <code>write</code>
操作引起的文件属性更新所需的I/O。</p></li>
<li><p><code>O_TRUNC</code>
如果此文件存在，而且为只写或读-写成功打开，则将其长度截断为0。</p></li>
<li><p><code>O_DSYNC</code>
让每个写入操作等待物理I/O完成，但如果文件属性的更新不影响读取刚写入的数据，就不必等待这些属性更新。</p></li>
<li><p><code>O_RSYNC</code>
让文件描述符上的每个读取操作等待，直到针对文件同一部分的所有未完成写入操作都完成。</p></li>
</ul>
<p>由<code>open</code>和<code>openat</code>函数<strong>返回的文件描述符一定是最小的未用描述符数值</strong>。例如，一个应用程序可以先关闭标准输出（通常是文件描述符1），然后打开另一个文件，执行打开操作前就能了解到该文件一定会在文件描述符1上打开。</p>
<hr />
<h2 id="函数close">函数close</h2>
<p>可调用<code>close</code>函数关闭一个打开文件。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">close</span> <span class="params">(<span class="type">int</span> fd)</span>； <span class="comment">//返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>关闭一个文件时还会释放该进程加在该文件上的所有记录锁。当一个进程终止时，内核自动关闭它所有的打开文件。很多程序都利用了这一功能而不显式地用<code>close</code>关闭打开文件。</p>
<hr />
<h2 id="函数lseek">函数lseek</h2>
<p>每个打开文件都有一个与其相关联的“当前文件偏移量”（current file
offset）。它通常是一个非负整数，用以度量从文件开始处计算的字节数。通常，读、写操作都从当前文件偏移量处开始，并使偏移量增加所读写的字节数。按系统默认的情况，当打开一个文件时，除非指定<code>O_APPEND</code>选项，否则该偏移量被设置为0。</p>
<p>可以调用<code>lseek</code>显式地为一个打开文件设置偏移量。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">off_t</span> <span class="title function_">lseek</span><span class="params">(<span class="type">int</span> fd, <span class="type">off_t</span> offset, <span class="type">int</span> whence)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回新的文件偏移量；若出错，返回为−1</span></span><br></pre></td></tr></table></figure>
<p>对参数offset的解释与参数whence的值有关。</p>
<ul>
<li>若whence是<code>SEEK_SET</code>，则将该文件的偏移量设置为距文件<strong>开始处</strong>offset个字节。</li>
<li>若whence是<code>SEEK_CUR</code>，则将该文件的偏移量设置为其<strong>当前值</strong>加offset，offset可为正或负。</li>
<li>若whence是<code>SEEK_END</code>，则将该文件的偏移量设置为<strong>文件长度</strong>加offset，offset可正可负。</li>
</ul>
<p>若<code>lseek</code>成功执行，则返回新的文件偏移量，为此可以用下列方式确定打开文件的当前偏移量：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">off_t</span> currpos;</span><br><span class="line">currpos = lseek(fd, <span class="number">0</span>, SEEK_CUR);</span><br></pre></td></tr></table></figure>
<p>这种方法也可用来确定所涉及的文件是否可以设置偏移量。如果文件描述符指向的是一个管道、FIFO或网络套接字，则<code>lseek</code>返回−1，并将errno设置为<code>ESPIPE</code>。</p>
<p>通常，文件的当前偏移量应当是一个非负整数，但是，某些设备也可能允许负的偏移量。但对于普通文件，其偏移量必须是非负值。因为偏移量可能是负值，所以在比较<code>lseek</code>的返回值时应当谨慎，不要测试它是否小于0，而要测试它是否等于−1。</p>
<p>因为偏移量（<code>off_t</code>）是带符号数据类型，所以文件的最大长度会减少一半。例如，若<code>off_t</code>是32位整型，则文件最大长度是<span
class="math inline">\(2^{31}-1\)</span>个字节。</p>
<p><code>lseek</code>仅将当前的文件偏移量记录在内核中，它并不引起任何I/O操作。然后，该偏移量用于下一个读或写操作。</p>
<p>文件偏移量可以大于文件的当前长度，在这种情况下，对该文件的下一次写将加长该文件，并在文件中构成一个空洞，这一点是允许的。位于文件中但没有写过的字节都被读为0。</p>
<p>文件中的空洞并不要求在磁盘上占用存储区。具体处理方式与文件系统的实现有关，当定位到超出文件尾端之后写时，对于新写的数据需要分配磁盘块，但是对于原文件尾端和新开始写位置之间的部分则不需要分配磁盘块。</p>
<hr />
<h2 id="函数read">函数read</h2>
<p>调用<code>read</code>函数从打开文件中读数据。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">read</span><span class="params">(<span class="type">int</span> fd, <span class="type">void</span> *buf, <span class="type">size_t</span> nbytes)</span>;</span><br><span class="line"><span class="comment">//返回值：读到的字节数，若已到文件尾，返回0；若出错，返回−1</span></span><br><span class="line"><span class="comment">//如read成功，则返回读到的字节数。如已到达文件的尾端，则返回0。</span></span><br></pre></td></tr></table></figure>
<p>有多种情况可使实际读到的字节数少于要求读的字节数：</p>
<ul>
<li>读普通文件时，在读到要求字节数之前已到达了文件尾端。例如，若在到达文件尾端之前有30个字节，而要求读100个字节，则read返回30。下一次再调用read时，它将返回0（文件尾端）。</li>
<li>当从终端设备读时，通常一次最多读一行。</li>
<li>当从网络读时，网络中的缓冲机制可能造成返回值小于所要求读的字节数。</li>
<li>当从管道或FIFO读时，如若管道包含的字节少于所需的数量，那么read将只返回实际可用的字节数。</li>
<li>当从某些面向记录的设备（如磁带）读时，一次最多返回一个记录。</li>
<li>当一信号造成中断，而已经读了部分数据量时。</li>
</ul>
<hr />
<h2 id="函数write">函数write</h2>
<p>调用<code>write</code>函数向打开文件写数据。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">write</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">void</span> *buf, <span class="type">size_t</span> nbytes)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回已写的字节数；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>其返回值通常与参数 <code>nbytes</code>
的值相同，否则表示出错。write出错的一个常见原因是磁盘已写满，或者超过了一个给定进程的文件长度限制。</p>
<p>对于普通文件，写操作从文件的当前偏移量处开始。如果在打开该文件时，指定了<code>O_APPEND</code>选项，则在每次写操作之前，将文件偏移量设置在文件的当前结尾处。在一次成功写之后，该文件偏移量增加实际写的字节数。</p>
<hr />
<h2 id="io的效率">I/O的效率</h2>
<p>下面的程序使用<code>read</code>和<code>write</code>函数复制一个文件。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;apue.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUFFSIZE 4096</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span>&#123;</span><br><span class="line">  <span class="type">int</span> n;</span><br><span class="line">  <span class="type">char</span> buf[BUFFSIZE];</span><br><span class="line">  <span class="keyword">while</span>((n = read(STDIN_FILENO, buf, BUFFSIZE)) &gt; <span class="number">0</span>)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="keyword">if</span>(write(STDOUT_FILENO, buf, n) != n)</span><br><span class="line">      err_sys(<span class="string">&quot;write error&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span>(n &lt; <span class="number">0</span>)</span><br><span class="line">    err_sys(<span class="string">&quot;read error&quot;</span>);</span><br><span class="line"></span><br><span class="line">  <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如何选取<code>BUFFSIZE</code>值？在回答此问题之前，先用各种不同的<code>BUFFSIZE</code>值来运行此程序。下图显示了用20种不同的缓冲区长度，读516
581 760字节的文件所得到的结果。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00051.jpg" alt="Linux上用不同缓冲长度进行读操作的时间结果" style="zoom:67%;" /></p>
<p>用上面的程序读文件，其标准输出被重新定向到<code>/dev/null</code>上。此测试所用的文件系统是Linux
ext4文件系统，其磁盘块长度为4096字节（磁盘块长度由<code>st_blksize</code>表示）。这也证明了图中系统CPU时间的几个最小值差不多出现在<code>BUFFSIZE</code>为4096及以后的位置，继续增加缓冲区长度对此时间几乎没有影响。</p>
<p>大多数文件系统为改善性能都采用某种 <strong>预读（read
ahead）技术</strong>
。当检测到正进行顺序读取时，系统就试图读入比应用所要求的更多数据，并假想应用很快就会读这些数据。预读的效果可以从图中看出，缓冲区长度小至32字节时的
<strong>时钟时间</strong> 与拥有较大缓冲区长度时的时钟时间几乎一样。</p>
<hr />
<h2 id="文件共享">文件共享</h2>
<p>UNIX系统支持在不同进程间共享打开文件。在介绍<code>dup</code>函数之前，先要说明这种共享。为此先介绍内核用于所有I/O的数据结构。</p>
<p>内核使用3种数据结构表示打开文件，它们之间的关系决定了在文件共享方面一个进程对另一个进程可能产生的影响。</p>
<p>（1）每个进程在进程表中都有一个记录项，记录项中包含一张打开文件描述符表，可将其视为一个矢量，每个描述符占用一项。与每个文件描述符相关联的是：</p>
<p>a．文件描述符标志（close_on_exec）</p>
<p>b．指向一个文件表项的指针。</p>
<p>（2）内核为所有打开文件维持一张文件表。每个文件表项包含：</p>
<p>a．文件状态标志（读、写、添写、同步和非阻塞等）；</p>
<p>b．当前文件偏移量；</p>
<p>c．指向该文件v节点表项的指针。</p>
<p>（3）每个打开文件（或设备）都有一个 v
节点（v-node）结构。v节点包含了文件类型和对此文件进行各种操作函数的指针。对于大多数文件，v节点还包含了该文件的i节点（i-node，索引节点）。这些信息是在打开文件时从磁盘上读入内存的，所以，文件的所有相关信息都是随时可用的。例如，i节点包含了文件的所有者、文件长度、指向文件实际数据块在磁盘上所在位置的指针等。</p>
<p>Linux没有使用v节点，而是使用了通用i节点结构。虽然两种实现有所不同，但概念上v节点与通用i节点相同。两者都指向文件系统特有的i节点结构。</p>
<p>下图显示了一个进程对应的3张表之间的关系。该进程有两个不同的打开文件：一个文件从标准输入打开（文件描述符0），另一个从标准输出打开（文件描述符为1）。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00052.jpg" alt="打开文件的内核数据结构" style="zoom:67%;" /></p>
<p>如果两个独立进程各自打开了同一文件，则有下图中所示的关系。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00053.jpg" alt="两个独立进程各自打开同一个文件" style="zoom:67%;" /></p>
<p>假定第一个进程在文件描述符3上打开该文件，而另一个进程在文件描述符4上打开该文件。打开该文件的每个进程都获得各自的一个文件表项，但对一个给定的文件只有一个v节点表项。之所以每个进程都获得自己的文件表项，是因为这可以使每个进程都有它自己的对该文件的当前偏移量。</p>
<p>给出了这些数据结构后，现在对前面所述的操作进一步说明。</p>
<ul>
<li><p>在完成每个<code>write</code>后，在文件表项中的当前文件偏移量即增加所写入的字节数。如果这导致当前文件偏移量超出了当前文件长度，则将i节点表项中的当前文件长度设置为当前文件偏移量（也就是该文件加长了）。</p></li>
<li><p>如果用<code>O_APPEND</code>标志打开一个文件，则相应标志也被设置到文件表项的文件状态标志中。每次对这种具有追加写标志的文件执行写操作时，文件表项中的当前文件偏移量首先会被设置为i节点表项中的文件长度。这就使得每次写入的数据都追加到文件的当前尾端处。</p></li>
<li><p>若一个文件用<code>lseek</code>定位到文件当前的尾端，则文件表项中的当前文件偏移量被设置为i节点表项中的当前文件长度（注意，这与用<code>O_APPEND</code>标志打开文件是不同的）。</p></li>
<li><p><code>lseek</code>函数只修改文件表项中的当前文件偏移量，不进行任何I/O操作。</p></li>
</ul>
<p>注意，文件描述符标志和文件状态标志在作用范围方面的区别，前者只用于一个进程的一个描述符，而后者则应用于指向该给定文件表项的任何进程中的所有描述符。</p>
<p>到目前为止，本节中所描述的所有内容，对于多个进程读取同一个文件来说都是正常工作的。每个进程都有自己的文件表项，其中包含自己当前的文件偏移量。然而，当多个进程写入同一个文件时，可能会出现意想不到的结果。为了了解如何避免一些意外情况，需要理解原子操作的概念。</p>
<hr />
<h2 id="原子操作">原子操作</h2>
<p>函数<code>pread</code>和<code>pwrite</code></p>
<p>Single UNIX
Specification包括了XSI扩展，该扩展允许原子性地定位并执行I/O。<code>pread</code>和<code>pwrite</code>就是这种扩展。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">pread</span><span class="params">(<span class="type">int</span> fd, <span class="type">void</span> *buf, <span class="type">size_t</span> nbytes, <span class="type">off_t</span> offset)</span>;</span><br><span class="line"><span class="comment">// 返回值：读到的字节数，若已到文件尾，返回0；若出错，返回−1</span></span><br><span class="line"></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">pwrite</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">void</span> *buf, <span class="type">size_t</span> nbytes, <span class="type">off_t</span> offset)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回已写的字节数；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>调用<code>pread</code>相当于调用<code>lseek</code>后调用<code>read</code>，但是<code>pread</code>又与这种顺序调用有下列重要区别。</p>
<ul>
<li><p>调用<code>pread</code>时，无法中断其定位和读操作。</p></li>
<li><p>不更新当前文件偏移量。</p></li>
</ul>
<p>调用<code>pwrite</code>相当于调用<code>lseek</code>后调用<code>write</code>，但也与它们有类似的区别。</p>
<p>一般而言，原子操作（atomic
operation）指的是由多步组成的一个操作。如果该操作原子地执行，则要么执行完所有步骤，要么一步也不执行，不可能只执行所有步骤的一个子集。</p>
<hr />
<h2 id="函数dup和dup2">函数dup和dup2</h2>
<p>下面两个函数都可用来复制一个现有的文件描述符。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">dup</span><span class="params">(<span class="type">int</span> fd)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">dup2</span><span class="params">(<span class="type">int</span> fd, <span class="type">int</span> fd2)</span>;</span><br><span class="line"><span class="comment">// 两函数的返回值：若成功，返回新的文件描述符；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>由<code>dup</code>返回的新文件描述符一定是当前可用文件描述符中的最小数值。对于
<code>dup2</code>，可以用fd2参数指定新描述符的值。如果fd2已经打开，则先将其关闭。如若fd等于fd2，则dup2返回fd2，而不关闭它。否则（<code>fd1 != fd2</code>），fd2
的 <code>FD_CLOEXEC</code> 文件描述符标志会被清除，这样当进程调用
<code>exec</code> 时，fd2 会保持打开状态。</p>
<p>这些函数返回的新文件描述符与参数fd共享同一个文件表项，如图所示。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00054.jpg" alt="dup(1)后的内核数据结构" style="zoom:67%;" /></p>
<p>在此图中，假定进程启动时执行了：<code>newfd = dup(1);</code></p>
<p>当此函数开始执行时，假定下一个可用的描述符是3（这是非常可能的，因为0，1和2都由shell打开）。因为两个描述符指向同一文件表项，所以它们共享同一文件状态标志（读、写、追加等）以及同一当前文件偏移量。</p>
<p>复制一个描述符的另一种方法是使用<code>fcntl</code>函数。实际上，调用<code>dup(fd);</code>等效于<code>fcntl (fd, F_DUPFD, 0);</code>而调用<code>dup2(fd, fd2)；</code>等效于<code>close(fd2); fcntl(fd, F_DUPFD, fd2);</code>。<code>dup2</code>
原子操作，后者分两步执行。</p>
<hr />
<h2 id="函数syncfsync和fdatasync">函数sync、fsync和fdatasync</h2>
<p>传统的UNIX系统实现在内核中设有缓冲区高速缓存或页高速缓存，大多数磁盘I/O都通过缓冲区进行。当我们向文件写入数据时，内核通常先将数据复制到缓冲区中，然后排入队列，晚些时候再写入磁盘。这种方式被称为延迟写（delayed
write）。</p>
<p>通常，当内核需要重用缓冲区来存放其他磁盘块数据时，它会把所有延迟写数据块写入磁盘。为了保证磁盘上实际文件系统与缓冲区中内容的一致性，UNIX系统提供了<code>sync</code>、<code>fsync</code>和<code>fdatasync</code>三个函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">fsync</span><span class="params">(<span class="type">int</span> fd)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fdatasync</span><span class="params">(<span class="type">int</span> fd)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">sync</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure>
<p><code>sync</code>只是将所有修改过的块缓冲区排入写队列，然后就返回，它并不等待实际写磁盘操作结束。</p>
<p>通常，称为 <code>update</code>
的系统守护进程周期性地调用（一般每隔30秒）<code>sync</code>函数。这就保证了定期冲洗（flush）内核的块缓冲区。</p>
<p><code>fsync</code>函数只对由文件描述符fd指定的一个文件起作用，并且等待写磁盘操作结束才返回。<code>fsync</code>可用于数据库这样的应用程序，这种应用程序需要确保修改过的块立即写到磁盘上。</p>
<p><code>fdatasync</code>函数类似于<code>fsync</code>，但它只影响文件的数据部分。而除数据外，<code>fsync</code>还会同步更新文件的属性。</p>
<hr />
<h2 id="函数fcntl">函数fcntl</h2>
<p><code>fcntl</code>函数可以改变已经打开文件的属性。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">fcntl</span><span class="params">(<span class="type">int</span> fd, <span class="type">int</span> cmd, ... <span class="comment">/* int arg */</span>)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，则依赖于cmd（见下）；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>在本节的各实例中，第3个参数总是一个整数，与上面所示函数原型中的注释部分对应。但是在说明记录锁时，第3个参数则是指向一个结构的指针。</p>
<p>fcntl函数有以下5种功能。</p>
<blockquote>
<p>（1）复制一个已有的描述符（cmd=F_DUPFD或F_DUPFD_CLOEXEC）。</p>
<p>（2）获取/设置文件描述符标志（cmd=F_GETFD或F_SETFD）。</p>
<p>（3）获取/设置文件状态标志（cmd=F_GETFL或F_SETFL）。</p>
<p>（4）获取/设置异步I/O所有权（cmd=F_GETOWN或F_SETOWN）。</p>
<p>（5）获取/设置记录锁（cmd=F_GETLK、F_SETLK或F_SETLKW）。</p>
</blockquote>
<p>先说明这11种cmd中的前8种。讨论与进程表项中各文件描述符相关联的文件描述符标志以及每个文件表项中的文件状态标志。</p>
<ul>
<li><p><code>F_DUPFD</code>
复制文件描述符fd。新文件描述符作为函数值返回。它是尚未打开的各描述符中大于或等于第3个参数值（取为整型值）中各值的最小值。新描述符与
fd共享同一文件表项。但是，新描述符有它自己的一套文件描述符标志，其
<code>FD_CLOEXEC</code> 文件描述符标志被清除（这表示该描述符在
<code>exec</code> 时仍保持有效）。</p></li>
<li><p><code>F_DUPFD_CLOEXEC</code>
复制文件描述符，设置与新描述符关联的<code>FD_CLOEXEC</code>文件描述符标志的值，返回新文件描述符。</p></li>
<li><p><code>F_GETFD</code>
对应于fd的文件描述符标志作为函数值返回。当前只定义了一个文件描述符标志
<code>FD_CLOEXEC</code>。</p></li>
<li><p><code>F_SETFD</code>
对于fd设置文件描述符标志。新标志值按第3个参数（整型值）设置。</p></li>
<li><p><code>F_GETFL</code>
对应于fd的文件状态标志作为函数值返回。在说明<code>open</code>函数时，已描述了文件状态标志。</p></li>
<li><p><code>F_SETFL</code>
将文件状态标志设置为第3个参数的值（取为整型值）。可以更改的几个标志是：<code>O_APPEND</code>、<code>O_NONBLOCK</code>、<code>O_SYNC</code>、<code>O_DSYNC</code>、<code>O_RSYNC</code>、<code>O_FSYNC</code>和
<code>O_ASYNC</code>。</p></li>
<li><p><code>F_GETOWN</code>
获取当前接收<code>SIGIO</code>和<code>SIGURG</code>信号的进程ID或进程组ID。</p></li>
<li><p><code>F_SETOWN</code>
设置接收<code>SIGIO</code>和<code>SIGURG</code>信号的进程ID或进程组ID。正的<code>arg</code>指定一个进程ID，负的<code>arg</code>表示等于arg绝对值的一个进程组ID。</p></li>
</ul>
<p><code>fcntl</code>的返回值与命令有关。如果出错，所有命令都返回-1，如果成功则返回某个其他值。下列4个命令有特定返回值：<code>F_DUPFD</code>、<code>F_GETFD</code>、<code>F_GETFL</code>以及<code>F_GETOWN</code>。第1个命令返回新的文件描述符，第2个和第3个命令返回相应的标志，最后一个命令返回一个正的进程ID或负的进程组ID。</p>
<hr />
<h2 id="函数ioctl">函数ioctl</h2>
<p><code>ioctl</code>函数一直是I/O操作的杂物箱。不能用本章中其他函数表示的I/O操作通常都能用<code>ioctl</code>表示。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span>　　<span class="comment">/* System V */</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/ioctl.h&gt;</span> <span class="comment">/* BSD and Linux */</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">ioctl</span><span class="params">(<span class="type">int</span> fd, <span class="type">int</span> request, ...)</span>;</span><br><span class="line"><span class="comment">// 返回值：若出错，返回−1；若成功，返回其他值</span></span><br></pre></td></tr></table></figure>
<p>对于ISO
C原型，它用省略号表示其余参数。但是，通常只有另外一个参数，它常常是指向一个变量或结构的指针。</p>
<p>在此原型中，我们表示的只是<code>ioctl</code>函数本身所要求的头文件。通常，还要求另外的设备专用头文件。例如，除POSIX.1所说明的基本操作之外，终端I/O的<code>ioctl</code>命令都需要头文件<code>&lt;termios.h&gt;</code>。</p>
<p>磁带操作使我们可以在磁带上写一个文件结束标志、倒带、越过指定个数的文件或记录等，用本章中的其他函数（<code>read</code>、<code>write</code>、<code>lseek</code>等）都难于表示这些操作，所以，对这些设备进行操作最容易的方法就是使用<code>ioctl</code>。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Standard I/O Library（五）</title>
    <url>/2025/05/14/notes-reading/computer/apue/apue-05-standard-io-library/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第5章 Standard I/O Library
的一些知识点。标准I/O库是由Dennis Ritchie在1975年左右编写的。</p>
<span id="more"></span>
<hr />
<h2 id="流和file对象">流和FILE对象</h2>
<p>在第3章中，所有I/O函数都是围绕文件描述符的。当打开一个文件时，即返回一个文件描述符，然后该文件描述符就用于后续的I/O操作。而对于标准I/O库，它们的操作是<strong>围绕流</strong>进行的。当用标准I/O库打开或创建一个文件时，我们已使一个流与一个文件相关联。</p>
<p>对于ASCII字符集，一个字符用一个字节表示。对于国际字符集，一个字符可用多个字节表示。标准I/O文件流可用于单字节或多字节（“宽”）字符集。流的定向（stream’s
orientation）决定了所读、写的字符是单字节还是多字节的。当一个流最初被创建时，它并没有定向。如若在未定向的流上使用一个多字节
I/O
函数（见<code>&lt;wchar.h&gt;</code>），则将该流的定向设置为宽定向的。若在未定向的流上使用一个单字节I/O函数，则将该流的定向设为字节定向的。只有两个函数可改变流的定向。
<code>freopen</code> 函数清除一个流的定向；<code>fwide</code>
函数可用于设置流的定向。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;wchar.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">fwide</span><span class="params">(FILE *fp, <span class="type">int</span> mode)</span>;</span><br><span class="line"><span class="comment">// 返回值：若流是宽定向的，返回正值；若流是字节定向的，返回负值；若流是未定向的，返回0</span></span><br></pre></td></tr></table></figure>
<p>根据mode参数的不同值，<code>fwide</code> 函数执行不同的工作。</p>
<ul>
<li><p>如若mode参数值为负，<code>fwide</code>
将试图使指定的流是字节定向的。</p></li>
<li><p>如若mode参数值为正，<code>fwide</code>
将试图使指定的流是宽定向的。</p></li>
<li><p>如若mode参数值为0，<code>fwide</code>
将不试图设置流的定向，但返回标识该流定向的值。</p></li>
</ul>
<p>注意，<code>fwide</code>
并不改变已定向流的定向。还应注意的是，<code>fwide</code>
无出错返回。试想，如若流是无效的，那么将发生什么呢？我们唯一可依靠的是，在调用
<code>fwide</code> 前先清除 errno，从 <code>fwide</code> 返回时检查
errno 的值。</p>
<p>当打开一个流时，标准I/O函数 <code>fopen</code> 返回一个指向
<code>FILE</code>
对象的指针。该对象通常是一个结构，它包含了标准I/O库为管理该流需要的所有信息，包括用于实际I/O的文件描述符、指向用于该流缓冲区的指针、缓冲区的长度、当前在缓冲区中的字符数以及出错标志等。</p>
<hr />
<h2 id="标准输入标准输出和标准错误">标准输入、标准输出和标准错误</h2>
<p>对一个进程预定义了 3 个流，并且这 3
个流可以自动地被进程使用，它们是：标准输入、标准输出和标准错误。这些流引用的文件与文件描述符
<code>STDIN_FILENO</code>、<code>STDOUT_FILENO</code> 和
<code>STDERR_FILENO</code> 所引用的相同。</p>
<p>这3个标准I/O流通过预定义文件指针
<code>stdin</code>、<code>stdout</code> 和 <code>stderr</code>
加以引用。这3个文件指针定义在头文件<code>&lt;stdio.h&gt;</code>中。</p>
<hr />
<h2 id="缓冲">缓冲</h2>
<p>标准I/O库 <strong>提供缓冲的目的是尽可能减少使用 <code>read</code> 和
<code>write</code>
调用的次数</strong>。它也对每个I/O流自动地进行缓冲管理，从而避免了应用程序需要考虑这一点所带来的麻烦。</p>
<p>标准I/O提供了以下3种类型的缓冲。</p>
<blockquote>
<p>（1）全缓冲。在这种情况下，在填满标准I/O缓冲区后才进行实际I/O操作。对于驻留在磁盘上的文件通常是由标准I/O库实施全缓冲的。在一个流上执行第一次I/O操作时，相关标准I/O函数通常调用
<code>malloc</code> 获得需使用的缓冲区。</p>
<p>（2）行缓冲。在这种情况下，当在输入和输出中遇到换行符时，标准I/O库执行I/O操作。这允许我们一次输出一个字符（用标准I/O函数fputc），但只有在写了一行之后才进行实际I/O操作。当流涉及一个终端时（如标准输入和标准输出），通常使用行缓冲。</p>
<p>（3）不带缓冲。标准I/O库不对字符进行缓冲存储。例如，若用标准I/O函数
<code>fputs</code>
写15个字符到不带缓冲的流中，我们就期望这15个字符能立即输出。</p>
<p>标准错误流 <code>stderr</code>
通常是不带缓冲的，这就使得出错信息可以尽快显示出来。</p>
</blockquote>
<p>对任何一个给定的流，如果我们并不喜欢这些系统默认，则可调用下列两个函数中的一个更改缓冲类型。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">setbuf</span><span class="params">(FILE *<span class="keyword">restrict</span> fp, <span class="type">char</span> *<span class="keyword">restrict</span> buf)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">setvbuf</span><span class="params">(FILE *<span class="keyword">restrict</span> fp, <span class="type">char</span> *<span class="keyword">restrict</span> buf, <span class="type">int</span> mode, <span class="type">size_t</span> size)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回非0</span></span><br></pre></td></tr></table></figure>
<p>可以使用 <code>setbuf</code> 函数打开或关闭缓冲机制。为了带缓冲进行
I/O，参数 <code>buf</code> 必须指向一个长度为 <code>BUFSIZ</code>
的缓冲区（该常量定义在<code>&lt;stdio.h&gt;</code>中）。通常在此之后该流就是全缓冲的，但是如果该流与一个终端设备相关，那么某些系统也可将其设置为行缓冲的。为了关闭缓冲，将buf设置为NULL。</p>
<p>使用 <code>setvbuf</code>，我们可以精确地说明所需的缓冲类型。这是用
<code>mode</code> 参数实现的：</p>
<p><code>_IOFBF</code> 全缓冲</p>
<p><code>_IOLBF</code> 行缓冲</p>
<p><code>_IONBF</code> 不带缓冲</p>
<p>如果指定一个不带缓冲的流，则忽略 <code>buf</code> 和
<code>size</code> 参数。如果指定全缓冲或行缓冲，则 <code>buf</code> 和
<code>size</code> 可选择地指定一个缓冲区及其长度。如果该流是带缓冲的，而
<code>buf</code> 是
<code>NULL</code>，则标准I/O库将自动地为该流分配适当长度的缓冲区。适当长度指的是由常量
<code>BUFSIZ</code> 所指定的值。</p>
<p>下图列出了这两个函数的动作，以及它们的各个选项。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00094.jpg" alt="img" style="zoom:67%;" /></p>
<p>任何时候，我们都可强制冲洗一个流。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">fflush</span><span class="params">(FILE *fp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回EOF</span></span><br></pre></td></tr></table></figure>
<p>此函数使该流所有未写的数据都被传送至内核。作为一种特殊情形，如若fp是NULL，则此函数将导致所有输出流被冲洗。</p>
<hr />
<h2 id="打开流">打开流</h2>
<p>下列3个函数打开一个标准I/O流。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line">FILE *<span class="title function_">fopen</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> pathname, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> type)</span>;</span><br><span class="line">FILE *<span class="title function_">freopen</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> pathname, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> type, FILE *<span class="keyword">restrict</span> fp)</span>;</span><br><span class="line">FILE *<span class="title function_">fdopen</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">char</span> *type)</span>;</span><br><span class="line"><span class="comment">// 3个函数的返回值：若成功，返回文件指针；若出错，返回NULL</span></span><br></pre></td></tr></table></figure>
<p>这3个函数的区别如下。</p>
<p>（1）<code>fopen</code> 函数打开路径名为 <code>pathname</code>
的一个指定的文件。</p>
<p>（2）<code>freopen</code>
函数在一个指定的流上打开一个指定的文件，如若该流已经打开，则先关闭该流。若该流已经定向，则使用
freopen
清除该定向。此函数一般用于将一个指定的文件打开为一个预定义的流：标准输入、标准输出或标准错误。</p>
<p>（3）<code>fdopen</code>
函数取一个已有的文件描述符，并使一个标准的I/O流与该描述符相结合。此函数常用于由创建管道和网络通信通道函数返回的描述符。因为这些特殊类型的文件不能用标准I/O函数
<code>fopen</code>
打开，所以我们必须先调用设备专用函数以获得一个文件描述符，然后用
<code>fdopen</code> 使一个标准I/O流与该描述符相结合。</p>
<p><code>type</code> 参数指定对该I/O流的读、写方式，ISO
C规定type参数可以有15种不同的值，如图所示。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00095.jpg" alt="img" style="zoom:67%;" /></p>
<p>使用字符 <code>b</code>
作为type的一部分，这使得标准I/O系统可以区分文本文件和二进制文件。因为UNIX内核并不对这两种文件进行区分，所以在UNIX系统环境下指定字符b作为type的一部分实际上并无作用。</p>
<p>当文件以读写模式（类型中的加号）打开时，有两个限制。（<strong>缓冲区读写切换</strong>）
-
在没有中间的fflush、fseek、fsetpos或rewind操作的情况下，输出不能直接紧跟输入。
-
在没有中间的fseek、fsetpos、rewind操作，或者没有遇到文件结束的输入操作的情况下，输入不能直接紧跟输出。</p>
<p>下图列出了打开一个流的6种不同的方式。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00096.jpg" alt="img" style="zoom:67%;" /></p>
<p>调用 <code>fclose</code> 关闭一个打开的流。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">fclose</span><span class="params">(FILE *fp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回EOF</span></span><br></pre></td></tr></table></figure>
<p>在该文件被关闭之前，冲洗缓冲中的输出数据。缓冲区中的任何输入数据被丢弃。如果标准I/O库已经为该流自动分配了一个缓冲区，则释放此缓冲区。</p>
<p>当一个进程正常终止时（直接调用 <code>exit</code> 函数，或从
<code>main</code>
函数返回），则所有带未写缓冲数据的标准I/O流都被冲洗，所有打开的标准I/O流都被关闭。</p>
<hr />
<h2 id="读和写流">读和写流</h2>
<p>一旦我们打开一个流，就可以从三种类型的无格式输入/输出中进行选择：</p>
<h3 id="每次一个字符的io">每次一个字符的I/O</h3>
<p>以下3个函数可用于一次读一个字符。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">getc</span><span class="params">(FILE *fp)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fgetc</span><span class="params">(FILE *fp)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">getchar</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 3个函数的返回值：若成功，返回下一个字符；若已到达文件尾端或出错，返回EOF</span></span><br></pre></td></tr></table></figure>
<p>函数 <code>getchar</code> 等同于 <code>getc(stdin)</code>
。前两个函数的区别是，<code>getc</code> 可被实现为宏，而
<code>fgetc</code> 不能实现为宏。这意味着以下几点。</p>
<p>（1）getc的参数不应当是具有副作用的表达式，因为它可能会被计算多次。</p>
<p>（2）因为fgetc一定是个函数，所以可以得到其地址。这就允许将fgetc的地址作为一个参数传送给另一个函数。</p>
<p>（3）调用fgetc所需时间很可能比调用getc要长，因为调用函数所需的时间通常长于调用宏。</p>
<p>注意，不管是出错还是到达文件尾端，这3个函数都返回同样的值。为了区分这两种不同的情况，必须调用
<code>ferror</code> 或 <code>feof</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">ferror</span><span class="params">(FILE *fp)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">feof</span><span class="params">(FILE *fp)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若条件为真，返回非0（真）；否则，返回0（假）</span></span><br></pre></td></tr></table></figure>
<p><code>void clearerr(FILE *fp);</code></p>
<p>在大多数实现中，为每个流在FILE对象中维护了两个标志：</p>
<ul>
<li><p>出错标志；</p></li>
<li><p>文件结束标志。</p></li>
</ul>
<p>调用 <code>clearerr</code> 可以清除这两个标志。</p>
<p>从流中读取数据以后，可以调用 <code>ungetc</code>
将字符再压送回流中。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">ungetc</span><span class="params">(<span class="type">int</span> c, FILE *fp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回c；若出错，返回EOF</span></span><br></pre></td></tr></table></figure>
<p>压送回到流中的字符以后又可从流中读出，但读出字符的顺序与压送回的顺序相反。应当了解，虽然ISO
C允许实现支持任何次数的回送，但是它要求实现提供一次只回送一个字符。我们不能期望一次能回送多个字符。</p>
<p>回送的字符，不一定必须是上一次读到的字符。不能回送EOF。但是当已经到达文件尾端时，仍可以回送一个字符。下次读将返回该字符，再读则返回EOF。之所以能这样做的原因是，一次成功的
<code>ungetc</code> 调用会清除该流的文件结束标志。</p>
<p>当正在读一个输入流，并进行某种形式的切词或记号切分操作时，会经常用到回送字符操作。有时需要先看一看下一个字符，以决定如何处理当前字符。然后就需要方便地将刚查看的字符回送，以便下一次调用getc时返回该字符。</p>
<p>用 <code>ungetc</code>
压送回字符时，并没有将它们写到底层文件中或设备上，只是将它们写回标准I/O库的流缓冲区中。</p>
<p>对应于上面所述的每个输入函数都有一个输出函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">putc</span><span class="params">(<span class="type">int</span> c, FILE *fp)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fputc</span><span class="params">(<span class="type">int</span> c, FILE *fp)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">putchar</span><span class="params">(<span class="type">int</span> c)</span>;</span><br><span class="line"><span class="comment">// 3个函数返回值：若成功，返回c；若出错，返回EOF</span></span><br></pre></td></tr></table></figure>
<p>与输入函数一样，<code>putchar(c)</code>等同于<code>putc(c, stdout)</code>，<code>putc</code>
可被实现为宏，而 <code>fputc</code> 不能实现为宏。</p>
<h3 id="每次一行io">每次一行I/O</h3>
<p>下面两个函数提供每次输入一行的功能。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">char</span> *<span class="title function_">fgets</span><span class="params">(<span class="type">char</span> *<span class="keyword">restrict</span> buf, <span class="type">int</span> n，FILE *<span class="keyword">restrict</span> fp)</span>;</span><br><span class="line"><span class="type">char</span> *<span class="title function_">gets</span><span class="params">(<span class="type">char</span> *buf)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回buf；若已到达文件尾端或出错，返回NULL</span></span><br></pre></td></tr></table></figure>
<p>这两个函数都指定了缓冲区的地址，读入的行将送入其中。<code>gets</code>
从标准输入读，而 <code>fgets</code> 则从指定的流读。</p>
<p>对于 <code>fgets</code>
，必须指定缓冲的长度n。此函数一直读到下一个换行符为止，但是不超过
<code>n − 1</code>
个字符，读入的字符被送入缓冲区。该缓冲区以null字节结尾。如若该行包括最后一个换行符的字符数超过
<code>n − 1</code>，则 <code>fgets</code>
只返回一个不完整的行，但是，缓冲区总是以null字节结尾。对fgets的下一次调用会继续读该行。</p>
<p><code>gets</code> 删除换行符，而 <code>fgets</code>
则保留换行符。尽量只用 <code>fgets</code>。</p>
<p>下面两个函数提供每次输出一行的功能。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">fputs</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> str, FILE *<span class="keyword">restrict</span> fp)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">puts</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *str)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回非负值；若出错，返回EOF</span></span><br></pre></td></tr></table></figure>
<p>函数 <code>fputs</code>
将一个以null字节终止的字符串写到指定的流，尾端的终止符null不写出。注意，这并不一定是每次输出一行，因为字符串不需要换行符作为最后一个非null字节。通常，在null字节之前是一个换行符，但并不要求总是如此。</p>
<p><code>puts</code>
将一个以null字节终止的字符串写到标准输出，终止符不写出。但是，<code>puts</code>
随后又将一个换行符写到标准输出。</p>
<p>puts 并不像它所对应的 gets
那样不安全。但是我们还是应避免使用它，以免需要记住它在最后是否添加了一个换行符。如果总是使用
<code>fgets</code> 和 <code>fputs</code>,
那么就会熟知在每行终止处我们必须自己处理换行符。</p>
<h3 id="二进制io">二进制I/O</h3>
<p>如果进行二进制I/O操作，那么我们更愿意一次读或写一个完整的结构。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">size_t</span> <span class="title function_">fread</span><span class="params">(<span class="type">void</span> *<span class="keyword">restrict</span> ptr, <span class="type">size_t</span> size, <span class="type">size_t</span> nobj, FILE *<span class="keyword">restrict</span> fp)</span>;</span><br><span class="line"><span class="type">size_t</span> <span class="title function_">fwrite</span><span class="params">(<span class="type">const</span> <span class="type">void</span> *<span class="keyword">restrict</span> ptr, <span class="type">size_t</span> size, <span class="type">size_t</span> nobj, FILE *<span class="keyword">restrict</span> fp)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：读或写的对象数</span></span><br></pre></td></tr></table></figure>
<p><code>size</code> 是结构的大小，<code>nobj</code>是元素个数。</p>
<p>fread和fwrite返回读或写的对象数。对于读，如果出错或到达文件尾端，则此数字可以少于nobj。在这种情况，应调用
<code>ferror</code> 或 <code>feof</code>
以判断究竟是那一种情况。对于写，如果返回值少于所要求的
<code>nobj</code>，则出错。</p>
<hr />
<h2 id="定位流">定位流</h2>
<p>有3种方法定位标准I/O流。</p>
<p>（1）<code>ftell</code> 和 <code>fseek</code>
函数。假定文件的位置可以存放在一个长整型中。</p>
<p>（2）<code>ftello</code> 和 <code>fseeko</code> 函数。使用
<code>off_t</code>数据类型代替了长整型。</p>
<p>（3）<code>fgetpos</code> 和 <code>fsetpos</code>
函数。使用抽象数据类型 <code>fpos_t</code>
记录文件的位置。这种数据类型可以根据需要定义为一个足够大的数，用以记录文件位置。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">long</span> <span class="title function_">ftell</span><span class="params">(FILE *fp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回当前文件位置指示；若出错，返回-1L</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">fseek</span><span class="params">(FILE *fp, <span class="type">long</span> offset, <span class="type">int</span> whence)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">rewind</span><span class="params">(FILE *fp)</span>;</span><br></pre></td></tr></table></figure>
<p>对于二进制文件，其文件位置指示器是从文件起始位置开始度量，并以字节为度量单位的。<code>ftell</code>
用于二进制文件时，其返回值就是这种字节位置。为了用<code>fseek</code>
定位一个二进制文件，必须指定一个字节
<code>offset</code>，以及解释这种偏移量的方式。<code>whence</code>
的值与 <code>lseek</code> 函数的相同：<code>SEEK_SET</code>
表示从文件的起始位置开始，<code>SEEK_CUR</code>
表示从当前文件位置开始，<code>SEEK_END</code> 表示从文件的尾端开始。</p>
<p>对于文本文件，它们的文件当前位置可能不以简单的字节偏移量来度量。这主要也是在非UNIX系统中，它们可能以不同的格式存放文本文件。为了定位一个文本文件，whence一定要是
<code>SEEK_SET</code>，而且 <code>offset</code>
只能有两种值：0（后退到文件的起始位置），或是对该文件的
<code>ftell</code> 所返回的值。使用
<code>rewind</code>函数也可将一个流设置到文件的起始位置。</p>
<p>除了偏移量的类型是 <code>off_t</code> 而非 <code>long</code>
以外，<code>ftello</code> 函数与 <code>ftell</code>
相同，<code>fseeko</code>函数与 <code>fseek</code>相同。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">off_t</span> <span class="title function_">ftello</span><span class="params">(FILE *fp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回当前文件位置；若出错，返回(off_t)-1</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">fseeko</span><span class="params">(FILE *fp, <span class="type">off_t</span> offset, <span class="type">int</span> whence)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">fgetpos</span><span class="params">(FILE *<span class="keyword">restrict</span> fp, <span class="type">fpos_t</span> *<span class="keyword">restrict</span> pos)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fsetpos</span><span class="params">(FILE *fp, <span class="type">const</span> <span class="type">fpos_t</span> *pos)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回0；若出错，返回非0</span></span><br></pre></td></tr></table></figure>
<p><code>fgetpos</code> 将文件位置指示器的当前值存入由 <code>pos</code>
指向的对象中。在以后调用 <code>fsetpos</code>
时，可以使用此值将流重新定位至该位置。</p>
<hr />
<h2 id="格式化io">格式化I/O</h2>
<h3 id="格式化输出">格式化输出</h3>
<p>格式化输出是由5个 <code>printf</code> 函数来处理的。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">printf</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, ...)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fprintf</span><span class="params">(FILE *<span class="keyword">restrict</span> fp, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, ...)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">dprintf</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, ...)</span>;</span><br><span class="line"><span class="comment">// 3个函数返回值：若成功，返回输出字符数；若输出出错，返回负值</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">sprintf</span><span class="params">(<span class="type">char</span> *<span class="keyword">restrict</span> buf, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, ...)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回存入数组的字符数；若编码出错，返回负值</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">snprintf</span><span class="params">(<span class="type">char</span> *<span class="keyword">restrict</span> buf, <span class="type">size_t</span> n, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, ...)</span>;</span><br><span class="line"><span class="comment">// 返回值：若缓冲区足够大，返回将要存入数组的字符数；若编码出错，返回负值</span></span><br></pre></td></tr></table></figure>
<p><code>printf</code> 将格式化数据写到标准输出，<code>fprintf</code>
写至指定的流，<code>dprintf</code>
写至指定的文件描述符，<code>sprintf</code>
将格式化的字符送入数组buf中。<code>sprintf</code>
在该数组的尾端自动加一个 null 字节，但该字符不包括在返回值中。</p>
<p>注意，<code>sprintf</code>
函数可能会造成由buf指向的缓冲区的溢出。调用者有责任确保该缓冲区足够大。因为缓冲区溢出会造成程序不稳定甚至安全隐患，为了解决这种缓冲区溢出问题，引入了
<code>snprintf</code>
函数。在该函数中，缓冲区长度是一个显式参数，超过缓冲区尾端写的所有字符都被丢弃。如果缓冲区足够大，<code>snprintf</code>函数就会返回写入缓冲区的字符数。与sprintf相同，该返回值不包括结尾的null字节。若
<code>snprintf</code>
函数返回小于缓冲区长度n的正值，那么没有截断输出。若发生了一个编码的错误，<code>snprintf</code>
返回负值。</p>
<p>格式说明控制其余参数如何编码，以后又如何显示。每个参数按照转换说明编码，转换说明以百分号
<code>%</code>
开始，除转换说明外，格式字符串中的其他字符将按原样，不经任何修改被复制输出。一个转换说明有4个可选择的部分，下面将它们都示于方括号中：</p>
<p><code>%[flags][fldwidth][precision][lenmodifier]convtype</code></p>
<p>下图总结了各种 <code>flags</code> 标志。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00101.jpg" alt="img" style="zoom: 67%;" /></p>
<p><code>fldwidth</code>
说明最小字段宽度。转换后参数字符数若小于宽度，则多余字符位置用空格填充。字段宽度是一个非负十进制数，或是一个星号（*）。</p>
<p><code>precision</code>
说明整型转换后最少输出数字位数、浮点数转换后小数点后的最少位数、字符串转换后最大字节数。精度是一个点（.），其后跟随一个可选的非负十进制数或一个星号（*）。</p>
<p><code>lenmodifier</code> 说明参数长度。其可能的值示于下图中。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00102.jpg" alt="img" style="zoom:67%;" /></p>
<p><code>convtype</code>
不是可选的。它控制如何解释参数。下图列出了各种转换类型字符。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00103.jpg" alt="img" style="zoom:67%;" /></p>
<p>下列5种
<code>printf</code>族的变体类似于上面的5种，但是可变参数表（…）替换成了
<code>arg</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdarg.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">vprintf</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, va_list arg)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">vfprintf</span><span class="params">(FILE *<span class="keyword">restrict</span> fp, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, va_list arg)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">vdprintf</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, va_list arg)</span>;</span><br><span class="line"><span class="comment">// 所有3个函数返回值：若成功，返回输出字符数；若输出出错，返回负值</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">vsprintf</span><span class="params">(<span class="type">char</span> *<span class="keyword">restrict</span> buf, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, va_list arg)</span>;</span><br><span class="line"><span class="comment">// 函数返回值：若成功，返回存入数组的字符数；若编码出错，返回负值</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">vsnprintf</span><span class="params">(<span class="type">char</span> *<span class="keyword">restrict</span> buf, <span class="type">size_t</span> n, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, va_list arg)</span>;</span><br><span class="line"><span class="comment">// 函数返回值：若缓冲区足够大，返回存入数组的字符数；若编码出错，返回负值</span></span><br></pre></td></tr></table></figure>
<h3 id="格式化输入">格式化输入</h3>
<p>执行格式化输入处理的是3个 <code>scanf</code> 函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">scanf</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, ...)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fscanf</span><span class="params">(FILE *<span class="keyword">restrict</span> fp, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, ...)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">sscanf</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> buf, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, ...)</span>;</span><br><span class="line"><span class="comment">// 3个函数返回值：赋值的输入项数；若输入出错或在任一转换前已到达文件尾端，返回EOF</span></span><br></pre></td></tr></table></figure>
<p><code>scanf</code>
族用于分析输入字符串，并将字符序列转换成指定类型的变量。在格式之后的各参数包含了变量的地址，用转换结果对这些变量赋值。</p>
<p>格式说明控制如何转换参数，以便对它们赋值。转换说明以%字符开始。<strong>除转换说明和空白字符外,
格式字符串中的其他字符必须与输入匹配.</strong>
若有一个字符不匹配，则停止后续处理，不再读输入的其余部分。</p>
<p>一个转换说明有3个可选择的部分，下面将它们都示于方括号中：</p>
<p><code>%[*][fldwidth][m][lenmodifier]convtype</code></p>
<p>可选择的星号（*）用于抑制转换。按照转换说明的其余部分对输入进行转换，但转换结果并不存放在参数中。</p>
<p><code>fldwidth</code>
说明最大宽度（即最大字符数）。<code>lenmodifier</code>
说明要用转换结果赋值的参数大小。</p>
<p><code>convtype</code> 字段类似于 <code>printf</code>
族的转换类型字段，但两者之间还有些差别。一个差别是，作为一种选项，输入中带符号的可赋予无符号类型。例如，输入流中的-1可被转换成4
294 967 295赋予无符号整型变量。下图总结了 <code>scanf</code>
族函数支持的转换类型。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00104.jpg" alt="img" style="zoom:67%;" /></p>
<p>在字段宽度和长度修饰符之间的可选项 <code>m</code>
是赋值分配符。它可以用于 <code>%c</code>、<code>%s</code> 以及
<code>%[</code>
转换符，迫使内存缓冲区分配空间以接纳转换字符串。在这种情况下，相关的参数必须是指针地址，分配的缓冲区地址必须复制给该指针。如果调用成功，该缓冲区不再使用时，由调用者负责通过调用
<code>free</code> 函数来释放该缓冲区。</p>
<p>与 <code>printf</code> 族相同，<code>scanf</code> 族也使用由
<code>&lt;stdarg.h&gt;</code> 说明的可变长度参数表。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdarg.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">vscanf</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, va_list arg)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">vfscanf</span><span class="params">(FILE *<span class="keyword">restrict</span> fp, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, va_list arg)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">vsscanf</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> buf, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, va_list arg)</span>;</span><br><span class="line"><span class="comment">// 3个函数返回值：指定的输入项目数；若输入出错或在任一转换前文件结束，返回EOF</span></span><br></pre></td></tr></table></figure>
<p>每个标准I/O流都有一个与其相关联的文件描述符，可以对一个流调用
<code>fileno</code> 函数以获得其描述符。</p>
<p>注意，fileno不是ISO C标准部分，而是POSIX.1支持的扩展。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">fileno</span><span class="params">(FILE *fp)</span>;</span><br><span class="line"><span class="comment">// 返回值：与该流相关联的文件描述符</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="临时文件">临时文件</h2>
<p>ISO C 标准 I/O
库提供了两个函数以帮助<strong>创建临时文件</strong>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">char</span> *<span class="title function_">tmpnam</span><span class="params">(<span class="type">char</span> *ptr)</span>;</span><br><span class="line"><span class="comment">// 返回值：指向唯一路径名的指针</span></span><br><span class="line"></span><br><span class="line">FILE *<span class="title function_">tmpfile</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回文件指针；若出错，返回NULL</span></span><br></pre></td></tr></table></figure>
<p><code>tmpnam</code>
函数产生一个与现有文件名不同的一个有效路径名字符串。每次调用它时，都产生一个不同的路径名，最多调用次数是
<code>TMP_MAX</code>。<code>TMP_MAX</code>
定义在<code>&lt;stdio.h&gt;</code>中。</p>
<p>若 <code>ptr</code>
是NULL，则所产生的路径名存放在一个静态区中，指向该静态区的指针作为函数值返回。后续调用
<code>tmpnam</code>
时，会重写该静态区（这意味着，如果我们调用此函数多次，而且想保存路径名，则我们应当保存该路径名的副本，而不是指针的副本）。如若
<code>ptr</code>
不是NULL，则认为它应该是指向长度至少是<code>L_tmpnam</code>
个字符的数组（常量 <code>L_tmpnam</code> 定义在头文件
<code>&lt;stdio.h&gt;</code>
中）。所产生的路径名存放在该数组中，<code>ptr</code>
也作为函数值返回。</p>
<p><code>tmpfile</code>
创建一个临时二进制文件（类型wb+），在关闭该文件或程序结束时将自动删除这种文件。</p>
<p>Single UNIX
Specification为处理临时文件定义了另外两个函数：<code>mkdtemp</code> 和
<code>mkstemp</code>，它们是XSI的扩展部分。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="type">char</span> *<span class="title function_">mkdtemp</span><span class="params">(<span class="type">char</span> *template)</span>;</span><br><span class="line"><span class="comment">//返回值：若成功，返回指向目录名的指针；若出错，返回NULL</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">mkstemp</span><span class="params">(<span class="type">char</span> *template)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回文件描述符；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p><code>mkdtemp</code>
函数创建了一个目录，该目录有一个唯一的名字；<code>mkstemp</code>
函数创建了一个文件，该文件有一个唯一的名字。名字是通过
<code>template</code>字符串进行选择的。这个字符串是后6位设置为
<code>XXXXXX</code>
的路径名。函数将这些占位符替换成不同的字符来构建一个唯一的路径名。如果成功的话，这两个函数将修改<code>template</code>
字符串反映临时文件的名字。</p>
<p>由 <code>mkdtemp</code>
函数创建的目录使用下列访问权限位集：<code>S_IRUSR | S_IWUSR | S_IXUSR</code>。注意，调用进程的文件模式创建屏蔽字可以进一步限制这些权限。如果目录创建成功，<code>mkdtemp</code>
返回新目录的名字。</p>
<p><code>mkstemp</code>函数以唯一的名字创建一个普通文件并且打开该文件，该函数返回的文件描述符以读写方式打开。由
<code>mkstemp</code> 创建的文件使用访问权限位
<code>S_IRUSR | S_IWUSR</code>。</p>
<p>与 <code>tempfile</code>不同，<code>mkstemp</code>
创建的临时文件并不会自动删除。如果希望从文件系统命名空间中删除该文件，必须自己对它解除链接。</p>
<hr />
<h2 id="内存流">内存流</h2>
<p>标准I/O库把数据缓存在内存中，因此每次一字符和每次一行的I/O更有效。也可以通过调用
<code>setbuf</code> 或 <code>setvbuf</code>
函数让I/O库使用我们自己的缓冲区。在SUSv4中支持了内存流。这就是标准I/O流，虽然仍使用FILE指针进行访问，但其实并没有底层文件。所有的I/O都是通过在缓冲区与主存之间来回传送字节来完成的。我们将看到，即便这些流看起来像文件流，它们的某些特征使其<strong>更适用于字符串操作</strong>。</p>
<p>有3个函数可用于内存流的创建，第一个是 <code>fmemopen</code>函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line">FILE *<span class="title function_">fmemopen</span><span class="params">(<span class="type">void</span> *<span class="keyword">restrict</span> buf, <span class="type">size_t</span> size, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> type)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回流指针；若错误，返回NULL</span></span><br></pre></td></tr></table></figure>
<p><code>fmemopen</code>函数允许调用者提供缓冲区用于内存流：<code>buf</code>
参数指向缓冲区的开始位置，<code>size</code>
参数指定了缓冲区大小的字节数。如果<code>buf</code>
参数为空，<code>fmemopen</code> 函数分配 <code>size</code>
字节数的缓冲区。在这种情况下，当流关闭时缓冲区会被释放。</p>
<p><code>type</code> 参数控制如何使用流。<code>type</code>
可能的取值如图所示。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250227214831313.png" alt="image-20250227214831301" style="zoom:67%;" /></p>
<p>注意，这些取值对应于基于文件的标准I/O流的type参数取值，但其中有些微小差别。第一，无论何时以追加写方式打开内存流时，当前文件位置设为缓冲区中的第一个null字节。如果缓冲区中不存在null字节，则当前位置就设为缓冲区结尾的后一个字节。当流并不是以追加写方式打开时，当前位置设为缓冲区的开始位置。因为追加写模式通过第一个null字节确定数据的尾端，内存流并不适合存储二进制数据（二进制数据在数据尾端之前就可能包含多个null字节）。</p>
<p>第二，如果buf参数是一个null指针，打开流进行读或者写都没有任何意义。因为在这种情况下缓冲区是通过fmemopen进行分配的，没有办法找到缓冲区的地址，只写方式打开流意味着无法读取已写入的数据，同样，以读方式打开流意味着只能读取那些我们无法写入的缓冲区中的数据。</p>
<p>第三，任何时候需要增加流缓冲区中数据量以及调用fclose、fflush、fseek、fseeko以及fsetpos时都会在当前位置写入一个null字节。</p>
<p>用于创建内存流的其他两个函数分别是 <code>open_memstream</code> 和
<code>open_wmemstream</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line">FILE *<span class="title function_">open_memstream</span><span class="params">(<span class="type">char</span> **bufp, <span class="type">size_t</span> *sizep)</span>;</span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;wchar.h&gt;</span></span></span><br><span class="line">FILE *<span class="title function_">open_wmemstream</span><span class="params">(<span class="type">wchar_t</span> **bufp, <span class="type">size_t</span> *sizep)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回流指针；若出错，返回NULL</span></span><br></pre></td></tr></table></figure>
<p><code>open_memstream</code>
函数创建的流是面向字节的，<code>open_wmemstream</code>
函数创建的流是面向宽字节的。这两个函数与 <code>fmemopen</code>
函数的不同在于：</p>
<ul>
<li><p>创建的流只能写打开；</p></li>
<li><p>不能指定自己的缓冲区，但可以分别通过 <code>bufp</code> 和
<code>sizep</code> 参数访问缓冲区地址和大小；</p></li>
<li><p>关闭流后需要自行释放缓冲区；</p></li>
<li><p>对流添加字节会增加缓冲区大小。</p></li>
</ul>
<p>但是在缓冲区地址和大小的使用上必须遵循一些原则。第一，缓冲区地址和长度只有在调用
<code>fclose</code> 或 <code>fflush</code>
后才有效；第二，这些值只有在下一次流写入或调用 <code>fclose</code>
前才有效。因为缓冲区可以增长，可能需要重新分配。如果出现这种情况，我们会发现缓冲区的内存地址值在下一次调用
<code>fclose</code> 或<code>fflush</code> 时会改变。</p>
<p>因为避免了缓冲区溢出，内存流非常适用于创建字符串。因为内存流只访问主存，不访问磁盘上的文件，所以对于把标准I/O流作为参数用于临时文件的函数来说，会有很大的性能提升。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：System Data Files and Information（六）</title>
    <url>/2025/05/20/notes-reading/computer/apue/apue-06-system-data-files-and-information/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第6章 System Data Files and
Information 的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="password-file">Password File</h2>
<p>UNIX 系统口令文件（POSIX.1
则将其称为用户数据库）包含了下图中所示的各字段，这些字段包含在
<code>&lt;pwd.h&gt;</code> 中定义的 <code>passwd</code> 结构中。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00114.jpg" alt="img" style="zoom: 67%;" /></p>
<p>由于历史原因，口令文件是 <code>/etc/passwd</code>，而且是一个 ASCII
文件。每一行包含上图所示的各字段，字段之间用冒号分隔。</p>
<p>POSIX.1定义了两个获取口令文件项的函数。在给出用户登录名或用户ID后，这两个函数就能查看相关项。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;pwd.h&gt;</span></span></span><br><span class="line"><span class="keyword">struct</span> passwd *<span class="title function_">getpwuid</span><span class="params">(<span class="type">uid_t</span> uid)</span>;</span><br><span class="line"><span class="keyword">struct</span> passwd *<span class="title function_">getpwnam</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回指针；若出错，返回NULL</span></span><br></pre></td></tr></table></figure>
<p><code>getpwuid</code> 函数由 <code>ls</code>
程序使用，它将i节点中的数字用户ID映射为用户登录名。在键入登录名时，<code>getpwnam</code>
函数由 <code>login</code> 程序使用。</p>
<p>这两个函数都返回一个指向 <code>passwd</code>
结构的指针，该结构已由这两个函数在执行时填入信息。<code>passwd</code>
结构通常是函数内部的静态变量，只要调用任一相关函数，其内容就会被重写。</p>
<p>如果要查看的只是登录名或用户ID，那么这两个POSIX.1函数能满足要求，但是也有些程序要查看整个口令文件。下列3个函数则可用于此种目的。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pwd.h&gt;</span></span></span><br><span class="line"><span class="keyword">struct</span> passwd *<span class="title function_">getpwent</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回指针；若出错或到达文件尾端，返回NULL</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">setpwent</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">endpwent</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure>
<p>基本POSIX.1标准没有定义这3个函数。在Single UNIX
Specification中，它们被定义为XSI扩展。因此，可预期所有UNIX实现都将提供这些函数。</p>
<p>调用 <code>getpwent</code>
时，它返回口令文件中的下一个记录项。如同上面所述的两个POSIX.1函数一样，它返回一个由它填写好的
<code>passwd</code>
结构的指针。每次调用此函数时都重写该结构。在第一次调用该函数时，它打开它所使用的各个文件。在使用本函数时，对口令文件中各个记录项的安排顺序并无要求。某些系统采用散列算法对
<code>/etc/passwd</code>文件中各项排序。</p>
<p>函数 <code>setpwent</code>
反绕（重置位置到开头）它所使用的文件，<code>endpwent</code>
则关闭这些文件。在使用 <code>getpwent</code>
查看完口令文件后，一定要调用 <code>endpwent</code>
关闭这些文件。<code>getpwent</code>
知道什么时间应当打开它所使用的文件（第一次被调用时），但是它并不知道何时关闭这些文件。</p>
<hr />
<h2 id="shadow-passwords">Shadow Passwords</h2>
<p>某些系统将加密口令存放在另一个通常称为阴影口令（shadow
password）的文件中。该文件至少要包含用户名和加密口令。与该口令相关的其他信息也可存放在该文件
<code>/etc/shadow</code> 中。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00117.jpg" alt="img" style="zoom:67%;" /></p>
<p>只有用户登录名和加密口令这两个字段是必须的。其他的字段控制口令更改的频率，或者说口令的衰老以及账户仍然处于活动状态的时间。</p>
<p>阴影口令文件不应是一般用户可以读取的。仅有少数几个程序需要访问加密口令，如
<code>login</code>和 <code>passwd</code>，这些程序常常是设置用户 ID 为
root 的程序。</p>
<p>有一组函数可用于访问阴影口令文件。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;shadow.h&gt;</span></span></span><br><span class="line"><span class="keyword">struct</span> spwd *<span class="title function_">getspnam</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name)</span>;</span><br><span class="line"><span class="keyword">struct</span> spwd *<span class="title function_">getspent</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回指针；若出错，返回NULL</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">setspent</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">endspent</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="group-file">Group File</h2>
<p>UNIX组文件
<code>/etc/group</code>（POSIX.1称其为组数据库）包含了下图所示字段。这些字段包含在
<code>&lt;grp.h&gt;</code>中所定义的 <code>group</code> 结构中。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00118.jpg" alt="img" style="zoom:67%;" /></p>
<p>字段 <code>gr_mem</code>
是一个指针数组，其中每个指针指向一个属于该组的用户名。该数组以
<code>null</code>
指针结尾。可以使用以下两个由POSIX.1定义的函数来查找组名或数字形式的组ID。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;grp.h&gt;</span></span></span><br><span class="line"><span class="keyword">struct</span> group *<span class="title function_">getgrgid</span><span class="params">(<span class="type">gid_t</span> gid)</span>;</span><br><span class="line"><span class="keyword">struct</span> group *<span class="title function_">getgrnam</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回指针；若出错，返回NULL</span></span><br></pre></td></tr></table></figure>
<p>如同对口令文件进行操作的函数一样，这两个函数通常也返回指向一个静态变量的指针，在每次调用时都重写该静态变量。</p>
<p>如果需要搜索整个组文件，则须使用另外几个函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;grp.h&gt;</span></span></span><br><span class="line"><span class="keyword">struct</span> group *<span class="title function_">getgrent</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回指针；若出错或到达文件尾端，返回NULL</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">setgrent</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">endgrent</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="supplementary-group-ids">Supplementary Group IDs</h2>
<p>我们不仅可以属于口令文件记录项中组ID所对应的组，也可属于多至16个另外的组。文件访问权限检查相应被修改为：不仅将进程的有效组ID与文件的组ID相比较，而且也将所有附属组ID与文件的组ID进行比较。</p>
<p>附属组 ID 是 POSIX.1 要求的特性。常量 <code>NGROUPS_MAX</code>
规定了附属组ID的数量，其常用值是16。</p>
<p>使用附属组 ID
的优点是不必再显式地经常更改组。一个用户会参与多个项目，因此也就要同时属于多个组，此类情况是常有的。</p>
<p>为了获取和设置附属组ID，提供了下列3个函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">getgroups</span><span class="params">(<span class="type">int</span> gidsetsize, <span class="type">gid_t</span> grouplist[])</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回附属组ID数量；若出错，返回-1</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;grp.h&gt;</span> <span class="comment">/* on Linux */</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span> <span class="comment">/* on FreeBSD, Mac OS X, and Solaris */</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">setgroups</span><span class="params">(<span class="type">int</span> ngroups, <span class="type">const</span> <span class="type">gid_t</span> grouplist[])</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;grp.h&gt;</span> <span class="comment">/* on Linux and Solaris */</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span> <span class="comment">/* on FreeBSD and Mac OS X */</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">initgroups</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *username, <span class="type">gid_t</span> basegid)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p><code>getgroups</code> 将进程所属用户的各附属组ID填写到数组
<code>grouplist</code> 中，填写入该数组的附属组ID数最多为
<code>gidsetsize</code> 个。实际填写到数组中的附属组ID数由函数返回。</p>
<p>作为一种特殊情况，如若 <code>gidsetsize</code>
为0，则函数只返回附属组ID数，而对数组 <code>grouplist</code>
则不做修改。</p>
<p><code>setgroups</code>
可由超级用户调用以便为调用进程设置附属组ID表。<code>grouplist</code>
是组ID数组，而 <code>ngroups</code>
说明了数组中的元素数。<code>ngroups</code>
的值不能大于<code>NGROUPS_MAX</code>。</p>
<p>通常，只有 <code>initgroups</code> 函数调用
<code>setgroups</code>，<code>initgroups</code>
读整个组文件（用前面说明的函数
<code>getgrent</code>、<code>setgrent</code> 和
<code>endgrent</code>），然后对 <code>username</code>
确定其组的成员关系。然后，它调用
<code>setgroups</code>，以便为该用户初始化附属组ID表。因为<code>initgroups</code>要调用
<code>setgroups</code>，所以只有超级用户才能调用
<code>initgroups</code>。除了在组文件中找到 <code>username</code>
是成员的所有组， <code>initgroups</code> 也在附属组ID表中包括了
<code>basegid</code>。<code>basegid</code> 是 <code>username</code>
在口令文件中的组ID。</p>
<p>只有少数几个程序调用 <code>initgroups</code>，例如 <code>login</code>
程序在用户登录时调用该函数。</p>
<hr />
<h2 id="其他数据文件">其他数据文件</h2>
<p>在日常操作中，UNIX系统还使用很多其他文件。例如，BSD网络软件有一个记录各网络服务器所提供服务的数据文件（<code>/etc/services</code>），有一个记录协议信息的数据文件（<code>/etc/protocols</code>），还有一个则是记录网络信息的数据文件（<code>/etc/networks</code>）。幸运的是，对于这些数据文件的接口都与上述对口令文件和组文件的相似。</p>
<p>一般情况下，对于每个数据文件至少有3个函数。</p>
<blockquote>
<p>（1）get函数：读下一个记录，如果需要，还会打开该文件。此种函数通常返回指向一个结构的指针。当已达到文件尾端时返回空指针。大多数get函数返回指向一个静态存储类结构的指针，如果要保存其内容，则需复制结构。</p>
<p>（2）set
函数：打开相应数据文件（如果尚末打开），然后反绕该文件。如果希望在相应文件起始处开始处理，则调用此函数。</p>
<p>（3）end函数：关闭相应数据文件。如前所述，在结束了对相应数据文件的读、写操作后，总应调用此函数以关闭所有相关文件。</p>
</blockquote>
<p>另外，如果数据文件支持某种形式的键搜索，则也提供搜索具有指定键的记录的例程。例如，对于口令文件，提供了两个按键进行搜索的程序：<code>getpwnam</code>寻找具有指定用户名的记录；<code>getpwuid</code>
寻找具有指定用户ID的记录。</p>
<p>下图列出了一些这样的例程，这些都是UNIX常用的。对于图中列出的所有数据文件都有get、set和end函数。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00120.jpg" alt="img" style="zoom:67%;" /></p>
<hr />
<h2 id="登录账户记录">登录账户记录</h2>
<p>大多数UNIX系统都提供下列两个数据文件：<code>utmp</code>
文件记录当前登录到系统的各个用户；<code>wtmp</code>
文件跟踪各个登录和注销事件。在V7中，每次写入这两个文件中的是包含下列结构的一个二进制记录：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">utmp</span> &#123;</span></span><br><span class="line">    <span class="type">char</span> ut_line[<span class="number">8</span>]; <span class="comment">/* tty line: &quot;ttyh0&quot;, &quot;ttyd0&quot;, &quot;ttyp0&quot;, ... */</span></span><br><span class="line">    <span class="type">char</span> ut_name[<span class="number">8</span>]; <span class="comment">/* login name */</span></span><br><span class="line">    <span class="type">long</span>　ut_time;　　<span class="comment">/* seconds since Epoch */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>登录时，<code>login</code> 程序填写此类型结构，然后将其写入到
<code>utmp</code> 文件中，同时也将其添写到 <code>wtmp</code>
文件中。注销时，<code>init</code> 进程将 <code>utmp</code>
文件中相应的记录擦除（每个字节都填以null字节），并将一个新记录添写到
<code>wtmp</code> 文件中。在 <code>wtmp</code>
文件的注销记录中，<code>ut_name</code>
字段清除为0。在系统再启动时，以及更改系统时间和日期的前后，都在
<code>wtmp</code> 文件中追加写特殊的记录项。<code>who</code> 程序读取
<code>utmp</code> 文件，并以可读格式打印其内容。后来的UNIX版本提供
<code>last</code>命令，它读 <code>wtmp</code>
文件并打印所选择的记录。</p>
<hr />
<h2 id="系统标识">系统标识</h2>
<p>POSIX.1定义了 <code>uname</code>
函数，它返回与当前主机和操作系统有关的信息。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/utsname.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">uname</span><span class="params">(<span class="keyword">struct</span> utsname *name)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回非负值；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>通过该函数的参数向其传递一个 <code>utsname</code>
结构的地址，然后该函数填写此结构。POSIX.1只定义了该结构中最少需提供的字段（它们都是字符数组），而每个数组的长度则由实现确定。某些实现在该结构中提供了另外一些字段。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">utsname</span> &#123;</span></span><br><span class="line">    <span class="type">char</span> sysname[ ]; <span class="comment">/* name of the operating system */</span></span><br><span class="line">    <span class="type">char</span> nodename[ ]; <span class="comment">/* name of this node */</span></span><br><span class="line">    <span class="type">char</span> release[ ]; <span class="comment">/* current release of operating system */</span></span><br><span class="line">    <span class="type">char</span> version[ ]; <span class="comment">/* current version of this release */</span></span><br><span class="line">    <span class="type">char</span> machine[ ]; <span class="comment">/* name of hardware type */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p><code>gethostname</code>函数只返回主机名，该名字通常就是TCP/IP网络上主机的名字。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">gethostname</span><span class="params">(<span class="type">char</span> *name, i n t namelen)</span>;</span><br><span class="line"><span class="comment">//返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>如果宿主机联接到TCP/IP网络中，则此主机名通常是该主机的完整域名。</p>
<p><code>hostname(1)</code>命令可用来获取和设置主机名（超级用户用一个类似的函数
<code>sethostname</code>来设置主机名）。主机名通常在系统自举时设置，由
<code>/etc/rc</code> 或 <code>init</code> 调用的某个启动文件。</p>
<hr />
<h2 id="时间和日期例程">时间和日期例程</h2>
<p>由UNIX内核提供的基本时间服务是计算自协调世界时（Coordinated Universal
Time，UTC）公元1970年1月1日00:00:00这一特定时间以来经过的秒数。这种秒数是以数据类型
<code>time_t</code>
表示的，称它们为日历时间。日历时间包括时间和日期。UNIX在这方面与其他操作系统的区别是：（a）以协调统一时间而非本地时间计时；（b）可自动进行转换，如变换到夏令时；（c）将时间和日期作为一个量值保存。</p>
<p><code>time</code> 函数返回当前时间和日期。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="type">time_t</span> <span class="title function_">time</span><span class="params">(<span class="type">time_t</span> *calptr)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回时间值；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>时间值作为函数值返回。如果参数非空，则时间值也存放在由
<code>calptr</code> 指向的单元内。</p>
<p>POSIX.1的实时扩展增加了对多个系统时钟的支持。在《单一UNIX规范》第4版中，用于控制这些时钟的接口从一个选项组移到了基础部分。
时钟由 <code>clockid_t</code> 类型标识。标准值汇总于下表中。</p>
<table>
<colgroup>
<col style="width: 29%" />
<col style="width: 26%" />
<col style="width: 43%" />
</colgroup>
<thead>
<tr class="header">
<th>Identifier</th>
<th>Option</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>CLOCK_REALTIME</code></td>
<td>—</td>
<td>real system time</td>
</tr>
<tr class="even">
<td><code>CLOCK_MONOTONIC</code></td>
<td><code>_POSIX_MONOTONIC_CLOCK</code></td>
<td>real system time with no negative jumps</td>
</tr>
<tr class="odd">
<td><code>CLOCK_PROCESS_CPUTIME_ID</code></td>
<td><code>_POSIX_CPUTIME</code></td>
<td>CPU time for calling process</td>
</tr>
<tr class="even">
<td><code>CLOCK_THREAD_CPUTIME_ID</code></td>
<td><code>_POSIX_THREAD_CPUTIME</code></td>
<td>CPU time for calling thread</td>
</tr>
</tbody>
</table>
<p><code>clock_gettime</code> 函数可用于获取指定时钟的时间，返回的时间在
<code>timespec</code> 结构中，它把时间表示为秒和纳秒。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">clock_gettime</span><span class="params">(<span class="type">clockid_t</span> clock_id, <span class="keyword">struct</span> timespec *tsp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>当时钟ID设置为 <code>CLOCK_REALTIME</code>
时，<code>clock_gettime</code>
函数提供了与time函数类似的功能，不过在系统支持高精度时间值的情况下，<code>clock_gettime</code>
可能比 <code>time</code> 函数得到更高精度的时间值。</p>
<p>可以使用 <code>clock_getres</code> 函数来确定特定系统时钟的分辨率。
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">clock_getres</span><span class="params">(<span class="type">clockid_t</span> clock_id, <span class="keyword">struct</span> timespec *tsp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure></p>
<p><code>clock_getres</code> 函数把参数 <code>tsp</code> 指向的
<code>timespec</code> 结构初始化为与 <code>clock_id</code>
参数对应的时钟精度。例如，如果精度为1毫秒，则 <code>tv_sec</code>
字段就是0，<code>tv_nsec</code> 字段就是1 000 000。</p>
<p>要对特定的时钟设置时间，可以调用 <code>clock_settime</code>
函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">clock_settime</span><span class="params">(<span class="type">clockid_t</span> clock_id, <span class="type">const</span> <span class="keyword">struct</span> timespec *tsp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p><code>gettimeofday</code> 函数提供的分辨率（高达一微秒）比
<code>time</code>函数更高。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">gettimeofday</span><span class="params">(<span class="keyword">struct</span> timeval *<span class="keyword">restrict</span> tp, <span class="type">void</span> *<span class="keyword">restrict</span> tzp)</span>;</span><br><span class="line"><span class="comment">// Returns: 0 always</span></span><br></pre></td></tr></table></figure>
<p>一旦取得这种从上述特定时间经过的秒数的整型时间值后，通常要调用函数将其转换为分解的时间结构，然后调用另一个函数生成人们可读的时间和日期。下图说明了各种时间函数之间的关系。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00123.jpg" alt="img" style="zoom:67%;" /></p>
<p>两个函数 <code>localtime</code> 和 <code>gmtime</code>
将日历时间转换成分解的时间，并将这些存放在一个tm结构中。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span>　 <span class="title">tm</span> &#123;</span>　　　　 <span class="comment">/* a broken-down time */</span></span><br><span class="line">    <span class="type">int</span>　　tm_sec;　　　 <span class="comment">/* seconds after the minute: [0 - 60] */</span></span><br><span class="line">    <span class="type">int</span>　　tm_min;　　　 <span class="comment">/* minutes after the hour: [0 - 59] */</span></span><br><span class="line">    <span class="type">int</span>　　tm_hour;　　 <span class="comment">/* hours after midnight: [0 - 23] */</span></span><br><span class="line">    <span class="type">int</span>　　tm_mday;　　 <span class="comment">/* day of the month: [1 - 31] */</span></span><br><span class="line">    <span class="type">int</span>　　tm_mon;　　　 <span class="comment">/* months since January: [0 - 11] */</span></span><br><span class="line">    <span class="type">int</span>　　tm_year;　　 <span class="comment">/* years since 1900 */</span></span><br><span class="line">    <span class="type">int</span>　　tm_wday;　　 <span class="comment">/* days since Sunday: [0 - 6] */</span></span><br><span class="line">    <span class="type">int</span>　　tm_yday;　　 <span class="comment">/* days since January 1: [0 - 365] */</span></span><br><span class="line">    <span class="type">int</span>　　tm_isdst;　　<span class="comment">/* daylight saving time flag: &lt;0, 0, &gt;0 */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="keyword">struct</span> tm *<span class="title function_">gmtime</span><span class="params">(<span class="type">const</span> <span class="type">time_t</span> *calptr)</span>;</span><br><span class="line"><span class="keyword">struct</span> tm *<span class="title function_">localtime</span><span class="params">(<span class="type">const</span> <span class="type">time_t</span> *calptr)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：指向分解的tm结构的指针；若出错，返回NULL</span></span><br></pre></td></tr></table></figure>
<p><code>localtime</code> 和 <code>gmtime</code>
之间的区别是：<code>localtime</code>
将日历时间转换成本地时间（考虑到本地时区和夏令时标志），而
<code>gmtime</code>
则将日历时间转换成协调统一时间的年、月、日、时、分、秒、周日分解结构。</p>
<p>函数 <code>mktime</code> 以本地时间的年、月、日等作为参数，将其变换成
<code>time_t</code> 值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="type">time_t</span> <span class="title function_">mktime</span><span class="params">(<span class="keyword">struct</span> tm *tmptr)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回日历时间；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>函数 <code>strftime</code> 是一个类似于 <code>printf</code>
的时间值函数。它非常复杂，可以通过可用的多个参数来定制产生的字符串。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="type">size_t</span> <span class="title function_">strftime</span><span class="params">(<span class="type">char</span> *<span class="keyword">restrict</span> buf, <span class="type">size_t</span> maxsize, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, <span class="type">const</span> <span class="keyword">struct</span> tm *<span class="keyword">restrict</span> tmptr)</span>;</span><br><span class="line"><span class="type">size_t</span> <span class="title function_">strftime_l</span><span class="params">(<span class="type">char</span> *<span class="keyword">restrict</span> buf, <span class="type">size_t</span> maxsize, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, <span class="type">const</span> <span class="keyword">struct</span> tm *<span class="keyword">restrict</span> tmptr, <span class="type">locale_t</span> locale)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若有空间，返回存入数组的字符数；否则，返回0</span></span><br></pre></td></tr></table></figure>
<p><code>strftime_l</code>
允许调用者将区域指定为参数，除此之外，<code>strftime</code> 和
<code>strftime_l</code> 函数是相同的。</p>
<p><code>tmptr</code> 参数是要格式化的时间值，由一个指向分解时间值
<code>tm</code> 结构的指针说明。格式化结果存放在一个长度为
<code>maxsize</code>
个字符的buf数组中，如果buf长度足以存放格式化结果及一个null终止符，则该函数返回在buf中存放的字符数（不包括null终止符）；否则该函数返回0。</p>
<p>format参数控制时间值的格式。如同printf函数一样，转换说明的形式是百分号之后跟一个特定字符。format中的其他字符则按原样输出。两个连续的百分号在输出中产生一个百分号。下图列出了37种ISO
C规定的转换说明。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00124.jpg" alt="img" style="zoom:50%;" /></p>
<p><code>strptime</code> 函数是 <code>strftime</code>
的反过来版本，把字符串时间转换成分解时间。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="type">char</span> *<span class="title function_">strptime</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> buf, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> format, <span class="keyword">struct</span> tm *<span class="keyword">restrict</span> tmptr)</span>;</span><br><span class="line"><span class="comment">// 返回值：指向解析的字符的下一个字符的指针；否则，返回NULL</span></span><br></pre></td></tr></table></figure>
<p><code>format</code>
参数给出了buf参数指向的缓冲区内的字符串的格式。虽然与
<code>strftime</code>
函数的说明稍有不同，但格式说明是类似的。<code>strptime</code>
函数转换说明符列在下图。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00126.jpg" alt="img" style="zoom:50%;" /></p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Files and Directories（四）</title>
    <url>/2025/05/08/notes-reading/computer/apue/apue-04-files-and-directories/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第4章 Files and Directories
的一些知识点。</p>
<p>本章将描述文件的性质。将从 stat 函数开始，逐个说明 stat
结构的每一个成员以了解文件的所有属性。</p>
<span id="more"></span>
<hr />
<h2
id="函数statfstatfstatat和lstat">函数stat、fstat、fstatat和lstat</h2>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/stat.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">stat</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> pathname, <span class="keyword">struct</span> stat *<span class="keyword">restrict</span> buf)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fstat</span><span class="params">(<span class="type">int</span> fd, <span class="keyword">struct</span> stat *buf)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">lstat</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> pathname, <span class="keyword">struct</span> stat *<span class="keyword">restrict</span> buf)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fstatat</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> pathname, <span class="keyword">struct</span> stat *<span class="keyword">restrict</span> buf, <span class="type">int</span> flag)</span>;</span><br><span class="line"><span class="comment">// 所有4个函数的返回值：若成功；返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>Linux中<code>stat</code>结构的基本形式是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/stat.h&gt;</span></span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">stat</span> &#123;</span></span><br><span class="line">    <span class="type">dev_t</span>      st_dev;      <span class="comment">/* ID of device containing file */</span></span><br><span class="line">    <span class="type">ino_t</span>      st_ino;      <span class="comment">/* Inode number */</span></span><br><span class="line">    <span class="type">mode_t</span>     st_mode;     <span class="comment">/* File type and mode */</span></span><br><span class="line">    <span class="type">nlink_t</span>    st_nlink;    <span class="comment">/* Number of hard links */</span></span><br><span class="line">    <span class="type">uid_t</span>      st_uid;      <span class="comment">/* User ID of owner */</span></span><br><span class="line">    <span class="type">gid_t</span>      st_gid;      <span class="comment">/* Group ID of owner */</span></span><br><span class="line">    <span class="type">dev_t</span>      st_rdev;     <span class="comment">/* Device ID (if special file) */</span></span><br><span class="line">    <span class="type">off_t</span>      st_size;     <span class="comment">/* Total size, in bytes */</span></span><br><span class="line">    <span class="type">blksize_t</span>  st_blksize;  <span class="comment">/* Block size for filesystem I/O */</span></span><br><span class="line">    <span class="type">blkcnt_t</span>   st_blocks;   <span class="comment">/* Number of 512 B blocks allocated</span></span><br><span class="line"><span class="comment">    /* Since POSIX.1-2008, this structure supports nanosecond</span></span><br><span class="line"><span class="comment">       precision for the following timestamp fields.</span></span><br><span class="line"><span class="comment">       For the details before POSIX.1-2008, see VERSIONS. */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">timespec</span>  <span class="title">st_atim</span>;</span>  <span class="comment">/* Time of last access */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">timespec</span>  <span class="title">st_mtim</span>;</span>  <span class="comment">/* Time of last modification */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">timespec</span>  <span class="title">st_ctim</span>;</span>  <span class="comment">/* Time of last status change */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> st_atime  st_atim.tv_sec  <span class="comment">/* Backward compatibility */</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> st_mtime  st_mtim.tv_sec</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> st_ctime  st_ctim.tv_sec</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p><code>timespec</code>结构类型按照秒和纳秒定义了时间，至少包括下面两个字段：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">time_t</span> tv_sec;</span><br><span class="line"><span class="type">long</span> tv_nsec;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="文件类型">文件类型</h2>
<p>文件类型包括如下几种:</p>
<p>（1）普通文件（regular file）。</p>
<p>（2）目录文件（directory
file）。这种文件包含了其他文件的名字以及指向与这些文件有关信息的指针。对一个目录文件具有读权限的任一进程都可以读该目录的内容，但只有内核可以直接写目录文件。进程必须使用本章介绍的函数才能更改目录。</p>
<p>（3）块特殊文件（block special
file）。这种类型的文件提供对设备（如磁盘）带缓冲的访问，每次访问以固定长度为单位进行。</p>
<p>（4）字符特殊文件（character special
file）。这种类型的文件提供对设备不带缓冲的访问，每次访问长度可变。系统中的所有设备要么是字符特殊文件，要么是块特殊文件。</p>
<p>（5）FIFO。这种类型的文件用于进程间通信，有时也称为命名管道（named
pipe）。</p>
<p>（6）套接字（socket）。这种类型的文件用于进程间的网络通信。套接字也可用于在一台宿主机上进程之间的非网络通信。</p>
<p>（7）符号链接（symbolic link）。这种类型的文件指向另一个文件。</p>
<p>文件类型信息包含在stat结构的<code>st_mode</code>成员中。可以用下图中的宏确定文件类型。这些宏的参数都是<code>stat</code>结构中的<code>st_mode</code>成员。</p>
<table>
<thead>
<tr class="header">
<th>宏</th>
<th>文件类型</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>S_ISREG()</code></td>
<td>普通文件</td>
</tr>
<tr class="even">
<td><code>S_ISDIR()</code></td>
<td>目录文件</td>
</tr>
<tr class="odd">
<td><code>S_ISCHR()</code></td>
<td>字符特殊文件</td>
</tr>
<tr class="even">
<td><code>S_ISBLK()</code></td>
<td>块特殊文件</td>
</tr>
<tr class="odd">
<td><code>S_ISFIFO</code></td>
<td>管道或FIFO</td>
</tr>
<tr class="even">
<td><code>S_ISLINK</code></td>
<td>符号链接</td>
</tr>
<tr class="odd">
<td><code>S_ISSOCK()</code></td>
<td>套接字</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="设置用户id和设置组id">设置用户ID和设置组ID</h2>
<p>在 Unix/Linux 中，每个进程有三个与用户ID相关的标识符：</p>
<ol type="1">
<li><strong>real user
ID（真实用户ID，RUID）</strong>：标识进程的实际拥有者（登录用户）。</li>
<li><strong>effective user
ID（有效用户ID，EUID）</strong>：决定进程当前的权限（如能否访问文件、执行特权操作）。</li>
<li><strong>saved
set-user-ID（保存的设置用户ID，SUID）</strong>：保存了程序启动时的有效用户ID（设置set-user-id位时是程序文件的所有者ID），作为权限切换的“备份”或“中转站”。</li>
</ol>
<p><strong>通常，有效用户ID等于实际用户ID，有效组ID等于实际组ID</strong>。</p>
<p>每个文件有一个所有者和组所有者，所有者由<code>stat</code>结构中的<code>st_uid</code>指定，组所有者则由<code>st_gid</code>指定。</p>
<p>当执行一个程序文件时，进程的有效用户ID通常就是实际用户ID，有效组ID通常是实际组ID。但是可以在文件模式字（<code>st_mode</code>）中设置一个特殊标志，其含义是“<strong>当执行此文件时，将进程的有效用户ID设置为文件所有者的用户ID（st_uid）</strong>”。与此相类似，在文件模式字中可以设置另一位，它将<strong>执行此文件的进程的有效组ID设置为文件的组所有者ID（st_gid）</strong>。在文件模式字中的这两位被称为<strong>设置用户ID（set-user-ID）位和设置组ID（set-group-ID）位</strong>。</p>
<p>当一个程序文件设置了 set-user-ID（通过
<code>chmod u+s filename</code> 配置）时，其执行逻辑与
<code>saved set-user-ID</code> 密切相关：</p>
<ol type="1">
<li>程序启动时：
<ul>
<li>进程的 <code>EUID</code>
会被设置为程序文件的所有者ID（例如，<code>/bin/passwd</code> 是 root
所有且设置了 set-user-ID 位，普通用户执行它时，<code>EUID</code> 会变为
root）。</li>
<li>同时，<code>saved set-user-ID</code> 会被初始化为这个
<code>EUID</code>（即 root 的 ID），作为后续权限切换的“基准”。</li>
</ul></li>
<li>程序执行中：
<ul>
<li>进程可以通过 <code>setuid()</code>
等系统调用临时降低权限（例如，<code>passwd</code> 程序大部分操作不需要
root 权限，会将 <code>EUID</code> 切换为普通用户的 RUID）。</li>
<li>当需要执行特权操作时（如写入
<code>/etc/passwd</code>），进程可以通过 <code>saved set-user-ID</code>
恢复之前保存的高权限 <code>EUID</code>（即
root），完成操作后再切换回低权限。</li>
</ul></li>
<li>作用：
<ul>
<li>防止权限被滥用：如果没有
<code>saved set-user-ID</code>，进程一旦降低 <code>EUID</code>
就无法再恢复到原来的高权限（除非直接使用 root 身份，风险更高）。</li>
<li>实现“最小权限原则”：仅在必要时临时提权，其他时间保持低权限，提升系统安全性。</li>
</ul></li>
</ol>
<p>再回到 <code>stat</code> 函数，设置用户ID位及设置组ID位都包含在文件的
<code>st_mode</code> 值中。这两位可分别用常量 <code>S_ISUID</code> 和
<code>S_ISGID</code> 测试。</p>
<p><code>saved set-user-ID</code>
的核心作用是<strong>保存程序启动时的有效用户ID</strong>，允许进程在执行过程中安全地切换权限（从高权限临时降为低权限，必要时再恢复），是
SUID 程序实现安全权限管理的关键机制。常见的 SUID 程序（如
<code>passwd</code>、<code>sudo</code>）都依赖它来平衡功能需求与系统安全。</p>
<hr />
<h2 id="文件访问权限">文件访问权限</h2>
<p><code>st_mode</code>值也包含了对文件的访问权限位。所有文件类型都有访问权限。</p>
<p>每个文件有9个访问权限位，可将它们分成3类，见下图。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00068.jpg" alt="9个访问权限位" style="zoom:67%;" /></p>
<p>前3行中，术语用户指的是文件所有者（owner）。<code>chmod</code>命令用于修改这9个权限位。该命令允许用<code>u</code>表示用户（所有者），用<code>g</code>表示组，用<code>o</code>表示其他。</p>
<hr />
<h2 id="新文件和目录的所有权">新文件和目录的所有权</h2>
<p><strong>新文件的用户ID设置为进程的有效用户ID</strong>。关于组ID，POSIX.1允许实现选择下列之一作为新文件的组ID。</p>
<p>（1）新文件的组ID可以是进程的有效组ID。</p>
<p>（2）新文件的组ID可以是它所在目录的组ID。</p>
<hr />
<h2 id="函数access和faccessat">函数access和faccessat</h2>
<p><strong><code>access</code>和<code>faccessat</code>函数是按实际用户ID和实际组ID进行访问权限测试</strong>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">access</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">int</span> mode)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">faccessat</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">int</span> mode, <span class="type">int</span> flag)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p><code>mode</code> 要么是用于测试文件是否存在的值
<code>F_OK</code>，要么是下图中所示任意标志的按位或运算结果。</p>
<table>
<thead>
<tr class="header">
<th>mode</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>R_OK</code></td>
<td>test for read permission</td>
</tr>
<tr class="even">
<td><code>W_OK</code></td>
<td>test for write permission</td>
</tr>
<tr class="odd">
<td><code>X_OK</code></td>
<td>test for execute permission</td>
</tr>
</tbody>
</table>
<p><code>flag</code> 参数可用于更改 <code>faccessat</code>
的行为。如果设置了 <code>AT_EACCESS</code>
标志，访问检查将使用调用进程的有效用户ID和组ID，而不是实际用户ID和组ID。</p>
<hr />
<h2 id="函数umask">函数umask</h2>
<p><code>umask</code>函数为进程设置文件模式创建屏蔽字，并返回之前的值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/stat.h&gt;</span></span></span><br><span class="line"><span class="type">mode_t</span> <span class="title function_">umask</span><span class="params">(<span class="type">mode_t</span> cmask)</span>;</span><br><span class="line"><span class="comment">// 返回值：之前的文件模式创建屏蔽字</span></span><br></pre></td></tr></table></figure>
<p>其中，参数cmask是由9个常量（<code>S_IRUSR</code>、<code>S_IWUSR</code>等）中的若干个按位或构成。</p>
<p>在进程创建一个新文件或新目录时，就一定会使用文件模式创建屏蔽字。</p>
<p>用户可以设置umask值以控制他们所创建文件的默认权限。该值表示成八进制数。设置了相应位后，它所对应的权限就会被拒绝。常用的几种umask值是002、022和027。002阻止其他用户写入你的文件，022阻止同组成员和其他用户写入你的文件，027阻止同组成员写你的文件以及其他用户读、写或执行你的文件。</p>
<hr />
<h2 id="函数chmodfchmod和fchmodat">函数chmod、fchmod和fchmodat</h2>
<p>这3个函数可以更改现有文件的访问权限。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/stat.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">chmod</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">mode_t</span> mode)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fchmod</span><span class="params">(<span class="type">int</span> fd, <span class="type">mode_t</span> mode)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fchmodat</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">mode_t</span> mode, <span class="type">int</span> flag)</span>;</span><br><span class="line"><span class="comment">// 3个函数返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>为了<strong>改变一个文件的权限位，进程的有效用户ID必须等于文件的所有者ID，或者该进程必须具有超级用户权限</strong>。</p>
<p>参数mode是下图中所示常量的按位或。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250215195930978.png" alt="chmod函数的mode常量" style="zoom: 50%;" /></p>
<p>注意，在图中，另外加了6个权限位，它们是两个设置ID常量（<code>S_ISUID</code>和<code>S_ISGID</code>）、保存正文常量（<code>S_ISVTX</code>）以及3个组合常量（<code>S_IRWXU</code>、<code>S_IRWXG</code>和<code>S_IRWXO</code>）。</p>
<hr />
<h2 id="粘性位">粘性位</h2>
<p>“sticky”这个名称的由来，是因为可执行文件的机器代码正文段会一直“滞留”在交换区（swap
area）中，直到系统重启才会消失。UNIX系统的后续版本将其称为保存正文位（saved-text
bit）；因此才有了常量<code>S_ISVTX</code>。
在如今较新的UNIX系统中，大多数都配备了虚拟内存系统和速度更快的文件系统，因此这种技术的需求已经不复存在了。</p>
<p>现今的系统扩展了粘着位的使用范围， Single UNIX
Specification允许针对目录设置粘着位。如果对一个目录设置了粘着位，只有对该目录具有写权限的用户并且满足下列条件之一，才能删除或重命名该目录下的文件：</p>
<ul>
<li>拥有此文件；</li>
<li>拥有此目录；</li>
<li>是超级用户。</li>
</ul>
<p>目录<code>/tmp</code> 和 <code>/var/tmp</code>
是设置粘着位的典型候选者，任何用户都可在这两个目录中创建文件。任一用户（用户、组和其他）对这两个目录的权限通常都是读、写和执行。但是用户不应能删除或重命名属于其他人的文件，为此在这两个目录的文件模式中都设置了粘性位。</p>
<p>在 Linux 系统中，粘性位（sticky bit） 的符号表示为小写字母 t
或大写字母
T，出现在文件或目录权限的最后一位（执行位的位置）。具体规则如下：</p>
<ul>
<li>当目录或文件同时具备执行权限（x） 且设置了粘性位时，符号为 t。</li>
<li>当未设置执行权限（x）但设置了粘性位时，符号为 T。</li>
</ul>
<hr />
<h2
id="函数chownfchownfchownat和lchown">函数chown、fchown、fchownat和lchown</h2>
<p>下面几个chown函数可用于更改文件的用户ID和组ID。如果两个参数owner或group中的任意一个是-1，则对应的ID不变。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">chown</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">uid_t</span> owner, <span class="type">gid_t</span> group)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fchown</span><span class="params">(<span class="type">int</span> fd, <span class="type">uid_t</span> owner, <span class="type">gid_t</span> group)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fchownat</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">uid_t</span> owner, <span class="type">gid_t</span> group, <span class="type">int</span> flag)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">lchown</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">uid_t</span> owner, <span class="type">gid_t</span> group)</span>;</span><br><span class="line"><span class="comment">// 4个函数的返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="file-size">File Size</h2>
<p><code>stat</code> 结构体的 <code>st_size</code>
成员包含文件的大小（以字节为单位）。此字段仅对普通文件、目录和符号链接有意义。</p>
<p>对于普通文件，允许文件大小为0。在第一次读取该文件时会得到文件结束的指示。对于目录，文件大小通常是某个数字的倍数，比如16或512。对于符号链接，文件大小是文件名所占的字节数。例如，在下面这种情况下，大小为7的文件对应的是路径名
<code>usr/lib</code> 的长度：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">lrwxrwxrwx 1 root 7 Sep 25 07:14 lib -&gt; usr/lib</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="文件截断">文件截断</h2>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">truncate</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">off_t</span> length)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">ftruncate</span><span class="params">(<span class="type">int</span> fd, <span class="type">off_t</span> length)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>这两个函数将一个现有文件长度截断为
<code>length</code>。如果该文件以前的长度大于
<code>length</code>，则超过 <code>length</code>
以外的数据就不再能访问。如果以前的长度
<code>小于length</code>，文件长度将增加，在以前的文件尾端和新的文件尾端之间的数据将读作0（也就是可能在文件中创建了一个空洞）。</p>
<hr />
<h2 id="文件系统">文件系统</h2>
<p>磁盘可以划分成几个部分，每个部分可以包含一个文件系统，如下图所示。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20251022192827001.png"
alt="磁盘，分区，文件系统" />
<figcaption aria-hidden="true">磁盘，分区，文件系统</figcaption>
</figure>
<p>更详细地柱面组（cylinder group）中索引节点（i-node）与数据块（data
block）的结构排布如图所示。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20251022193010815.png"
alt="柱面组的i节点与数据块" />
<figcaption aria-hidden="true">柱面组的i节点与数据块</figcaption>
</figure>
<p>在 <code>mkdir testdir</code>
创建目录后柱面组的分布如图所示。其中<code>i-node = 2549</code>指向<code>testdir</code>目录，<code>i-node = 1267</code>指向<code>testdir</code>的父目录。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20251022193749475.png"
alt="创建testdir目录后的柱面组分布" />
<figcaption
aria-hidden="true">创建testdir目录后的柱面组分布</figcaption>
</figure>
<hr />
<h2
id="函数linklinkatunlinkunlinkat和remove">函数link、linkat、unlink、unlinkat和remove</h2>
<p>创建一个指向现有文件的链接的方法是使用<code>link</code>函数或<code>linkat</code>函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">link</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *existingpath, <span class="type">const</span> <span class="type">char</span> *newpath)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">linkat</span><span class="params">(<span class="type">int</span> efd, <span class="type">const</span> <span class="type">char</span> *existingpath, <span class="type">int</span> nfd, <span class="type">const</span> <span class="type">char</span> *newpath, <span class="type">int</span> flag)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>这两个函数创建一个新目录项 <code>newpath</code>，它引用现有文件
<code>existingpath</code>。如果 <code>newpath</code>
已经存在，则返回出错。只创建 <code>newpath</code>
中的最后一个分量，路径中的其他部分应当已经存在。</p>
<p>为了删除一个现有的目录项，可以调用 <code>unlink</code> 函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">unlink</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">unlinkat</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">int</span> flag)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>也可以用<code>remove</code>函数解除对一个文件或目录的链接。对于文件，<code>remove</code>
的功能与 <code>unlink</code> 相同。对于目录，<code>remove</code>
的功能与 <code>rmdir</code> 相同。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">remove</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="函数rename和renameat">函数rename和renameat</h2>
<p>文件或目录可以用<code>rename</code>函数或者<code>renameat</code>函数进行重命名。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">rename</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *oldname, <span class="type">const</span> <span class="type">char</span> *newname)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">renameat</span><span class="params">(<span class="type">int</span> oldfd, <span class="type">const</span> <span class="type">char</span> *oldname, <span class="type">int</span> newfd, <span class="type">const</span> <span class="type">char</span> *newname)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="symbolic-links">Symbolic Links</h2>
<p>符号链接是指向文件的间接指针，这与硬链接不同，硬链接直接指向文件的索引节点。引入符号链接是为了规避硬链接的局限性：</p>
<ul>
<li>硬链接通常要求链接和文件位于同一个文件系统中。</li>
<li>只有超级用户可以创建指向目录的硬链接（当底层文件系统支持时）。</li>
</ul>
<p>下图总结了不同的函数是否会跟随符号链接。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260129194733605.png" alt="符号链接" style="zoom:50%;" /></p>
<h2 id="创建和读取符号链接">创建和读取符号链接</h2>
<p>可以用<code>symlink</code>或<code>symlinkat</code>函数创建一个符号链接。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">symlink</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *actualpath, <span class="type">const</span> <span class="type">char</span> *sympath)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">symlinkat</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *actualpath, <span class="type">int</span> fd, <span class="type">const</span> <span class="type">char</span> *sympath)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>因为<code>open</code>函数跟随符号链接，所以需要有一种方法打开该链接本身，并读该链接中的名字。<code>readlink</code>和<code>readlinkat</code>函数提供了这种功能。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">readlink</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> pathname, <span class="type">char</span> *<span class="keyword">restrict</span> buf, <span class="type">size_t</span> bufsize)</span>;</span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">readlinkat</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">char</span>* <span class="keyword">restrict</span> pathname, <span class="type">char</span> *<span class="keyword">restrict</span> buf, <span class="type">size_t</span> bufsize)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回读取的字节数；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>两个函数组合了
<code>open</code>、<code>read</code>和<code>close</code>的所有操作。如果函数成功执行，则返回读入
<code>buf</code> 的字节数。在 <code>buf</code>
中返回的符号链接的内容不以 <code>null</code> 字节终止。</p>
<hr />
<h2 id="文件的时间">文件的时间</h2>
<p>对每个文件都维护3个时间字段，它们的含义如下表。</p>
<table>
<thead>
<tr class="header">
<th>字段</th>
<th>描述</th>
<th>示例</th>
<th>ls 选项</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>st_atim</code></td>
<td>最后<strong>访问</strong>文件数据的时间</td>
<td><code>read</code></td>
<td><code>-u</code></td>
</tr>
<tr class="even">
<td><code>st_mtim</code></td>
<td>最后<strong>修改</strong>文件数据的时间</td>
<td><code>write</code></td>
<td>默认</td>
</tr>
<tr class="odd">
<td><code>st_ctim</code></td>
<td><strong>i节点状态</strong>的最后更改时间</td>
<td><code>chmod</code>, <code>chown</code></td>
<td><code>-c</code></td>
</tr>
</tbody>
</table>
<p>不同函数对文件和父目录的访问、修改和状态改变的时间的影响如下图所示。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20251022232752804.png"
alt="不同函数对访问、修改和状态改变时间的影响" />
<figcaption
aria-hidden="true">不同函数对访问、修改和状态改变时间的影响</figcaption>
</figure>
<hr />
<h2
id="函数futimensutimensat和utimes">函数futimens、utimensat和utimes</h2>
<p>一个文件的访问和修改时间可以用以下几个函数更改。<code>futimens</code>
和 <code>utimensat</code>
函数可以指定纳秒级精度的时间戳。用到的数据结构是与 <code>stat</code>
函数族相同的 <code>timespec</code> 结构。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/stat.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">futimens</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="keyword">struct</span> timespec times[<span class="number">2</span>])</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">utimensat</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">char</span> *path, <span class="type">const</span> <span class="keyword">struct</span> timespec times[<span class="number">2</span>], <span class="type">int</span> flag)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>这两个函数的 <code>times</code>
数组参数的第一个元素包含访问时间，第二元素包含修改时间。这两个时间值是日历时间，这是自特定时间（1970年1月1日00:00:00）以来所经过的秒数。不足秒的部分用纳秒表示。</p>
<p>时间戳可以按下列4种方式之一进行指定。</p>
<p>（1）如果 <code>times</code>
参数是一个空指针，则访问时间和修改时间两者都设置为当前时间。</p>
<p>（2）如果 <code>times</code> 参数指向两个 <code>timespec</code>
结构的数组，任一数组元素的<code>tv_nsec</code>字段的值为<code>UTIME_NOW</code>，相应的时间戳就设置为当前时间，忽略相应的<code>tv_sec</code>字段。</p>
<p>（3）如果<code>times</code>参数指向两个<code>timespec</code>结构的数组，任一数组元素的<code>tv_nsec</code>字段的值为<code>UTIME_OMIT</code>，相应的时间戳保持不变，忽略相应的<code>tv_sec</code>字段。</p>
<p>（4）如果<code>times</code>参数指向两个<code>timespec</code>结构的数组，且<code>tv_nsec</code>字段的值为既不是<code>UTIME_NOW</code>也不是<code>UTIME_OMIT</code>，在这种情况下，相应的时间戳设置为相应的
<code>tv_sec</code> 和<code>tv_nsec</code>字段的值。</p>
<p><code>futimens</code>和<code>utimensat</code>函数都包含在POSIX.1
中，第3 个函数<code>utimes</code>包含在Single UNIX
Specification的XSI扩展选项中。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">utimes</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">const</span> <span class="keyword">struct</span> timeval times[<span class="number">2</span>])</span>;</span><br><span class="line"><span class="comment">// 函数返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p><code>times</code>参数是指向包含两个时间戳（访问时间和修改时间）元素的数组的指针，两个时间戳是用秒和微秒表示的。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timeval</span> &#123;</span></span><br><span class="line">    <span class="type">time_t</span> tv_sec; <span class="comment">/* seconds */</span></span><br><span class="line">    <span class="type">long</span> tv_usec;  <span class="comment">/* microseconds */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>注意，不能对状态更改时间<code>st_ctim</code>（i节点最近被修改的时间）指定一个值，因为调用<code>utimes</code>函数时，此字段会被自动更新。</p>
<hr />
<h2 id="函数mkdirmkdirat和rmdir">函数mkdir、mkdirat和rmdir</h2>
<p>用<code>mkdir</code>和<code>mkdirat</code>函数创建目录。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/stat.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">mkdir</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">mode_t</span> mode)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">mkdirat</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">mode_t</span> mode)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>用<code>rmdir</code>函数可以删除一个空目录。空目录是只包含<code>.</code>和<code>..</code>这两项的目录。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">rmdir</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="读目录">读目录</h2>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;dirent.h&gt;</span></span></span><br><span class="line">DIR *<span class="title function_">opendir</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname)</span>;</span><br><span class="line">DIR *<span class="title function_">fdopendir</span><span class="params">(<span class="type">int</span> fd)</span>;</span><br><span class="line"><span class="comment">//两个函数返回值：若成功，返回指针；若出错，返回NULL</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> dirent *<span class="title function_">readdir</span><span class="params">(DIR *dp)</span>;</span><br><span class="line"><span class="comment">//返回值：若成功，返回指针；若在目录尾或出错，返回NULL</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">rewinddir</span><span class="params">(DIR *dp)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">closedir</span><span class="params">(DIR *dp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回-1</span></span><br><span class="line"></span><br><span class="line"><span class="type">long</span> <span class="title function_">telldir</span><span class="params">(DIR *dp)</span>;</span><br><span class="line"><span class="comment">// 返回值：与dp关联的目录中的当前位置</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">seekdir</span><span class="params">(DIR *dp, <span class="type">long</span> loc)</span>;</span><br></pre></td></tr></table></figure>
<p>由<code>opendir</code>和<code>fdopendir</code>返回的指向<code>DIR</code>结构的指针由另外5个函数使用。<code>opendir</code>执行初始化操作，使第一个<code>readdir</code>返回目录中的第一个目录项。<code>DIR</code>结构由<code>fdopendir</code>创建时，<code>readdir</code>返回的第一项取决于传给<code>fdopendir</code>函数的文件描述符相关联的文件偏移量。注意，目录中各目录项的顺序与实现有关。它们通常并不按字母顺序排列。</p>
<hr />
<h2 id="函数chdirfchdir和getcwd">函数chdir、fchdir和getcwd</h2>
<p>每个进程都有一个当前工作目录，此目录是搜索所有相对路径名的起点。当用户登录到
UNIX
系统时，其当前工作目录通常是口令文件（<code>/etc/passwd</code>）中该用户登录项的第6个字段—用户的起始目录（home
directory）。当前工作目录是进程的一个属性，起始目录则是登录名的一个属性。</p>
<p>进程调用<code>chdir</code>或<code>fchdir</code>函数可以更改当前工作目录。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">chdir</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fchdir</span><span class="params">(<span class="type">int</span> fd)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>在这两个函数中，分别用<code>pathname</code>或打开文件描述符来指定新的当前工作目录。</p>
<p>用<code>getcwd</code>获取当前工作目录。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">char</span> *<span class="title function_">getcwd</span><span class="params">(<span class="type">char</span> *buf, <span class="type">size_t</span> size)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回buf；若出错，返回NULL</span></span><br></pre></td></tr></table></figure>
<p>必须向此函数传递两个参数，一个是缓冲区地址
<code>buf</code>，另一个是缓冲区的长度
<code>size</code>（以字节为单位）。该缓冲区必须有足够的长度以容纳绝对路径名再加上一个终止
null 字节，否则返回出错。</p>
<hr />
<h2 id="设备特殊文件">设备特殊文件</h2>
<p>字段<code>st_dev</code>和<code>st_rdev</code>一些使用规则如下：</p>
<ul>
<li>每个文件系统所在的存储设备都由其主、次设备号表示。设备号所用的数据类型是基本系统数据类型<code>dev_t</code>。一个磁盘驱动器经常包含若干个文件系统。在同一磁盘驱动器上的各文件系统通常具有相同的主设备号，但是次设备号却不同。</li>
<li>通常可以使用两个宏： <code>major</code>和<code>minor</code>
来访问主、次设备号，大多数实现都定义这两个宏。这意味着无需关心这两个数是如何存放在<code>dev_t</code>
对象中的。</li>
<li>系统中与每个文件名关联的
<code>st_dev</code>值是文件系统的设备号，该文件系统包含了这一文件名以及与其对应的
i 节点。</li>
<li>只有字符特殊文件和块特殊文件才有<code>st_rdev</code>值。此值包含实际设备的设备号。</li>
</ul>
<hr />
<h2 id="文件访问权限位小结">文件访问权限位小结</h2>
<p>下图列出了所有这些文件访问权限位，以及它们对普通文件和目录文件的作用。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20251023184954452.png"
alt="文件访问权限位小结" />
<figcaption aria-hidden="true">文件访问权限位小结</figcaption>
</figure>
<p>最后9个常量可以分成以下三组：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">S_IRWXU = S_IRUSR | S_IWUSR | S_IXUSR</span><br><span class="line">S_IRWXG = S_IRGRP | S_IWGRP | S_IXGRP</span><br><span class="line">S_IRWXO = S_IROTH | S_IWOTH | S_IXOTH</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Process Environment（七）</title>
    <url>/2025/05/26/notes-reading/computer/apue/apue-07-process-environment/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第7章 Process Environment
的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="main函数">main函数</h2>
<p>C程序总是从main函数开始执行。main函数的原型是：</p>
<p><code>int main(int argc, char *argv[]);</code></p>
<p>其中，<code>argc</code> 是命令行参数的数目，<code>argv</code>
是指向参数的各个指针所构成的数组。</p>
<p>当内核执行C程序时，在调用main前先调用一个特殊的启动例程。可执行程序文件将此启动例程指定为程序的起始地址——这是由连接编辑器设置的，而连接编辑器则由C编译器调用。启动例程从内核取得命令行参数和环境变量值，然后为按上述方式调用main函数做好安排。</p>
<hr />
<h2 id="进程终止">进程终止</h2>
<p>有8种方式使进程终止（termination），其中5种为正常终止，它们是：</p>
<blockquote>
<p>（1）从 <code>main</code> 返回；</p>
<p>（2）调用 <code>exit</code>；</p>
<p>（3）调用 <code>_exit</code> 或 <code>_Exit</code>；</p>
<p>（4）最后一个线程从其启动例程返回；</p>
<p>（5）从最后一个线程调用 <code>pthread_exit</code>。</p>
</blockquote>
<p>异常终止有3种方式，它们是：</p>
<blockquote>
<p>（6）调用 <code>abort</code>；</p>
<p>（7）接到一个信号；</p>
<p>（8）最后一个线程对取消请求做出响应。</p>
</blockquote>
<p>上节提及的启动例程是这样编写的，使得从main返回后立即调用
<code>exit</code>
函数。如果将启动例程以C代码形式表示（实际上该例程常常用汇编语言编写），则它调用main函数的形式可能是：<code>exit(main(argc, argv));</code></p>
<h3 id="退出函数">退出函数</h3>
<p>3个函数用于正常终止一个程序：<code>_exit</code> 和 <code>_Exit</code>
立即进入内核，<code>exit</code>则先执行一些清理处理，然后返回内核。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">exit</span><span class="params">(<span class="type">int</span> status)</span>;</span><br><span class="line"><span class="type">void</span> _Exit(<span class="type">int</span> status);</span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> _exit(<span class="type">int</span> status);</span><br></pre></td></tr></table></figure>
<p>由于历史原因，exit 函数总是执行一个标准 I/O
库的清理关闭操作：对于所有打开流调用 <code>fclose</code>
函数。这造成输出缓冲中的所有数据都被冲洗（写到文件上）。</p>
<p>3个退出函数都带一个整型参数，称为终止状态（或退出状态，exit
status）。大多数UNIX系统shell都提供检查进程终止状态的方法。如果（a）调用这些函数时不带终止状态，或（b）main执行了一个无返回值的return语句，或（c）main没有声明返回类型为整型，则该进程的终止状态是未定义的。但是，若main的返回类型是整型，并且main执行到最后一条语句时返回（隐式返回），那么该进程的终止状态是0。</p>
<p>main函数返回一个整型值与用该值调用exit是等价的。于是在main函数中<code>exit(0);</code>等价于<code>return(0);</code></p>
<h3 id="函数atexit">函数atexit</h3>
<p>按照ISO C的规定，一个进程可以登记多至32个函数，这些函数将由
<code>exit</code> 自动调用。我们称这些函数为终止处理程序（exit
handler），并调用 <code>atexit</code>函数来登记这些函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">atexit</span><span class="params">(<span class="type">void</span> (*func)(<span class="type">void</span>))</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回非0</span></span><br></pre></td></tr></table></figure>
<p>其中，<code>atexit</code>
的参数是一个函数地址，当调用此函数时无需向它传递任何参数，也不期望它返回一个值。<code>exit</code>
调用这些函数的顺序与它们登记时候的顺序相反。同一函数如若登记多次，也会被调用多次。</p>
<p>根据ISO
C和POSIX.1，exit首先调用各终止处理程序，然后关闭（通过fclose）所有打开流。POSIX.1扩展了ISO
C标准，它说明，如若程序调用exec函数族中的任一函数，则将清除所有已安装的终止处理程序。下图显示了一个C程序是如何启动的，以及它终止的各种方式。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260205125002285.png"
alt="C程序是怎样开始和终止的" />
<figcaption aria-hidden="true">C程序是怎样开始和终止的</figcaption>
</figure>
<p>注意，内核使程序执行的唯一方法是调用一个 <code>exec</code>
函数。进程自愿终止的唯一方法是显式或隐式地（通过调用
<code>exit</code>）调用<code>_exit</code>或<code>_Exit</code>。进程也可非自愿地由一个信号使其终止。</p>
<hr />
<h2 id="命令行参数">命令行参数</h2>
<p>当执行一个程序时，调用
<code>exec</code>的进程可将命令行参数传递给该新程序。</p>
<hr />
<h2 id="environment-list">Environment List</h2>
<p>每个程序都接收到一张环境表。与参数表一样，环境表也是一个字符指针数组，其中每个指针包含一个以null结束的C字符串的地址。全局变量
<code>environ</code>
则包含了该指针数组的地址：<code>extern char **environ;</code></p>
<p>例如，如果该环境包含5个字符串，那么它看起来如下图所示。其中，每个字符串的结尾处都显式地有一个null字节。我们称
<code>environ</code> 为环境指针（environment
pointer），指针数组为环境表，其中各指针指向的字符串为环境字符串。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00132.jpg" alt="由五个C语言字符串组成的环境" style="zoom:67%;" /></p>
<p>按照惯例，环境由 <code>name = value</code>
这样的字符串组成。大多数预定义名完全由大写字母组成，但这只是一个惯例。</p>
<hr />
<h2 id="c程序的存储空间布局">C程序的存储空间布局</h2>
<p>C程序由下列几部分组成：</p>
<blockquote>
<ul>
<li><p>正文段。这是由CPU执行的机器指令部分。通常，正文段是可共享的，所以即使是频繁执行的程序（如文本编辑器、C编译器和shell等）在存储器中也只需有一个副本，另外，正文段常常是只读的，以防止程序由于意外而修改其指令。</p></li>
<li><p>初始化数据段。通常将此段称为数据段，它包含了程序中需明确地赋初值的变量。例如，
C程序中任何函数之外的声明：<code>int maxcount = 99;</code>。</p></li>
<li><p>未初始化数据段。通常将此段称为bss段，这一名称来源于早期汇编程序一个操作符，意思是“由符号开始的块”（block
started by
symbol），在程序开始执行之前，内核将此段中的数据初始化为0或空指针。函数外的声明：
<code>long sum[1000];</code>使此变量存放在非初始化数据段中。</p></li>
<li><p>栈。自动变量以及每次函数调用时所需保存的信息都存放在此段中。每次函数调用时，其返回地址以及调用者的环境信息（如某些机器寄存器的值）都存放在栈中。然后，最近被调用的函数在栈上为其自动和临时变量分配存储空间。通过以这种方式使用栈，C递归函数可以工作。递归函数每次调用自身时，就用一个新的栈帧，因此一次函数调用实例中的变量集不会影响另一次函数调用实例中的变量。</p></li>
<li><p>堆。通常在堆中进行动态存储分配。由于历史上形成的惯例，堆位于未初始化数据段和栈之间。</p></li>
</ul>
</blockquote>
<p>下图显示了这些段的一种典型安排方式。这是程序的逻辑布局，虽然并不要求一个具体实现一定以这种方式安排其存储空间，但这是一种我们便于说明的典型安排。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260205224017695.png"
alt="典型的内存排列" />
<figcaption aria-hidden="true">典型的内存排列</figcaption>
</figure>
<p>未初始化数据段的内容并不存放在磁盘程序文件中。其原因是，内核在程序开始运行前将它们都设置为
0。需要存放在磁盘程序文件中的段只有正文段和初始化数据段。</p>
<p><code>size</code>
命令报告正文段、数据段和bss段的长度（以字节为单位）。例如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">size /usr/bin/cc /bin/sh</span><br><span class="line">text　　data　　 bss　　　dec　　hex　 filename</span><br><span class="line">346919　　3576　　6680　 357175　 57337　 /usr/bin/cc</span><br><span class="line">102134　　1776　 11272　 115182　 1c1ee　 /bin/sh</span><br></pre></td></tr></table></figure>
<p>第4列和第5列是分别以十进制和十六进制表示的3段总长度。</p>
<hr />
<h2 id="共享库">共享库</h2>
<p>共享库从可执行文件中移除了通用的库程序，而是在内存中的某个位置保留该库程序的单一副本，供所有进程引用。</p>
<p>这会减小每个可执行文件的大小，但可能会增加一些运行时开销，要么是在程序首次执行时，要么是在每个共享库函数首次被调用时。</p>
<p>共享库的另一个优点是可以用库函数的新版本代替老版本而无需对使用该库的程序重新编译连接（假定参数的数目和类型都没有发生改变）。</p>
<hr />
<h2 id="存储空间分配">存储空间分配</h2>
<p>ISO C说明了3个用于存储空间动态分配的函数。</p>
<blockquote>
<p>（1）<code>malloc</code>，分配指定字节数的存储区。此存储区中的初始值不确定。</p>
<p>（2）<code>calloc</code>，为指定数量指定长度的对象分配存储空间。该空间中的每一位（bit）都初始化为0。</p>
<p>（3）<code>realloc</code>，增加或减少以前分配区的长度。当增加长度时，可能需将以前分配区的内容移到另一个足够大的区域，以便在尾端提供增加的存储区，而新增区域内的初始值则不确定。</p>
</blockquote>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">malloc</span><span class="params">(<span class="type">size_t</span> size)</span>;</span><br><span class="line"><span class="type">void</span> *<span class="title function_">calloc</span><span class="params">(<span class="type">size_t</span> nobj, <span class="type">size_t</span> size)</span>;</span><br><span class="line"><span class="type">void</span> *<span class="title function_">realloc</span><span class="params">(<span class="type">void</span> *ptr, <span class="type">size_t</span> newsize)</span>;</span><br><span class="line"><span class="comment">// 3个函数返回值：若成功，返回非空指针；若出错，返回NULL</span></span><br></pre></td></tr></table></figure>
<p>注意，<code>realloc</code>
的最后一个参数是存储区的新长度，不是新、旧存储区长度之差。作为一个特例，若
<code>ptr</code> 是一个空指针，则 <code>realloc</code> 的功能与
<code>malloc</code> 相同，用于分配一个指定长度为newsize的存储区。</p>
<hr />
<h2 id="环境变量">环境变量</h2>
<p>环境字符串的形式是：<code>name=value</code></p>
<p>UNIX内核并不查看这些字符串，它们的解释完全取决于各个应用程序。例如，shell使用了大量的环境变量。其中某一些在登录时自动设置（如HOME、USER等），有些则由用户设置。我们通常在一个shell启动文件中设置环境变量以控制shell的动作。</p>
<p>ISO C定义了一个函数 <code>getenv</code>，可以用其取环境变量值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="type">char</span> *<span class="title function_">getenv</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name)</span>;</span><br><span class="line"><span class="comment">// 返回值：指向与name关联的value的指针；若未找到，返回NULL</span></span><br></pre></td></tr></table></figure>
<p>注意，此函数返回一个指针，它指向 <code>name=value</code>
字符串中的value。</p>
<p>除了获取环境变量值，有时也需要设置环境变量。我们可能希望改变现有变量的值，或者是增加新的环境变量。（我们能影响的只是当前进程及其后生成和调用的任何子进程的环境，但不能影响父进程的环境，这通常是一个shell进程。尽管如此，修改环境表的能力仍然是很有用的。）下图列出了由不同的标准及实现支持的各种函数。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00135.jpg" alt="img" style="zoom:67%;" /></p>
<p><code>clearenv</code>
被用来删除环境表中的所有项。中间3个函数的原型是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">putenv</span><span class="params">(<span class="type">char</span> *str)</span>;</span><br><span class="line"><span class="comment">// 函数返回值：若成功，返回0；若出错，返回非0</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">setenv</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name, <span class="type">const</span> <span class="type">char</span> *value, <span class="type">int</span> rewrite)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">unsetenv</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>这3个函数的操作如下。</p>
<blockquote>
<ul>
<li><p><code>putenv</code> 取形式为 <code>name=value</code>
的字符串，将其放到环境表中。如果 <code>name</code>
已经存在，则先删除其原来的定义。</p></li>
<li><p><code>setenv</code> 将 <code>name</code> 设置为
<code>value</code>。如果在环境中name已经存在，那么（a）若rewrite非0，则首先删除其现有的定义；（b）若rewrite为0，则不删除其现有定义（name不设置为新的value，而且也不出错）。</p></li>
<li><p><code>unsetenv</code>删除name的定义。即使不存在这种定义也不算出错。</p></li>
</ul>
</blockquote>
<hr />
<h2 id="函数setjmp和longjmp">函数setjmp和longjmp</h2>
<p>在C中，<code>goto</code>
语句是不能跨越函数的，而执行这种类型跳转功能的是函数 <code>setjmp</code>
和
<code>longjmp</code>。这两个函数对于处理发生在很深层嵌套函数调用中的出错情况是非常有用的。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;setjmp.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">setjmp</span><span class="params">(jmp_buf env)</span>;</span><br><span class="line"><span class="comment">// 返回值：若直接调用，返回0；若从longjmp返回，则为非0</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">longjmp</span><span class="params">(jmp_buf env, <span class="type">int</span> val)</span>;</span><br></pre></td></tr></table></figure>
<p>在希望返回到的位置调用 <code>setjmp</code>。<code>setjmp</code> 参数
<code>env</code> 的类型是一个特殊类型
<code>jmp_buf</code>。这一数据类型是某种形式的数组，其中存放在调用
<code>longjmp</code>
时能用来恢复栈状态的所有信息。因为需在另一个函数中引用 <code>env</code>
变量，所以通常将 <code>env</code> 变量定义为全局变量。</p>
<hr />
<h2 id="函数getrlimit和setrlimit">函数getrlimit和setrlimit</h2>
<p>每个进程都有一组资源限制，其中一些可以用 <code>getrlimit</code> 和
<code>setrlimit</code> 函数查询和更改。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/resource.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">getrlimit</span><span class="params">(<span class="type">int</span> resource, <span class="keyword">struct</span> rlimit *rlptr)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">setrlimit</span><span class="params">(<span class="type">int</span> resource, <span class="type">const</span> <span class="keyword">struct</span> rlimit *rlptr)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回0；若出错，返回非0</span></span><br></pre></td></tr></table></figure>
<p>对这两个函数的每一次调用都指定一个资源以及一个指向下列结构的指针。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rlimit</span> &#123;</span></span><br><span class="line">    <span class="type">rlim_t</span> rlim_cur; <span class="comment">/* soft limit: current limit */</span></span><br><span class="line">    <span class="type">rlim_t</span> rlim_max; <span class="comment">/* hard limit: maximum value for rlim_cur */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>在更改资源限制时，须遵循下列3条规则。</p>
<blockquote>
<p>（1）任何一个进程都可将一个软限制值更改为小于或等于其硬限制值。</p>
<p>（2）任何一个进程都可降低其硬限制值，但它必须大于或等于其软限制值。这种降低，对普通用户而言是不可逆的。</p>
<p>（3）只有超级用户进程可以提高硬限制值。</p>
</blockquote>
<p>常量 <code>RLIM_INFINITY</code> 指定了一个无限量的限制。</p>
<p>这两个函数的 resource 参数取下列值之一。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/Image00144.jpg" alt="img" style="zoom:50%;" /></p>
<p><code>RLIMIT_AS</code>
进程总的可用存储空间的最大长度（字节）。这影响到 <code>sbrk</code>
函数和 <code>mmap</code> 函数。</p>
<p><code>RLIMIT_CORE</code> <code>core</code>
文件的最大字节数，若其值为0则阻止创建core文件。</p>
<p><code>RLIMIT_CPU</code>
CPU时间的最大量值（秒），当超过此软限制时，向该进程发送
<code>SIGXCPU</code> 信号。</p>
<p><code>RLIMIT_DATA</code>
数据段的最大字节长度。这是初始化数据、非初始以及堆的总和。</p>
<p><code>RLIMIT_FSIZE</code>
可以创建的文件的最大字节长度。当超过此软限制时，则向该进程发送
<code>SIGXFSZ</code> 信号。</p>
<p><code>RLIMIT_MEMLOCK</code> 一个进程使用 <code>mlock</code>
能够锁定在存储空间中的最大字节长度。</p>
<p><code>RLIMIT_MSGQUEUE</code>
进程为POSIX消息队列可分配的最大存储字节数。</p>
<p><code>RLIMIT_NICE</code> 为了影响进程的调度优先级，<code>nice</code>
值可设置的最大限制。</p>
<p><code>RLIMIT_NOFILE</code> 每个进程能打开的最多文件数。</p>
<p><code>RLIMIT_NPROC</code> 每个实际用户 ID 可拥有的最大子进程数。</p>
<p><code>RLIMIT_NPTS</code> 用户可同时打开的伪终端的最大数量。</p>
<p><code>RLIMIT_RSS</code> 最大驻内存集字节长度（resident set size in
bytes，RSS）。如果可用物理内存不足，内核会从超出其驻留集大小（RSS）的进程中获取内存。</p>
<p><code>RLIMIT_SBSIZE</code>
在任一给定时刻，一个用户可以占用的套接字缓冲区的最大长度（字节）。</p>
<p><code>RLIMIT_SIGPENDING</code>
一个进程可排队的信号最大数量。这个限制是 <code>sigqueue</code>
函数实施的。</p>
<p><code>RLIMIT_STACK</code> 栈的最大字节长度。</p>
<p><code>RLIMIT_SWAP</code> 用户可消耗的交换空间的最大字节数</p>
<p><code>RLIMIT_VMEM</code> 这是 <code>RLIMIT_AS</code> 的同义词。</p>
<p>资源限制影响到调用进程并由其子进程继承。这就意味着，为了影响一个用户的所有后续进程，需将资源限制的设置构造在shell之中。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Process Control（八）</title>
    <url>/2025/06/01/notes-reading/computer/apue/apue-08-process-control/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第8章 Process Control
的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="进程标识符">进程标识符</h2>
<p>每个进程都有一个非负整型表示的唯一进程ID。</p>
<p>系统中有一些专用进程，但具体细节随实现而不同。ID为0的进程通常是调度进程，常常被称为交换进程（swapper）。该进程是内核的一部分，磁盘上没有与该进程对应的程序，因此也被称为系统进程。进程ID
1通常是 <code>init</code>
进程，在自举过程结束时由内核调用，并将系统引导到一个状态（如多用户）。init
进程决不会终止。它是一个普通的用户进程（与交换进程不同，它不是内核中的系统进程），但是它以超级用户特权运行。</p>
<p>除了进程ID，每个进程还有一些其他标识符。下列函数返回这些标识符。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">getpid</span><span class="params">(<span class="type">void</span>)</span>; <span class="comment">// 返回值：调用进程的进程ID</span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">getppid</span><span class="params">(<span class="type">void</span>)</span>; <span class="comment">// 返回值：调用进程的父进程ID</span></span><br><span class="line"><span class="type">uid_t</span> <span class="title function_">getuid</span><span class="params">(<span class="type">void</span>)</span>; <span class="comment">// 返回值：调用进程的实际用户ID</span></span><br><span class="line"><span class="type">uid_t</span> <span class="title function_">geteuid</span><span class="params">(<span class="type">void</span>)</span>; <span class="comment">// 返回值：调用进程的有效用户ID</span></span><br><span class="line"><span class="type">gid_t</span> <span class="title function_">getgid</span><span class="params">(<span class="type">void</span>)</span>; <span class="comment">//返回值：调用进程的实际组ID</span></span><br><span class="line"><span class="type">gid_t</span> <span class="title function_">getegid</span><span class="params">(<span class="type">void</span>)</span>; <span class="comment">//返回值：调用进程的有效组ID</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="fork">fork</h2>
<p>一个现有的进程可以调用fork函数创建一个新进程。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">fork</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 返回值：子进程返回0，父进程返回子进程ID；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>由fork创建的新进程被称为子进程（child
process）。fork函数被调用一次，但返回两次。两次返回的区别是子进程的返回值是
0，而父进程的返回值则是新建子进程的进程 ID。</p>
<p>子进程和父进程继续执行fork调用之后的指令。子进程是父进程的副本。例如，子进程获得父进程数据空间、堆和栈的副本。注意，这是子进程所拥有的副本。父进程和子进程并不共享这些存储空间部分。父进程和子进程共享正文段。</p>
<p>由于在fork之后经常跟随着exec，所以现在的很多实现并不执行一个父进程数据段、栈和堆的完全副本。作为替代，使用了写时复制（Copy-On-Write，COW）技术。这些区域由父进程和子进程共享，而且内核将它们的访问权限改变为只读。如果父进程和子进程中的任一个试图修改这些区域，则内核只为修改区域的那块内存制作一个副本，通常是虚拟存储系统中的一“页”。</p>
<h3 id="文件共享">文件共享</h3>
<p>在重定向父进程的标准输出时，子进程的标准输出也被重定向。实际上，fork的一个特性是父进程的所有打开文件描述符都被复制到子进程中。我们说“复制”是因为对每个文件描述符来说，就好像执行了dup函数。父进程和子进程每个相同的打开描述符共享一个文件表项。</p>
<p>父进程和子进程共享同一个文件偏移量。考虑下述情况：一个进程fork了一个子进程，然后等待子进程终止。假定，作为普通处理的一部分，父进程和子进程都向标准输出进行写操作。如果父进程的标准输出已重定向（很可能是由
shell
实现的），那么子进程写到该标准输出时，它将更新与父进程共享的该文件的偏移量。</p>
<p>在fork之后处理文件描述符有以下两种常见的情况。</p>
<p>（1）父进程等待子进程完成。在这种情况下，父进程无需对其描述符做任何处理。当子进程终止后，它曾进行过读、写操作的任一共享描述符的文件偏移量已做了相应更新。</p>
<p>（2）父进程和子进程各自执行不同的程序段。在这种情况下，在fork之后，父进程和子进程各自关闭它们不需使用的文件描述符，这样就不会干扰对方使用的文件描述符。这种方法是网络服务进程经常使用的。</p>
<p>除了打开文件之外，父进程的很多其他属性也由子进程继承，包括：</p>
<blockquote>
<ul>
<li><p>实际用户ID、实际组ID、有效用户ID、有效组ID</p></li>
<li><p>附属组ID</p></li>
<li><p>进程组ID</p></li>
<li><p>会话ID</p></li>
<li><p>控制终端</p></li>
<li><p>设置用户ID标志和设置组ID标志</p></li>
<li><p>当前工作目录</p></li>
<li><p>根目录</p></li>
<li><p>文件模式创建屏蔽字</p></li>
<li><p>信号屏蔽和安排</p></li>
<li><p>对任一打开文件描述符的执行时关闭（close-on-exec）标志</p></li>
<li><p>环境</p></li>
<li><p>连接的共享存储段</p></li>
<li><p>内存映射</p></li>
<li><p>资源限制</p></li>
</ul>
</blockquote>
<p><strong>父进程和子进程之间的区别</strong>具体如下。</p>
<blockquote>
<ul>
<li><p>fork的返回值不同。</p></li>
<li><p>进程ID不同。</p></li>
<li><p>这两个进程的父进程ID不同。</p></li>
<li><p>子进程的tms_utime、tms_stime、tms_cutime和tms_ustime的值设置为0。</p></li>
<li><p>子进程不继承父进程设置的文件锁。</p></li>
<li><p>子进程的未处理定时器被清除。</p></li>
<li><p>子进程的未处理信号集设置为空集。</p></li>
</ul>
</blockquote>
<p>使fork失败的两个主要原因是：（a）系统中已经有了太多的进程（通常意味着某个方面出了问题），（b）该实际用户ID的进程总数超过了系统限制。<code>CHILD_MAX</code>
规定了每个实际用户ID在任一时刻可拥有的最大进程数。</p>
<p>fork有以下两种用法。</p>
<p>（1）一个父进程希望复制自己，使父进程和子进程同时执行不同的代码段。这在网络服务进程中是常见的——父进程等待客户端的服务请求。当这种请求到达时，父进程调用fork，使子进程处理此请求。父进程则继续等待下一个服务请求。</p>
<p>（2）一个进程要执行一个不同的程序。这对 shell
是常见的情况。在这种情况下，子进程从fork返回后立即调用exec。</p>
<hr />
<h2 id="函数vfork">函数vfork</h2>
<p>vfork函数的调用序列和返回值与fork相同，但两者的语义不同。可移植的应用程序不应该使用这个函数。</p>
<p>vfork函数用于创建一个新进程，而该新进程的目的是exec一个新程序。vfork与fork一样都创建一个子进程，但是它并不将父进程的地址空间完全复制到子进程中，因为子进程会立即调用exec（或exit），于是也就不会引用该地址空间。不过<strong>在子进程调用exec或exit之前，它在父进程的空间中运行</strong>。这种优化工作方式在某些UNIX系统的实现中提高了效率，但如果子进程修改数据（除了用于存放vfork返回值的变量）、进行函数调用、或者没有调用
exec 或 exit 就返回都可能会带来未知的结果。</p>
<p>vfork和fork之间的另一个区别是：<strong>vfork保证子进程先运行，在它调用exec或exit之后父进程才可能被调度运行</strong>，当子进程调用这两个函数中的任意一个时，父进程会恢复运行。（如果在调用这两个函数之前子进程依赖于父进程的进一步动作，则会导致死锁。）</p>
<hr />
<h2 id="函数exit">函数exit</h2>
<p>进程有5种正常终止及3种异常终止方式。<strong>5种正常终止方式</strong>具体如下。</p>
<blockquote>
<p>（1）在main函数内执行return语句。等效于调用exit。</p>
<p>（2）调用exit函数。此函数由ISO
C定义，其操作包括调用各终止处理程序（终止处理程序在调用atexit函数时登记），然后关闭所有标准I/O流等。</p>
<p>（3）调用 <code>_exit</code>或 <code>_Exit</code>函数。ISOC定义
<code>_Exit</code>，其目的是为进程提供一种无需运行终止处理程序或信号处理程序而终止的方法。对标准
I/O 流是否进行冲洗，这取决于实现。在 UNIX系统中，<code>_Exit</code>和
<code>_exit</code> 是同义的，并不冲洗标准 I/O 流。<code>_exit</code>
函数由 <code>exit</code>
调用，它处理UNIX系统特定的细节。<code>_exit</code>
是由POSIX.1说明的。</p>
<p>在大多数UNIX系统实现中，
<code>exit(3)</code>是标准C库中的一个函数，而<code>_exit(2)</code>则是一个系统调用。</p>
<p>（4）进程的最后一个线程在其启动例程中执行return语句。但是，该线程的返回值不用作进程的返回值。当最后一个线程从其启动例程返回时，该进程以终止状态0返回。</p>
<p>（5）进程的最后一个线程调用 <code>pthread_exit</code>
函数。在这种情况中，进程终止状态总是0，这与传送给
<code>pthread_exit</code> 的参数无关。</p>
</blockquote>
<p><strong>3种异常终止</strong>具体如下。</p>
<blockquote>
<p>（1）调用abort。它产生 <code>SIGABRT</code>
信号，这是下一种异常终止的一种特例。</p>
<p>（2）当进程接收到某些信号时。信号可由进程自身（如调用abort函数）、其他进程或内核产生。</p>
<p>（3）最后一个线程对“取消”（cancellation）请求作出响应。默认情况下，“取消”以延迟方式发生：一个线程要求取消另一个线程，若干时间之后，目标线程终止。</p>
</blockquote>
<p>无论一个进程以何种方式终止，内核中的同一代码最终都会被执行。这段内核代码会关闭该进程的所有打开的描述符，释放它所使用的内存，等等。</p>
<p>对上述任意一种终止情形，我们都希望终止进程能够通知其父进程它是如何终止的。对于
3个终止函数（<code>exit</code>、<code>_exit</code>和<code>_Exit</code>），将其<strong>退出状态作为参数</strong>传送给函数。在异常终止情况，内核（不是进程本身）产生一个指示其异常终止原因的终止状态。在任意一种情况下，该终止进程的父进程都能用
<strong>wait或waitpid</strong> 取得其终止状态。</p>
<p>对于父进程已经终止的所有进程，它们的父进程都改变为 <code>init</code>
进程。我们称这些进程由 <code>init</code>
进程收养。其操作过程大致是：在一个进程终止时，内核逐个检查所有活动进程，以判断它是否是正要终止进程的子进程，如果是，则该进程的父进程ID就更改为1。这种处理方法保证了每个进程有一个父进程。</p>
<p>另一种情况是子进程在其父进程之前终止。如果子进程完全消失了，那么当父进程最终准备好检查子进程是否已终止时，就无法获取其终止状态。内核会为每个正在终止的进程保留少量信息，以便当该终止进程的父进程调用wait或waitpid时，这些信息是可用的。这些信息至少包括进程ID、进程的终止状态以及该进程所占用的CPU时间量。</p>
<p>内核可以释放进程所使用的所有内存，并关闭其打开的文件。在UNIX系统术语中，一个已经终止但父进程尚未对其进行等待的进程被称为僵尸进程。<code>ps</code>
命令将僵死进程的状态打印为Z。如果编写一个长期运行的程序，它fork了很多子进程，那么除非父进程等待取得子进程的终止状态，不然这些子进程终止后就会变成僵死进程。</p>
<p>当一个被init继承的进程终止时会发生什么？它会变成僵尸进程吗？答案是“不会”，因为init程序在编写时做了这样的处理：每当它的一个子进程终止时，init就会调用某个wait函数来获取该子进程的终止状态。通过这种方式，init防止了系统被僵尸进程塞满。当我们说“init的某个子进程”时，指的要么是init直接生成的进程，要么是其父进程已经终止、随后被init继承的进程。</p>
<hr />
<h2 id="函数wait和waitpid">函数wait和waitpid</h2>
<p>当一个进程正常或异常终止时，内核就向其父进程发送 <code>SIGCHLD</code>
信号。因为子进程终止是个异步事件（这可以在父进程运行的任何时候发生），所以这种信号也是内核向父进程发的异步通知。父进程可以选择忽略该信号，或者提供一个该信号发生时即被调用执行的函数（信号处理程序）。对于这种信号的系统默认动作是忽略它。调用
<code>wait</code> 或 <code>waitpid</code> 的进程可能会发生:</p>
<blockquote>
<ul>
<li><p>如果其所有子进程都还在运行，则阻塞。</p></li>
<li><p>如果一个子进程已终止，正等待父进程获取其终止状态，则取得该子进程的终止状态立即返回。</p></li>
<li><p>如果它没有任何子进程，则立即出错返回。</p></li>
</ul>
</blockquote>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">wait</span><span class="params">(<span class="type">int</span> *statloc)</span>;</span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">waitpid</span><span class="params">(<span class="type">pid_t</span> pid, <span class="type">int</span> *statloc, <span class="type">int</span> options)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回进程ID；若出错，返回0（见后面的说明）或−1</span></span><br></pre></td></tr></table></figure>
<p>这两个函数的区别如下。</p>
<ul>
<li><p>在一个子进程终止前，<code>wait</code> 使其调用者阻塞，而
<code>waitpid</code> 有一选项，可使调用者不阻塞。</p></li>
<li><p><code>waitpid</code>
并不等待在其调用之后的第一个终止子进程，它有若干个选项，可以控制它所等待的进程。</p></li>
</ul>
<p>这两个函数的参数 <code>statloc</code> 是一个整型指针。如果
<code>statloc</code>
不是一个空指针，则终止进程的终止状态就存放在它所指向的单元内。如果不关心终止状态，则可将该参数指定为空指针。</p>
<p>POSIX.1规定，终止状态用定义在 <code>&lt;sys/wait.h&gt;</code>
中的各个宏来查看。有4个互斥的宏可用来取得进程终止的原因，它们的名字都以
<code>WIF</code>开始。基于这4个宏中哪一个值为真，就可选用其他宏来取得退出状态、信号编号等。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260209190026770.png" alt="用于检查wait和waitpid返回的终止状态的宏" style="zoom: 67%;" /></p>
<p>对于waitpid函数中pid参数的作用解释如下。</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">pid ==−1</th>
<th
style="text-align: left;">等待任一子进程。此种情况下，waitpid与wait等效。</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;">pid &gt; 0</td>
<td style="text-align: left;">等待进程ID与pid相等的子进程。</td>
</tr>
<tr class="even">
<td style="text-align: left;">pid == 0</td>
<td
style="text-align: left;">等待组ID等于调用进程组ID的任一子进程。</td>
</tr>
<tr class="odd">
<td style="text-align: left;">pid &lt;−1</td>
<td style="text-align: left;">等待组ID等于pid绝对值的任一子进程。</td>
</tr>
</tbody>
</table>
<p><code>waitpid</code>
函数返回终止子进程的进程ID，并将该子进程的终止状态存放在由
<code>statloc</code> 指向的存储单元中。</p>
<p><code>options</code>
参数能进一步控制waitpid的操作。此参数或者是0，或者是下图中常量按位或运算的结果。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260209192108078.png" alt="waitpid的options常量值" style="zoom:67%;" /></p>
<p>waitpid函数提供了wait函数没有提供的3个功能。</p>
<blockquote>
<p>（1）waitpid可等待一个特定的进程，而wait则返回任一终止子进程的状态。</p>
<p>（2）waitpid提供了一个 wait
的非阻塞版本。有时希望获取一个子进程的状态，但不想阻塞。</p>
<p>（3）waitpid通过 <code>WUNTRACED</code> 和 <code>WCONTINUED</code>
选项支持作业控制。</p>
</blockquote>
<hr />
<h2 id="函数waitid">函数waitid</h2>
<p>Single UNIX
Specification包括了另一个取得进程终止状态的函数waitid，类似于waitpid，但提供了更多的灵活性。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">waitid</span><span class="params">(<span class="type">idtype_t</span> idtype, <span class="type">id_t</span> id, <span class="type">siginfo_t</span> *infop, <span class="type">int</span> options)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>waitid
允许一个进程指定要等待的子进程。但它使用两个单独的参数表示要等待的子进程。id参数的作用与idtype的值相关。该函数支持的
<code>idtype</code> 类型列在下图中。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260209202106966.png"
alt="waitid的idtype参数" />
<figcaption aria-hidden="true">waitid的idtype参数</figcaption>
</figure>
<p>options参数是下图所示标志的按位或运算结果。这些标志表明调用者对哪些状态变化感兴趣。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260209202515591.png"
alt="waitid的options参数" />
<figcaption aria-hidden="true">waitid的options参数</figcaption>
</figure>
<p><code>WCONTINUED</code>、<code>WEXITED</code> 或
<code>WSTOPPED</code> 这3个常量之一必须在 <code>options</code>
参数中指定。</p>
<p><code>infop</code> 参数是指向 <code>siginfo</code>
结构的指针。该结构包含了造成子进程状态改变有关信号的详细信息。</p>
<hr />
<h2 id="函数wait3wait4">函数wait3、wait4</h2>
<p>大多数UNIX系统实现提供了另外两个函数wait3和wait4。它们提供的功能比POSIX.1函数wait、waitpid和waitid所提供功能的要多一个，这与附加参数有关。该参数允许内核返回由终止进程及其所有子进程使用的资源概况。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/resource.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">wait3</span><span class="params">(<span class="type">int</span> *statloc, <span class="type">int</span> options, <span class="keyword">struct</span> rusage *rusage)</span>;</span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">wait4</span><span class="params">(<span class="type">pid_t</span> pid, <span class="type">int</span> *statloc, <span class="type">int</span> options, <span class="keyword">struct</span> rusage *rusage)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回进程ID；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>资源统计信息包括用户CPU时间总量、系统CPU时间总量、缺页次数、接收到信号的次数等。</p>
<p>下图列出了各个wait函数所支持的参数。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260209203336012.png"
alt="各种系统上等待函数所支持的参数" />
<figcaption
aria-hidden="true">各种系统上等待函数所支持的参数</figcaption>
</figure>
<hr />
<h2 id="竞争条件">竞争条件</h2>
<p>当多个进程都企图对共享数据进行某种处理，而最后的结果又取决于进程运行的顺序时，我们认为发生了竞争条件（race
condition）。</p>
<p>如果一个进程希望等待一个子进程终止，则它必须调用wait函数中的一个。如果一个进程要等待其父进程终止，则可使用下列形式的循环：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span>(getppid() != <span class="number">1</span>)</span><br><span class="line">  sleep(<span class="number">1</span>);</span><br></pre></td></tr></table></figure>
<p>这种形式的循环称为轮询（polling），它的问题是浪费了CPU时间，因为调用者每隔1s都被唤醒，然后进行条件测试。</p>
<p>为了避免竞争条件和轮询，多个进程之间需要某种形式的信号传递。信号可用于此目的，各种形式的进程间通信（IPC）也可用于此目的。</p>
<hr />
<h2 id="函数exec">函数exec</h2>
<p>当进程调用一种exec函数时，该进程执行的程序完全替换为新程序，而新程序则从其main函数开始执行。因为调用exec并不创建新进程，所以前后的进程ID并未改变。exec只是用磁盘上的一个新程序替换了当前进程的正文段、数据段、堆段和栈段。</p>
<p>有7种不同的exec函数可供使用，它们常常被统称为exec函数。用fork可以创建新进程，用exec可以初始执行新的程序。exit函数和wait函数处理终止和等待终止。这些是基本的进程控制原语。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">execl</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">const</span> <span class="type">char</span> *arg0, ... <span class="comment">/* (char *)0 */</span> )</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">execv</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">char</span> *<span class="type">const</span> argv[])</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">execle</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">const</span> <span class="type">char</span> *arg0, ... <span class="comment">/* (char *)0, char *const envp[] */</span> )</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">execve</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pathname, <span class="type">char</span> *<span class="type">const</span> argv[], <span class="type">char</span> *<span class="type">const</span> envp[])</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">execlp</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *filename, <span class="type">const</span> <span class="type">char</span> *arg0, ... <span class="comment">/* (char *)0 */</span> )</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">execvp</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *filename, <span class="type">char</span> *<span class="type">const</span> argv[])</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fexecve</span><span class="params">(<span class="type">int</span> fd, <span class="type">char</span> *<span class="type">const</span> argv[], <span class="type">char</span> *<span class="type">const</span> envp[])</span>;</span><br><span class="line"><span class="comment">// 7个函数返回值：若出错，返回−1；若成功，不返回</span></span><br></pre></td></tr></table></figure>
<p>这些函数之间的第一个区别是前4个函数取路径名作为参数，后两个函数取文件名作为参数，最后一个取文件描述符作为参数。指定
<code>filename</code> 作为参数时：</p>
<ul>
<li><p>如果filename中包含 <code>/</code>，则就将其视为路径名；</p></li>
<li><p>否则就按PATH环境变量，在它所指定的各目录中搜寻可执行文件。</p></li>
</ul>
<p>PATH
变量包含了一张目录表（称为路径前缀），目录之间用冒号（:）分隔。例如，下列
<code>name=value</code> 环境字符串指定在4个目录中进行搜索。</p>
<p><code>PATH=/bin:/usr/bin:/usr/local/bin:.</code></p>
<p>最后的路径前缀 <code>.</code>
表示当前目录。（零长前缀也表示当前目录。在value的开始处可用
<code>:</code> 表示，在行中间则要用 <code>::</code> 表示，在行尾以
<code>:</code> 表示。）</p>
<p>如果 <code>execlp</code> 或 <code>execvp</code>
使用路径前缀中的一个找到了一个可执行文件，但是该文件不是由连接编辑器产生的机器可执行文件，则就认为该文件是一个shell脚本，于是试着调用
<code>/bin/sh</code>，并以该filename作为shell的输入。</p>
<p>第二个区别与参数表的传递有关（l表示列表list，v表示矢量vector）。函数
<code>execl</code>、<code>execlp</code> 和 <code>execle</code>
要求将新程序的每个命令行参数都说明为一个单独的参数。这种参数表以空指针结尾。对于另外4个函数（<code>execv</code>、<code>execvp</code>、<code>execve</code>
和
<code>fexecve</code>），则应先构造一个指向各参数的指针数组，然后将该数组地址作为这4个函数的参数。</p>
<p>最后一个区别与向新程序传递环境表相关。以e结尾的3个函数（<code>execle</code>、<code>execve</code>
和
<code>fexecve</code>）可以传递一个指向环境字符串指针数组的指针。其他4个函数则使用调用进程中的environ变量为新程序复制现有的环境。通常，一个进程允许将其环境传播给其子进程，但有时也有这种情况，进程想要为子进程指定某一个确定的环境。例如，在初始化一个新登录的shell时，login程序通常创建一个只定义少数几个变量的特殊环境，而在我们登录时，可以通过shell启动文件，将其他变量加到环境中。</p>
<p>打开文件的处理方式取决于每个描述符的“执行时关闭”标志的值。</p>
<p>进程中的每个打开的描述符都有一个“执行时关闭”标志。如果该标志被设置，那么在执行exec时，该描述符会被关闭。否则，在执行exec时，该描述符会保持打开状态。默认情况下，在执行exec时描述符会保持打开状态，除非使用fcntl专门设置“执行时关闭”标志。</p>
<p>POSIX.1特别要求打开的目录流（opendir函数）在执行时被关闭。这通常是由opendir函数调用fcntl来为与打开的目录流对应的描述符设置执行时关闭标志来完成的。</p>
<p>请注意，实际用户ID和实际组ID在exec过程中保持不变，但有效ID可能会发生变化，这取决于所执行程序文件的set-user-ID和set-group-ID位的状态。如果新程序的set-user-ID位被设置，那么有效用户ID就会变为该程序文件的所有者ID。否则，有效用户ID不会改变。组ID的处理方式与此相同。</p>
<p>在很多UNIX实现中，这7个函数中只有 <code>execve</code>
是内核的系统调用。另外6个只是库函数，它们最终都要调用该系统调用。这7个函数之间的关系在下图中。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260210213018217.png" alt="七个exec函数之间的关系" style="zoom:67%;" /></p>
<hr />
<h2 id="更改用户id和更改组id">更改用户ID和更改组ID</h2>
<p>可以用 <code>setuid</code>
函数设置实际用户ID和有效用户ID。与此类似，可以用 <code>setgid</code>
函数设置实际组ID和有效组ID。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">setuid</span><span class="params">(<span class="type">uid_t</span> uid)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">setgid</span><span class="params">(<span class="type">gid_t</span> gid)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>关于谁能更改ID有若干规则。现在先考虑更改用户ID的规则（关于用户ID所说明的一切都适用于组ID）。</p>
<blockquote>
<p>（1）若进程具有超级用户特权，则 <code>setuid</code>
函数将实际用户ID、有效用户ID以及保存的设置用户ID（saved
set-user-ID）设置为 <code>uid</code>。</p>
<p>（2）若进程没有超级用户特权，但是 <code>uid</code>
等于实际用户ID或保存的设置用户ID，则 <code>setuid</code>
只将有效用户ID设置为uid。不更改实际用户ID和保存的设置用户ID。</p>
<p>（3）如果上面两个条件都不满足，则 <code>errno</code> 设置为
<code>EPERM</code>，并返回−1。</p>
</blockquote>
<p>下图总结了更改这3个用户ID的不同方法。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260210222338934.png"
alt="改变3种用户ID的方法" />
<figcaption aria-hidden="true">改变3种用户ID的方法</figcaption>
</figure>
<h3 id="函数setreuid和setregid">函数setreuid和setregid</h3>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">setreuid</span><span class="params">(<span class="type">uid_t</span> ruid, <span class="type">uid_t</span> euid)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">setregid</span><span class="params">(<span class="type">gid_t</span> rgid, <span class="type">gid_t</span> egid)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>如若其中任一参数的值为−1，则表示相应的ID应当保持不变。</p>
<h3 id="函数seteuid和setegid">函数seteuid和setegid</h3>
<p>POIX.1包含了两个函数seteuid和setegid。它们类似于setuid和setgid，但只更改有效用户ID和有效组ID。
01gb <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">seteuid</span><span class="params">(<span class="type">uid_t</span> uid)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">setegid</span><span class="params">(<span class="type">gid_t</span> gid)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure></p>
<p>一个非特权用户可将其有效用户ID设置为其实际用户ID或其保存的设置用户ID。对于一个特权用户则可将有效用户ID设置为uid。（这区别于setuid函数，它更改所有3个用户ID。）</p>
<hr />
<h2 id="解释器文件">解释器文件</h2>
<p>所有现今的UNIX系统都支持解释器文件（interpreter
file）。这种文件是文本文件，其起始行的形式是：</p>
<p><code>#! pathname [ optional-argument ]</code></p>
<p>在感叹号和pathname之间的空格是可选的。最常见的解释器文件以下列行开始：</p>
<p><code>#! /bin/sh</code></p>
<p>pathname通常是绝对路径名，对它不进行什么特殊的处理（不使用PATH进行路径搜索）。对这种文件的识别是由内核作为
exec系统调用处理的一部分来完成的。内核使调用exec函数的进程实际执行的并不是该解释器文件，而是在该解释器文件第一行中pathname所指定的文件。一定要将解释器文件（文本文件，它以
<code>#!</code>
开头）和解释器（由该解释器文件第一行中的pathname指定）区分开来。</p>
<hr />
<h2 id="函数system">函数system</h2>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">system</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *cmdstring)</span>;</span><br></pre></td></tr></table></figure>
<p>如果<code>cmdstring</code>是一个空指针，则仅当命令处理程序可用时，system返回非0值，这一特征可以确定在一个给定的操作系统上是否支持system函数。在UNIX中，system总是可用的。</p>
<p>因为system在其实现中调用了 <code>fork</code>、<code>exec</code> 和
<code>waitpid</code>，因此有3种返回值。</p>
<p>（1）fork失败或者waitpid返回除EINTR之外的出错，则system返回−1，并且设置
<code>errno</code> 以指示错误类型。</p>
<p>（2）如果exec失败（表示不能执行shell），则其返回值如同shell执行了
<code>exit(127)</code> 一样。</p>
<p>（3）否则所有3个函数（fork、exec和waitpid）都成功，那么system的返回值是shell的终止状态，其格式已在waitpid中说明。</p>
<p>使用system而不是直接使用fork和exec的优点是：system进行了所需的各种出错处理以及各种信号处理。</p>
<hr />
<h2 id="process-accounting">Process Accounting</h2>
<p>大多数UNIX系统提供了一个选项以进行进程记账（process
accounting）处理。启用该选项后，每当进程结束时内核就写一个会计记录。典型的会计记录包含总量较小的二进制数据，一般包括命令名、所使用的CPU时间总量、用户ID和组ID、启动时间等。</p>
<p>一个至今没有说明的函数（<code>acct</code>）启用和禁用进程会计。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">acct</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *_Nullable filename)</span>;</span><br></pre></td></tr></table></figure>
<p>会计记录结构定义在头文件<code>&lt;sys/acct.h&gt;</code>中，虽然每种系统的实现各不相同，但会计记录样式基本如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span>　u_short <span class="type">comp_t</span>;　　　　<span class="comment">/* 3-bit base 8 exponent; 13-bit fraction*/</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">acct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">char</span>　 ac_flag;　　　　　　　 <span class="comment">/* flag (see Figure 8.26)*/</span></span><br><span class="line">    <span class="type">char</span>　 ac_stat;　　　　　　　 <span class="comment">/* termination status(signal &amp; core flag only)*/</span></span><br><span class="line">    <span class="comment">/* (Solaris only)*/</span></span><br><span class="line">    <span class="type">uid_t</span>　ac_uid;　　　　　　　　<span class="comment">/* real user ID*/</span></span><br><span class="line">    <span class="type">gid_t</span>　ac_gid;　　　　　　　　<span class="comment">/* real group ID*/</span></span><br><span class="line">    <span class="type">dev_t</span>　ac_tty;　　　　　　　　<span class="comment">/* controlling terminal*/</span></span><br><span class="line">    <span class="type">time_t</span> ac_btime;　　　　　　　<span class="comment">/* starting calendar time*/</span></span><br><span class="line">    <span class="type">comp_t</span> ac_utime;　　　　　　　<span class="comment">/* user CPU time*/</span></span><br><span class="line">    <span class="type">comp_t</span> ac_stime;　　　　　　　<span class="comment">/* system CPU time*/</span></span><br><span class="line">    <span class="type">comp_t</span> ac_etime;　　　　　　　<span class="comment">/* elapsed time*/</span></span><br><span class="line">    <span class="type">comp_t</span> ac_mem;　　　　　　　　<span class="comment">/* average memory usage*/</span></span><br><span class="line">    <span class="type">comp_t</span> ac_io;　　　　　　　　 <span class="comment">/* bytes transferred (by read and write)*/</span></span><br><span class="line">    <span class="type">comp_t</span> ac_rw;　　　　　　　　 <span class="comment">/* blocks read or written*/</span></span><br><span class="line">    <span class="type">char</span>　 ac_comm[<span class="number">8</span>];　　　　　　<span class="comment">/* command name: [8] for Solaris,*/</span></span><br><span class="line">    <span class="comment">/* &quot;blocks&quot; on BSD systems*/</span></span><br><span class="line">    <span class="comment">/* (not present on BSD systems)*/</span></span><br><span class="line">    <span class="comment">/* [10] for Mac OS X, [16] for FreeBSD, and*/</span></span><br><span class="line">    <span class="comment">/* [17] for Linux*/</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="用户标识">用户标识</h2>
<p>任一进程都可以得到其实际用户ID和有效用户ID及组ID。但是，我们有时希望找到运行该程序用户的登录名。我们可以调用
<code>getpwuid(getuid())</code>，但是如果一个用户有多个登录名，这些登录名又对应着同一个用户ID，又将如何呢？（一个人在口令文件中可以有多个登录项，它们的用户
ID 相同，但登录 shell 不同。）系统通常记录用户登录时使用的名字，用
<code>getlogin</code> 函数可以获取此登录名。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">char</span> *<span class="title function_">getlogin</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回指向登录名字符串的指针；若出错，返回NULL</span></span><br></pre></td></tr></table></figure>
<p>如果调用此函数的进程没有连接到用户登录时所用的终端，则函数会失败。通常称这些进程为守护进程（daemon）。</p>
<hr />
<h2 id="进程调度">进程调度</h2>
<p>调度策略和调度优先级是由内核确定的。进程可以通过调整nice值选择以更低优先级运行（通过调整nice值降低它对CPU的占有，因此该进程是“友好的”）。只有特权进程允许提高调度权限。</p>
<p>nice值越小，优先级越高。虽然这可能看起来有些反常，但实际上是有道理的：<strong>你越友好，你的调度优先级就越低。</strong></p>
<p>进程可以通过nice函数获取或更改它的nice值。使用这个函数，进程只能影响自己的nice值，不能影响任何其他进程的nice值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">nice</span><span class="params">(<span class="type">int</span> incr)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回新的nice值；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p><code>incr</code> 参数被增加到调用进程的 <code>nice</code> 值上。如果
<code>incr</code>
太大，系统直接把它降到最大合法值，不给出提示。类似地，如果
<code>incr</code>
太小，系统也会无声息地把它提高到最小合法值。由于−1是合法的成功返回值，在调用nice函数之前需要清楚
<code>errno</code>，在nice函数返回−1时，需要检查它的值。如果
<code>nice</code> 调用成功，并且返回值为−1，那么 <code>errno</code>
仍然为0。如果 <code>errno</code> 不为0，说明 <code>nice</code>
调用失败。</p>
<p><code>getpriority</code> 函数可以像 <code>nice</code>
函数那样用于获取进程的nice值，但是 <code>getpriority</code>
还可以获取一组相关进程的nice值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/resource.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">getpriority</span><span class="params">(<span class="type">int</span> which, <span class="type">id_t</span> who)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回-NZERO～NZERO-1之间的nice值；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>which参数可以取以下三个值之一：<code>PRIO_PROCESS</code>
表示进程，<code>PRIO_PGRP</code> 表示进程组，
<code>PRIO_USER</code>表示用户ID。which参数控制who参数是如何解释的，who参数选择感兴趣的一个或多个进程。如果who参数为0，表示调用进程、进程组或者用户（取决于which参数的值）。当which设为<code>PRIO_USER</code>并且who为0时，使用调用进程的实际用户ID。如果which参数作用于多个进程，则返回所有作用进程中优先级最高的（最小的nice值）。</p>
<p><code>setpriority</code>
函数可用于为进程、进程组和属于特定用户ID的所有进程设置优先级。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/resource.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">setpriority</span><span class="params">(<span class="type">int</span> which, <span class="type">id_t</span> who, <span class="type">int</span> value)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="进程时间">进程时间</h2>
<p>可以度量的3个时间：墙上时钟时间、用户CPU时间和系统CPU时间。任一进程都可调用
<code>times</code> 函数获得它自己以及已终止子进程的上述值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/times.h&gt;</span></span></span><br><span class="line"><span class="type">clock_t</span> <span class="title function_">times</span><span class="params">(<span class="keyword">struct</span> tms *buf)</span>);</span><br><span class="line"><span class="comment">// 返回值：若成功，返回流逝的墙上时钟时间（以时钟滴答数为单位）；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>此函数填写由 <code>buf</code> 指向的 <code>tms</code>
结构，该结构定义如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">tms</span> &#123;</span></span><br><span class="line">    <span class="type">clock_t</span> tms_utime; <span class="comment">/* user CPU time */</span></span><br><span class="line">    <span class="type">clock_t</span> tms_stime; <span class="comment">/* system CPU time */</span></span><br><span class="line">    <span class="type">clock_t</span> tms_cutime; <span class="comment">/* user CPU time,terminated children */</span></span><br><span class="line">    <span class="type">clock_t</span> tms_cstime; <span class="comment">/* system CPU time,terminated children */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>注意，此结构没有包含墙上时钟时间。 <code>times</code>
函数返回墙上时钟时间作为其函数值。此值是相对于过去的某一时刻度量的，所以不能用其绝对值而必须使用其相对值。例如，调用times，保存其返回值。在以后某个时间再次调用times，从新返回的值中减去以前返回的值，此差值就是墙上时钟时间。</p>
<p>该结构中两个针对子进程的字段包含了此进程用wait函数族已等待到的各子进程的值。所有由此函数返回的
<code>clock_t</code> 值都可用
<code>_SC_CLK_TCK</code>（由sysconf函数返回的每秒时钟滴答数<code>sysconf(_SC_CLK_TCK)</code>）转换成秒数。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Process Relationships（九）</title>
    <url>/2025/06/07/notes-reading/computer/apue/apue-09-process-relationships/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第9章 Process Relationships
的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="终端登录">终端登录</h2>
<p>现在描述的过程用于经由终端登录至UNIX系统。该过程几乎与所使用的终端类型无关，所使用的终端可以是基于字符的终端、仿真基于字符终端的图形终端，或者运行窗口系统的图形终端。</p>
<h3 id="bsd终端登录">BSD终端登录</h3>
<p>系统管理员创建通常名为<code>/etc/ttys</code>的文件，其中，每个终端设备都有一行，每一行说明设备名和传递给
<code>getty</code>
程序的参数。当系统自举时，内核创建进程ID为1的进程，也就是
<code>init</code> 进程。<code>init</code>
进程使系统进入多用户模式。<code>init</code> 读取文件
<code>/etc/ttys</code>，对每一个允许登录的终端设备，<code>init</code>
调用一次fork，它所生成的子进程则exec <code>getty</code>程序。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260212165824444.png"
alt="由init调用以允许终端登录的进程" />
<figcaption
aria-hidden="true">由init调用以允许终端登录的进程</figcaption>
</figure>
<p>上图中显示的所有进程都有一个真实用户ID为0和一个有效用户ID为0（即它们都拥有超级用户权限）。init进程还会执行getty程序，且其环境为空。</p>
<p><code>getty</code> 对终端设备调用 <code>open</code>
函数，以读、写方式将终端打开。一旦设备被打开，则文件描述符0、1、2就被设置到该设备。然后
<code>getty</code> 输出“login: ”之类的信息，并等待用户键入用户名。</p>
<p>当用户键入了用户名后，<code>getty</code>
的工作就完成了。然后它以类似于下列的方式调用 <code>login</code>
程序：</p>
<p><code>execle("/bin/login", "login", "-p", username, (char *)0, envp);</code></p>
<p>下图显示了 <code>login</code> 刚被调用后这些进程的状态。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260212200631315.png"
alt="调用登录后进程的状态" />
<figcaption aria-hidden="true">调用登录后进程的状态</figcaption>
</figure>
<p><code>login</code> 能处理多项工作。因为它得到了用户名，所以能调用
<code>getpwnam</code>
取得相应用户的口令文件登录项。然后调用<code>getpass</code>以显示提示“Password:
”，接着读用户键入的口令。它调用<code>crypt</code>将用户键入的口令加密，并与该用户在阴影口令文件中登录项的
<code>pw_passwd</code>
字段相比较。如果用户几次键入的口令都无效，则login以参数1调用exit表示登录过程失败。父进程（init）了解到子进程的终止情况后，将再次调用fork，其后又执行了getty，对此终端重复上述过程。</p>
<p>这是UNIX系统传统的用户身份验证过程。现代UNIX系统已发展到支持多个身份验证过程。例如，FreeBSD、Linux、Mac
OS X 以及 Solaris 都支持被称为 PAM（Pluggable Authentication
Modules，可插入的身份验证模块）的更加灵活的方案。PAM
允许管理人员配置使用何种身份验证方法来访问那些使用PAM库编写的服务。</p>
<p>如果用户正确登录，<code>login</code> 就将完成如下工作。</p>
<ul>
<li><p>将当前工作目录更改为该用户的起始目录（chdir）。</p></li>
<li><p>调用chown更改该终端的所有权，使登录用户成为它的所有者。</p></li>
<li><p>将对该终端设备的访问权限改变成“用户读和写”。</p></li>
<li><p>调用setgid及initgroups设置进程的组ID。</p></li>
<li><p>用login得到的所有信息初始化环境：起始目录（HOME）、shell（SHELL）、用户
名（USER和LOGNAME）以及一个系统默认路径（PATH）。</p></li>
<li><p>改登录用户的ID（setuid）并调用用户的登录shell，其方式类似于：<code>execl("/bin/sh", "-sh", (char *)0)</code>;</p></li>
</ul>
<p><code>argv[0]</code>的第一个字符为减号，这是向所有shell发出的一个标志，表示它们正作为登录shell被调用。shell可以查看这个字符，并相应地修改它们的启动方式。</p>
<p>至此，登录用户的登录shell开始运行。其父进程ID是init进程（进程ID
1），所以当此登录shell终止时，init会得到通知（接到<code>SIGCHLD</code>信号），它会对该终端重复全部上述过程。登录shell的文件描述符0、1和2设置为终端设备。下图显示了这种安排。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260212203753197.png" alt="终端登录的所有设置完成后，进程的安排" style="zoom:67%;" /></p>
<p>现在，登录 shell 读取其启动文件（<code>.profile</code> for the Bourne
shell and Korn shell; <code>.bash_profile</code>,
<code>.bash_login</code>, or <code>.profile</code> for the GNU
Bourne-again shell; and <code>.cshrc</code> and <code>.login</code> for
the C
shell）。这些启动文件通常更改某些环境变量并增加其他的环境变量。例如，大多数用户设置他们自己的
<code>PATH</code>
并常常提示实际终端类型（TERM）。当启动文件完成后，我们最终会得到shell的提示符，然后就可以输入命令了。</p>
<h3 id="mac-os-x终端登录">Mac OS X终端登录</h3>
<p>Mac OS
X部分地基于FreeBSD，所以其终端登录进程与BSD终端登录进程的工作步骤基本相同。但是，Mac
OS X有些不同之处。</p>
<ul>
<li><p>init的工作是由<code>launchd</code>完成的。</p></li>
<li><p>一开始提供的就是图形终端。</p></li>
</ul>
<h3 id="linux终端登录">Linux终端登录</h3>
<p>Linux的终端登录过程非常类似于BSD。Linux login命令是从4.3BSD
login命令派生出来的。BSD登录过程与Linux登录过程的主要区别在于终端配置的指定方式。</p>
<h3 id="solaris终端登录">Solaris终端登录</h3>
<p>Solaris支持两种形式的终端登录：（a）getty方式，这与前面对BSD终端登录的说明一样；（b）ttymon登录，这是SVR4引入的一种新特性。通常，getty用于控制台，ttymon则用于其他终端的登录。</p>
<hr />
<h2 id="网络登录">网络登录</h2>
<p>通过串行终端登录系统与通过网络登录系统之间的主要（物理层面的）区别在于，终端和计算机之间的连接并非点对点的。在这种情况下，登录只是一种可用的服务，就像其他任何网络服务一样，例如文件传输协议（FTP）或简单邮件传输协议（SMTP）。</p>
<p>在上节所述的终端登录中，init知道哪些终端设备可用来进行登录，并为每个设备生成一个getty进程。但是，对网络登录情况则有所不同，所有登录都经由内核的网络接口驱动程序（如以太网驱动程序），而且事先并不知道将会有多少这样的登录。不再让一个进程等待每一个可能的登录，现在我们只需要等待网络连接请求的到来。</p>
<p>为使同一个软件既能处理终端登录，又能处理网络登录，系统使用了一种称为伪终端（pseudo
terminal）的软件驱动程序，它仿真串行终端的运行行为，并将终端操作映射为网络操作，反之亦然。</p>
<h3 id="bsd网络登录">BSD网络登录</h3>
<p>在BSD中，有一个 <code>inetd</code> 进程（有时称为 Internet
superserver），它等待大多数网络连接。本节将说明 BSD
网络登录中所涉及的进程序列。</p>
<p>作为系统启动的一部分，<code>init</code> 调用一个
<code>shell</code>，使其执行shell脚本
<code>/etc/rc</code>。由此shell脚本启动一个守护进程
<code>inetd</code>。一旦此shell脚本终止，<code>inetd</code>
的父进程就变成 <code>init</code>。<code>inetd</code>
等待TCP/IP连接请求到达主机，而当一个连接请求到达时，它执行一次fork，然后生成的子进程exec适当的程序。</p>
<p>让我们假设一个TCP连接请求到达了TELNET服务器。TELNET是一个使用TCP协议的远程登录应用程序。另一台主机（通过某种形式的网络连接到服务器所在主机）上的用户，或者同一台主机上的用户，通过启动TELNET客户端来发起登录：</p>
<p><code>telnet hostname</code></p>
<p>该客户进程打开一个到hostname主机的TCP连接，在hostname主机上启动的程序被称为TELNET服务器。然后，客户进程和服务进程之间使用TELNET应用协议通过TCP连接交换数据。启动客户进程的用户现在登录到了服务进程所在的主机（当然，假定用户在服务进程主机上有一个有效的账号）。下图显示了在执行TELNET服务进程（称为
<code>telnetd</code>）中所涉及的进程序列。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260212224641529.png" alt="执行TELNET服务器所涉及的一系列进程" style="zoom:67%;" /></p>
<p>然后，<code>telnetd</code>
进程会打开一个伪终端设备，并使用fork分裂成两个进程。父进程负责通过网络连接进行通信，子进程则执行
<code>login</code>
程序。父进程和子进程通过伪终端相连。在执行exec之前，子进程会将文件描述符0、1和2设置为伪终端。如果我们成功登录，<code>login</code>
程序会执行上一节中描述的相同步骤：它会切换到我们的主目录，并设置我们的组ID、用户ID以及初始环境。之后，login程序通过调用exec将自身替换为我们的登录shell。下图展示了此时进程的排列情况。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260214110757968.png" alt="网络登录的所有事宜准备就绪后，进程的排列" style="zoom:67%;" /></p>
<p>显然，伪终端设备驱动程序和终端上的实际用户之间发生着很多事情重要的是要理解，无论我们是通过终端还是网络登录，都会有一个登录shell，其标准输入、标准输出和标准错误都连接到终端设备或伪终端设备。这个登录shell是POSIX.1会话的起点，而该终端或伪终端则是该会话的控制终端。</p>
<h3 id="mac-os-x网络登录">Mac OS X网络登录</h3>
<p>通过网络登录Mac OS X系统与登录BSD系统完全相同，因为Mac OS
X部分基于FreeBSD。不过，在Mac OS X上，<code>telnet</code> 守护进程是从
<code>launchd</code> 启动的。</p>
<h3 id="linux网络登录">Linux网络登录</h3>
<p>Linux下的网络登录与BSD下的相同，不过有些发行版会使用一种替代
<code>inetd</code> 的进程，称为扩展互联网服务守护进程，即
<code>xinetd</code>。与<code>inetd</code> 相比，<code>xinetd</code>
进程对其启动的服务提供了更精细的控制。</p>
<h3 id="solaris网络登录">Solaris网络登录</h3>
<p>Solaris中网络登录的工作过程与BSD和Linux中的步骤几乎一样。同样使用了类似
Solaris 系统下网络登录的场景与 BSD 和 Linux
系统下的步骤几乎相同。这里使用的 inetd 服务器在概念上与 BSD
版本类似，不同之处在于 Solaris 版本作为 Service Management
Facility（SMF，服务管理工具）中的重启程序运行。重启程序是一种守护进程，负责启动和监控其他守护进程，并在它们发生故障时重启它们。尽管
<code>inetd</code> 服务器是由 SMF 中的主重启程序启动的，但主重启程序是由
init 启动的，因此总体情况与图 9.5 所示一致。</p>
<hr />
<h2 id="进程组">进程组</h2>
<p>每个进程除了有一进程ID之外，还属于一个进程组。进程组是一个或多个进程的集合，通常与同一个作业相关联，这些进程可以接收来自同一个终端的信号。每个进程组都有一个唯一的进程组ID。进程组ID与进程ID类似：它们都是正整数，并且可以存储在pid_t数据类型中。getpgrp函数返回调用进程的进程组ID。</p>
<p>函数 <code>getpgrp</code> 返回调用进程的进程组ID。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">getpgrp</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 返回值：调用进程的进程组ID</span></span><br></pre></td></tr></table></figure>
<p>Single UNIX Specification定义了 <code>getpgid</code> 函数，参数是
<code>pid</code>，返回该进程的进程组 ID。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">getpgid</span><span class="params">(<span class="type">pid_t</span> pid)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回进程组ID；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>若 <code>pid</code>
是0，返回调用进程的进程组ID，于是，<code>getpgid(0)</code> 等价于
<code>getpgrp()</code>。</p>
<p>每个进程组有一个<strong>组长进程</strong>。组长进程的进程组ID等于其进程ID。</p>
<p>进程组组长可以创建一个进程组、创建该组中的进程，然后终止。只要在某个进程组中有一个进程存在，则该进程组就存在，这与其组长进程是否终止无关。从进程组创建开始到其中最后一个进程离开为止的时间区间称为进程组的生命期。某个进程组中的最后一个进程可以终止，也可以转移到另一个进程组。</p>
<p>进程调用 <code>setpgid</code>
可以加入一个现有的进程组或者创建一个新进程组。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">setpgid</span><span class="params">(<span class="type">pid_t</span> pid, <span class="type">pid_t</span> pgid)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p><code>setpgid</code> 函数将 <code>pid</code> 进程的进程组ID设置为
<code>pgid</code>。如果这两个参数相等，则由 <code>pid</code>
指定的进程变成进程组组长。如果 <code>pid</code>
是0，则使用调用者的进程ID。另外，如果 <code>pgid</code>
是0，则由pid指定的进程ID用作进程组ID。</p>
<p>一个进程只能为它自己或它的子进程设置进程组ID。在它的子进程调用了exec后，它就不再能更改该子进程的进程组ID。</p>
<p>在大多数作业控制shell中，此函数会在fork之后被调用，让父进程设置子进程的进程组ID，并让子进程设置自己的进程组ID。其中一个调用是多余的，但通过同时执行这两个调用，我们可以确保在任一进程认为子进程已被放入其自己的进程组之前，子进程确实已被放入自己的进程组中。如果不这样做，就会出现竞争条件，因为子进程的进程组成员身份将取决于哪个进程先执行。</p>
<hr />
<h2 id="会话">会话</h2>
<p>会话（session）是一个或多个进程组的集合。例如，可以具有下图所示的安排。其中，在一个会话中有3个进程组。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260214145249357.png" alt="Arrangement of processes into process groups and sessions" style="zoom:67%;" /></p>
<p>通常是由shell的管道将几个进程编成一组的。例如，图中的安排可能是由下列形式的shell命令形成的：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">procl | proc2 &amp;</span><br><span class="line">proc3 | proc4 | proc5</span><br></pre></td></tr></table></figure>
<p>进程调用 <code>setsid</code> 函数建立一个新会话。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">setsid</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回进程组ID；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>如果<strong>调用此函数的进程不是一个进程组的组长，则此函数创建一个新会话</strong>。具体会发生以下3件事。</p>
<blockquote>
<p>（1）该进程变成新会话的会话首进程（session
leader，会话首进程是创建该会话的进程）。此时，该进程是新会话中的唯一进程。</p>
<p>（2）该进程成为一个新进程组的组长进程。新进程组ID是该调用进程的进程ID。</p>
<p>（3）该进程没有控制终端（下一节讨论控制终端）。如果在调用
<code>setsid</code> 之前该进程有一个控制终端，那么这种联系也被切断。</p>
</blockquote>
<p>如果该调用进程已经是一个进程组的组长，则此函数返回出错。为了保证不处于这种情况，通常先调用fork，然后使其父进程终止，而子进程则继续。因为子进程继承了父进程的进程组ID，而其进程ID则是新分配的，两者不可能相等，这就保证了子进程不是一个进程组的组长。</p>
<p>Single UNIX
Specification只说明了会话首进程，而没有类似于进程ID和进程组ID的会话ID。显然，会话首进程是具有唯一进程ID的单个进程，所以可以将会话首进程的进程ID视为会话ID。<code>getsid</code>函数返回会话首进程的进程组ID。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">getsid</span><span class="params">(<span class="type">pid_t</span> pid)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回会话首进程的进程组ID；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>如若 <code>pid</code> 是0，<code>getsid</code>
返回调用进程的会话首进程的进程组ID。</p>
<hr />
<h2 id="控制终端">控制终端</h2>
<p>会话和进程组还有一些其他特性。</p>
<blockquote>
<ul>
<li><p>一个会话可以有一个控制终端（controlling
terminal）。这通常是终端设备（在终端登录情况下）或伪终端设备（在网络登录情况下）。</p></li>
<li><p>建立与控制终端连接的会话首进程被称为控制进程（controlling
process）。</p></li>
<li><p>一个会话中的几个进程组可被分成一个前台进程组（foreground process
group）以及一个或多个后台进程组（background process group）。</p></li>
<li><p>如果一个会话有一个控制终端，则它有一个前台进程组，其他进程组为后台进程组。</p></li>
<li><p>无论何时键入终端的中断键（常常是Delete或Ctrl+C），都会将中断信号发送至前台进程组的所有进程。</p></li>
<li><p>无论何时键入终端的退出键（常常是<code>Ctrl+\</code>），都会将退出信号发送至前台进程组的所有进程。</p></li>
<li><p>如果终端接口检测到调制解调器（或网络）已经断开连接，则将挂断信号发送至控制进程（会话首进程）。</p></li>
</ul>
</blockquote>
<p>这些特性示于下图中。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260214152537829.png" alt="Process groups and sessions showing controlling terminal" style="zoom:67%;" /></p>
<hr />
<h2
id="函数tcgetpgrptcsetpgrp和tcgetsid">函数tcgetpgrp、tcsetpgrp和tcgetsid</h2>
<p>需要有一种方法来通知内核哪一个进程组是前台进程组，这样，终端设备驱动程序就能知道将终端输入和终端产生的信号发送到何处。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">tcgetpgrp</span><span class="params">(<span class="type">int</span> fd)</span>; <span class="comment">// 返回值：若成功，返回前台进程组ID；若出错，返回−1</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">tcsetpgrp</span><span class="params">(<span class="type">int</span> fd, <span class="type">pid_t</span> pgrpid)</span>; <span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>函数 <code>tcgetpgrp</code>
返回前台进程组ID，它与在fd上打开的终端相关联。</p>
<p>如果进程有一个控制终端，则该进程可以调用 <code>tcsetpgrp</code>
将前台进程组ID设置为 <code>pgrpid</code> 。<code>pgrpid</code>
值应当是在同一会话中的一个进程组的ID。<code>fd</code>
必须引用该会话的控制终端。</p>
<p>大多数应用程序并不直接调用这两个函数。它们通常由作业控制shell调用。</p>
<p><code>tcgetsid</code>
函数允许应用程序根据控制终端的文件描述符获取会话领导者的进程组ID。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;termios.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">tcgetsid</span><span class="params">(<span class="type">int</span> fd)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回会话首进程的进程组ID；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="作业控制">作业控制</h2>
<p>作业控制允许在一个终端上启动多个作业（进程组），它控制哪一个作业可以访问该终端以及哪些作业在后台运行。</p>
<p>从shell使用作业控制功能的角度观察，用户可以在前台或后台启动一个作业。一个作业只是几个进程的集合，通常是一个进程的管道。例如：</p>
<p><code>vi main.c</code> 在前台启动了只有一个进程组成的作业。</p>
<p>下面的命令：</p>
<p><code>pr *.c | lpr &amp;</code></p>
<p><code>make all &amp;</code></p>
<p>在后台启动了两个作业。这两个后台作业调用的所有进程都在后台运行。</p>
<p>挂起键（通常采用
Ctrl+Z）键入此字符使终端驱动程序将信号<code>SIGTSTP</code>发送至前台进程组中的所有进程，后台进程组作业则不受影响。实际上有3个特殊字符可使终端驱动程序产生信号，并将它们发送至前台进程组，它们是：</p>
<blockquote>
<ul>
<li><p>中断字符（一般采用Delete或<code>Ctrl+C</code>）产生SIGINT；</p></li>
<li><p>退出字符（一般采用<code>Ctrl+\</code>）产生SIGQUIT；</p></li>
<li><p>挂起字符（一般采用<code>Ctrl+Z</code>）产生SIGTSTP。</p></li>
</ul>
</blockquote>
<p>下图总结了作业控制的某些功能。穿过终端驱动程序框的实线表明终端 I/O
和终端产生的信号总是从前台进程组连接到实际终端。对应于
<code>SIGTTOU</code>
信号的虚线表明后台进程组进程的输出是否出现在终端是可选择的。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260214212751113.png" alt="Summary of job control features with foreground and background jobs, and terminal driver" style="zoom:67%;" /></p>
<hr />
<h2 id="freebsd实现">FreeBSD实现</h2>
<p>在讨论了进程、进程组、会话和控制终端的各种属性之后，值得研究一下所有这些是如何实现的。下面将简要了解FreeBSD所使用的实现方式。</p>
<p>下图展示了FreeBSD所使用的各种数据结构。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260215092831725.png" alt="FreeBSD implementation of sessions and process groups" style="zoom:50%;" /></p>
<p>下面从session结构开始说明图中标出的各个字段。每个会话都分配一个session结构（例如，每次调用
<code>setsid</code> 时）。</p>
<ul>
<li><p><code>s_count</code>
是该会话中的进程组数。当此计数器减至0时，则可释放此结构。</p></li>
<li><p><code>s_leader</code> 是指向会话首进程proc结构的指针。</p></li>
<li><p><code>s_ttyvp</code> 是指向控制终端vnode结构的指针。</p></li>
<li><p><code>s_ttyp</code> 是指向控制终端tty结构的指针。</p></li>
<li><p><code>s_sid</code> 是会话ID。</p></li>
</ul>
<p>在调用 <code>setsid</code>
时，在内核中分配一个新的session结构。<code>s_count</code>
设置为1，<code>s_leader</code> 设置为调用进程 proc
结构的指针，<code>s_sid</code> 设置为进程
ID，因为新会话没有控制终端，所以 <code>s_ttyvp</code> 和
<code>s_ttyp</code> 设置为空指针。</p>
<p>接着来看tty结构。内核为每个终端设备和每个伪终端设备都包含一个这样的结构。</p>
<ul>
<li><code>t_session</code>
指向将此终端作为控制终端的session结构（注意，tty结构指向session结构，session结构也指向tty结构）。如果终端失去载波，此指针会被终端用来向会话领导者发送挂断信号（图9.7）。</li>
<li><code>t_pgrp</code>
指向前台进程组的pgrp结构。终端驱动程序用此字段将信号发送给前台进程组。由输入特殊字符（中断、退出和挂起）而产生的3个信号被发送至前台进程组。</li>
<li><code>t_termios</code>
是包含所有这些特殊字符和与该终端有关信息（如波特率、回显打开或关闭等）的结构。</li>
<li><code>t_winsize</code>
是包含终端窗口当前大小的winsize型结构。当终端窗口大小改变时，信号
<code>SIGWINCH</code> 被发送至前台进程组。</li>
</ul>
<p>为了找到特定会话的前台进程组，内核从session结构开始，然后用
<code>s_ttyp</code> 得到控制终端的tty结构，再用 <code>t_pgrp</code>
得到前台进程组的pgrp结构。</p>
<p>pgrp结构包含一个特定进程组的信息。其中各相关字段具体如下。</p>
<ul>
<li><p><code>pg_id</code> 是进程组ID。</p></li>
<li><p><code>pg_session</code>
指向此进程组所属会话的session结构。</p></li>
<li><p><code>pg_members</code> 是指向此进程组proc结构表的指针，该 proc
结构代表进程组的成员。proc结构中 <code>p_pglist</code>
结构是双向链表，指向该组中的下一个进程和上一个进程。直到遇到进程组中的最后一个进程，它的proc结构中
<code>p_pglist</code> 结构为空指针。</p></li>
</ul>
<p>proc结构包含一个进程的所有信息。</p>
<ul>
<li><p><code>p_pid</code> 包含进程ID。</p></li>
<li><p><code>p_pptr</code> 是指向父进程proc结构的指针。</p></li>
<li><p><code>p_pgrp</code>
指向本进程所属的进程组的pgrp结构的指针。</p></li>
<li><p><code>p_pglist</code>
是一个结构，其中包含两个指针，分别指向进程组中上一个和下一个进程。</p></li>
</ul>
<p>最后还有一个vnode结构。如前所述，在打开控制终端设备时分配此结构。一个进程中所有对
<code>/dev/tty</code> 的引用都会通过这个vnode结构。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Signals（十）</title>
    <url>/2025/06/13/notes-reading/computer/apue/apue-10-signals/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第10章 Signals 的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="信号概念">信号概念</h2>
<p>信号是软件中断。信号提供了一种处理异步事件的方法。</p>
<p>首先，每个信号都有一个名字。这些名字都以3个字符 <code>SIG</code>
开头。</p>
<p>在头文件 <code>&lt;signal.h&gt;</code>
中，信号名都被定义为正整数常量（信号编号）。实际上，实现将各信号定义在另一个头文件中，但是该头文件又包括在
<code>&lt;signal.h&gt;</code> 中。</p>
<p>不存在编号为 0 的信号。</p>
<p>许多情况都能产生信号：</p>
<blockquote>
<ul>
<li><p>终端生成的信号在用户按下某些终端按键时产生。在终端上按 Delete
键（或者很多系统中的Ctrl+C键）通常产生中断信号（
<code>SIGINT</code>）。这是停止一个已失去控制的程序的方法。</p></li>
<li><p>硬件异常产生信号：除数为0、无效的内存引用等。这些条件通常由硬件检测到，并通知内核。然后内核为该条件发生时正在运行的进程产生适当的信号。例如，对执行一个无效内存引用的进程产生
<code>SIGSEGV</code> 信号。</p></li>
<li><p>进程调用 <code>kill</code>
函数可将任意信号发送给另一个进程或进程组。自然，对此有所限制：我们必须是我们要向其发送信号的进程的所有者，或者我们必须是超级用户。</p></li>
<li><p>用户可用
<code>kill</code>命令将信号发送给其他进程。此命令只是kill函数的接口。常用此命令终止一个失控的后台进程。</p></li>
<li><p>当某个进程需要获知各种事件时，软件条件能够生成信号。这里指的不是硬件产生条件（如除以
0），而是软件条件。例如
<code>SIGURG</code>（当网络连接上有带外数据到达时产生）、<code>SIGPIPE</code>（当进程向没有读取器的管道写入数据时产生）以及
<code>SIGALRM</code>（当进程设置的闹钟到期时产生）。</p></li>
</ul>
</blockquote>
<p>在某个信号出现时，可以告诉内核按下列3种方式之一进行处理，称之为信号的处理或与信号相关的动作。</p>
<blockquote>
<p>（1）忽略此信号。大多数信号都可使用这种方式进行处理，但有两种信号却决不能被忽略。它们是
<code>SIGKILL</code>和
<code>SIGSTOP</code>。这两种信号不能被忽略的原因是：它们向内核和超级用户提供了使进程终止或停止的可靠方法。另外，如果忽略某些由硬件异常产生的信号（如非法内存引用或除以0），则进程的运行行为是未定义的。</p>
<p>（2）捕捉信号。为了做到这一点，要通知内核在某种信号发生时，调用一个用户函数。在用户函数中，可执行用户希望对这种事件进行的处理。注意，不能捕捉
<code>SIGKILL</code> 和 <code>SIGSTOP</code> 信号。</p>
<p>（3）执行系统默认动作。</p>
</blockquote>
<p>在下列条件下不产生core文件：（a）进程是设置用户ID的，而且当前用户并非程序文件的所有者；（b）进程是设置组ID的，而且当前用户并非该程序文件的组所有者；（c）用户没有写当前工作目录的权限；（d）文件已存在，而且用户对该文件没有写权限；（e）文件太大（<code>RLIMIT_CORE</code>
限制）。</p>
<p>Type <code>man 7 signal</code> see more related to signals in
Linux.</p>
<hr />
<h2 id="函数signal">函数signal</h2>
<p>UNIX系统信号机制最简单的接口是signal函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> (*signal(<span class="type">int</span> signo, <span class="type">void</span> (*func)(<span class="type">int</span>)))(<span class="type">int</span>);</span><br><span class="line"><span class="comment">// 返回值：若成功，返回以前的信号处理配置；若出错，返回SIG_ERR</span></span><br></pre></td></tr></table></figure>
<p><code>signal</code> 函数由ISO C定义。因为ISO
C不涉及多进程、进程组以及终端I/O等，所以它对信号的定义非常含糊，以致于对UNIX系统而言几乎毫无用处。</p>
<p>因为signal的语义与实现有关，所以最好使用 <code>sigaction</code>
函数代替signal函数。</p>
<p><code>signo</code> 参数是信号名。<code>func</code> 的值是常量
<code>SIG_IGN</code>、常量 <code>SIG_DFL</code>
或当接到此信号后要调用的函数的地址。如果指定
<code>SIG_IGN</code>，则向内核表示忽略此信号（记住有两个信号
<code>SIGKILL</code> 和 <code>SIGSTOP</code> 不能忽略）。如果指定
<code>SIG_DFL</code>，
则表示接到此信号后的动作是系统默认动作。当指定函数地址时，则在信号发生时，调用该函数，这种处理为捕捉该信号，称此函数为信号处理程序或信号捕捉函数。</p>
<p>signal
函数原型说明此函数要求两个参数，返回一个函数指针，而该指针所指向的函数无返回值（void）。第一个参数
signo
是一个整型数，第二个参数是函数指针，它所指向的函数需要一个整型参数，无返回值。signal
的返回值是一个函数地址，该函数有一个整型参数（即最后的(int)）。</p>
<p>使用下面的typedef，则可使其简单一些。</p>
<p><code>typedef void Sigfunc(int);</code></p>
<p>然后，可将signal函数原型写成：</p>
<p><code>Sigfunc *signal(int, Sigfunc *);</code></p>
<hr />
<h2 id="中断的系统调用">中断的系统调用</h2>
<p>早期UNIX系统的一个特性是：如果进程在执行一个<strong>低速系统调用而阻塞期间捕捉到一个信号</strong>，则该系统调用就被中断不再继续执行。该系统调用返回出错，其<code>errno</code>
设置为
<code>EINTR</code>。这是基于这样一种假设进行的：既然发生了一个信号，且进程捕获到了该信号，那么很有可能发生了某些事情，这些事情应该会唤醒被阻塞的系统调用。</p>
<p>为了支持这种特性，将系统调用分成两类：低速系统调用和其他系统调用。<strong>低速系统调用是可能会使进程永远阻塞的一类系统调用</strong>，包括：</p>
<ul>
<li><p>如果某些文件类型（管道、终端设备和网络设备）中没有数据，读取操作可能会永远阻塞调用方；</p></li>
<li><p>如果这些相同的文件类型无法立即接受数据，写入操作可能会永远阻塞调用者；</p></li>
<li><p>打开某些文件类型时会阻塞调用方，直到满足某些条件（例如，打开终端设备时会等待，直到连接的调制解调器接听电话）</p></li>
<li><p><code>pause</code>
函数（根据定义，它会使调用进程进入睡眠状态，直到捕获到信号为止）和
<code>wait</code> 函数；</p></li>
<li><p>某些 <code>ioctl</code> 操作；</p></li>
<li><p>某些进程间通信函数。</p></li>
</ul>
<p>这些慢速系统调用的一个显著例外是任何与磁盘输入/输出相关的操作。尽管对磁盘文件的读取或写入可能会暂时阻塞调用者（当磁盘驱动程序将请求排队，然后执行该请求时），但除非发生硬件错误，否则输入/输出操作总会迅速返回并解除对调用者的阻塞。</p>
<p>例如，中断的系统调用所处理的一种情况是，当一个进程开始从终端设备读取数据，而终端前的用户长时间离开终端时。在这个例子中，该进程可能会被阻塞数小时甚至数天，并且除非系统关闭，否则会一直保持这种状态。</p>
<p>POSIX.1 要求只有中断信号的 <code>SA_RESTART</code>
标志有效时，实现才重启动系统调用。<code>sigaction</code>
函数使用这个标志允许应用程序请求重启动被中断的系统调用。</p>
<hr />
<h2 id="可重入函数">可重入函数</h2>
<p>可重入函数是指在多线程或多任务环境中安全调用的函数。这意味着如果一个函数在执行过程中被中断（例如，由操作系统调度器切换到另一个线程或任务），并且在中断后再次被调用，那么该函数的执行状态不会受到影响，能够正确地返回结果。具体来说，可重入函数具有以下几个特征：</p>
<ol type="1">
<li><p><strong>无状态性</strong>：可重入函数不依赖于全局或静态变量，或者如果使用它们，则在函数内部对它们的访问是安全的。例如，函数不应修改任何共享数据或状态，除非使用适当的同步机制（如锁）。</p></li>
<li><p><strong>局部数据</strong>：可重入函数应使用局部变量，这样每个调用都有自己的变量副本，互不干扰。</p></li>
<li><p><strong>无副作用</strong>：可重入函数在执行时不应产生影响外部状态的副作用，例如不应修改全局变量、文件等。</p></li>
<li><p><strong>可并发执行</strong>：可重入函数可以被多个线程或任务并发调用，而不会导致数据竞争或不一致的状态。</p></li>
</ol>
<hr />
<h2 id="可靠信号术语和语义">可靠信号术语和语义</h2>
<p>首先，当造成信号的事件发生时，为进程生成一个信号（或向进程发送一个信号）。事件可以是硬件异常（如除以
0）、软件条件（如alarm 定时器过期）、终端产生的信号或调用kill
函数。当一个信号产生时，内核通常在进程表中以某种形式设置一个标志。</p>
<p>我们说，当为某个信号执行相应操作时，该信号就被传递到了一个进程。在信号产生到其被传递的这段时间里，这个信号被称为处于未决状态（pending）。</p>
<p>进程可以选择阻止信号的传递。如果为某个进程生成了一个被阻塞的信号，并且该信号的处理动作是默认动作或捕获该信号，那么该信号会一直处于待处理状态，直到进程要么（a）解除对该信号的阻塞，要么（b）将处理动作改为忽略该信号。系统会在信号被递送（而非生成）时决定如何处理被阻塞的信号。这使得进程可以在信号被递送前更改对该信号的处理动作。进程可以调用
<code>sigpending</code> 函数来确定哪些信号处于被阻塞且未决的状态。</p>
<p>如果一个被阻塞的信号在进程解除对该信号的阻塞之前被多次生成，会发生什么情况？POSIX.1允许系统要么只递送一次该信号，要么递送多次。如果系统多次递送该信号，我们就说这些信号被排队了。然而，大多数UNIX系统不会对信号进行排队，除非它们支持POSIX.1的实时扩展。相反，UNIX内核只会递送一次该信号。</p>
<p>如果多个信号准备好要传递给一个进程，会发生什么情况？POSIX.1
没有规定这些信号传递给进程的顺序。不过，POSIX.1
的基本原理确实建议，与进程当前状态相关的信号应在其他信号之前传递（<code>SIGSEGV</code>
就是这样一种信号）。</p>
<p>每个进程都有一个信号掩码，它定义了当前被阻止传递到该进程的一组信号。我们可以将这个掩码视为每个可能的信号都有一个对应的位。如果某个给定信号的位是开启的，那么该信号当前就处于被阻塞状态。进程可以通过调用
<code>sigprocmask</code> 来检查和更改其当前的信号掩码。</p>
<p>信号编号可能会超过一个整型所包含的二进制位数，因此 POSIX.1
定义了一个新数据类型
<code>sigset_t</code>，它可以容纳一个信号集。例如，信号掩码就存储在这些信号集之一中。</p>
<hr />
<h2 id="函数kill和raise">函数kill和raise</h2>
<p><code>kill</code> 函数将信号发送给进程或进程组。<code>raise</code>
函数则允许进程向自身发送信号。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">kill</span><span class="params">(<span class="type">pid_t</span> pid, <span class="type">int</span> signo)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">raise</span><span class="params">(<span class="type">int</span> signo)</span>;</span><br><span class="line"><span class="comment">// 两个函数返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>调用 <code>raise(signo);</code> 等价于调用
<code>kill(getpid(), signo);</code></p>
<p><code>kill</code> 的 <code>pid</code> 参数有以下4种不同的情况。</p>
<p><code>pid &gt; 0</code> 将该信号发送给进程ID为pid的进程。</p>
<p><code>pid == 0</code>
该信号会发送给所有满足以下条件的进程：其进程组ID与发送者的进程组ID相同，且发送者有权限向其发送信号。请注意，“所有进程”这一术语不包括一组由实现定义的系统进程。对于大多数UNIX系统而言，这组系统进程包括内核进程和init进程（进程ID为1）。</p>
<p><code>pid &lt; 0</code>
该信号会发送给所有进程组ID等于pid绝对值且发送者有权向其发送信号的进程。同样，所有进程的集合不包括某些系统进程，如前所述。</p>
<p><code>pid == −1</code>
该信号会发送到系统上所有发送者有权限向其发送信号的进程。和之前一样，这组进程不包括某些系统进程。</p>
<p>如果被发送的信号是
<code>SIGCONT</code>，则进程可将它发送给属于同一会话的任一其他进程。</p>
<p>如果对 <code>kill</code>
的调用导致为调用进程生成信号，且该信号未被阻塞，那么在kill返回之前，要么signo信号，要么其他一些未决且未被阻塞的信号会被传递给该进程。</p>
<hr />
<h2 id="函数alarm和pause">函数alarm和pause</h2>
<p>使用 <code>alarm</code>
函数可以设置一个定时器（闹钟时间），在将来的某个时刻该定时器会超时。当定时器超时时，产生
<code>SIGALRM</code>
信号。如果忽略或不捕捉此信号，则其默认动作是终止调用该
<code>alarm</code> 函数的进程。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> <span class="title function_">alarm</span><span class="params">(<span class="type">unsigned</span> <span class="type">int</span> seconds)</span>;</span><br><span class="line"><span class="comment">// 返回值：0或以前设置的闹钟时间的余留秒数</span></span><br></pre></td></tr></table></figure>
<p>参数 <code>seconds</code> 的值是产生信号 <code>SIGALRM</code>
需要经过的时钟秒数。当这一时刻到达时，信号由内核产生，不过，由于处理器调度延迟，在进程获得控制权来处理信号之前，可能还会有额外的时间流逝。</p>
<p>每个进程中只有一个这样的闹钟。如果在调用 <code>alarm</code>
时，该进程先前注册的闹钟尚未过期，那么该闹钟剩余的秒数将作为此函数的返回值。而先前注册的闹钟会被新值取代。</p>
<p>虽然 <code>SIGALRM</code>
的默认动作是终止进程，但是大多数使用闹钟的进程捕捉此信号。如果此时进程要终止，则在终止之前它可以执行所需的清理操作。如果我们想捕捉
<code>SIGALRM</code> 信号，则必须在调用 <code>alarm</code>
之前安装该信号的处理程序。</p>
<p><code>pause</code> 函数使调用进程挂起直至捕捉到一个信号。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pause</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 返回值：−1，errno设置为EINTR</span></span><br></pre></td></tr></table></figure>
<p>只有执行了一个信号处理程序并从其返回时，pause才返回。在这种情况下，pause返回−1，
errno设置为 <code>EINTR</code>。</p>
<hr />
<h2 id="信号集">信号集</h2>
<p>我们需要一种数据类型来表示多个信号——即信号集（signal
set）。我们会将这种数据类型与 <code>sigprocmask</code>
等函数配合使用，以告知内核不允许该集合中的任何信号发生。POSIX.1定义了
<code>sigset_t</code>
数据类型来包含一个信号集，并定义了以下五个函数来操作信号集。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sigemptyset</span><span class="params">(<span class="type">sigset_t</span> *<span class="built_in">set</span>)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">sigfillset</span><span class="params">(<span class="type">sigset_t</span> *<span class="built_in">set</span>)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">sigaddset</span><span class="params">(<span class="type">sigset_t</span> *<span class="built_in">set</span>, <span class="type">int</span> signo)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">sigdelset</span><span class="params">(<span class="type">sigset_t</span> *<span class="built_in">set</span>, <span class="type">int</span> signo)</span>;</span><br><span class="line"><span class="comment">// 4个函数返回值：若成功，返回0；若出错，返回−1</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sigismember</span><span class="params">(<span class="type">const</span> <span class="type">sigset_t</span> *<span class="built_in">set</span>, <span class="type">int</span> signo)</span>;</span><br><span class="line"><span class="comment">// 返回值：若真，返回1；若假，返回0</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="函数sigprocmask">函数sigprocmask</h2>
<p>进程的信号掩码是当前被阻止递送到该进程的一组信号。进程可以通过调用以下函数来检查其信号掩码、更改其信号掩码，或者在一个步骤中执行这两种操作。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sigprocmask</span><span class="params">(<span class="type">int</span> how, <span class="type">const</span> <span class="type">sigset_t</span> *<span class="keyword">restrict</span> <span class="built_in">set</span>, <span class="type">sigset_t</span> *<span class="keyword">restrict</span> oset)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>首先，若 <code>oset</code> 是非空指针，那么进程的当前信号掩码通过
<code>oset</code> 返回。</p>
<p>其次，若 <code>set</code> 是一个非空指针，则参数 <code>how</code>
指示如何修改当前信号屏蔽字。下表说明了 <code>how</code>
可选的值。<code>SIG_BLOCK</code> 是或操作，而 <code>SIG_SETMASK</code>
则是赋值操作。注意，不能阻塞 <code>SIGKILL</code> 和
<code>SIGSTOP</code> 信号。</p>
<table>
<colgroup>
<col style="width: 51%" />
<col style="width: 48%" />
</colgroup>
<thead>
<tr class="header">
<th>how</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>SIG_BLOCK</strong></td>
<td>The new signal mask for the process is the <strong>union</strong> of
its current signal mask and the signal set pointed to by
<code>set</code>. That is, <code>set</code> contains the additional
signals that we want to block.</td>
</tr>
<tr class="even">
<td><strong>SIG_UNBLOCK</strong></td>
<td>The new signal mask for the process is the
<strong>intersection</strong> of its current signal mask and the
<strong>complement</strong> of the signal set pointed to by
<code>set</code>. That is, <code>set</code> contains the signals that we
want to unblock.</td>
</tr>
<tr class="odd">
<td><strong>SIG_SETMASK</strong></td>
<td>The new signal mask for the process is <strong>replaced</strong> by
the value of the signal set pointed to by <code>set</code>.</td>
</tr>
</tbody>
</table>
<p>如果 <code>set</code>
是个空指针，则不改变该进程的信号掩码，<code>how</code>
的值也无意义。</p>
<p>在调用 <code>sigprocmask</code>
后如果有任何未决的、不再阻塞的信号，则在 <code>sigprocmask</code>
返回前，至少将其中之一递送给该进程。</p>
<p>调用 <code>sigprocmask</code>
之后，如果存在任何未阻塞且处于挂起状态的信号，那么在<code>sigprocmask</code>
返回之前，这些信号中至少有一个会被传递给该进程。</p>
<p><code>sigprocmask</code>
是仅为单线程进程定义的。处理多线程进程中信号的屏蔽使用另一个函数。</p>
<hr />
<h2 id="函数sigpending">函数sigpending</h2>
<p><code>sigpending</code>
函数返回对调用进程而言被阻塞无法传递且当前处于挂起状态的信号集。该信号集通过
<code>set</code> 参数返回。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sigpending</span><span class="params">(<span class="type">sigset_t</span> *<span class="built_in">set</span>)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="函数sigaction">函数sigaction</h2>
<p><code>sigaction</code>函数的功能是检查或修改（或检查并修改）与指定信号相关联的处理动作。此函数取代了UNIX早期版本使用
<code>signal</code> 函数。在本节末尾用 <code>sigaction</code> 函数实现了
<code>signal</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sigaction</span><span class="params">(<span class="type">int</span> signo, <span class="type">const</span> <span class="keyword">struct</span> sigaction *<span class="keyword">restrict</span> act, <span class="keyword">struct</span> sigaction *<span class="keyword">restrict</span> oact)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>其中，参数 <code>signo</code> 是要检测或修改其具体动作的信号编号。若
<code>act</code> 指针非空，则要修改其动作。如果 <code>oact</code>
指针非空，则系统经由 <code>oact</code>
指针返回该信号的上一个动作。此函数使用下列结构：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sigaction</span> &#123;</span></span><br><span class="line">    <span class="type">void</span>　　 (*sa_handler)(<span class="type">int</span>); <span class="comment">/* addr of signal handler, */</span></span><br><span class="line">    <span class="comment">/* or SIG_IGN, or SIG_DFL */</span></span><br><span class="line">    <span class="type">sigset_t</span> sa_mask;　　　　　　 <span class="comment">/* additional signals to block */</span></span><br><span class="line">    <span class="type">int</span>　　　sa_flags;　　　　　　<span class="comment">/* signal options*/</span></span><br><span class="line">    <span class="comment">/* alternate handler */</span></span><br><span class="line">    <span class="type">void</span>　　(*sa_sigaction)(<span class="type">int</span>, <span class="type">siginfo_t</span> *, <span class="type">void</span> *);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>当更改信号动作时，如果 <code>sa_handler</code>
字段包含一个信号捕捉函数的地址（不是常量 <code>SIG_IGN</code> 或
<code>SIG_DFL</code>），则 <code>sa_mask</code>
字段说明了一个信号集，在调用该信号捕捉函数之前，这一信号集要加到进程的信号掩码中。仅当从信号捕捉函数返回时再将进程的信号掩码恢复为原先值。这样，在调用信号处理程序时就能阻塞某些信号。操作系统在调用处理程序时，会将正在传递的信号包含在信号掩码中。因此，我们可以确保，每当我们处理某个特定信号时，该信号的另一次出现会被阻塞，直到我们处理完第一次出现的信号为止。同一信号的额外出现通常不会被排队。如果该信号在被阻塞期间出现了五次，那么当我们解除对该信号的阻塞时，该信号的信号处理函数通常只会被调用一次。</p>
<p>一旦为某个特定信号安装了一个动作，该动作会一直保持安装状态，直到我们通过调用
<code>sigaction</code>
来明确更改它。与早期系统中那些不可靠的信号不同，POSIX.1要求信号处理程序在被明确更改前一直保持安装状态。</p>
<p><code>act</code> 结构体的 <code>sa_flags</code>
字段指定了处理该信号的各种选项。下图详细说明了这些选项被设置时的含义。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260217101456505.png" alt="Option flags (sa_flags) for the handling of each signal" style="zoom:50%;" /></p>
<p><code>sa_sigaction</code>字段是一个替代的信号处理程序，在
<code>sigaction</code> 结构中使用了 <code>SA_SIGINFO</code>
标志时，使用该信号处理程序。对于 <code>sa_sigaction</code>
字段和<code>sa_handler</code>
字段两者，实现可能使用同一存储区，所以应用只能一次使用这两个字段中的一个。</p>
<p><code>sa_sigaction</code> 字段是一种替代信号处理程序，当与
<code>sigaction</code> 一起使用<code>SA_SIGINFO</code>
标志时会用到它。实现可能会为 <code>sa_sigaction</code> 字段和
<code>sa_handler</code>
字段使用相同的存储空间，因此应用程序一次只能使用这两个字段中的一个。</p>
<p>通常，按下列方式调用信号处理程序：</p>
<p><code>void handler(int signo);</code></p>
<p>但是，如果设置了 <code>SA_SIGINFO</code>
标志，那么按下列方式调用信号处理程序：</p>
<p><code>void handler(int signo, siginfo_t *info, void *context);</code></p>
<p><code>siginfo</code>
结构包含了信号产生原因的有关信息。该结构的大致样式如下所示。符合POSIX.1的所有实现必须至少包括
<code>si_signo</code> 和 <code>si_code</code>
成员。另外，符合XSI的实现至少应包含下列字段：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">siginfo</span> &#123;</span></span><br><span class="line">    <span class="type">int</span>　　　　　 si_signo;　<span class="comment">/* signal number */</span></span><br><span class="line">    <span class="type">int</span>　　　　　 si_errno;　<span class="comment">/* if nonzero, errno value from &lt;errno.h&gt; */</span></span><br><span class="line">    <span class="type">int</span>　　　　　 si_code;　 <span class="comment">/* additional info (depends on signal) */</span></span><br><span class="line">    <span class="type">pid_t</span>　　　　 si_pid;　　 <span class="comment">/* sending process ID */</span></span><br><span class="line">    <span class="type">uid_t</span>　　　　 si_uid;　　 <span class="comment">/* sending process real user ID */</span></span><br><span class="line">    <span class="type">void</span>　　　　*si_addr;　　<span class="comment">/* address that caused the fault */</span></span><br><span class="line">    <span class="type">int</span>　　　　　 si_status; <span class="comment">/* exit value or signal number */</span></span><br><span class="line">    <span class="class"><span class="keyword">union</span> <span class="title">sigval</span> <span class="title">si_value</span>;</span> <span class="comment">/* application-specific value */</span></span><br><span class="line">    <span class="comment">/* possibly other fields also */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="函数sigsetjmp和siglongjmp">函数sigsetjmp和siglongjmp</h2>
<p>调用 <code>longjmp</code>
有一个问题。当捕捉到一个信号时，进入信号捕捉函数，此时当前信号被自动地加到进程的信号屏蔽字中。这阻止了后来产生的这种信号中断该信号处理程序。如果用
<code>longjmp</code>
跳出信号处理程序，那么，对此进程的信号屏蔽字会发生什么呢？</p>
<p>在FreeBSD 8.0和Mac OS X
10.6.8中，setjmp和longjmp保存和恢复信号屏蔽字。但是， Linux
3.2.0和Solaris
10并不执行这种操作，虽然Linux支持提供BSD行为的选项。FreeBSD 8.0和Mac OS
X
10.6.8提供函数<code>_setjmp</code>和<code>_longjmp</code>，它们也不保存和恢复信号屏蔽字。</p>
<p>为了允许两种形式并存，POSIX.1并没有指定 <code>setjmp</code> 和
<code>longjmp</code> 对信号屏蔽字的作用，而是定义了两个新函数
<code>sigsetjmp</code> 和
<code>siglongjmp</code>。在信号处理程序中进行非局部转移时应当使用这两个函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;setjmp.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sigsetjmp</span><span class="params">(sigjmp_buf env, <span class="type">int</span> savemask)</span>;</span><br><span class="line"><span class="comment">// 返回值：若直接调用，返回0；若从siglongjmp调用返回，则返回非0</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">siglongjmp</span><span class="params">(sigjmp_buf env, <span class="type">int</span> val)</span>;</span><br></pre></td></tr></table></figure>
<p>这两个函数和 <code>setjmp</code>、<code>longjmp</code>
之间的唯一区别是 <code>sigsetjmp</code> 增加了一个参数。如果
<code>savemask</code> 非0，则 <code>sigsetjmp</code> 在 <code>env</code>
中保存进程的当前信号屏蔽字。调用 <code>siglongjmp</code> 时，如果带非0
<code>savemask</code> 的 <code>sigsetjmp</code> 调用已经保存了
<code>env</code>，则 <code>siglongjmp</code>
从其中恢复保存的信号屏蔽字。</p>
<hr />
<h2 id="函数sigsuspend">函数sigsuspend</h2>
<p>需要在一个原子操作中先恢复信号屏蔽字，然后使进程休眠。这种功能是由
<code>sigsuspend</code> 函数所提供的。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sigsuspend</span><span class="params">(<span class="type">const</span> <span class="type">sigset_t</span> *sigmask)</span>;</span><br><span class="line"><span class="comment">// 返回值：−1，并将errno设置为EINTR</span></span><br></pre></td></tr></table></figure>
<p>进程的信号屏蔽字设置为由 <code>sigmask</code>
指向的值。在捕捉到一个信号或发生了一个会终止该进程的信号之前，该进程被挂起。如果捕捉到一个信号而且从该信号处理程序返回，则
<code>sigsuspend</code> 返回，并且该进程的信号屏蔽字设置为调用
<code>sigsuspend</code> 之前的值。</p>
<p>注意，此函数没有成功返回值。如果它返回到调用者，则总是返回−1，并将
<code>errno</code> 设置为
<code>EINTR</code>（表示一个被中断的系统调用）。</p>
<hr />
<h2 id="函数abort">函数abort</h2>
<p><code>abort</code> 函数的功能是使程序异常终止。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">abort</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 此函数不返回值</span></span><br></pre></td></tr></table></figure>
<p>此函数将 <code>SIGABRT</code>
信号发送给调用进程（进程不应忽略此信号）。ISO C规定，调用
<code>abort</code>
将向主机环境递送一个未成功终止的通知，其方法是调用<code>raise(SIGABRT)</code>
函数。</p>
<p>ISO C要求若捕捉到此信号而且相应信号处理程序返回， <code>abort</code>
仍不会返回到其调用者。如果捕捉到此信号，则信号处理程序不能返回的唯一方法是它调用<code>exit</code>、<code>_exit</code>、<code>_Exit</code>、<code>longjmp</code>
或 <code>siglongjmp</code>。POSIX.1也说明 <code>abort</code>
并不理会进程对此信号的阻塞和忽略。</p>
<hr />
<h2
id="函数sleepnanosleep和clock_nanosleep">函数sleep、nanosleep和clock_nanosleep</h2>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> <span class="title function_">sleep</span><span class="params">(<span class="type">unsigned</span> <span class="type">int</span> seconds)</span>;</span><br><span class="line"><span class="comment">// 返回值：0或未休眠完的秒数</span></span><br></pre></td></tr></table></figure>
<p>此函数使调用进程被挂起直到满足下面两个条件之一。</p>
<p>（1）已经过了 <code>seconds</code> 所指定的墙上时钟时间。</p>
<p>（2）调用进程捕捉到一个信号并从信号处理程序返回。</p>
<p>如同 <code>alarm</code>
信号一样，由于其他系统活动，实际返回时间比所要求的会迟一些。</p>
<p>在第1种情形，返回值是0。当由于捕捉到某个信号sleep提早返回时（第2种情形），返回值是未休眠完的秒数（所要求的时间减去实际休眠时间）。</p>
<p>FreeBSD 8.0、Linux 3.2.0、Mac OS X 10.6.8和Solaris 10用
<code>nanosleep</code>
函数实现sleep，使sleep具体实现与信号和闹钟定时器相互独立。</p>
<p><code>nanosleep</code> 函数与 <code>sleep</code>
函数类似，但提供了纳秒级的精度。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">nanosleep</span><span class="params">(<span class="type">const</span> <span class="keyword">struct</span> timespec *reqtp, <span class="keyword">struct</span> timespec *remtp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若休眠到要求的时间，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>这个函数挂起调用进程，直到要求的时间已经超时或者某个信号中断了该函数。
<code>reqtp</code>
参数用秒和纳秒指定了需要休眠的时间长度。如果某个信号中断了休眠间隔，进程并没有终止，<code>remtp</code>
参数指向的 <code>timespec</code>
结构就会被设置为未休眠完的时间长度。如果对未休眠完的时间并不感兴趣，可以把该参数置为NULL。</p>
<p>因为 <code>nanosleep</code>
函数并不涉及产生任何信号，所以不需要担心与其他函数的交互。</p>
<p>随着多个系统时钟的引入，需要使用相对于特定时钟的延迟时间来挂起调用线程。<code>clock_nanosleep</code>
函数提供了这种功能。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">clock_nanosleep</span><span class="params">(<span class="type">clockid_t</span> clock_id, <span class="type">int</span> flags, <span class="type">const</span> <span class="keyword">struct</span> timespec *reqtp, <span class="keyword">struct</span> timespec *remtp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若休眠到要求的时间，返回0；若出错，返回错误码</span></span><br></pre></td></tr></table></figure>
<p><code>clock_id</code>
参数指定用于评估时间延迟的时钟。<code>flags</code>
参数用于控制延迟是相对的还是绝对的。<code>flags</code>
为0时表示休眠时间是相对的（例如，希望休眠的时间长度），如果
<code>flags</code> 值设置为
<code>TIMER_ABSTIME</code>，表示休眠时间是绝对的（例如，希望休眠到达某个特定的时间）。</p>
<p>注意，除了出错返回，调用
<code>clock_nanosleep(CLOCK_REALTIME, 0, reqtp, remtp);</code>和调用
<code>nanosleep(reqtp, remtp);</code>的效果是相同的。使用相对休眠的问题是有些应用对休眠长度有精度要求，相对休眠时间会导致实际休眠时间比要求的长。例如，某个应用程序希望按固定的时间间隔执行任务，就必须获取当前时间，计算下次执行任务的时间，然后调用
<code>nanosleep</code>。在获取当前时间和调用 <code>nanosleep</code>
之间，处理器调度和抢占可能会导致相对休眠时间超过实际需要的时间间隔。使用绝对时间可以提高精度，尽管分时进程调度器无法保证我们的任务会在睡眠时间结束后立即执行。</p>
<hr />
<h2 id="函数sigqueue">函数sigqueue</h2>
<p>使用排队信号必须做以下几个操作。</p>
<blockquote>
<p>（1）使用 <code>sigaction</code> 函数安装信号处理程序时指定
<code>SA_SIGINFO</code>
标志。如果我们不指定这个标志，信号将会被发送，但信号是否会进入队列则由具体实现来决定。</p>
<p>（2）在 <code>sigaction</code> 结构的 <code>sa_sigaction</code>
成员中（而不是通常的 <code>sa_handler</code>
字段）提供信号处理程序。实现可能允许使用 <code>sa_handler</code>
字段，但无法获取通过 <code>sigqueue</code> 函数发送的额外信息。</p>
<p>（3）使用 <code>sigqueue</code> 函数发送信号。</p>
</blockquote>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sigqueue</span><span class="params">(<span class="type">pid_t</span> pid, <span class="type">int</span> signo, <span class="type">const</span> <span class="keyword">union</span> sigval value)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p><code>sigqueue</code> 函数只能把信号发送给单个进程，可以使用
<code>value</code>
参数向信号处理程序传递整数或指针值，除此之外，<code>sigqueue</code>
函数与 <code>kill</code> 函数类似。</p>
<p>信号不能被无限排队。到达相应的 <code>SIGQUEUE_MAX</code>
限制以后，<code>sigqueue</code> 就会失败，将 <code>errno</code> 设为
<code>EAGAIN</code>。</p>
<hr />
<h2 id="信号名和编号">信号名和编号</h2>
<p>本节介绍如何在信号编号和信号名之间进行映射。某些系统提供数组</p>
<p><code>extern char *sys_siglist[];</code></p>
<p>数组下标是信号编号，数组中的元素是指向信号名符串的指针。</p>
<p>可以使用 <code>psignal</code>
函数可移植地打印与信号编号对应的字符串。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">psignal</span><span class="params">(<span class="type">int</span> signo, <span class="type">const</span> <span class="type">char</span> *msg)</span>;</span><br></pre></td></tr></table></figure>
<p>字符串
<code>msg</code>（通常是程序名）输出到标准错误文件，后面跟随一个冒号和一个空格，再后面对该信号的说明，最后是一个换行符。如果msg为
<code>NULL</code>，只有信号说明部分输出到标准错误文件，该函数类似于
<code>perror</code>。</p>
<p>如果在 <code>sigaction</code> 信号处理程序中有
<code>siginfo</code>结构，可以使用 <code>psiginfo</code>
函数打印信号信息。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">psiginfo</span><span class="params">(<span class="type">const</span> <span class="type">siginfo_t</span> *info, <span class="type">const</span> <span class="type">char</span> *msg)</span>;</span><br></pre></td></tr></table></figure>
<p>它的工作方式与 <code>psignal</code>
函数类似。虽然这个函数访问除信号编号以外的更多信息，但不同的平台输出的这些额外信息可能有所不同。</p>
<p>如果只需要信号的字符描述部分，也不需要把它写到标准错误文件中（如可以写到日志文件中），可以使用
<code>strsignal</code> 函数，它类似于 <code>strerror</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="type">char</span> *<span class="title function_">strsignal</span><span class="params">(<span class="type">int</span> signo)</span>;</span><br><span class="line"><span class="comment">// 返回值：指向描述该信号的字符串的指针</span></span><br></pre></td></tr></table></figure>
<p>给出一个信号编号，<code>strsignal</code>
将返回描述该信号的字符串。应用程序可用该字符串打印关于接收到信号的出错消息。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Daemon Processes（十三）</title>
    <url>/2025/08/09/notes-reading/computer/apue/apue-13-daemon-processes/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第13章 Daemon Processes
的一些知识点。</p>
<span id="more"></span>
<hr />
<p>守护进程是长期运行的进程。它们通常在系统启动时启动，仅在系统关闭时终止。由于它们没有控制终端，所以我们说它们在后台运行。UNIX系统有许多守护进程，用于执行日常活动。</p>
<h2 id="守护进程的特征">13.1 守护进程的特征</h2>
<p>任何父进程ID为0的进程通常都是作为系统引导程序的一部分启动的<strong>内核进程</strong>。（
<code>init</code>
（PID=1）是个例外，它是一个由内核在引导装入时启动的<strong>用户层次</strong>的命令。）内核进程是特殊的，通常存在于系统的整个生命期中。它们以超级用户特权运行，无控制终端，无命令行。</p>
<p>在ps的输出实例中，<strong>内核守护进程的名字出现在方括号中</strong>。Linux使用一个名为
<code>kthreadd</code> （PID=2）的特殊内核进程来创建其他内核进程，所以
<code>kthreadd</code>
表现为其他内核进程的父进程。每个需要在进程上下文中执行工作，但不是从用户级进程的上下文中调用的内核组件，通常都会有自己的内核守护进程。</p>
<p>进程1通常是
<code>init</code>（MacOS是<code>launchd</code>），除了其他工作外，主要负责启动各运行层次特定的系统服务。这些服务通常是在它们自己拥有的守护进程的帮助下实现的。</p>
<p>注意，大多数守护进程都以超级用户（root）特权运行。所有的守护进程都没有控制终端，其终端名设置为问号。内核守护进程以无控制终端方式启动。用户级守护进程缺少控制终端可能是守护进程调用了
<code>setsid</code>
的结果。大多数用户层守护进程都是进程组的组长进程以及会话的首进程，而且是这些进程组和会话中的唯一进程（<code>rsyslogd</code>
是一个例外）。最后，应当注意的是用户层守护进程的父进程是
<code>init</code> 进程。</p>
<hr />
<h2 id="编写规则">编写规则</h2>
<p>编写守护进程的一些基本规则可以防止不必要的交互发生。在此列出这些规则，然后展示一个实现这些规则的函数
<code>daemonize</code>。</p>
<blockquote>
<p>（1）首先要做的是<strong>调用 <code>umask</code></strong>
将文件模式创建屏蔽字设置为一个已知值（通常是0）。</p>
<p>（2）<strong>调用fork，然后使父进程exit。</strong>这样做实现了下面几点。第一，如果该守护进程是作为一条简单的shell命令启动的，那么父进程终止会让shell认为这条命令已经执行完毕。第二，虽然子进程继承了父进程的进程组
ID，但获得了一个新的进程
ID，这就保证了子进程不是一个进程组的组长进程。这是下面将要进行的
<code>setsid</code>调用的先决条件。</p>
<p>（3）<strong>调用 <code>setsid</code>
创建一个新会话</strong>。使调用进程：（a）成为新会话的首进程，（b）成为一个新进程组的组长进程，（c）与它的控制终端断开了连接。</p>
<p>（4）<strong>将当前工作目录更改为根目录</strong>。从父进程处继承过来的当前工作目录可能在一个挂载的文件系统中。由于守护进程通常会一直存在直到系统重启，所以如果守护进程停留在已挂载的文件系统上，该文件系统就无法被卸载。</p>
<p>（5）<strong>关闭不再需要的文件描述符</strong>。这使守护进程不再持有从其父进程继承来的任何文件描述符（父进程可能是
shell 进程，或某个其他进程）。</p>
<p>（6）<strong>一些守护进程会将文件描述符0、1和2指向/dev/null</strong>。这样，任何试图从标准输入读取数据或者向标准输出、标准错误写入数据的库函数都不会产生任何效果。因为守护进程并不与终端设备相关联，所以其输出无处显示，也无处从交互式用户那里接收输入。</p>
</blockquote>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;apue.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;syslog.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/resource.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">daemonize</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *cmd)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> i, fd0, fd1, fd2;</span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">rlimit</span> <span class="title">rl</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sigaction</span> <span class="title">sa</span>;</span></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    * Clear file creation mask.</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    umask(<span class="number">0</span>);</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    * Get maximum number of file descriptors.</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">if</span> (getrlimit(RLIMIT_NOFILE, &amp;rl) &lt; <span class="number">0</span>)</span><br><span class="line">        err_quit(<span class="string">&quot;%s: can’t get file limit&quot;</span>, cmd);</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    * Become a session leader to lose controlling TTY.</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">if</span> ((pid = fork()) &lt; <span class="number">0</span>)</span><br><span class="line">        err_quit(<span class="string">&quot;%s: can’t fork&quot;</span>, cmd);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (pid != <span class="number">0</span>) <span class="comment">/* parent */</span></span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    setsid();</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    * Ensure future opens won&#x27;t allocate controlling TTYs.</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    sa.sa_handler = SIG_IGN;</span><br><span class="line">    sigemptyset(&amp;sa.sa_mask);</span><br><span class="line">    sa.sa_flags = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">if</span> (sigaction(SIGHUP, &amp;sa, <span class="literal">NULL</span>) &lt; <span class="number">0</span>)</span><br><span class="line">        err_quit(<span class="string">&quot;%s: can’t ignore SIGHUP&quot;</span>, cmd);</span><br><span class="line">    <span class="keyword">if</span> ((pid = fork()) &lt; <span class="number">0</span>)</span><br><span class="line">        err_quit(<span class="string">&quot;%s: can’t fork&quot;</span>, cmd);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (pid != <span class="number">0</span>) <span class="comment">/* parent */</span></span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    * Change the current working directory to the root so</span></span><br><span class="line"><span class="comment">    * we won&#x27;t prevent file systems from being unmounted.</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">if</span> (chdir(<span class="string">&quot;/&quot;</span>) &lt; <span class="number">0</span>)</span><br><span class="line">        err_quit(<span class="string">&quot;%s: can’t change directory to /&quot;</span>, cmd);</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    * Close all open file descriptors.</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">if</span> (rl.rlim_max == RLIM_INFINITY)</span><br><span class="line">        rl.rlim_max = <span class="number">1024</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; rl.rlim_max; i++)</span><br><span class="line">        close(i);</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    * Attach file descriptors 0, 1, and 2 to /dev/null.</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    fd0 = open(<span class="string">&quot;/dev/null&quot;</span>, O_RDWR);</span><br><span class="line">    fd1 = dup(<span class="number">0</span>);</span><br><span class="line">    fd2 = dup(<span class="number">0</span>);</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    * Initialize the log file.</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    openlog(cmd, LOG_CONS, LOG_DAEMON);</span><br><span class="line">    <span class="keyword">if</span> (fd0 != <span class="number">0</span> || fd1 != <span class="number">1</span> || fd2 != <span class="number">2</span>) &#123;</span><br><span class="line">        syslog(LOG_ERR, <span class="string">&quot;unexpected file descriptors %d %d %d&quot;</span>, fd0, fd1, fd2);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="错误日志记录">错误日志记录</h2>
<p>守护进程存在的一个问题是如何处理出错消息。因为它本就不应该有控制终端，所以不能只是简单地写到标准错误上。需要一个中央守护进程错误日志记录工具。</p>
<p>大多数守护进程都使用 <code>syslog</code> 工具。下图显示了
<code>syslog</code> 工具的详细组织结构。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260308101313462.png" alt="BSD syslog 工具" style="zoom:67%;" /></p>
<p>有3种产生日志消息的方法。</p>
<p>（1）内核例程可以调用 <code>log</code>
函数。任何一个用户进程都可以通过打开（open）并读取（read）<code>/dev/klog</code>
设备来读取这些消息。</p>
<p>（2）大多数用户进程（守护进程）调用 <code>syslog(3)</code>
函数来产生日志消息。这使消息被发送至UNIX域数据报套接字
<code>/dev/log</code>。</p>
<p>（3）无论一个用户进程是在此主机上，还是在通过TCP/IP网络连接到此主机的其他主机上，都可将日志消息发向UDP端口514。注意，<code>syslog</code>
函数从不产生这些UDP数据报，它们要求产生此日志消息的进程进行显式的网络编程。</p>
<p>通常，<code>syslogd</code>
守护进程读取所有3种格式的日志消息。此守护进程在启动时读一个配置文件，其文件名一般为
<code>/etc/syslog.conf</code>，该文件决定了不同种类的消息应送向何处。例如，紧急消息可发送至系统管理员（若已登录），并在控制台上打印，而警告消息则可记录到一个文件中。</p>
<p>该工具的接口是 <code>syslog</code> 函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;syslog.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">openlog</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *ident, <span class="type">int</span> option, <span class="type">int</span> facility)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">syslog</span><span class="params">(<span class="type">int</span> priority, <span class="type">const</span> <span class="type">char</span> *format, ...)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">closelog</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">setlogmask</span><span class="params">(<span class="type">int</span> maskpri)</span>;</span><br><span class="line"><span class="comment">// 返回值：前日志记录优先级屏蔽字值</span></span><br></pre></td></tr></table></figure>
<p>调用 <code>openlog</code> 是可选择的。如果不调用
<code>openlog</code>，则在第一次调用 <code>syslog</code> 时，自动调用
<code>openlog</code>。调用 <code>closelog</code>
也是可选择的，因为它只是关闭了用于与<code>syslogd</code>
守护进程通信的描述符。</p>
<p>调用 <code>syslog</code> 产生一个日志消息。其 <code>priority</code>
参数是 <code>facility</code> 和 <code>level</code>
的组合。<code>level</code> 值按优先级从最高到最低依次排列。</p>
<table>
<colgroup>
<col style="width: 18%" />
<col style="width: 81%" />
</colgroup>
<thead>
<tr class="header">
<th>level</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>LOG_EMERG</code></td>
<td>emergency (system is unusable) (highest priority)</td>
</tr>
<tr class="even">
<td><code>LOG_ALERT</code></td>
<td>condition that must be fixed immediately</td>
</tr>
<tr class="odd">
<td><code>LOG_CRIT</code></td>
<td>critical condition (e.g., hard device error)</td>
</tr>
<tr class="even">
<td><code>LOG_ERR</code></td>
<td>error condition</td>
</tr>
<tr class="odd">
<td><code>LOG_WARNING</code></td>
<td>warning condition</td>
</tr>
<tr class="even">
<td><code>LOG_NOTICE</code></td>
<td>normal, but significant condition</td>
</tr>
<tr class="odd">
<td><code>LOG_INFO</code></td>
<td>informational message</td>
</tr>
<tr class="even">
<td><code>LOG_DEBUG</code></td>
<td>debug message (lowest priority)</td>
</tr>
</tbody>
</table>
<p>将 <code>format</code> 参数以及其他所有参数传至 <code>vsprintf</code>
函数以便进行格式化。在 <code>format</code> 中，每个出现的
<code>%m</code> 字符都先被代换成与 <code>errno</code>
值对应的出错消息字符串（<code>strerror</code>）。</p>
<p><code>setlogmask</code>
函数用于设置进程的记录优先级屏蔽字。它返回调用它之前的屏蔽字。参数表示
“允许哪些级别通过”。注意，试图将记录优先级屏蔽字设置为0并不会有什么作用。</p>
<p>除了
<code>syslog</code>，很多平台还提供它的一种变体来处理可变参数列表。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;syslog.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdarg.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">vsyslog</span><span class="params">(<span class="type">int</span> priority, <span class="type">const</span> <span class="type">char</span> *format, va_list arg)</span>;</span><br></pre></td></tr></table></figure>
<p>大多数 <code>syslogd</code>
实现会将消息短时间排队。如果在此段时间中有重复消息到达，那么
<code>syslog</code>
守护进程不会把它写到日志记录中，而是会打印输出一条类似于”last message
repeated N times.”的消息。</p>
<hr />
<h2 id="单实例守护进程">单实例守护进程</h2>
<p>有些守护进程在实现时，为了保证正常运行，同一时间只能有一个副本在运行。例如，这样的守护进程可能需要对某个设备拥有独占访问权。以
<code>cron</code>
守护进程为例，如果有多个实例在运行，每个副本可能都会尝试启动同一个计划任务，这会导致任务重复执行，并且很可能会出错。</p>
<p>如果守护进程需要访问某个设备，设备驱动程序有时会阻止对
<code>/dev</code>
中相应设备节点的多次打开尝试。这就限制了我们一次只能运行一个守护进程副本。但是，如果没有这样的设备可用，我们就需要自己来完成这项工作。</p>
<p>文件和记录锁定机制为确保只有一个守护进程副本在运行提供了一种基础方法。如果每个守护进程都创建一个具有固定名称的文件，并在整个文件上放置写锁，那么只允许创建一个这样的写锁。后续创建写锁的尝试将会失败，这向守护进程的后续副本表明已有另一个实例在运行。</p>
<p>文件和记录锁定提供了一种便捷的互斥机制。如果守护进程获得了整个文件的写锁，那么当守护进程退出时，该锁会自动移除。这简化了恢复过程，省去了我们清理守护进程前一个实例的必要。</p>
<hr />
<h2 id="守护进程的惯例">守护进程的惯例</h2>
<p>在UNIX系统中，守护进程遵循下列通用惯例。</p>
<blockquote>
<ul>
<li><p>若守护进程使用锁文件，那么该文件通常存储在 <code>/var/run</code>
目录中。需要注意的是，守护进程可能需要具有超级用户权限才能在此目录下创建文件。锁文件的名字通常是
<code>name.pid</code>，其中，<code>name</code>
是该守护进程或服务的名字。例如，<code>cron</code> 守护进程锁文件的名字是
<code>/var/run/crond.pid</code>。</p></li>
<li><p>若守护进程支持配置选项，那么配置文件通常存放在 <code>/etc</code>
目录中。配置文件的名字通常是
<code>name.conf</code>，其中，<code>name</code>
是该守护进程或服务的名字。例如，<code>syslogd</code>
守护进程的配置文件通常是 <code>/etc/syslog.conf</code>。</p></li>
<li><p>守护进程可用命令行启动，但通常它们是由系统初始化脚本之一（<code>/etc/rc*</code>或
<code>/etc/init.d/*</code>）启动的。如果守护进程退出时应自动重启，那么只要我们在
<code>/etc/inittab</code> 中为其包含一个 <code>respawn</code>
条目（假设系统使用System V风格的init命令），就可以让 <code>init</code>
重启它。</p></li>
<li><p>若一个守护进程有一个配置文件，那么当该守护进程启动时会读该文件，但在此之后一般就不会再查看它。若某个管理员更改了配置文件，那么该守护进程需要被停止，然后再启动，以使配置文件的更改生效。为避免此种麻烦，某些守护进程将捕捉
<code>SIGHUP</code>
信号，当它们接收到该信号时，重新读配置文件。因为守护进程并不与终端相结合，它们或者是无控制终端的会话首进程，或者是孤儿进程组的成员，所以守护进程没有理由期望接收
<code>SIGHUP</code>。于是，守护进程可以安全地重复使用
<code>SIGHUP</code>。</p></li>
</ul>
</blockquote>
<hr />
<h2 id="客户进程-服务器进程模型">客户进程-服务器进程模型</h2>
<p>守护进程常常用作服务器进程。通常来说，服务器是一个等待客户端与其联系并请求某种服务的进程。在图13.2中，<code>syslogd</code>
服务器所提供的服务是记录错误消息。</p>
<p>常见的情况是，服务器会通过 <code>fork</code> 子进程并
<code>exec</code>
另一个程序来为客户端提供服务。这些服务器通常会管理多个文件描述符：通信端点、配置文件、日志文件等等。往好里说，在子进程中让这些文件描述符保持打开状态是一种疏忽，因为子进程执行的程序可能用不到它们，尤其是当该程序与服务器无关时。往坏里说，让它们保持打开状态可能会带来安全问题——被执行的程序可能会做出恶意行为，比如修改服务器的配置文件，或者欺骗客户端使其认为自己正在与服务器通信，从而获取未授权的信息。</p>
<p>解决此问题的一个简单方法是对所有被执行程序不需要的文件描述符设置执行时关闭（close-on-exec）标志。下面的代码展示了一个可以用来在服务器进程中执行上述工作的函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">include &lt;fcntl.h&gt;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">set_cloexec</span><span class="params">(<span class="type">int</span> fd)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> val;</span><br><span class="line">    <span class="keyword">if</span>((val = fcntl(fd, F_GETFD, <span class="number">0</span>)) &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    val |= FD_CLOEXEC;</span><br><span class="line">    <span class="keyword">return</span> (fcntl(fd,F_SETFD,val));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Advanced I/O（十四）</title>
    <url>/2025/08/15/notes-reading/computer/apue/apue-14-advanced-io/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第14章 Advanced I/O
的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="非阻塞io">非阻塞I/O</h2>
<p>系统调用分成两类：“低速”系统调用和其他。低速系统调用是可能会使进程永远阻塞的一类系统调用，包括：</p>
<blockquote>
<ul>
<li><p>如果某些文件类型（如读管道、终端设备和网络设备）的数据并不存在，读操作可能会使调用者永远阻塞；</p></li>
<li><p>如果数据不能被相同的文件类型立即接受（如管道中无空间、网络流控制），写操作可能会使调用者永远阻塞；</p></li>
<li><p>在某种条件发生之前打开某些文件类型可能会发生阻塞（如要打开一个终端设备，需要先等待与之连接的调制解调器应答，又如若以只写模式打开FIFO，那么在没有其他进程已用读模式打开该FIFO时也要等待）；</p></li>
<li><p>对已经加上强制性记录锁的文件进行读写；</p></li>
<li><p>某些ioctl操作；</p></li>
<li><p>某些进程间通信函数。</p></li>
</ul>
</blockquote>
<p>我们也曾说过，虽然读写磁盘文件会暂时阻塞调用者，但并不能将与磁盘I/O有关的系统调用视为“低速”。</p>
<p>非阻塞I/O使我们可以发出open、read和write这样的I/O操作，并使这些操作不会永远阻塞。如果这种操作不能完成，则调用立即出错返回，表示该操作如继续执行将阻塞。</p>
<p>对于一个给定的描述符，有两种为其指定非阻塞I/O的方法。</p>
<p>（1）如果调用 <code>open</code> 获得描述符，则可指定
<code>O_NONBLOCK</code> 标志。</p>
<p>（2）对于已经打开的一个描述符，则可调用
<code>fcntl</code>，由该函数打开 <code>O_NONBLOCK</code>
文件状态标志。</p>
<hr />
<h2 id="记录锁定">记录锁定</h2>
<p>当两个人同时编辑一个文件时，在大多数UNIX系统中，该文件的最后状态取决于写该文件的最后一个进程。但是对于有些应用程序，<strong>如数据库</strong>，进程有时需要确保它正在单独写一个文件。为了向进程提供这种功能，商用UNIX系统提供了记录锁定机制。</p>
<p>记录锁定（record
locking）的功能是：<strong>当第一个进程正在读或修改文件的某个部分时，使用记录锁定可以阻止其他进程修改同一文件区</strong>。对于
UNIX 系统而言，“记录”这个词是一种误用，因为 UNIX
系统内核根本没有在一个文件中使用记录这种概念。一个更适合的术语可能是<strong>字节范围锁定</strong>（byte-range
locking），因为它锁定的只是文件中的一个区域（也可能是整个文件）。</p>
<p>早期的伯克利版本只支持 <code>flock</code>
函数。该函数只能对整个文件加锁，不能对文件中的一部分加锁。</p>
<p>SVR3通过 <code>fcntl</code> 函数增加了记录锁功能。在此基础上构造了
<code>lockf</code>
函数，它提供了一个简化的接口。这些函数允许调用者对一个文件中任意字节数的区域加锁，长至整个文件，短至文件中的一个字节。</p>
<p>POSIX.1标准的基础是 <code>fcntl</code>
方法。下图列出了各种系统提供的不同形式的记录锁。</p>
<table>
<thead>
<tr class="header">
<th>System</th>
<th>Advisory</th>
<th>Mandatory</th>
<th>fcntl</th>
<th>lockf</th>
<th>flock</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>SUS</td>
<td>•</td>
<td></td>
<td>•</td>
<td>XSI</td>
<td></td>
</tr>
<tr class="even">
<td>FreeBSD 8.0</td>
<td>•</td>
<td></td>
<td>•</td>
<td>•</td>
<td>•</td>
</tr>
<tr class="odd">
<td>Linux 3.2.0</td>
<td>•</td>
<td>•</td>
<td>•</td>
<td>•</td>
<td>•</td>
</tr>
<tr class="even">
<td>Mac OS X 10.6.8</td>
<td>•</td>
<td></td>
<td>•</td>
<td>•</td>
<td>•</td>
</tr>
<tr class="odd">
<td>Solaris 10</td>
<td>•</td>
<td>•</td>
<td>•</td>
<td>•</td>
<td>•</td>
</tr>
</tbody>
</table>
<p>本节最后部分将说明建议性锁和强制性锁之间的区别。</p>
<h3 id="fcntl-record-locking"><code>fcntl</code> Record Locking</h3>
<p><code>fcntl</code> 函数的原型如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fcnt1.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">fcnt1</span><span class="params">(<span class="type">int</span> fd, <span class="type">int</span> cmd, ...<span class="comment">/* struct flock *flockptr */</span>)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，依赖于cmd（见下），否则，返回−1</span></span><br></pre></td></tr></table></figure>
<p>对于记录锁定，<code>cmd</code> 是
<code>F_GETLK</code>、<code>F_SETLK</code> 或
<code>F_SETLKW</code>。第三个参数（<code>flockptr</code> ）是一个指向
<code>flock</code> 结构的指针。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">flock</span> &#123;</span></span><br><span class="line">    <span class="type">short</span> l_type;　　　 <span class="comment">/* F_RDLCK, F_WRLCK, or F_UNLCK */</span></span><br><span class="line">    <span class="type">short</span> l_whence;　　 <span class="comment">/* SEEK_SET, SEEK_CUR, or SEEK_END */</span></span><br><span class="line">    <span class="type">off_t</span> l_start;　　　 <span class="comment">/* offset in bytes, relative to l_whence */</span></span><br><span class="line">    <span class="type">off_t</span> l_len;　　　　 <span class="comment">/* length, in bytes; 0 means lock to EOF */</span></span><br><span class="line">    <span class="type">pid_t</span> l_pid;　　　　 <span class="comment">/* returned with F_GETLK */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>对 <code>flock</code> 结构说明如下。</p>
<ul>
<li><p>所希望的锁类型：<code>F_RDLCK</code>（共享读锁）、<code>F_WRLCK</code>（独占性写锁）或
<code>F_UNLCK</code>（解锁一个区域）。</p></li>
<li><p>要加锁或解锁区域的起始字节偏移量（<code>l_start</code> 和
<code>l_whence</code>）。</p></li>
<li><p>区域的字节长度（<code>l_len</code>）。</p></li>
<li><p>持有可能会阻塞当前进程的锁的进程的ID（<code>l_pid</code>）</p></li>
</ul>
<p>如果一个进程对一个文件区间已经有了一把锁，后来该进程又企图在同一文件区间再加一把锁，那么新锁将替换已有锁。因此，若一进程在某文件的16～32字节区间有一把写锁，然后又试图在16～32字节区间加一把读锁，那么该请求将成功执行，原来的写锁会被替换为读锁。</p>
<p>加读锁时，该描述符必须是读打开。加写锁时，该描述符必须是写打开。</p>
<p>下面说明一下 <code>fcntl</code>函数的3种命令。</p>
<p><code>F_GETLK</code> 判断由 <code>flockptr</code>
所描述的锁是否会被另外一把锁所排斥（阻塞）。如果存在一把锁，它阻止创建由
<code>flockptr</code>
所描述的锁，则该现有锁的信息将重写<code>flockptr</code>
指向的信息。如果不存在这种情况，则除了将 <code>l_type</code> 设置为
<code>F_UNLCK</code> 之外， <code>flockptr</code>
所指向结构中的其他信息保持不变。</p>
<p><code>F_SETLK</code> 设置由 <code>flockptr</code>
所描述的锁。如果我们试图获得一把读锁（<code>l_type</code> 为
<code>F_RDLCK</code>）或写锁（<code>l_type</code> 为
<code>F_WRLCK</code>），而兼容性规则阻止系统给我们这把锁，那么
<code>fcntl</code> 会立即出错返回，此时<code>errno</code> 设置为
<code>EACCES</code> 或 <code>EAGAIN</code>。此命令也用来清除由
<code>flockptr</code> 指定的锁（<code>l_type</code> 为
<code>F_UNLCK</code>）。</p>
<p><code>F_SETLKW</code> 这个命令是 <code>F_SETLK</code>
的阻塞版本（命令名中的W表示等待（wait））。如果所请求的读锁或写锁因另一个进程当前已经对所请求区域的某部分进行了加锁而不能被授予，那么调用进程会被置为休眠。如果请求创建的锁已经可用，或者休眠由信号中断，则该进程被唤醒。</p>
<p>应当了解，用 <code>F_GETLK</code> 测试能否建立一把锁，然后用
<code>F_SETLK</code> 或 <code>F_SETLKW</code>
企图建立那把锁，这两者不是一个原子操作。因此不能保证在这两次
<code>fcntl</code>
调用之间不会有另一个进程插入并建立一把相同的锁。如果不希望在等待锁变为可用时产生阻塞，就必须处理由
<code>F_SETLK</code> 返回的可能的出错。</p>
<p>实例：请求和释放一把锁</p>
<p>为了避免每次分配 <code>flock</code>
结构，然后又填入各项信息，可以用函数 <code>lock_reg</code>
来处理所有这些细节。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;apue.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">lock_reg</span><span class="params">(<span class="type">int</span> fd, <span class="type">int</span> cmd, <span class="type">int</span> type, <span class="type">off_t</span> offset, <span class="type">int</span> whence, <span class="type">off_t</span> len)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">flock</span> <span class="title">lock</span>;</span></span><br><span class="line">    lock.l_type = type; <span class="comment">/* F_RDLCK, F_WRLCK, F_UNLCK */</span></span><br><span class="line">    lock.l_start = offset; <span class="comment">/* byte offset, relative to l_whence */</span></span><br><span class="line">    lock.l_whence = whence; <span class="comment">/* SEEK_SET, SEEK_CUR, SEEK_END */</span></span><br><span class="line">    lock.l_len = len; <span class="comment">/* #bytes (0 means to EOF) */</span></span><br><span class="line">    <span class="keyword">return</span>(fcntl(fd, cmd, &amp;lock));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>因为大多数锁调用是加锁或解锁一个文件区域（命令 <code>F_GETLK</code>
很少使用），故通常使用下列5个宏中的一个。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> read_lock(fd,offset,whence,len)   lock_reg((fd), F_SETLK, F_RDLCK, (offset), (whence), (len))</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> readw_lock(fd,offset,whence,len)   lock_reg((fd), F_SETLKW, F_RDLCK, (offset), (whence), (len))</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> write_lock(fd,offset,whence,len)   lock_reg((fd), F_SETLK, F_WRLCK, (offset), (whence), (len))</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> writew_lock(fd,offset,whence,len)   lock_reg((fd), F_SETLKW, F_WRLCK, (offset), (whence), (len))</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> un_lock(fd,offset,whence,len)     lock_reg((fd), F_SETLK, F_UNLCK, (offset), (whence), (len))</span></span><br></pre></td></tr></table></figure>
<p>实例：测试一把锁</p>
<p>下图中定义了一个函数
<code>lock_test</code>，我们将用它测试一把锁。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;apue.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">lock_test</span><span class="params">(<span class="type">int</span> fd, <span class="type">int</span> type, <span class="type">off_t</span> offset, <span class="type">int</span> whence, <span class="type">off_t</span> len)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">flock</span> <span class="title">lock</span>;</span></span><br><span class="line">    lock.l_type = type; <span class="comment">/* F_RDLCK or F_WRLCK */</span></span><br><span class="line">    lock.l_start = offset; <span class="comment">/* byte offset, relative to l_whence */</span></span><br><span class="line">    lock.l_whence = whence; <span class="comment">/* SEEK_SET, SEEK_CUR, SEEK_END */</span></span><br><span class="line">    lock.l_len = len; <span class="comment">/* #bytes (0 means to EOF) */</span></span><br><span class="line">    <span class="keyword">if</span> (fcntl(fd, F_GETLK, &amp;lock) &lt; <span class="number">0</span>)</span><br><span class="line">        err_sys(<span class="string">&quot;fcntl error&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (lock.l_type == F_UNLCK)</span><br><span class="line">        <span class="keyword">return</span>(<span class="number">0</span>);      <span class="comment">/* false, region isn’t locked by another proc */</span></span><br><span class="line">    <span class="keyword">return</span>(lock.l_pid); <span class="comment">/* true, return pid of lock owner */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果存在一把锁，它阻塞由参数指定的锁请求，则此函数返回持有这把现有锁的进程的进程ID，否则此函数返回0。通常用下面两个宏来调用此函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> is_read_lockable(fd, offset, whence, len) (lock_test((fd), F_RDLCK, (offset), (whence), (len)) == 0)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> is_write_lockable(fd, offset, whence, len) (lock_test((fd), F_WRLCK, (offset), (whence), (len)) == 0)</span></span><br></pre></td></tr></table></figure>
<p>注意，进程不能使用 <code>lock_test</code>
函数测试它自己是否在文件的某一部分持有一把锁。<code>F_GETLK</code>
命令的定义说明，返回信息指示是否有现有的锁阻止调用进程设置它自己的锁。因为
<code>F_SETLK</code> 和 <code>F_SETLKW</code>
命令总是替换调用进程现有的锁（若已存在，新锁将替换已有锁），所以调用进程决不会阻塞在自己持有的锁上，于是，<code>F_GETLK</code>
命令决不会报告调用进程自己持有的锁。</p>
<h3 id="锁的隐含继承和释放">锁的隐含继承和释放</h3>
<p>关于记录锁的自动继承和释放有3条规则。</p>
<blockquote>
<p>（1）锁与进程和文件两者相关联。这有两重含义：第一重很明显，当一个进程终止时，它所建立的锁全部释放；第二重则不太明显，无论一个描述符何时关闭，该进程通过这一描述符引用的文件上的任何一把锁都会释放。</p>
<p>（2）锁不会在fork操作中由子进程继承。这意味着，如果一个进程获取了锁，然后调用fork，那么对于父进程所获取的锁而言，子进程会被视为另一个进程。子进程必须调用
<code>fcntl</code>，以在通过fork继承的任何描述符上获取自己的锁。这种限制是合理的，因为锁的作用是防止多个进程同时写入同一个文件。如果子进程在fork时继承了锁，那么父进程和子进程就可能同时写入同一个文件。</p>
<p>（3）在执行exec时，新程序会继承锁。不过需要注意的是，如果为文件描述符设置了执行时关闭（close-on-exec）标志，那么当该描述符作为exec的一部分被关闭时，底层文件的所有锁都会被释放。</p>
</blockquote>
<h3 id="freebsd实现">FreeBSD实现</h3>
<p>先简要地观察FreeBSD实现中使用的数据结构。这会帮助进一步理解记录锁的自动继承和释放的第一条规则：锁与进程和文件两者相关联。</p>
<p>考虑一个进程，它执行下列语句（忽略出错返回）。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">fd1 = open(pathname, ...);</span><br><span class="line">write_lock(fd1, <span class="number">0</span>, SEEK_SET, <span class="number">1</span>);　　<span class="comment">/* parent write locks byte 0 */</span></span><br><span class="line"><span class="keyword">if</span> ((pid = fork()) &gt; <span class="number">0</span>) &#123;　　　　　 <span class="comment">/* parent */</span></span><br><span class="line">    fd2 = dup(fd1);</span><br><span class="line">    fd3 = open(pathname, ...);</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (pid == <span class="number">0</span>) &#123;</span><br><span class="line">    read_lock(fd1, <span class="number">1</span>, SEEK_SET, <span class="number">1</span>); <span class="comment">/* child read locks byte 1 */</span></span><br><span class="line">&#125;</span><br><span class="line">pause();</span><br></pre></td></tr></table></figure>
<p>下图显示了父进程和子进程暂停（执行pause()）后的数据结构情况。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260309102807787.png" alt="The FreeBSD data structures for record locking" style="zoom:67%;" /></p>
<p>每个 <code>lockf</code>
结构描述了一个给定进程的一个加锁区域（由偏移量和长度定义的）。图中显示了两个
<code>lockf</code> 结构，一个是由父进程调用 <code>write_lock</code>
形成的，另一个则是由子进程调用 <code>read_lock</code>
形成的。每一个结构都包含了相应的进程ID。</p>
<p>在父进程中，关闭fd1、fd2或fd3中的任意一个都将释放由父进程设置的写锁。在关闭这3个描述符中的任意一个时，内核会从该描述符所关联的i节点开始，逐个检查
<code>lockf</code>
链接表中的各项，并释放由调用进程持有的各把锁。内核并不清楚（也不关心）父进程是用这3个描述中的哪一个来设置这把锁的。</p>
<p>实例</p>
<p>守护进程可用一把文件锁来保证只有该守护进程的唯一副本在运行。下图展示了
<code>lockfile</code> 函数的实现，守护进程可用该函数在文件上加写锁。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">lockfile</span><span class="params">(<span class="type">int</span> fd)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">flock</span> <span class="title">fl</span>;</span></span><br><span class="line">    fl.l_type = F_WRLCK;</span><br><span class="line">    fl.l_start = <span class="number">0</span>;</span><br><span class="line">    fl.l_whence = SEEK_SET;</span><br><span class="line">    fl.l_len = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">return</span>(fcntl(fd, F_SETLK, &amp;fl));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>另一种方法是用 <code>write_lock</code> 函数定义 <code>lockfile</code>
函数。</p>
<p><code>#define lockfile(fd) write_lock((fd), 0, SEEK_SET, 0)</code></p>
<hr />
<h2 id="io多路复用">I/O多路复用</h2>
<p>为了使用这种技术，先构造一张我们感兴趣的描述符（通常都不止一个）的列表，然后调用一个函数，直到这些描述符中的一个已准备好进行I/O时，该函数才返回。<code>poll</code>、<code>pselect</code>
和 <code>select</code>
这3个函数使我们能够执行I/O多路复用。在从这些函数返回时，进程会被告知哪些描述符已准备好可以进行I/O。</p>
<h3 id="函数select和pselect">函数select和pselect</h3>
<p>传给select的参数告诉内核：</p>
<ul>
<li><p>我们所关心的描述符；</p></li>
<li><p>对于每个描述符我们所关心的条件（是否想<strong>从一个给定的描述符读，是否想写一个给定的描述符，是否关心一个给定描述符的异常条件</strong>）；</p></li>
<li><p>愿意等待多长时间（可以永远等待、等待一个固定的时间或者根本不等待）。</p></li>
</ul>
<p>从select返回时，内核告诉我们：</p>
<ul>
<li><p>已准备好的描述符的总数量；</p></li>
<li><p>对于读、写或异常这3个条件中的每一个，哪些描述符已准备好。</p></li>
</ul>
<p>使用这种返回信息，就可调用相应的I/O函数（一般是read或write），并且确知函数不会阻塞。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/select.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">select</span><span class="params">(<span class="type">int</span> maxfdp1, fd_set *<span class="keyword">restrict</span> readfds, fd_set *<span class="keyword">restrict</span> writefds, fd_set *<span class="keyword">restrict</span> exceptfds, </span></span><br><span class="line"><span class="params">           <span class="keyword">struct</span> timeval *<span class="keyword">restrict</span> tvptr)</span>;</span><br><span class="line"><span class="comment">// 返回值：准备就绪的描述符数目；若超时，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>先来说明最后一个参数，它指定愿意等待的时间长度，单位为秒和微秒。有以下3种情况。</p>
<ol type="1">
<li><p><code>tvptr == NULL</code>
永远等待。如果捕捉到一个信号则中断此无限期等待。当所指定的描述符中的一个已准备好或捕捉到一个信号则返回。如果捕捉到一个信号，则select返回-1，errno设置为
<code>EINTR</code>。</p></li>
<li><p><code>tvptr-&gt;tv_sec == 0 &amp;&amp; tvptr-&gt;tv_usec == 0</code>
根本不等待。测试所有指定的描述符并立即返回。这是轮询系统找到多个描述符状态而不阻塞select函数的方法。</p></li>
<li><p><code>tvptr-&gt;tv_sec != 0 || tvptr-&gt;tv_usec != 0</code>
等待指定的秒数和微秒数。当指定的描述符之一已准备好，或当指定的时间值已经超过时立即返回。如果在超时到期时还没有一个描述符准备好，则返回值是
0。与第一种情况一样，这种等待可被捕捉到的信号中断。</p></li>
</ol>
<p>POSIX.1允许实现修改 <code>timeval</code>
结构中的值，所以在select返回后，你不能指望该结构仍旧保持调用select之前它所包含的值。FreeBSD
8.0、Mac OS X 10.6.8和Solaris
10都保持该结构中的值不变。但是，若在超时时间尚未到期时，select就返回，那么Linux
3.2.0将用剩余时间值更新该结构。</p>
<p>中间3个参数 <code>readfds</code>、<code>writefds</code> 和
<code>exceptfds</code>
是指向描述符集的指针。这3个描述符集说明了我们关心的可读、可写或处于异常条件的描述符集合。每个描述符集存储在一个
<code>fd_set</code>
数据类型中。这个数据类型是由实现选择的，它可以为每一个可能的描述符保持一位。我们可认为它只是一个很大的字节数组。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260309134731811.png" alt="Specifying the read, write, and exception descriptors for select" style="zoom:67%;" /></p>
<p>对于 <code>fd_set</code>
数据类型，唯一可以进行的处理是：分配一个这种类型的变量，将这种类型的一个变量值赋给同类型的另一个变量，或对这种类型的变量使用下列4个函数中的一个。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/select.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">FD_ISSET</span><span class="params">(<span class="type">int</span> fd, fd_set *fdset)</span>;</span><br><span class="line"><span class="comment">// 返回值：若fd在描述符集中，返回非0值；否则，返回0</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">FD_CLR</span><span class="params">(<span class="type">int</span> fd, fd_set *fdset)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">FD_SET</span><span class="params">(<span class="type">int</span> fd, fd_set *fdset)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">FD_ZERO</span><span class="params">(fd_set *fdset)</span>;</span><br></pre></td></tr></table></figure>
<p>在声明了一个描述符集之后，必须用 <code>FD_ZERO</code>
将这个描述符集置为0，然后在其中设置我们关心的各个描述符的位。具体操作如下所示：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">fd_set rset;</span><br><span class="line"><span class="type">int</span>　　fd;</span><br><span class="line">FD_ZERO(&amp;rset);</span><br><span class="line">FD_SET(fd, &amp;rset);</span><br><span class="line">FD_SET(STDIN_FILENO, &amp;rset);</span><br></pre></td></tr></table></figure>
<p>从 <code>select</code> 返回时，可以用 <code>FD_ISSET</code>
测试该集中的一个给定位是否仍处于打开状态：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (FD_ISSET(fd, &amp;rset)) &#123;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>select</code>
的中间3个参数（指向描述符集的指针）中的任意一个（或全部）可以是空指针，这表示对相应条件并不关心。如果所有3个指针都是<code>NULL</code>，则
<code>select</code>
提供了比<code>sleep</code>更精确的定时器。（<code>sleep</code>
等待整数秒，而 <code>select</code>
的等待时间则可以小于1秒，其实际精度取决于系统时钟。）</p>
<p><code>select</code> 第一个参数 <code>maxfdp1</code>
的意思是“<strong>最大文件描述符编号值加1</strong>”。考虑所有3个描述符集，在3个描述符集中找出最大描述符编号值，然后加1，这就是第一个参数值。也可将第一个参数设置为
<code>FD_SETSIZE</code>，这是 <code>&lt;sys/select.h&gt;</code>
中的一个常量，它指定最大描述符数（经常是1
024），但是对大多数应用程序而言，此值太大了。确实，大多数应用程序只使用3～10个描述符（某些应用程序需要更多的描述符，但这种UNIX程序并不典型）。通过指定我们所关注的最大描述符，内核就只需在此范围内寻找打开的位，而不必在3个描述符集中的数百个没有使用的位内搜索。</p>
<p>select有3个可能的返回值。</p>
<blockquote>
<p>（1）返回值-1表示出错。这是可能发生的，例如，在所指定的描述符一个都没准备好时捕捉到一个信号。在此种情况下，一个描述符集都不修改。</p>
<p>（2）返回值0表示没有描述符准备好。若指定的描述符一个都没准备好，指定的时间就过了，那么就会发生这种情况。此时，所有描述符集都会置0。</p>
<p>（3）一个正返回值说明了已经准备好的描述符数。<strong>该值是3个描述符集中已准备好的描述符数之和</strong>，所以如果同一描述符已准备好读和写，那么在返回值中会对其计两次数。在这种情况下，
3个描述符集中仍旧打开的位对应于已准备好的描述符。</p>
</blockquote>
<p>如果在一个描述符上碰到了文件尾端，则select会认为该描述符是可读的。然后调用read，它返回0，这是UNIX系统指示到达文件尾端的方法。</p>
<p>POSIX.1也定义了一个 <code>select</code> 的变体，称为
<code>pselect</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/select.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pselect</span><span class="params">(<span class="type">int</span> maxfdp1, fd_set *<span class="keyword">restrict</span> readfds, fd_set *<span class="keyword">restrict</span> writefds, fd_set *<span class="keyword">restrict</span> exceptfds,</span></span><br><span class="line"><span class="params">      <span class="type">const</span> <span class="keyword">struct</span> timespec *<span class="keyword">restrict</span> tsptr, <span class="type">const</span> <span class="type">sigset_t</span> *<span class="keyword">restrict</span> sigmask)</span>;</span><br><span class="line"><span class="comment">// 返回值：准备就绪的描述符数目；若超时，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>除下列几点外，<code>pselect</code> 与 <code>select</code> 相同。</p>
<ul>
<li><p><code>select</code> 的超时值用 <code>timeval</code> 结构指定，但
<code>pselect</code> 使用
<code>timespec</code>结构。<code>timespec</code>
结构以秒和纳秒表示超时值，而非秒和微秒。如果平台支持这样的时间精度，那么
<code>timespec</code> 就能提供更精准的超时时间。</p></li>
<li><p><code>pselect</code> 的超时值被声明为
<code>const</code>，这保证了调用 <code>pselect</code>
不会改变此值。</p></li>
<li><p><code>pselect</code> 可使用可选信号屏蔽字。若
<code>sigmask</code> 为 <code>NULL</code>，那么在与信号有关的方面，
<code>pselect</code> 的运行状况和 <code>select</code>
相同。否则，<code>sigmask</code> 指向一信号屏蔽字，在调用
<code>pselect</code>
时，以原子操作的方式安装该信号屏蔽字。在返回时，恢复以前的信号屏蔽字。在
<code>pselect</code> 调用期间被屏蔽的信号不会被处理，但在
<code>pselect</code> 返回后，这些信号会恢复为可处理状态。</p></li>
</ul>
<h3 id="函数poll">函数poll</h3>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;poll.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">poll</span><span class="params">(<span class="keyword">struct</span> pollfd fdarray[], <span class="type">nfds_t</span> nfds, <span class="type">int</span> timeout)</span>;</span><br><span class="line"><span class="comment">// 返回值：准备就绪的描述符数目；若超时，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>与 <code>select</code>不同，<code>poll</code>
不是为每个条件（可读性、可写性和异常条件）构造一个描述符集，而是构造一个
<code>pollfd</code>
结构的数组，每个数组元素指定一个描述符编号以及我们对该描述符感兴趣的条件。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">pollfd</span> &#123;</span></span><br><span class="line">    <span class="type">int</span>　 fd;　　　 <span class="comment">/* file descriptor to check, or &lt; 0 to ignore */</span></span><br><span class="line">    <span class="type">short</span> events; <span class="comment">/* events of interest on fd */</span></span><br><span class="line">    <span class="type">short</span> revents; <span class="comment">/* events that occurred on fd */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p><code>fdarray</code> 数组中的元素数由 <code>nfds</code> 指定。</p>
<p>当一个描述符被挂断（<code>POLLHUP</code>）后，就不能再写该描述符，但是有可能仍然可以从该描述符读取到数据。</p>
<p><code>poll</code> 的最后一个参数指定的是我们愿意等待多长时间。如同
<code>select</code> 一样，有3种不同的情形。</p>
<ol type="1">
<li><p><code>timeout == -1</code>永远等待。当所指定的描述符中的一个已准备好，或捕捉到一个信号时返回。如果捕捉到一个信号，则poll返回-1，errno设置为
<code>EINTR</code>。</p></li>
<li><p><code>timeout == 0</code>
不等待。测试所有描述符并立即返回。这是一种轮询系统的方法，可以找到多个描述符的状态而不阻塞
<code>poll</code> 函数。</p></li>
<li><p><code>timeout &gt; 0</code> 等待 <code>timeout</code>
毫秒。当指定的描述符之一已准备好，或 <code>timeout</code>
到期时立即返回。若 <code>timeout</code>
到期时还没有一个描述符准备好，返回值是0。</p></li>
</ol>
<p>理解文件尾端与挂断之间的区别是很重要的。如果我们正从终端输入数据，并键入文件结束符，那么就会打开
<code>POLLIN</code>，于是我们就可以读文件结束指示（read返回0）。<code>revents</code>
中的 <code>POLLHUP</code>
没有打开。如果正在读调制解调器，并且电话线已挂断，我们将接到
<code>POLLHUP</code> 通知。</p>
<hr />
<h2 id="异步io">异步I/O</h2>
<p>在探讨使用异步输入/输出的不同方式之前，我们需要先讨论其成本。当我们决定使用异步输入/输出时，由于要处理多个并发操作，会使应用程序的设计变得复杂。一种更简单的方法可能是使用多线程，这能让我们采用同步模型来编写程序，并让各个线程彼此异步运行。</p>
<hr />
<h2 id="函数readv和writev">函数readv和writev</h2>
<p><code>readv</code> 和 <code>writev</code>
函数用于在一次函数调用中读、写<strong>多个非连续缓冲区</strong>。有时也将这两个函数称为散布读（scatter
read）和聚集写（gather write）。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/uio.h&gt;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">readv</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="keyword">struct</span> iovec *iov, <span class="type">int</span> iovcnt)</span>;</span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">writev</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="keyword">struct</span> iovec *iov, <span class="type">int</span> iovcnt)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：已读或已写的字节数；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>这两个函数的第二个参数是指向 <code>iovec</code>
结构数组的一个指针：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">iovec</span> &#123;</span></span><br><span class="line">    <span class="type">void</span> *iov_base; <span class="comment">/* starting address of buffer */</span></span><br><span class="line">    <span class="type">size_t</span> iov_len; <span class="comment">/* size of buffer */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p><code>iov</code> 数组中的元素数由 <code>iovcnt</code>
指定，其最大值受限于 <code>IOV_MAX</code>。</p>
<p><code>writev</code>
函数从缓冲区中聚集输出数据的顺序是：<code>iov[0]</code>、<code>iov[1]</code>
直至 <code>iov[iovcnt-1]</code>。<code>writev</code>
返回输出的字节总数，通常应等于所有缓冲区长度之和。</p>
<p><code>readv</code>
函数则将读入的数据按上述同样顺序散布到缓冲区中。<code>readv</code>
总是先填满一个缓冲区，然后再填写下一个。<code>readv</code>
返回读到的字节总数。如果遇到文件尾端，已无数据可读，则返回0。</p>
<hr />
<h2 id="函数readn和writen">函数readn和writen</h2>
<p>管道、FIFO以及某些设备（特别是终端和网络）有下列两种性质。</p>
<p>（1）即使我们尚未遇到文件末尾，<code>read</code>
操作返回的数据也可能少于请求的数据量。这并非错误，只需继续从设备读取即可。</p>
<p>（2）<code>write</code>
操作返回的字节数可能少于我们指定的数量。例如，这可能是由内核输出缓冲区已满导致的。不过，这并非错误，我们应该继续写入剩余的数据。（通常，只有在使用非阻塞描述符或者捕获到信号时，才会出现写操作返回的字节数少于预期这种情况。）</p>
<p>在读写磁盘文件时，永远不会遇到这种情况，除非文件系统空间耗尽，或者达到了配额限制，导致无法写入所有请求的内容。</p>
<p>通常，在读、写一个管道、网络设备或终端时，需要考虑这些特性。可以分别使用
<code>readn</code> 和 <code>writen</code>
函数来读取和写入N字节的数据，让这些函数处理可能小于请求值的返回值。这两个函数会根据需要多次调用read或write，以读取或写入全部的N字节数据。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 自定义函数</span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">readn</span><span class="params">(<span class="type">int</span> fd, <span class="type">void</span> *buf, <span class="type">size_t</span> nbytes)</span>;</span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">writen</span><span class="params">(<span class="type">int</span> fd, <span class="type">void</span> *buf, <span class="type">size_t</span> nbytes)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：读、写的字节数；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stddef.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 从指定描述符读n个字节</span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">readn</span><span class="params">(<span class="type">int</span> fd, <span class="type">void</span> *buf, <span class="type">size_t</span> nbytes)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">size_t</span> nleft;</span><br><span class="line">    <span class="type">ssize_t</span> nread;</span><br><span class="line">    nleft = nbytes;</span><br><span class="line">    <span class="keyword">while</span>(nleft &gt; <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>((nread = read(fd,buf,nleft)) &lt; <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(nleft == nbytes)</span><br><span class="line">                <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                <span class="keyword">break</span>; <span class="comment">// 返回已经读取的数量</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(nread == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">break</span>; <span class="comment">// EOF</span></span><br><span class="line">        nleft -= nread;</span><br><span class="line">        buf += nread;</span><br><span class="line">  &#125;</span><br><span class="line">    <span class="keyword">return</span> (nbytes - nleft);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 向指定描述符写n个字节</span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">writen</span><span class="params">(<span class="type">int</span> fd, <span class="type">void</span> *buf, <span class="type">size_t</span> nbytes)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">size_t</span> nleft;</span><br><span class="line">    <span class="type">ssize_t</span> nwrite;</span><br><span class="line">    nleft = nbytes;</span><br><span class="line">    <span class="keyword">while</span>(nleft &gt; <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>((nwrite = write(fd,buf,nleft)) &lt; <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(nleft == nbytes)</span><br><span class="line">                <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(nwrite == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        nleft -= nwrite;</span><br><span class="line">        buf += nwrite;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> (nbytes - nleft);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="memory-mapped-io">Memory-Mapped I/O</h2>
<p>存储映射I/O（memory-mapped
I/O）能将一个磁盘文件映射到存储空间中的一个缓冲区上，于是，当从缓冲区中取数据时，就相当于读文件中的相应字节。与此类似，将数据存入缓冲区时，相应字节就自动写入文件。这样，就可以在不使用read和write的情况下执行I/O。</p>
<p>要使用此功能，我们必须告知内核将给定的文件映射到内存中的一个区域。这项任务由
<code>mmap</code> 函数处理。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">mmap</span><span class="params">(<span class="type">void</span> *addr, <span class="type">size_t</span> len, <span class="type">int</span> prot, <span class="type">int</span> flag, <span class="type">int</span> fd, <span class="type">off_t</span> off)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回映射区的起始地址；若出错，返回MAP_FAILED</span></span><br></pre></td></tr></table></figure>
<p><code>addr</code>
参数用于指定映射存储区的起始地址。通常将其设置为0，这表示由系统选择该映射区的起始地址。此函数的返回值是该映射区的起始地址。</p>
<p><code>fd</code>
参数是指定要被映射文件的描述符。在文件映射到地址空间之前，必须先打开该文件。<code>len</code>
参数是映射的字节数，<code>off</code>
是要映射字节在文件中的起始偏移量。</p>
<p><code>prot</code> 参数指定了映射存储区的保护要求，如表所示。</p>
<table>
<thead>
<tr class="header">
<th>prot</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>PROT_READ</code></td>
<td>Region can be read.</td>
</tr>
<tr class="even">
<td><code>PROT_WRITE</code></td>
<td>Region can be written.</td>
</tr>
<tr class="odd">
<td><code>PROT_EXEC</code></td>
<td>Region can be executed.</td>
</tr>
<tr class="even">
<td><code>PROT_NONE</code></td>
<td>Region cannot be accessed.</td>
</tr>
</tbody>
</table>
<p>可将 <code>prot</code> 参数指定为 <code>PROT_NONE</code> ，也可指定为
<code>PROT_READ</code>、<code>PROT_WRITE</code> 和
<code>PROT_EXEC</code>
的任意组合的按位或。对指定映射存储区的保护要求<strong>不能超过文件open模式访问权限</strong>。例如，若该文件是只读打开的，那么对映射存储区就不能指定
<code>PROT_WRITE</code>。</p>
<p>在说明 <code>flag</code>
参数之前，先看一下存储映射文件的基本情况。下图显示了一个存储映射文件。在此图中，“起始地址”是
<code>mmap</code> 的返回值。内存映射区位于堆和栈之间。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260309180558242.png" alt="Example of a memory-mapped file" style="zoom:67%;" /></p>
<p>下面是 <code>flag</code> 参数影响映射存储区的多种属性。</p>
<blockquote>
<ul>
<li><p><code>MAP_FIXED</code> 返回值必须等于
<code>addr</code>。因为这不利于可移植性，所以不鼓励使用此标志。如果未指定此标志，而且
<code>addr</code> 非0，则内核只把 <code>addr</code>
视为在何处设置映射区的一种建议，但是不保证会使用所要求的地址。将
<code>addr</code> 指定为0可获得最大可移植性。</p></li>
<li><p><code>MAP_SHARED</code>
此标志描述了该进程将存储操作部署到映射区域的方式。该标志规定，存储操作会修改映射文件，也就是说，一次存储操作等同于对该文件的一次写入。必须指定此标志或下一个标志（<code>MAP_PRIVATE</code>），但不能同时指定两者。</p></li>
<li><p><code>MAP_PRIVATE</code>
该标志表明，对映射区域执行存储操作会导致创建映射文件的私有副本。所有后来对映射区域的引用会转而引用该副本。（此标志的一种用途是供调试器使用，调试器会映射程序文件的文本部分，但允许用户修改指令。任何修改都会影响副本，而不是原始程序文件。）</p></li>
</ul>
</blockquote>
<p><code>off</code> 的值和 <code>addr</code> 的值（如果指定了
<code>MAP_FIXED</code>）通常被要求是系统虚拟存储页长度的倍数。虚拟存储页长可用带参数<code>_SC_PAGESIZE</code>或
<code>_SC_PAGE_SIZE</code> 的<code>sysconf</code>
函数得到。因为off和addr常常指定为0，所以这种要求一般并不重要。</p>
<p>与映射区相关的信号有 <code>SIGSEGV</code> 和
<code>SIGBUS</code>。信号 <code>SIGSEGV</code>
通常用于指示进程试图访问对它不可用的存储区。如果映射存储区被
<code>mmap</code>
指定成了只读的，那么进程试图将数据存入这个映射存储区的时候，也会产生此信号。如果映射区的某个部分在访问时已不存在，则产生
<code>SIGBUS</code>
信号。例如，假设用文件长度映射了一个文件，但在引用该映射区之前，另一个进程已将该文件截断。此时，如果进程试图访问对应于该文件已截去部分的映射区，将会接收到
<code>SIGBUS</code> 信号。</p>
<p>子进程能通过fork继承内存映射区（因为子进程复制父进程地址空间，而内存映射区是该地址空间中的一部分），但是由于同样的原因，新程序则不能通过
<code>exec</code> 继承存储映射区。</p>
<p>调用 <code>mprotect</code> 可以更改一个现有映射的权限。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">mprotect</span><span class="params">(<span class="type">void</span> *addr, <span class="type">size_t</span> len, <span class="type">int</span> prot)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p><code>prot</code> 的合法值与 <code>mmap</code> 中 <code>prot</code>
参数的一样。请注意，实现可能要求 <code>addr</code>
的值必须是系统页长的整数倍。</p>
<p>如果修改的页是通过 <code>MAP_SHARED</code>
标志映射到地址空间的，那么修改并不会立即写回到文件中。相反，何时写回脏页由内核的守护进程决定，决定的依据是系统负载和用来限制在系统失败事件中的数据损失的配置参数。因此，如果只修改了一页中的一个字节，当修改被写回到文件中时，整个页都会被写回。</p>
<p>如果共享映射中的页已修改，那么可以调用 <code>msync</code>
将该页冲洗到被映射的文件中。<code>msync</code> 函数类似于
<code>fsync</code>，但作用于内存映射区。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">msync</span><span class="params">(<span class="type">void</span> *addr, <span class="type">size_t</span> len, <span class="type">int</span> flags)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>如果映射是私有的，那么不修改被映射的文件。与其他内存映射函数一样，地址必须与页边界对齐。</p>
<p><code>flags</code>
参数使我们对如何冲洗存储区有某种程度的控制。可以指定
<code>MS_ASYNC</code>
标志来简单地安排页面的写入。如果希望在返回之前等待写操作完成，则可指定
<code>MS_SYNC</code> 标志。一定要指定 <code>MS_ASYNC</code> 和
<code>MS_SYNC</code> 中的一个。</p>
<p><code>MS_INVALIDATE</code>是一个可选标志，允许我们通知操作系统丢弃那些与底层存储器没有同步的页。若使用了此标志，某些实现将丢弃指定范围中的所有页，但这种行为并不是必需的。</p>
<p>当进程终止时，会自动解除内存映射区的映射，或者直接调用
<code>munmap</code>
函数也可以解除映射区。关闭映射存储区时使用的文件描述符并不解除映射区。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">munmap</span><span class="params">(<span class="type">void</span> *addr, <span class="type">size_t</span> len)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p><code>munmap</code> 并不影响被映射的对象，也就是说，调用
<code>munmap</code> 并不会使映射区的内容写到磁盘文件上。对于
<code>MAP_SHARED</code>
区域，内核的虚拟内存算法会在我们向内存映射区域写入数据后的某个时刻自动更新磁盘文件。而对于
<code>MAP_PRIVATE</code>
区域，当该区域被解除映射时，对其内存所做的修改会被丢弃。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Threads（十一）</title>
    <url>/2025/07/27/notes-reading/computer/apue/apue-11-threads/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第11章 Threads 的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="线程概念">线程概念</h2>
<p>典型的UNIX进程可以看成只有一个控制线程：一个进程在某一时刻只能做一件事情。有了多个控制线程以后，在程序设计时就可以把进程设计成在某一时刻能够做不止一件事，每个线程处理各自独立的任务。这种方法有很多好处。</p>
<blockquote>
<ul>
<li><p>通过为每种事件类型分配单独的处理线程，可以简化处理异步事件的代码。每个线程在进行事件处理时可以采用同步编程模式，同步编程模式要比异步编程模式简单得多。</p></li>
<li><p>多个进程必须使用操作系统提供的复杂机制才能实现内存和文件描述符的共享。而多个线程自动地可以访问相同的存储地址空间和文件描述符。</p></li>
<li><p>有些问题可以分解从而提高整个程序的吞吐量。</p></li>
<li><p>交互的程序同样可以通过使用多线程来改善响应时间，多线程可以把程序中处理用户输入输出的部分与其他部分分开。</p></li>
</ul>
</blockquote>
<p>每个线程都包含有表示执行环境所必需的信息，其中包括进程中标识线程的线程ID、一组寄存器值、栈、调度优先级和策略、信号屏蔽字、errno变量以及线程私有数据。一个进程的所有信息对该进程的所有线程都是共享的，包括可执行程序的正文段、程序的全局内存和堆内存、栈以及文件描述符。</p>
<hr />
<h2 id="线程标识">线程标识</h2>
<p>就像每个进程有一个进程ID一样，每个线程也有一个线程ID。进程
ID在整个系统中是唯一的，但线程ID不同，线程ID只有在它所属的进程上下文中才有意义。</p>
<p>回忆一下进程ID，它是用 <code>pid_t</code>
数据类型来表示的，是一个非负整数。线程ID是用 <code>pthread_t</code>
数据类型来表示的，实现的时候可以用一个结构来代表<code>pthread_t</code>
数据类型，所以可移植的操作系统实现不能把它作为整数处理。因此必须使用一个函数来<strong>对两个线程ID进行比较</strong>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_equal</span><span class="params">(<span class="type">pthread_t</span> tid1, <span class="type">pthread_t</span> tid2)</span>;</span><br><span class="line"><span class="comment">// 返回值：若相等，返回非0数值；否则，返回0</span></span><br></pre></td></tr></table></figure>
<p>Linux 3.2.0使用<strong>无符号长整型</strong>表示
<code>pthread_t</code> 数据类型。Solaris 10把 <code>pthread_t</code>
数据类型表示为无符号整型。FreeBSD 8.0和Mac OS X 10.6.8用一个指向
<code>pthread</code> 结构的指针来表示 <code>pthread_t</code>
数据类型。</p>
<p>线程可以通过调用 <code>pthread_self</code>
函数<strong>获得自身的线程ID</strong>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">pthread_t</span> <span class="title function_">pthread_self</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 返回值：调用线程的线程ID</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="线程创建">线程创建</h2>
<p>新增的线程可以通过调用 <code>pthread_create</code> 函数创建。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_create</span><span class="params">(<span class="type">pthread_t</span> *<span class="keyword">restrict</span> tidp, <span class="type">const</span> <span class="type">pthread_attr_t</span> *<span class="keyword">restrict</span> attr, <span class="type">void</span> *(*start_rtn)(<span class="type">void</span> *), <span class="type">void</span> *<span class="keyword">restrict</span> arg)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>当 <code>pthread_create</code>
成功返回时，新创建线程的线程ID会被设置成 <code>tidp</code>
指向的内存单元。<code>attr</code>
参数用于定制各种不同的线程属性。置为NULL，创建一个具有默认属性的线程。</p>
<p>新创建的线程从 <code>start_rtn</code>
函数的地址开始运行，该函数只有一个无类型指针参数
<code>arg</code>。如果需要向 <code>start_rtn</code>
函数传递的参数有一个以上，那么需要把这些参数放到一个结构中，然后把这个结构的地址作为
<code>arg</code> 参数传入。</p>
<p>线程创建时并不能保证哪个线程会先运行：是新创建的线程，还是调用线程。新创建的线程可以访问进程的地址空间，并且继承调用线程的浮点环境和信号屏蔽字，但是该线程的挂起信号集会被清除。</p>
<hr />
<h2 id="线程终止">线程终止</h2>
<p>如果进程中的任意线程调用了 <code>exit</code>、<code>_Exit</code>
或者<code>_exit</code>，那么整个进程就会终止。与此相类似，如果默认的动作是终止进程，那么，发送到线程的信号就会终止整个进程。</p>
<p>单个线程可以通过3种方式退出，因此可以在不终止整个进程的情况下，停止它的控制流。</p>
<p>（1）线程可以简单地从启动例程中返回，返回值是线程的退出码。</p>
<p>（2）线程可以被同一进程中的其他线程取消。</p>
<p>（3）线程调用 <code>pthread_exit</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">pthread_exit</span><span class="params">(<span class="type">void</span> *rval_ptr)</span>;</span><br></pre></td></tr></table></figure>
<p><code>rval_ptr</code>
参数是一个无类型指针，与传给启动例程的单个参数类似。</p>
<p>进程中的其他线程也可以通过调用 <code>pthread_join</code>
函数访问到这个指针。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_join</span><span class="params">(<span class="type">pthread_t</span> thread, <span class="type">void</span> **rval_ptr)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>调用线程将<strong>一直阻塞</strong>，直到指定的线程调用
<code>pthread_exit</code>、从启动例程中返回或者被取消。如果线程简单地从它的启动例程返回，<code>rval_ptr</code>
就包含返回码。如果线程被取消，由 <code>rval_ptr</code>
指定的内存单元就设置为 <code>PTHREAD_CANCELED</code>。</p>
<p>如果对线程的返回值并不感兴趣，那么可以把 <code>rval_ptr</code> 设置为
NULL。在这种情况下，调用 <code>pthread_join</code>
函数可以等待指定的线程终止，但并不获取线程的终止状态。</p>
<p>线程可以通过调用 <code>pthread_cancel</code>
函数来请求取消同一进程中的其他线程。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_cancel</span><span class="params">(<span class="type">pthread_t</span> tid)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>在默认情况下，<code>pthread_cancel</code> 函数会使得由
<code>tid</code> 标识的线程的行为表现为如同调用了参数为
<code>PTHREAD_ CANCELED</code> 的 <code>pthread_exit</code>
函数，但是，线程可以选择忽略取消或者控制如何被取消。注意
<code>pthread_cancel</code> 并不等待线程终止，它仅仅提出请求。</p>
<p>线程可以安排它退出时需要调用的函数，这与进程在退出时可以用
<code>atexit</code>
函数安排退出是类似的。这样的函数称为线程清理处理程序（thread cleanup
handler）。一个线程可以建立多个清理处理程序。处理程序记录在栈中，也就是说，它们的执行顺序与它们注册时相反。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">pthread_cleanup_push</span><span class="params">(<span class="type">void</span> (*rtn)(<span class="type">void</span> *), <span class="type">void</span> *arg)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">pthread_cleanup_pop</span><span class="params">(<span class="type">int</span> execute)</span>;</span><br></pre></td></tr></table></figure>
<p><code>pthread_cleanup_push</code> 函数会安排清理函数 <code>rtn</code>
在线程执行以下操作之一时，以单个参数 <code>arg</code> 被调用：</p>
<ul>
<li><p>调用 <code>pthread_exit</code> 时；</p></li>
<li><p>响应取消请求时；</p></li>
<li><p>用非零 <code>execute</code> 参数调用
<code>pthread_cleanup_pop</code> 时。</p></li>
</ul>
<p>如果 <code>execute</code> 参数设置为
0，清理函数将不被调用。不管发生上述哪种情况，<code>pthread_cleanup_pop</code>
都将删除上次 <code>pthread_cleanup_push</code>
调用建立的清理处理程序。</p>
<p>这些函数有一个限制，由于它们可以实现为宏，所以必须在与线程相同的作用域中以匹配对的形式使用。<code>pthread_cleanup_push</code>
的宏定义可以包含字符<code>{</code>这种情况下，在
<code>pthread_cleanup_pop</code>
的定义中要有对应的匹配字符<code>}</code>。</p>
<p>下图总结了线程函数和进程函数之间的相似的函数。</p>
<table>
<colgroup>
<col style="width: 18%" />
<col style="width: 24%" />
<col style="width: 56%" />
</colgroup>
<thead>
<tr class="header">
<th>Process primitive</th>
<th>Thread primitive</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>fork</code></td>
<td><code>pthread_create</code></td>
<td>create a new flow of control</td>
</tr>
<tr class="even">
<td><code>exit</code></td>
<td><code>pthread_exit</code></td>
<td>exit from an existing flow of control</td>
</tr>
<tr class="odd">
<td><code>waitpid</code></td>
<td><code>pthread_join</code></td>
<td>get exit status from flow of control</td>
</tr>
<tr class="even">
<td><code>atexit</code></td>
<td><code>pthread_cleanup_push</code></td>
<td>register function to be called at exit from flow of control</td>
</tr>
<tr class="odd">
<td><code>getpid</code></td>
<td><code>pthread_self</code></td>
<td>get ID for flow of control</td>
</tr>
<tr class="even">
<td><code>abort</code></td>
<td><code>pthread_cancel</code></td>
<td>request abnormal termination of flow of control</td>
</tr>
</tbody>
</table>
<p>在默认情况下，线程的终止状态会保存直到对该线程调用
<code>pthread_join</code>。如果线程已经被分离，线程的底层存储资源可以在线程终止时立即被收回。在线程被分离后，我们不能用
<code>pthread_join</code> 函数等待它的终止状态，因为对分离状态的线程调用
<code>pthread_join</code>
会产生未定义行为。可以调用<code>pthread_detach</code> 分离线程。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_detach</span><span class="params">(<span class="type">pthread_t</span> tid)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="线程同步">线程同步</h2>
<p>当多个控制线程共享相同的内存时，需要确保每个线程看到一致的数据视图。</p>
<h3 id="互斥量">互斥量</h3>
<p>可以通过使用pthreads互斥接口来保护数据，并确保每次只有一个线程能够访问数据。互斥量（mutex）从本质上说是一把锁，在访问共享资源前对互斥量进行设置（加锁），在访问完成后释放（解锁）互斥量。对互斥量进行加锁以后，任何其他试图再次对互斥量加锁的线程都会被阻塞直到当前线程释放该互斥锁。如果释放互斥量时有一个以上的线程阻塞，那么所有该锁上的阻塞线程都会变成可运行状态，第一个变为运行的线程就可以对互斥量加锁，其他线程就会看到互斥量依然是锁着的，只能回去再次等待它重新变为可用。在这种方式下，每次只有一个线程可以向前执行。</p>
<p>互斥变量是用 <code>pthread_mutex_t</code>
数据类型表示的。在使用互斥变量前，必须首先对它进行初始化，可以把它设置为常量
<code>PTHREAD_MUTEX_INITIALIZER</code>（只适用于静态分配的互斥量），也可以通过调用
<code>pthread_mutex_init</code>
函数进行初始化。如果动态分配互斥量（例如，通过调用malloc函数），在释放内存前需要调用
<code>pthread_mutex_destroy</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutex_init</span><span class="params">(<span class="type">pthread_mutex_t</span> *<span class="keyword">restrict</span> mutex, <span class="type">const</span> <span class="type">pthread_mutexattr_t</span> *<span class="keyword">restrict</span> attr)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutex_destroy</span><span class="params">(<span class="type">pthread_mutex_t</span> *mutex)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>要用默认的属性初始化互斥量，只需把 <code>attr</code> 设为
<code>NULL</code>。</p>
<p>对互斥量进行加锁，需要调用
<code>pthread_mutex_lock</code>。如果互斥量已经上锁，调用线程将阻塞直到互斥量被解锁。对互斥量解锁，需要调用
<code>pthread_mutex_unlock</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutex_lock</span><span class="params">(<span class="type">pthread_mutex_t</span> *mutex)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutex_trylock</span><span class="params">(<span class="type">pthread_mutex_t</span> *mutex)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutex_unlock</span><span class="params">(<span class="type">pthread_mutex_t</span> *mutex)</span>;</span><br><span class="line"><span class="comment">// 所有函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>如果线程不希望被阻塞，它可以使用 <code>pthread_mutex_trylock</code>
尝试对互斥量进行加锁。如果调用 <code>pthread_mutex_trylock</code>
时互斥量处于未锁住状态，那么 <code>pthread_mutex_trylock</code>
将锁住互斥量，不会出现阻塞直接返回0，否则
<code>pthread_mutex_trylock</code> 就会失败，不能锁住互斥量，返回
<code>EBUSY</code>。</p>
<h3 id="避免死锁">避免死锁</h3>
<p>如果线程试图对同一个互斥量加锁两次，那么它自身就会陷入死锁状态，但是使用互斥量时，还有其他不太明显的方式也能产生死锁。例如，程序中使用一个以上的互斥量时，如果允许一个线程一直占有第一个互斥量，并且在试图锁住第二个互斥量时处于阻塞状态，但是拥有第二个互斥量的线程也在试图锁住第一个互斥量。因为两个线程都在相互请求另一个线程拥有的资源，所以这两个线程都无法向前运行，于是就产生死锁。</p>
<p>可以通过仔细控制互斥量加锁的顺序来避免死锁的发生。例如，假设需要对两个互斥量A和B同时加锁。如果所有线程总是在对互斥量B加锁之前锁住互斥量A，那么使用这两个互斥量就不会产生死锁（当然在其他的资源上仍可能出现死锁）。类似地，如果所有的线程总是在锁住互斥量A之前锁住互斥量B，那么也不会发生死锁。可能出现的死锁只会发生在一个线程试图锁住另一个线程以相反的顺序锁住的互斥量。</p>
<p>有时候，应用程序的结构使得对互斥量进行排序是很困难的。如果涉及了太多的锁和数据结构，可用的函数并不能把它转换成简单的层次，那么就需要采用另外的方法。在这种情况下，可以先释放占有的锁，然后过一段时间再试。这种情况可以使用
<code>pthread_mutex_trylock</code>
接口避免死锁。如果已经占有某些锁而且<code>pthread_mutex_trylock</code>
接口返回成功，那么就可以前进。但是，如果不能获取锁，可以先释放已经占有的锁，做好清理工作，然后过一段时间再重新试。</p>
<p>多线程的软件设计涉及这两者之间的折中。如果锁的粒度太粗，就会出现很多线程阻塞等待相同的锁，这可能并不能改善并发性。如果锁的粒度太细，那么过多的锁开销会使系统性能受到影响，而且代码变得复杂。作为一个程序员，需要在满足锁需求的情况下，在代码复杂性和性能之间找到正确的平衡。</p>
<h3 id="函数pthread_mutex_timedlock">函数pthread_mutex_timedlock</h3>
<p>当线程试图获取一个已加锁的互斥量时，<code>pthread_mutex_timedlock</code>
互斥量原语允许绑定线程阻塞时间。<code>pthread_mutex_timedlock</code>
函数与 <code>pthread_mutex_lock</code>
是基本等价的，但是在达到超时时间值时，<code>pthread_mutex_timedlock</code>
不会对互斥量进行加锁，而是返回错误码 <code>ETIMEDOUT</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutex_timedlock</span><span class="params">(<span class="type">pthread_mutex_t</span> *<span class="keyword">restrict</span> mutex, <span class="type">const</span> <span class="keyword">struct</span> timespec *<span class="keyword">restrict</span> tsptr)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>超时指定愿意等待的绝对时间。这个超时时间是用 <code>timespec</code>
结构来表示的，它用秒和纳秒来描述时间。</p>
<h3 id="读写锁">读写锁</h3>
<p>读写锁（reader-writer
lock）与互斥量类似，不过读写锁允许更高的并行性。互斥量要么是锁住状态，要么就是不加锁状态，而且一次只有一个线程可以对其加锁。读写锁可以有3种状态：读模式下加锁状态，写模式下加锁状态，不加锁状态。一次只有一个线程可以占有写模式的读写锁，但是多个线程可以同时占有读模式的读写锁。</p>
<p>当读写锁是写加锁状态时，在这个锁被解锁之前，所有试图对这个锁加锁的线程都会被阻塞。当读写锁在读加锁状态时，所有试图以读模式对它进行加锁的线程都可以得到访问权，但是任何希望以写模式对此锁进行加锁的线程都会阻塞，直到所有的线程释放它们的读锁为止。虽然各操作系统对读写锁的实现各不相同，但当读写锁处于读模式锁住的状态，而这时有一个线程试图以写模式获取锁时，读写锁通常会阻塞随后的读模式锁请求。这样可以避免读模式锁长期占用，而等待的写模式锁请求一直得不到满足。</p>
<p>读写锁非常适合于对数据结构读的次数远大于写的情况。当读写锁在写模式下时，它所保护的数据结构就可以被安全地修改，因为一次只有一个线程可以在写模式下拥有这个锁。当读写锁在读模式下时，只要线程先获取了读模式下的读写锁，该锁所保护的数据结构就可以被多个获得读模式锁的线程读取。</p>
<p>读写锁也叫做共享互斥锁（shared-exclusive
lock）。当读写锁是读模式锁住时，就可以说成是以共享模式锁住的。当它是写模式锁住的时候，就可以说成是以互斥模式锁住的。</p>
<p>与互斥量一样，读写锁在使用之前必须初始化，在释放它们底层的内存之前必须销毁。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_rwlock_init</span><span class="params">(<span class="type">pthread_rwlock_t</span> *<span class="keyword">restrict</span> rwlock, <span class="type">const</span> <span class="type">pthread_rwlockattr_t</span> *<span class="keyword">restrict</span> attr)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_rwlock_destroy</span><span class="params">(<span class="type">pthread_rwlock_t</span> *rwlock)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>Single UNIX Specification在XSI扩展中定义了
<code>PTHREAD_RWLOCK_INITIALIZER</code>
常量。如果默认属性就足够的话，可以用它对静态分配的读写锁进行初始化。</p>
<p>在释放读写锁占用的内存之前，需要调用
<code>pthread_rwlock_destroy</code> 做清理工作。如果
<code>pthread_rwlock_init</code>
为读写锁分配了资源，<code>pthread_rwlock_destroy</code>
将释放这些资源。如果在调用 <code>pthread_rwlock_destroy</code>
之前就释放了读写锁占用的内存，那么分配给这个锁的资源就会丢失。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_rwlock_rdlock</span><span class="params">(<span class="type">pthread_rwlock_t</span> *rwlock)</span>; <span class="comment">// 要在读模式下锁定读写锁，需要调用pthread_rwlock_rdlock</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_rwlock_wrlock</span><span class="params">(<span class="type">pthread_rwlock_t</span> *rwlock)</span>; <span class="comment">// 要在写模式下锁定读写锁，需要调用pthread_rwlock_wrlock</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_rwlock_unlock</span><span class="params">(<span class="type">pthread_rwlock_t</span> *rwlock)</span>; <span class="comment">// 不管以何种方式锁住读写锁，都可以调用pthread_rwlock_unlock进行解锁</span></span><br><span class="line"><span class="comment">// 所有函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>Single UNIX Specification还定义了读写锁原语的条件版本。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_rwlock_tryrdlock</span><span class="params">(<span class="type">pthread_rwlock_t</span> *rwlock)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_rwlock_trywrlock</span><span class="params">(<span class="type">pthread_rwlock_t</span> *rwlock)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>可以获取锁时，这两个函数返回0。否则，它们返回错误
<code>EBUSY</code>。</p>
<h3 id="带有超时的读写锁">带有超时的读写锁</h3>
<p>与互斥量一样，Single UNIX
Specification提供了带有超时的读写锁加锁函数，使应用程序在获取读写锁时避免陷入永久阻塞状态。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_rwlock_timedrdlock</span><span class="params">(<span class="type">pthread_rwlock_t</span> *<span class="keyword">restrict</span> rwlock, <span class="type">const</span> <span class="keyword">struct</span> timespec *<span class="keyword">restrict</span> tsptr)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_rwlock_timedwrlock</span><span class="params">(<span class="type">pthread_rwlock_t</span> *<span class="keyword">restrict</span> rwlock, <span class="type">const</span> <span class="keyword">struct</span> timespec *<span class="keyword">restrict</span> tsptr)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>这两个函数的行为与它们“不计时的”版本类似。<code>tsptr</code> 参数指向
<code>timespec</code>
结构，指定线程应该停止阻塞的时间。如果它们不能获取锁，那么超时到期时，这两个函数将返回
<code>ETIMEDOUT</code> 错误。与 <code>pthread_mutex_timedlock</code>
函数类似，超时指定的是绝对时间，而不是相对时间。</p>
<h3 id="条件变量">条件变量</h3>
<p>条件变量是线程可用的另一种同步机制。条件变量给多个线程提供了一个会合的场所。条件变量与互斥量一起使用时，允许线程以无竞争的方式等待特定的条件发生。</p>
<p>条件本身受互斥锁保护。线程必须先锁定互斥锁才能更改条件状态。其他线程在获取互斥锁之前不会察觉到这种变化，因为必须锁定互斥锁才能评估该条件。</p>
<p>在使用条件变量之前，必须先对它进行初始化。由
<code>pthread_cond_t</code>
数据类型表示的条件变量可以用两种方式进行初始化，可以把常量
<code>PTHREAD_COND_INITIALIZER</code>
赋给静态分配的条件变量，但是如果条件变量是动态分配的，则需要使用
<code>pthread_cond_init</code> 函数对它进行初始化。</p>
<p>在释放条件变量底层的内存空间之前，可以使用
<code>pthread_cond_destroy</code>
函数对条件变量进行反初始化（deinitialize）。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_cond_init</span><span class="params">(<span class="type">pthread_cond_t</span> *<span class="keyword">restrict</span> cond, <span class="type">const</span> <span class="type">pthread_condattr_t</span> *<span class="keyword">restrict</span> attr)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_cond_destroy</span><span class="params">(<span class="type">pthread_cond_t</span> *cond)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>使用 <code>pthread_cond_wait</code>
来等待某个条件变为真。还提供了一个变体，如果在指定时间内该条件未得到满足，它会返回一个错误代码。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_cond_wait</span><span class="params">(<span class="type">pthread_cond_t</span> *<span class="keyword">restrict</span> cond, <span class="type">pthread_mutex_t</span> *<span class="keyword">restrict</span> mutex)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_cond_timedwait</span><span class="params">(<span class="type">pthread_cond_t</span> *<span class="keyword">restrict</span> cond, <span class="type">pthread_mutex_t</span> *<span class="keyword">restrict</span> mutex, <span class="type">const</span> <span class="keyword">struct</span> timespec *<span class="keyword">restrict</span> tsptr)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>传递给 <code>pthread_cond_wait</code>
的互斥量对条件进行保护。调用者把锁住的互斥量传给函数，函数然后自动把调用线程放到等待条件的线程列表上，对互斥量解锁。这就关闭了条件检查和线程进入休眠状态等待条件改变这两个操作之间的时间通道，这样线程就不会错过条件的任何变化。<code>pthread_cond_wait</code>
返回时，互斥量再次被锁住。</p>
<p><code>pthread_cond_timedwait</code> 函数的功能与
<code>pthread_cond_wait</code>
函数相似，只是多了一个超时。超时值指定了我们愿意等待多长时间。这个时间值是一个绝对数而不是相对数。例如，假设愿意等待3分钟。那么，并不是把3分钟转换成
<code>timespec</code> 结构，而是需要把当前时间加上3分钟再转换成
<code>timespec</code> 结构。</p>
<p>可以使用 <code>clock_gettime</code> 函数获取 <code>timespec</code>
结构表示的当前时间。但是目前并不是所有的平台都支持这个函数，因此，也可以用另一个函数
<code>gettimeofday</code> 获取 <code>timeval</code>
结构表示的当前时间，然后把这个时间转换成 <code>timespec</code>
结构。要得到超时值的绝对时间，可以使用下面的函数：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">maketimeout</span><span class="params">(<span class="keyword">struct</span> timespec *tsp, <span class="type">long</span> minutes)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">timeval</span> <span class="title">now</span>;</span></span><br><span class="line">    <span class="comment">/* get the current time */</span></span><br><span class="line">    gettimeofday(&amp;now, <span class="literal">NULL</span>);</span><br><span class="line">    tsp-&gt;tv_sec = now.tv_sec;</span><br><span class="line">    tsp-&gt;tv_nsec = now.tv_usec * <span class="number">1000</span>; <span class="comment">/* usec to nsec */</span></span><br><span class="line">    <span class="comment">/* add the offset to get timeout value */</span></span><br><span class="line">    tsp-&gt;tv_sec += minutes * <span class="number">60</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果超时到期时条件还是没有出现，<code>pthread_cond_timewait</code>
将重新获取互斥量，然后返回错误 <code>ETIMEDOUT</code>。从
<code>pthread_cond_wait</code> 或者 <code>pthread_cond_timedwait</code>
调用成功返回时，线程需要重新计算条件，因为另一个线程可能已经在运行并改变了条件。</p>
<p>有两个函数可以用于通知线程条件已经满足。<code>pthread_cond_signal</code>
函数<strong>至少能唤醒一个</strong>等待该条件的线程，而
<code>pthread_cond_broadcast</code>
函数则能唤醒等待该条件的所有线程。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_cond_signal</span><span class="params">(<span class="type">pthread_cond_t</span> *cond)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_cond_broadcast</span><span class="params">(<span class="type">pthread_cond_t</span> *cond)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>在调用 <code>pthread_cond_signal</code> 或者
<code>pthread_cond_broadcast</code>
时，我们说这是在给线程或者条件发信号。必须注意，一定要在改变条件状态以后再给线程发信号。</p>
<h3 id="自旋锁">自旋锁</h3>
<p>自旋锁与互斥量类似，但它不是通过休眠使进程阻塞，而是在获取锁之前一直处于忙等（自旋）阻塞状态。自旋锁可用于以下情况：锁被持有的时间短，而且线程并不希望在重新调度上花费太多的成本。</p>
<p>自旋锁通常作为底层原语用于实现其他类型的锁。</p>
<p>自旋锁的接口与互斥量的接口类似，这使得它可以比较容易地从一个替换为另一个。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_spin_init</span><span class="params">(<span class="type">pthread_spinlock_t</span> *lock, <span class="type">int</span> pshared)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_spin_destroy</span><span class="params">(<span class="type">pthread_spinlock_t</span> *lock)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p><code>pshared</code>
参数表示进程共享属性，表明自旋锁是如何获取的。如果将其设置为
<code>PTHREAD_PROCESS_SHARED</code>，那么只要线程能够访问该自旋锁的底层内存，即使这些线程来自不同的进程，也可以获取该自旋锁。否则，<code>pshared</code>
参数会被设置为
<code>PTHREAD_PROCESS_PRIVATE</code>，此时该自旋锁只能由初始化它的进程内的线程访问。</p>
<p>可以用 <code>pthread_spin_lock</code> 或
<code>pthread_spin_trylock</code>
对自旋锁进行加锁，前者在获取锁之前一直自旋，后者如果不能获取锁，就立即返回
<code>EBUSY</code> 错误。注意， <code>pthread_spin_trylock</code>
不能自旋。不管以何种方式加锁，自旋锁都可以调用
<code>pthread_spin_unlock</code> 函数解锁。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_spin_lock</span><span class="params">(<span class="type">pthread_spinlock_t</span> *lock)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_spin_trylock</span><span class="params">(<span class="type">pthread_spinlock_t</span> *lock)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_spin_unlock</span><span class="params">(<span class="type">pthread_spinlock_t</span> *lock)</span>;</span><br><span class="line"><span class="comment">// 所有函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>注意，如果自旋锁当前在解锁状态的话，<code>pthread_spin_lock</code>
函数不要自旋就可以对它加锁。如果线程已经对它加锁了，结果就是未定义的。调用<code>pthread_spin_lock</code>
会返回 <code>EDEADLK</code>
错误（或其他错误），或者调用可能会永久自旋。具体行为依赖于实际的实现。试图对没有加锁的自旋锁进行解锁，结果也是未定义的。</p>
<p>不管是 <code>pthread_spin_lock</code>还是
<code>pthread_spin_trylock</code>，返回值为0的话就表示自旋锁被加锁。需要注意，不要调用在持有自旋锁情况下可能会进入休眠状态的函数。如果调用了这些函数，会浪费CPU资源，因为其他线程需要获取自旋锁需要等待的时间就延长了。</p>
<h3 id="屏障">屏障</h3>
<p>屏障（barrier）是用户协调多个线程并行工作的同步机制。屏障允许每个线程等待，直到所有的合作线程都到达某一点，然后从该点继续执行。我们已经见过一种形式的屏障——
<code>pthread_join</code>
函数就起到了屏障的作用，它能让一个线程等待另一个线程退出。</p>
<p>但是屏障对象的概念更广，它们允许任意数量的线程等待，直到所有的线程完成处理工作，而线程不需要退出。所有线程达到屏障后可以接着工作。</p>
<p>可以使用 <code>pthread_barrier_init</code> 函数对屏障进行初始化，用
<code>thread_barrier_destroy</code> 函数反初始化。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_barrier_init</span><span class="params">(<span class="type">pthread_barrier_t</span> *<span class="keyword">restrict</span> barrier, <span class="type">const</span> <span class="type">pthread_barrierattr_t</span> *<span class="keyword">restrict</span> attr, <span class="type">unsigned</span> <span class="type">int</span> count)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_barrier_destroy</span><span class="params">(<span class="type">pthread_barrier_t</span> *barrier)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>初始化屏障时，可以使用 <code>count</code>
参数指定，在允许所有线程继续运行之前，必须到达屏障的线程数目。使用
<code>attr</code> 参数指定屏障对象的属性。</p>
<p>可以使用 <code>pthread_barrier_wait</code>
函数来表明，线程已完成工作，准备等所有其他线程赶上来。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_barrier_wait</span><span class="params">(<span class="type">pthread_barrier_t</span> *barrier)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0或者PTHREAD_BARRIER_SERIAL_THREAD；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>调用 <code>pthread_barrier_wait</code> 的线程在屏障计数（调用
<code>pthread_barrier_init</code>
时设定）未满足条件时，会进入休眠状态。如果该线程是最后一个调用
<code>pthread_barrier_wait</code>
的线程，就满足了屏障计数，所有的线程都被唤醒。</p>
<p><code>pthread_barrier_wait()</code> 会随机选择一个线程返回
<code>PTHREAD_BARRIER_SERIAL_THREAD</code>，其余线程返回
0，这样可以让一个线程作为 master 在所有线程同步后执行必要的串行操作</p>
<p>一旦达到屏障计数值，而且线程处于非阻塞状态，屏障就可以被重用。但是除非在调用了
<code>pthread_barrier_destroy</code> 函数之后，又调用了
<code>pthread_barrier_init</code>
函数对计数用另外的数进行初始化，否则屏障计数不会改变。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Interprocess Communication（十五）</title>
    <url>/2025/08/21/notes-reading/computer/apue/apue-15-interprocess-communication/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第15章 Interprocess Communication
的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="introduction">Introduction</h2>
<p>下图列出了4种实现所支持的不同形式的（InterProcess
Communication，IPC）。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260310194515513.png" alt="Summary of UNIX System IPC" style="zoom: 80%;" /></p>
<p>使用“（full）”来表示通过使用全双工管道支持半双工管道的实现。</p>
<p>前10种IPC形式通常限于同一台主机的两个进程之间的IPC。最后两行（套接字和STREAMS）是仅有的支持不同主机上两个进程之间IPC的两种形式。</p>
<hr />
<h2 id="pipes">Pipes</h2>
<p>管道是UNIX系统IPC的最古老形式，所有UNIX系统都提供此种通信机制。管道有以下两种局限性。</p>
<p>（1）历史上，它们是半双工的（数据只能在一个方向上流动）。现在，某些系统提供全双工管道，但是为了最佳可移植性，决不应预先假定系统支持全双工管道。</p>
<p>（2）管道只能在具有公共祖先的两个进程之间使用。通常，一个管道由一个进程创建，在进程调用fork之后，这个管道就能在父进程和子进程之间使用了（不一定非要是父子，只要公共祖先创建管道，后续子孙进程继承也可以）。</p>
<p>FIFO没有第二种局限性，UNIX域套接字没有这两种局限性。</p>
<p>尽管有这两种局限性，半双工管道仍是最常用的IPC形式。每当在管道中键入命令序列，让
shell 执行时，shell
都会为每一条命令单独创建一个进程，然后用管道将前一条命令进程的标准输出与后一条命令的标准输入相连接。</p>
<p>管道是通过调用 <code>pipe</code> 函数创建的。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pipe</span><span class="params">(<span class="type">int</span> fd[<span class="number">2</span>])</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0，若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>经由参数 <code>fd</code>
返回两个文件描述符：<strong>fd[0]为读而打开，fd[1]为写而打开。fd[1]的输出是fd[0]的输入。</strong></p>
<p>POSIX.1允许实现支持全双工管道。对于这些实现，fd[0]和fd[1]可以以读/写方式打开。</p>
<p>下图中给出了两种描绘半双工管道的方法。图的左半部分显示了管道的两端在一个单一进程中相连。图的右半部分则着重表明，管道中的数据是通过内核流动的。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260310202157146.png" alt="Two ways to view a half-duplex pipe" style="zoom:67%;" /></p>
<p><code>fstat</code>
函数对管道的每一端都返回一个FIFO类型的文件描述符。可以用
<code>S_ISFIFO</code> 宏来测试管道。</p>
<p>单个进程中的管道几乎没有任何用处。通常，进程会先调用
<code>pipe</code>，接着调用
<code>fork</code>，从而创建从父进程到子进程的IPC通道，反之亦然。下图显示了这种情况。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260311181006967.png" alt="Half-duplex pipe after a fork" style="zoom:67%;" /></p>
<p>fork
之后做什么取决于我们想要的数据流的方向。对于从<strong>父进程到子进程的管道</strong>，父进程关闭管道的读端（fd[0]），子进程关闭写端（fd[1]）。下图显示了在此之后描述符的状态结果。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260311181105493.png" alt="Pipe from parent to child" style="zoom:67%;" /></p>
<p>对于一个从子进程到父进程的管道，父进程关闭fd[1]，子进程关闭fd[0]。</p>
<p>当管道的一端被关闭后，下列两条规则起作用。</p>
<p>（1）当读（read）一个写端已被关闭的管道时，在所有数据都被读取后，<code>read</code>
返回0，表示文件结束。</p>
<p>（2）如果写（write）一个读端已被关闭的管道，则产生信号
<code>SIGPIPE</code>。如果忽略该信号或者捕捉该信号并从其处理程序返回，则
<code>write</code> 返回−1，<code>errno</code>
设置为<code>EPIPE</code>。</p>
<p>在写管道（或 FIFO）时，常量 <code>PIPE_BUF</code>
规定了内核的管道缓冲区大小。如果对管道调用
<code>write</code>，而且要求写的字节数小于等于
<code>PIPE_BUF</code>，则此操作不会与其他进程对同一管道（或FIFO）的
<code>write</code> 操作交叉进行。但是，若有多个进程同时写一个管道（或
FIFO），而且我们要求写的字节数超过
<code>PIPE_BUF</code>，那么我们所写的数据可能会与其他进程所写的数据相互交叉。用
<code>pathconf</code> 或 <code>fpathconf</code> 函数可以确定
<code>PIPE_BUF</code> 的值。</p>
<hr />
<h2 id="函数popen和pclose">函数popen和pclose</h2>
<p>常见的操作是创建一个连接到另一个进程的管道，然后读其输出或向其输入端发送数据，为此，标准I/O库提供了两个函数
<code>popen</code> 和
<code>pclose</code>。这两个函数实现的操作是：创建一个管道，
fork一个子进程，关闭未使用的管道端，执行一个shell运行命令，然后等待命令终止。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line">FILE *<span class="title function_">popen</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *cmdstring, <span class="type">const</span> <span class="type">char</span> *type)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回文件指针；若出错，返回NULL</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">pclose</span><span class="params">(FILE *fp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回cmdstring的终止状态；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p><code>popen</code> 先执行 <code>fork</code>，然后调用
<code>exec</code> 执行
<code>cmdstring</code>，并且返回一个标准I/O文件指针。若
<code>type</code>是 <code>r</code>，则文件指针连接到
<code>cmdstring</code> 的标准输出（见下图）。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260312171535278.png"
alt="popen r" />
<figcaption aria-hidden="true">popen r</figcaption>
</figure>
<p>如果 <code>type</code> 是 <code>w</code>，则文件指针连接到
<code>cmdstring</code> 的标准输入，如图所示。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260312171647129.png"
alt="pread w" />
<figcaption aria-hidden="true">pread w</figcaption>
</figure>
<p>有一种方法可以帮助记住 <code>popen</code>
的最后一个参数及其作用，这就是与 <code>fopen</code> 进行类比。如果
<code>type</code> 是”r”，则返回的文件指针是可读的，如果
<code>type</code> 是”w”，则是可写的。</p>
<p><code>pclose</code>
函数关闭标准I/O流，等待命令终止，然后返回shell的终止状态。如果 shell
不能被执行，则 <code>pclose</code> 返回的终止状态与shell已执行
<code>exit(127)</code> 一样。</p>
<hr />
<h2 id="协程">协程</h2>
<p>UNIX系统过滤器是一个从标准输入读取数据并向标准输出写入数据的程序。过滤器通常在线性连接的shell管道中使用。当同一个程序既生成过滤器的输入又读取过滤器的输出时，该过滤器就成了协同进程。</p>
<p>协同进程通常在 shell
的后台运行，其标准输入和标准输出通过管道连接到另一个程序。尽管启动协同进程并将其输入和输出连接到其他进程所需的
shell 语法相当复杂，但协同进程在 C 程序中也很有用。</p>
<p><code>popen</code>
提供了一条通向另一个进程的标准输入或来自其标准输出的单向管道，而对于协同进程，我们有两条通向该进程的单向管道：一条通向其标准输入，另一条来自其标准输出。我们希望向其标准输入写入数据，让它对数据进行处理，然后从其标准输出读取数据。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260312191747195.png"
alt="Driving a coprocess by writing its standard input and reading its standard output" />
<figcaption aria-hidden="true">Driving a coprocess by writing its
standard input and reading its standard output</figcaption>
</figure>
<hr />
<h2 id="fifos">FIFOs</h2>
<p>FIFO有时被称为命名管道。无名管道仅能在有共同祖先创建了该管道的相关进程之间使用。然而，借助FIFO，不相关的进程也可以交换数据。</p>
<p>FIFO是一种文件类型。通过 <code>stat</code> 结构的
<code>st_mode</code> 成员的编码可以知道文件是否是FIFO类型。可以用
<code>S_ISFIFO</code> 宏对此进行测试。</p>
<p>创建FIFO类似于创建文件。事实上，FIFO的路径名存在于文件系统中。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/stat.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">mkfifo</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">mode_t</span> mode)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">mkfifoat</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">char</span> *path, <span class="type">mode_t</span> mode)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>一旦使用 <code>mkfifo</code> 或 <code>mkfifoat</code>
创建了一个FIFO，就可以使用 <code>open</code>
来打开它。实际上，常规的文件I/O函数（例如
<code>close</code>、<code>read</code>、<code>write</code>、<code>unlink</code>）都可用于FIFO。</p>
<p>当 <code>open</code>
一个FIFO时，非阻塞标志（<code>O_NONBLOCK</code>）会产生下列影响。</p>
<blockquote>
<ul>
<li><p>在一般情况下（没有指定
<code>O_NONBLOCK</code>），只读open要阻塞到某个其他进程为写而打开这个FIFO为止。类似地，只写open要阻塞到某个其他进程为读而打开它为止。</p></li>
<li><p>如果指定了 <code>O_NONBLOCK</code>，则只读
<code>open</code>立即返回。但是，如果没有进程为读而打开，那么只写open将返回−1，并将
<code>errno</code> 设置成<code>ENXIO</code>。</p></li>
</ul>
</blockquote>
<p>类似于管道，若 write 一个尚无进程为读而打开的FIFO，则产生信号
<code>SIGPIPE</code>。当一个FIFO的最后一个写入者关闭该FIFO时，会为该FIFO的读取者生成一个文件结束标志。</p>
<p>一个给定的 FIFO
有多个写进程是常见的。这就意味着，如果不希望多个进程所写的数据交叉，则必须考虑原子写操作。和管道一样，常量
<code>PIPE_BUF</code> 说明了可被原子地写到FIFO的最大数据量。</p>
<p>FIFO有以下两种用途。</p>
<blockquote>
<p>（1）FIFO 被 shell 命令用来将数据从一个 shell
管道传递到另一个，而无需创建中间临时文件。</p>
<p>（2）在客户机-服务器应用程序中，FIFO被用作会合点，以便在客户机和服务器之间传递数据。</p>
</blockquote>
<p>各用一个实例来说明这两种用途。</p>
<h3 id="实例用fifo复制输出流">实例：用FIFO复制输出流</h3>
<p>FIFO可用于复制一系列sell命令中的输出流。这就防止了将数据写向中间磁盘文件（类似于使用管道来避免中间磁盘文件）。但是不同的是，管道只能用于两个进程之间的线性连接，而<strong>FIFO是有名字的，因此它可用于非线性连接</strong>。</p>
<p>考虑这样一个过程，它需要对一个经过过滤的输入流进行两次处理。下图显示了这种安排。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260312201230479.png" alt="Procedure that processes a filtered input stream twice" style="zoom:67%;" /></p>
<p>使用FIFO和UNIX程序 <code>tee(1)</code>
就可以实现这样的过程而无需使用临时文件。（<code>tee</code>
程序将其标准输入同时复制到其标准输出以及其命令行中命名的文件中。）</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mkfifo fifo1</span><br><span class="line">prog3 &lt; fifo1 &amp;</span><br><span class="line">prog1 &lt; infile | tee fifo1 | prog2</span><br></pre></td></tr></table></figure>
<p>创建FIFO，然后在后台启动prog3，从FIFO读数据。然后启动prog1，用tee将其输出发送到FIFO和prog2。下图显示了进程安排。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260312201908119.png" alt="Using a FIFO and tee to send a stream to two different processes" style="zoom:67%;" /></p>
<h3
id="实例使用fifo进行客户进程-服务器进程通信">实例：使用FIFO进行客户进程-服务器进程通信</h3>
<p>如果有一个服务器进程，它与很多客户进程连接，每个客户进程都可将其请求写到一个该服务器进程创建的众所周知的FIFO中。下图显示了这种安排。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260312204310928.png" alt="Clients sending requests to a server using a FIFO" style="zoom:50%;" /></p>
<p>因为该 FIFO
有多个写进程，所以客户进程发送给服务器进程的请求的长度要小于
<code>PIPE_BUF</code> 字节。这样就能避免客户进程的多次写之间的交叉。</p>
<p>在这种类型的客户进程-服务器进程通信中使用FIFO的问题是：服务器进程如何将回答送回各个客户进程。不能使用单个FIFO，因为客户端永远无法知道何时读取自己的响应，何时读取其他客户端的响应。一种解决方法是，每个客户进程都在其请求中包含它的进程ID。然后服务器进程为每个客户进程创建一个FIFO，所使用的路径名基于客户进程的进程ID。例如，服务器进程可以用名字
<code>/tmp/serv1.XXXXX</code> 创建FIFO，其中 <code>XXXXX</code>
被替换成客户进程的进程ID。下图显示了这种安排。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260312204403792.png" alt="Client–server communication using FIFOs" style="zoom:50%;" /></p>
<p>虽然这种安排可以工作，但服务器进程不能判断一个客户进程是否崩溃终止，这就使得客户进程专用FIFO会遗留在文件系统中。另外，服务器进程还必须得捕捉
<code>SIGPIPE</code>
信号，因为客户进程在发送一个请求后有可能没有读取响应就终止了，于是留下一个只有写进程（服务器进程）而无读进程的客户进程专用FIFO。</p>
<p>按照图中的安排，如果服务器进程以只读方式打开众所周知的FIFO（因为它只需读该FIFO），则每当客户进程个数从1变成0时，服务器进程就将在FIFO中读到（read）一个文件结束标志。为使服务器进程免于处理这种情况，一种常用的技巧是使服务器进程以读-写方式打开该众所周知的FIFO。</p>
<hr />
<h2 id="xsi-ipc">XSI IPC</h2>
<p>有3种称作XSI
IPC的IPC：<strong>消息队列、信号量以及共享内存</strong>。它们之间有很多相似之处。本节先介绍它们相类似的特征，后面几节将说明这些IPC各自的特殊功能。</p>
<h3 id="标识符和键">标识符和键</h3>
<p>每个内核中的 IPC
结构（消息队列、信号量或共享存储段）都用一个非负整数的标识符（identifier）加以引用。例如，要向一个消息队列发送消息或者从一个消息队列取消息，只需要知道其队列标识符。与文件描述符不同，IPC标识符不是小的整数。当一个IPC结构被创建，然后又被删除时，与这种结构相关的标识符连续加1，直至达到一个整型数的最大正值，然后又回转到0。</p>
<p>标识符是IPC对象的内部名。协作进程需要一种外部命名机制，以便能够使用相同的IPC对象进行会合。为此，每个IPC对象都与一个键（key）相关联，将这个键作为该对象的外部名。</p>
<p>无论何时创建IPC结构（通过调用<code>msgget</code>、<code>semget</code>
或 <code>shmget</code>
创建），都应指定一个键。这个键的数据类型是基本系统数据类型
<code>key_t</code>，通常在头文件 <code>&lt;sys/types.h&gt;</code>
中被定义为长整型。这个键由内核转换为标识符。</p>
<p>有多种方法使客户进程和服务器进程在同一IPC结构上汇聚。</p>
<p>（1）服务器进程可以指定键 <code>IPC_PRIVATE</code>
创建一个新IPC结构，将返回的标识符存放在某处（如一个文件）以便客户进程取用。键
<code>IPC_PRIVATE</code>
保证服务器进程创建一个新IPC结构。这种技术的缺点是：文件系统操作需要服务器进程将整型标识符写到文件中，此后客户进程又要读这个文件取得此标识符。</p>
<p><code>IPC_PRIVATE</code> 键也可用于父子进程关系。父进程指定
<code>IPC_PRIVATE</code> 创建一个新IPC结构，所返回的标识符可供
<code>fork</code> 后的子进程使用。接着，子进程又可将此标识符作为
<code>exec</code> 函数的一个参数传给一个新程序。</p>
<p>（2）可以在一个公用头文件中定义一个客户进程和服务器进程都认可的键。然后服务器进程指定此键创建一个新的IPC结构。这种方法的问题是该键可能已与一个IPC结构相结合，在此情况下，get函数（msgget、semget或shmget）出错返回。服务器进程必须处理这一错误，删除已存在的IPC结构，然后试着再创建它。</p>
<p>（3）客户进程和服务器进程认同一个路径名和项目ID（项目ID是0～255之间的字符值），接着，调用函数
<code>ftok</code>
将这两个值变换为一个键。然后在方法（2）中使用此键。<code>ftok</code>
提供的唯一服务就是由一个路径名和项目ID产生一个键。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/ipc.h&gt;</span></span></span><br><span class="line"><span class="type">key_t</span> <span class="title function_">ftok</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">int</span> id)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回键；若出错，返回(key_t)−1</span></span><br></pre></td></tr></table></figure>
<p><code>path</code>
参数必须引用一个现有的文件。生成key时，仅使用id的低8位。</p>
<p>3个get函数（<code>msgget</code>、<code>semget</code> 和
<code>shmget</code> ）都有两个类似的参数：一个 <code>key</code>
和一个整型 <code>flag</code>。如果键是
<code>IPC_PRIVATE</code>，或者键当前未与特定类型的IPC结构相关联且指定了标志的
<code>IPC_CREAT</code>
位，就会创建一个新的IPC结构（通常由服务器创建）。要引用一个已存在的队列（通常由客户端执行），<code>key</code>
必须与创建队列时指定的 <code>key</code> 相同，且不得指定
<code>IPC_CREAT</code>。</p>
<p>注意，决不能指定 <code>IPC_PRIVATE</code>
作为键来引用一个现有队列，因为这个特殊的键值总是用于创建一个新队列。为了引用一个用
<code>IPC_PRIVATE</code>
键创建的现有队列，一定要知道这个相关的标识符，然后在其他IPC调用中（如
<code>msgsnd</code>、<code>msgrcv</code>）使用该标识符，这样可以绕过get函数。</p>
<p>如果我们想要创建一个新的IPC结构，且确保不会引用到具有相同标识符的现有结构，就必须指定一个同时设置了
<code>IPC_CREAT</code> 和 <code>IPC_EXCL</code> 位的
<code>flag</code>。这样一来，如果该IPC结构已经存在，就会返回
<code>EEXIST</code> 错误。（这类似于指定了 <code>O_CREAT</code> 和
<code>O_EXCL</code> 标志的 <code>open</code> 操作。）</p>
<hr />
<h3 id="权限结构">权限结构</h3>
<p>XSI IPC为每一个IPC结构关联了一个 <code>ipc_perm</code>
结构。该结构规定了权限和所有者，它至少包括下列成员：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ipc_perm</span> &#123;</span></span><br><span class="line">    <span class="type">uid_t</span> uid; <span class="comment">/* owner&#x27;s effective user id */</span></span><br><span class="line">    <span class="type">gid_t</span> gid; <span class="comment">/* owner&#x27;s effective group id */</span></span><br><span class="line">    <span class="type">uid_t</span> cuid; <span class="comment">/* creator&#x27;s effective user id */</span></span><br><span class="line">    <span class="type">gid_t</span> cgid; <span class="comment">/* creator&#x27;s effective group id */</span></span><br><span class="line">    <span class="type">mode_t</span> mode; <span class="comment">/* access modes */</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>每个实现都包含额外的成员。有关完整定义，请查看系统上的
<code>&lt;sys/ipc.h&gt;</code>。</p>
<p>在创建IPC结构时，对所有字段都赋初值。以后，可以调用
<code>msgctl</code>、<code>semctl</code> 或 <code>shmctl</code> 修改
<code>uid</code>、<code>gid</code> 和 <code>mode</code>
字段。为了修改这些值，调用进程必须是IPC结构的创建者或超级用户。修改这些字段类似于对文件调用
<code>chown</code> 和 <code>chmod</code>。</p>
<p>对于任何IPC结构 <code>mode</code>
字段的值都不存在执行权限。另外，消息队列和共享内存使用术语“读”和“写”，而信号量则用术语“读”和“更改”（alter）。下表显示了每种IPC的6种权限。</p>
<table>
<thead>
<tr class="header">
<th>Permission</th>
<th>Bit</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>user-read</td>
<td>0400</td>
</tr>
<tr class="even">
<td>user-write (alter)</td>
<td>0200</td>
</tr>
<tr class="odd">
<td>group-read</td>
<td>0040</td>
</tr>
<tr class="even">
<td>group-write (alter)</td>
<td>0020</td>
</tr>
<tr class="odd">
<td>other-read</td>
<td>0004</td>
</tr>
<tr class="even">
<td>other-write (alter)</td>
<td>0002</td>
</tr>
</tbody>
</table>
<hr />
<h3 id="配置限制">配置限制</h3>
<p>所有3种形式的XSI
IPC都有内置限制。大多数限制可以通过重新配置内核来改变。在对这3种形式的IPC中的每一种进行描述时，我们都会指出它的限制。</p>
<p>在报告和修改限制方面，每种平台都有自己的方法。FreeBSD 8.0、Linux
3.2.0和Mac OS X 10.6.8提供了 <code>sysctl</code>
命令来观察和修改内核配置参数。在Solaris 10中，可以用 <code>prctl</code>
命令来改变内核IPC的限制。</p>
<p>在Linux中，可以运行 <code>ipcs –l</code>
来显示IPC相关的限制。在FreeBSD和Mac中，等效的命令是
<code>ipcs -T</code>。在Solaris中，可以通过运行 <code>sysdef –i</code>
来找到可调节参数。</p>
<hr />
<h3 id="优点和缺点">优点和缺点</h3>
<p>XSI
IPC的一个基本问题是，IPC结构是系统级的，并且没有引用计数。例如，如果我们创建一个消息队列，在队列中放置一些消息，然后终止进程，该消息队列及其内容并不会被删除。它们会一直存在于系统中，直到某个进程调用
<code>msgrcv</code> 或 <code>msgctl</code>
来专门读取或删除它们，或者有人执行 <code>ipcrm(1)</code>
命令，又或者系统被重启。将其与管道进行比较，管道会在最后一个引用它的进程终止时被完全移除。对于FIFO（命名管道），尽管其名称会保留在文件系统中，直到被显式删除，但当最后一个引用FIFO的进程终止时，FIFO中遗留的任何数据都会被移除。</p>
<p>XSI
IPC的另一个问题是，这些IPC结构在文件系统中没有名称标识。我们无法使用在第3章和第4章中介绍的函数来访问它们并修改其属性。内核中新增了近十几个系统调用（如
<code>msgget</code>、<code>semop</code>、<code>shmat</code>等）来支持这些IPC对象。我们不能用
<code>ls</code> 命令查看这些IPC对象，不能用 <code>rm</code>
命令删除它们，也不能用 <code>chmod</code>
命令更改它们的权限。相反，新增了两个命令 <code>ipcs(1)</code>和
<code>ipcrm(1)</code> 来完成这些操作。</p>
<p>由于这些IPC形式不使用文件描述符，所以无法将多路复用I/O函数（
<code>select</code> 和 <code>poll</code>
）与它们一同使用。这使得同时使用多个此类IPC结构，或者将任何此类IPC结构与文件I/O或设备I/O结合使用变得更加困难。例如，我们无法让服务器在不借助某种形式的忙等循环的情况下，等待消息被放入两个消息队列中的任意一个。</p>
<p>下表比较了这些不同形式的进程间通信（IPC）的部分特性。</p>
<table>
<colgroup>
<col style="width: 26%" />
<col style="width: 15%" />
<col style="width: 9%" />
<col style="width: 13%" />
<col style="width: 8%" />
<col style="width: 28%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">IPC type</th>
<th style="text-align: center;">Connectionless?</th>
<th style="text-align: center;">Reliable?</th>
<th style="text-align: center;">Flow control?</th>
<th style="text-align: center;">Records?</th>
<th style="text-align: center;">Message types or priorities?</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;">message queues</td>
<td style="text-align: center;">no</td>
<td style="text-align: center;">yes</td>
<td style="text-align: center;">yes</td>
<td style="text-align: center;">yes</td>
<td style="text-align: center;">yes</td>
</tr>
<tr class="even">
<td style="text-align: left;">STREAMS</td>
<td style="text-align: center;">no</td>
<td style="text-align: center;">yes</td>
<td style="text-align: center;">yes</td>
<td style="text-align: center;">yes</td>
<td style="text-align: center;">yes</td>
</tr>
<tr class="odd">
<td style="text-align: left;">UNIX domain stream socket</td>
<td style="text-align: center;">no</td>
<td style="text-align: center;">yes</td>
<td style="text-align: center;">yes</td>
<td style="text-align: center;">no</td>
<td style="text-align: center;">no</td>
</tr>
<tr class="even">
<td style="text-align: left;">UNIX domain datagram socket</td>
<td style="text-align: center;">yes</td>
<td style="text-align: center;">yes</td>
<td style="text-align: center;">no</td>
<td style="text-align: center;">yes</td>
<td style="text-align: center;">no</td>
</tr>
<tr class="odd">
<td style="text-align: left;">FIFOs (non-STREAMS)</td>
<td style="text-align: center;">no</td>
<td style="text-align: center;">yes</td>
<td style="text-align: center;">yes</td>
<td style="text-align: center;">no</td>
<td style="text-align: center;">no</td>
</tr>
</tbody>
</table>
<p>Connectionless“无连接”，是指无需先调用某种形式的打开函数就能发送消息的能力。由于所有这些进程间通信（IPC）形式都局限于单一主机，所以它们都是可靠的。当消息通过网络发送时，消息丢失的可能性就成了一个需要关注的问题。Flow
control“流量控制”指的是，如果系统资源（缓冲区）不足，或者接收方无法再接收更多消息，发送方会进入睡眠状态。当流量控制的条件缓解时（即队列中有空间时），发送方应自动被唤醒。</p>
<p>下面3节将对3种形式的XSI IPC进行详细的描述。</p>
<hr />
<h2 id="消息队列">消息队列</h2>
<p>XSI / System V 消息队列（message queue） 的概念结构图如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">用户进程 A                                   用户进程 B</span><br><span class="line">------------                                 ------------</span><br><span class="line">key_t key = 0x1234                           key_t key = 0x1234</span><br><span class="line">msqid = msgget(key, IPC_CREAT|0666)          msqid = msgget(key, 0)</span><br><span class="line"></span><br><span class="line">msgsnd(msqid, ...)                           msgrcv(msqid, ...)</span><br><span class="line">      |                                            ^</span><br><span class="line">      v                                            |</span><br><span class="line">+------------------------------------------------------------------+</span><br><span class="line">|                              内核                                 |</span><br><span class="line">|                                                                  |</span><br><span class="line">|   System V IPC 对象表 / 消息队列管理区                            |</span><br><span class="line">|                                                                  |</span><br><span class="line">|   根据 key 查找/创建                                              |</span><br><span class="line">|                                                                  |</span><br><span class="line">|   key = 0x1234                                                    |</span><br><span class="line">|      |                                                           |</span><br><span class="line">|      v                                                           |</span><br><span class="line">|   +----------------------------------------------------------+   |</span><br><span class="line">|   | 消息队列对象（message queue object）                     |   |</span><br><span class="line">|   |----------------------------------------------------------|   |</span><br><span class="line">|   | IPC key      : 0x1234                                    |   |</span><br><span class="line">|   | msqid        : 17   &lt;---- 内核返回给用户的消息队列 ID     |   |</span><br><span class="line">|   | uid/gid      : ...                                       |   |</span><br><span class="line">|   | mode/perm    : 0666                                      |   |</span><br><span class="line">|   | qnum         : 当前消息数                                |   |</span><br><span class="line">|   | qbytes       : 队列允许的总字节数                        |   |</span><br><span class="line">|   | lspid        : 最后发送消息的进程 PID                    |   |</span><br><span class="line">|   | lrpid        : 最后接收消息的进程 PID                    |   |</span><br><span class="line">|   | stime        : 最后 msgsnd 时间                          |   |</span><br><span class="line">|   | rtime        : 最后 msgrcv 时间                          |   |</span><br><span class="line">|   | ctime        : 最后变更时间                              |   |</span><br><span class="line">|   |                                                          |   |</span><br><span class="line">|   | 消息链表 / 消息存储区                                     |   |</span><br><span class="line">|   |                                                          |   |</span><br><span class="line">|   |   +-----------------------------+                        |   |</span><br><span class="line">|   |   | message #1                  |                        |   |</span><br><span class="line">|   |   | mtype = 5                   |                        |   |</span><br><span class="line">|   |   | mtext = &quot;hello&quot;             |                        |   |</span><br><span class="line">|   |   +-----------------------------+                        |   |</span><br><span class="line">|   |                  |                                       |   |</span><br><span class="line">|   |                  v                                       |   |</span><br><span class="line">|   |   +-----------------------------+                        |   |</span><br><span class="line">|   |   | message #2                  |                        |   |</span><br><span class="line">|   |   | mtype = 2                   |                        |   |</span><br><span class="line">|   |   | mtext = &quot;world&quot;             |                        |   |</span><br><span class="line">|   |   +-----------------------------+                        |   |</span><br><span class="line">|   |                  |                                       |   |</span><br><span class="line">|   |                 ...                                      |   |</span><br><span class="line">|   +----------------------------------------------------------+   |</span><br><span class="line">|                                                                  |</span><br><span class="line">+------------------------------------------------------------------+</span><br></pre></td></tr></table></figure>
<p>消息队列是消息的链接表，存储在内核中，由消息队列标识符标识。在本节中，把消息队列简称为队列，其<strong>标识符简称为队列ID</strong>。</p>
<p><code>msgget</code>
用于创建一个新队列或打开一个现有队列。<code>msgsnd</code>
将新消息添加到队列尾端。每个消息包含一个正的长整型类型的字段、一个非负的长度以及实际数据字节数（对应于长度），所有这些在消息被添加到队列时都被指定给
<code>msgsnd</code>。<code>msgrcv</code>
用于从队列中取消息。我们并不一定要以先进先出次序取消息，也可以按消息的类型字段取消息。</p>
<p>每个队列都有一个 <code>msqid_ds</code> 结构与其相关联：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">msqid_ds</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ipc_perm</span>　　 <span class="title">msg_perm</span>;</span>　　　　 <span class="comment">/* see Section 15.6.2 */</span></span><br><span class="line">    <span class="type">msgqnum_t</span>　　　　　　msg_qnum;　　　　 <span class="comment">/* # of messages on queue */</span></span><br><span class="line">    <span class="type">msglen_t</span>　　　　　　 msg_qbytes;　　　 <span class="comment">/* max # of bytes on queue */</span></span><br><span class="line">    <span class="type">pid_t</span>　　　　　　　　msg_lspid;　　　　<span class="comment">/* pid of last msgsnd() */</span></span><br><span class="line">    <span class="type">pid_t</span>　　　　　　　　msg_lrpid;　　　　<span class="comment">/* pid of last msgrcv() */</span></span><br><span class="line">    <span class="type">time_t</span>　　　　　　　 msg_stime;　　　　<span class="comment">/* last-msgsnd() time */</span></span><br><span class="line">    <span class="type">time_t</span>　　　　　　　 msg_rtime;　　　　<span class="comment">/* last-msgrcv() time */</span></span><br><span class="line">    <span class="type">time_t</span>　　　　　　　 msg_ctime;　　　　<span class="comment">/* last-change time */</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>此结构定义了队列的当前状态。</p>
<p>调用的第一个函数通常是
<code>msgget</code>，其功能是打开一个现有队列或创建一个新队列。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/msg.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">msgget</span><span class="params">(<span class="type">key_t</span> key, <span class="type">int</span> flag)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回消息队列ID；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p><code>msgctl</code>
函数对队列执行多种操作。它和另外两个与信号量及共享存储有关的函数（<code>semctl</code>
和 <code>shmctl</code>）都是XSI IPC的类似于 <code>ioctl</code>
的函数（亦即垃圾桶函数）。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/msg.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">msgctl</span><span class="params">(<span class="type">int</span> msqid, <span class="type">int</span> cmd, <span class="keyword">struct</span> msqid_ds *buf)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p><code>cmd</code> 参数指定对 <code>msqid</code>
指定的队列要执行的命令。</p>
<p><code>IPC_STAT</code> 取此队列的 <code>msqid_ds</code>
结构，并将它存放在buf指向的结构中。</p>
<p><code>IPC_SET</code> 将字段
<code>msg_perm.uid</code>、<code>msg_perm.gid</code>、<code>msg_perm.mode</code>
和 <code>msg_qbytes</code> 从buf指向的结构复制到与这个队列相关的
<code>msqid_ds</code>
结构中。此命令只能由下列两种进程执行：一种是其有效用户ID等于
<code>msg_perm.cuid</code> 或
<code>msg_perm.uid</code>，另一种是具有超级用户特权的进程。只有超级用户才能增加
<code>msg_qbytes</code> 的值。</p>
<p><code>IPC_RMID</code>
从系统中删除该消息队列以及仍在该队列中的所有数据。这种删除立即生效。仍在使用这一消息队列的其他进程在它们下一次试图对此队列进行操作时，将得到
<code>EIDRM</code>
错误。此命令只能由下列两种进程执行：一种是其有效用户ID等于
<code>msg_perm.cuid</code> 或
<code>msg_perm.uid</code>；另一种是具有超级用户特权的进程。</p>
<p>这3条命令（<code>IPC_STAT</code>、<code>IPC_SET</code> 和
<code>IPC_RMID</code>）也可用于信号量和共享存储。</p>
<p>调用 <code>msgsnd</code> 将数据放到消息队列中。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/msg.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">msgsnd</span><span class="params">(<span class="type">int</span> msqid, <span class="type">const</span> <span class="type">void</span> *ptr, <span class="type">size_t</span> nbytes, <span class="type">int</span> flag)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>每个消息都由3部分组成：一个正的长整型类型的字段、一个非负的长度（nbytes）以及实际数据字节数（对应于长度）。消息总是放在队列尾端。</p>
<p><code>ptr</code>
参数指向一个长整型数，它包含了正的整型消息类型，其后紧接着的是消息数据（若nbytes是0，则无消息数据）。若发送的最长消息是512字节的，则可定义下列结构：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mymesg</span> &#123;</span></span><br><span class="line">    <span class="type">long</span> mtype;　　　 <span class="comment">/* positive message type */</span></span><br><span class="line">    <span class="type">char</span> mtext[<span class="number">512</span>]; <span class="comment">/* message data, of length nbytes */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p><code>ptr</code> 是一个指向 <code>mymesg</code>
结构的指针。接收者可以使用消息类型以非先进先出的次序取消息。</p>
<p>参数 <code>flag</code> 的值可以指定为
<code>IPC_NOWAIT</code>。这类似于文件I/O的非阻塞I/O标志。若消息队列已满（或者是队列中的消息总数等于系统限制值，或队列中的字节总数等于系统限制值），则指定
<code>IPC_NOWAIT</code> 使得 <code>msgsnd</code> 立即出错返回
<code>EAGAIN</code>。如果没有指定
<code>IPC_NOWAIT</code>，则进程会一直阻塞到：有空间可以容纳要发送的消息；或者从系统中删除了此队列；或者捕捉到一个信号，并从信号处理程序返回。在第二种情况下，会返回
<code>EIDRM</code> 错误（“标识符被删除”）。最后一种情况则返回
<code>EINTR</code> 错误。</p>
<p>当 <code>msgsnd</code> 成功返回时，与消息队列相关联的
<code>msqid_ds</code>
结构会被更新，以指明进行该调用的进程ID（<code>msg_lspid</code>）、调用发生的时间（<code>msg_stime</code>），以及队列中又增加了一条消息（<code>msg_qnum</code>）。</p>
<p><code>msgrcv</code> 从队列中取用消息。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/msg.h&gt;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">msgrcv</span><span class="params">(<span class="type">int</span> msqid, <span class="type">void</span> *ptr, <span class="type">size_t</span> nbytes, <span class="type">long</span> type, <span class="type">int</span> flag)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回消息数据部分的长度；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>和 <code>msgsnd</code> 一样， <code>ptr</code>
参数指向一个长整型数（其中存储的是返回的消息类型），其后跟随的是存储实际消息数据的缓冲区。<code>nbytes</code>
指定数据缓冲区的长度。若返回的消息长度大于
<code>nbytes</code>，而且在flag中设置了 <code>MSG_NOERROR</code>
位，则该消息会被截断（在这种情况下，没有通知告诉我们消息截断了，消息被截去的部分被丢弃）。如果没有设置这一标志，而消息又太长，则出错返回
<code>E2BIG</code>（消息仍留在队列中）。</p>
<p>参数 <code>type</code> 可以指定想要哪一种消息。</p>
<p><code>type == 0</code> 返回队列中的第一个消息。</p>
<p><code>type &gt; 0</code> 返回队列中消息类型为 <code>type</code>
的第一个消息。</p>
<p><code>type &lt; 0</code> 返回队列中消息类型的值小于或等于
<code>type</code> 的绝对值且为最小值的第一条消息。</p>
<p>type值非0用于以非先进先出次序读消息。例如，若应用程序对消息赋予优先权，那么
<code>type</code>
就可以是优先权值。如果一个消息队列由多个客户进程和一个服务器进程使用，那么
<code>type</code>
字段可以用来包含客户进程的进程ID（只要进程ID可以存放在长整型中）。</p>
<p>可以将flag值指定为
<code>IPC_NOWAIT</code>，使操作不阻塞，这样，如果没有所指定类型的消息可用，则<code>msgrcv</code>返回−1，<code>errno</code>
设置为
<code>ENOMSG</code>。如果没有指定<code>IPC_NOWAIT</code>，则进程会一直阻塞到有了指定类型的消息可用，或者从系统中删除了此队列（返回−1，<code>error</code>
设置为
<code>EIDRM</code>），或者捕捉到一个信号并从信号处理程序返回（这会导致
<code>msgrcv</code> 返回−1，errno设置为 <code>EINTR</code>）。</p>
<p><code>msgrcv</code> 成功执行时，内核会更新与该消息队列相关联的
<code>msgid_ds</code>
结构，以指示调用者的进程ID（<code>msg_lrpid</code>）和调用时间（<code>msg_rtime</code>），并指示队列中的消息数减少了1个（<code>msg_qnum</code>）。</p>
<hr />
<h2 id="信号量">信号量</h2>
<p>信号量与已经介绍过的 IPC方式（管道、FIFO
以及消息队列）不同。信号量是一种计数器，用于为多个进程提供对共享数据对象的访问权限。</p>
<p>为了获得共享资源，进程需要执行下列操作：</p>
<blockquote>
<p>（1）测试控制该资源的信号量。</p>
<p>（2）若此信号量的值为正，则进程可以使用该资源。在这种情况下，进程会将信号量值减1，表示它使用了一个资源单位。</p>
<p>（3）否则，若此信号量的值为 0，则进程进入休眠状态，直至信号量值大于
0。进程被唤醒后，它返回至步骤（1）。</p>
</blockquote>
<p>当一个进程使用完由信号量控制的共享资源后，信号量的值会增加1。如果有其他进程处于休眠状态，等待该信号量，它们将会被唤醒。</p>
<p>为了正确地实现信号量，信号量值的测试及减1操作应当是原子操作。为此，信号量通常是在内核中实现的。</p>
<p>常用的信号量形式被称为二元信号量（binary
semaphore）。它控制单个资源，其初始值为1。但是，一般而言，信号量的初值可以是任意一个正值，该值表明有多少个共享资源单位可供共享。</p>
<p>遗憾的是，XSI信号量与此相比要复杂得多。以下3种特性造成了这种不必要的复杂性。</p>
<p>（1）信号量并非是单个非负值，而必需定义为含有一个或多个信号量值的集合。当创建信号量时，要指定集合中信号量值的数量。</p>
<p>（2）信号量的创建（<code>semget</code>）是独立于它的初始化（<code>semctl</code>）的。这是一个致命的缺点，因为不能原子地创建一个信号量集合，并且对该集合中的各个信号量值赋初值。</p>
<p>（3）即使没有进程正在使用各种形式的XSI
IPC，它们仍然是存在的。有的程序在终止时并没有释放已经分配给它的信号量，所以我们不得不为这种程序担心。后面将要说明的
<code>undo</code> 功能就是处理这种情况的。</p>
<h3 id="xsi-信号量概念结构">XSI 信号量概念结构</h3>
<p>XSI / System V 信号量（semaphore） 的概念结构图如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">用户进程 A                                      用户进程 B</span><br><span class="line">------------                                    ------------</span><br><span class="line">key_t key = 0x5678                              key_t key = 0x5678</span><br><span class="line">semid = semget(key, 3, IPC_CREAT|0666)          semid = semget(key, 0, 0)</span><br><span class="line"></span><br><span class="line">semop(semid, ...)                               semop(semid, ...)</span><br><span class="line">      |                                               |</span><br><span class="line">      v                                               v</span><br><span class="line">+------------------------------------------------------------------+</span><br><span class="line">|                              内核                                 |</span><br><span class="line">|                                                                  |</span><br><span class="line">|   System V IPC 对象表 / 信号量管理区                              |</span><br><span class="line">|                                                                  |</span><br><span class="line">|   根据 key 查找/创建                                              |</span><br><span class="line">|                                                                  |</span><br><span class="line">|   key = 0x5678                                                    |</span><br><span class="line">|      |                                                           |</span><br><span class="line">|      v                                                           |</span><br><span class="line">|   +----------------------------------------------------------+   |</span><br><span class="line">|   | 信号量集对象（semaphore set object）                     |   |</span><br><span class="line">|   |----------------------------------------------------------|   |</span><br><span class="line">|   | IPC key      : 0x5678                                    |   |</span><br><span class="line">|   | semid        : 21   &lt;---- 内核返回给用户的信号量集 ID     |   |</span><br><span class="line">|   | uid/gid      : ...                                       |   |</span><br><span class="line">|   | mode/perm    : 0666                                      |   |</span><br><span class="line">|   | nsems        : 3     &lt;---- 这个集合里有 3 个信号量        |   |</span><br><span class="line">|   | otime        : 最后一次 semop 时间                       |   |</span><br><span class="line">|   | ctime        : 最后一次变更时间                          |   |</span><br><span class="line">|   |                                                          |   |</span><br><span class="line">|   | 信号量数组                                               |   |</span><br><span class="line">|   |                                                          |   |</span><br><span class="line">|   |   sem[0] -------------------------------------------+    |   |</span><br><span class="line">|   |     semval : 当前值                                 |    |   |</span><br><span class="line">|   |     sempid : 最后操作它的 PID                       |    |   |</span><br><span class="line">|   |     semncnt: 等待其值增加的进程数                   |    |   |</span><br><span class="line">|   |     semzcnt: 等待其值变为 0 的进程数                |    |   |</span><br><span class="line">|   |                                                      |    |   |</span><br><span class="line">|   |   sem[1] -------------------------------------------+    |   |</span><br><span class="line">|   |     semval : 当前值                                 |    |   |</span><br><span class="line">|   |     sempid : 最后操作它的 PID                       |    |   |</span><br><span class="line">|   |     semncnt: ...                                    |    |   |</span><br><span class="line">|   |     semzcnt: ...                                    |    |   |</span><br><span class="line">|   |                                                      |    |   |</span><br><span class="line">|   |   sem[2] -------------------------------------------+    |   |</span><br><span class="line">|   |     semval : 当前值                                 |    |   |</span><br><span class="line">|   |     sempid : 最后操作它的 PID                       |    |   |</span><br><span class="line">|   |     semncnt: ...                                    |    |   |</span><br><span class="line">|   |     semzcnt: ...                                    |    |   |</span><br><span class="line">|   +----------------------------------------------------------+   |</span><br><span class="line">|                                                                  |</span><br><span class="line">+------------------------------------------------------------------+</span><br></pre></td></tr></table></figure>
<p>内核为每个信号量集合维护着一个 <code>semid_ds</code> 结构：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">semid_ds</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ipc_perm</span>　<span class="title">sem_perm</span>;</span>　　<span class="comment">/* see Section 15.6.2 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">short</span> sem_nsems; <span class="comment">/* # of semaphores in set */</span></span><br><span class="line">    <span class="type">time_t</span>　　　　　 sem_otime;　<span class="comment">/* last-semop() time */</span></span><br><span class="line">    <span class="type">time_t</span>　　　　　 sem_ctime;　<span class="comment">/* last-change time */</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>每个信号量由一个无名结构表示，它至少包含下列成员：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">short</span>　semval;　　　<span class="comment">/* semaphore value, always &gt;= 0 */</span></span><br><span class="line">    <span class="type">pid_t</span>　　　　　 sempid;　　　<span class="comment">/* pid for last operation */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">short</span>　semncnt;　　 <span class="comment">/* # processes awaiting semval&gt;curval */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">short</span>　semzcnt;　　 <span class="comment">/* # processes awaiting semval==0 */</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h3 id="函数semget">函数semget</h3>
<p>当我们想使用XSI信号量时，首先需要通过调用函数 <code>semget</code>
来获得一个信号量ID。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/sem.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">semget</span><span class="params">(<span class="type">key_t</span> key, <span class="type">int</span> nsems, <span class="type">int</span> flag)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回信号量ID；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>创建一个新集合时，要对 <code>semid_ds</code>
结构的下列成员赋初值。</p>
<ul>
<li>初始化 <code>ipc_perm</code> 结构。该结构中的 <code>mode</code>
成员被设置为 <code>flag</code> 中的相应权限位。</li>
<li><code>sem_otime</code> 设置为0。</li>
<li><code>sem_ctime</code> 设置为当前时间。</li>
<li><code>sem_nsems</code> 设置为 <code>nsems</code>。</li>
</ul>
<p><code>nsems</code>
是该集合中的信号量数。如果是创建新集合（一般在服务器进程中），则必须指定
<code>nsems</code>。如果是引用现有集合（客户进程中），则将
<code>nsems</code> 指定为0。</p>
<h3 id="函数semctl">函数semctl</h3>
<p><code>semctl</code> 函数包含了多种信号量操作。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/sem.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">semctl</span><span class="params">(<span class="type">int</span> semid, <span class="type">int</span> semnum, <span class="type">int</span> cmd, ... <span class="comment">/* union semun arg */</span>)</span>;</span><br><span class="line"><span class="comment">// 返回值：（见下）</span></span><br></pre></td></tr></table></figure>
<p>第4个参数是可选的，是否使用取决于所请求的命令，如果使用该参数，则其类型是
<code>semun</code>，它是多个命令特定参数的联合（<code>union</code>）：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">union</span> <span class="title">semun</span> &#123;</span></span><br><span class="line">    <span class="type">int</span>　　　　　　　val;　　<span class="comment">/* for SETVAL */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">semid_ds</span> *<span class="title">buf</span>;</span>  <span class="comment">/* for IPC_STAT and IPC_SET */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">short</span> *<span class="built_in">array</span>; <span class="comment">/* for GETALL and SETALL */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>注意，这个选项参数是一个联合，而非指向联合的指针。</p>
<p>通常应用程序必须定义 <code>semun</code> 联合。然而，在FreeBSD
8.0中，<code>semun</code> 已经由 <code>&lt;sys/sem.h&gt;</code>
为我们定义好了。</p>
<h3 id="函数semop">函数semop</h3>
<p>函数 <code>semop</code> 以原子方式对信号量集执行一组操作。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/sem.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">semop</span><span class="params">(<span class="type">int</span> semid, <span class="keyword">struct</span> sembuf semoparray[], <span class="type">size_t</span> nops)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>参数 <code>semoparray</code> 是一个指针，它指向一个由
<code>sembuf</code> 结构表示的信号量操作数组：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sembuf</span> &#123;</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">short</span>　　　sem_num;　　 <span class="comment">/* member # in set (0, 1, ..., nsems-1 */</span></span><br><span class="line">    <span class="type">short</span>　　　　　　　　sem_op;　　　<span class="comment">/* operation(negative, 0,or pasitive */</span></span><br><span class="line">    <span class="type">short</span>　　　　　　　　sem_flg;　　 <span class="comment">/* IPC_NOWAIT, SEM_UNDO */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>参数 <code>nops</code> 规定该数组中操作的数量（元素数）。</p>
<p>对集合中每个成员的操作由相应的 <code>sem_op</code>
值规定。此值可以是负值、0或正值。（下面的讨论将提到信号量的“undo”标志。此标志对应于相应的
<code>sem_flg</code> 成员的 <code>SEM_UNDO</code> 位。）</p>
<p>（1）最易于处理的情况是 <code>sem_op</code>
为正值。这对应于进程释放的占用的资源数。<code>sem_op</code>
值会加到信号量的值上。如果指定了 <code>undo</code>
标志，则也从该进程的此信号量调整值中减去 <code>sem_op</code>。</p>
<p>（2）若 <code>sem_op</code>
为负值，则表示要获取由该信号量控制的资源。</p>
<p>如若该信号量的值大于等于 <code>sem_op</code>
的绝对值（具有所需的资源），则从信号量值中减去 <code>sem_op</code>
的绝对值。这能保证信号量的结果值大于等于0。如果指定了 <code>undo</code>
标志，则 <code>sem_op</code>
的绝对值也加到该进程的此信号量调整值上。</p>
<p>如果信号量值小于 <code>sem_op</code>
的绝对值（资源不能满足要求），则适用下列条件。</p>
<p>a．若指定了 <code>IPC_NOWAIT</code>，则 <code>semop</code> 出错返回
<code>EAGAIN</code>。</p>
<p>b．若未指定 <code>IPC_NOWAIT</code>，则该信号量的
<code>semncnt</code>
值加1（因为调用进程将进入休眠状态），然后调用进程被挂起直至下列事件之一发生。</p>
<p>i．此信号量值变成大于等于 <code>sem_op</code>
的绝对值（即某个进程已释放了某些资源）。此信号量的 <code>semncnt</code>
值减1（因为已结束等待），并且从信号量值中减去<code>sem_op</code>
的绝对值。</p>
<p>如果指定了 <code>undo</code> 标志，则 <code>sem_op</code>
的绝对值也加到该进程的此信号量调整值上。</p>
<p>ii．从系统中删除了此信号量。在这种情况下，函数出错返回
<code>EIDRM</code>。</p>
<p>iii．进程捕捉到一个信号，并从信号处理程序返回，在这种情况下，此信号量的
<code>semncnt</code> 值减1（因为调用进程不再等待），并且函数出错返回
<code>EINTR</code>。</p>
<p>（3）若 <code>sem_op</code>
为0，这表示调用进程希望等待到该信号量值变成0。</p>
<p>如果信号量值当前是0，则此函数立即返回。</p>
<p>如果信号量值非0，则适用下列条件。</p>
<p>a．若指定了 <code>IPC_NOWAIT</code>，则出错返回
<code>EAGAIN</code>。</p>
<p>b．若未指定 <code>IPC_NOWAIT</code>，则该信号量的
<code>semzcnt</code> 值加
1（因为调用进程将进入休眠状态），然后调用进程被挂起，直至下列的一个事件发生。</p>
<p>i．此信号量值变成0。此信号量的 <code>semzcnt</code>
值减1（因为调用进程已结束等待）。</p>
<p>ii．从系统中删除了此信号量。在这种情况下，函数出错返回
<code>EIDRM</code>。</p>
<p>iii．进程捕捉到一个信号，并从信号处理程序返回。在这种情况下，此信号量的
<code>semzcnt</code> 值减1（因为调用进程不再等待），并且函数出错返回
<code>EINTR</code>。</p>
<p><code>semop</code>
函数具有原子性，它或者执行数组中的所有操作，或者一个也不做。</p>
<h3 id="exit-时的信号量调整记账本"><code>exit</code>
时的信号量调整（记账本）</h3>
<p>正如我们前面所提到的，如果一个进程在通过信号量分配了资源的情况下终止，这会是个问题。每当我们为信号量操作指定
<code>SEM_UNDO</code> 标志，并且分配资源（<code>sem_op</code>
值小于0）时，内核会记录我们从该特定信号量分配了多少资源（
<code>sem_op</code>
的绝对值）。当进程终止时，无论是自愿终止还是非自愿终止，内核都会检查该进程是否有任何未完成的信号量调整，如果有的话，就会对相应的信号量应用这些调整。</p>
<p>如果用带 <code>SETVAL</code> 或 <code>SETALL</code> 命令的
<code>semctl</code>
设置一个信号量的值，则在所有进程中，该信号量的调整值都将设置为0（有人动了账本了，以前的旧账就乱了恢复困难，所以直接作废，重新开始算）。</p>
<hr />
<h2 id="共享存储">共享存储</h2>
<p>XSI 共享内存（shared memory）的概念结构图如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">进程 A                                             进程 B</span><br><span class="line">---------------------------                       ---------------------------</span><br><span class="line">key_t key = 0x9999                                key_t key = 0x9999</span><br><span class="line">shmid = shmget(key, 4096, IPC_CREAT|0666)         shmid = shmget(key, 0, 0)</span><br><span class="line"></span><br><span class="line">addrA = shmat(shmid, NULL, 0)                     addrB = shmat(shmid, NULL, 0)</span><br><span class="line"></span><br><span class="line">虚拟地址空间                                       虚拟地址空间</span><br><span class="line">+---------------------------+                     +---------------------------+</span><br><span class="line">| code / data / heap /stack |                     | code / data / heap /stack |</span><br><span class="line">|                           |                     |                           |</span><br><span class="line">| 共享内存映射 addrA ------ | -----------------\  | 共享内存映射 addrB ------ | -----------------\</span><br><span class="line">+---------------------------+                   \ +---------------------------+                  \</span><br><span class="line">                                                 \                                                 \</span><br><span class="line">                                                  \                                               /</span><br><span class="line">                                                   \                                             /</span><br><span class="line">                                                    v                                           v</span><br><span class="line"></span><br><span class="line">+-----------------------------------------------------------------------------------------------+</span><br><span class="line">|                                              内核                                             |</span><br><span class="line">|                                                                                               |</span><br><span class="line">|   System V IPC 对象表 / 共享内存管理区                                                         |</span><br><span class="line">|                                                                                               |</span><br><span class="line">|   key = 0x9999                                                                                |</span><br><span class="line">|      |                                                                                        |</span><br><span class="line">|      v                                                                                        |</span><br><span class="line">|   +---------------------------------------------------------------------------------------+   |</span><br><span class="line">|   | 共享内存对象（shared memory segment）                                                 |   |</span><br><span class="line">|   |---------------------------------------------------------------------------------------|   |</span><br><span class="line">|   | IPC key      : 0x9999                                                                 |   |</span><br><span class="line">|   | shmid        : 37    &lt;---- 内核返回给用户的共享内存 ID                                |   |</span><br><span class="line">|   | uid/gid      : ...                                                                    |   |</span><br><span class="line">|   | mode/perm    : 0666                                                                   |   |</span><br><span class="line">|   | size         : 4096 bytes                                                             |   |</span><br><span class="line">|   | nattch       : 2      &lt;---- 当前附接(attach)到它的进程数                              |   |</span><br><span class="line">|   | cpid         : 创建者 PID                                                             |   |</span><br><span class="line">|   | lpid         : 最近一次 shmat/shmdt 操作的 PID                                        |   |</span><br><span class="line">|   | atime        : 最后一次 shmat 时间                                                    |   |</span><br><span class="line">|   | dtime        : 最后一次 shmdt 时间                                                    |   |</span><br><span class="line">|   | ctime        : 最后一次变更时间                                                       |   |</span><br><span class="line">|   |                                                                                       |   |</span><br><span class="line">|   | 对应的物理页 / 页框内容                                                               |   |</span><br><span class="line">|   |   +---------------------------------------------+                                     |   |</span><br><span class="line">|   |   |               shared bytes                  |                                     |   |</span><br><span class="line">|   |   |      所有附接进程看到的是同一份内容         |                                     |   |</span><br><span class="line">|   |   +---------------------------------------------+                                     |   |</span><br><span class="line">|   +---------------------------------------------------------------------------------------+   |</span><br><span class="line">|                                                                                               |</span><br><span class="line">+-----------------------------------------------------------------------------------------------+</span><br></pre></td></tr></table></figure>
<p>共享内存允许两个或多个进程共享特定的内存区域。这是最快的进程间通信（IPC）形式，因为数据无需在客户端和服务器之间进行复制。使用共享内存时，唯一的难点在于在多个进程之间同步对特定区域的访问。如果服务器正在向共享内存区域写入数据，那么客户端在服务器完成操作之前不应尝试访问该数据。通常，信号量被用于同步共享内存访问。（但正如我们在上一节末尾所看到的，记录锁定或互斥锁也可以使用。）</p>
<p>我们已经见过一种共享内存的形式，即多个进程将同一个文件映射到它们的地址空间中。XSI共享内存与内存映射文件的不同之处在于，它没有关联的文件。XSI共享内存段是匿名的内存段。</p>
<p>内核为每个共享内存段维护一个至少包含以下成员的结构：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">shmid_ds</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ipc_perm</span> <span class="title">shm_perm</span>;</span>   <span class="comment">/* see Section 15.6.2 */</span></span><br><span class="line">    <span class="type">size_t</span>　　　　　shm_segsz;　 <span class="comment">/* size of segment in bytes */</span></span><br><span class="line">    <span class="type">pid_t</span>　　　　　 shm_lpid;　  <span class="comment">/* pid of last shmop() */</span></span><br><span class="line">    <span class="type">pid_t</span>　　　　　 shm_cpid;　  <span class="comment">/* pid of creator */</span></span><br><span class="line">    <span class="type">shmatt_t</span>　　　　shm_nattch;　<span class="comment">/* number of current attaches */</span></span><br><span class="line">    <span class="type">time_t</span>　　　　　shm_atime;　 <span class="comment">/* last-attach time */</span></span><br><span class="line">    <span class="type">time_t</span>　　　　　shm_dtime;　 <span class="comment">/* last-detach time */</span></span><br><span class="line">    <span class="type">time_t</span>　　　　　shm_ctime;　 <span class="comment">/* last-change time */</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p><code>shmatt_t</code> 类型定义为无符号整型，它至少与unsigned
short一样大。</p>
<p>调用的第一个函数通常是
<code>shmget</code>，它获得一个共享存储标识符。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/shm.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">shmget</span><span class="params">(<span class="type">key_t</span> key, <span class="type">size_t</span> size, <span class="type">int</span> flag)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回共享存储ID；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p><code>size</code>
参数是共享内存段的大小（以字节为单位）。实现通常会将此大小向上舍入为系统页面大小的倍数，但如果应用程序指定的
<code>size</code>
不是系统页面大小的整数倍，则最后一页的剩余部分将无法使用。如果要创建新段（通常由服务器创建），我们必须指定其大小。如果我们要引用现有段（客户端），可以将
<code>size</code> 指定为0。创建新段时，段的内容会初始化为零。</p>
<p><code>shmctl</code> 函数对共享存储段执行多种操作。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/shm.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">shmctl</span><span class="params">(<span class="type">int</span> shmid, <span class="type">int</span> cmd, <span class="keyword">struct</span> shmid_ds *buf)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>一旦共享内存段被创建，进程就会通过调用 <code>shmat</code>
将其附加到自己的地址空间中。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/shm.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">shmat</span><span class="params">(<span class="type">int</span> shmid, <span class="type">const</span> <span class="type">void</span> *addr, <span class="type">int</span> flag)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回指向共享存储段的指针；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>调用进程中附加该段的地址取决于 <code>addr</code> 参数以及
<code>flag</code> 中是否指定了 <code>SHM_RND</code> 位。</p>
<ul>
<li>如果 <code>addr</code>
为0，该段落在内核选择的第一个可用地址处附加。这是推荐的技术。</li>
<li>如果 <code>addr</code> 非0，并且没有指定
<code>SHM_RND</code>，则此段连接到 <code>addr</code>
所指定的地址上。</li>
<li>如果 <code>addr</code> 非0，且指定了
<code>SHM_RND</code>，那么该段会被附加到由（addr减去（addr对SHMLBA取模的结果））所给出的地址处。<code>SHM_RND</code>
命令代表“舍入”。<code>SHMLBA</code>
代表“低边界地址倍数”，且它始终是2的幂。这种算术运算的作用是将地址向下舍入到
<code>SHMLBA</code> 的下一个倍数。</li>
</ul>
<p>除非我们打算仅在单一类型的硬件上运行该应用程序（如今这是极不可能的），否则我们不应该指定要附加段的地址。相反，我们应该将
<code>addr</code> 指定为0，让系统来选择地址。</p>
<p>如果在 <code>flag</code> 中指定了 <code>SHM_RDONLY</code>
位，则以只读方式连接此段，否则以读写方式连接此段。</p>
<p><code>shmat</code> 返回的值是段所连接的地址，若发生错误则返回−1。如果
<code>shmat</code> 调用成功，内核会增加与该共享内存段相关联的
<code>shmid_ds</code> 结构中的 <code>shm_nattch</code> 计数器。</p>
<p>当使用完一个共享内存段后，会调用 <code>shmdt</code> 来
detach（分离）它。需要注意的是，这并不会从系统中移除该标识符及其关联的数据结构。该标识符会一直存在，直到某个进程（通常是服务器）通过调用
<code>shmctl</code> 并使用 <code>IPC_RMID</code> 命令来专门移除它。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/shm.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">shmdt</span><span class="params">(<span class="type">const</span> <span class="type">void</span> *addr)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p><code>addr</code> 参数是以前调用 <code>shmat</code>
时的返回值。如果成功，<code>shmdt</code> 将使相关 <code>shmid_ds</code>
结构中的 <code>shm_nattch</code> 计数器值减1。</p>
<p>用户进程的虚拟地址空间分布图如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">               进程虚拟地址空间</span><br><span class="line"></span><br><span class="line">高地址</span><br><span class="line">+------------------------------+</span><br><span class="line">|            stack             |</span><br><span class="line">+------------------------------+</span><br><span class="line">|      mmap / 映射区           |</span><br><span class="line">|  +------------------------+  |</span><br><span class="line">|  | XSI shared memory      |  |</span><br><span class="line">|  +------------------------+  |</span><br><span class="line">|  | shared libraries       |  |</span><br><span class="line">|  +------------------------+  |</span><br><span class="line">|  | file/anon mappings     |  |</span><br><span class="line">|  +------------------------+  |</span><br><span class="line">+------------------------------+</span><br><span class="line">|             heap             |</span><br><span class="line">+------------------------------+</span><br><span class="line">|          data/bss            |</span><br><span class="line">+------------------------------+</span><br><span class="line">|            text              |</span><br><span class="line">+------------------------------+</span><br><span class="line">低地址</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="posix信号量">POSIX信号量</h2>
<p>POSIX 信号量（semaphore）概念结构图如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">                   POSIX 信号量结构图</span><br><span class="line"></span><br><span class="line">1) 未命名信号量</span><br><span class="line">------------------------------------------------</span><br><span class="line">   sem_t 就在内存里</span><br><span class="line">   [线程间] 进程普通地址空间中的 sem_t</span><br><span class="line">   [进程间] 共享内存中的 sem_t</span><br><span class="line"></span><br><span class="line">2) 命名信号量</span><br><span class="line">------------------------------------------------</span><br><span class="line">   &quot;/semname&quot;</span><br><span class="line">        |</span><br><span class="line">        v</span><br><span class="line">   sem_open(&quot;/semname&quot;)</span><br><span class="line">        |</span><br><span class="line">        v</span><br><span class="line">   系统维护的命名信号量对象</span><br></pre></td></tr></table></figure>
<p>POSIX信号量机制是源于POSIX.1实时扩展的三种IPC机制(消息队列、信号量和共享内存)之一。</p>
<p>POSIX信号量接口意在解决XSI信号量接口的几个缺陷：</p>
<ul>
<li>相比于XSI接口，POSIX信号量接口考虑到了更高性能的实现。</li>
<li>POSIX信号量接口使用起来更简单：不存在信号量集，而且有几个接口是仿照人们熟悉的文件系统操作设计的。尽管并没有要求必须在文件系统中实现它们，但有些系统确实采用了这种方法。</li>
<li>POSIX信号量在被移除时表现得更为得体。回想一下，当一个XSI信号量被移除时，使用相同信号量标识符的操作会失败，且
<code>errno</code> 会被设置为
<code>EIDRM</code>。而对于POSIX信号量，在最后一个对该信号量的引用被释放之前，相关操作会继续正常工作。</li>
</ul>
<p>POSIX信号量有两种形式：命名的和未命名的。它们的差异在于创建和销毁的形式上，但其他工作一样。无名信号量仅存在于内存中，并且要求进程必须能够访问该内存才能使用这些信号量。这意味着它们只能被同一进程中的线程，或者已将相同内存区域映射到自身地址空间的不同进程中的线程使用。相比之下，有名信号量通过名称进行访问，任何知道其名称的进程中的线程都可以使用它们。</p>
<p>可以调用 <code>sem_open</code>
函数来创建一个新的命名信号量或者使用一个现有信号量。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;semaphore.h&gt;</span></span></span><br><span class="line"><span class="type">sem_t</span> *<span class="title function_">sem_open</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name, <span class="type">int</span> oflag, ... <span class="comment">/* mode_t mode, unsigned int value */</span> )</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回指向信号量的指针；若出错，返回SEM_FAILED</span></span><br></pre></td></tr></table></figure>
<p>当使用一个现有的命名信号量时，我们仅仅指定两个参数：信号量的名字和
<code>oflag</code> 参数的0值。当这个 <code>oflag</code> 参数有
<code>O_CREAT</code>
标志集时，如果命名信号量不存在，则创建一个新的。如果它已经存在，则会被使用，但是不会有额外的初始化发生。</p>
<p>当指定 <code>O_CREAT</code>
标志时，需要提供两个额外的参数。<code>mode</code>
参数指定谁可以访问信号量。<code>mode</code>
的取值和打开文件的权限位相同。赋值给信号量的权限可以被调用者的文件创建屏蔽字修改。然而，需要注意的是，只有读写权限才重要，但这些接口不允许我们在打开现有信号量时指定模式。实现通常会以读写方式打开信号量。</p>
<p>在创建信号量时，<code>value</code>
参数用来指定信号量的初始值。它的取值是
<code>0~SEM_VALUE_MAX</code>。</p>
<p>如果我们想确保创建的是信号量，可以设置 <code>oflag</code> 参数为
<code>O_CREAT|O_EXCL</code>。如果信号量已经存在，会导致
<code>sem_open</code> 失败。</p>
<p>为了提高可移植性，我们在选择信号量名称时必须遵循特定的约定。</p>
<ul>
<li>名称中的第一个字符应该是斜杠（<code>/</code>）。尽管并没有要求POSIX信号量的实现必须使用文件系统，但如果使用了文件系统，我们希望消除在解释名称时从哪个起点开始的任何歧义。</li>
<li>该名称不应包含其他斜杠，以避免因实现方式不同而产生的不确定行为。</li>
<li>信号量名称的最大长度由具体实现定义。该名称不应超过
<code>_POSIX_NAME_MAX</code>
个字符，因为如果实现确实使用文件系统，这是最大名称长度的最小可接受限制。</li>
</ul>
<p><code>sem_open</code>
会返回一个信号量指针，想要对信号量进行操作时，可以将该指针传递给其他信号量函数。当使用完信号量后，可以调用
<code>sem_close</code> 来释放与该信号量相关的所有资源。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;semaphore.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sem_close</span><span class="params">(<span class="type">sem_t</span> *sem)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p>如果我们的进程在未先调用 <code>sem_close</code>
的情况下退出，内核会自动关闭所有打开的信号量。需要注意的是，这不会影响信号量值的状态——如果我们已经增加了它的值，并不会仅仅因为进程退出而改变。同样，调用
<code>sem_close</code> 也不会影响信号量的值。这里没有与XSI信号量中的
<code>SEM_UNDO</code> 标志等效的机制。</p>
<p>可以使用 <code>sem_unlink</code> 函数来销毁一个命名信号量。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;semaphore.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sem_unlink</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回-1</span></span><br></pre></td></tr></table></figure>
<p><code>sem_unlink</code>
函数会移除信号量的名称。如果没有对该信号量的打开引用，那么它会被销毁。否则，销毁操作会推迟到最后一个打开的引用被关闭时进行。</p>
<p>与XSI信号量不同，我们通过一次函数调用只能将POSIX信号量的值调整1。减少计数类似于锁定二进制信号量，或者获取与计数信号量相关联的资源。</p>
<p>可以使用 <code>sem_wait</code> 或者 <code>sem_trywait</code>
函数来实现信号量的减1操作。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;semaphore.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sem_trywait</span><span class="params">(<span class="type">sem_t</span> *sem)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">sem_wait</span><span class="params">(<span class="type">sem_t</span> *sem)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；若出错则，返回−1</span></span><br></pre></td></tr></table></figure>
<p>使用
<code>sem_wait</code>函数时，如果信号量计数是0就会发生阻塞。直到成功使信号量减1或者被信号中断时才返回。可以使用
<code>sem_trywait</code> 函数来避免阻塞。调用<code>sem_trywait</code>
时，如果信号量是0，则不会阻塞，而是会返回−1并且将errno置为
<code>EAGAIN</code>。</p>
<p>第三个选择是阻塞一段确定的时间。为此，可以使用
<code>sem_timedwait</code> 函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;semaphore.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sem_timedwait</span><span class="params">(<span class="type">sem_t</span> *<span class="keyword">restrict</span> sem, <span class="type">const</span> <span class="keyword">struct</span> timespec *<span class="keyword">restrict</span> tsptr)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p><code>tsptr</code>
参数指定了我们希望放弃等待信号量的绝对时间。超时基于
<code>CLOCK_REALTIME</code>
时钟（图6.8）。如果可以立即对信号量执行减操作，那么超时值就无关紧要了——即便它指定的时间可能是过去的某个时间，尝试对信号量执行减操作仍会成功。如果超时时间已到却仍无法对信号量计数执行减操作，那么
<code>sem_timedwait</code> 将返回-1，并将 <code>errno</code> 设置为
<code>ETIMEDOUT</code>。</p>
<p>要增加信号量的值，调用 <code>sem_post</code>
函数。这类似于解锁一个二元信号量，或者释放与计数信号量相关联的资源。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;semaphore.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sem_post</span><span class="params">(<span class="type">sem_t</span> *sem)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>如果一个进程在我们调用 <code>sem_post</code> 时因调用
<code>sem_wait</code>（或
<code>sem_timedwait</code>）而处于阻塞状态，那么该进程会被唤醒，且刚刚被
<code>sem_post</code> 递增的信号量计数会被 <code>sem_wait</code>（或
<code>sem_timedwait</code>）递减。</p>
<p>当我们想在单个进程中使用POSIX信号量时，使用无名信号量会更简便。这仅仅改变我们创建和销毁信号量的方式。要创建无名信号量，需要调用
<code>sem_init</code> 函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;semaphore.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sem_init</span><span class="params">(<span class="type">sem_t</span> *sem, <span class="type">int</span> pshared, <span class="type">unsigned</span> <span class="type">int</span> value)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p><code>pshared</code>
参数表明是否在多个进程中使用信号量。如果是，将其设置成一个非0值。<code>value</code>
参数指定了信号量的初始值。</p>
<p>需要声明一个 <code>sem_t</code>
类型的变量并把它的地址传递给sem_init来实现初始化，而不是像
<code>sem_open</code>
函数那样返回一个指向信号量的指针。如果计划在两个进程之间使用信号量，就需要确保
<code>sem</code> 参数指向这两个进程之间共享的内存区域。</p>
<p>对未命名信号量的使用已经完成时，可以调用 <code>sem_destroy</code>
函数丢弃它。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;semaphore.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sem_destroy</span><span class="params">(<span class="type">sem_t</span> *sem)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>调用 <code>sem_destroy</code> 后，不能再使用任何带有 <code>sem</code>
的信号量函数，除非通过调用 <code>sem_init</code> 重新初始化它。</p>
<p><code>sem_getvalue</code> 函数可以用来检索信号量值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;semaphore.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sem_getvalue</span><span class="params">(<span class="type">sem_t</span> *<span class="keyword">restrict</span> sem, <span class="type">int</span> *<span class="keyword">restrict</span> valp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>成功后，<code>valp</code>
指向的整数值将包含信号量值。然而，需要注意的是，当我们尝试使用刚刚读取的信号量值时，该值可能已经发生了变化。除非我们使用额外的同步机制来避免这种竞争状态，否则
<code>sem_getvalue</code> 函数仅在调试时有用。</p>
<hr />
<h2 id="clientserver-properties">Client–Server Properties</h2>
<p>open server</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">                [fork before exec]</span><br><span class="line"></span><br><span class="line">                    two pipes</span><br><span class="line">     request  ------------------------&gt;</span><br><span class="line">+----------------+                 +----------------------+</span><br><span class="line">| Client(parent) |                 | Server(child+exec)   |</span><br><span class="line">|                |                 | setuid program       |</span><br><span class="line">| ordinary user  |                 | maybe privileged     |</span><br><span class="line">+----------------+                 +----------------------+</span><br><span class="line">     &lt;------------------------  result/content</span><br><span class="line">               response</span><br><span class="line"></span><br><span class="line">工作方式：</span><br><span class="line">1. client 发请求</span><br><span class="line">2. server 根据 client&#x27;s real UID 判断是否允许</span><br><span class="line">3. server 代替 client 执行 open/read</span><br><span class="line">4. server 把“内容”传回 client</span><br><span class="line"></span><br><span class="line">限制：</span><br><span class="line">- server 新打开的 fd 只在 server 自己的 fd table 里</span><br><span class="line">- 不能靠普通 pipe 直接把 fd 回传给 parent</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="小结">小结</h2>
<p>我们已经详细介绍了多种进程间通信形式：管道、命名管道（FIFO）、通常称为XSI
IPC的三种进程间通信形式（消息队列、信号量和共享内存），以及POSIX提供的另一种信号量机制。信号量实际上是一种同步原语，并非真正的进程间通信方式，它常被用于同步对共享资源（如共享内存段）的访问。关于管道，我们探讨了
<code>popen</code>
函数的实现、协同进程以及标准I/O库缓冲可能带来的问题。</p>
<p>在比较了消息队列与全双工管道的性能，以及信号量与记录锁的性能之后，我们可以给出以下建议：<strong>学习管道和FIFO</strong>，因为这两种基本技术在许多应用中仍能有效发挥作用。在任何新应用中都应避免使用XSI消息队列和信号量。相反，应考虑<strong>使用全双工管道和记录锁，因为它们要简单得多</strong>。共享内存仍有其用武之地，不过通过
<code>mmap</code> 函数也能提供相同的功能。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Network IPC： Sockets（十六）</title>
    <url>/2025/08/28/notes-reading/computer/apue/apue-16-network-ipc-sockets/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第16章 Network IPC： Sockets
的一些知识点。</p>
<span id="more"></span>
<hr />
<p>解决网络问题的一般步骤（粗看-细看-深入分析）：</p>
<ol type="1">
<li><code>netstat -an</code></li>
<li>抓包 <code>tcpdump</code></li>
<li>使用 <code>Wireshark</code> 工具分析</li>
</ol>
<hr />
<h2 id="套接字描述符">套接字描述符</h2>
<p>套接字是通信端点的抽象。正如使用文件描述符访问文件，应用程序用套接字描述符访问套接字。在UNIX系统中，套接字描述符是作为文件描述符来实现的。实际上，许多用于处理文件描述符的函数，如
<code>read</code> 和 <code>write</code>，也可用于套接字描述符。</p>
<p>为创建一个套接字，调用 <code>socket</code> 函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">socket</span><span class="params">(<span class="type">int</span> domain, <span class="type">int</span> type, <span class="type">int</span> protocol)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回文件（套接字）描述符；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p><code>domain</code>
参数决定了通信的性质，包括地址格式。图16.1总结了POSIX.1所规定的域。这些常量以
<code>AF_</code>（address
family）开头，因为每个域都有其自身的地址表示格式。</p>
<table>
<thead>
<tr class="header">
<th>Domain</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>AF_INET</td>
<td>IPv4 Internet domain</td>
</tr>
<tr class="even">
<td>AF_INET6</td>
<td>IPv6 Internet domain (optional in POSIX.1)</td>
</tr>
<tr class="odd">
<td>AF_UNIX</td>
<td>UNIX domain</td>
</tr>
<tr class="even">
<td>AF_UNSPEC</td>
<td>unspecified</td>
</tr>
</tbody>
</table>
<p>大多数系统还定义了 <code>AF_LOCAL</code> 域，这是
<code>AF_UNIX</code> 的别名。<code>AF_UNSPEC</code>
可以代表“任何”域。</p>
<p>参数 <code>type</code>
确定套接字的类型，进一步确定通信特征。下表总结了由POSIX.1定义的套接字类型，但在实现中可以自由增加其他类型的支持。</p>
<table>
<colgroup>
<col style="width: 19%" />
<col style="width: 80%" />
</colgroup>
<thead>
<tr class="header">
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>SOCK_DGRAM</td>
<td>fixed-length, connectionless, unreliable messages</td>
</tr>
<tr class="even">
<td>SOCK_RAW</td>
<td>datagram interface to IP (optional in POSIX.1)</td>
</tr>
<tr class="odd">
<td>SOCK_SEQPACKET</td>
<td>fixed-length, sequenced, reliable, connection-oriented messages</td>
</tr>
<tr class="even">
<td>SOCK_STREAM</td>
<td>sequenced, reliable, bidirectional, connection-oriented byte
streams</td>
</tr>
</tbody>
</table>
<p>参数 <code>protocol</code> 通常是
0，表示为给定的域和套接字类型选择默认协议。当对同一域和套接字类型支持多个协议时，可以使用
<code>protocol</code> 选择一个特定协议。在 <code>AF_INET</code>
通信域中，套接字类型 <code>SOCK_STREAM</code>
的默认协议是传输控制协议（Transmission Control Protocol，TCP）。在
<code>AF_INET</code> 通信域中，套接字类型 <code>SOCK_DGRAM</code>
的默认协议是
<code>UDP</code>。下表列出了为因特网域套接字定义的协议。</p>
<table>
<thead>
<tr class="header">
<th>Protocol</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>IPPROTO_IP</td>
<td>IPv4 Internet Protocol</td>
</tr>
<tr class="even">
<td>IPPROTO_IPV6</td>
<td>IPv6 Internet Protocol (optional in POSIX.1)</td>
</tr>
<tr class="odd">
<td>IPPROTO_ICMP</td>
<td>Internet Control Message Protocol</td>
</tr>
<tr class="even">
<td>IPPROTO_RAW</td>
<td>Raw IP packets protocol (optional in POSIX.1)</td>
</tr>
<tr class="odd">
<td>IPPROTO_TCP</td>
<td>Transmission Control Protocol</td>
</tr>
<tr class="even">
<td>IPPROTO_UDP</td>
<td>User Datagram Protocol</td>
</tr>
</tbody>
</table>
<p>调用 <code>socket</code> 与调用 <code>open</code>
相类似，均可获得用于I/O的文件描述符。当不再需要该文件描述符时，调用
<code>close</code>
来关闭对文件或套接字的访问，并且释放该描述符以便重新使用。</p>
<p>虽然套接字描述符本质上是一个文件描述符，但不是所有参数为文件描述符的函数都可以接受套接字描述符。下图总结了到目前为止所讨论的大多数以文件描述符为参数的函数使用套接字描述符时的行为。未指定和由实现定义的行为通常意味着该函数对套接字描述符无效。例如，
<code>lseek</code>
不能以套接字描述符为参数，因为套接字不支持文件偏移量的概念。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260315104627639.png" alt="How file descriptor functions act with sockets" style="zoom: 67%;" /></p>
<p>套接字通信是双向的。可以采用 <code>shutdown</code>
函数来禁用一个套接字的I/O。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">shutdown</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">int</span> how)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>如果 <code>how</code> 是
<code>SHUT_RD</code>（关闭读端），那么无法从套接字读取数据。如果<code>how</code>
是
<code>SHUT_WR</code>（关闭写端），那么无法使用套接字发送数据。如果<code>how</code>是<code>SHUT_RDWR</code>，则既无法读取数据，又无法发送数据。</p>
<p>能够关闭（close）一个套接字，为何还使用shutdown呢？这里有若干理由。首先，只有最后一个活动引用关闭时，close才释放网络端点。这意味着如果复制一个套接字（如采用dup），要直到关闭了最后一个引用它的文件描述符才会释放这个套接字。而
<code>shutdown</code>
允许使一个套接字处于不活动状态，和引用它的文件描述符数目无关。其次，有时可以很方便地关闭套接字双向传输中的一个方向。例如，如果想让所通信的进程能够确定数据传输何时结束，可以关闭该套接字的写端，然而通过该套接字读端仍可以继续接收数据。</p>
<p>既然可以 <code>close</code> 套接字，那为什么还需要
<code>shutdown</code>
呢？原因有几个。首先，只有当最后一个活动引用被关闭时，<code>close</code>才会释放网络端点。如果复制了套接字（例如使用
<code>dup</code>），那么只有关闭最后一个引用它的文件描述符时，该套接字才会被释放。而
<code>shutdown</code>
函数允许独立于引用套接字的活动文件描述符的数量来停用套接字。其次，有时仅在一个方向上关闭套接字会很方便。例如，如果希望与之通信的进程能够知道我们何时完成数据传输，同时仍允许我们使用该套接字接收该进程发送给我们的数据，就可以关闭套接字的写功能。</p>
<hr />
<h2 id="寻址">寻址</h2>
<p>在学习用套接字做一些有意义的事情之前，需要知道如何标识一个目标通信进程。进程标识由两部分组成。一部分是<strong>计算机的网络地址</strong>，它可以帮助标识网络上我们想与之通信的计算机；另一部分是该计算机上用<strong>端口号</strong>表示的服务，它可以帮助标识特定的进程。</p>
<h3 id="字节序">字节序</h3>
<p>大小端字节序存储方式可参考 <span class="exturl" data-url="aHR0cHM6Ly93d3cuY25ibG9ncy5jb20vSW52aW5jLVovcC8xODgwMjU3MC9ieXRlLW9yZGVyaW5n">字节序<i class="fa fa-external-link-alt"></i></span>。</p>
<p>网络协议指定了字节序，因此异构计算机系统能够交换协议信息而不会被字节序所混淆。<strong>TCP/IP协议栈使用大端字节序</strong>。</p>
<p>对于TCP/IP应用程序，有4个用来在处理器字节序和网络字节序之间实施转换的函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="type">uint32_t</span> <span class="title function_">htonl</span><span class="params">(<span class="type">uint32_t</span> hostint32)</span>; <span class="comment">// 返回值：以网络字节序表示的32位整数</span></span><br><span class="line"></span><br><span class="line"><span class="type">uint16_t</span> <span class="title function_">htons</span><span class="params">(<span class="type">uint16_t</span> hostint16)</span>; <span class="comment">// 返回值：以网络字节序表示的16位整数</span></span><br><span class="line"></span><br><span class="line"><span class="type">uint32_t</span> <span class="title function_">ntohl</span><span class="params">(<span class="type">uint32_t</span> netint32)</span>;  <span class="comment">// 返回值：以主机字节序表示的32位整数</span></span><br><span class="line"></span><br><span class="line"><span class="type">uint16_t</span> <span class="title function_">ntohs</span><span class="params">(<span class="type">uint16_t</span> netint16)</span>;  <span class="comment">// 返回值：以主机字节序表示的16位整数</span></span><br></pre></td></tr></table></figure>
<p><code>h</code> 表示“主机”字节序，<code>n</code>
表示“网络”字节序。<code>l</code> 表示“长”（即4字节）整数，<code>s</code>
表示“短”（即2字节）整数。</p>
<h3 id="地址格式">地址格式</h3>
<p>一个地址标识一个特定通信域的套接字端点，地址格式与这个特定的通信域相关。为使不同格式地址能够传入到套接字函数，地址会被强制转换成一个<strong>通用的地址结构</strong>
<code>sockaddr</code>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span> &#123;</span></span><br><span class="line">    <span class="type">sa_family_t</span>　　　　　sa_family;　 <span class="comment">/* address family */</span></span><br><span class="line">    <span class="type">char</span>　　　　　　　　 sa_data[];　 <span class="comment">/* variable-length address */</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sockaddr            通用接口外壳</span><br><span class="line">   |</span><br><span class="line">   +-- sockaddr_in  IPv4 完整地址</span><br><span class="line">   |      |</span><br><span class="line">   |      +-- in_addr   仅 IPv4 地址</span><br><span class="line">   |</span><br><span class="line">   +-- sockaddr_in6 IPv6 完整地址</span><br><span class="line">          |</span><br><span class="line">          +-- in6_addr  仅 IPv6 地址</span><br></pre></td></tr></table></figure>
<p>有时，需要打印出能被人理解而不是计算机所理解的地址格式。BSD
网络软件包含函数 <code>inet_addr</code> 和
<code>inet_ntoa</code>，用于二进制地址格式与点分十进制字符表示（a.b.c.d）之间的相互转换。但是这些函数仅适用于IPv4地址。有两个新函数
<code>inet_ntop</code> 和 <code>inet_pton</code>
具有相似的功能，而且同时支持IPv4地址和IPv6地址。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="type">const</span> <span class="type">char</span> *<span class="title function_">inet_ntop</span><span class="params">(<span class="type">int</span> domain, <span class="type">const</span> <span class="type">void</span> *<span class="keyword">restrict</span> addr, <span class="type">char</span> *<span class="keyword">restrict</span> str, <span class="type">socklen_t</span> size)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回地址字符串指针；若出错，返回NULL</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">inet_pton</span><span class="params">(<span class="type">int</span> domain, <span class="type">const</span> <span class="type">char</span> * <span class="keyword">restrict</span> str, <span class="type">void</span> *<span class="keyword">restrict</span> addr)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回1；若格式无效，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>函数 <code>inet_ntop</code>
将网络字节序的二进制地址转换成文本字符串格式。<code>inet_pton</code>
将文本字符串格式转换成网络字节序的二进制地址。参数 <code>domain</code>
仅支持两个值：<code>AF_INET</code> 和 <code>AF_INET6</code>。</p>
<p>对于 <code>inet_ntop</code>，参数 <code>size</code>
指定了保存文本字符串的缓冲区（<code>str</code>）的大小。两个常数用于简化工作：<code>INET_ADDRSTRLEN</code>
定义了足够大的空间来存放一个表示 IPv4
地址的文本字符串；<code>INET6_ADDRSTRLEN</code>
定义了足够大的空间来存放一个表示 IPv6 地址的文本字符串。对于
<code>inet_pton</code>，如果 domain是
<code>AF_INET</code>，则缓冲区<code>addr</code>
需要足够大的空间来存放一个32位地址，如果 <code>domain</code> 是
<code>AF_INET6</code>，则需要足够大的空间来存放一个128位地址。</p>
<h3 id="地址查询">地址查询</h3>
<p>通过调用
<code>gethostent</code>，可以找到给定计算机系统的主机信息。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="keyword">struct</span> hostent *<span class="title function_">gethostent</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回指针；若出错，返回NULL</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">sethostent</span><span class="params">(<span class="type">int</span> stayopen)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">endhostent</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure>
<p>如果主机数据库文件没有打开，<code>gethostent</code> 会打开它。函数
<code>gethostent</code> 返回文件中的下一个条目。函数
<code>sethostent</code> 会打开文件，如果文件已经被打开，那么将其回绕。当
<code>stayopen</code> 参数设置成非0值时，调用 <code>gethostent</code>
之后，文件将依然是打开的。函数 <code>endhostent</code>
可以关闭文件。</p>
<p>当 <code>gethostent</code> 返回时，会得到一个指向
<code>hostent</code>
结构的指针，该结构可能包含一个静态的数据缓冲区，每次调用
<code>gethostent</code>，缓冲区都会被覆盖。<code>hostent</code>结构至少包含以下成员：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">hostent</span>&#123;</span></span><br><span class="line">    <span class="type">char</span>　　*h_name;　　　　 <span class="comment">/* name of host */</span></span><br><span class="line">    <span class="type">char</span>　 **h_aliases;　　　<span class="comment">/* pointer to alternate host name array */</span></span><br><span class="line">    <span class="type">int</span>　　　h_addrtype;　　 <span class="comment">/* address type */</span></span><br><span class="line">    <span class="type">int</span>　　　h_length;　　　 <span class="comment">/* length in bytes of address */</span></span><br><span class="line">    <span class="type">char</span>　 **h_addr_list;　 <span class="comment">/* pointer to array of network addresses */</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>返回的地址采用网络字节序。</p>
<p>获得网络名字和网络编号的相似接口的函数如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="keyword">struct</span> netent *<span class="title function_">getnetbyaddr</span><span class="params">(<span class="type">uint32_t</span> net, <span class="type">int</span> type)</span>;</span><br><span class="line"><span class="keyword">struct</span> netent *<span class="title function_">getnetbyname</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name)</span>;</span><br><span class="line"><span class="keyword">struct</span> netent *<span class="title function_">getnetent</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 3个函数的返回值：若成功，返回指针；若出错，返回NULL</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">setnetent</span><span class="params">(<span class="type">int</span> stayopen)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">endnetent</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure>
<p><code>netent</code> 结构至少包含以下字段：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">netent</span> &#123;</span></span><br><span class="line">    <span class="type">char</span>　　 *n_name;　　　　<span class="comment">/* network name */</span></span><br><span class="line">    <span class="type">char</span>　　**n_aliases;　　 <span class="comment">/* alternate network name array pointer */</span></span><br><span class="line">    <span class="type">int</span>　　　 n_addrtype;　  <span class="comment">/* address type */</span></span><br><span class="line">    <span class="type">uint32_t</span>　n_net;　　　　 <span class="comment">/* network number */</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>网络编号按照网络字节序返回。地址类型是地址族常量之一（如
<code>AF_INET</code>）。</p>
<p>可以用以下函数在协议名字和协议编号之间进行映射。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="keyword">struct</span> protoent *<span class="title function_">getprotobyname</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name)</span>;</span><br><span class="line"><span class="keyword">struct</span> protoent *<span class="title function_">getprotobynumber</span><span class="params">(<span class="type">int</span> proto)</span>;</span><br><span class="line"><span class="keyword">struct</span> protoent *<span class="title function_">getprotoent</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 3个函数的返回值：若成功，返回指针；若出错，返回NULL</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">setprotoent</span><span class="params">(<span class="type">int</span> stayopen)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">endprotoent</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure>
<p>POSIX.1定义的 <code>protoent</code>结构至少包含以下成员：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">protoent</span> &#123;</span></span><br><span class="line">    <span class="type">char</span>　 *p_name;　　　　　<span class="comment">/* protocol name */</span></span><br><span class="line">    <span class="type">char</span>　**p_ aliases;　　　<span class="comment">/* pointer to altername protocol name array */</span></span><br><span class="line">    <span class="type">int</span>　　 p_proto;　　　　 <span class="comment">/* protocol number */</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>服务是由地址的端口号部分表示的。每个服务由一个唯一的众所周知的端口号来支持。可以使用函数
<code>getservbyname</code>
将一个服务名映射到一个端口号，使用函数<code>getservbyport</code>
将一个端口号映射到一个服务名，使用函数 <code>getservent</code>
顺序扫描服务数据库。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="keyword">struct</span> servent *<span class="title function_">getservbyname</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name, <span class="type">const</span> <span class="type">char</span> *proto)</span>;</span><br><span class="line"><span class="keyword">struct</span> servent *<span class="title function_">getserbyport</span><span class="params">(<span class="type">int</span> port, <span class="type">const</span> <span class="type">char</span> *proto)</span>;</span><br><span class="line"><span class="keyword">struct</span> servent *<span class="title function_">getservent</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="comment">// 3个函数的返回值：若成功，返回指针，若出错，返回NULL</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">setservent</span><span class="params">(<span class="type">int</span> stayopen)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">endservent</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure>
<p><code>servent</code> 结构至少包含以下成员：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">servent</span>&#123;</span></span><br><span class="line">    <span class="type">char</span>　 *s_name;　　　　　<span class="comment">/* service name */</span></span><br><span class="line">    <span class="type">char</span>　**s_aliases;　　　 <span class="comment">/* pointer to alternate service name array */</span></span><br><span class="line">    <span class="type">int</span>　　 s_port;　　　　　<span class="comment">/* port　number */</span></span><br><span class="line">    <span class="type">char</span>　 *s_proto;　　　　 <span class="comment">/* name of protocol */</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>POSIX.1定义了若干新的函数，允许一个应用程序将一个主机名和一个服务名映射到一个地址，或者反之。这些函数代替了较老的函数
<code>gethostbyname</code>
和<code>gethostbyaddr</code>。<code>getaddrinfo</code>
函数允许将一个主机名和一个服务名映射到一个地址。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">getaddrinfo</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> host, <span class="type">const</span> <span class="type">char</span> *<span class="keyword">restrict</span> service, </span></span><br><span class="line"><span class="params">                <span class="type">const</span> <span class="keyword">struct</span> addrinfo *<span class="keyword">restrict</span> hint, <span class="keyword">struct</span> addrinfo **<span class="keyword">restrict</span> res)</span>;</span><br><span class="line"><span class="comment">//返回值：若成功，返回0；若出错，返回非0错误码</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">freeaddrinfo</span><span class="params">(<span class="keyword">struct</span> addrinfo *ai)</span>;</span><br></pre></td></tr></table></figure>
<p>需要提供主机名、服务名，或者两者都提供。如果仅仅提供一个名字，另外一个必须是一个空指针。主机名可以是一个节点名或点分格式的主机地址。</p>
<p><code>getaddrinfo</code> 函数返回一个链表结构 <code>addrinfo</code>
。可以用 <code>freeaddrinfo</code> 来释放一个或多个这种结构，这取决于用
<code>ai_next</code> 字段链接起来的结构有多少。</p>
<p><code>addrinfo</code> 结构的定义至少包含以下成员：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> &#123;</span></span><br><span class="line">    <span class="type">int</span>　　　　　　　　　ai_flags;　　　　 <span class="comment">/* customize behavior */</span></span><br><span class="line">    <span class="type">int</span>　　　　　　　　　ai_family;　　　　<span class="comment">/* address family */</span></span><br><span class="line">    <span class="type">int</span>　　　　　　　　　ai_socktype;　　　<span class="comment">/* socket type */</span></span><br><span class="line">    <span class="type">int</span>　　　　　　　　　ai_protocol;　　　<span class="comment">/* protocol */</span></span><br><span class="line">    <span class="type">socklen_t</span>　　　　　　ai_addrlen;　　　 <span class="comment">/* length in bytes of address */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span>　　*<span class="title">ai_addr</span>;</span>　　　　　<span class="comment">/* address */</span></span><br><span class="line">    <span class="type">char</span>　　　　　　　　*ai_canonname;　　 <span class="comment">/* canonical name of host */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span>　　*<span class="title">ai_next</span>;</span>　　　　　<span class="comment">/* next in list */</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>可以提供一个可选的 <code>hint</code>
来选择符合特定条件的地址。<code>hint</code>
是一个用于过滤地址的模板，包括
<code>ai_family</code>、<code>ai_flags</code>、<code>ai_protocol</code>
和 <code>ai_socktype</code>
字段。剩余的整数字段必须设置为0，指针字段必须为空。下图总结了
<code>ai_flags</code>
字段中的标志，可以用这些标志来自定义如何处理地址和名字。</p>
<table>
<colgroup>
<col style="width: 18%" />
<col style="width: 81%" />
</colgroup>
<thead>
<tr class="header">
<th>Flag</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>AI_ADDRCONFIG</td>
<td>Query for whichever address type (IPv4 or IPv6) is configured.</td>
</tr>
<tr class="even">
<td>AI_ALL</td>
<td>Look for both IPv4 and IPv6 addresses (used only with
AI_V4MAPPED).</td>
</tr>
<tr class="odd">
<td>AI_CANONNAME</td>
<td>Request a canonical name (as opposed to an alias).</td>
</tr>
<tr class="even">
<td>AI_NUMERICHOST</td>
<td>The host address is specified in numeric format; don’t try to
translate it.</td>
</tr>
<tr class="odd">
<td>AI_NUMERICSERV</td>
<td>The service is specified as a numeric port number; don’t try to
translate it.</td>
</tr>
<tr class="even">
<td>AI_PASSIVE</td>
<td>Socket address is intended to be bound for listening.</td>
</tr>
<tr class="odd">
<td>AI_V4MAPPED</td>
<td>If no IPv6 addresses are found, return IPv4 addresses mapped in IPv6
format.</td>
</tr>
</tbody>
</table>
<p>如果 <code>getaddrinfo</code> 失败，不能使用 <code>perror</code> 或
<code>strerror</code> 来生成错误消息，而是要调用
<code>gai_strerror</code> 将返回的错误码转换成错误消息。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="type">const</span> <span class="type">char</span> *<span class="title function_">gai_strerror</span><span class="params">(<span class="type">int</span> error)</span>;</span><br><span class="line"><span class="comment">// 返回值：指向描述错误的字符串的指针</span></span><br></pre></td></tr></table></figure>
<p><code>getnameinfo</code> 函数将一个地址转换成主机名和服务名。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">getnameinfo</span><span class="params">(<span class="type">const</span> <span class="keyword">struct</span> sockaddr *<span class="keyword">restrict</span> addr, <span class="type">socklen_t</span> alen,</span></span><br><span class="line"><span class="params">        <span class="type">char</span> *<span class="keyword">restrict</span> host, <span class="type">socklen_t</span> hostlen,</span></span><br><span class="line"><span class="params">        <span class="type">char</span> *<span class="keyword">restrict</span> service, <span class="type">socklen_t</span> servlen, <span class="type">int</span> flags)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回非0值</span></span><br></pre></td></tr></table></figure>
<p>套接字地址（<code>addr</code>）被翻译成一个主机名和一个服务名。如果
<code>host</code> 非空，则指向一个长度为 <code>hostlen</code>
字节的缓冲区用于存放返回的主机名。同样，如果 <code>service</code>
非空，则指向一个长度为 <code>servlen</code>
字节的缓冲区用于存放返回的主机名。</p>
<p><code>flags</code>参数提供了一些控制翻译的方式。下图总结了支持的标志。</p>
<table>
<colgroup>
<col style="width: 56%" />
<col style="width: 43%" />
</colgroup>
<thead>
<tr class="header">
<th>Flag</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>NI_DGRAM</td>
<td>The service is datagram based instead of stream based.</td>
</tr>
<tr class="even">
<td>NI_NAMEREQD</td>
<td>If the host name can’t be found, treat this as an error.</td>
</tr>
<tr class="odd">
<td>NI_NOFQDN</td>
<td>Return only the node name portion of the fully qualified domain name
for local hosts.</td>
</tr>
<tr class="even">
<td>NI_NUMERICHOST</td>
<td>Return the numeric form of the host address instead of the
name.</td>
</tr>
<tr class="odd">
<td>NI_NUMERICSCOPE</td>
<td>For IPv6, return the numeric form of the scope ID instead of the
name.</td>
</tr>
<tr class="even">
<td>NI_NUMERICSERV</td>
<td>Return the numeric form of the service address (i.e., the port
number) instead of the name.</td>
</tr>
</tbody>
</table>
<h3 id="将套接字与地址绑定">将套接字与地址绑定</h3>
<p>将一个客户端的套接字绑定上一个地址没有多少新意，可以让系统选一个默认的地址。然而，对于服务器，需要给一个接收客户端请求的服务器套接字绑定上一个众所周知的地址。</p>
<p>使用 <code>bind</code> 函数来绑定地址和套接字。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">bind</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">const</span> <span class="keyword">struct</span> sockaddr *addr, <span class="type">socklen_t</span> len)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>对于使用的地址有以下一些限制。</p>
<ul>
<li>在进程正在运行的计算机上，指定的地址必须有效（本地地址）；不能指定一个其他机器的地址。</li>
<li>地址必须和创建套接字时的地址族所支持的格式相匹配。</li>
<li>地址中的端口号必须不小于1024，除非该进程具有相应的特权（即超级用户）。</li>
<li>通常情况下，只有一个套接字端点可以绑定到特定的地址，不过有些协议允许重复绑定。</li>
</ul>
<p>对于因特网域，如果指定IP地址为
<code>INADDR_ANY</code>（<code>&lt;netinet/in.h&gt;</code>中定义的），套接字端点可以被绑定到所有的系统网络接口上。这意味着可以接收这个系统所安装的任何一个网卡的数据包。</p>
<p>可以调用 <code>getsockname</code>
函数来发现绑定到套接字上的地址（local address）。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">getsockname</span><span class="params">(<span class="type">int</span> sockfd, <span class="keyword">struct</span> sockaddr *<span class="keyword">restrict</span> addr, <span class="type">socklen_t</span> *<span class="keyword">restrict</span> alenp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>调用 <code>getsockname</code> 之前，将 <code>alenp</code>
设置为一个指向整数的指针，该整数指定缓冲区 <code>sockaddr</code>
的长度。返回时，该整数会被设置成返回地址的大小。如果地址和提供的缓冲区长度不匹配，地址会被自动截断而不报错。如果当前没有地址绑定到该套接字，则其结果是未定义的。</p>
<p>如果套接字已经和对等方连接，可以调用 <code>getpeername</code>
函数来找到对方的地址（remote address）。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">getpeername</span><span class="params">(<span class="type">int</span> sockfd, <span class="keyword">struct</span> sockaddr *<span class="keyword">restrict</span> addr, <span class="type">socklen_t</span> *<span class="keyword">restrict</span> alenp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>除了返回对等方的地址，函数 <code>getpeername</code> 和
<code>getsockname</code> 一样。</p>
<hr />
<h2 id="建立连接">建立连接</h2>
<p>如果要处理一个面向连接的网络服务（<code>SOCK_STREAM</code> 或
<code>SOCK_SEQPACKET</code>），那么在开始交换数据以前，需要在请求服务的进程套接字（客户端）和提供服务的进程套接字（服务器）之间建立一个连接。使用
<code>connect</code> 函数来建立连接。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">connect</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">const</span> <span class="keyword">struct</span> sockaddr *addr, <span class="type">socklen_t</span> len)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>在 <code>connect</code> 中指定的地址是想与之通信的服务器地址。如果
<code>sockfd</code> 没有绑定到一个地址，<code>connect</code>
会给调用者绑定一个默认地址。</p>
<p>当尝试连接服务器时，出于一些原因，连接可能会失败。要想一个连接请求成功，要连接的计算机必须是开启的，并且正在运行，服务器必须绑定到一个想与之连接的地址上，并且服务器的等待连接队列中必须有空间。因此，应用程序必须能够处理
<code>connect</code>
返回的错误，这些错误可能是由一些瞬时条件引起的。</p>
<p>如果套接字描述符处于非阻塞模式，那么在连接不能马上建立时，connect将会返回−1并且将
<code>errno</code> 设置为特殊的错误码
<code>EINPROGRESS</code>。应用程序可以使用 <code>poll</code> 或者
<code>select</code> 来判断文件描述符何时可写。如果可写，连接完成。</p>
<p><code>connect</code>
函数还可以用于无连接的网络服务（<code>SOCK_DGRAM</code>）。这看起来有点矛盾，实际上却是一个不错的选择。如果用
<code>SOCK_DGRAM</code> 套接字调用
<code>connect</code>，传送的报文的目标地址会设置成 <code>connect</code>
调用中所指定的地址，这样每次传送报文时就不需要再提供地址。另外，仅能接收来自指定地址的报文。</p>
<p>服务器调用 <code>listen</code> 函数来宣告它愿意接受连接请求。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">listen</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">int</span> backlog)</span>;    <span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>参数 <code>backlog</code> 说的是
等待队列长度。其实际值由系统决定，但上限由
<code>&lt;sys/socket.h&gt;</code> 中的 <code>SOMAXCONN</code> 指定。</p>
<p>一旦队列满，系统就会拒绝多余的连接请求。</p>
<p>一旦服务器调用了
<code>listen</code>，所用的套接字就能接收连接请求。使用
<code>accept</code> 函数来接收连接请求并将其转换为连接。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">accept</span><span class="params">(<span class="type">int</span> sockfd, <span class="keyword">struct</span> sockaddr *<span class="keyword">restrict</span> addr, <span class="type">socklen_t</span> *<span class="keyword">restrict</span> len)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回文件（套接字）描述符；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>函数 <code>accept</code>
所返回的文件描述符是套接字描述符，该描述符连接到调用
<code>connect</code>
的客户端。这个新的套接字描述符和原始套接字（sockfd）具有相同的套接字类型和地址族。传给
<code>accept</code>
的原始套接字没有关联到这个连接，而是继续保持可用状态并接收其他连接请求。</p>
<p>如果不关心客户端标识，可以将参数 <code>addr</code> 和
<code>len</code> 设为 <code>NULL</code>。否则，在调用
<code>accept</code> 之前，将 <code>addr</code>
参数设为足够大的缓冲区来存放地址，并且将 <code>len</code>
指向的整数设为这个缓冲区的字节大小。返回时，<code>accept</code>
会在缓冲区填充客户端的地址，并且更新指向 <code>len</code>
的整数来反映该地址的大小。</p>
<p>如果没有连接请求在等待，<code>accept</code>
会阻塞直到一个请求到来。如果 <code>sockfd</code> 处于非阻塞模式，
<code>accept</code> 会返回−1，并将 <code>errno</code> 设置为
<code>EAGAIN</code> 或 <code>EWOULDBLOCK</code>。</p>
<p>如果服务器调用
<code>accept</code>，并且当前没有连接请求，服务器会阻塞直到一个请求到来。另外，服务器可以使用
<code>poll</code> 或 <code>select</code>
来等待一个请求的到来。在这种情况下，有等待连接请求的套接字会显示为可读状态。</p>
<hr />
<h2 id="数据传输">数据传输</h2>
<p>既然一个套接字端点表示为一个文件描述符，那么只要建立连接，就可以使用
<code>read</code> 和 <code>write</code>
来通过套接字通信。回忆前面所讲，通过在 <code>connect</code>
函数里面设置默认对等地址，数据报套接字也可以被“连接”。在套接字描述符上使用
<code>read</code> 和 <code>write</code>
是非常有意义的，因为这意味着可以将套接字描述符传递给那些原先为处理本地文件而设计的函数。还可以安排将套接字描述符传递给子进程，这些子进程执行的程序对套接字一无所知。</p>
<p>尽管可以通过 <code>read</code> 和 <code>write</code>
交换数据，但这就是这两个函数所能做的一切。如果想指定选项，从多个客户端接收数据包，或者发送带外数据，就需要使用6个为数据传递而设计的套接字函数中的一个。</p>
<p>3个函数用来发送数据，3个用于接收数据。</p>
<p>首先看发送的这一组，最简单的是 <code>send</code>，它和
<code>write</code> 很像，但是
<strong>可以指定标志来改变处理传输数据的方式</strong>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">send</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">const</span> <span class="type">void</span> *buf, <span class="type">size_t</span> nbytes, <span class="type">int</span> flags)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回发送的字节数；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>类似 <code>write</code>，使用 <code>send</code>
时套接字必须已经连接。参数 <code>buf</code> 和 <code>nbytes</code>
的含义与 <code>write</code> 中的一致。</p>
<p>与 <code>write</code> 不同，<code>send</code> 支持第4个参数
<code>flags</code> 。</p>
<p>即使 <code>send</code>
成功返回，也并不表示连接的另一端的进程就一定接收了数据。所能保证的是，当
<code>send</code> 成功时，数据已毫无错误地传送到了网络驱动程序。</p>
<p>对于支持消息边界的协议，如果我们尝试发送一条大于该协议所支持的最大长度的消息，发送操作会失败，且errno会被设置为
<code>EMSGSIZE</code>。而对于字节流协议，发送操作会一直阻塞，直到所有数据都传输完毕。</p>
<p>函数 <code>sendto</code> 和 <code>send</code> 很类似。区别在于
<code>sendto</code> 可以在无连接的套接字上指定一个目标地址。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">sendto</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">const</span> <span class="type">void</span> *buf, <span class="type">size_t</span> nbytes, <span class="type">int</span> flags, <span class="type">const</span> <span class="keyword">struct</span> sockaddr *destaddr, <span class="type">socklen_t</span> destlen)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回发送的字节数；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>对于面向连接的套接字，目标地址是被忽略的，因为连接中隐含了目标地址。对于无连接的套接字，除非先调用
<code>connect</code> 设置了目标地址，否则不能使用
<code>send</code>。<code>sendto</code> 提供了发送消息的另一种方式。</p>
<p>通过套接字发送数据时，还有一个选择。可以调用带有 <code>msghdr</code>
结构的 <code>sendmsg</code> 来指定多重缓冲区传输数据，这和
<code>writev</code> 函数很相似。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">sendmsg</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">const</span> <span class="keyword">struct</span> msghdr *msg, <span class="type">int</span> flags)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回发送的字节数；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>POSIX.1定义了 <code>msghdr</code> 结构，至少有以下成员：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">msghdr</span> &#123;</span></span><br><span class="line">    <span class="type">void</span>　　　　　 *msg_name;　　　　　<span class="comment">/* optional address */</span></span><br><span class="line">    <span class="type">socklen_t</span>　　　 msg_namelen;　　　<span class="comment">/* address size in bytes */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">iovec</span>　*<span class="title">msg_iov</span>;</span>　　　　　 <span class="comment">/* array of I/O buffers */</span></span><br><span class="line">    <span class="type">int</span>　　　　　　 msg_iovlen;　　　  <span class="comment">/* number of elements in array */</span></span><br><span class="line">    <span class="type">void</span>　　　　　 *msg_control;　　　 <span class="comment">/* ancillary data */</span></span><br><span class="line">    <span class="type">socklen_t</span>　　　 msg_controllen;　 <span class="comment">/* number of ancillary bytes */</span></span><br><span class="line">    <span class="type">int</span>　　　　　　 msg_flags;　　　　 <span class="comment">/* flags */</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>函数 <code>recv</code> 和 <code>read</code> 相似，但是
<code>recv</code> 可以指定标志来控制如何接收数据。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">recv</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">void</span> *buf, <span class="type">size_t</span> nbytes, <span class="type">int</span> flags)</span>;</span><br><span class="line"><span class="comment">// 返回值：返回数据的字节长度；若无可用数据或对等方已经按序结束，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>当指定 <code>MSG_PEEK</code>
标志时，可以查看下一个要读取的数据但不真正取走它。当再次调用read或其中一个recv函数时，会返回刚才查看的数据。</p>
<p>对于
<code>SOCK_STREAM</code>套接字，可能收到的数据比请求的少。<code>MSG_WAITALL</code>
标志会抑制这种行为，它会阻止 <code>recv</code>
返回，直到我们请求的所有数据都已收到。对于<code>SOCK_DGRAM</code> 和
<code>SOCK_SEQPACKET</code> 套接字，<code>MSG_WAITALL</code>
标志不会改变其行为，因为这些基于消息的套接字类型在单次读取中就已经返回完整的消息。</p>
<p>如果发送者已经调用 <code>shutdown</code>
来结束传输，或者如果网络协议默认支持有序关闭，且发送方已关闭套接字，那么当接收完所有数据后，<code>recv</code>
将返回0。</p>
<p>如果对发送方的身份感兴趣，可以使用 <code>recvfrom</code>
来获取发送数据的源地址。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">recvfrom</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">void</span> *<span class="keyword">restrict</span> buf, <span class="type">size_t</span> len, <span class="type">int</span> flags,</span></span><br><span class="line"><span class="params">        <span class="keyword">struct</span> sockaddr *<span class="keyword">restrict</span> addr, <span class="type">socklen_t</span> *<span class="keyword">restrict</span> addrlen)</span>;</span><br><span class="line"><span class="comment">// 返回值：返回数据的字节长度；若无可用数据或对等方已经按序结束，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>如果 <code>addr</code>
非空，它将包含数据发送者的套接字端点地址。当调用 <code>recvfrom</code>
时，需要设置 <code>addrlen</code> 参数指向一个整数，该整数包含
<code>addr</code>
所指向的套接字缓冲区的字节长度。返回时，该整数设为该地址的实际字节长度。</p>
<p>因为可以获得发送者的地址，<code>recvfrom</code>
通常用于无连接的套接字。否则，<code>recvfrom</code> 等同于
<code>recv</code>。</p>
<p>为了将接收到的数据送入多个缓冲区，类似于
<code>readv</code>，或者想接收辅助数据，可以使用
<code>recvmsg</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">recvmsg</span><span class="params">(<span class="type">int</span> sockfd, <span class="keyword">struct</span> msghdr *msg, <span class="type">int</span> flags)</span>;</span><br><span class="line"><span class="comment">// 返回值：返回数据的字节长度；若无可用数据或对等方已经按序结束，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p><code>recvmsg</code> 用 <code>msghdr</code>
结构指定接收数据的输入缓冲区。可以设置参数 <code>flags</code> 来改变
<code>recvmsg</code> 的默认行为。返回时，<code>msghdr</code> 结构中的
<code>msg_flags</code> 字段被设为所接收数据的各种特征。（进入
<code>recvmsg</code> 时 <code>msg_flags</code> 被忽略。）</p>
<hr />
<h2 id="套接字选项">套接字选项</h2>
<p>套接字机制提供了两个套接字选项接口来控制套接字行为。一个接口用来设置选项，另一个接口可以查询选项的状态。可以获取或设置以下3种选项。</p>
<p>（1）通用选项，工作在所有套接字类型上。</p>
<p>（2）在套接字层次管理的选项，但是依赖于下层协议的支持。</p>
<p>（3）特定于某协议的选项，每个协议独有的。</p>
<p>Single UNIX
Specification定义了套接字层的选项（上述选项中的前两个选项类型）。</p>
<p>可以使用 <code>setsockopt</code> 函数来设置套接字选项。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">setsockopt</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">int</span> level, <span class="type">int</span> option, <span class="type">const</span> <span class="type">void</span> *val, <span class="type">socklen_t</span> len)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>参数 <code>level</code>
标识了选项应用的协议。如果选项是通用的套接字层次选项，则
<code>level</code> 设置成
<code>SOL_SOCKET</code>。否则，<code>level</code>
设置成控制这个选项的协议编号。对于TCP选项，level是
<code>IPPROTO_TCP</code>，对于IP，level是 <code>IPPROTO_IP</code>。</p>
<p>参数 <code>val</code>
根据选项的不同指向一个数据结构或者一个整数。一些选项是on/off开关。如果整数非0，则启用选项。如果整数为0，则禁止选项。参数
<code>len</code> 指定了 <code>val</code> 指向的对象的大小。</p>
<p>可以使用 <code>getsockopt</code> 函数来查看选项的当前值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">getsockopt</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">int</span> level, <span class="type">int</span> option, <span class="type">void</span> *<span class="keyword">restrict</span> val, <span class="type">socklen_t</span> *<span class="keyword">restrict</span> lenp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>参数 <code>lenp</code> 是一个指向整数的指针。在调用
<code>getsockopt</code>
之前，设置该整数为复制选项缓冲区的长度。如果选项的实际长度大于此值，则选项会被截断。如果实际长度小于此值，那么返回时将此值更新为实际长度。</p>
<hr />
<h2 id="带外数据">带外数据</h2>
<p>带外数据（out-of-band
data）是一些通信协议所支持的可选功能，与普通数据相比，它允许更高优先级的数据传输。带外数据先行传输，即使传输队列已经有数据。TCP
支持带外数据，但是UDP不支持。套接字接口对带外数据的支持很大程度上受TCP带外数据具体实现的影响。</p>
<p>TCP将带外数据称为紧急数据（urgent
data）。<strong>TCP仅支持一个字节的紧急数据</strong>，但是允许紧急数据在普通数据传递机制数据流之外传输。为了产生紧急数据，可以在3个send函数中的任何一个里指定
<code>MSG_OOB</code> 标志。如果带 <code>MSG_OOB</code>
标志发送的字节数超过一个时，最后一个字节将被视为紧急数据字节。</p>
<p>当收到紧急数据时，如果我们已经通过套接字设置了信号生成，就会收到
<code>SIGURG</code> 信号。可以使用 <code>fcntl</code> 的
<code>F_SETOWN</code> 命令来设置套接字的所属权。如果 <code>fcntl</code>
的第三个参数为正数，它指定的是进程ID。如果该参数是除-1之外的负值，则表示进程组ID。因此，可以通过调用（相关函数）来安排我们的进程从套接字接收信号:</p>
<p><code>fcntl(sockfd, F_SETOWN, pid);</code></p>
<p><code>F_GETOWN</code> 命令可以用来获得当前套接字所有权。对于
<code>F_SETOWN</code>
命令，负值代表进程组ID，正值代表进程ID。因此，调用</p>
<p><code>owner = fcntl(sockfd, F_GETOWN, 0);</code></p>
<p>将返回owner，如果owner为正值，则等于配置为接收套接字信号的进程的ID。如果owner为负值，其绝对值为接收套接字信号的进程组的ID。</p>
<p>TCP支持紧急标记（urgent
mark）的概念，即在普通数据流中紧急数据所在的位置。如果采用套接字选项
<code>SO_OOBINLINE</code>，那么可以在普通数据中接收紧急数据。为帮助判断是否已经到达紧急标记，可以使用函数
<code>sockatmark</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sockatmark</span><span class="params">(<span class="type">int</span> sockfd)</span>;</span><br><span class="line"><span class="comment">// 返回值：若在标记处，返回1；若没在标记处，返回0；若出错，返回−1</span></span><br></pre></td></tr></table></figure>
<p>当下一个要读取的字节在紧急标志处时，<code>sockatmark</code>
返回1。</p>
<p>当带外数据出现在套接字读取队列时，<code>select</code>
函数会返回具有待处理异常条件的文件描述符。我们可以选择将紧急数据与普通数据一起接收，也可以在某个
<code>recv</code> 函数中使用 <code>MSG_OOB</code>
标志，以在其他任何队列数据之前接收紧急数据。TCP仅为紧急数据排队一个字节。如果在接收当前紧急字节之前又有一个紧急字节到达，现有的那个字节就会被丢弃。</p>
<hr />
<h2 id="非阻塞和异步io">非阻塞和异步I/O</h2>
<p>通常，<code>recv</code>
函数没有数据可用时会阻塞。同样地，当套接字输出队列没有足够空间来发送消息时，<code>send</code>
函数会阻塞。在套接字非阻塞模式下，行为会改变。在这种情况下，这些函数不会阻塞而是会失败，将
<code>errno</code> 设置为 <code>EWOULDBLOCK</code> 或者
<code>EAGAIN</code> 。当这种情况发生时，可以使用 <code>poll</code> 或
<code>select</code> 来判断能否接收或者传输数据。</p>
<p>Single UNIX
Specification包含通用异步I/O机制的支持。套接字机制有其自己的处理异步I/O的方式，但是这在Single
UNIX
Specification中没有标准化。一些文献把经典的基于套接字的异步I/O机制称为“基于信号的I/O”，区别于Single
UNIX Specification中的通用异步I/O机制。</p>
<p>在基于套接字的异步I/O中，当从套接字中读取数据时，或者当套接字写队列中空间变得可用时，可以安排要发送的信号SIGIO。启用异步I/O是一个两步骤的过程。</p>
<p>（1）确立套接字的所有权，以便信号能够传递到正确的进程。</p>
<p>（2）告知套接字，我们希望它在I/O操作不会阻塞时向我们发出信号。</p>
<p>可以使用3种方式来完成第一个步骤。</p>
<p>（1）在 <code>fcntl</code> 中使用 <code>F_SETOWN</code> 命令。</p>
<p>（2）在 <code>ioctl</code> 中使用 <code>FIOSETOWN</code> 命令。</p>
<p>（3）在 <code>ioctl</code> 中使用 <code>SIOCSPGRP</code> 命令。</p>
<p>要完成第二个步骤，有两个选择。</p>
<p>（1）在 <code>fcntl</code> 中使用 <code>F_SETFL</code>
命令并且启用文件标志 <code>O_ASYNC</code>。</p>
<p>（2）在 <code>ioctl</code> 中使用 <code>FIOASYNC</code> 命令。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Thread Control（十二）</title>
    <url>/2025/08/03/notes-reading/computer/apue/apue-12-thread-control/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第12章 Thread Control
的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="线程属性">线程属性</h2>
<p>pthread
接口允许我们通过设置每个对象关联的不同属性来细调线程和同步对象的行为。通常，管理这些属性的函数都遵循相同的模式。</p>
<p>（1）每个对象与它自己类型的属性对象进行关联（线程与线程属性关联，互斥量与互斥量属性关联，等等）。一个属性对象可以代表多个属性。属性对象对应用程序来说是不透明的。这意味着应用程序并不需要了解有关属性对象内部结构的详细细节，这样可以增强应用程序的可移植性。取而代之的是，需要提供相应的函数来管理这些属性对象。</p>
<p>（2）有一个初始化函数，把属性设置为默认值。</p>
<p>（3）还有一个销毁属性对象的函数。如果初始化函数分配了与属性对象关联的资源，销毁函数负责释放这些资源。</p>
<p>（4）每个属性都有一个函数，用于从属性对象中获取该属性的值。由于该函数在成功时返回0，在失败时返回错误编号，因此会通过将值存储在其中一个参数所指定的内存位置，来将值返回给调用者。</p>
<p>（5）每个属性都有一个设置属性值的函数。在这种情况下，属性值作为参数按值传递。</p>
<p>在调用 <code>pthread_create</code> 函数中，可以使用
<code>pthread_attr_t</code>
结构修改线程默认属性，并把这些属性与创建的线程联系起来。可以使用
<code>pthread_attr_init</code> 函数初始化 <code>pthread_attr_t</code>
结构。在调用 <code>pthread_attr_init</code>
以后，<code>pthread_attr_t</code>
结构所包含的就是操作系统实现支持的所有线程属性的默认值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_attr_init</span><span class="params">(<span class="type">pthread_attr_t</span> *attr)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_attr_destroy</span><span class="params">(<span class="type">pthread_attr_t</span> *attr)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>POSIX.1定义的线程属性汇总于下表中。POSIX.1在线程执行调度选项中定义了额外的属性，旨在支持实时应用程序，但在此不对此进行讨论。</p>
<table>
<thead>
<tr class="header">
<th>Name</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>detachstate</code></td>
<td>detached thread attribute</td>
</tr>
<tr class="even">
<td><code>guardsize</code></td>
<td>guard buffer size in bytes at end of thread stack</td>
</tr>
<tr class="odd">
<td><code>stackaddr</code></td>
<td>lowest address of thread stack</td>
</tr>
<tr class="even">
<td><code>stacksize</code></td>
<td>minimum size in bytes of thread stack</td>
</tr>
</tbody>
</table>
<p>如果对现有的某个线程的终止状态不感兴趣的话，可以使用
<code>pthread_detach</code>
函数让操作系统在线程退出时收回它所占用的资源。</p>
<p>如果在创建线程时就知道不需要了解线程的终止状态，就可以修改
<code>pthread_attr_t</code> 结构中的 <code>detachstate</code>
线程属性，让线程一开始就处于分离状态。可以使用
<code>pthread_attr_setdetachstate</code>
函数将分离状态线程属性设置为两个合法值之一：<code>PTHREAD_CREATE_DETACHED</code>（使线程以分离状态启动）或
<code>PTHREAD_CREATE_JOINABLE</code>（使线程正常启动，这样应用程序可以获取其终止状态）。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_attr_getdetachstate</span><span class="params">(<span class="type">const</span> <span class="type">pthread_attr_t</span> *<span class="keyword">restrict</span> attr, <span class="type">int</span> *detachstate)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_attr_setdetachstate</span><span class="params">(<span class="type">pthread_attr_t</span> *attr, <span class="type">int</span> *detachstate)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>对于遵循Single UNIX Specification 中 XSI
选项的系统来说，支持线程栈属性就是必需的。可以在编译阶段使用
<code>_POSIX_THREAD_ATTR_STACKADDR</code> 和
<code>_POSIX_THREAD_ATTR_STACKSIZE</code>
符号来检查系统是否支持每一个线程栈属性。如果系统定义了这些符号中的一个，就说明它支持相应的线程栈属性。或者，也可以在运行阶段把
<code>_SC_THREAD_ATTR_ STACKADDR</code> 和
<code>_SC_THREAD_ATTR_STACKSIZE</code> 参数传给 <code>sysconf</code>
函数，检查运行时系统对线程栈属性的支持情况。</p>
<p>可以使用函数 <code>pthread_attr_getstack</code> 和
<code>pthread_attr_setstack</code> 对线程栈属性进行管理。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_attr_getstack</span><span class="params">(<span class="type">const</span> <span class="type">pthread_attr_t</span> *<span class="keyword">restrict</span> attr, <span class="type">void</span> **<span class="keyword">restrict</span> stackaddr, <span class="type">size_t</span> *<span class="keyword">restrict</span> stacksize)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_attr_setstack</span><span class="params">(<span class="type">pthread_attr_t</span> *attr, <span class="type">void</span> *stackaddr, <span class="type">size_t</span> stacksize)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>对于进程来说，虚地址空间的大小是固定的。因为进程中只有一个栈，所以它的大小通常不是问题。但对于线程来说，同样大小的虚地址空间必须被所有的线程栈共享。如果应用程序使用了许多线程，以致这些线程栈的累计大小超过了可用的虚地址空间，就需要减少默认的线程栈大小。另一方面，如果线程调用的函数分配了大量的自动变量，或者调用的函数涉及许多很深的栈帧（stack
frame），那么需要的栈大小可能要比默认的大。</p>
<p>如果线程栈的虚拟地址空间耗尽，可以使用 <code>malloc</code> 或者
<code>mmap</code> 来为可替代的栈分配空间，并使用
<code>pthread_attr_setstack</code>
来更改你所创建线程的栈位置。<code>stackaddr</code>
参数指定的地址是用作线程栈的内存范围中可寻址的最低地址，它要按照处理器架构的适当边界对齐。当然，这前提是
<code>malloc</code> 或 <code>mmap</code>
所使用的虚拟地址范围与当前线程栈正在使用的范围不同。</p>
<p>应用程序也可以通过 <code>pthread_attr_getstacksize</code> 和
<code>pthread_attr_setstacksize</code> 函数读取或设置线程属性
<code>stacksize</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_attr_getstacksize</span><span class="params">(<span class="type">const</span> <span class="type">pthread_attr_t</span> *<span class="keyword">restrict</span> attr, <span class="type">size_t</span> *<span class="keyword">restrict</span> stacksize)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_attr_setstacksize</span> <span class="params">(<span class="type">pthread_attr_t</span> *attr, <span class="type">size_t</span> stacksize)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>如果希望改变默认的栈大小，但又不想自己处理线程栈的分配问题，这时使用
<code>pthread_attr_setstacksize</code> 函数就非常有用。</p>
<p>线程属性 <code>guardsize</code>
控制着线程栈末尾之后用以避免栈溢出的扩展内存的大小。这个属性默认值是由具体实现来定义的，但常用值是系统页大小。可以把<code>guardsize</code>
线程属性设置为0，不允许属性的这种特征行为发生：在这种情况下，不会提供缓冲区。同样，如果修改了线程属性
<code>stackaddr</code>，系统就认为我们将自己管理栈，进而使栈警戒缓冲区机制无效，这等同于把
<code>guardsize</code> 线程属性设置为0。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_attr_getguardsize</span><span class="params">(<span class="type">const</span> <span class="type">pthread_attr_t</span> *<span class="keyword">restrict</span> attr, <span class="type">size_t</span> *<span class="keyword">restrict</span> guardsize)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_attr_setguardsize</span><span class="params">(<span class="type">pthread_attr_t</span> *attr, <span class="type">size_t</span> guardsize)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>如果修改了线程属性中的保护大小（guardsize），操作系统可能会将其向上取整为页面大小的整数倍。如果线程的栈指针溢出到保护区域，应用程序会收到错误，可能还会伴有信号。</p>
<p>Single UNIX Specification
定义了其他几个可选的线程属性，旨在供实时应用程序使用。在此不讨论这些属性。线程还有一些未由
<code>pthread_attr_t</code>
结构表示的其他属性：可取消状态和可取消类型。</p>
<hr />
<h2 id="同步属性">同步属性</h2>
<p>就像线程具有属性一样，线程的同步对象也有属性。本节讨论互斥量属性、读写锁属性、条件变量属性和屏障属性。</p>
<hr />
<h3 id="互斥量属性">互斥量属性</h3>
<p>互斥量属性是用 <code>pthread_mutexattr_t</code> 结构表示的。</p>
<p>对于非默认属性，可以用 <code>pthread_mutexattr_init</code> 初始化
<code>pthread_mutexattr_t</code> 结构，用
<code>pthread_mutexattr_destroy</code> 来反初始化。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutexattr_init</span><span class="params">(<span class="type">pthread_mutexattr_t</span> *attr)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutexattr_destroy</span><span class="params">(<span class="type">pthread_mutexattr_t</span> *attr)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p><code>pthread_mutexattr_init</code> 函数将用默认的互斥量属性初始化
<code>pthread_mutexattr_t</code>
结构。值得注意的3个属性是：==<strong>process-shared属性、
robust属性和type属性</strong>==。可以通过检查系统中是否定义了
<code>_POSIX_THREAD_PROCESS_SHARED</code>
符号来判断这个平台是否支持进程共享这个属性，也可以在运行时把
<code>_SC_THREAD_PROCESS_SHARED</code> 参数传给 <code>sysconf</code>
函数进行检查。</p>
<p>在进程中，多个线程可以访问同一个同步对象。这是默认的行为。在这种情况下，<code>process-shared</code>
属性需设置为 <code>PTHREAD_PROCESS_PRIVATE</code>。</p>
<p>存在一些机制，能够让独立进程将相同范围的内存映射到它们各自的地址空间中。多个进程对共享数据的访问通常需要同步，就像多个线程对共享数据的访问也需要同步一样。如果进程共享互斥锁属性被设置为
<code>PTHREAD_PROCESS_SHARED</code>，那么从多个进程之间共享的内存区域分配的互斥锁可被这些进程用于同步。</p>
<p>可以使用 <code>pthread_mutexattr_getpshared</code> 函数查询
<code>pthread_mutexattr_t</code> 结构，得到它的进程共享属性，使用
<code>pthread_mutexattr_setpshared</code> 函数修改进程共享属性。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutexattr_getpshared</span><span class="params">(<span class="type">const</span> <span class="type">pthread_mutexattr_t</span> *<span class="keyword">restrict</span> attr, <span class="type">int</span> *<span class="keyword">restrict</span> pshared)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutexattr_setpshared</span><span class="params">(<span class="type">pthread_mutexattr_t</span> *attr, <span class="type">int</span> pshared)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>进程共享互斥量属性设置为 <code>PTHREAD_PROCESS_PRIVATE</code>
时，允许 <code>pthread</code>
线程库提供更有效的互斥量实现，这在多线程应用程序中是默认的情况。</p>
<p><code>robust</code>
属性与在多个进程间共享的互斥量有关。它旨在解决当进程持有互斥锁时终止的情况下，互斥锁状态的恢复问题。当这种情况发生时，互斥锁会处于锁定状态，且恢复工作十分困难。其他进程中因该锁而阻塞的线程将无限期阻塞。</p>
<p>可以使用 <code>pthread_mutexattr_getrobust</code>
函数获取健壮的互斥量属性的值。可以调用
<code>pthread_mutexattr_setrobust</code>
函数设置健壮的互斥量属性的值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutexattr_getrobust</span><span class="params">(<span class="type">const</span> <span class="type">pthread_mutexattr_t</span> *<span class="keyword">restrict</span> attr, <span class="type">int</span> *<span class="keyword">restrict</span> robust)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutexattr_setrobust</span><span class="params">(<span class="type">pthread_mutexattr_t</span> *attr, <span class="type">int</span> robust)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p><code>robust</code> 属性取值有两种可能的情况。默认值是
<code>PTHREAD_MUTEX_STALLED</code>，这意味着持有互斥量的进程终止时不需要采取特别的动作。这种情况下，使用互斥量后的行为是未定义的，等待该互斥量解锁的应用程序会被有效地
“stalled”。另一个取值是
<code>PTHREAD_MUTEX_ROBUST</code>。当持有锁的另一个进程未先解锁就终止时，此值会使在调用
<code>pthread_mutex_lock</code>
时被阻塞的线程获得该锁，但<code>pthread_mutex_lock</code> 的返回值是
<code>EOWNERDEAD</code>
而非0。应用程序可以使用这个特殊的返回值来表明，它们需要恢复互斥锁所保护的任何状态（如果可能的话）。请注意，在这种情况下，返回<code>EOWNERDEAD</code>
错误并非真正的错误，因为调用者将拥有该锁。</p>
<p>使用 <code>robust</code> 的互斥量改变了使用
<code>pthread_mutex_lock</code>
的方式，因为现在必须检查3个返回值而不是之前的两个：不需要恢复的成功、需要恢复的成功以及失败。</p>
<p>如果应用状态无法恢复，在线程对互斥量解锁以后，该互斥量将处于永久不可用状态。为了避免这样的问题，线程可以调用
<code>pthread_mutex_consistent</code>
函数，指明与该互斥量相关的状态在互斥量解锁之前是一致的。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutex_consistent</span><span class="params">(<span class="type">pthread_mutex_t</span> *mutex)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>如果线程没有先调用 <code>pthread_mutex_consistent</code>
就对互斥量进行了解锁，那么其他试图获取该互斥量的阻塞线程就会得到错误码
<code>ENOTRECOVERABLE</code>。如果发生这种情况，互斥量将不再可用。线程通过提前调用
<code>pthread_mutex_consistent</code>，能让互斥量正常工作，这样它就可以持续被使用。</p>
<p><code>type</code>
互斥量属性控制着互斥量的锁定特性。POSIX.1定义了4种类型。</p>
<ul>
<li><code>PTHREAD_MUTEX_NORMAL</code>
一种标准的互斥锁类型，它不执行任何特殊的错误检查或死锁检测。</li>
<li><code>PTHREAD_MUTEX_ERRORCHECK</code>
一种提供错误检查的互斥锁类型。</li>
<li><code>PTHREAD_MUTEX_RECURSIVE</code>
一种互斥锁类型，允许同一线程多次锁定它，而无需先解锁。递归互斥锁会维护一个锁定计数，并且只有在解锁次数与锁定次数相同时才会被释放。</li>
<li><code>PTHREAD_MUTEX_DEFAULT</code>
一种提供默认特性和行为的互斥锁类型。实现可以自由地将其映射到其他互斥锁类型之一。例如，Linux
3.2.0 将此类型映射为普通互斥锁类型，而 FreeBSD 8.0
则将其映射为错误检查类型。</li>
</ul>
<p>这四种类型的行为在下表中进行了总结。 | Mutex type | 特性 | |—|—| |
<code>PTHREAD_MUTEX_NORMAL</code> | 同一线程再次加锁会死锁 | |
<code>PTHREAD_MUTEX_ERRORCHECK</code> | 错误操作会返回 error | |
<code>PTHREAD_MUTEX_RECURSIVE</code> | 同一线程可以多次加锁 | |
<code>PTHREAD_MUTEX_DEFAULT</code> | 实现相关（通常等同 NORMAL） |</p>
<p>可以用 <code>pthread_mutexattr_gettype</code>
函数得到互斥量类型属性，用 <code>pthread_mutexattr_settype</code>
函数修改互斥量类型属性。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutexattr_gettype</span><span class="params">(<span class="type">const</span> <span class="type">pthread_mutexattr_t</span> * <span class="keyword">restrict</span> attr, <span class="type">int</span> * <span class="keyword">restrict</span> type)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_mutexattr_settype</span><span class="params">(<span class="type">pthread_mutexattr_t</span> * attr, <span class="type">int</span> type)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值: 若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>递归互斥锁在需要将现有的单线程接口适配到多线程环境，但由于兼容性限制而无法更改函数接口时非常有用。然而，使用递归锁可能会很棘手，并且只有在没有其他解决方案的情况下才应该使用它们。</p>
<hr />
<h3 id="读写锁属性">读写锁属性</h3>
<p>读写锁与互斥量类似，也是有属性的。可以用
<code>pthread_rwlockattr_init</code> 初始化
<code>pthread_rwlockattr_t</code> 结构。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_rwlockattr_init</span><span class="params">(<span class="type">pthread_rwlockattr_t</span> *attr)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_rwlockattr_destroy</span><span class="params">(<span class="type">pthread_rwlockattr_t</span> *attr)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>读写锁支持的唯一属性是 <code>process-shared</code>
属性。它与互斥量的进程共享属性是相同的。就像互斥量的进程共享属性一样，有一对函数用于读取和设置读写锁的进程共享属性。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_rwlockattr_getpshared</span><span class="params">(<span class="type">const</span> <span class="type">pthread_rwlockattr_t</span> * <span class="keyword">restrict</span> attr, <span class="type">int</span> *<span class="keyword">restrict</span> pshared)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_rwlockattr_setpshared</span><span class="params">(<span class="type">pthread_rwlockattr_t</span> *attr, <span class="type">int</span> pshared)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>尽管POSIX只定义了一种读写锁属性，但实现可以自由定义其他非标准的属性。</p>
<hr />
<h3 id="条件变量属性">条件变量属性</h3>
<p>Single UNIX
Specification目前定义了条件变量的两个属性：<strong><code>process-shared</code>
属性和 <code>colck</code> 属性</strong>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_condattr_init</span><span class="params">(<span class="type">pthread_condattr_t</span> *attr)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_condattr_destroy</span><span class="params">(<span class="type">pthread_condattr_t</span> *attr)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>与其他的同步属性一样，条件变量支持进程共享属性。它控制着条件变量是可以被单进程的多个线程使用，还是可以被多进程的线程使用。要获取进程共享属性的当前值，可以用
<code>pthread_condattr_getpshared</code> 函数。设置该值可以用
<code>pthread_condattr_setpshared</code> 函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_condattr_getpshared</span><span class="params">(<span class="type">const</span> <span class="type">pthread_condattr_t</span> * <span class="keyword">restrict</span> attr, <span class="type">int</span> *<span class="keyword">restrict</span> pshared)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_condattr_setpshared</span><span class="params">(<span class="type">pthread_condattr_t</span> *attr, <span class="type">int</span> pshared)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p><code>clock</code> 属性控制计算 <code>pthread_cond_timedwait</code>
函数的超时参数（<code>tsptr</code>）时采用的是哪个时钟。可以使用
<code>pthread_condattr_getclock</code> 函数获取可被用于
<code>pthread_cond_timedwait</code> 函数的时钟ID，在使用
<code>pthread_cond_timedwait</code> 函数前需要用
<code>pthread_condattr_t</code> 对象对条件变量进行初始化。可以用
<code>pthread_condattr_setclock</code> 函数对时钟ID进行修改。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_condattr_getclock</span><span class="params">(<span class="type">const</span> <span class="type">pthread_condattr_t</span> * <span class="keyword">restrict</span> attr, <span class="type">clockid_t</span> *<span class="keyword">restrict</span> clock_id)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_condattr_setclock</span><span class="params">(<span class="type">pthread_condattr_t</span> *attr, <span class="type">clockid_t</span> clock_id)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值:若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<hr />
<h3 id="屏障属性">屏障属性</h3>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_barrierattr_init</span><span class="params">(<span class="type">pthread_barrierattr_t</span> *attr)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_barrierattr_destroy</span><span class="params">(<span class="type">pthread_barrierattr_t</span> *attr)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>目前定义的屏障属性只有 <code>process-shared</code>
属性，它控制着屏障是可以被多进程的线程使用，还是只能被初始化屏障的进程内的多线程使用。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_barrierattr_getpshared</span><span class="params">(<span class="type">const</span> <span class="type">pthread_barrierattr_t</span> * <span class="keyword">restrict</span> attr, <span class="type">int</span> *<span class="keyword">restrict</span> pshared)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_barrierattr_setpshared</span><span class="params">(<span class="type">pthread_barrierattr_t</span> *attr, <span class="type">int</span> pshared)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>进程共享属性的值可以是
<code>PTHREAD_PROCESS_SHARED</code>（多进程中的多个线程可用），也可以是
<code>PTHREAD_PROCESS_PRIVATE</code>（只有初始化屏障的那个进程内的多个线程可用）。</p>
<hr />
<h2 id="可重入性">可重入性</h2>
<p>线程在可重入性方面与信号处理器类似。在这两种情况下，多个控制线程都有可能同时调用同一个函数。</p>
<p><strong>如果一个函数可以被多个线程同时安全地调用，我们就说这个函数是线程安全的。</strong></p>
<p>很多函数并不是线程安全的，因为它们返回的数据存放在静态的内存缓冲区中。通过修改接口，要求调用者自己提供缓冲区可以使函数变为线程安全。</p>
<p>如果一个函数对多个线程来说是可重入的，就说这个函数就是线程安全（<code>thread-safe</code>）的。但这并不能说明对信号处理程序来说该函数也是可重入的。如果函数对异步信号处理程序的重入是安全的，那么就可以说函数是异步信号安全（<code>async-signal safe</code>）的。</p>
<p>POSIX.1还提供了以线程安全的方式管理 <code>FILE</code>
对象的方法。可以使用 <code>flockfile</code> 和 <code>ftrylockfile</code>
获取给定 <code>FILE</code>
对象关联的锁。这个锁是递归的：当你占有这把锁的时候，还是可以再次获取该锁，而且不会导致死锁。尽管锁的具体实现方式未作规定，但所有操作FILE对象的标准I/O例程都必须表现得如同在内部调用了
<code>flockfile</code> 和 <code>funlockfile</code> 函数一样。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">ftrylockfile</span><span class="params">(FILE *fp)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；若不能获取锁，返回非0数值</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">flockfile</span><span class="params">(FILE *fp)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">funlockfile</span><span class="params">(FILE *fp)</span>;</span><br></pre></td></tr></table></figure>
<p>尽管从自身内部数据结构的角度来看，标准I/O例程可能被实现为线程安全的，但将锁定机制暴露给应用程序仍然是有用的。这使得应用程序能够将对标准I/O函数的多次调用组合成原子序列。当然，在处理多个FILE对象时，需要留意潜在的死锁问题，并谨慎地安排锁的顺序。</p>
<p>如果标准I/O例程获取了它们自己的锁，那么在进行逐字符I/O操作时，可能会遇到严重的性能下降问题。在这种情况下，我们最终会为读取或写入的每个字符都进行一次锁的获取和释放。为了避免这种开销，基于字符的标准I/O例程有非锁定版本可供使用。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">getchar_unlocked</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">getc_unlocked</span><span class="params">(FILE *fp)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值: 若成功，返回下一个字符；若遇到文件尾或者出错，返回EOF</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">putchar_unlocked</span><span class="params">(<span class="type">int</span> c)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">putc_unlocked</span><span class="params">(<span class="type">int</span> c, FILE *fp)</span>;</span><br><span class="line"><span class="comment">// 两个函数的返回值：若成功，返回c；若出错，返回EOF</span></span><br></pre></td></tr></table></figure>
<p>除非这四个函数被 <code>flockfile</code>（或
<code>ftrylockfile</code>）和 <code>funlockfile</code>
的调用所包围，否则不应调用它们。否则，可能会出现不可预测的结果（即，由多个控制线程对数据进行非同步访问而导致的各种问题）。</p>
<p>一旦锁定了 <code>FILE</code>
对象，你就可以在释放锁之前多次调用这些函数。这样可以将锁定开销分摊到读取或写入的数据量上。</p>
<hr />
<h2 id="线程特定数据">线程特定数据</h2>
<p>线程特定数据，也称为线程私有数据，是一种用于存储和查找与特定线程相关联的数据的机制。我们将这些数据称为线程特定的或线程私有的，原因是我们希望每个线程都能访问自己单独的数据副本，而不必担心与其他线程的访问同步问题。</p>
<p>许多人煞费苦心设计了一个线程模型，该模型有助于共享进程数据和属性。那么，为什么会有人想要推广在这种模型中阻止共享的接口呢？原因有两个。</p>
<p>首先，有时我们需要基于每个线程来维护数据。由于无法保证线程ID是小的、连续的整数，我们不能简单地分配一个包含每个线程数据的数组，并将线程ID用作索引。即便我们可以依赖小的、连续的线程ID，我们也希望能有一些额外的保护措施，以防止一个线程干扰另一个线程的数据。</p>
<p>线程私有数据的第二个原因是提供一种机制，使基于进程的接口能够适应多线程环境。一个明显的例子就是
<code>errno</code>。较旧的接口（在线程出现之前）将 <code>errno</code>
定义为一个整数，在进程的上下文中可以全局访问。系统调用和库例程会在失败时将
<code>errno</code>
设为一种副作用。为了让线程也能够使用那些原本基于进程的系统调用和库例程，<code>errno</code>
被重新定义为线程私有数据。因此，一个线程调用函数设置 <code>errno</code>
时，不会影响进程中其他线程的 <code>errno</code> 值。</p>
<p>回想一下，一个进程中的所有线程都可以访问该进程的整个地址空间。除了使用寄存器之外，一个线程无法阻止另一个线程访问其数据。即使是线程特定数据也是如此。尽管底层实现不会阻止这种访问，但所提供的用于管理线程特定数据的函数通过增加线程从其他线程获取线程特定数据的难度，促进了线程之间的数据分离。</p>
<p>在分配线程特定数据之前，需要创建与该数据关联的键。这个键将用于获取对线程特定数据的访问。使用
<code>pthread_key_create</code> 创建一个键。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_key_create</span><span class="params">(<span class="type">pthread_key_t</span> *keyp, <span class="type">void</span> (*destructor)(<span class="type">void</span> *))</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>创建的键存储在 <code>keyp</code>
指向的内存单元中，这个键可以被进程中的所有线程使用，但每个线程把这个键与不同的线程特定数据地址进行关联。创建新键时，每个线程的数据地址设为空值。</p>
<p>除了创建键以外，<code>pthread_key_create</code>
可以为该键关联一个可选择的析构函数。当线程退出时，如果数据地址已被设置为非空值，析构函数会以该数据地址作为唯一参数被调用。如果析构函数为null，则没有析构函数与该键相关联。当线程正常退出时，无论是通过调用<code>pthread_exit</code>
还是通过返回，析构函数都会被调用。此外，如果线程被取消，析构函数也会被调用，但仅在最后一个清理处理程序返回之后。但是，如果线程调用
<code>exit</code>、<code>_exit</code>、<code>_Exit</code>或<code>abort</code>，或者以其他方式异常退出，则不会调用析构函数。</p>
<p>线程通常使用 <code>malloc</code>
为线程特定数据分配内存。析构函数通常释放已分配的内存。如果线程在没有释放内存之前就退出了，那么这块内存就会丢失，即线程所属进程就出现了内存泄漏。</p>
<p>线程可以为线程特定数据分配多个键，每个键都可以有一个析构函数与它关联。每个键的析构函数可以互不相同，当然所有键也可以使用相同的析构函数。每个操作系统实现可以对进程可分配的键的数量进行限制。</p>
<p>当一个线程退出时，其线程特定数据的析构函数会按实现定义的顺序被调用。析构函数有可能调用另一个函数，该函数会创建新的线程特定数据并将其与键相关联。所有析构函数调用完毕后，系统会检查是否有任何非空的线程特定值与这些键相关联，若有，则会再次调用析构函数。此过程会重复进行，直到该线程的所有键都具有空的线程特定数据值，或者已进行了最多<code>PTHREAD_DESTRUCTOR_ITERATIONS</code>
次尝试为止。</p>
<p>对所有的线程，可以通过调用 <code>pthread_key_delete</code>
来取消键与线程特定数据值之间的关联关系。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_key_delete</span><span class="params">(<span class="type">pthread_key_t</span> key)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>请注意，调用 <code>pthread_key_delete</code>
不会触发与该键相关联的析构函数。要释放与该键的线程特定数据值相关的任何内存，我们需要在应用程序中采取额外的步骤。</p>
<p>需要确保分配的键并不会由于在初始化阶段的竞争而发生变动。下面的代码会导致两个线程都调用
<code>pthread_key_create</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">destructor</span><span class="params">(<span class="type">void</span> *)</span>;</span><br><span class="line"><span class="type">pthread_key_t</span> key;</span><br><span class="line"><span class="type">int</span> init_done = <span class="number">0</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">threadfunc</span><span class="params">(<span class="type">void</span> *arg)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (!init_done) &#123;</span><br><span class="line">        init_done = <span class="number">1</span>;</span><br><span class="line">        err = pthread_key_create(&amp;key, destructor);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>有些线程可能看到一个键值，而其他的线程看到的可能是另一个不同的键值，这取决于系统是如何调度线程的（这里代码的目标是只创建一个全局key）。解决这种竞争的办法是使用
<code>pthread_once</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">pthread_once_t</span> initflag = PTHREAD_ONCE_INIT;</span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_once</span><span class="params">(<span class="type">pthread_once_t</span> *initflag, <span class="type">void</span> (*initfn)(<span class="type">void</span>))</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p><code>initflag</code>
必须是一个非本地变量（如全局变量或静态变量），而且必须初始化为
<code>PTHREAD_ONCE_INIT</code>。</p>
<p>如果每个线程都调用 <code>pthread_once</code>，系统就能保证初始化例程
<code>initfn</code> 只被调用一次，即系统首次调用
<code>pthread_once</code>
时。创建键时避免出现冲突的一个正确方法如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">destructor</span><span class="params">(<span class="type">void</span> *)</span>;</span><br><span class="line"><span class="type">pthread_key_t</span> key;</span><br><span class="line"><span class="type">pthread_once_t</span> init_done = PTHREAD_ONCE_INIT;</span><br><span class="line"><span class="type">void</span> <span class="title function_">thread_init</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    err = pthread_key_create(&amp;key, destructor);</span><br><span class="line">&#125;</span><br><span class="line"><span class="type">int</span> <span class="title function_">threadfunc</span><span class="params">(<span class="type">void</span> *arg)</span></span><br><span class="line">&#123;</span><br><span class="line">    pthread_once(&amp;init_done, thread_init);</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>键一旦创建以后，就可以通过调用 <code>pthread_setspecific</code>
函数把键和线程特定数据关联起来。可以通过
<code>pthread_getspecific</code> 函数获得线程特定数据的地址。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">pthread_getspecific</span><span class="params">(<span class="type">pthread_key_t</span> key)</span>;</span><br><span class="line"><span class="comment">// 返回值：线程特定数据地址；若没有值与该键关联，返回NULL</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_setspecific</span><span class="params">(<span class="type">pthread_key_t</span> key, <span class="type">const</span> <span class="type">void</span> *value)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>如果没有线程特定数据值与键关联，<code>pthread_getspecific</code>
将返回一个空指针，可以用这个返回值来确定是否需要调用
<code>pthread_setspecific</code>。</p>
<hr />
<h2 id="取消选项">取消选项</h2>
<p>有两个线程属性没有包含在 <code>pthread_attr_t</code>
结构中，它们是可取消状态和可取消类型。这两个属性影响着线程在响应
<code>pthread_cancel</code> 函数调用时所呈现的行为。</p>
<p>可取消状态属性可以是 <code>PTHREAD_CANCEL_ENABLE</code>，也可以是
<code>PTHREAD_CANCEL_DISABLE</code>。线程可以通过调用
<code>pthread_setcancelstate</code> 修改它的可取消状态。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_setcancelstate</span><span class="params">(<span class="type">int</span> state, <span class="type">int</span> *oldstate)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p><code>pthread_setcancelstate</code>
把当前的可取消状态设置为state，把原来的可取消状态存储在由
<code>oldstate</code> 指向的内存单元，这两步是一个原子操作。</p>
<p>调用 <code>pthread_cancel</code>
函数并不会等待线程终止。在默认情况下，线程在收到取消请求后会继续执行，直到到达一个取消点。取消点是线程检查自身是否已被取消的地方，如果已被取消，线程就会响应这个请求。POSIX.1标准保证，当线程调用图12.14中列出的任何一个函数时，都会出现取消点。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260306134130236.png"
alt="取消点函数" />
<figcaption aria-hidden="true">取消点函数</figcaption>
</figure>
<p>线程启动时默认的可取消状态是
<code>PTHREAD_CANCEL_ENABLE</code>。当状态设为
<code>PTHREAD_CANCEL_DISABLE</code> 时，对 <code>pthread_cancel</code>
的调用并不会杀死线程。相反，取消请求对这个线程来说还处于挂起状态，当取消状态再次变为
<code>PTHREAD_CANCEL_ENABLE</code>
时，线程将在下一个取消点上对所有挂起的取消请求进行处理。</p>
<p>可以调用 <code>pthread_testcancel</code>
函数在程序中添加自己的取消点。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">pthread_testcancel</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure>
<p>调用 <code>pthread_testcancel</code>
时，如果有某个取消请求正处于挂起状态，而且取消并没有禁用，那么线程就会被取消。但是，如果取消被禁用，
<code>pthread_testcancel</code> 调用就没有任何效果了。</p>
<p>我们所描述的默认的取消类型也称为推迟取消。调用<code>pthread_cancel</code>以后，在线程到达取消点之前，并不会出现真正的取消。可以通过调用<code>pthread_setcanceltype</code>
来修改取消类型。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_setcanceltype</span><span class="params">(<span class="type">int</span> type, <span class="type">int</span> *oldtype)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p><code>pthread_setcanceltype</code> 函数把取消类型设置为
<code>type</code>（类型参数可以是
<code>PTHREADCANCEL_DEFERRED</code>，也可以是
<code>PTHREAD_CANCEL_ASYNCHRONOUS</code>），把原来的取消类型返回到
<code>oldtype</code> 指向的整型单元。</p>
<p>异步取消与延迟取消的不同之处在于，线程可以在任何时候被取消。线程不一定需要到达取消点才能被取消。</p>
<hr />
<h2 id="线程和信号">线程和信号</h2>
<p>即使采用基于进程的范式，处理信号也可能很复杂。引入线程会让情况变得更加复杂。</p>
<p>每个线程都有自己的信号掩码，但信号处置方式由进程中的所有线程共享。因此，单个线程可以阻塞信号，但当一个线程修改与特定信号相关的操作时，所有线程都会共享该操作。所以，如果一个线程选择忽略某个特定信号，另一个线程可以通过恢复默认处置方式或为该信号安装信号处理程序来撤销这一选择。</p>
<p>信号会传递给进程中的单个线程。如果信号与硬件故障相关，该信号通常会发送给其操作导致该事件发生的线程。而其他信号则会传递给任意一个线程。</p>
<p>进程可以使用 <code>sigprocmask</code>
函数来阻止信号发送。然而，<code>sigprocmask</code>
的行为在多线程的进程中并没有定义，线程必须使用
<code>pthread_sigmask</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_sigmask</span><span class="params">(<span class="type">int</span> how, <span class="type">const</span> <span class="type">sigset_t</span> *<span class="keyword">restrict</span> <span class="built_in">set</span>, <span class="type">sigset_t</span> *<span class="keyword">restrict</span> oset)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p><code>pthread_sigmask</code> 函数与 <code>sigprocmask</code>
函数基本相同，不过 <code>pthread_sigmask</code>
工作在线程中，而且失败时返回错误码，不再像 <code>sigprocmask</code>
中那样设置 <code>errno</code> 并返回−1。</p>
<p><code>set</code>
参数包含线程用于修改信号屏蔽字的信号集。how参数可以取下列3个值之一：<code>SIG_BLOCK</code>
把信号集添加到线程信号屏蔽字中，<code>SIG_SETMASK</code>
用信号集替换线程的信号屏蔽字；<code>SIG_UNBLOCK</code>
从线程信号屏蔽字中移除信号集。如果<code>oset</code>
参数不为空，线程之前的信号屏蔽字就存储在它指向的 <code>sigset_t</code>
结构中。线程可以通过把 <code>set</code> 参数设置为
<code>NULL</code>，并把 <code>oset</code> 参数设置为
<code>sigset_t</code>
结构的地址，来获取当前的信号屏蔽字。这种情况中的how参数会被忽略。</p>
<p>线程可以通过调用 <code>sigwait</code> 等待一个或多个信号的出现。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sigwait</span><span class="params">(<span class="type">const</span> <span class="type">sigset_t</span> *<span class="keyword">restrict</span> <span class="built_in">set</span>, <span class="type">int</span> *<span class="keyword">restrict</span> signop)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p><code>set</code> 参数指定了线程等待的信号集。返回时，
<code>signop</code> 指向一个整数的指针，用于存储接收到的信号值。</p>
<p>如果在调用 <code>sigwait</code>
时，指定集中的某个信号处于未决状态，那么 <code>sigwait</code>
将立即返回而不会阻塞。返回之前，<code>sigwait</code>
会从进程的未决信号集中移除该信号。如果实现支持排队信号，且某个信号有多个实例处于未决状态，sigwait只会移除该信号的一个实例，其他实例仍会保持排队状态。</p>
<p>为避免错误行为，线程在调用 <code>sigwait</code>
之前必须阻塞它正在等待的信号。<code>sigwait</code>
函数会自动解除这些信号的阻塞，并等待其中一个信号被送达。在返回之前，<code>sigwait</code>
会恢复线程的信号掩码。如果在调用 <code>sigwait</code>
时信号未被阻塞，那么就会出现一个时间窗口，其中一个信号可能在线程完成对
<code>sigwait</code> 的调用之前被送达该线程。</p>
<p>使用 <code>sigwait</code>
的优势在于，它能够通过允许我们以同步方式处理异步生成的信号来简化信号处理。我们可以通过将信号添加到每个线程的信号掩码中，防止这些信号中断线程。然后，我们可以专门指定某些线程来处理这些信号。这些专用线程可以进行函数调用，而不必担心哪些函数从信号处理器中调用是安全的，因为它们是从正常的线程上下文中被调用的，而不是从中断正常线程执行的传统信号处理器中调用的。</p>
<p>如果多个线程在调用 <code>sigwait</code>
以等待同一个信号时被阻塞，那么当该信号被传递时，只有其中一个线程会从
<code>sigwait</code>返回。如果一个信号被捕获（例如进程通过使用
<code>sigaction</code> 建立了一个信号处理程序），而一个线程正在
<code>sigwait</code>
调用中等待同一信号，那么这时将由操作系统实现来决定以何种方式递送信号。操作系统实现可以让
<code>sigwait</code>
返回，也可以调用信号处理程序，但这两种情况不会同时发生。</p>
<p>要把信号发送给进程，可以调用
<code>kill</code>。要把信号发送给线程，可以调用
<code>pthread_kill</code>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_kill</span><span class="params">(<span class="type">pthread_t</span> thread, <span class="type">int</span> signo)</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>可以传一个0值的signo来检查线程是否存在。如果信号的默认处理动作是终止该进程，那么把信号传递给某个线程仍然会杀死整个进程。</p>
<p>注意，闹钟定时器是进程资源，并且所有的线程共享相同的闹钟。所以，进程中的多个线程不可能互不干扰（或互不合作）地使用闹钟定时器。</p>
<hr />
<h2 id="线程和fork">线程和fork</h2>
<p>当线程调用 <code>fork</code>
时，就为子进程创建了整个进程地址空间的副本。回忆之前讨论的写时复制，子进程与父进程是完全不同的进程，只要两者都没有对内存内容做出改动，父进程和子进程之间还可以<strong>共享内存页</strong>的副本。</p>
<p>子进程通过继承整个地址空间的副本，还从父进程那儿继承了每个互斥量、读写锁和条件变量的状态。如果父进程包含一个以上的线程，子进程在fork返回以后，如果紧接着不是马上调用
<code>exec</code> 的话，就需要清理锁状态。</p>
<p>在子进程内部，只存在一个线程。它是由父进程中调用fork的线程复制而来的。如果父进程中的线程持有任何锁，那么子进程中也会持有相同的锁。问题在于，子进程中并没有持有这些锁的线程的副本，因此子进程无法知道哪些锁被持有以及需要被解锁。</p>
<p>如果子进程在从fork返回后直接调用某个exec函数，这个问题就可以避免。在这种情况下，旧的地址空间会被丢弃，所以锁的状态无关紧要。然而，这并非总能实现，因此如果子进程需要继续处理，我们就需要采用不同的策略。</p>
<p>为避免多线程进程中出现状态不一致的问题，POSIX.1规定，子进程在fork返回后到调用某个exec函数前的这段时间内，只能调用异步信号安全的函数。这限制了子进程在调用exec之前可以执行的操作，但并未解决子进程中的锁状态问题。</p>
<p>要清除锁状态，可以通过调用 <code>pthread_atfork</code>
函数建立fork处理程序。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_atfork</span><span class="params">(<span class="type">void</span> (*prepare)(<span class="type">void</span>), <span class="type">void</span> (*parent)(<span class="type">void</span>), <span class="type">void</span> (*child)(<span class="type">void</span>))</span>;</span><br><span class="line"><span class="comment">// 返回值：若成功，返回0；否则，返回错误编号</span></span><br></pre></td></tr></table></figure>
<p>用 <code>pthread_atfork</code>
函数最多可以安装3个帮助清理锁的函数。<code>prepare fork</code>
处理程序由父进程在fork创建子进程前调用。这个fork处理程序的任务是获取父进程定义的所有锁。<code>parent fork</code>
处理程序是在fork
创建子进程以后、返回之前在父进程上下文中调用的。这个fork处理程序的任务是对
<code>prepare fork</code> 处理程序获取的所有锁进行解锁。
<code>child fork</code> 处理程序在fork返回之前在子进程上下文中调用。与
<code>parent fork</code> 处理程序一样，<code>child fork</code>
处理程序也必须释放 <code>prepare fork</code> 处理程序获取的所有锁。</p>
<p>需要注意的是，这些锁并非像看起来那样被锁定一次、解锁两次。当子地址空间创建时，它会获得父进程定义的所有锁的副本。由于
<code>prepare fork</code>
获取了所有的锁，父进程和子进程中的内存一开始就具有完全相同的内容。当父进程和子进程解锁它们各自的锁“副本”时，会为子进程分配新的内存，并且父进程的内存内容会被复制到子进程的内存中（写时复制），因此我们会面临这样一种情况：看起来就像父进程锁定了其所有的锁副本，而子进程也锁定了其所有的锁副本。父级和子级最终解开了存储在不同内存位置的重复锁，就好像发生了以下一系列事件：</p>
<p>（1）父进程获取所有的锁。</p>
<p>（2）子进程获取所有的锁。</p>
<p>（3）父进程释放它的锁。</p>
<p>（4）子进程释放它的锁。</p>
<p>可以多次调用 <code>pthread_atfork</code>
函数从而设置多套fork处理程序。如果不需要使用其中某个处理程序，可以给特定的处理程序参数传入空指针，它就不会起任何作用了。使用多个fork处理程序时，处理程序的调用顺序并不相同。<code>parent</code>
和<code>child fork</code> 处理程序是以它们注册时的顺序进行调用的，而
<code>prepare fork</code>
处理程序的调用顺序与它们注册时的顺序相反。这样可以允许多个模块注册它们自己的fork处理程序，而且可以保持锁的层次。</p>
<p>我们可以多次调用 <code>pthread_atfork</code>
来安装多组fork处理程序。如果我们不需要使用其中某个处理程序，可以为该特定的处理程序参数传递一个空指针，这样它就不会产生任何效果。当使用多个fork处理程序时，处理程序的调用顺序会有所不同。<code>parent</code>
和 <code>child</code> 的fork处理程序会按照它们注册的顺序被调用，而
<code>prepare</code>
的fork处理程序则会按照与注册顺序相反的顺序被调用。这种顺序安排使得多个模块能够注册各自的fork处理程序，同时仍然遵循锁定层次结构。</p>
<p>例如，假设模块A调用模块B中的函数，而且每个模块有自己的一套锁。如果锁的层次是A在B之前，模块B必须在模块A之前设置它的fork处理程序。当父进程调用fork时，就会执行以下的步骤，假设子进程在父进程之前运行：</p>
<p>（1）调用模块A的prepare fork处理程序获取模块A的所有锁。</p>
<p>（2）调用模块B的prepare fork处理程序获取模块B的所有锁。</p>
<p>（3）创建子进程。</p>
<p>（4）调用模块B中的child fork处理程序释放子进程中模块B的所有锁。</p>
<p>（5）调用模块A中的child fork处理程序释放子进程中模块A的所有锁。</p>
<p>（6）fork函数返回到子进程。</p>
<p>（7）调用模块B中的parent fork处理程序释放父进程中模块B的所有锁。</p>
<p>（8）调用模块A中的parent
fork处理程序来释放父进程中模块A的所有锁。</p>
<p>（9）fork函数返回到父进程。</p>
<hr />
<h2 id="线程和io">线程和I/O</h2>
<p><code>pread</code> 和 <code>pwrite</code>
函数在多线程环境下是非常有用的，因为进程中的所有线程共享相同的文件描述符。</p>
<p>考虑两个线程，在同一时间对同一个文件描述符进行读写操作。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">线程A　　　　　　　　　　　　　　　　　　 线程B</span><br><span class="line">lseek(fd, 300, SEEK_SET);　　　　　　　　lseek(fd, 700, SEEK_SET);</span><br><span class="line">read(fd, buf1, 100);　　　　　　　　　　 read(fd, buf2, 100);</span><br></pre></td></tr></table></figure>
<p>如果线程A执行 <code>lseek</code> 然后线程B在线程A调用
<code>read</code> 之前调用
<code>lseek</code>，那么两个线程最终会读取同一条记录。很显然这不是我们希望的。</p>
<p>为了解决这个问题，可以使用
<code>pread</code>，使偏移量的设定和数据的读取成为一个<strong>原子操作</strong>。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">线程A　　　　　　　　　　　　　　　　　　 线程B</span><br><span class="line">pread(fd, buf1, 100, 300);　　　　　　　 pread(fd, buf2, 100, 700);</span><br></pre></td></tr></table></figure>
<p>使用 <code>pread</code>
可以确保线程A读取偏移量为300的记录，而线程B读取偏移量为700的记录。可以使用
<code>pwrite</code> 来解决并发线程对同一文件进行写操作的问题。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Terminal I/O（十八）</title>
    <url>/2025/09/11/notes-reading/computer/apue/apue-18-terminal-io/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第18章 Terminal I/O
的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="overview">Overview</h2>
<p>终端输入输出有两种模式：</p>
<ol type="1">
<li>规范模式输入处理。在这种模式下，终端输入按行进行处理。终端驱动程序每次读取请求最多返回一行。</li>
<li>非规范模式输入处理。输入字符不会被组合成 lines。</li>
</ol>
<p>如果我们不做任何特殊处理，规范模式就是默认模式。例如，如果 shell
将标准输入重定向到终端，而使用 read 和 write
函数将标准输入复制到标准输出，此时终端处于规范模式，每次 read
调用最多返回一行内容。像 vi
编辑器这类需要操作整个屏幕的程序，则会使用非规范模式，因为它们的命令可能是单个字符，且不以换行符结尾。此外，这类编辑器不希望系统对特殊字符进行处理，因为这些字符可能会与编辑器的命令产生冲突。例如，Control-D
字符通常是终端的文件结束符，但它也是 vi 编辑器中向下滚动半屏的命令。</p>
<p>我们可以将终端设备视为由终端驱动程序控制，该驱动程序通常位于内核中。每个终端设备都有一个输入队列和一个输出队列，如图18.1所示。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260320231628901.png" alt="Logical picture of input and output queues for a terminal device" style="zoom: 67%;" /></p>
<p>大多数UNIX系统在一个名为终端线路规程（terminal line
discipline）的模块中实现所有的规范处理。可以将这个模块视为一个盒子，它位于内核的通用读写函数与实际的设备驱动程序之间（见图18.2）。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260320234331640.png" alt="Terminal line discipline" style="zoom:67%;" /></p>
<p>通过将规范处理隔离在一个单独的模块中，所有终端驱动程序都能一致地支持规范处理。在第19章讨论伪终端时会再回到这部分内容。</p>
<p>可以查看和修改的所有终端设备特性都包含在一个 <code>termios</code>
结构中。该结构在头文件 <code>&lt;termios.h&gt;</code> 中定义。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">termios</span> &#123;</span></span><br><span class="line">    <span class="type">tcflag_t</span> c_iflag;   <span class="comment">/* input flags */</span></span><br><span class="line">    <span class="type">tcflag_t</span> c_oflag;   <span class="comment">/* output flags */</span></span><br><span class="line">    <span class="type">tcflag_t</span> c_cflag;   <span class="comment">/* control flags */</span></span><br><span class="line">    <span class="type">tcflag_t</span> c_lflag;   <span class="comment">/* local flags */</span></span><br><span class="line">    <span class="type">cc_t</span> c_cc[NCCS];    <span class="comment">/* control characters */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p><code>tcflag_t</code>
类型足够大，能够容纳每个标志值，通常被定义为无符号整数（<code>unsigned int</code>）或无符号长整数（<code>unsigned long</code>）。<code>c_cc</code>
数组包含所有我们可以修改的特殊字符。<code>NCCS</code>
是该数组中的元素数量，通常在 15 到 20 之间（因为大多数 UNIX
系统的实现支持的特殊字符数量超过了 POSIX 定义的 11
个）。<code>cc_t</code>
类型足够大，可容纳每个特殊字符，通常为无符号字符（<code>unsigned char</code>）。</p>
<p>下表总结了由Single UNIX
Specification定义的、用于操作终端设备的各种函数。</p>
<table>
<colgroup>
<col style="width: 50%" />
<col style="width: 50%" />
</colgroup>
<thead>
<tr class="header">
<th>Function</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>tcgetattr</code></td>
<td>fetch attributes (<code>termios</code> structure)</td>
</tr>
<tr class="even">
<td><code>tcsetattr</code></td>
<td>set attributes (<code>termios</code> structure)</td>
</tr>
<tr class="odd">
<td><code>cfgetispeed</code></td>
<td>get input speed</td>
</tr>
<tr class="even">
<td><code>cfgetospeed</code></td>
<td>get output speed</td>
</tr>
<tr class="odd">
<td><code>cfsetispeed</code></td>
<td>set input speed</td>
</tr>
<tr class="even">
<td><code>cfsetospeed</code></td>
<td>set output speed</td>
</tr>
<tr class="odd">
<td><code>tcdrain</code></td>
<td>wait for all output to be transmitted</td>
</tr>
<tr class="even">
<td><code>tcflow</code></td>
<td>suspend transmit or receive</td>
</tr>
<tr class="odd">
<td><code>tcflush</code></td>
<td>flush pending input and/or output</td>
</tr>
<tr class="even">
<td><code>tcsendbreak</code></td>
<td>send <code>BREAK</code> character</td>
</tr>
<tr class="odd">
<td><code>tcgetpgrp</code></td>
<td>get foreground process group ID</td>
</tr>
<tr class="even">
<td><code>tcsetpgrp</code></td>
<td>set foreground process group ID</td>
</tr>
<tr class="odd">
<td><code>tcgetsid</code></td>
<td>get process group ID of session leader for controlling TTY</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="special-input-characters">Special Input Characters</h2>
<p>POSIX.1 定义了 11
个在输入时会被特殊处理的字符。各实现还定义了额外的特殊字符。图 18.9
总结了这些特殊字符。</p>
<p>在11个POSIX.1特殊字符中，可以将其中9个更改为几乎任何我们喜欢的值。例外情况是换行符和回车符（分别为
<code>\n</code> 和
<code>\r</code>），或许还有停止（<code>STOP</code>）和开始（<code>START</code>）字符（这取决于具体实现）。要做到这一点，需要修改
<code>termios</code> 结构的 <code>c_cc</code>
数组中的相应条目。该数组中的元素通过名称来引用，每个名称都以V开头（如图18.9中的第三列所示）。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260321104951532.png" alt="Summary of special terminal input characters" style="zoom:67%;" /></p>
<p>POSIX.1允许我们禁用这些字符。如果将 <code>c_cc</code>
数组中某个条目的值设置为 <code>_POSIX_VDISABLE</code>
的值，那么就会禁用相应的特殊字符。</p>
<hr />
<h2 id="getting-and-setting-terminal-attributes">Getting and Setting
Terminal Attributes</h2>
<p>要获取和设置 <code>termios</code> 结构，需要调用两个函数：
<code>tcgetattr</code> 和
<code>tcsetattr</code>。通过这种方式，可以检查和修改各种选项标志以及特殊字符，从而让终端按我们期望的方式运行。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;termios.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">tcgetattr</span><span class="params">(<span class="type">int</span> fd, <span class="keyword">struct</span> termios *termptr)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">tcsetattr</span><span class="params">(<span class="type">int</span> fd, <span class="type">int</span> opt, <span class="type">const</span> <span class="keyword">struct</span> termios *termptr)</span>;</span><br><span class="line"><span class="comment">//Both return: 0 if OK, −1 on error</span></span><br></pre></td></tr></table></figure>
<p>这两个函数都接受一个指向 <code>termios</code>
结构的指针，要么返回当前的终端属性，要么设置终端的属性。由于这两个函数仅对终端设备进行操作，所以如果文件描述符（fd）所指向的不是终端设备，<code>errno</code>
就会被设置为 <code>ENOTTY</code>，并且返回-1。</p>
<p><code>tcsetattr</code> 的 <code>opt</code>
参数可以指定新的终端属性何时生效。该参数被指定为以下常量之一：</p>
<ul>
<li><code>TCSANOW</code> 更改立即生效。</li>
<li><code>TCSADRAIN</code>
所有输出传输完成后才会发生更改。如果我们要更改输出参数，应使用此选项。</li>
<li><code>TCSAFLUSH</code>
所有输出传输完成后才会发生更改。此外，当更改发生时，所有未读取的输入数据都将被丢弃（刷新）。</li>
</ul>
<p><code>tcsetattr</code>
的返回状态在正确使用时可能会令人困惑。该函数如果能够执行任何所请求的操作，即使无法执行所有请求的操作，也会返回OK。如果函数返回OK，我们有责任检查所有请求的操作是否都已执行。这意味着，在我们调用
<code>tcsetattr</code> 来设置所需的属性之后，需要调用
<code>tcgetattr</code>，并将终端的实际属性与所需属性进行比较，以检测任何差异。</p>
<p>首次打开的终端有哪些属性？答案是“取决于具体情况”。有些系统可能会将终端属性初始化为实现定义的值。其他系统可能会保留终端上次使用时的属性值。如果希望确保终端行为符合标准，可以使用
<code>O_TTY_INIT</code> 标志打开终端设备。这将确保当调用
<code>tcgetattr</code> 时，<code>termios</code>
结构的任何非标准部分都会被初始化，这样当更改属性并调用<code>tcsetattr</code>
时，终端就能按预期工作。</p>
<hr />
<h2 id="stty-command"><code>stty</code> Command</h2>
<p>选项可以在程序内部通过 <code>tcgetattr</code> 和
<code>tcsetattr</code>
函数来检查和修改，也可以从命令行（或shell脚本）通过 <code>stty(1)</code>
命令来操作。如果我们在执行这个命令时加上 <code>-a</code>
选项，它就会显示所有的终端选项：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">stty</span> -a</span><br><span class="line">speed 38400 baud; rows 44; columns 206; line = 0;</span><br><span class="line">intr = ^C; quit = ^\; erase = ^?; <span class="built_in">kill</span> = ^U; eof = ^D; eol = &lt;undef&gt;; eol2 = &lt;undef&gt;; swtch = &lt;undef&gt;; start = ^Q; stop = ^S; susp = ^Z; rprnt = ^R; werase = ^W; lnext = ^V; discard = ^O; min = 1; <span class="keyword">time</span> = 0;</span><br><span class="line">-parenb -parodd -cmspar cs8 -hupcl -cstopb cread -clocal -crtscts</span><br><span class="line">-ignbrk -brkint -ignpar -parmrk -inpck -istrip -inlcr -igncr icrnl ixon -ixoff -iuclc -ixany -imaxbel iutf8</span><br><span class="line">opost -olcuc -ocrnl onlcr -onocr -onlret -ofill -ofdel nl0 cr0 tab0 bs0 vt0 ff0</span><br><span class="line">isig icanon iexten <span class="built_in">echo</span> echoe echok -echonl -noflsh -xcase -tostop -echoprt echoctl echoke -flusho -extproc</span><br></pre></td></tr></table></figure>
<p>前面带有连字符的选项名称处于禁用状态。</p>
<hr />
<h2 id="terminal-window-size">Terminal Window Size</h2>
<p>大多数UNIX系统都提供了一种跟踪当前终端窗口大小的方法，并且当窗口大小改变时，内核会通知前台进程组。内核为每个终端和伪终端维护一个
<code>winsize</code> 结构：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">winsize</span> &#123;</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">short</span> ws_row;    <span class="comment">/* rows, in characters */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">short</span> ws_col;    <span class="comment">/* columns, in characters */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">short</span> ws_xpixel; <span class="comment">/* horizontal size, pixels (unused) */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">short</span> ws_ypixel; <span class="comment">/* vertical size, pixels (unused) */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>此结构的规则如下：</p>
<ul>
<li><p>可以使用 <code>TIOCGWINSZ</code> 的 <code>ioctl</code>
来获取此结构的当前值。</p></li>
<li><p>可以使用 <code>TIOCSWINSZ</code> 的 <code>ioctl</code>
将该结构的新值存储到内核中。如果这个新值与内核中存储的当前值不同，就会向前台进程组发送一个
<code>SIGWINCH</code> 信号。</p></li>
<li><p>除了存储该结构的当前值并在值发生变化时生成信号外，内核不会对这个结构进行任何其他操作。对该结构的解读完全取决于应用程序。</p></li>
</ul>
<p>提供此功能是为了在窗口大小改变时通知应用程序（例如vi编辑器）。当应用程序接收到该信号时，它可以获取新的大小并重新绘制屏幕。</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Advanced IPC（十七）</title>
    <url>/2025/09/04/notes-reading/computer/apue/apue-17-advanced-ipc/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第17章 Advanced IPC
的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="unix-domain-sockets">UNIX Domain Sockets</h2>
<p>UNIX域套接字用于与运行在同一台机器上的进程进行通信。尽管因特网域套接字也能用于相同的目的，但UNIX域套接字的效率更高。UNIX域套接字只进行数据复制，它们无需执行协议处理，无需添加或移除网络头部，无需计算校验和，无需生成序列号，也无需发送确认信息。</p>
<p>UNIX域套接字既提供流式接口，也提供数据报接口。不过，UNIX域数据报服务是可靠的——消息既不会丢失，也不会乱序交付。UNIX域套接字就像是套接字和管道的混合体。你既可以将面向网络的套接字接口用于它们，也可以使用
<code>socketpair</code> 函数创建一对未命名的、已连接的UNIX域套接字。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">socketpair</span><span class="params">(<span class="type">int</span> domain, <span class="type">int</span> type, <span class="type">int</span> protocol, <span class="type">int</span> sockfd[<span class="number">2</span>])</span>;  <span class="comment">//Returns: 0 if OK, −1 on error</span></span><br></pre></td></tr></table></figure>
<p>尽管该接口具有足够的通用性，允许将 <code>socketpair</code>
用于其他域，但操作系统通常仅支持UNIX域。</p>
<p>一对相连的UNIX域套接字的作用类似于全双工管道：两端都可以进行读写操作（见图17.1）。将其称为“文件描述符管道（fd-pipes）”，以区别于普通的半双工管道。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260318091729665.png" alt="A socket pair" style="zoom:67%;" /></p>
<p>尽管socketpair函数创建的套接字彼此相连，但各个套接字并没有名称。这意味着它们无法被不相关的进程寻址。</p>
<p>在16.3.4节中，我们了解了如何将一个地址绑定到因特网域套接字。和因特网域套接字一样，UNIX域套接字也可以被命名并用于提供服务。不过，UNIX域套接字所使用的地址格式与因特网域套接字所使用的不同。</p>
<p>UNIX域套接字的地址由 <code>sockaddr_un</code> 结构表示。在Linux
3.2.0和Solaris 10系统中，<code>sockaddr_un</code>
结构在&lt;sys/un.h&gt;头文件中定义如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_un</span> &#123;</span></span><br><span class="line">  <span class="type">sa_family_t</span> sun_family; <span class="comment">/* AF_UNIX */</span></span><br><span class="line">  <span class="type">char</span> sun_path[<span class="number">108</span>]; <span class="comment">/* 路径名 */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>然而，在FreeBSD 8.0和Mac OS X 10.6.8系统中，<code>sockaddr_un</code>
结构的定义为：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_un</span> &#123;</span></span><br><span class="line">  <span class="type">unsigned</span> <span class="type">char</span> sun_len; <span class="comment">/* 套接字地址长度 */</span></span><br><span class="line">  <span class="type">sa_family_t</span> sun_family; <span class="comment">/* AF_UNIX */</span></span><br><span class="line"><span class="type">char</span> sun_path[<span class="number">104</span>]; <span class="comment">/* 路径名 */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p><code>sockaddr_un</code> 结构的 <code>sun_path</code>
成员包含一个路径名。当我们将一个地址绑定到UNIX域套接字时，系统会创建一个类型为
<code>S_IFSOCK</code>
且名称相同的文件。该文件的存在仅作为向客户端公布套接字名称的一种方式。应用程序无法打开此文件，也不能用它进行通信。</p>
<p>如果在尝试绑定相同地址时该文件已存在，绑定请求将会失败。当关闭套接字时，此文件不会被自动删除，因此需要确保在应用程序退出前将其解除链接。</p>
<hr />
<h2 id="passing-file-descriptors">Passing File Descriptors</h2>
<p>在进程间传递打开的文件描述符是一种强大的技术。它能催生出多种客户端-服务器应用程序的设计方式。</p>
<p>这一技术允许一个进程（通常是服务器）完成打开文件所需的所有操作（包括将网络名称转换为网络地址、拨号调制解调器、协商文件锁等细节），然后只需向调用进程返回一个可用于所有I/O函数的描述符。打开文件或设备所涉及的所有细节都对客户端隐藏。</p>
<p>我们必须更明确地说明从一个进程向另一个进程“传递打开的文件描述符”的确切含义。回想一下图3.8，该图展示了两个打开了同一个文件的进程。尽管它们共享同一个v节点，但每个进程都有自己的文件表项。</p>
<p>当我们从一个进程向另一个进程传递打开的文件描述符时，我们希望发送进程和接收进程共享同一个文件表项。图17.11展示了这种理想的结构。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20260318143733952.png" alt="Passing an open file from the top process to the bottom process" style="zoom:67%;" /></p>
<p>从技术上讲，我们是将一个指向打开文件表项的指针从一个进程传递到另一个进程。这个指针会被分配给接收进程中第一个可用的描述符。（说我们在传递一个打开的描述符，容易让人误以为接收进程中的描述符编号与发送进程中的相同，而这通常是不正确的。）两个进程共享一个打开的文件表，这与
<code>fork</code> 之后发生的情况完全一致（回想一下图8.2）。</p>
<p>通常，当一个描述符从一个进程传递到另一个进程时，发送进程在传递描述符后会关闭该描述符。发送方关闭描述符并不会真正关闭文件或设备，因为对于接收进程而言，该描述符仍被视为处于打开状态（即使接收方尚未明确接收该描述符）。</p>
<hr />
<h2 id="open-servers">Open Servers</h2>
<p>通过文件描述符传递，我们现在开发一个打开服务器——这是一个由进程执行以打开一个或多个文件的程序。不过，该服务器不会将文件内容发送回调用进程，而是发送回一个打开的文件描述符。因此，这个打开服务器可以处理任何类型的文件（如设备或套接字），而不仅仅是普通文件。客户端和服务器使用进程间通信（IPC）交换最少的信息：客户端发送的文件名和打开模式，以及服务器返回的描述符。文件内容不会通过进程间通信进行交换。</p>
<p>将服务器设计为一个独立的可执行程序有几个优点（既可以是由客户端执行的程序，也可以是守护进程服务器）：</p>
<ul>
<li>任何客户端都能轻松联系到服务器，这类似于客户端调用库函数。
我们不会在应用程序中硬编码特定服务，而是设计一种可供他人复用的通用工具。</li>
<li>如果需要更换服务器，只会影响一个程序。相反，更新库函数可能需要所有调用该函数的程序都进行更新（即使用链接编辑器重新链接）。共享库可以简化这种更新（见7.7节）。</li>
<li>服务器可以是一个设置用户ID的程序，为其提供客户端所没有的额外权限。请注意，库函数（或共享库函数）无法提供这种功能。</li>
</ul>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>APUE笔记：Pseudo Terminals（十九）</title>
    <url>/2025/09/18/notes-reading/computer/apue/apue-19-pseudo-terminals/</url>
    <content><![CDATA[<p>本文记录《UNIX环境高级编程》第3版 第19章 Pseudo Terminals
的一些知识点。</p>
<span id="more"></span>
<hr />
<h2 id="overview">Overview</h2>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/20260324155839.png" alt="Typical arrangement of processes using a pseudo terminal" style="zoom:67%;" /></p>
<p>图19.1展示了使用伪终端时所涉及的进程的典型排列。</p>
<p>建立伪终端的典型流程如下：</p>
<blockquote>
<ol type="1">
<li>打开一个 PTY master</li>
<li>找到它对应的 slave 设备名</li>
<li>设置 slave 的权限</li>
<li>解锁 slave</li>
<li>fork</li>
<li>子进程建立新会话、把 slave 变成控制终端</li>
<li>子进程把 stdin/stdout/stderr 重定向到 slave</li>
<li>子进程 exec(shell)</li>
<li>父进程继续持有 master，和子进程交互</li>
</ol>
</blockquote>
<p>示意图如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">父进程（终端模拟器/sshd/你写的程序）</span><br><span class="line">        |</span><br><span class="line">        | posix_openpt(O_RDWR)</span><br><span class="line">        v</span><br><span class="line">+------------------------------+</span><br><span class="line">|          PTY master          |  &lt;---- 父进程持有 master fd</span><br><span class="line">+------------------------------+</span><br><span class="line">               ||</span><br><span class="line">               || 内核关联的一对伪终端</span><br><span class="line">               ||</span><br><span class="line">+------------------------------+</span><br><span class="line">|          PTY slave           |  &lt;---- 设备文件 /dev/pts/N</span><br><span class="line">+------------------------------+</span><br><span class="line">               |</span><br><span class="line">               | fork()</span><br><span class="line">      +--------+--------+</span><br><span class="line">      |                 |</span><br><span class="line">      |                 |</span><br><span class="line"> 父进程继续用 master      子进程：</span><br><span class="line"> 与 shell 交互            setsid()</span><br><span class="line">                         打开 slave</span><br><span class="line">                         ioctl(TIOCSCTTY) / 作为控制终端</span><br><span class="line">                         dup2(slave, 0/1/2)</span><br><span class="line">                         exec(&quot;/bin/bash&quot;)</span><br></pre></td></tr></table></figure>
<h3 id="network-login-servers">Network Login Servers</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/20260324162245.png" alt="Arrangement of processes for rlogind server" style="zoom:67%;" /></p>
<h3 id="windowing-system-terminal-emulation">Windowing System Terminal
Emulation</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/20260324162800.png" alt="Arrangement of processes for windowing system" style="zoom:67%;" /></p>
<p>✒️</p>
<p>✒️</p>
<p>✒️</p>
]]></content>
      <categories>
        <category>Reading &amp; Notes</category>
        <category>Computer</category>
      </categories>
      <tags>
        <tag>APUE</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX之快速编译和删除中间文件</title>
    <url>/2025/06/25/tools/latex/usage-skills/latex-bat-makefile-latexmk/</url>
    <content><![CDATA[<p>本文介绍了在 <span class="math inline">\(\LaTeX{}\)</span>
中如何使用bat批处理文件 、Makefile 和 latexmk
来实现快速编译和删除中间文件，保持工作目录的清爽整洁。</p>
<span id="more"></span>
<hr />
<h2 id="批处理文件">批处理文件</h2>
<p>在Windows下可以使用批处理文件来处理，也可以使用Makefile(但需配置make环境)。这里为了操作简单性，在Windows下只介绍如何使用批处理文件来实现快速删除中间文件和快速编译。</p>
<h3 id="快速删除中间文件辅助文件">快速删除中间文件（辅助文件）</h3>
<p>步骤如下：</p>
<ol type="1">
<li><p>新建文本文件命名为<code>clean.bat</code>；</p></li>
<li><p>复制下面的代码放到文本文件中；</p>
<figure class="highlight bat"><table><tr><td class="code"><pre><span class="line">@<span class="built_in">echo</span> off</span><br><span class="line"><span class="built_in">echo</span> Cleaning auxiliary files...</span><br><span class="line"><span class="built_in">del</span> /s /q &quot;*.<span class="built_in">aux</span>&quot; &quot;*.log&quot; &quot;*.out&quot; &quot;*.bbl&quot; &quot;*.blg&quot; &quot;*.toc&quot; &quot;*.lof&quot; &quot;*.lot&quot; &quot;*.synctex.gz&quot;</span><br><span class="line"><span class="built_in">echo</span> Cleaning completed!</span><br><span class="line"><span class="built_in">pause</span></span><br></pre></td></tr></table></figure></li>
<li><p>将文件放入主文件（.tex）所在文件夹中，双击运行即可删除中间文件以及子文件夹中的中间文件。</p></li>
</ol>
<h3 id="快速编译并删除中间文件">快速编译并删除中间文件</h3>
<p>步骤如下：</p>
<ol type="1">
<li><p>新建文本文件命名为<code>compile.bat</code>；</p></li>
<li><p>复制下面的代码放到文本文件中；</p>
<figure class="highlight bat"><table><tr><td class="code"><pre><span class="line">@<span class="built_in">echo</span> off</span><br><span class="line">:: ==============================================</span><br><span class="line">:: LaTeX Compile Automation Script (XeLaTeX + BibTeX)</span><br><span class="line">:: Usage: Drag and drop the .tex file onto this script or manually specify the file name</span><br><span class="line">:: ==============================================</span><br><span class="line"></span><br><span class="line">:: <span class="built_in">set</span> variable</span><br><span class="line"><span class="built_in">set</span> TEX_COMPILER=xelatex</span><br><span class="line"><span class="built_in">set</span> BIB_COMPILER=bibtex</span><br><span class="line"><span class="built_in">set</span> MAX_ATTEMPTS=<span class="number">3</span></span><br><span class="line"><span class="built_in">set</span> LOG_EXTENSIONS=*.<span class="built_in">aux</span> *.log *.out *.bbl *.blg *.toc *.lof *.lot *.synctex.gz</span><br><span class="line"></span><br><span class="line">:: Check whether the file is obtained by dragging</span><br><span class="line"><span class="keyword">if</span> &quot;%~<span class="number">1</span>&quot;==&quot;&quot; (</span><br><span class="line">    <span class="built_in">echo</span> Error: Please drag the .tex file onto this script or manually specify the file name</span><br><span class="line">    <span class="built_in">pause</span></span><br><span class="line">    <span class="keyword">exit</span> /b <span class="number">1</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">:: Extract the file name (without extension)</span><br><span class="line"><span class="built_in">set</span> &quot;TEX_FILE=%~<span class="number">1</span>&quot;</span><br><span class="line"><span class="built_in">set</span> &quot;BASE_NAME=%~n1&quot;</span><br><span class="line"></span><br><span class="line">:: Compile function definition</span><br><span class="line">:compile</span><br><span class="line"><span class="built_in">echo</span>.</span><br><span class="line"><span class="built_in">echo</span> =============== <span class="built_in">Start</span> Compiling... ===============</span><br><span class="line"><span class="built_in">echo</span> Compiling document: <span class="variable">%TEX_FILE%</span></span><br><span class="line"></span><br><span class="line">:: First XeLaTeX Compilation</span><br><span class="line"><span class="built_in">echo</span>.</span><br><span class="line"><span class="built_in">echo</span> [<span class="number">1</span>/<span class="number">4</span>] First <span class="variable">%TEX_COMPILER%</span> compiling...</span><br><span class="line"><span class="variable">%TEX_COMPILER%</span> -interaction=nonstopmode -synctex=<span class="number">1</span> &quot;<span class="variable">%BASE_NAME%</span>.tex&quot;</span><br><span class="line"><span class="keyword">if</span> <span class="variable">%ERRORLEVEL%</span> <span class="keyword">neq</span> <span class="number">0</span> (</span><br><span class="line">    <span class="built_in">echo</span> Error: First <span class="variable">%TEX_COMPILER%</span> Compilation failed</span><br><span class="line">    <span class="keyword">goto</span> error_handling</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">:: BibTeX Compilation</span><br><span class="line"><span class="built_in">echo</span>.</span><br><span class="line"><span class="built_in">echo</span> [<span class="number">2</span>/<span class="number">4</span>] <span class="variable">%BIB_COMPILER%</span> compiling reference...</span><br><span class="line"><span class="variable">%BIB_COMPILER%</span> &quot;<span class="variable">%BASE_NAME%</span>.<span class="built_in">aux</span>&quot;</span><br><span class="line"><span class="keyword">if</span> <span class="variable">%ERRORLEVEL%</span> <span class="keyword">neq</span> <span class="number">0</span> (</span><br><span class="line">    <span class="built_in">echo</span> Warning: <span class="variable">%BIB_COMPILER%</span> There may be issues with the compilation (check the .blg file)</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">:: Second XeLaTeX Compilation</span><br><span class="line"><span class="built_in">echo</span>.</span><br><span class="line"><span class="built_in">echo</span> [<span class="number">3</span>/<span class="number">4</span>] Second <span class="variable">%TEX_COMPILER%</span> compiling...</span><br><span class="line"><span class="variable">%TEX_COMPILER%</span> -interaction=nonstopmode -synctex=<span class="number">1</span> &quot;<span class="variable">%BASE_NAME%</span>.tex&quot;</span><br><span class="line"><span class="keyword">if</span> <span class="variable">%ERRORLEVEL%</span> <span class="keyword">neq</span> <span class="number">0</span> (</span><br><span class="line">    <span class="built_in">echo</span> Error: Second <span class="variable">%TEX_COMPILER%</span> Compilation failed</span><br><span class="line">    <span class="keyword">goto</span> error_handling</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">:: Third XeLaTeX Compilation (Ensure correct cross-referencing)</span><br><span class="line"><span class="built_in">echo</span>.</span><br><span class="line"><span class="built_in">echo</span> [<span class="number">4</span>/<span class="number">4</span>] Third <span class="variable">%TEX_COMPILER%</span> compiling...</span><br><span class="line"><span class="variable">%TEX_COMPILER%</span> -interaction=nonstopmode -synctex=<span class="number">1</span> &quot;<span class="variable">%BASE_NAME%</span>.tex&quot;</span><br><span class="line"><span class="keyword">if</span> <span class="variable">%ERRORLEVEL%</span> <span class="keyword">neq</span> <span class="number">0</span> (</span><br><span class="line">    <span class="built_in">echo</span> Error: Third <span class="variable">%TEX_COMPILER%</span> Compilation failed</span><br><span class="line">    <span class="keyword">goto</span> error_handling</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">:: Cleaning auxiliary files (Optional)</span><br><span class="line"><span class="built_in">echo</span>.</span><br><span class="line"><span class="built_in">echo</span> Cleaning auxiliary files...</span><br><span class="line"><span class="built_in">del</span> /s /q <span class="variable">%LOG_EXTENSIONS%</span> <span class="number">2</span>&gt;<span class="built_in">nul</span></span><br><span class="line"></span><br><span class="line">:: Completed Successfully</span><br><span class="line"><span class="built_in">echo</span>.</span><br><span class="line"><span class="built_in">echo</span> =============== Compilation Completed Successfully ===============</span><br><span class="line"><span class="built_in">echo</span> Final output file: <span class="variable">%BASE_NAME%</span>.pdf</span><br><span class="line"><span class="built_in">start</span> &quot;&quot; &quot;<span class="variable">%BASE_NAME%</span>.pdf&quot;  :: Automatically open the generated PDF</span><br><span class="line"><span class="keyword">goto</span> end</span><br><span class="line"></span><br><span class="line">:: Error Handling</span><br><span class="line">:error_handling</span><br><span class="line"><span class="built_in">set</span> /a ATTEMPTS+=<span class="number">1</span></span><br><span class="line"><span class="keyword">if</span> <span class="variable">%ATTEMPTS%</span> <span class="keyword">lss</span> <span class="variable">%MAX_ATTEMPTS%</span> (</span><br><span class="line">    <span class="built_in">echo</span>.</span><br><span class="line">    <span class="built_in">echo</span> Attempting to fix the issue (attempt <span class="variable">%ATTEMPTS%</span>/<span class="number">3</span>)...</span><br><span class="line">    <span class="keyword">goto</span> compile</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="built_in">echo</span>.</span><br><span class="line"><span class="built_in">echo</span> =============== Compilation Failed ===============</span><br><span class="line"><span class="built_in">echo</span> After <span class="variable">%MAX_ATTEMPTS%</span> attempts, it has <span class="keyword">not</span> been successful. Please check the logs:</span><br><span class="line"><span class="built_in">type</span> &quot;<span class="variable">%BASE_NAME%</span>.log&quot; | <span class="built_in">more</span></span><br><span class="line"><span class="keyword">goto</span> end</span><br><span class="line"></span><br><span class="line">:end</span><br><span class="line"><span class="built_in">pause</span></span><br></pre></td></tr></table></figure></li>
<li><p>将文件放入主文件（.tex）所在文件夹中，拖动主文件到该脚本上，或者命令行运行：<code>compile.bat main.tex</code>。</p></li>
</ol>
<p><strong>注意事项：</strong><code>TEX_COMPILER</code>可更换为<code>pdflatex</code>或<code>lualatex</code>编译命令，且删除辅助文件的命令可选择删除掉，避免每次都需要重新生成中间文件浪费时间。</p>
<hr />
<h2 id="makefile">Makefile</h2>
<h3 id="常规编译方法">常规编译方法</h3>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义编译器</span></span><br><span class="line">LATEX = xelatex</span><br><span class="line"><span class="comment"># 定义需要清理的辅助文件扩展名</span></span><br><span class="line">AUX_FILES = *.aux *.log *.out *.toc *.lof *.lot *.bbl *.blg *.synctex.gz *.fls *.fdb_latexmk *.run.xml *.nav *.snm *.vrb *.bcf *.idx *.ilg *.ind *.xdv</span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取当前目录下所有 .tex 文件（排除带空格的文件名）</span></span><br><span class="line">TEX_FILES = <span class="variable">$(<span class="built_in">wildcard</span> *.tex)</span></span><br><span class="line">PDF_FILES = $(TEX_FILES:.tex=.pdf)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 默认目标：编译所有 .tex 文件</span></span><br><span class="line"><span class="section">all: <span class="variable">$(PDF_FILES)</span></span></span><br><span class="line">  @echo <span class="string">&quot;编译完成！&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 模式规则：从 .tex 生成 .pdf</span></span><br><span class="line"><span class="section">%.pdf: %.tex</span></span><br><span class="line">  <span class="variable">$(LATEX)</span> -interaction=nonstopmode -halt-on-error <span class="variable">$&lt;</span></span><br><span class="line">  <span class="comment">#@# 如果有参考文献，运行 biber 或 bibtex</span></span><br><span class="line">  <span class="comment">#@if [ -f $(basename $&lt;).bcf ]; then biber $(basename $&lt;); fi</span></span><br><span class="line">  <span class="comment">#@if [ -f $(basename $&lt;).aux ]; then bibtex $(basename $&lt;); fi</span></span><br><span class="line">  @<span class="comment"># 第二次编译确保交叉引用正确</span></span><br><span class="line">  <span class="variable">$(LATEX)</span> -interaction=nonstopmode -halt-on-error <span class="variable">$&lt;</span></span><br><span class="line">  <span class="comment">#@# 第三次编译确保所有引用稳定</span></span><br><span class="line">  <span class="comment">#$(LATEX) -interaction=nonstopmode -halt-on-error $&lt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 清理所有辅助文件</span></span><br><span class="line"><span class="section">clean:</span></span><br><span class="line">  @echo <span class="string">&quot;正在清理辅助文件...&quot;</span></span><br><span class="line">  @rm -f <span class="variable">$(AUX_FILES)</span></span><br><span class="line">  @echo <span class="string">&quot;清理完成！&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 编译并清理（常用目标）</span></span><br><span class="line"><span class="section">build: all clean</span></span><br><span class="line"></span><br><span class="line"><span class="meta"><span class="keyword">.PHONY</span>: all clean build</span></span><br></pre></td></tr></table></figure>
<hr />
<h2 id="latexmk">latexmk</h2>
<p><span class="exturl" data-url="aHR0cHM6Ly93d3cuY3Rhbi5vcmcvcGtnL2xhdGV4bWs=">latexmk – Fully automated
LaTeX document generation<i class="fa fa-external-link-alt"></i></span></p>
<p>✒️</p>
<p>✒️</p>
<p>✒️</p>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX使用技巧</category>
      </categories>
      <tags>
        <tag>LaTeX</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX之实现标题自动换行</title>
    <url>/2024/03/23/tools/latex/usage-skills/latex-control-title-newline/</url>
    <content><![CDATA[<p>在进行<span
class="math inline">\(\LaTeX{}\)</span>学位论文排版时，可能会遇到论文标题过长，并且模板没有实现标题换行的功能。这种情况下标题会超出页边距导致页面布局丑陋，可以通过在cls文件里设置命令来实现自动换行。</p>
<span id="more"></span>
<hr />
<p>具体命令如下：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\RequirePackage</span>&#123;lineno&#125;</span><br><span class="line"><span class="keyword">\RequirePackage</span>&#123;setspace&#125;   <span class="comment">%设置行间距离</span></span><br><span class="line"><span class="keyword">\def</span><span class="keyword">\NJUPT@label@title</span>&#123;论文题目：&#125;</span><br><span class="line"><span class="comment">% 其他命令...</span></span><br><span class="line"><span class="comment">% 下面的NJUPT为南京邮电大学缩写，可替换为自己的模板名称</span></span><br><span class="line"><span class="keyword">\newlength</span>&#123;<span class="keyword">\NJUPT@length@titlelinewidth</span>&#125; <span class="comment">% 用于定义一个新的长度变量</span></span><br><span class="line"><span class="keyword">\addtolength</span>&#123;<span class="keyword">\NJUPT@length@titlelinewidth</span>&#125;&#123;240pt&#125; <span class="comment">% 用于控制标题下划线的长度</span></span><br><span class="line"></span><br><span class="line"><span class="comment">% Define a macro for underlined text with adaptive multilines</span></span><br><span class="line"><span class="comment">% #1: The width of text line, not text itself (set by paper)</span></span><br><span class="line"><span class="comment">% #2: The thickness of text line (set by paper)</span></span><br><span class="line"><span class="comment">% #3: The content of text (set by user)</span></span><br><span class="line"><span class="keyword">\def</span><span class="keyword">\NJUPT@macro@exmultiunderline</span><span class="params">#1</span><span class="params">#2</span><span class="params">#3</span>&#123;</span><br><span class="line">    <span class="keyword">\begin</span>&#123;minipage&#125;[t][][t]&#123;<span class="params">#1</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span><span class="keyword">\internallinenumbers</span></span><br><span class="line">        <span class="keyword">\renewcommand</span>&#123;<span class="keyword">\makeLineNumber</span>&#125;&#123;<span class="keyword">\rule</span>[-0.6ex]&#123;<span class="keyword">\textwidth</span>&#125;&#123;<span class="params">#2</span>&#125;<span class="keyword">\hss</span>&#125;</span><br><span class="line">        <span class="params">#3</span><span class="keyword">\par</span></span><br><span class="line">    <span class="keyword">\end</span>&#123;minipage&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">% ...</span></span><br><span class="line">  <span class="keyword">\begin</span>&#123;tabular&#125;[t]&#123;rc&#125;</span><br><span class="line">  &#123;<span class="keyword">\songti</span><span class="keyword">\zihao</span>&#123;-2&#125; <span class="keyword">\NJUPT@label@title</span>&#125; <span class="built_in">&amp;</span> &#123;<span class="keyword">\zihao</span>&#123;3&#125;<span class="keyword">\centering</span> <span class="keyword">\NJUPT@macro@exmultiunderline</span>&#123;<span class="keyword">\NJUPT@length@titlelinewidth</span>&#125;&#123;0.4pt&#125;&#123;<span class="keyword">\NJUPT@value@title</span>&#125;&#125; <span class="comment">% 下划线高度默认0.4pt</span></span><br><span class="line">  <span class="keyword">\end</span>&#123;tabular&#125;</span><br><span class="line">  <span class="keyword">\vfill</span></span><br></pre></td></tr></table></figure>
<p>标题长度超出预设长度时会自动换行，也可以在<code>\title{}</code>
需要换行的位置插入<code>\\</code>，效果如下所示。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250528232727702.png" alt="image-20250528232727702" style="zoom:67%;" /></p>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX使用技巧</category>
      </categories>
      <tags>
        <tag>标题页</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX之图片使用技巧</title>
    <url>/2024/03/29/tools/latex/usage-skills/latex-figure-tutorial/</url>
    <content><![CDATA[<p>本文介绍了在 <span class="math inline">\(\LaTeX{}\)</span>
中插入图片的基本语法和一些常用的技巧，并且给出了一些在排版中常用的模板样例。</p>
<span id="more"></span>
<hr />
<h2 id="基本语法">基本语法</h2>
<p><span class="math inline">\(\LaTeX{}\)</span>
本身不支持插图功能，需要由 <code>graphicx</code> 宏包辅助支持。</p>
<p>使用 <span class="math inline">\(\textrm{latex + dvipdfmx}\)</span>
编译命令时，调用 <code>graphicx</code> 宏包时要指定 <span
class="math inline">\(\textrm{dvipdfmx}\)</span> 选项；而使用 <span
class="math inline">\(\textrm{pdflatex}\)</span> 或 <span
class="math inline">\(\textrm{xelatex}\)</span> 命令编译时不需要。</p>
<p>不同编译命令支持的图片格式种类各异，见下表。这个表格也能解答诸如“为什么
<span class="math inline">\(\textrm{.eps}\)</span> 格式图片在 <span
class="math inline">\(\textrm{pdflatex}\)</span>
编译命令下出错”之类的问题。本表格也再一次说明，使用 <span
class="math inline">\(\textrm{xelatex}\)</span> 命令是最推荐的方式。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115144839982.png" alt="image-20250115144839982" style="zoom:67%;" /></p>
<p>在调用了 <code>graphicx</code> 宏包以后，就可以使用
<code>\includegraphics</code> 命令加载图片了：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\includegraphics</span>[⟨options⟩]&#123;⟨filename⟩&#125;</span><br></pre></td></tr></table></figure>
<p>其中 <code>&lt;filename&gt;</code>
为图片文件名，文件名可能需要用相对路径或绝对路径表示。图片文件的扩展名一般可不写。另外一定要注意，<strong>文件名里既不要有空格也不要有多余的英文点号</strong>，否则宏包在解析文件名的过程中会出错。</p>
<p>另外 <strong><code>graphicx</code> 宏包还提供了
<code>graphicspath</code>
命令，用于声明一个或多个图片文件存放的目录</strong>，使用这些目录里的图片时可不用写路径：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% 假设主要的图片放在 figures 子目录下，标志放在 logo 子目录下</span></span><br><span class="line"><span class="keyword">\graphicspath</span>&#123;&#123;figures/&#125;&#123;logo/&#125;&#125;</span><br></pre></td></tr></table></figure>
<p>在 <code>\includegraphics</code> 命令的可选参数
<code>&lt;options&gt;</code>中可以使用
<code>&lt;key&gt; = &lt;value&gt;</code> 的形式，常用的参数如下：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115145631351.png" alt="image-20250115145631351" style="zoom:67%;" /></p>
<p><code>graphicx</code> 宏包也支持 <span
class="math inline">\(\textrm{draft}\)</span>/<span
class="math inline">\(\textrm{final}\)</span> 选项。当
<code>graphicx</code> 宏包或文档类指定 <span
class="math inline">\(\textrm{draft}\)</span>
选项时，图片将不会被实际插入，取而代之的是一个包含文件名的与原图片等大的方框。(<strong>加快编译过程</strong>)</p>
<hr />
<h2 id="并排和子图表">并排和子图表</h2>
<p>时常有在一个浮动体里面放置多张图的用法。最简单的用法就是直接并排放置，也可以通过分段或者换行命令
<code>\\</code>
排版多行多列的图片。以下为示意代码，效果大致如图所示。</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;figure&#125;[htbp]</span><br><span class="line">  <span class="keyword">\centering</span></span><br><span class="line">  <span class="keyword">\includegraphics</span>[width=...]&#123;...&#125;</span><br><span class="line">  <span class="keyword">\qquad</span></span><br><span class="line">  <span class="keyword">\includegraphics</span>[width=...]&#123;...&#125; <span class="keyword">\\</span>[...pt]</span><br><span class="line">  <span class="keyword">\includegraphics</span>[width=...]&#123;...&#125;</span><br><span class="line">  <span class="keyword">\caption</span>&#123;...&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;figure&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115160703778.png" alt="image-20250115160703778" style="zoom:67%;" /></p>
<p>由于标题是横跨一行的，用 <code>\caption</code>
命令为每个图片单独生成标题就需要借助前文提到的 <code>parbox</code> 或者
<span class="math inline">\(\textrm{minipage}\)</span>
环境，将标题限制在盒子内。效果见图所示。</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;figure&#125;[htbp]</span><br><span class="line">  <span class="keyword">\centering</span></span><br><span class="line">  <span class="keyword">\begin</span>&#123;minipage&#125;&#123;...&#125;</span><br><span class="line">    <span class="keyword">\centering</span></span><br><span class="line">    <span class="keyword">\includegraphics</span>[width=...]&#123;...&#125;</span><br><span class="line">    <span class="keyword">\caption</span>&#123;...&#125;</span><br><span class="line">  <span class="keyword">\end</span>&#123;minipage&#125;</span><br><span class="line">  <span class="keyword">\qquad</span></span><br><span class="line">  <span class="keyword">\begin</span>&#123;minipage&#125;&#123;...&#125;</span><br><span class="line">    <span class="keyword">\centering</span></span><br><span class="line">    <span class="keyword">\includegraphics</span>[width=...]&#123;...&#125;</span><br><span class="line">    <span class="keyword">\caption</span>&#123;...&#125;</span><br><span class="line">  <span class="keyword">\end</span>&#123;minipage&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;figure&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115161039696.png" alt="image-20250115161039696" style="zoom:67%;" /></p>
<p>当需要更进一步，给每个图片定义小标题时，就要用到
<code>subcaption</code>
宏包的功能了。这里仅举一例，效果见图。更详细的用法请参考
<code>subcaption</code> 宏包的帮助文档。</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;figure&#125;[htbp]</span><br><span class="line">  <span class="keyword">\centering</span></span><br><span class="line">  <span class="keyword">\begin</span>&#123;subfigure&#125;&#123;...&#125;</span><br><span class="line">    <span class="keyword">\centering</span></span><br><span class="line">    <span class="keyword">\includegraphics</span>[width=...]&#123;...&#125;</span><br><span class="line">    <span class="keyword">\caption</span>&#123;...&#125;</span><br><span class="line">  <span class="keyword">\end</span>&#123;subfigure&#125;</span><br><span class="line">  <span class="keyword">\qquad</span></span><br><span class="line">  <span class="keyword">\begin</span>&#123;subfigure&#125;&#123;...&#125;</span><br><span class="line">    <span class="keyword">\centering</span></span><br><span class="line">    <span class="keyword">\includegraphics</span>[width=...]&#123;...&#125;</span><br><span class="line">    <span class="keyword">\caption</span>&#123;...&#125;</span><br><span class="line">  <span class="keyword">\end</span>&#123;subfigure&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;figure&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115161220429.png" alt="image-20250115161220429" style="zoom:67%;" /></p>
<p><code>subcaption</code> 依赖上文提到过的 <code>caption</code>
宏包，因此也支持子图表标题样式的定制。并排子图表的功能也可通过
<code>subfig</code> 宏包的 <code>subfloat</code>
命令实现，具体请参考宏包文档。</p>
<hr />
<h2 id="示例模板">示例模板</h2>
<h3 id="并排子图">并排子图</h3>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;graphicx&#125;</span></span><br><span class="line"><span class="comment">% \usepackage&#123;subcaption&#125;</span></span><br><span class="line"><span class="comment">% \usepackage&#123;float&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;figure&#125;[H]</span><br><span class="line">    <span class="keyword">\centering</span></span><br><span class="line">    <span class="comment">% 第一行：2个子图</span></span><br><span class="line">    <span class="keyword">\begin</span>&#123;subfigure&#125;[b]&#123;0.48<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="keyword">\caption</span>&#123;子图1&#125;</span><br><span class="line">    <span class="keyword">\end</span>&#123;subfigure&#125;</span><br><span class="line">    <span class="keyword">\hfill</span> <span class="comment">% 填充水平间距</span></span><br><span class="line">    <span class="keyword">\begin</span>&#123;subfigure&#125;[b]&#123;0.48<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="keyword">\caption</span>&#123;子图2&#125;</span><br><span class="line">    <span class="keyword">\end</span>&#123;subfigure&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">% 第二行：2个子图</span></span><br><span class="line">    <span class="keyword">\begin</span>&#123;subfigure&#125;[b]&#123;0.48<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="keyword">\caption</span>&#123;子图3&#125;</span><br><span class="line">    <span class="keyword">\end</span>&#123;subfigure&#125;</span><br><span class="line">    <span class="keyword">\hfill</span></span><br><span class="line">    <span class="keyword">\begin</span>&#123;subfigure&#125;[b]&#123;0.48<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="keyword">\caption</span>&#123;子图4&#125;</span><br><span class="line">    <span class="keyword">\end</span>&#123;subfigure&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">% 第三行：单独居中的子图</span></span><br><span class="line">    <span class="comment">% \vspace&#123;0.5cm&#125; % 调整垂直间距</span></span><br><span class="line">    <span class="keyword">\centering</span></span><br><span class="line">    <span class="keyword">\begin</span>&#123;subfigure&#125;[c]&#123;0.48<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="keyword">\caption</span>&#123;子图5&#125;</span><br><span class="line">    <span class="keyword">\end</span>&#123;subfigure&#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">\caption</span>&#123;整体标题&#125;</span><br><span class="line">    <span class="keyword">\label</span>&#123;fig:my<span class="built_in">_</span>figure&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;figure&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250808203757692.png" alt="image-20250808203757692" style="zoom: 80%;" /></p>
<h3 id="上一下二">上一下二</h3>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;graphicx&#125;</span></span><br><span class="line"><span class="comment">% \usepackage&#123;subcaption&#125;</span></span><br><span class="line"><span class="comment">% \usepackage&#123;float&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;figure&#125;[H]</span><br><span class="line">    <span class="keyword">\centering</span></span><br><span class="line">  <span class="keyword">\begin</span>&#123;subfigure&#125;[b]&#123;0.8<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">    <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">    <span class="keyword">\caption</span>&#123;Diagram.&#125;<span class="keyword">\label</span>&#123;fig:1a&#125;</span><br><span class="line">    <span class="keyword">\end</span>&#123;subfigure&#125;</span><br><span class="line">    <span class="comment">%\vspace&#123;0.5cm&#125; % 调整垂直间距</span></span><br><span class="line">    <span class="keyword">\begin</span>&#123;subfigure&#125;[b]&#123;0.48<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">    <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">    <span class="keyword">\caption</span>&#123;Grid.&#125;<span class="keyword">\label</span>&#123;fig:1b&#125;</span><br><span class="line">    <span class="keyword">\end</span>&#123;subfigure&#125;</span><br><span class="line">    <span class="keyword">\hfill</span></span><br><span class="line">    <span class="keyword">\begin</span>&#123;subfigure&#125;[b]&#123;0.48<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">    <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">    <span class="keyword">\caption</span>&#123;Arrangement.&#125;<span class="keyword">\label</span>&#123;fig:1c&#125;</span><br><span class="line">    <span class="keyword">\end</span>&#123;subfigure&#125;</span><br><span class="line">    <span class="keyword">\caption</span>&#123;Structural.&#125;</span><br><span class="line">    <span class="keyword">\label</span>&#123;fig:1&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;figure&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250808203945509.png" alt="image-20250808203945509" style="zoom: 80%;" /></p>
<h3 id="边注旋转加图片">边注旋转加图片</h3>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;graphicx&#125;</span></span><br><span class="line"><span class="comment">% \usepackage&#123;subcaption&#125;</span></span><br><span class="line"><span class="comment">% \usepackage&#123;float&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;figure&#125;[H]</span><br><span class="line">    <span class="keyword">\centering</span></span><br><span class="line">    <span class="comment">% 三行四列子图布局</span></span><br><span class="line">    <span class="keyword">\begin</span>&#123;tabular&#125;&#123;rcccc&#125;</span><br><span class="line">      <span class="comment">% 第一行</span></span><br><span class="line">      <span class="keyword">\rotatebox</span>&#123;90&#125;&#123;10dB&#125; <span class="built_in">&amp;</span></span><br><span class="line">      <span class="keyword">\begin</span>&#123;subfigure&#125;[c]&#123;0.23<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125; <span class="comment">% 替换为你的图片</span></span><br><span class="line">        <span class="comment">%\caption&#123;子图1&#125;</span></span><br><span class="line">        <span class="keyword">\label</span>&#123;fig:sub1&#125;</span><br><span class="line">      <span class="keyword">\end</span>&#123;subfigure&#125; <span class="built_in">&amp;</span></span><br><span class="line">      <span class="keyword">\begin</span>&#123;subfigure&#125;[c]&#123;0.23<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="comment">%\caption&#123;子图2&#125;</span></span><br><span class="line">        <span class="keyword">\label</span>&#123;fig:sub2&#125;</span><br><span class="line">      <span class="keyword">\end</span>&#123;subfigure&#125; <span class="built_in">&amp;</span></span><br><span class="line">      <span class="keyword">\begin</span>&#123;subfigure&#125;[c]&#123;0.23<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="comment">%\caption&#123;子图3&#125;</span></span><br><span class="line">        <span class="keyword">\label</span>&#123;fig:sub3&#125;</span><br><span class="line">      <span class="keyword">\end</span>&#123;subfigure&#125; <span class="built_in">&amp;</span></span><br><span class="line">      <span class="keyword">\begin</span>&#123;subfigure&#125;[c]&#123;0.23<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="comment">%\caption&#123;子图4&#125;</span></span><br><span class="line">        <span class="keyword">\label</span>&#123;fig:sub4&#125;</span><br><span class="line">      <span class="keyword">\end</span>&#123;subfigure&#125; <span class="keyword">\\</span></span><br><span class="line">      </span><br><span class="line">      <span class="comment">% 第二行</span></span><br><span class="line">      <span class="keyword">\rotatebox</span>&#123;90&#125;&#123;10dB&#125; <span class="built_in">&amp;</span></span><br><span class="line">      <span class="keyword">\begin</span>&#123;subfigure&#125;[c]&#123;0.23<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="comment">%\caption&#123;子图5&#125;</span></span><br><span class="line">        <span class="keyword">\label</span>&#123;fig:sub5&#125;</span><br><span class="line">      <span class="keyword">\end</span>&#123;subfigure&#125; <span class="built_in">&amp;</span></span><br><span class="line">      <span class="keyword">\begin</span>&#123;subfigure&#125;[c]&#123;0.23<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="comment">%\caption&#123;子图6&#125;</span></span><br><span class="line">        <span class="keyword">\label</span>&#123;fig:sub6&#125;</span><br><span class="line">      <span class="keyword">\end</span>&#123;subfigure&#125; <span class="built_in">&amp;</span></span><br><span class="line">      <span class="keyword">\begin</span>&#123;subfigure&#125;[c]&#123;0.23<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="comment">%\caption&#123;子图7&#125;</span></span><br><span class="line">        <span class="keyword">\label</span>&#123;fig:sub7&#125;</span><br><span class="line">      <span class="keyword">\end</span>&#123;subfigure&#125; <span class="built_in">&amp;</span></span><br><span class="line">      <span class="keyword">\begin</span>&#123;subfigure&#125;[c]&#123;0.23<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="comment">%\caption&#123;子图8&#125;</span></span><br><span class="line">        <span class="keyword">\label</span>&#123;fig:sub8&#125;</span><br><span class="line">      <span class="keyword">\end</span>&#123;subfigure&#125; <span class="keyword">\\</span></span><br><span class="line">      </span><br><span class="line">      <span class="comment">% 第三行</span></span><br><span class="line">      <span class="keyword">\rotatebox</span>&#123;90&#125;&#123;10dB&#125; <span class="built_in">&amp;</span></span><br><span class="line">      <span class="keyword">\begin</span>&#123;subfigure&#125;[c]&#123;0.23<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="keyword">\caption</span>&#123;a&#125;</span><br><span class="line">        <span class="keyword">\label</span>&#123;fig:sub9&#125;</span><br><span class="line">      <span class="keyword">\end</span>&#123;subfigure&#125; <span class="built_in">&amp;</span></span><br><span class="line">      <span class="keyword">\begin</span>&#123;subfigure&#125;[c]&#123;0.23<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="keyword">\caption</span>&#123;b&#125;</span><br><span class="line">        <span class="keyword">\label</span>&#123;fig:sub10&#125;</span><br><span class="line">      <span class="keyword">\end</span>&#123;subfigure&#125; <span class="built_in">&amp;</span></span><br><span class="line">      <span class="keyword">\begin</span>&#123;subfigure&#125;[c]&#123;0.23<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="keyword">\caption</span>&#123;c&#125;</span><br><span class="line">        <span class="keyword">\label</span>&#123;fig:sub11&#125;</span><br><span class="line">      <span class="keyword">\end</span>&#123;subfigure&#125; <span class="built_in">&amp;</span></span><br><span class="line">      <span class="keyword">\begin</span>&#123;subfigure&#125;[c]&#123;0.23<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\includegraphics</span>[width=<span class="keyword">\linewidth</span>]&#123;tree&#125;</span><br><span class="line">        <span class="keyword">\caption</span>&#123;d&#125;</span><br><span class="line">        <span class="keyword">\label</span>&#123;fig:sub12&#125;</span><br><span class="line">      <span class="keyword">\end</span>&#123;subfigure&#125;</span><br><span class="line">    <span class="keyword">\end</span>&#123;tabular&#125;</span><br><span class="line">    <span class="keyword">\caption</span>&#123;边注旋转加图片&#125;</span><br><span class="line">    <span class="keyword">\label</span>&#123;fig:grid&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;figure&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250808204307036.png" alt="image-20250808204307036" style="zoom: 80%;" /></p>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX使用技巧</category>
      </categories>
      <tags>
        <tag>图片</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX之如何加快编译速度</title>
    <url>/2024/12/17/tools/latex/usage-skills/latex-how-to-accelerate-the-compilation-process/</url>
    <content><![CDATA[<p>本文首先介绍了在 LaTeX
大文件编译过程中影响编译速度的一些原因，然后根据这些原因介绍了相关的方法来加快编译速度以提高效率，最后结合实验数据对比分析了这些方法的实际提升效果。</p>
<span id="more"></span>
<hr />
<h2 id="什么拖慢了编译速度">什么拖慢了编译速度</h2>
<p>影响 LaTeX
编译速度原因主要可以归纳为两方面，一方面是文档外部的，这方面主要与编译
LaTeX
的环境和机器有关。另一方面是文档内部的，这一方面主要与文件的反复读写各种中间文件，加载图片和字体等有关。</p>
<p>文档外部的原因主要有：</p>
<ul>
<li>LaTeX编译的操作系统</li>
<li>编译LaTeX的电脑配置</li>
<li>本地编译还是云端</li>
</ul>
<p>首先是编译的操作系统，LaTeX 是“为 Unix 设计的工具链”，Windows
能跑，但不是它最擅长的舞台。</p>
<p>类Unix系统（Linux、Mac等）编译速度快于Windows。由于LaTeX编译过程中，需要创建很多短生命周期进程（<code>pdflatex</code>，<code>bibtex/biber</code>，<code>makeindex</code>，<code>python(minted)</code>、<code>repstopdf</code>、<code>epstopdf</code>、<code>synctex</code>等），类Unix的进程创建成本低于Windows，其次在编译过程中会读写很多中间文件<code>.aux</code>，<code>.toc</code>，<code>.log</code>，<code>.out</code>，<code>.bbl</code>，<code>.bcf</code>等，类Unix的文件I/O速度优于Windows。总体来说，由于类Unix系统的内核设计，其在编译LaTeX文件时速度远胜于Windows。</p>
<p>其次，电脑的硬件配置也会对编译速度有影响。因此，如果电脑的CPU性能比较一般，内存较小或读写速度较慢，会降低编译速度。</p>
<p>一般云端的网页编译（Overleaf、TeXPage等）通常是架构在服务器的服务，服务器基本上也都采用Linux作为操作系统，其速度通常也是比本地的Windows系统要快。然而云端编译存在网络波动、付费、编译时长限制、隐私性等这样那样的问题，在实际排版中需要考虑自身情况来选择是否使用。（在我看来，云端相比本地唯一的优势在于多人协同支持的较好。）</p>
<p>文档内部的原因主要有：</p>
<ul>
<li>反复多轮编译</li>
<li>图片与图形</li>
<li>字体系统（XeLaTeX / LuaLaTeX）</li>
</ul>
<p>最主要原因是 LaTeX
<strong>天生是多遍编译系统</strong>，在输出最终pdf前会反复编译文件以获取之前不知道的位置的信息。</p>
<p>加载图片与图形需要大量的I/O，并且不缓存，即每轮编译都要重新加载图片，编译速度自然就会被严重降低。</p>
<p><code>pdfLaTeX</code>使用的是TeX字体，字体加载几乎“零成本”，而XeLaTeX
/
LuaLaTeX使用的是系统字体，字体加载成本较高，但是功能也更强大，可支持多语言（中文/日文/韩文等）。</p>
<hr />
<h2 id="如何加快编译">如何加快编译</h2>
<p>针对这些影响编译速度的原因，可以选择相应的加快编译速度的方法。</p>
<h3 id="更换操作系统">更换操作系统</h3>
<p>由于类Unix系统的内核设计，在编译LaTeX文件时速度远胜于Windows，因此可以使用Linux或Mac，或者使用WSL2（Windows下的Linux子系统）。也可以在云端编译（Overleaf、TeXPage等）（电脑性能一般最好使用云端）。</p>
<h3 id="使用-latexmk-编译">使用 latexmk 编译</h3>
<p><code>latexmk</code> 在思想上就是 LaTeX 世界的 <code>make</code>，
但它内置了 LaTeX
的“依赖规则和收敛逻辑”，自动多次调用编译器直到稳定，不用再像<code>make</code>需要手写依赖关系。核心思想是增量编译（减少编译次数），通过检测文档是否“收敛”，只执行“必需的最少编译轮数”。</p>
<h3 id="draft-模式">draft 模式</h3>
<p>在有大量图片的情况下,编译速度变慢, 可采用 <code>draft</code>
模式，最终版再取消该模式。</p>
<p>对 <code>documentclass</code> 或 <code>graphicx</code> 加一个
<code>draft</code> 选项: <code>\documentclass[draft]{article}</code> 或
<code>\usepackage[draft]{graphicx}</code>。</p>
<p><code>draft</code> 模式下,如果希望看某张图片,可以单独对该图片取消
<code>draft</code> 模式:
<code>\includegraphics[draft=false]{image.pdf}</code>。</p>
<h3 id="图片格式选择">图片格式选择</h3>
<p>尽量选用
<code>pdf</code>或<code>eps</code>格式，速度快又清晰。<code>png</code>和<code>jpeg</code>格式编译时会对图片解码和重解码，<code>svg</code>会调用外部程序转换为<code>pdf</code>，而<code>pdf</code>格式直接嵌入对象，因此速度比其他格式快。</p>
<h3 id="取消压缩最终版本再压缩">取消压缩,最终版本再压缩</h3>
<p>取消 LaTeX 编译器对 PDF
的内容压缩和对象压缩，生成一个体积大但结构清晰、可读的
PDF，主要用于调试。</p>
<p>方法是在 <code>tex</code> 文档开头加入如下代码：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% XeTeX</span></span><br><span class="line"><span class="keyword">\special</span>&#123;dvipdfmx:config z 0&#125; <span class="comment">%取消PDF压缩，加快速度，最终版本注释掉此行</span></span><br></pre></td></tr></table></figure>
<p>或或自动判断使用以上两种的哪个方式：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\ifx</span><span class="keyword">\XeTeXversion</span><span class="keyword">\undefined</span></span><br><span class="line">  <span class="comment">% 非 XeTeX（pdfTeX / LuaTeX）</span></span><br><span class="line">  <span class="keyword">\pdfcompresslevel</span>=0</span><br><span class="line">  <span class="keyword">\pdfobjcompresslevel</span>=0</span><br><span class="line"><span class="keyword">\else</span></span><br><span class="line">  <span class="comment">% XeTeX → dvipdfmx</span></span><br><span class="line">  <span class="keyword">\special</span>&#123;dvipdfmx:config z 0&#125;</span><br><span class="line"><span class="keyword">\fi</span></span><br></pre></td></tr></table></figure>
<p>最终版本要注释或删掉这些优化命令。</p>
<hr />
<h2 id="实验">实验</h2>
<h3 id="电脑配置">电脑配置</h3>
<p>CPU：12th Gen Intel® Core™ i7-12700H 核心数：14 逻辑处理器：20
频率：2.3GHz</p>
<p>内存：DDR5 32G 频率：4800MT/s</p>
<h3 id="实验结果">实验结果</h3>
<p>实验文档：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">lshort-zh-cn<i class="fa fa-external-link-alt"></i></span>，编译方式为<code>xelatex</code>，不同系统环境编译时间比较：</p>
<table>
<thead>
<tr class="header">
<th>Windows11</th>
<th>VMware下Ubuntu24.04</th>
<th>WSL2下Ubuntu24.04</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>123.096s</td>
<td>9.799s</td>
<td>9.456s</td>
</tr>
</tbody>
</table>
<p>可以看出切换系统有较高的效率提升。</p>
<p>由于 <span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">lshort-zh-cn<i class="fa fa-external-link-alt"></i></span>
没有导入外部图片，所以手动插入10张外部图片进行测试。这10张图片首先是<code>png</code>格式，每张图片大小约5M，然后转换为<code>pdf</code>格式，每张大小约1.2M。</p>
<p>不同条件下编译时间大致如下：</p>
<table>
<colgroup>
<col style="width: 31%" />
<col style="width: 34%" />
<col style="width: 34%" />
</colgroup>
<thead>
<tr class="header">
<th>Windows11</th>
<th>VMware下Ubuntu24.04</th>
<th>WSL2下Ubuntu24.04</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>png</code>格式：296.928s</td>
<td><code>png</code>格式：9.956s</td>
<td><code>png</code>格式：11.588s</td>
</tr>
<tr class="even">
<td><code>pdf</code>格式：297.779s</td>
<td><code>pdf</code>格式：9.953s</td>
<td><code>pdf</code>格式：10.926s</td>
</tr>
<tr class="odd">
<td>辅助文件存在时<code>latexmk</code>：90s</td>
<td>辅助文件存在时<code>latexmk</code>：3.729s</td>
<td>辅助文件存在时<code>latexmk</code>：4.038s</td>
</tr>
<tr class="even">
<td><code>draft</code>模式：293.089s</td>
<td><code>draft</code>模式：9.763s</td>
<td><code>draft</code>模式：9.811s</td>
</tr>
<tr class="odd">
<td><code>pdf</code>+取消压缩：295.689s</td>
<td><code>pdf</code>+取消压缩：9.817s</td>
<td><code>pdf</code>+取消压缩：10.699s</td>
</tr>
</tbody>
</table>
<p>从以上表中不同条件下编译时间分析可知，编译速度优化的优先级如下：</p>
<blockquote>
<p>操作系统 &gt; <code>latexmk</code> &gt; <code>draft</code>模式 &gt;
取消压缩</p>
</blockquote>
<p>实际编译长文档时可以考虑使用这些方法加快编译。</p>
<hr />
<h2 id="参考博文">参考博文</h2>
<ol type="1">
<li><span class="exturl" data-url="aHR0cHM6Ly92aW8xZXR1cy5naXRodWIuaW8vYmxvZy8yMDIzLzAyLzE2L21pc2MvbGF0ZXgvbGF0ZXhfcHJpbmNpcGxlcy8=">LaTeX
编译原理，如何降低编译时间<i class="fa fa-external-link-alt"></i></span></li>
</ol>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX使用技巧</category>
      </categories>
      <tags>
        <tag>编译</tag>
        <tag>效率</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX之minted使用技巧</title>
    <url>/2025/03/28/tools/latex/usage-skills/latex-minted-tutorial/</url>
    <content><![CDATA[<p>本文介绍了如何在<span
class="math inline">\(\LaTeX{}\)</span>中使用<code>minted</code>宏包实现代码高亮效果。</p>
<span id="more"></span>
<hr />
<h2 id="介绍">介绍</h2>
<p>minted 使用 Pygments
库提供语法高亮功能。它还提供自定义高亮源代码输出的选项，包括用 Python
实现的功能，例如使用正则表达式选择代码片段。</p>
<p>由于 minted 使用了 Pygments 和其他 Python
软件，因此它能够提供比纯粹用 LATEX 实现的语法高亮包（例如
<code>listings</code>）更丰富的高亮功能。过去，这种对外部软件的依赖带来了一些缺点，例如需要单独安装
Pygments。从 minted 版本 3 开始，所有 Python 软件（包括 Pygments）在使用
TEX 包管理器安装 <code>minted</code>时都会与 LATEX
包捆绑在一起，无需单独安装任何依赖项。</p>
<hr />
<h2 id="安装">安装</h2>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">tlmgr install minted <span class="comment"># TeX Live</span></span><br><span class="line">mpm --admin --install=minted  <span class="comment"># MiKTeX</span></span><br></pre></td></tr></table></figure>
<p>安装 minted 软件包后，它将包含 latexminted Python
可执行文件以及所有必需的 Python 库（包括
Pygments）。为了使这些库正常运行，<strong>必须安装 Python 3.8+
并将其添加到 系统环境变量 PATH
中</strong>（v3.7.0还不支持Python3.14版本，可安装3.8-3.13中任意版本使用）。</p>
<hr />
<h2 id="基本用法">基本用法</h2>
<h3 id="最小示例">最小示例</h3>
<p>默认缓存目录名称现在为
<code>_minted</code>。目录中的所有文件现在共享同一个缓存，而不是每个文档都拥有单独的缓存。</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\documentclass</span>&#123;article&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;minted&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>[svgnames]&#123;xcolor&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;minted&#125;[bgcolor=Beige, bgcolorpadding=0.5em]&#123;c&#125;<span class="string"></span></span><br><span class="line"><span class="string">int main() &#123;</span></span><br><span class="line"><span class="string">printf(&quot;hello, world&quot;);</span></span><br><span class="line"><span class="string">return 0;</span></span><br><span class="line"><span class="string">&#125;</span></span><br><span class="line"><span class="string"></span><span class="keyword">\end</span>&#123;minted&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250530152046131.png" alt="minted-minimal" style="zoom:67%;" /></p>
<h3 id="格式化源代码">格式化源代码</h3>
<h4 id="minted环境"><code>minted</code>环境</h4>
<p><code>minted</code> 环境的基本使用如下：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;minted&#125;&#123;python&#125;<span class="string"></span></span><br><span class="line"><span class="string">def boring(args = None):</span></span><br><span class="line"><span class="string">pass</span></span><br><span class="line"><span class="string"></span><span class="keyword">\end</span>&#123;minted&#125;</span><br></pre></td></tr></table></figure>
<p>要使用 Pygments 不支持的语言来使用
<code>minted</code>，或者只是禁用突出显示，请将语言设置为文本：<code>\begin{minted}{text}</code>。</p>
<h4 id="mint单行源代码"><code>\mint</code>单行源代码</h4>
<p>对于单行源代码，可以使用 <code>\mint</code> 作为 <code>minted</code>
的简写：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250530153051999.png" alt="image-20250530153051999" style="zoom:50%;" /></p>
<p>这使用命令而不是环境来排版一行代码，因此可以节省一些输入，但其输出与
<code>minted</code> 环境的输出相同。代码由一对相同的字符分隔，类似于
<code>\verb</code> 的工作方式。</p>
<p>完整的语法为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">\mint[&lt;options&gt;]&#123;&lt;language&gt;&#125;&lt;delim&gt;&lt;code&gt;&lt;delim&gt;</span><br></pre></td></tr></table></figure>
<p>其中代码分隔符可以是几乎任何标点符号。<code>&lt;code&gt;</code>
也可以使用成对的花括号 <code>{}</code> 分隔，只要
<code>&lt;code&gt;</code> 本身不包含未成对的花括号即可。
请注意，<code>\mint</code>
命令不适用于行内使用。相反，当只有一行代码时，它是 <code>minted</code>
的快捷方式。</p>
<p><code>\mintinline</code> 命令适用于行内使用。</p>
<h4 id="mintinline行内源代码"><code>\mintinline</code>行内源代码</h4>
<p>语法为 :</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">\mintinline[&lt;options&gt;]&#123;&lt;language&gt;&#125;&lt;delim&gt;&lt;code&gt;&lt;delim&gt;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250530153840449.png" alt="image-20250530153840449" style="zoom:67%;" /></p>
<p>分隔符可以是单个重复字符，就像 <code>\verb</code>
一样。它们也可以是一对花括号 <code>{}</code>。当
<code>\mintinline</code> 用于可移动参数（例如
<code>\section</code>）时，需要使用花括号。</p>
<h4
id="inputminted插入外部文件"><code>\inputminted</code>插入外部文件</h4>
<p><code>\inputminted</code> 命令用于输入外部文件。其语法为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">\inputminted[&lt;options&gt;]&#123;&lt;language&gt;&#125;&#123;&lt;filename&gt;&#125;</span><br></pre></td></tr></table></figure>
<h3 id="使用不同的style">使用不同的style</h3>
<p>除了使用默认的高亮<code>style</code>，您还可以选择 Pygments
提供的其他样式。有两种等效的方法可以做到这一点：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\usemintedstyle</span>&#123;name&#125;</span><br><span class="line"><span class="keyword">\setminted</span>&#123;style=name&#125;</span><br></pre></td></tr></table></figure>
<p><code>\usemintedstyle</code>
只能修改代码<code>style</code>，<code>\setminted</code>
方法的优势在于它也能接受其他 <code>minted</code>
选项，比如字号、行号、行距、背景色、自动换行等，语义上是把
<code>style</code> 当作一个普通选项。</p>
<p>完整语法为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">\usemintedstyle[&lt;language&gt;]&#123;&lt;style&gt;&#125;</span><br><span class="line">\setminted[&lt;language&gt;]&#123;&lt;key=value&gt;&#125;</span><br></pre></td></tr></table></figure>
<p><code>style</code>可以设置为整个文档（不指定语言），也可以仅为特定语言设置。请注意，<code>style</code>也可以通过每个命令和环境的可选参数来设置。</p>
<p>pygments.org/styles 提供了<span class="exturl" data-url="aHR0cHM6Ly9weWdtZW50cy5vcmcvc3R5bGVzLw==">样式高亮示例<i class="fa fa-external-link-alt"></i></span>。可以使用
<code>pygments.org/demo</code> 上的<span class="exturl" data-url="aHR0cHM6Ly9weWdtZW50cy5vcmcvZGVtby8=">在线演示预览<i class="fa fa-external-link-alt"></i></span>不同样式下的代码。也可以通过运行命令
<code>pygmentize -L style</code> 列出可用的样式。</p>
<h3 id="支持语言">支持语言</h3>
<p>Pygments
支持数百种不同的编程语言、模板语言和其他标记语言。当前支持的<span class="exturl" data-url="aHR0cHM6Ly9weWdtZW50cy5vcmcvZG9jcy9sZXhlcnMv">语言列表<i class="fa fa-external-link-alt"></i></span>。也可以运行
<code>pygmentize -L lexers</code> 命令。</p>
<hr />
<h2 id="浮动-listings">浮动 listings</h2>
<p><code>minted</code> 提供了一个 <code>listing</code>
环境，可用于包裹代码块。这会将代码放置在类似于图形或表格的浮动框中，默认位置为
<code>tbp</code>。 还可以提供 <code>\caption</code> 和
<code>\label</code>：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250530161050482.png" alt="image-20250530161050482" style="zoom:50%;" /></p>
<p>默认列表位置可以轻松修改。当包选项
<code>newfloat=false</code>（默认）时，<code>float</code>
包用于创建列表环境。可以通过重新定义 <code>\fps@listing</code>
来修改位置。例如，</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\makeatletter</span></span><br><span class="line"><span class="keyword">\renewcommand</span>&#123;<span class="keyword">\fps@listing</span>&#125;&#123;htp&#125;</span><br><span class="line"><span class="keyword">\makeatother</span></span><br></pre></td></tr></table></figure>
<p>当 <code>newfloat=true</code> 时，将使用功能更强大的
<code>newfloat</code> 包来创建列表环境。在这种情况下，可以使用
<code>newfloat</code> 命令来自定义列表：</p>
<p><code>\SetupFloatingEnvironment{listing}{placement=htp}</code></p>
<p><code>\listoflistings</code>
宏将插入文档中所有（浮动）listings的列表：</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250530162056012.png" alt="image-20250530162056012" style="zoom:67%;" /></p>
<h3 id="个性化-listing-环境">个性化 listing 环境</h3>
<p>默认情况下，列表环境使用 <code>float</code>
包创建。在这种情况下，可以使用下文描述的 <code>\listingscaption</code>
和 <code>\listoflistingscaption</code>
宏来自定义<code>caption and list of listings.</code>。如果
<code>minted</code> 加载了 <code>newfloat</code>
选项，则列表环境将使用功能更强大的 <code>newfloat</code>
包创建。<code>newfloat</code> 是 <code>caption</code>
的一部分，<code>caption</code> 提供了许多自定义标题的选项。</p>
<p>使用 <code>newfloat</code> 创建列表环境时，应使用
<code>newfloat</code> 的 <code>\SetupFloatingEnvironment</code>
命令进行自定义。例如，可以使用以下命令将标题中的字符串“Listing”更改为“Program
code”：</p>
<p><code>\SetupFloatingEnvironment{listing}{name=Program code}</code></p>
<p>并且“List of Listings”可以更改为“List of Program Code”，</p>
<p><code>\SetupFloatingEnvironment{listing}{listname=List of Program Code}</code></p>
<p>请参阅 <code>newfloat</code> 和 <code>caption</code>
文档以获取更多信息。</p>
<p>默认情况下，包选项 <code>newfloat=false</code>时，自定义列表
<code>\listingscaption</code> 标题中的字符串“Listing”。此功能仅在包选项
<code>newfloat=false</code> 时有效。例如：</p>
<p><code>\renewcommand{\listingscaption}{Program code}</code></p>
<p>自定义列表标题“List of Listings”。此功能仅在包选项
<code>newfloat=false</code> 时有效。例如：</p>
<p><code>\renewcommand{\listoflistingscaption}{List of Program Code}</code></p>
<hr />
<h2 id="选项">选项</h2>
<h3 id="宏包选项">宏包选项</h3>
<p><code>\usepackage[options]{minted}</code></p>
<p>常用的 <code>options</code> 如下：</p>
<ul>
<li><code>chapter</code> 控制Listing的章节序号，也可选为
<code>section</code> 。</li>
<li><code>cache=true</code>
缓存源代码选项。默认情况下处于启用状态。cache
选项会在文档根目录中创建一个 <code>_minted</code> 目录（可以使用
<code>cachedir</code>
选项自定义）。高亮代码的文件存储在此目录中，以便以后无需再次高亮代码。不再使用的缓存文件会被自动删除。在大多数情况下，缓存可以显著加快文档的编译速度。</li>
<li><code>cachedir=_minted</code>
缓存文件的存储目录。默认值：<code>_minted</code>，路径应使用正斜杠，即使在
Windows 下也是如此。</li>
<li><code>newfolat=false</code> 默认情况下，列表环境使用
<code>float</code> 包创建。<code>newfloat</code> 选项会改为使用
<code>newfloat</code> 创建列表环境。这可以更好地与 <code>caption</code>
包集成。</li>
</ul>
<p>其他<code>options</code>包括<code>debug=false</code>、<code>frozencache=false</code>、<code>highlightmode=fastfirst</code>、<code>inputlexerlinenos=false</code>、<code>lexerlinenos=false</code>、<code>placeholder=false</code>、<code>verbatim=false</code>，具体含义参考说明文档<code>texdoc minted</code>。</p>
<h3 id="设置命令和环境的选项">设置命令和环境的选项</h3>
<p>通过设置<code>\setminted[&lt;lexer&gt;]{key=value,...}</code>
和<code>\setmintedinline[&lt;lexer&gt;]{key=value,...}</code>来为整个文档或特定语言设置选项，这个设置的选项覆盖文档整体层面的选项。单命令选项会覆盖这里的选项。</p>
<p>且用<code>\setmintedinline</code>指定的设置都会覆盖用<code>\setminted</code>设置的设置。也就是说，内联设置总是比常规设置具有更高的优先级。</p>
<h3 id="命令和环境选项">命令和环境选项</h3>
<p>以下是常用的<code>key</code>列表：</p>
<ul>
<li><p><code>autogobble=false</code>
自动删除所有常见的前导空格，当<code>autogobble</code>和<code>gobble</code>一起使用时，效果是累积的。首先<code>autogobble</code>删除所有常见的缩进，然后应用<code>gobble</code>。</p></li>
<li><p><code>bgcolor=none</code> 命令和环境的背景颜色。</p></li>
<li><p><code>breaklines=false</code> 长行自动换行。</p></li>
<li><p><code>encoding=UTF-8</code>
<code>\inputminted</code>及其派生命令读取文件时使用的文件编码。</p></li>
<li><p><code>escapeinside=none</code>
在指定的两个字符之间切换到LATEX模式。这两个字符之间的所有代码都将被解读为LATEX并相应地排版。这允许进行额外的格式化。转义字符不必相同。</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\setminted</span>&#123;escapeinside=||&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;minted&#125;&#123;py&#125;<span class="string"></span></span><br><span class="line"><span class="string">def f(x):</span></span><br><span class="line"><span class="string">y = x|\colorbox&#123;green&#125;&#123;**&#125;|2</span></span><br><span class="line"><span class="string">return y</span></span><br><span class="line"><span class="string"></span><span class="keyword">\end</span>&#123;minted&#125;</span><br></pre></td></tr></table></figure></li>
<li><p><code>firstline=1</code>
要显示的第一行。该行之前的所有行都将被忽略，不会出现在输出中。</p></li>
<li><p><code>firstnumber=1</code> 第一行的行号。</p></li>
<li><p><code>fontsize</code> 代码字体大小。</p></li>
<li><p><code>frame=none</code>
用于源代码列表周围的边框类型。可选项：(<code>none</code> |
<code>leftline</code> | <code>topline</code> | <code>bottomline</code> |
<code>lines</code> | <code>single</code>)。</p></li>
<li><p><code>framerule=0.4pt</code> 框架的线宽。</p></li>
<li><p><code>framesep=\fboxsep</code> 框架与内容之间的距离。</p></li>
<li><p><code>gobble=0</code> 从每一行输入中删除前n个字符。</p></li>
<li><p><code>highlightcolor=LightCyan</code>
高亮的代码行背景颜色。</p></li>
<li><p><code>highlightlines=none</code>
这会根据行号高亮显示单行或一系列行。例如，<code>highlightlines={1, 3-4}</code>。</p></li>
<li><p><code>label</code>
在代码周围的框架顶部、底部或两者都添加一个标签。</p></li>
<li><p><code>labelposition</code> 标签的位置。可选项：(<code>none</code>
| <code>topline</code> | <code>bottomline</code> |
<code>all</code>)。</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;minted&#125;[frame=single,labelposition=all,label=test]&#123;c&#125;<span class="string"></span></span><br><span class="line"><span class="string">#include &lt;stdio.h&gt;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">int main() &#123;</span></span><br><span class="line"><span class="string">  printf(&quot;hello, world&quot;);</span></span><br><span class="line"><span class="string">  return 0;</span></span><br><span class="line"><span class="string">&#125;</span></span><br><span class="line"><span class="string"></span><span class="keyword">\end</span>&#123;minted&#125;</span><br></pre></td></tr></table></figure>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20251216234243798.png"
alt="latex-minted-label" />
<figcaption aria-hidden="true">latex-minted-label</figcaption>
</figure></li>
<li><p><code>linenos=false</code> 启用行号。</p></li>
<li><p><code>mathescape=false</code>
在注释中启用LATEX数学模式。</p></li>
<li><p><code>numberblanklines=true</code> 启用或禁用空行编号。</p></li>
<li><p><code>numberfirstline=false</code>
无论<code>stepnumber</code>怎么样，都要给第一行编号。</p></li>
<li><p><code>numbers</code> 本质上与 <code>linenos</code>
相同，不同之处在于可以指定数字出现的一侧。可选项：(<code>left</code> |
<code>right</code> | <code>both</code> | <code>none</code>)。</p></li>
<li><p><code>numbersep=12pt</code> 数字与行首之间的间距。</p></li>
<li><p><code>obeytabs=false</code>
将制表符视为制表符，而不是将它们转换为空格。</p></li>
<li><p><code>rulecolor=black</code> 框架的颜色。</p></li>
<li><p><code>showspaces=false</code> 启用可见空格。</p></li>
<li><p><code>showtabs=false</code>
启用可见的制表符，仅在与<code>obeytabs</code>结合使用时有效。</p></li>
<li><p><code>stepnumber=1</code> 行号出现的间隔。</p></li>
<li><p><code>style=default</code> 设置 Pygments
所使用的样式表。</p></li>
<li><p><code>texcl=false</code> 允许在注释中使用LATEX代码。</p></li>
</ul>
<p>更多<code>key</code>值参考说明文档<code>texdoc minted</code>。</p>
<hr />
<h2 id="定义快捷方式">定义快捷方式</h2>
<p>包含多个列表的大型文档可能对于大多数列表使用相同的语言和相同的选项集。因此，<code>minted</code>
定义了一组命令，允许定义命令和环境的快捷方式。每个快捷方式都特定于一种语言。</p>
<ul>
<li><p><code>\newminted</code> <code>\newminted</code>为
<code>minted</code> 环境定义了一个新别名：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\newminted</span>&#123;cpp&#125;&#123;gobble=2,linenos&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;cppcode&#125;</span><br><span class="line">template &lt;typename T&gt;</span><br><span class="line">T id(T value) &#123;</span><br><span class="line">return value;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;cppcode&#125;</span><br></pre></td></tr></table></figure>
<p>如果你想动态提供额外的选项，或者覆盖现有的默认选项，你也可以这样做：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\newminted</span>&#123;cpp&#125;&#123;gobble=2,linenos&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;cppcode&#125;[linenos=false,</span><br><span class="line">frame=single]</span><br><span class="line">int const answer = 42;</span><br><span class="line"><span class="keyword">\end</span>&#123;cppcode&#125;</span><br></pre></td></tr></table></figure>
<p>环境的默认名称是<code>&lt;lexer&gt;code</code>。如果此名称与另一个环境，或者如果你想选择一个不同的名称，你可以使用可选的参数：<code>\newminted[environment name]{lexer}{options}</code>。</p></li>
<li><p><code>\newmint</code> 为<code>\mint</code>
定义的快捷方式语法是：<code>\newmint[&lt;macro name&gt;]{lexer}{options}</code>，参数和用法与<code>\newminted</code>相同。如果<code>macro name</code>未指定，则使用<code>lexer</code>。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20251217003346422.png"
alt="minted-newmint" />
<figcaption aria-hidden="true">minted-newmint</figcaption>
</figure></li>
<li><p><code>\newmintinline</code>
这创建了个性化版本的<code>mintinline</code>。语法和<code>\newmint</code>语法相同。如果<code>macro name</code>未指定，则使用<code>&lt;lexer&gt;inline</code>。</p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20251217003506238.png"
alt="minted-newmintinline" />
<figcaption aria-hidden="true">minted-newmintinline</figcaption>
</figure></li>
<li><p><code>newmintedfile</code>
这创建了个性化版本的<code>inputminted</code>。语法是<code>\newmintedfile[&lt;macro name&gt;]{lexer}{options}</code>，如果<code>macro name</code>未指定，则使用<code>&lt;lexer&gt;file</code>。</p></li>
</ul>
<hr />
<h2 id="导言区常用设置">导言区常用设置</h2>
<p>导言区常用设置</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\usepackage</span>&#123;float&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;minted&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">\renewcommand</span>&#123;<span class="keyword">\listingscaption</span>&#125;&#123;源代码&#125;        <span class="comment">% 默认为&quot;Listing&quot;</span></span><br><span class="line"><span class="keyword">\renewcommand</span>&#123;<span class="keyword">\listoflistingscaption</span>&#125;&#123;源代码列表&#125;    <span class="comment">% 默认为&quot;List of Listings&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">\setminted</span>&#123;</span><br><span class="line">  encoding=utf8,            <span class="comment">% 源码编码</span></span><br><span class="line">  autogobble,               <span class="comment">% 自动去除公共缩进</span></span><br><span class="line">  fontsize=<span class="keyword">\small</span>,          <span class="comment">% 代码字体大小</span></span><br><span class="line">  breaklines,               <span class="comment">% 自动换行</span></span><br><span class="line">  linenos,                  <span class="comment">% 显示行号 left | right | both | none </span></span><br><span class="line">  numbers=left,        	<span class="comment">% 行号位置</span></span><br><span class="line">  frame=single              <span class="comment">% single | lines | leftline | none | topline | bottomline</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">\makeatletter</span></span><br><span class="line"><span class="keyword">\renewcommand</span>&#123;<span class="keyword">\fps@listing</span>&#125;&#123;htp&#125; <span class="comment">% 设置listing环境默认浮动位置</span></span><br><span class="line"><span class="keyword">\makeatother</span></span><br></pre></td></tr></table></figure>
<h2 id="参考文章">参考文章</h2>
<ol type="1">
<li><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2dwb29yZS9taW50ZWQvYmxvYi9tYWluL2xhdGV4L21pbnRlZC9taW50ZWQucGRm">The
minted package: Highlighted source code in LATEX<i class="fa fa-external-link-alt"></i></span></li>
</ol>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX使用技巧</category>
      </categories>
      <tags>
        <tag>宏包</tag>
        <tag>源代码</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX之表格使用技巧</title>
    <url>/2024/04/05/tools/latex/usage-skills/latex-table-tutorial/</url>
    <content><![CDATA[<p>本文介绍了在 <span class="math inline">\(\LaTeX{}\)</span>
中插入表格的基本语法和一些常用的技巧，并且给出了一些在排版中常用的模板样例。</p>
<span id="more"></span>
<hr />
<h2 id="基本语法">基本语法</h2>
<p>排版表格最基本的 <span
class="math inline">\(\textrm{tabular}\)</span> 环境用法为：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;[⟨align⟩]&#123;⟨column-spec⟩&#125;</span><br><span class="line">⟨item1⟩ <span class="built_in">&amp;</span> ⟨item2⟩ <span class="built_in">&amp;</span> … <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\hline</span></span><br><span class="line">⟨item1⟩ <span class="built_in">&amp;</span> ⟨item2⟩ <span class="built_in">&amp;</span> … <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p>其中 <code>&lt;column-spec&gt;</code>
是列格式标记，在接下来的内容将仔细介绍；<span
class="math inline">\(\textrm\&amp;\)</span>
用来分隔单元格；<code>\\</code> 用来换行；<code>\hline</code>
用来在行与行之间绘制横线。</p>
<p>直接使用 <span class="math inline">\(\textrm{tabular}\)</span>
环境的话，会 <strong>和周围的文字混排</strong>。此时可用一个可选参数
<code>&lt;align&gt;</code> 控制垂直对齐：<span
class="math inline">\(\verb|t|\)</span> 和 <span
class="math inline">\(\verb|b|\)</span>
分别表示按表格顶部、底部对齐，其他参数或省略不写（默认）表示居中对齐。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|&#125;</span><br><span class="line">  center-<span class="keyword">\\</span> aligned <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;,</span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;[t]&#123;|c|&#125;</span><br><span class="line">  top-<span class="keyword">\\</span> aligned <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;,</span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;[b]&#123;|c|&#125;</span><br><span class="line">  bottom-<span class="keyword">\\</span> aligned<span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125; tabulars.</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250114222501332.png" alt="image-20250114222501332" style="zoom:67%;" /></p>
<p>但是通常情况下 <span class="math inline">\(\textrm{tabular}\)</span>
环境很少与文字直接混排，而是会放在 <span
class="math inline">\(\textrm{table}\)</span> 浮动体环境中，并用
<code>\caption</code> 命令加标题。</p>
<h3 id="列格式">列格式</h3>
<p><span class="math inline">\(\textrm{tabular}\)</span> 环境使用
&lt;column-spec&gt;
参数指定表格的列数以及每列的格式。基本的列格式见下表。</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;"><strong>列格式</strong></th>
<th style="text-align: left;"><strong>说明</strong></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\textrm{l/c/r
}\)</span></td>
<td style="text-align: left;">单元格内容左对齐/居中/右对齐，不折行</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\textrm{p\{&lt;width&gt;\}}\)</span></td>
<td style="text-align: left;">单元格宽度固定为
&lt;width&gt;，可自动折行</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(|\)</span></td>
<td style="text-align: left;">绘制竖线</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(@\{&lt;string&gt;\}\)</span></td>
<td style="text-align: left;">自定义内容 &lt;string&gt;</td>
</tr>
</tbody>
</table>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;lcr|p&#123;6em&#125;&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  left <span class="built_in">&amp;</span> center <span class="built_in">&amp;</span> right</span><br><span class="line">       <span class="built_in">&amp;</span> par box with fixed width<span class="keyword">\\</span></span><br><span class="line">  L    <span class="built_in">&amp;</span> C      <span class="built_in">&amp;</span> R     <span class="built_in">&amp;</span> P <span class="keyword">\\</span></span><br><span class="line"> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115130809432.png" alt="image-20250115130809432" style="zoom:67%;" /></p>
<p>表格中每行的单元格数目不能多于列格式里 $ $
的总数（可以少于这个总数），否则出错。</p>
<p><span class="math inline">\(\textrm{@}\)</span>
格式可在单元格前后插入任意的文本，但同时它也消除了单元格前后额外添加的间距。<span
class="math inline">\(\textrm{@}\)</span>
格式可以适当使用以充当“竖线”。特别地，<span
class="math inline">\(\textrm{@\{\}}\)</span>
可直接用来消除单元格前后的间距：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;@&#123;&#125; r@&#123;:&#125;lr @&#123;&#125;&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  1  <span class="built_in">&amp;</span> 1 <span class="built_in">&amp;</span> one <span class="keyword">\\</span></span><br><span class="line">  11 <span class="built_in">&amp;</span> 3 <span class="built_in">&amp;</span> eleven <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115131115056.png" alt="image-20250115131115056" style="zoom:67%;" /></p>
<p>另外还提供了简便的将格式参数重复的写法 <span
class="math inline">\(\textrm*\{&lt;n&gt;\}\{&lt;columnspec&gt;\}\)</span>，比如以下两种写法是等效的：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|c|c|c|c|p&#123;4em&#125;|p&#123;4em&#125;|&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|*&#123;5&#125;&#123;c|&#125;*&#123;2&#125;&#123;p&#123;4em&#125;|&#125;&#125;</span><br></pre></td></tr></table></figure>
<p>有时需要为整列修饰格式，比如整列改变为粗体，如果每个单元格都加上
<code>\bfseries}</code> 命令会比较麻烦。<code>array</code>
宏包提供了辅助格式 <span class="math inline">\(\textrm&gt;\)</span> 和
<span
class="math inline">\(\textrm&lt;\)</span>，用于给列格式前后加上修饰命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;array&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;&gt;&#123;<span class="keyword">\itshape</span>&#125;r&lt;&#123;*&#125;l&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  italic <span class="built_in">&amp;</span> normal <span class="keyword">\\</span></span><br><span class="line">  column <span class="built_in">&amp;</span> column <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115131903387.png" alt="image-20250115131903387" style="zoom:67%;" /></p>
<p>辅助格式甚至支持插入 <code>\centering</code> 等命令改变 <span
class="math inline">\(\textrm{p}\)</span>
列格式的对齐方式，一般还要加额外的命令 <code>\arraybackslash</code>
以免出错。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;array&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;<span class="comment">%</span></span><br><span class="line">&#123;&gt;&#123;<span class="keyword">\centering</span><span class="keyword">\arraybackslash</span>&#125;p&#123;9em&#125;&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  Some center-aligned long text. <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115132303588.png" alt="image-20250115132303588" style="zoom:67%;" /></p>
<p><code>array</code> 宏包还提供了类似 <span
class="math inline">\(\textrm{p}\)</span> 格式的 <span
class="math inline">\(\textrm{m}\)</span> 格式和 <span
class="math inline">\(\textrm{b}\)</span>
格式，三者分别在垂直方向上靠顶端对齐、居中以及底端对齐。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;array&#125;</span></span><br><span class="line"><span class="keyword">\newcommand</span><span class="keyword">\txt</span>&#123;a b c d e f g h i&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;cp&#123;2em&#125;m&#123;2em&#125;b&#123;2em&#125;&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  pos <span class="built_in">&amp;</span> <span class="keyword">\txt</span> <span class="built_in">&amp;</span> <span class="keyword">\txt</span> <span class="built_in">&amp;</span> <span class="keyword">\txt</span> <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115132526382.png" alt="image-20250115132526382" style="zoom:67%;" /></p>
<h3 id="列宽">列宽</h3>
<p>在控制列宽方面，<span class="math inline">\(\LaTeX{}\)</span>
表格有着明显的不足：<span class="math inline">\(\textrm{l/c/r}\)</span>
格式的列宽是由文字内容的自然宽度决定的，而 <span
class="math inline">\(\textrm{p}\)</span>
格式给定了列宽却不好控制对齐（可用 <code>array</code>
宏包的辅助格式），更何况列与列之间通常还有间距，所以直接生成给定总宽度的表格并不容易。</p>
<p><span class="math inline">\(\LaTeX{}\)</span> 本身提供了 <span
class="math inline">\(\textrm{tabular*}\)</span>
环境用来排版定宽表格，但是不太方便使用，比如要用到 <span
class="math inline">\(\textrm{@}\)</span>
格式插入额外命令，令单元格之间的间距为
<code>\fill</code>，但即使这样仍然有瑕疵：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular*&#125;&#123;14em&#125;<span class="comment">%</span></span><br><span class="line">&#123;@&#123;<span class="keyword">\extracolsep</span>&#123;<span class="keyword">\fill</span>&#125;&#125;|c|c|c|c|&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  A <span class="built_in">&amp;</span> B <span class="built_in">&amp;</span> C <span class="built_in">&amp;</span> D <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line">  a <span class="built_in">&amp;</span> b <span class="built_in">&amp;</span> c <span class="built_in">&amp;</span> d <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular*&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115132953911.png" alt="image-20250115132953911" style="zoom:67%;" /></p>
<p><code>tabularx</code> 宏包提供了方便的解决方案。它引入了一个 <span
class="math inline">\(\textrm{X}\)</span> 列格式，类似 <span
class="math inline">\(\textrm{p}\)</span>
列格式，不过会根据表格宽度自动计算列宽，多个 <span
class="math inline">\(\textrm{X}\)</span> 列格式平均分配列宽。 <span
class="math inline">\(\textrm{X}\)</span> 列格式也可以用
<code>array</code> 里的辅助格式修饰对齐方式：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;array,tabularx&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;tabularx&#125;&#123;14em&#125;<span class="comment">%</span></span><br><span class="line">&#123;|*&#123;4&#125;&#123;&gt;&#123;<span class="keyword">\centering</span><span class="keyword">\arraybackslash</span>&#125;X|&#125;&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  A <span class="built_in">&amp;</span> B <span class="built_in">&amp;</span> C <span class="built_in">&amp;</span> D <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line">  a <span class="built_in">&amp;</span> b <span class="built_in">&amp;</span> c <span class="built_in">&amp;</span> d <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabularx&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115133221441.png" alt="image-20250115133221441" style="zoom:67%;" /></p>
<h3 id="横线">横线</h3>
<p>在之前的例子见过许多次绘制表格线的 <code>\hline</code> 命令。另外
<code>\cline{&lt;i&gt;-&lt;j&gt;}</code>
用来绘制跨越部分单元格的横线：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|c|c|&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  4 <span class="built_in">&amp;</span> 9 <span class="built_in">&amp;</span> 2 <span class="keyword">\\</span> </span><br><span class="line">  <span class="keyword">\cline</span>&#123;2-3&#125;</span><br><span class="line">  3 <span class="built_in">&amp;</span> 5 <span class="built_in">&amp;</span> 7 <span class="keyword">\\</span> </span><br><span class="line">  <span class="keyword">\cline</span>&#123;1-1&#125;</span><br><span class="line">  8 <span class="built_in">&amp;</span> 1 <span class="built_in">&amp;</span> 6 <span class="keyword">\\</span> </span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115133628644.png"
alt="image-20250115133628644" />
<figcaption aria-hidden="true">image-20250115133628644</figcaption>
</figure>
<p>在科技论文排版中广泛应用的表格形式是三线表，形式干净简明。三线表由
<code>booktabs</code> 宏包支持，它提供了
<code>\toprule</code>、<code>\midrule</code> 和 <code>\bottomrule</code>
命令用以排版三线表的三条线，以及和 <code>\cline</code> 对应的
<code>\cmidrule</code>。除此之外，最好不要用其它横线以及竖线：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;booktabs&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;cccc&#125;</span><br><span class="line">  <span class="keyword">\toprule</span></span><br><span class="line">   <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;3&#125;&#123;c&#125;&#123;Numbers&#125; <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\cmidrule</span>&#123;2-4&#125;</span><br><span class="line">           <span class="built_in">&amp;</span> 1 <span class="built_in">&amp;</span> 2 <span class="built_in">&amp;</span> 3 <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\midrule</span></span><br><span class="line">  Alphabet <span class="built_in">&amp;</span> A <span class="built_in">&amp;</span> B <span class="built_in">&amp;</span> C <span class="keyword">\\</span></span><br><span class="line">  Roman    <span class="built_in">&amp;</span> I <span class="built_in">&amp;</span> II<span class="built_in">&amp;</span> III <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\bottomrule</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115140704162.png" alt="image-20250115140704162" style="zoom:67%;" /></p>
<h3 id="合并单元格">合并单元格</h3>
<p>横向合并单元格由 <code>\multicolumn</code> 命令实现：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\multicolumn</span>&#123;⟨n⟩&#125;&#123;⟨column-spec⟩&#125;&#123;⟨item⟩&#125;</span><br></pre></td></tr></table></figure>
<p>其中<code>&lt;n&gt;</code>
为要合并的列数，<code>&lt;column-spec&gt;</code>
为合并单元格后的列格式，只允许出现一个 <span
class="math inline">\(\textrm{l/c/r}\)</span> 或 <span
class="math inline">\(\textrm{p}\)</span>
格式。如果合并前的单元格前后带表格线 |，合并后的列格式也要带 |
以使得表格的竖线一致。</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|c|c|&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  1 <span class="built_in">&amp;</span> 2 <span class="built_in">&amp;</span> Center <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line">  <span class="keyword">\multicolumn</span>&#123;2&#125;&#123;|c|&#125;&#123;3&#125; <span class="built_in">&amp;</span></span><br><span class="line">  <span class="keyword">\multicolumn</span>&#123;1&#125;&#123;r|&#125;&#123;Right&#125; <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line">  4 <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;2&#125;&#123;c|&#125;&#123;C&#125; <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115142430321.png" alt="image-20250115142430321" style="zoom:67%;" /></p>
<p>上面的例子还体现了，形如 <code>\multicolumn{1}{r|}{item}</code>的命令
<strong>可以用来修改某一个单元格的列格式。</strong></p>
<p><strong>纵向合并单元格</strong> 需要用到 <code>multirow</code>
宏包提供的 <code>\multirow</code> 命令：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\multirow</span>&#123;⟨n⟩&#125;&#123;⟨width⟩&#125;&#123;⟨item⟩&#125;</span><br></pre></td></tr></table></figure>
<p><code>&lt;width&gt;</code> 为合并后单元格的宽度，可以填 <span
class="math inline">\(\textrm{*}\)</span> 以使用自然宽度。</p>
<p>我们看一个结合 <code>\cline</code>、<code>\multicolumn</code> 和
<code>\multirow</code> 命令的例子：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;multirow&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;ccc&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  <span class="keyword">\multirow</span>&#123;2&#125;&#123;*&#125;&#123;Item&#125; <span class="built_in">&amp;</span></span><br><span class="line">    <span class="keyword">\multicolumn</span>&#123;2&#125;&#123;c&#125;&#123;Value&#125; <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\cline</span>&#123;2-3&#125;</span><br><span class="line">    <span class="built_in">&amp;</span> First <span class="built_in">&amp;</span> Second <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line">  A <span class="built_in">&amp;</span> 1     <span class="built_in">&amp;</span> 2 <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115142135335.png" alt="image-20250115142135335" style="zoom:67%;" /></p>
<h3 id="嵌套表格">嵌套表格</h3>
<p>在以下的例子中，注意要用 <code>\multicolumn</code> 命令配合 <span
class="math inline">\(\textrm{@\{\}}\)</span>
格式把单元格的额外边距去掉，使得嵌套的表格线能和外层的表格线正确相连：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|c|c|&#125;</span><br><span class="line"> <span class="keyword">\hline</span></span><br><span class="line"> a <span class="built_in">&amp;</span> b <span class="built_in">&amp;</span> c <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"> a <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;1&#125;&#123;@&#123;&#125;c@&#123;&#125;|&#125;</span><br><span class="line"> &#123;<span class="keyword">\begin</span>&#123;tabular&#125;&#123;c|c&#125;</span><br><span class="line">   e <span class="built_in">&amp;</span> f <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line">   e <span class="built_in">&amp;</span> f <span class="keyword">\\</span></span><br><span class="line">  <span class="keyword">\end</span>&#123;tabular&#125;&#125;</span><br><span class="line">       <span class="built_in">&amp;</span> c <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"> a <span class="built_in">&amp;</span> b <span class="built_in">&amp;</span> c <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115142824913.png" alt="image-20250115142824913" style="zoom:67%;" /></p>
<p>如果不需要为“拆分的单元格”画线，并且只在垂直方向“拆分”的话，<code>makecell</code>
宏包提供的 <code>\makecell</code> 命令是一个简单的解决方案：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% \usepackage&#123;makecell&#125;</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|c|&#125;</span><br><span class="line"> <span class="keyword">\hline</span></span><br><span class="line"> a <span class="built_in">&amp;</span> <span class="keyword">\makecell</span>&#123;d1 <span class="keyword">\\</span> d2&#125; <span class="keyword">\\</span></span><br><span class="line"> <span class="keyword">\hline</span></span><br><span class="line"> b <span class="built_in">&amp;</span> c <span class="keyword">\\</span></span><br><span class="line"> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115143114195.png" alt="image-20250115143114195" style="zoom:67%;" /></p>
<h3 id="行距控制">行距控制</h3>
<p><span class="math inline">\(\LaTeX{}\)</span>
生成的表格看起来通常比较紧凑。修改参数 <code>\arraystretch</code>
可以得到行距更加宽松的表格。</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\renewcommand</span><span class="keyword">\arraystretch</span>&#123;1.8&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  Really loose <span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line">  tabular rows.<span class="keyword">\\</span> <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;example&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115143416039.png" alt="image-20250115143416039" style="zoom:67%;" /></p>
<p>另一种增加间距的办法是给换行命令 <code>\\</code>
添加可选参数，在这一行下面加额外的间距，适合用于在行间不加横线的表格：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;c&#125;</span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line">  Head lines <span class="keyword">\\</span>[6pt]</span><br><span class="line">  tabular lines <span class="keyword">\\</span></span><br><span class="line">  tabular lines <span class="keyword">\\</span> </span><br><span class="line">  <span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115143537544.png" alt="image-20250115143537544" style="zoom:67%;" /></p>
<p>但是这种换行方式的存在导致了一个缺陷——从第二行开始，表格的首个单元格不能直接使用中括号
<span class="math inline">\(\textrm{[]}\)</span>，否则 <code>\\</code>
往往会将下一行的中括号当作自己的可选参数，因而出错。如果要使用中括号，应当放在花括号
<code>{}</code> 里面。或者也可以选择将换行命令写成 <span
class="math inline">\(\textrm{\\\\[0pt]}\)</span>。</p>
<hr />
<h2 id="浮动体">浮动体</h2>
<p>内容丰富的文章或者书籍往往包含许多图片和表格等内容。这些内容的尺寸往往太大，导致分页困难。<span
class="math inline">\(\LaTeX{}\)</span>
为此引入了浮动体的机制，令大块的内容可以脱离上下文，放置在合适的位置。
<span class="math inline">\(\LaTeX{}\)</span> 预定义了两类浮动体环境
<span class="math inline">\(\textrm{figure}\)</span> 和 <span
class="math inline">\(\textrm{table}\)</span>。习惯上 <span
class="math inline">\(\textrm{figure}\)</span> 里放图片，<span
class="math inline">\(\textrm{table}\)</span>
里放表格，但并没有严格限制，可以在任何一个浮动体里放置文字、公式、表格、图片等等任意内容。</p>
<p>以 <span class="math inline">\(\textrm{table}\)</span>
环境的用法举例，<span class="math inline">\(\textrm{figure}\)</span>
同理：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;table&#125;[⟨placement⟩]</span><br><span class="line">…</span><br><span class="line"><span class="keyword">\end</span>&#123;table&#125;</span><br></pre></td></tr></table></figure>
<p>&lt;placement&gt; 参数提供了一些符号用来表示浮动体允许排版的位置，如
<span class="math inline">\(\textrm{hbp}\)</span>
允许浮动体排版在当前位置、底部或者单独成页。<span
class="math inline">\(\textrm{table}\)</span> 和 <span
class="math inline">\(\textrm{figure}\)</span> 浮动体的默认设置为 <span
class="math inline">\(\textrm{tbp}\)</span>。</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250115155105789.png" alt="image-20250115155105789" style="zoom:67%;" /></p>
<p>双栏排版环境下，<span class="math inline">\(\LaTeX{}\)</span> 提供了
<span class="math inline">\(\textrm{table*}\)</span> 和 <span
class="math inline">\(\textrm{figure*}\)</span> 环境用来排版
<strong>跨栏</strong> 的浮动体。用法与 <span
class="math inline">\(\textrm{table}\)</span> 和 <span
class="math inline">\(\textrm{figure}\)</span> 一样，不同之处为双栏的
<code>&lt;placement&gt;</code> 参数只能用 <span
class="math inline">\(\textrm{tp}\)</span> 两个位置。</p>
<p>浮动体的位置选取受到先后顺序的限制。如果某个浮动体由于参数限制、空间限制等原因在当前页无法放置，就要推迟到之后处理，并使得之后的同类浮动体一并推迟。<code>\clearpage</code>
命令会在另起一页之前，先将所有推迟处理的浮动体排版成页，此时 <span
class="math inline">\(\textrm{htbp}\)</span> 等位置限制被完全忽略。</p>
<p><code>float</code> 宏包为浮动体提供了 <span
class="math inline">\(\textrm{H}\)</span> 位置参数，不与 <span
class="math inline">\(\textrm{htbp}\)</span> 及 <span
class="math inline">\(\textrm{!}\)</span> 混用。使用 <span
class="math inline">\(\textrm{H}\)</span>
位置参数时，会取消浮动机制，将浮动体视为一般的盒子插入当前位置。这在一些特殊情况下很有用（如使用
<code>multicol</code>
宏包排版分栏内容的时候），但尺寸过大的浮动体可能使得分页比较困难。</p>
<h3 id="浮动体的标题">浮动体的标题</h3>
<p>图表等浮动体提供了 <code>\caption</code>
命令加标题，并且自动给浮动体编号，可以用带星号的命令
<code>\caption*</code>(需加载相关宏包，如 <code>caption</code>)
生成不带编号的标题，也可以使用带可选参数的形式
<code>\caption[...]{...}</code>，使得在目录里使用短标题。命令之后还可以紧跟
<code>\label</code> 命令标记交叉引用。</p>
<p><code>\caption</code> 生成的标题形如“Figure 1: …”或“Table 1:
…”可通过修改 <code>\figurename</code> 和 <code>\tablename</code>
的内容来修改标题的前缀。标题样式的定制功能由 <code>caption</code>
宏包提供，详见该宏包的帮助文档，在此不作赘述。</p>
<p><span class="math inline">\(\textrm{table}\)</span> 和 <span
class="math inline">\(\textrm{figure}\)</span>
两种浮动体分别有各自的生成目录的命令：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\listoftables</span></span><br><span class="line"><span class="keyword">\listoffigures</span></span><br></pre></td></tr></table></figure>
<p>它们类似 <code>\tableofcontents</code> 生成单独的章节。</p>
<hr />
<h2 id="将-excel-表格转换为-latex-代码">将 Excel 表格转换为 LaTeX
代码</h2>
<h3 id="excel2latex-插件推荐">Excel2LaTeX 插件（推荐）</h3>
<ul>
<li><p><strong>特点</strong>：易用，可直接在 Excel 中将表格转换为 LaTeX
代码并复制粘贴到文档中。</p></li>
<li><p><strong>下载地址</strong>：<span class="exturl" data-url="aHR0cHM6Ly93d3cuY3Rhbi5vcmcvcGtnL2V4Y2VsMmxhdGV4">Excel2LaTeX<i class="fa fa-external-link-alt"></i></span></p></li>
<li><p>使用方法：</p>
<ol type="1">
<li>下载并安装 Excel2LaTeX.xla 插件。</li>
<li>在 Excel 中打开插件后，选中表格，点击“Convert Table”按钮。</li>
<li>复制生成的 LaTeX 代码并粘贴到你的 LaTeX 文档中。</li>
</ol></li>
</ul>
<h3 id="table-generator-在线工具">Table Generator 在线工具</h3>
<ul>
<li><p><strong>网站</strong>：<span class="exturl" data-url="aHR0cHM6Ly93d3cudGFibGVzZ2VuZXJhdG9yLmNvbS9sYXRleF90YWJsZXM=">Table
Generator<i class="fa fa-external-link-alt"></i></span></p></li>
<li><p><strong>特点</strong>：支持粘贴 Excel 表格，自动转换为 LaTeX
代码。</p></li>
<li><p>使用方法：</p>
<ol type="1">
<li>复制 Excel 表格数据（Ctrl + C）。</li>
<li>在网站中粘贴（Ctrl + V）。</li>
<li>生成 LaTeX 代码后复制到 LaTeX 文档中。</li>
</ol></li>
</ul>
<h3 id="pandoc适合批量转换">pandoc（适合批量转换）</h3>
<ul>
<li><p>安装：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install pandoc  <span class="comment"># Linux</span></span><br><span class="line">choco install pandoc      <span class="comment"># Windows（使用 Chocolatey）</span></span><br></pre></td></tr></table></figure></li>
<li><p>使用方法：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">pandoc -f csv -t latex input.csv -o output.tex</span><br></pre></td></tr></table></figure></li>
<li><p><strong>特点</strong>：适合从 CSV 文件批量转换。</p></li>
</ul>
<h3 id="python-脚本适合自定义需求">Python 脚本（适合自定义需求）</h3>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">from</span> tabulate <span class="keyword">import</span> tabulate</span><br><span class="line"></span><br><span class="line">df = pd.read_excel(<span class="string">&quot;your_file.xlsx&quot;</span>)</span><br><span class="line">latex_code = tabulate(df, headers=<span class="string">&quot;keys&quot;</span>, tablefmt=<span class="string">&quot;latex&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(latex_code)</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>特点</strong>：灵活，可自定义格式。</li>
</ul>
<p>这些工具各有优缺点，<strong>如果是日常使用，推荐 Excel2LaTeX 或 Table
Generator 在线工具；如果是批量处理，可以考虑 Pandoc 或
Python</strong>。</p>
<hr />
<h2 id="几种常见表格模板">几种常见表格模板</h2>
<h3 id="导言区包含宏包和命令">导言区包含宏包和命令</h3>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\usepackage</span>&#123;booktabs&#125; <span class="comment">% 导入三线表</span></span><br><span class="line"><span class="keyword">\usepackage</span>&#123;longtable&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;ltxtable&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;array&#125;       <span class="comment">% 基础包最先</span></span><br><span class="line"><span class="keyword">\usepackage</span>&#123;tabularx&#125;    <span class="comment">% 早期加载</span></span><br><span class="line"><span class="keyword">\usepackage</span>&#123;multirow&#125;    <span class="comment">% 多行处理</span></span><br><span class="line"><span class="keyword">\usepackage</span>&#123;siunitx&#125;     <span class="comment">% 特殊列格式</span></span><br><span class="line"><span class="keyword">\usepackage</span>&#123;makecell&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;graphicx&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;diagbox&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;rotating&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;subcaption&#125; <span class="comment">% 关键宏包</span></span><br><span class="line"><span class="keyword">\usepackage</span>&#123;float&#125; <span class="comment">% 关键宏包：提供[H]选项</span></span><br><span class="line"><span class="keyword">\newcolumntype</span>&#123;L&#125;&#123;&gt;&#123;<span class="keyword">\raggedright</span><span class="keyword">\arraybackslash</span>&#125;X&#125; <span class="comment">% 左对齐</span></span><br><span class="line"><span class="keyword">\newcolumntype</span>&#123;C&#125;&#123;&gt;&#123;<span class="keyword">\centering</span><span class="keyword">\arraybackslash</span>&#125;X&#125;    <span class="comment">% 居中对齐</span></span><br><span class="line"><span class="keyword">\newcolumntype</span>&#123;R&#125;&#123;&gt;&#123;<span class="keyword">\raggedleft</span><span class="keyword">\arraybackslash</span>&#125;X&#125;   <span class="comment">% 右对齐</span></span><br></pre></td></tr></table></figure>
<h3 id="普通三线表">普通三线表</h3>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;table&#125;[H]</span><br><span class="line">  <span class="keyword">\centering</span></span><br><span class="line">  <span class="keyword">\caption</span>&#123;Five different mesh densities.&#125;</span><br><span class="line">  <span class="comment">%\renewcommand\arraystretch&#123;1.6&#125;</span></span><br><span class="line">  <span class="keyword">\begin</span>&#123;tabular&#125;&#123;cccc&#125;</span><br><span class="line">    <span class="keyword">\toprule</span></span><br><span class="line">    <span class="built_in">&amp;</span> Total cells <span class="built_in">&amp;</span> <span class="built_in">$</span><span class="keyword">\mathrm</span>&#123;C&#125;<span class="built_in">_</span><span class="keyword">\mathrm</span>&#123;L&#125;<span class="built_in">$</span> <span class="built_in">&amp;</span> <span class="built_in">$</span><span class="keyword">\mathrm</span>&#123;C&#125;<span class="built_in">_</span><span class="keyword">\mathrm</span>&#123;D&#125;<span class="built_in">$</span> <span class="keyword">\\</span></span><br><span class="line">    <span class="keyword">\midrule</span></span><br><span class="line">    Mesh 1 <span class="built_in">&amp;</span> <span class="built_in">$</span>33.45 <span class="keyword">\times</span> 10<span class="built_in">^</span>4<span class="built_in">$</span> <span class="built_in">&amp;</span> 0.5606 <span class="built_in">&amp;</span> 0.0225 <span class="keyword">\\</span> </span><br><span class="line">    Mesh 2 <span class="built_in">&amp;</span> <span class="built_in">$</span>103.24 <span class="keyword">\times</span> 10<span class="built_in">^</span>4<span class="built_in">$</span> <span class="built_in">&amp;</span> 0.5656 <span class="built_in">&amp;</span> 0.0202 <span class="keyword">\\</span> </span><br><span class="line">    Mesh 3 <span class="built_in">&amp;</span> <span class="built_in">$</span>200.16 <span class="keyword">\times</span> 10<span class="built_in">^</span>4<span class="built_in">$</span> <span class="built_in">&amp;</span> 0.5689 <span class="built_in">&amp;</span> 0.0197 <span class="keyword">\\</span> </span><br><span class="line">    Mesh 4 <span class="built_in">&amp;</span> <span class="built_in">$</span>305.47 <span class="keyword">\times</span> 10<span class="built_in">^</span>4<span class="built_in">$</span> <span class="built_in">&amp;</span> 0.5689 <span class="built_in">&amp;</span> 0.0197 <span class="keyword">\\</span> </span><br><span class="line">    Mesh 5 <span class="built_in">&amp;</span> <span class="built_in">$</span>412.66 <span class="keyword">\times</span> 10<span class="built_in">^</span>4<span class="built_in">$</span> <span class="built_in">&amp;</span> 0.5689 <span class="built_in">&amp;</span> 0.0197 <span class="keyword">\\</span> </span><br><span class="line">    <span class="keyword">\bottomrule</span></span><br><span class="line">  <span class="keyword">\end</span>&#123;tabular&#125;<span class="comment">%</span></span><br><span class="line">  <span class="keyword">\label</span>&#123;tab:commom&#125;<span class="comment">%</span></span><br><span class="line"><span class="keyword">\end</span>&#123;table&#125;<span class="comment">%</span></span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250529120855733.png" alt="image-20250529120855733" style="zoom:67%;" /></p>
<h3 id="等页宽三线表">等页宽三线表</h3>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;table&#125;[H]</span><br><span class="line">  <span class="keyword">\centering</span></span><br><span class="line">  <span class="keyword">\caption</span>&#123;Five different mesh densities.&#125;</span><br><span class="line">  <span class="comment">%\renewcommand\arraystretch&#123;1.6&#125;</span></span><br><span class="line">  <span class="keyword">\begin</span>&#123;tabularx&#125;&#123;<span class="keyword">\linewidth</span>&#125;&#123;CCCC&#125; <span class="comment">% C 表示自动填充列并居中</span></span><br><span class="line">    <span class="keyword">\toprule</span></span><br><span class="line">    <span class="built_in">&amp;</span> Total cells <span class="built_in">&amp;</span> <span class="built_in">$</span><span class="keyword">\mathrm</span>&#123;C&#125;<span class="built_in">_</span><span class="keyword">\mathrm</span>&#123;L&#125;<span class="built_in">$</span> <span class="built_in">&amp;</span> <span class="built_in">$</span><span class="keyword">\mathrm</span>&#123;C&#125;<span class="built_in">_</span><span class="keyword">\mathrm</span>&#123;D&#125;<span class="built_in">$</span> <span class="keyword">\\</span></span><br><span class="line">    <span class="keyword">\midrule</span></span><br><span class="line">    Mesh 1 <span class="built_in">&amp;</span> <span class="built_in">$</span>33.45 <span class="keyword">\times</span> 10<span class="built_in">^</span>4<span class="built_in">$</span>  <span class="built_in">&amp;</span> 0.5606 <span class="built_in">&amp;</span> 0.0225 <span class="keyword">\\</span> </span><br><span class="line">    Mesh 2 <span class="built_in">&amp;</span> <span class="built_in">$</span>103.24 <span class="keyword">\times</span> 10<span class="built_in">^</span>4<span class="built_in">$</span> <span class="built_in">&amp;</span> 0.5656 <span class="built_in">&amp;</span> 0.0202 <span class="keyword">\\</span> </span><br><span class="line">    Mesh 3 <span class="built_in">&amp;</span> <span class="built_in">$</span>200.16 <span class="keyword">\times</span> 10<span class="built_in">^</span>4<span class="built_in">$</span> <span class="built_in">&amp;</span> 0.5689 <span class="built_in">&amp;</span> 0.0197 <span class="keyword">\\</span> </span><br><span class="line">    Mesh 4 <span class="built_in">&amp;</span> <span class="built_in">$</span>305.47 <span class="keyword">\times</span> 10<span class="built_in">^</span>4<span class="built_in">$</span> <span class="built_in">&amp;</span> 0.5689 <span class="built_in">&amp;</span> 0.0197 <span class="keyword">\\</span> </span><br><span class="line">    Mesh 5 <span class="built_in">&amp;</span> <span class="built_in">$</span>412.66 <span class="keyword">\times</span> 10<span class="built_in">^</span>4<span class="built_in">$</span> <span class="built_in">&amp;</span> 0.5689 <span class="built_in">&amp;</span> 0.0197 <span class="keyword">\\</span> </span><br><span class="line">    <span class="keyword">\bottomrule</span></span><br><span class="line">  <span class="keyword">\end</span>&#123;tabularx&#125;<span class="comment">%</span></span><br><span class="line">  <span class="keyword">\label</span>&#123;tab:same<span class="built_in">_</span>textwidth&#125;<span class="comment">%</span></span><br><span class="line"><span class="keyword">\end</span>&#123;table&#125;<span class="comment">%</span></span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250529121102129.png" alt="image-20250529121102129" style="zoom:67%;" /></p>
<h3 id="跨行跨列表格">跨行跨列表格</h3>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;table&#125;[H]</span><br><span class="line">  <span class="keyword">\centering</span></span><br><span class="line">  <span class="keyword">\caption</span>&#123;跨行跨列表格&#125;</span><br><span class="line">  <span class="keyword">\renewcommand</span><span class="keyword">\arraystretch</span>&#123;1.6&#125;</span><br><span class="line">  <span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|c|c|c|c|&#125;</span><br><span class="line">    <span class="keyword">\hline</span></span><br><span class="line">    <span class="keyword">\multicolumn</span>&#123;3&#125;&#123;|r|&#125;&#123;&#125; <span class="built_in">&amp;</span> 转移特性曲线的象限 <span class="built_in">&amp;</span> 输出特性曲线的象限 <span class="keyword">\\</span></span><br><span class="line">    <span class="keyword">\hline</span></span><br><span class="line">    <span class="keyword">\multicolumn</span>&#123;1&#125;&#123;|c|&#125;&#123;<span class="keyword">\multirow</span>&#123;4&#125;[4]&#123;*&#125;&#123;<span class="keyword">\rotatebox</span>&#123;90&#125;&#123;结型场效应管&#125;&#125;&#125; <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;2&#125;&#123;c|&#125;&#123;<span class="keyword">\multirow</span>&#123;2&#125;[2]&#123;*&#125;&#123;N沟道&#125;&#125; <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;1&#125;&#123;c|&#125;&#123;<span class="keyword">\multirow</span>&#123;2&#125;[2]&#123;*&#125;&#123;2&#125;&#125; <span class="built_in">&amp;</span> <span class="keyword">\multirow</span>&#123;2&#125;[2]&#123;*&#125;&#123;1&#125; <span class="keyword">\\</span></span><br><span class="line">    <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;2&#125;&#123;c|&#125;&#123;&#125; <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;1&#125;&#123;c|&#125;&#123;&#125; <span class="built_in">&amp;</span>  <span class="keyword">\\</span></span><br><span class="line">    <span class="keyword">\cline</span>&#123;2-5&#125;          <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;2&#125;&#123;c|&#125;&#123;<span class="keyword">\multirow</span>&#123;2&#125;[2]&#123;*&#125;&#123;P沟道&#125;&#125; <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;1&#125;&#123;c|&#125;&#123;<span class="keyword">\multirow</span>&#123;2&#125;[2]&#123;*&#125;&#123;4&#125;&#125; <span class="built_in">&amp;</span> <span class="keyword">\multirow</span>&#123;2&#125;[2]&#123;*&#125;&#123;3&#125; <span class="keyword">\\</span></span><br><span class="line">    <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;2&#125;&#123;c|&#125;&#123;&#125; <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;1&#125;&#123;c|&#125;&#123;&#125; <span class="built_in">&amp;</span>  <span class="keyword">\\</span></span><br><span class="line">    <span class="keyword">\hline</span></span><br><span class="line">    <span class="keyword">\multicolumn</span>&#123;1&#125;&#123;|c|&#125;&#123;<span class="keyword">\multirow</span>&#123;4&#125;[8]&#123;*&#125;&#123;<span class="keyword">\begin</span>&#123;turn&#125;&#123;90&#125;&#123;绝缘栅型场效应管&#125;<span class="keyword">\end</span>&#123;turn&#125;&#125;&#125; <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;1&#125;&#123;c|&#125;&#123;<span class="keyword">\multirow</span>&#123;2&#125;[4]&#123;*&#125;&#123;N沟道&#125;&#125; <span class="built_in">&amp;</span> 增强型   <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;1&#125;&#123;c|&#125;&#123;2&#125; <span class="built_in">&amp;</span> 1 <span class="keyword">\\</span></span><br><span class="line">    <span class="keyword">\cline</span>&#123;3-5&#125;          <span class="built_in">&amp;</span>       <span class="built_in">&amp;</span> 耗尽型   <span class="built_in">&amp;</span> 1，2   <span class="built_in">&amp;</span> 1 <span class="keyword">\\</span></span><br><span class="line">    <span class="keyword">\cline</span>&#123;2-5&#125;          <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;1&#125;&#123;c|&#125;&#123;<span class="keyword">\multirow</span>&#123;2&#125;[4]&#123;*&#125;&#123;P沟道&#125;&#125; <span class="built_in">&amp;</span> 增强型   <span class="built_in">&amp;</span> <span class="keyword">\multicolumn</span>&#123;1&#125;&#123;c|&#125;&#123;3&#125; <span class="built_in">&amp;</span> 3 <span class="keyword">\\</span></span><br><span class="line">    <span class="keyword">\cline</span>&#123;3-5&#125;          <span class="built_in">&amp;</span>       <span class="built_in">&amp;</span> 耗尽型   <span class="built_in">&amp;</span> 2，4   <span class="built_in">&amp;</span> 3 <span class="keyword">\\</span></span><br><span class="line">    <span class="keyword">\hline</span></span><br><span class="line">  <span class="keyword">\end</span>&#123;tabular&#125;<span class="comment">%</span></span><br><span class="line">  <span class="keyword">\label</span>&#123;tab:addlabel&#125;<span class="comment">%</span></span><br><span class="line"><span class="keyword">\end</span>&#123;table&#125;<span class="comment">%</span></span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250809091710630.png" alt="image-20250809091710630" style="zoom:67%;" /></p>
<h3 id="表格中包含图">表格中包含图</h3>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;table&#125;[H]</span><br><span class="line"><span class="keyword">\centering</span></span><br><span class="line"><span class="keyword">\caption</span>&#123;表格中包含图&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|c|&#125;</span><br><span class="line"><span class="keyword">\hline</span></span><br><span class="line">文字 <span class="built_in">&amp;</span> </span><br><span class="line"><span class="keyword">\begin</span>&#123;minipage&#125;[c][5cm]&#123;8cm&#125; <span class="comment">% [c] 表示垂直居中</span></span><br><span class="line">  <span class="keyword">\includegraphics</span>[width=8cm]&#123;tree.png&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;minipage&#125; <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\hline</span></span><br><span class="line">跨页 <span class="built_in">&amp;</span> 表格 <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\hline</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular&#125;</span><br><span class="line"><span class="keyword">\label</span>&#123;tab:fig<span class="built_in">_</span>in<span class="built_in">_</span>tab&#125;<span class="comment">%</span></span><br><span class="line"><span class="keyword">\end</span>&#123;table&#125;<span class="comment">%</span></span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250809092038553.png" alt="image-20250809092038553" style="zoom:67%;" /></p>
<h3 id="跨页长表格">跨页长表格</h3>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;center&#125;</span><br><span class="line"><span class="keyword">\begingroup</span></span><br><span class="line"><span class="keyword">\small</span> <span class="comment">% 整个longtable小一号</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;longtable&#125;&#123;lccccc&#125;</span><br><span class="line"><span class="keyword">\caption</span>&#123;跨页长表格&#125;<span class="keyword">\label</span>&#123;tab:long1&#125; <span class="keyword">\\</span> </span><br><span class="line"><span class="comment">% 表格第一页表头显示内容</span></span><br><span class="line"><span class="keyword">\toprule</span></span><br><span class="line"><span class="keyword">\textbf</span>&#123;家庭特征&#125; <span class="built_in">&amp;</span> <span class="keyword">\textbf</span>&#123;续发病例数（例）&#125;  <span class="built_in">&amp;</span> <span class="keyword">\textbf</span>&#123;家庭密接数（例）&#125;</span><br><span class="line"><span class="built_in">&amp;</span><span class="keyword">\makecell</span>&#123;<span class="keyword">\textbf</span>&#123;家庭二代续发率&#125;<span class="keyword">\\</span> <span class="keyword">\textbf</span>&#123;[<span class="keyword">\%</span> (95<span class="keyword">\%</span>CI)]&#125;&#125; <span class="built_in">&amp;</span> <span class="keyword">\textbf</span>&#123;<span class="built_in">$</span>x<span class="built_in">^</span>2<span class="built_in">$</span>值&#125; <span class="built_in">&amp;</span> <span class="keyword">\textbf</span>&#123;P值&#125; <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\midrule</span></span><br><span class="line"><span class="keyword">\endfirsthead</span> </span><br><span class="line"></span><br><span class="line"><span class="comment">%后续页面表头显示内容</span></span><br><span class="line"><span class="keyword">\multicolumn</span>&#123;6&#125;&#123;c&#125;&#123;表~<span class="keyword">\ref</span>&#123;tab:long1&#125;（续）&#125;<span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\toprule</span></span><br><span class="line"><span class="keyword">\textbf</span>&#123;家庭特征&#125; <span class="built_in">&amp;</span> <span class="keyword">\textbf</span>&#123;续发病例数（例）&#125;  <span class="built_in">&amp;</span> <span class="keyword">\textbf</span>&#123;家庭密接数（例）&#125;</span><br><span class="line"><span class="built_in">&amp;</span><span class="keyword">\makecell</span>&#123;<span class="keyword">\textbf</span>&#123;家庭二代续发率&#125;<span class="keyword">\\</span> <span class="keyword">\textbf</span>&#123;[<span class="keyword">\%</span> (95<span class="keyword">\%</span>CI)]&#125;&#125;  <span class="built_in">&amp;</span> <span class="keyword">\textbf</span>&#123;<span class="built_in">$</span>x<span class="built_in">^</span>2<span class="built_in">$</span>值&#125; <span class="built_in">&amp;</span> <span class="keyword">\textbf</span>&#123;P值&#125; <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\midrule</span></span><br><span class="line"><span class="keyword">\endhead</span></span><br><span class="line"></span><br><span class="line"><span class="comment">% 表格最后一页前 每页底部显示内容</span></span><br><span class="line"><span class="keyword">\bottomrule</span></span><br><span class="line"><span class="keyword">\endfoot</span></span><br><span class="line"></span><br><span class="line"><span class="comment">% 表格最后一页尾部显示内容</span></span><br><span class="line"><span class="keyword">\bottomrule</span></span><br><span class="line"><span class="keyword">\endlastfoot</span></span><br><span class="line"></span><br><span class="line">总体蓄发率 <span class="built_in">&amp;</span> 201  <span class="built_in">&amp;</span> 477 <span class="built_in">&amp;</span> 42.14 (37.69,46.72) <span class="built_in">&amp;</span>  <span class="built_in">&amp;</span>  <span class="keyword">\\</span></span><br><span class="line">发生传播家庭蓄发率  <span class="built_in">&amp;</span> 201  <span class="built_in">&amp;</span> 477 <span class="built_in">&amp;</span> 42.14 (37.69,46.72) <span class="built_in">&amp;</span>  <span class="built_in">&amp;</span>  <span class="keyword">\\</span></span><br><span class="line">家庭结构 <span class="built_in">&amp;</span> <span class="built_in">&amp;</span> <span class="built_in">&amp;</span> <span class="built_in">&amp;</span>        0.309 <span class="built_in">&amp;</span>	0.636 <span class="keyword">\\</span></span><br><span class="line">二代户  <span class="built_in">&amp;</span> 83 <span class="built_in">&amp;</span>	190	<span class="built_in">&amp;</span> 43.68 (36.57,51.05)	<span class="built_in">&amp;</span> <span class="built_in">&amp;</span> <span class="keyword">\\</span>	</span><br><span class="line">三代户  <span class="built_in">&amp;</span>118<span class="built_in">&amp;</span>	287	<span class="built_in">&amp;</span> 41.11 (35.40,47.06) <span class="built_in">&amp;</span> <span class="built_in">&amp;</span><span class="keyword">\\</span>		</span><br><span class="line">家庭规模  <span class="built_in">&amp;</span> <span class="built_in">&amp;</span> <span class="built_in">&amp;</span> <span class="built_in">&amp;</span>			5.191	<span class="built_in">&amp;</span> 0.027 <span class="keyword">\\</span></span><br><span class="line">总体蓄发率 <span class="built_in">&amp;</span> 201  <span class="built_in">&amp;</span> 477 <span class="built_in">&amp;</span> 42.14 (37.69,46.72) <span class="built_in">&amp;</span>  <span class="built_in">&amp;</span>  <span class="keyword">\\</span></span><br><span class="line">发生传播家庭蓄发率  <span class="built_in">&amp;</span> 201  <span class="built_in">&amp;</span> 477 <span class="built_in">&amp;</span> 42.14 (37.69,46.72) <span class="built_in">&amp;</span>  <span class="built_in">&amp;</span>  <span class="keyword">\\</span></span><br><span class="line">家庭结构 <span class="built_in">&amp;</span> <span class="built_in">&amp;</span> <span class="built_in">&amp;</span> <span class="built_in">&amp;</span>        0.309 <span class="built_in">&amp;</span>	0.636 <span class="keyword">\\</span></span><br><span class="line">二代户  <span class="built_in">&amp;</span> 83 <span class="built_in">&amp;</span>	190	<span class="built_in">&amp;</span> 43.68 (36.57,51.05)	<span class="built_in">&amp;</span> <span class="built_in">&amp;</span> <span class="keyword">\\</span>	</span><br><span class="line">三代户  <span class="built_in">&amp;</span>118<span class="built_in">&amp;</span>	287	<span class="built_in">&amp;</span> 41.11 (35.40,47.06) <span class="built_in">&amp;</span> <span class="built_in">&amp;</span><span class="keyword">\\</span>		</span><br><span class="line">家庭规模  <span class="built_in">&amp;</span> <span class="built_in">&amp;</span> <span class="built_in">&amp;</span> <span class="built_in">&amp;</span>			5.191	<span class="built_in">&amp;</span> 0.027 <span class="keyword">\\</span></span><br><span class="line">总体蓄发率 <span class="built_in">&amp;</span> 201  <span class="built_in">&amp;</span> 477 <span class="built_in">&amp;</span> 42.14 (37.69,46.72) <span class="built_in">&amp;</span>  <span class="built_in">&amp;</span>  <span class="keyword">\\</span></span><br><span class="line">发生传播家庭蓄发率  <span class="built_in">&amp;</span> 201  <span class="built_in">&amp;</span> 477 <span class="built_in">&amp;</span> 42.14 (37.69,46.72) <span class="built_in">&amp;</span>  <span class="built_in">&amp;</span>  <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\end</span>&#123;longtable&#125;</span><br><span class="line"><span class="keyword">\endgroup</span> <span class="comment">% 字号恢复</span></span><br><span class="line"><span class="keyword">\end</span>&#123;center&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250809092539328.png" alt="image-20250809092539328" style="zoom:67%;" /></p>
<h3 id="跨页长表格宽度设置为页面宽度">跨页长表格宽度设置为页面宽度</h3>
<p>这里需要采用分文件写法，将表格内容放到<code>set_table_width.tex</code>文件中，在主文件中插入如下代码，其中<code>\textwidth</code>控制表格宽度。</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\LTXtable</span>&#123;<span class="keyword">\textwidth</span>&#125;&#123;set<span class="built_in">_</span>table<span class="built_in">_</span>width.tex&#125;</span><br></pre></td></tr></table></figure>
<p>下面的内容单独放到<code>set_table_width.tex</code>中。</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% set_table_width.tex</span></span><br><span class="line"><span class="keyword">\begingroup</span></span><br><span class="line"><span class="keyword">\small</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;longtable&#125;&#123;LCCC&#125;</span><br><span class="line"><span class="keyword">\caption</span>&#123;跨页长表格设置宽度&#125;<span class="keyword">\label</span>&#123;tab:set<span class="built_in">_</span>width&#125; <span class="keyword">\\</span> </span><br><span class="line"><span class="comment">% 表格第一页表头显示内容</span></span><br><span class="line"><span class="keyword">\toprule</span></span><br><span class="line"><span class="keyword">\textbf</span>&#123;家庭特征&#125; <span class="built_in">&amp;</span> <span class="keyword">\makecell</span>&#123;<span class="keyword">\textbf</span>&#123;发生家庭传播（n=99）&#125;<span class="keyword">\\</span> <span class="keyword">\textbf</span>&#123;[户,百分比（<span class="keyword">\%</span>）]&#125;&#125;  <span class="built_in">&amp;</span> <span class="keyword">\makecell</span>&#123;<span class="keyword">\textbf</span>&#123;未发生家庭传播（n=57）&#125;<span class="keyword">\\</span> <span class="keyword">\textbf</span>&#123;[户,百分比（<span class="keyword">\%</span>）]&#125;&#125;</span><br><span class="line"><span class="built_in">&amp;</span><span class="keyword">\makecell</span>&#123;<span class="keyword">\textbf</span>&#123;总计（n=156）&#125;<span class="keyword">\\</span> <span class="keyword">\textbf</span>&#123;[户,百分比（<span class="keyword">\%</span>）]&#125;&#125;  <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\midrule</span></span><br><span class="line"><span class="keyword">\endfirsthead</span> </span><br><span class="line"></span><br><span class="line"><span class="comment">%后续页面表头显示内容</span></span><br><span class="line"><span class="keyword">\multicolumn</span>&#123;4&#125;&#123;c&#125;&#123;表~<span class="keyword">\ref</span>&#123;tab:set<span class="built_in">_</span>width&#125;（续）&#125;<span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\toprule</span></span><br><span class="line"><span class="keyword">\textbf</span>&#123;家庭特征&#125; <span class="built_in">&amp;</span> <span class="keyword">\makecell</span>&#123;<span class="keyword">\textbf</span>&#123;发生家庭传播（n=99）&#125;<span class="keyword">\\</span> <span class="keyword">\textbf</span>&#123;[户,百分比（<span class="keyword">\%</span>）]&#125;&#125;  <span class="built_in">&amp;</span> <span class="keyword">\makecell</span>&#123;<span class="keyword">\textbf</span>&#123;未发生家庭传播（n=57）&#125;<span class="keyword">\\</span> <span class="keyword">\textbf</span>&#123;[户,百分比（<span class="keyword">\%</span>）]&#125;&#125;</span><br><span class="line"><span class="built_in">&amp;</span><span class="keyword">\makecell</span>&#123;<span class="keyword">\textbf</span>&#123;总计（n=156）&#125;<span class="keyword">\\</span> <span class="keyword">\textbf</span>&#123;[户,百分比（<span class="keyword">\%</span>）]&#125;&#125; <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\midrule</span></span><br><span class="line"><span class="keyword">\endhead</span></span><br><span class="line"></span><br><span class="line"><span class="comment">% 表格最后一页前 每页底部显示内容</span></span><br><span class="line"><span class="keyword">\bottomrule</span></span><br><span class="line"><span class="keyword">\endfoot</span></span><br><span class="line"></span><br><span class="line"><span class="comment">% 表格最后一页尾部显示内容</span></span><br><span class="line"><span class="keyword">\bottomrule</span></span><br><span class="line"><span class="keyword">\endlastfoot</span></span><br><span class="line">家庭结构                    <span class="built_in">&amp;</span> ~          <span class="built_in">&amp;</span> ~          <span class="built_in">&amp;</span> ~            <span class="keyword">\\</span></span><br><span class="line">~ ~ 二代户                 <span class="built_in">&amp;</span> 47（47.5<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 29（50.9<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 76（48.7<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">~ ~ 三代户                 <span class="built_in">&amp;</span> 52（52.5<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 28（49.1<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 80（51.3<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">家庭规模                    <span class="built_in">&amp;</span> ~          <span class="built_in">&amp;</span> ~          <span class="built_in">&amp;</span> ~            <span class="keyword">\\</span></span><br><span class="line">~ ~ 2<span class="keyword">\textasciitilde</span>&#123;&#125;3 <span class="built_in">&amp;</span> 27（27.3<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 29（50.9<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 56（35.9<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">~ ~ 4<span class="keyword">\textasciitilde</span>&#123;&#125;5 <span class="built_in">&amp;</span> 60（60.6<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 26（45.6<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 86（55.1<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">~ ~ 6+                  <span class="built_in">&amp;</span> 12（12.1<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 2（3.5<span class="keyword">\%</span>）   <span class="built_in">&amp;</span> 14（9.0<span class="keyword">\%</span>）    <span class="keyword">\\</span></span><br><span class="line">住房类型                    <span class="built_in">&amp;</span> ~          <span class="built_in">&amp;</span> ~          <span class="built_in">&amp;</span> ~            <span class="keyword">\\</span></span><br><span class="line">~ ~ 楼房                  <span class="built_in">&amp;</span> 74（74.7<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 41（71.9<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 115（73.7<span class="keyword">\%</span>）  <span class="keyword">\\</span></span><br><span class="line">~ ~ 平房                  <span class="built_in">&amp;</span> 22（22.2<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 15（26.3<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 37（23.7<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">~ ~ 其他                  <span class="built_in">&amp;</span> 3（3.0<span class="keyword">\%</span>）   <span class="built_in">&amp;</span> 1（1.8<span class="keyword">\%</span>）   <span class="built_in">&amp;</span> 4（2.6<span class="keyword">\%</span>）     <span class="keyword">\\</span></span><br><span class="line">人均住房面积（㎡）               <span class="built_in">&amp;</span> ~          <span class="built_in">&amp;</span> ~          <span class="built_in">&amp;</span> ~            <span class="keyword">\\</span></span><br><span class="line">~ ~ ≤20                 <span class="built_in">&amp;</span> 22（22.2<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 10（17.5<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 32（20.5<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">~ ~ 20-40               <span class="built_in">&amp;</span> 58（58.6<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 36（63.2<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 94（60.3<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">~ ~ ≥40                 <span class="built_in">&amp;</span> 19（19.2<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 11（19.3<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 30（19.2<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">家庭人均年收入（元）              <span class="built_in">&amp;</span> ~          <span class="built_in">&amp;</span> ~          <span class="built_in">&amp;</span> ~            <span class="keyword">\\</span></span><br><span class="line">~ ~ 5000                <span class="built_in">&amp;</span> 3（3.0<span class="keyword">\%</span>）   <span class="built_in">&amp;</span> 0（0<span class="keyword">\%</span>）     <span class="built_in">&amp;</span> 3（1.9<span class="keyword">\%</span>）     <span class="keyword">\\</span></span><br><span class="line">~ ~ 5000-19999          <span class="built_in">&amp;</span> 9（9.1<span class="keyword">\%</span>）   <span class="built_in">&amp;</span> 6（10.5<span class="keyword">\%</span>）  <span class="built_in">&amp;</span> 15（9.6<span class="keyword">\%</span>）    <span class="keyword">\\</span></span><br><span class="line">~ ~ 20000-49999         <span class="built_in">&amp;</span> 18（18.2<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 9（15.8<span class="keyword">\%</span>）  <span class="built_in">&amp;</span> 27（17.3<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">~ ~ 50000-99999         <span class="built_in">&amp;</span> 29（29.3<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 15（26.3<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 44（28.2<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">~ ~ ≥100000             <span class="built_in">&amp;</span> 40（40.4<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 27（47.4<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 67（42.9<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">吸烟家庭                    <span class="built_in">&amp;</span> ~          <span class="built_in">&amp;</span> ~          <span class="built_in">&amp;</span> ~            <span class="keyword">\\</span></span><br><span class="line">~ ~ 是                   <span class="built_in">&amp;</span> 57（57.6<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 26（45.6<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 83（53.2<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">~ ~ 否                   <span class="built_in">&amp;</span> 42（42.4<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 31（54.4<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 73（46.8<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">家庭地区                    <span class="built_in">&amp;</span> ~          <span class="built_in">&amp;</span> ~          <span class="built_in">&amp;</span> ~            <span class="keyword">\\</span></span><br><span class="line">~ ~ 富阳地区                <span class="built_in">&amp;</span> 20（20.2<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 21（36.8<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 41（26.3<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">~ ~ 嘉兴地区                <span class="built_in">&amp;</span> 29（29.3<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 6（10.5<span class="keyword">\%</span>）  <span class="built_in">&amp;</span> 35（22.4<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">~ ~ 丽水地区                <span class="built_in">&amp;</span> 34（34.3<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 14（24.6<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 48（30.8<span class="keyword">\%</span>）   <span class="keyword">\\</span></span><br><span class="line">~ ~ 温州地区                <span class="built_in">&amp;</span> 16（16.2<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 16（28.1<span class="keyword">\%</span>） <span class="built_in">&amp;</span> 32（20.5<span class="keyword">\%</span>）        </span><br><span class="line"><span class="keyword">\end</span>&#123;longtable&#125;</span><br><span class="line"><span class="keyword">\endgroup</span></span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250809093452734.png" alt="image-20250809093452734" style="zoom:67%;" /></p>
<h3 id="并排表格">并排表格</h3>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;table&#125;[htbp]</span><br><span class="line">    <span class="keyword">\centering</span></span><br><span class="line">    <span class="comment">% 左侧表格（宽度略小于一半，留出间距）</span></span><br><span class="line">    <span class="keyword">\begin</span>&#123;minipage&#125;[t]&#123;0.7<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\caption</span>&#123;道路资源花费&#125;<span class="keyword">\label</span>&#123;tab:left&#125;</span><br><span class="line">        <span class="keyword">\vspace</span>*&#123;-.5<span class="keyword">\baselineskip</span>&#125;&#123;<span class="keyword">\small</span> Table~<span class="keyword">\ref</span>&#123;tab:left&#125;<span class="keyword">\ </span><span class="keyword">\ </span>Road resource cost&#125; <span class="keyword">\\</span>[.5<span class="keyword">\baselineskip</span>]</span><br><span class="line">        <span class="keyword">\tabcolsep</span> 15pt <span class="comment">%space between two columns. 用于调整列间距</span></span><br><span class="line">        <span class="keyword">\begin</span>&#123;tabularx&#125;&#123;<span class="keyword">\linewidth</span>&#125;&#123;CCCCCCCC&#125;</span><br><span class="line">            <span class="keyword">\hline</span> </span><br><span class="line">            <span class="built_in">$</span>e<span class="built_in">_</span>&#123;l&#125;<span class="built_in">$</span><span class="built_in">&amp;</span> <span class="built_in">$</span>e<span class="built_in">_</span>&#123;1&#125;<span class="built_in">$</span> <span class="built_in">&amp;</span><span class="built_in">$</span>e<span class="built_in">_</span>&#123;2&#125;<span class="built_in">$</span><span class="built_in">&amp;</span> <span class="built_in">$</span>e<span class="built_in">_</span>&#123;3&#125;<span class="built_in">$</span><span class="built_in">&amp;</span><span class="built_in">$</span>e<span class="built_in">_</span>&#123;4&#125;<span class="built_in">$</span><span class="built_in">&amp;</span><span class="built_in">$</span>e<span class="built_in">_</span>&#123;5&#125;<span class="built_in">$</span><span class="built_in">&amp;</span><span class="built_in">$</span>e<span class="built_in">_</span>&#123;6&#125;<span class="built_in">$</span><span class="built_in">&amp;</span><span class="built_in">$</span>e<span class="built_in">_</span>&#123;7&#125;<span class="built_in">$</span>  <span class="keyword">\\</span></span><br><span class="line">            <span class="keyword">\hline</span></span><br><span class="line">            <span class="built_in">$</span>c<span class="built_in">_</span>&#123;l&#125;(1)<span class="built_in">$</span><span class="built_in">&amp;</span> 1 <span class="built_in">&amp;</span> 2 <span class="built_in">&amp;</span> 2<span class="built_in">&amp;</span>3 <span class="built_in">&amp;</span>2 <span class="built_in">&amp;</span>2 <span class="built_in">&amp;</span>1<span class="keyword">\\</span></span><br><span class="line">            <span class="keyword">\hline</span></span><br><span class="line">            <span class="built_in">$</span>c<span class="built_in">_</span>&#123;l&#125;(2)<span class="built_in">$</span> <span class="built_in">&amp;</span> 3 <span class="built_in">&amp;</span> 3 <span class="built_in">&amp;</span> 4<span class="built_in">&amp;</span>5 <span class="built_in">&amp;</span>4 <span class="built_in">&amp;</span>4 <span class="built_in">&amp;</span>5<span class="keyword">\\</span></span><br><span class="line">            <span class="keyword">\hline</span></span><br><span class="line">            <span class="built_in">$</span>c<span class="built_in">_</span>&#123;l&#125;(3)<span class="built_in">$</span> <span class="built_in">&amp;</span> 5 <span class="built_in">&amp;</span> 6 <span class="built_in">&amp;</span> 7<span class="built_in">&amp;</span>7 <span class="built_in">&amp;</span>6 <span class="built_in">&amp;</span>6 <span class="built_in">&amp;</span>7<span class="keyword">\\</span></span><br><span class="line">            <span class="keyword">\hline</span></span><br><span class="line">        <span class="keyword">\end</span>&#123;tabularx&#125;</span><br><span class="line">    <span class="keyword">\end</span>&#123;minipage&#125;</span><br><span class="line">    <span class="keyword">\hfill</span> <span class="comment">% 填充间距</span></span><br><span class="line">    <span class="comment">% 右侧表格</span></span><br><span class="line">    <span class="keyword">\begin</span>&#123;minipage&#125;[t]&#123;0.26<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\caption</span>&#123;充电资源花费&#125;<span class="keyword">\label</span>&#123;tab:right&#125;</span><br><span class="line">        <span class="keyword">\vspace</span>*&#123;-.5<span class="keyword">\baselineskip</span>&#125;&#123;<span class="keyword">\small</span> Table~<span class="keyword">\ref</span>&#123;tab:right&#125;<span class="keyword">\ </span><span class="keyword">\ </span>Charing resource cost&#125; <span class="keyword">\\</span>[.5<span class="keyword">\baselineskip</span>]</span><br><span class="line">        <span class="keyword">\tabcolsep</span> 15pt <span class="comment">%space between two columns. 用于调整列间距</span></span><br><span class="line">        <span class="keyword">\begin</span>&#123;tabularx&#125;&#123;<span class="keyword">\linewidth</span>&#125;&#123;CCC&#125;</span><br><span class="line">         <span class="keyword">\hline</span> </span><br><span class="line">         <span class="built_in">$</span>r<span class="built_in">_</span>&#123;m&#125;<span class="built_in">$</span><span class="built_in">&amp;</span> <span class="built_in">$</span>r<span class="built_in">_</span>&#123;1&#125;<span class="built_in">$</span> <span class="built_in">&amp;</span><span class="built_in">$</span>r<span class="built_in">_</span>&#123;2&#125;<span class="built_in">$</span>   <span class="keyword">\\</span></span><br><span class="line">         <span class="keyword">\hline</span></span><br><span class="line">         <span class="built_in">$</span><span class="keyword">\xi</span><span class="built_in">_</span>&#123;m&#125;(1)<span class="built_in">$</span><span class="built_in">&amp;</span> 2 <span class="built_in">&amp;</span> 3   <span class="keyword">\\</span></span><br><span class="line">         <span class="keyword">\hline</span></span><br><span class="line">         <span class="built_in">$</span><span class="keyword">\xi</span><span class="built_in">_</span>&#123;m&#125;(2)<span class="built_in">$</span> <span class="built_in">&amp;</span> 6 <span class="built_in">&amp;</span> 8  <span class="keyword">\\</span></span><br><span class="line">         <span class="keyword">\hline</span></span><br><span class="line">         <span class="built_in">$</span><span class="keyword">\xi</span><span class="built_in">_</span>&#123;m&#125;(3)<span class="built_in">$</span> <span class="built_in">&amp;</span> 7 <span class="built_in">&amp;</span> 10  <span class="keyword">\\</span></span><br><span class="line">         <span class="keyword">\hline</span></span><br><span class="line">        <span class="keyword">\end</span>&#123;tabularx&#125;        </span><br><span class="line">    <span class="keyword">\end</span>&#123;minipage&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;table&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250809093623659.png" alt="image-20250809093623659" style="zoom: 80%;" /></p>
<h3 id="并排子表格">并排子表格</h3>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\begin</span>&#123;table&#125;[htbp]</span><br><span class="line">    <span class="keyword">\centering</span></span><br><span class="line">    <span class="comment">% 左侧子表</span></span><br><span class="line">    <span class="keyword">\begin</span>&#123;subtable&#125;[t]&#123;0.48<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|c|&#125;</span><br><span class="line">            <span class="keyword">\hline</span></span><br><span class="line">            项目 <span class="built_in">&amp;</span> 值 <span class="keyword">\\</span></span><br><span class="line">            <span class="keyword">\hline</span></span><br><span class="line">            A <span class="built_in">&amp;</span> 1 <span class="keyword">\\</span></span><br><span class="line">            B <span class="built_in">&amp;</span> 2 <span class="keyword">\\</span></span><br><span class="line">            <span class="keyword">\hline</span></span><br><span class="line">        <span class="keyword">\end</span>&#123;tabular&#125;</span><br><span class="line">        <span class="keyword">\caption</span>&#123;子表A&#125;</span><br><span class="line">        <span class="keyword">\label</span>&#123;tab:subA&#125;</span><br><span class="line">    <span class="keyword">\end</span>&#123;subtable&#125;</span><br><span class="line">    <span class="keyword">\hfill</span></span><br><span class="line">    <span class="comment">% 右侧子表</span></span><br><span class="line">    <span class="keyword">\begin</span>&#123;subtable&#125;[t]&#123;0.48<span class="keyword">\textwidth</span>&#125;</span><br><span class="line">        <span class="keyword">\centering</span></span><br><span class="line">        <span class="keyword">\begin</span>&#123;tabular&#125;&#123;|c|c|c|&#125;</span><br><span class="line">            <span class="keyword">\hline</span></span><br><span class="line">            项目 <span class="built_in">&amp;</span> 值 <span class="built_in">&amp;</span> 备注 <span class="keyword">\\</span></span><br><span class="line">            <span class="keyword">\hline</span></span><br><span class="line">            X <span class="built_in">&amp;</span> 10 <span class="built_in">&amp;</span> 测试 <span class="keyword">\\</span></span><br><span class="line">            Y <span class="built_in">&amp;</span> 20 <span class="built_in">&amp;</span> 示例 <span class="keyword">\\</span></span><br><span class="line">            <span class="keyword">\hline</span></span><br><span class="line">        <span class="keyword">\end</span>&#123;tabular&#125;</span><br><span class="line">        <span class="keyword">\caption</span>&#123;子表B&#125;</span><br><span class="line">        <span class="keyword">\label</span>&#123;tab:subB&#125;</span><br><span class="line">    <span class="keyword">\end</span>&#123;subtable&#125;</span><br><span class="line">    <span class="keyword">\caption</span>&#123;并排子表示例&#125;</span><br><span class="line">    <span class="keyword">\label</span>&#123;tab:main&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;table&#125;</span><br></pre></td></tr></table></figure>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250809093829774.png" alt="image-20250809093829774" style="zoom:80%;" /></p>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX使用技巧</category>
      </categories>
      <tags>
        <tag>表格</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX 线圈日记本模板</title>
    <url>/2025/04/02/tools/latex/project-templates/latex-coil-diary-template/</url>
    <content><![CDATA[<p>本文给出了一个简单的<span
class="math inline">\(\LaTeX{}\)</span>线圈日记本模板，使用xelatex编译。</p>
<span id="more"></span>
<hr />
<h1 id="latex-coil-diary-template">LaTeX Coil Diary Template</h1>
<p><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL25lb3podWFuZy9sYXRleC1kaWFyeS10ZW1wbGF0ZQ==">仓库地址<i class="fa fa-external-link-alt"></i></span></p>
<p><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL25lb3podWFuZy9sYXRleC1kaWFyeS10ZW1wbGF0ZS9ibG9iL21haW4vZGlhcnkucGRm">点击查看预览效果<i class="fa fa-external-link-alt"></i></span></p>
<hr />
<h2 id="how-to-compile">How to Compile</h2>
<table>
<thead>
<tr class="header">
<th>文件</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>diary.tex</code></td>
<td>主文件</td>
</tr>
<tr class="even">
<td><code>diary.cls</code></td>
<td>库文件</td>
</tr>
<tr class="odd">
<td><code>Makefile</code></td>
<td>linux编译文件</td>
</tr>
<tr class="even">
<td><code>clean.bat</code></td>
<td>windows一键清理辅助文件</td>
</tr>
<tr class="odd">
<td><code>build.bat</code></td>
<td>windows一键编译源文件</td>
</tr>
</tbody>
</table>
<p><strong>编译方法：</strong> <code>xelatex *.tex</code> 两次</p>
<p>Windows编译便捷批处理文件：</p>
<ul>
<li><code>clean.bat</code> - 清理当前目录及子目录所有辅助文件；</li>
<li><code>build.bat</code> -
重新编译目录中所有<code>.tex</code>并清理辅助文件。</li>
</ul>
<p>Linux/Mac等类Unix快速编译：</p>
<ul>
<li><code>make</code> -
编译目录中所有<code>.tex</code>文件，不清理中间文件；</li>
<li><code>make clean</code> - 清理所有辅助文件；</li>
<li><code>make rebuild</code> -
先清理辅助文件，再编译所有.tex文件并清理辅助文件。</li>
</ul>
<p>latexmk:</p>
<ul>
<li><code>latexmk -xelatex</code> -
默认逐个编译当前目录所有.tex文件</li>
<li><code>latexmk -c</code> -
清理所有.tex文件生成的中间文件，不包括最终pdf</li>
<li><code>latexmk -C</code> -
清理所有.tex文件生成的中间文件，包括最终pdf</li>
</ul>
<hr />
<h2 id="usage">Usage</h2>
<h3 id="标题页起止日期">标题页起止日期</h3>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% 指定起止日期</span></span><br><span class="line"><span class="keyword">\BeginAt</span>[2026][01][01]                  <span class="comment">% 指定起始年月日</span></span><br><span class="line"><span class="keyword">\EndAt</span>[<span class="keyword">\getYear</span>][<span class="keyword">\getMonth</span>][<span class="keyword">\getDay</span>]    <span class="comment">% 最后一次编译当天的日期 可不修改</span></span><br></pre></td></tr></table></figure>
<p>开始日期可手动填写。[]表示采用可选默认参数，可依次省略后面的不写。
比如 <code>\BeginAt[2026]</code> 默认显示2026年01月01日，默认值在 cls
文件中修改。</p>
<p>终止日期同上。</p>
<h3 id="字体设置">字体设置</h3>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% 标题页专用字体，如不设置沿用正文字体</span></span><br><span class="line"><span class="comment">%\TitleCJKFont&#123;Noto Sans CJK SC&#125;         % 标题页中文字体</span></span><br><span class="line"><span class="comment">%\TitleFont&#123;Noto Sans&#125;                   % 标题页英文字体</span></span><br><span class="line"></span><br><span class="line"><span class="comment">% 正文字体，如不设置则使用系统默认字体</span></span><br><span class="line"><span class="comment">%\setCJKmainfont&#123;Noto Serif CJK SC&#125;      % 正文中文字体</span></span><br><span class="line"><span class="comment">%\setmainfont&#123;Noto Serif&#125;                % 正文英文字体</span></span><br></pre></td></tr></table></figure>
<p>可将上述几种字体分别设置为本地已安装的字体。</p>
<blockquote>
<p>[!NOTE] 设置正文字体后编译会出现如下警告：</p>
<p>[LaTeX Warning] Line 1: Package xeCJK: Redefining CJKfamily `’
(xeCJK) (FandolSong-Regular(0)).</p>
<p>这个是由于我们自己设置了正文字体与ctex提供的默认版本的重复导致的，可以忽略此warning。</p>
</blockquote>
<p>查看系统已安装字体：</p>
<ul>
<li>Windows: <code>dir C:\Windows\Fonts</code></li>
<li>Linux: <code>fc-list : family | sort | uniq</code></li>
<li>Mac:
<code>system_profiler SPFontsDataType | grep "Family:" | sed 's/.*Family: //' | sort | uniq</code></li>
</ul>
<p>如何搭配选择好看的字体：<span class="exturl" data-url="aHR0cHM6Ly9jaGF0Z3B0LmNvbS8=">ChatGPT<i class="fa fa-external-link-alt"></i></span></p>
<h3 id="位置日期天气">位置、日期、天气</h3>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\Address</span>[中国][北京]</span><br><span class="line"><span class="keyword">\Date</span>&#123;2026&#125;&#123;1&#125;&#123;1&#125;[<span class="keyword">\faSun</span>]<span class="comment">%</span></span><br></pre></td></tr></table></figure>
<p><code>\Address[][]</code> 两个参数设置位置信息，默认值为江苏
苏州，可在 cls 中修改。</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\Address</span>            <span class="comment">% 显示江苏苏州</span></span><br><span class="line"><span class="keyword">\Address</span>[][家]      <span class="comment">% 第一个参数设为空，只显示家</span></span><br></pre></td></tr></table></figure>
<p><code>Date{}{}{}[]</code>
前三个必选参数分别指定年月日，第四个可选参数为天气或心情等图标。
图标使用fontawesome7图标库，可选样式（solid或regular），默认是 solid。
图标命令可终端输入 <code>texdoc fontawesome7</code>
查看文档，或查看fontawesome官网。 省略后跟随前一天填写的图标。</p>
<p><code>\Date{}{}{}[]</code>
后面必须要有<code>%</code>，且该命令与日记内容之间不能有空行，否则会引入不必要的空格和空行。</p>
<h3 id="其他">其他</h3>
<p><code>\Theme{}</code> 为每一天设定一个主题，格式为居中。</p>
<p>星期几根据日期值通过 <code>datetime2</code> 宏包自动计算。</p>
<blockquote>
<p>[!CAUTION] <code>\Date</code>
强制换页，实际每页只使用其中一栏（左右对称装饰）。
用户若想在双栏间自然流动内容，可能会发现排版错乱。
因此此模板适用于单篇日记独立成页。</p>
</blockquote>
<hr />
<h2 id="to-do">TO DO</h2>
<ol type="1">
<li><strong>背景性能</strong> 每页都绘制大量 TikZ
图形（线圈、网格等），编译速度较慢。可考虑预定义样式或使用 pgf
缓存。</li>
<li><strong>线圈位置计算</strong> <code>\SpiralPosition</code> 中的
<code>\Valor*(\textheight+2.5cm)/(\SpiralNumber-1)</code>
混合了绝对长度与相对长度，可能导致不同页面（如标题页）线圈位置偏移。可改用
<code>\pgfmathsetlength</code> 动态计算。</li>
<li><strong>多语言兼容</strong>
封面日期以及正文页的日期年月，还有星期几的映射问题，需要查看<code>babel</code>
<code>datetime2</code>等宏包使用说明，考虑做一个语言开关还是直接再写别的语言版本的cls。</li>
</ol>
<hr />
<h2 id="references">References</h2>
<p>该模板基于以下模板修改：</p>
<ul>
<li><p>https://github.com/cs-qyzhang/LaTeX-Diary</p></li>
<li><p>https://github.com/Come-on-ZMZ/LaTeX-Diary</p></li>
</ul>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX项目模板</category>
      </categories>
      <tags>
        <tag>个人项目</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX 个人简历模板</title>
    <url>/2024/12/23/tools/latex/project-templates/latex-resume-template/</url>
    <content><![CDATA[<p>本文给出了一个简单的LaTeX个人简历模板，使用xelatex编译。</p>
<span id="more"></span>
<hr />
<h2 id="引言">引言</h2>
<p>该模板来源于billryan的<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2JpbGxyeWFuL3Jlc3VtZQ==">resume<i class="fa fa-external-link-alt"></i></span>和PM-Hu的<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL1BNLUh1L015X3Jlc3VtZQ==">My_resume<i class="fa fa-external-link-alt"></i></span>，使用xelatex编译。简历整体的布局没有改动，主要是细节处的变化，包括一些对<code>cls</code>文件的DIY和<code>tex</code>文件一些格式的实现等。可以看作是对参考模板的一种不完全的实例化。</p>
<hr />
<h2 id="仓库地址以及实现效果">仓库地址以及实现效果</h2>
<p><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0ludmluYy1aL2xhdGV4LXJlc3VtZS10ZW1wbGF0ZQ==">仓库地址<i class="fa fa-external-link-alt"></i></span></p>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20251126180310755.png"
alt="LaTeX个人简历模板" />
<figcaption aria-hidden="true">LaTeX个人简历模板</figcaption>
</figure>
<hr />
<h2 id="实现细节">实现细节</h2>
<h3 id="姓名与联系方式">姓名与联系方式</h3>
<p>默认使用的是姓名在上方、联系方式在下方的结构。</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% ============================= 联系方式上下结构 ============================</span></span><br><span class="line"><span class="keyword">\name</span>&#123;<span class="keyword">\Huge</span>  <span class="keyword">\kaishu</span> 姓名&#125;</span><br><span class="line"><span class="keyword">\basicInfo</span>&#123;</span><br><span class="line">  <span class="keyword">\email</span>&#123;zhuangzhuangzhang97@gmail.com&#125; <span class="keyword">\textperiodcentered</span><span class="keyword">\ </span></span><br><span class="line">  <span class="keyword">\phone</span>&#123;188-8888-8888&#125; <span class="keyword">\textperiodcentered</span><span class="keyword">\ </span></span><br><span class="line">  <span class="keyword">\github</span>&#123;https://github.com/Invinc-Z&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以注释掉这种方式选择左右结构或左右结构带照片的布局方式。</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% ============================= 联系方式左右结构 ============================</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular*&#125;&#123;<span class="keyword">\textwidth</span>&#125;&#123;@&#123;<span class="keyword">\extracolsep</span>&#123;<span class="keyword">\fill</span>&#125;&#125; l l&#125;</span><br><span class="line"><span class="keyword">\multirow</span>[c]&#123;3&#125;&#123;*&#125;[-0.05in]&#123;<span class="keyword">\Huge</span><span class="keyword">\kaishu</span> 姓名&#125;</span><br><span class="line">    <span class="built_in">&amp;</span> <span class="keyword">\email</span>&#123;zhuangzhuangzhang97@gmail.com&#125; <span class="keyword">\\</span></span><br><span class="line">    <span class="built_in">&amp;</span> <span class="keyword">\phone</span>&#123;188-8888-8888&#125; <span class="keyword">\\</span></span><br><span class="line">    <span class="built_in">&amp;</span> <span class="keyword">\github</span>&#123;https://github.com/Invinc-Z&#125; <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular*&#125;</span><br></pre></td></tr></table></figure>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20251126180624701.png"
alt="姓名与联系方式左右结构" />
<figcaption aria-hidden="true">姓名与联系方式左右结构</figcaption>
</figure>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="comment">% ============================= 联系方式左右结构带照片 ============================</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;tabular*&#125;&#123;<span class="keyword">\textwidth</span>&#125;&#123;l @&#123;<span class="keyword">\hspace</span>&#123;6.8cm&#125;&#125; r|l&#125;</span><br><span class="line"><span class="keyword">\multirow</span>[c]&#123;3&#125;&#123;*&#125;[-0.05in]&#123;<span class="keyword">\Huge</span><span class="keyword">\kaishu</span> 姓名&#125;</span><br><span class="line"><span class="built_in">&amp;</span>  <span class="keyword">\multirow</span>&#123;3&#125;&#123;*&#125;&#123;</span><br><span class="line">    <span class="keyword">\includegraphics</span>[width=1.5cm]&#123;avatar&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">&amp;</span> <span class="keyword">\email</span>&#123;zhuangzhuangzhang97@gmail.com&#125; <span class="keyword">\\</span></span><br><span class="line"><span class="built_in">&amp;</span> <span class="built_in">&amp;</span> <span class="keyword">\phone</span>&#123;188-8888-8888&#125; <span class="keyword">\\</span></span><br><span class="line"><span class="built_in">&amp;</span> <span class="built_in">&amp;</span> <span class="keyword">\github</span>&#123;https://github.com/Invinc-Z&#125; <span class="keyword">\\</span></span><br><span class="line"><span class="keyword">\end</span>&#123;tabular*&#125;</span><br></pre></td></tr></table></figure>
<figure>
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20251126180802481.png"
alt="姓名和联系方式左右结构带照片" />
<figcaption aria-hidden="true">姓名和联系方式左右结构带照片</figcaption>
</figure>
<p>这里，姓名和照片之间的距离通过<code>\hspace{}</code>来动态调整。</p>
<h3 id="字体">字体</h3>
<p>如果对当前内容字体不满意，想要<strong>添加自己的字体</strong>，选中命令<code>\fangzheng</code>或者<code>\kaishu</code>，将其改为<strong>你想要的字体名称</strong>。<strong>你想要的字体</strong>可以来源于：</p>
<ul>
<li><strong>系统字体</strong></li>
<li><strong>自己添加</strong></li>
</ul>
<blockquote>
<p>由于调用了<code>zh_CN-Adobefonts_external</code>宏包，下面的中文字体，可以在全文中<strong>直接使用</strong>：</p>
<ul>
<li>宋体，<code>\songti</code></li>
<li>黑体，<code>\heiti</code></li>
<li>楷书，<code>\kaishu</code></li>
<li>仿宋，<code>\fangsong</code></li>
<li>方正，<code>\fangzheng</code></li>
</ul>
<p>想要自己添加喜欢的字体怎么办？以方正字体为例：</p>
<ol type="1">
<li>将下载的字体文件，<code>fangzheng.ttf</code>，放到文件夹<code>..\fonts\zh_CN-Adobe</code>下</li>
<li>打开<code>zh_CN-adobefonts_external.sty</code></li>
<li>添加命令，写入字体：<code>\setCJKfamilyfont{fangzheng}{fangzheng.ttf}</code></li>
<li>命名字体为<code>fangzheng</code>：<code>\newcommand*{\fangzheng}{\CJKfamily{fangzheng}} % 方正</code></li>
<li>保存，关闭，就可以在<code>*.tex</code>中使用方正字体了</li>
</ol>
</blockquote>
<h3 id="fontawesome6">fontawesome6</h3>
<p>更新图标字体为fontawesome6，可以<code>texdoc fontawesome6</code>查看<code>fontawesome6.pdf</code>，<code>Ctrl+F</code>
搜索图标名称，获得使用命令。</p>
<hr />
<h2 id="参考模板">参考模板</h2>
<ul>
<li><p><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2JpbGxyeWFuL3Jlc3VtZQ==">https://github.com/billryan/resume<i class="fa fa-external-link-alt"></i></span></p></li>
<li><p><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL1BNLUh1L015X3Jlc3VtZQ==">https://github.com/PM-Hu/My_resume<i class="fa fa-external-link-alt"></i></span></p></li>
</ul>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX项目模板</category>
      </categories>
      <tags>
        <tag>个人项目</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX之编译报错及解决方案</title>
    <url>/2025/03/21/tools/latex/environment-configuration/latex-compile-error-solutions/</url>
    <content><![CDATA[<p>本文记录遇到的 LaTeX 编译报错问题和对应的解决方案。</p>
<span id="more"></span>
<hr />
<h2 id="环境变量">环境变量</h2>
<p>Windows首先查看系统环境变量Path是否正常，以下为常用值，最后texlive为你本地的安装路径。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">C:\Windows\system32</span><br><span class="line">C:\Windows</span><br><span class="line">C:\Windows\System32\Wbem</span><br><span class="line">C:\Windows\System32\WindowsPowerShell\v1.0\</span><br><span class="line">C:\Windows\System32\OpenSSH\</span><br><span class="line">C:\Program Files (x86)\NVIDIA Corporation\PhysX\Common</span><br><span class="line">D:\install\texlive\2024\bin\windows</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="ctex常见问题和解决方案">CTEX常见问题和解决方案</h2>
<h3
id="winedt编译提示pdf文件打不开cannot-run-pdf">WinEdt编译提示pdf文件打不开，Cannot
Run pdf</h3>
<p><strong>原因：</strong>
遇到这个问题是因为其他的PDF阅读器强制占用了winedt的pdf打开程序。</p>
<p><strong>解决方法：</strong> 重新添加阅读器SumatraPDF。</p>
<p><strong>步骤：</strong></p>
<ol type="1">
<li><p>在安装CTEX的文件夹搜索SumatraPDF.exe，一般在
<code>D:\Program Files\CTEX\CTeX\ctex\bin</code></p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20240901105058347.png" alt="image-20240901105058347" style="zoom:50%;" /></p></li>
<li><p>复制路径，<code>D:\Program Files\CTEX\CTeX\ctex\bin\SumatraPDF.exe</code>，注意要加上SumatraPDF.exe。</p></li>
<li><p>打开WinEdt，option-&gt;execution modes-&gt;pdf
viewer，修改路径后确认一下</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20240901105515360.png" alt="image-20240901105515360" style="zoom:50%;" /></p></li>
</ol>
<h3
id="miktex更新后无法打开报错this-application-failed-to-start-because-no-qt-platform-plugin-could-be-initialized.-reinstalling-the-application-may-fix-this-problem">miktex更新后无法打开，报错：This
application failed to start because no Qt platform plugin could be
initialized. Reinstalling the application may fix this problem</h3>
<p><strong>解决方法：</strong>原文链接：<span class="exturl" data-url="aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0V0aW5jZWxsZS9hcnRpY2xlL2RldGFpbHMvMTQwMDI1Mjgw">https://blog.csdn.net/Etincelle/article/details/140025280<i class="fa fa-external-link-alt"></i></span></p>
<ol type="1">
<li><p>点击<span class="exturl" data-url="aHR0cHM6Ly9jdGFuLm5ldC9zeXN0ZW1zL3dpbjMyL21pa3RleC90bS9wYWNrYWdlcy8=">链接<i class="fa fa-external-link-alt"></i></span>，下载miktex-qt6-bin-x64.tar.lzma</p>
<p>（该文件Windows下不容易解压，已将其中的x64文件夹转换为windows下容易解压的rar格式）链接：<span class="exturl" data-url="aHR0cHM6Ly9wYW4uYmFpZHUuY29tL3MvMVpBYjBydEJ2RVVGM1BQYU1VWElzVXc/cHdkPWM3b2M=">https://pan.baidu.com/s/1ZAb0rtBvEUF3PPaMUXIsUw?pwd=c7oc<i class="fa fa-external-link-alt"></i></span>
提取码：c7oc</p></li>
<li><p>解压程序包后，进入此文件夹，找到</p></li>
<li><p>找到MikTeX安装文件夹，进入，将步骤2的x64文件夹替换步骤3的x64文件夹</p></li>
</ol>
<h3 id="winedt与sumatrapdf互联配置">Winedt与SumatraPDF互联配置</h3>
<p>参考<span class="exturl" data-url="aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzMwMDgyNzg1L2FydGljbGUvZGV0YWlscy8xMjM3Njc5Mzg=">latex中Winedt和Sumatra
PDF互联设置及失效解决<i class="fa fa-external-link-alt"></i></span>。</p>
<h3 id="latex-error-file-picins.sty-not-found">LaTeX Error: File
`picins.sty’ not found</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20240905124943012.png" alt="image-20240905124943012" style="zoom:50%;" /></p>
<p><strong>原因：</strong> picins.sty宏包缺失。</p>
<p><strong>解决方法：</strong> 添加picins.sty宏包。</p>
<p><strong>步骤：</strong></p>
<ol type="1">
<li><p>从<span class="exturl" data-url="aHR0cHM6Ly9jdGFuLm9yZy8=">https://ctan.org/<i class="fa fa-external-link-alt"></i></span>
下载宏包；
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20240905153702004.png" alt="image-20240905153702004" style="zoom: 25%;" /></p></li>
<li><p>解压到<strong>\</strong>目录下，并打开64miktex-console_admin.exe，点击<strong>任务-刷新文件名数据库</strong>，更新宏包库（相当于激活该宏包）；</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20240905155622980.png" alt="image-20240905155622980" style="zoom:50%;" /></p></li>
</ol>
<h3 id="file-ended-while-scanning-use-of-writefile-或-newlbel">File
ended while scanning use of @writefile 或 @newl@bel</h3>
<p><strong>报错现象：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&gt; Runaway argument? </span><br><span class="line">&gt; ! File ended while scanning use of @writefile. </span><br><span class="line">&gt; </span><br><span class="line">&gt; par </span><br><span class="line">&gt; l.110 begin&#123;document&#125; </span><br></pre></td></tr></table></figure>
<p><strong>解决方法：</strong>
删除编译文件夹内<code>.aux</code>扩展名结尾的文件，重新编译，自动生成正确的<code>.aux</code>文件，完成错误的修复。
（还是有问题可以删除全部中间文件）</p>
<hr />
<h2 id="tex-live常见问题和解决方案">TeX Live常见问题和解决方案</h2>
<h3 id="中文用户名-卡在狮子页面不动">中文用户名 卡在狮子页面不动</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250417145624162.png" alt="image-20250417145624162" style="zoom:50%;" /></p>
<p><strong>解决方法：</strong></p>
<ol type="1">
<li><p>右击电脑- -&gt;属性- -&gt; 高级系统设置- -&gt;在[高级]下点击<a
href="#环境变量">环境变量</a>，</p></li>
<li><p>在<strong>用户变量</strong>和<strong>系统变量</strong>栏里，
找到TEMP与TMP， 分别双击，设置如下：</p>
<p>变量名：TEMP 变量值：%SystemRoot%</p>
<p>变量名：TMP变量值：%SystemRoot%</p></li>
</ol>
<p>注意：若用户和系统变量栏里没有TEMP与TMP，新建即可。</p>
<h3
id="应用程序无法正常启动0xc000007b">应用程序无法正常启动0xc000007b</h3>
<p><strong>原因：</strong>缺少C++文件或者系统dll文件缺失</p>
<p><strong>解决方法：</strong><span class="exturl" data-url="aHR0cDovL3d3dy56amt3ZWlxaS5jbi9uZXdzLzU4MTEyLmh0bWw=">http://www.zjkweiqi.cn/news/58112.html<i class="fa fa-external-link-alt"></i></span></p>
<ol type="1">
<li>命令行输入【sfc
/scannow】（中间有空格），然后点击键盘上的回车（enter键），就会“开始系统扫描”。</li>
<li>电脑通过下载DirectX Repair修复工具来检测并修复。</li>
</ol>
<h3
id="vars-expected-but-powershell-不是内部或外部命令也不是可运行的程序-found">‘vars’
expected but ‘powershell’ 不是内部或外部命令，也不是可运行的程序
found</h3>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250102195322647.png" alt="image-20250102195322647" style="zoom:50%;" /></p>
<p><strong>解决方案：</strong> 添加环境变量Path
<code>C:\Windows\System32\WindowsPowerShell\v1.0\</code></p>
<h3 id="字体问题">字体问题</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">kpathsea: Running mktextfm STFangsong/OT The command name is D:\install\texlive\2024\bin\windows\mktextfm</span><br><span class="line">name = STFangsong/OT, rootname = STFangsong/OT, pointsize = mktexmf: empty or non-existent rootfile!</span><br><span class="line">kpathsea: Running mktexmf STFangsong/OT.mf The command name is D:\install\texlive\2024\bin\windows\mktexmf Cannot find STFangsong/OT.mf.</span><br><span class="line">kpathsea: Appending font creation commands to missfont.log.</span><br></pre></td></tr></table></figure>
<p><strong>解决方案：</strong>
下载安装对应字体，Windows需选择<strong>为所有用户安装</strong>。</p>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX环境配置</category>
      </categories>
      <tags>
        <tag>编译</tag>
        <tag>报错</tag>
      </tags>
  </entry>
  <entry>
    <title>一份简短的LaTeX相关术语的介绍</title>
    <url>/2025/07/14/tools/latex/environment-configuration/latex-intro-terms/</url>
    <content><![CDATA[<p>本文主要介绍<span
class="math inline">\(\LaTeX{}\)</span>的相关术语以及在文件编译过程中发生了什么。</p>
<span id="more"></span>
<hr />
<p>新人在刚接触和使用<span
class="math inline">\(\LaTeX{}\)</span>时可能会有以下一些概念的困扰：</p>
<blockquote>
<ul>
<li>什么是<span class="math inline">\(\TeX\)</span>/<span
class="math inline">\(\LaTeX\)</span>，它们之间有什么关系？</li>
<li>pdfTeX、LuaTeX、XeTeX这些是什么？</li>
<li>pdflatex、lualatex、xelatex这些是什么？</li>
<li>CTEX套装、TeX Live、MacTeX、MiKTeX这些是什么？</li>
<li>tex文件所在目录里面一大堆不同后缀名的文件都是什么东西？</li>
<li>如何通过一堆代码就能生成优雅的pdf文件，底层究竟发生了什么？</li>
</ul>
</blockquote>
<p>了解<span
class="math inline">\(\LaTeX{}\)</span>在编译过程中底层发生了哪些事情对于我们遇到问题时快速定位原因是大有帮助的，这增强了我们使用<span
class="math inline">\(\LaTeX{}\)</span>的底气，使我们能够心定神闲地编写tex文件，遇到问题时心中不慌。同时，这也使我们更深入地理解<span
class="math inline">\(\LaTeX{}\)</span>。</p>
<p>本文主要介绍<span
class="math inline">\(\LaTeX{}\)</span>的相关术语以及在文件编译过程中发生了什么。</p>
<hr />
<h2 id="tex与latex"><span class="math inline">\(\TeX\)</span>与<span
class="math inline">\(\LaTeX{}\)</span></h2>
<h3 id="tex"><span class="math inline">\(\TeX{}\)</span></h3>
<p>TeX 的核心程序（即“TeX 引擎”）最初由高德纳（Donald Knuth）在
1978-1982 年间用<strong>Pascal 语言</strong>编写。TeX
引擎的执行逻辑是通过 Pascal 代码描述，再经 Pascal
编译器转换为汇编语言，最终汇编为机器码运行。其核心功能（如字符处理、排版算法、文件
IO）都对应底层汇编级的内存操作、分支跳转和系统调用。</p>
<p>当你启动一个纯粹的、未经任何初始化的 TeX 引擎时，它只认识大约 300
个原始指令，比如 <code>\def</code>, <code>\hbox</code>,
<code>\vskip</code>,
<code>\advance</code>等。在此时，这些原始指令本身不是宏。它们是引擎的内置功能，是原子操作，无法被展开或分解。可以把它们想象成
CPU 的硬件指令。</p>
<p>纯粹的 TeX 太难用了。因此，每次你运行 tex 或 latex
命令时，引擎做的第一件事不是读你的 .tex
文件，而是先加载一个“格式文件”。</p>
<p>这个格式文件是一个宏定义的集合，它是由 TeX
原始指令预先编写好的、并被引擎预编译成了一种高效加载的二进制形式。</p>
<p>加载格式文件的过程，就像是给一个只有基本指令的计算机安装了一个操作系统和标准库。</p>
<p>高德纳本人还编写了一个简单的 plain TEX 格式，没有定义诸如
<code>\documentclass</code> 和 <code>\section</code> 等等命令。</p>
<h3 id="latex"><span class="math inline">\(\LaTeX{}\)</span></h3>
<p><span class="math inline">\(\LaTeX{}\)</span> 也是一种格式，建立在
Plain TeX 之上的、一个更庞大、更结构化、更易用的宏包集合。它定义了像
<code>\documentclass</code>, <code>\begin&#123;document&#125;</code>,
<code>\section</code> 这样的高级命令。</p>
<p>所有这些 LaTeX 命令，最终都会被一步步展开，转换成 Plain TeX
的宏，然后再展开成 TeX 的原始指令，最后由引擎执行。</p>
<h3 id="texplain-texlatex-的关系">TeX、Plain TeX、LaTeX 的关系</h3>
<p>三者本质是<strong>不同抽象层次的排版工具</strong>，从底层技术看，三者的关系类似“汇编
→ C 标准库 → 高级语言”，宏的展开过程类似编译中的预处理和代码生成，最终由
TeX 引擎（二进制程序）执行底层操作：</p>
<ol type="1">
<li><p><strong>TeX</strong>：最底层的“排版引擎”<br />
它是一个<strong>编程语言解释器</strong>，自带一套极简的排版原语（如字符输出、行距控制、页面分割等）和语法规则（变量、条件判断、循环、宏定义等）。但直接用
TeX 原语写文档非常繁琐（类似用汇编语言写程序）。</p></li>
<li><p><strong>Plain TeX</strong>：TeX 的“标准宏包”<br />
为简化使用，高德纳在 TeX
基础上定义了一套<strong>预定义宏（macro）</strong>，封装了常用功能（如段落格式、标题、列表等），形成了“Plain
TeX 格式”。<br />
它相当于给 TeX 内核加了一层“标准库”，类似 C
语言的标准库（<code>stdio.h</code>
等）对汇编的封装，让用户无需重复编写基础功能。</p></li>
<li><p><strong>LaTeX</strong>：基于 TeX 的“高级文档排版系统”<br />
LaTeX 由 Leslie Lamport 设计，是更高层的“应用框架”，是在 Plain TeX
之上进一步封装的<strong>宏集合</strong>，提供了更高层次的语义（如
<code>\section</code>、<code>\begin&#123;document&#125;</code>
等），专注于“文档结构”而非底层排版细节。<br />
它的定位类似高级编程语言，而 TeX 内核相当于它的“解释器/虚拟机”，Plain
TeX 则是其依赖的底层库之一。</p></li>
</ol>
<hr />
<h2 id="格式">格式</h2>
<p>对于TeX系统，其在编译.tex源文件前，会预加载一个格式文件，其中包含各种提前定义好的宏，以被用户在源文件中调用。</p>
<p><strong>格式文件（.fmt）</strong>
是预编译的宏集合与状态信息的二进制文件，用于加速 TeX
引擎的启动和执行。它们本质是将常用格式（宏）（如 Plain TeX、LaTeX
等的核心定义）预先解析、展开并存储，避免每次运行时重复处理，类似“预编译的标准库”。</p>
<p>常见的格式文件如下：</p>
<h3 id="基础格式文件">基础格式文件</h3>
<ul>
<li><p><strong>plain.fmt</strong> 对应 Plain TeX
格式的格式文件，包含高德纳定义的基础宏集合（如段落、标题、列表等基础排版功能）。</p></li>
<li><p><strong>latex.fmt</strong><br />
对应 <strong>LaTeX</strong> 格式的基础格式文件，是由Leslie
Lamport设计的格式，属于Plain TeX的套娃，实现了很多强大的宏。包含 LaTeX
核心宏（如
<code>\documentclass</code>、<code>\section</code>、文档环境等）。</p></li>
</ul>
<h3 id="扩展格式文件">扩展格式文件</h3>
<ul>
<li><p><strong>pdflatex.fmt</strong><br />
对应 <strong>PDFLaTeX</strong> 格式的格式文件，是 LaTeX
格式的变体，直接生成 PDF 而非 DVI（需配合 pdfTeX 引擎），格式中包含 PDF
相关的宏定义（如图片嵌入、字体映射等）。</p></li>
<li><p><strong>xelatex.fmt</strong><br />
对应 <strong>XeLaTeX</strong> 格式的格式文件，基于 XeTeX 引擎，支持
Unicode 和系统原生字体，格式中包含 Unicode 处理、OpenType
字体支持等宏。</p></li>
<li><p><strong>lualatex.fmt</strong><br />
对应 <strong>LuaLaTeX</strong> 格式的格式文件，基于 LuaTeX 引擎，集成
Lua 脚本功能，格式中包含 Lua 交互、高级字体处理等宏。</p></li>
<li><p><strong>amstex.fmt</strong><br />
对应 <strong>AMS-TeX</strong>
格式的格式文件，专注于数学公式排版，提供更丰富的数学宏（如复杂方程、定理环境等）。</p></li>
</ul>
<hr />
<h2 id="引擎">引擎</h2>
<p><strong>pdfTeX、LuaTeX、XeTeX是由TeX衍生的排版引擎</strong>，是用于编译源代码并生成文档的程序，有时也称为<strong>编译器</strong>。</p>
<p>高纳德将TeX的排版引擎设计得如此开放且易扩展，以至于出现了一些由全球社区在此基础上编写的新排版引擎，它们虽然拓展了若干高级特性，但仍严格兼容TeX引擎本身的严谨性。</p>
<h3 id="pdftex">pdfTeX</h3>
<p>pdfTeX 是 TeX 引擎的一个重要扩展版本。您可以把它理解为 TeX
程序的一个“升级版”，它最革命性的功能是能够直接输出 PDF
文件，而不仅仅是传统的 DVI 文件。</p>
<h3 id="luatex">LuaTeX</h3>
<p>LuaTeX于pdfTeX的基础上开发而来，主要特性是内置Lua脚本引擎，理论上能利用Lua获得更灵活的扩展性，但其流行性及性能均不如XeTeX。</p>
<h3 id="xetex">XeTeX</h3>
<p>由Jonathan
Kew开发，在TeX基础上增加了对unicode的支持，同时增加若干高级字体渲染技术、高级数学排版功能，其预载的为Plain
TeX格式。XeTeX生成的目标文件为.xdv(extend
DVI)，其可由dvipdf或其他工具转换为PDF文件。</p>
<hr />
<h2 id="编译命令">编译命令</h2>
<p><strong>编译命令</strong>
是实际调用的、结合了引擎和格式的命令（可执行程序）。如 <span
class="math inline">\(\texttt{xelatex}\)</span> 命令是结合 XeTeX引擎和
XeLaTeX
格式的一个编译命令（类似于选择编译器（XeTeX引擎）和链接库函数（选择XeLaTeX
格式）的过程）。</p>
<p>常见的引擎、格式和编译命令的关系总结于下表。 其中[xxx]<span
class="math inline">\(\LaTeX{}\)</span> 格式
表示与对应命令相匹配的格式，比如 <span
class="math inline">\(\texttt{latex}\)</span> 命令对应LaTeX 格式，<span
class="math inline">\(\texttt{pdflatex}\)</span>
命令对应PDFLaTeX格式。</p>
<table>
<colgroup>
<col style="width: 18%" />
<col style="width: 22%" />
<col style="width: 27%" />
<col style="width: 31%" />
</colgroup>
<thead>
<tr class="header">
<th></th>
<th>文档格式</th>
<th>plain <span class="math inline">\(\TeX{}\)</span> 格式</th>
<th>[xxx]<span class="math inline">\(\LaTeX{}\)</span> 格式</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>TeX 引擎</td>
<td><span class="math inline">\(\textrm{DVI}\)</span></td>
<td><span class="math inline">\(\texttt{tex}\)</span></td>
<td>N/A</td>
</tr>
<tr class="even">
<td>pdfTeX 引擎</td>
<td><span class="math inline">\(\textrm{DVI}\)</span></td>
<td><span class="math inline">\(\texttt{etex}\)</span></td>
<td><span class="math inline">\(\texttt{latex}\)</span></td>
</tr>
<tr class="odd">
<td></td>
<td><span class="math inline">\(\textrm{PDF}\)</span></td>
<td><span class="math inline">\(\texttt{pdftex}\)</span></td>
<td><span class="math inline">\(\texttt{pdflatex}\)</span></td>
</tr>
<tr class="even">
<td>XeTeX 引擎</td>
<td><span class="math inline">\(\textrm{PDF}\)</span></td>
<td><span class="math inline">\(\texttt{xetex}\)</span></td>
<td><span class="math inline">\(\texttt{xelatex}\)</span></td>
</tr>
<tr class="odd">
<td>LuaTeX 引擎</td>
<td><span class="math inline">\(\textrm{PDF}\)</span></td>
<td><span class="math inline">\(\texttt{luatex}\)</span></td>
<td><span class="math inline">\(\texttt{lualatex}\)</span></td>
</tr>
</tbody>
</table>
<p>在此介绍一下几个编译命令的基本特点：</p>
<table>
<colgroup>
<col style="width: 24%" />
<col style="width: 75%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">编译命令</th>
<th style="text-align: left;">解释</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{latex}\)</span></td>
<td style="text-align: left;">虽然名为 <span
class="math inline">\(\texttt{latex}\)</span> 命令，底层调用的引擎其实是
pdfTeX。 该命令生成 <span
class="math inline">\(\texttt{dvi}\)</span>（Device
Independent）格式的文档, 用 <span
class="math inline">\(\texttt{dvipdfmx}\)</span> 命令可以将其转为 <span
class="math inline">\(\texttt{pdf}\)</span>。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{pdflatex}\)</span></td>
<td style="text-align: left;">底层调用的引擎也是 pdfTeX，可以直接生成
<span class="math inline">\(\texttt{pdf}\)</span> 格式的文档。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{xelatex}\)</span></td>
<td style="text-align: left;">底层调用的引擎是 XeTeX，支持 UTF-8
编码和对 TrueType/OpenType 字体的调用。
当前较为方便的<strong>中文排版</strong>解决方案基于 <span
class="math inline">\(\texttt{xelatex}\)</span>。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{lualatex}\)</span></td>
<td style="text-align: left;">底层调用的引擎是 LuaTeX。这个引擎在pdfTeX
引擎基础上发展而来，除了支持 UTF-8 编码和对 TrueType/OpenType
字体的调用外，还支持通过 Lua 语言扩展 <span
class="math inline">\(\TeX{}\)</span> 的功能。 <span
class="math inline">\(\texttt{lualatex}\)</span>
编译命令下的中文排版支持需要借助 <code>luatexja</code>宏包。</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="latex-发行版">LaTeX 发行版</h2>
<p>LaTeX 发行版（LaTeX Distribution）是一套<strong>预打包的 TeX/LaTeX
系统集合</strong>，包含了编译文档所需的所有核心组件（引擎、宏包、字体、工具等），目的是让用户无需手动零散安装各种组件就能直接使用
LaTeX（类似于Linux的发行版）。</p>
<p>简单说，它类似 “软件套件”—— 就像 “Office 套件” 包含 Word、Excel
等工具，LaTeX 发行版包含了排版所需的 “引擎、宏包、字体、编译工具”
等一整套工具链。</p>
<h3 id="发行版的核心组成">发行版的核心组成</h3>
<p>一个完整的 LaTeX 发行版通常包含：</p>
<ol type="1">
<li><strong>TeX 引擎</strong>：如 pdfTeX、XeTeX、LuaTeX
等，负责解析代码并生成输出文件（PDF 或 DVI）；</li>
<li><strong>基础宏包与文档类</strong>：如 LaTeX
核心宏包（<code>latex.ltx</code>）、标准文档类（<code>article.cls</code>、<code>book.cls</code>）、常用扩展宏包（<code>amsmath</code>、<code>graphicx</code>
等）；</li>
<li><strong>字体文件</strong>：包括 TeX 原生字体（如 Computer Modern
系列）和现代字体（OpenType/TrueType 等，供 XeLaTeX/LuaLaTeX
使用）；</li>
<li><strong>辅助工具</strong>：如文献管理工具（BibTeX、Biber）、索引生成工具（MakeIndex）、格式文件生成工具（iniTeX）等；</li>
<li><strong>配置文件与搜索路径</strong>：定义宏包、字体的存储位置，确保引擎能正确找到所需文件。</li>
</ol>
<p>宏包就是别人通过编写宏集造的轮子，直接拿来用就可以了。类似C的标准库或者第三方库。</p>
<h3 id="主流的-latex-发行版">主流的 LaTeX 发行版</h3>
<p>不同发行版针对不同操作系统优化，核心功能一致，但安装和维护方式略有差异：</p>
<ol type="1">
<li><strong>TeX Live</strong>
<ul>
<li>最主流、跨平台（Windows、macOS、Linux）的发行版，由国际 TeX
用户组（TUG）维护；</li>
<li>每年更新一次，包含几乎所有常用宏包和工具，兼容性极强；</li>
<li>适合多数用户，尤其是需要跨平台一致性的场景（如团队协作）。</li>
</ul></li>
<li><strong>MiKTeX</strong>
<ul>
<li>主要面向 Windows 系统（也支持 macOS/Linux），特点是 “按需安装”——
初始安装体积小，使用时自动下载缺失的宏包；</li>
<li>适合初学者或对磁盘空间敏感的用户，但跨平台兼容性略逊于 TeX
Live。</li>
</ul></li>
<li><strong>MacTeX</strong>
<ul>
<li>基于 TeX Live 的 macOS 专用发行版，预装了针对 macOS 优化的组件（如
PDF 预览工具 Skim、字体管理器等）；</li>
<li>是 macOS 用户的首选，无需手动配置系统适配。</li>
</ul></li>
<li><strong>CTeX 套装</strong>
<ul>
<li>针对中文用户的 Windows 发行版，集成了中文支持宏包（如
CJK）和字体；</li>
<li>逐渐被 TeX Live + 现代中文宏包（如 <code>ctex</code>）替代。</li>
</ul></li>
</ol>
<h3 id="为什么需要发行版">为什么需要发行版？</h3>
<p>LaTeX
系统的组件极其庞大（宏包数千个，字体和工具繁多），手动收集、安装和维护这些组件会非常繁琐，且容易出现版本冲突（如宏包依赖不兼容）。发行版通过预打包和统一管理，解决了这些问题：</p>
<ul>
<li>确保所有组件版本匹配，减少 “编译报错”；</li>
<li>提供统一的更新机制（如 TeX Live 的 <code>tlmgr</code> 工具）；</li>
<li>内置中文、日文等多语言支持（现代发行版中已默认集成）。</li>
</ul>
<p>LaTeX 发行版是 “开箱即用” 的 TeX/LaTeX
工具集合，包含了编译文档所需的引擎、宏包、字体和工具。主流选择是跨平台的
<strong>TeX Live</strong>（适合多数用户）和 macOS 专用的
<strong>MacTeX</strong>，Windows 用户也可考虑
<strong>MiKTeX</strong>。安装发行版后，即可通过
<code>pdflatex</code>、<code>xelatex</code> 等命令编译 LaTeX 文档。</p>
<hr />
<h2 id="编辑器">编辑器</h2>
<p>所谓编辑器就是可以编辑和书写latex源码的程序软件，比如Notepad（记事本）、NotePad3、Vim等。在这些简单的编辑器中写好代码保存后，需要到命令行中输入编译命令进行编译（熟练之后可以编写批处理文件和Makefile文件到命令行编译）。</p>
<blockquote>
<p>“四十岁后，不滞于物，草木竹石均可为剑。自此精修，渐进于无剑胜有剑之境。”——《神雕侠侣》独孤求败</p>
</blockquote>
<p>为了简化书写和编译的复杂度，一些集成开发环境（IDE，Integrated
Development Environment）被开发出用于帮助用户提高效率。 在 LaTeX
领域，常见的 IDE 有 VS Code、TeXstudio、WinEdt、Texworks、TeXShop
等，它们集成了 LaTeX 代码编辑、语法高亮、一键编译、PDF
预览等功能，方便用户编写和排版文档。</p>
<blockquote>
<p>IDE是一种集成了代码编辑、编译、调试、项目管理等多种功能的软件工具，旨在为开发者提供一个统一的工作环境，提高开发效率。</p>
</blockquote>
<p>较为常用的是VS
Code和TeXstudio，这两个都支持跨操作系统，WinEdt主要搭配CTeX套装在Windows环境下使用。</p>
<p>个人建议WinEdt只搭配CTEX使用，原因有三个，其一，CTEX套装默认集成了WinEdt编辑器。其二，WinEdt为商用软件，需要付费，虽然免费版也能使用全部功能。其三，软件闭源，更新缓慢。</p>
<p>VS Code和Texstudio看个人习惯，没使用过VS
Code的推荐使用Texstudio，新手推荐使用Texstudio，原因是它职责单一，只用来编写tex文件，并且个人感觉Debug比VS
Code好用。并且Texstudio是用QT框架编写的开源软件，如果有功能建议可以去其Github<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL3RleHN0dWRpby1vcmcvdGV4c3R1ZGlv">主页<i class="fa fa-external-link-alt"></i></span>提issues。</p>
<h2 id="latex-用到的文件一览"><span
class="math inline">\(\LaTeX{}\)</span> 用到的文件一览</h2>
<p>除了源代码文件 <span class="math inline">\(\texttt{.tex}\)</span>
以外，使用 <span class="math inline">\(\LaTeX{}\)</span>
时还可能接触到各种格式的文件。本节简单介绍一下经常见到的文件。</p>
<p>每个宏包和文档类都是带特定扩展名的文件，除此之外也有一些文件出现于
<span class="math inline">\(\LaTeX{}\)</span> 模板中：</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">文件扩展名</th>
<th style="text-align: left;">作用</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.sty}\)</span></td>
<td style="text-align: left;">宏包文件。宏包的名称与文件名一致。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.cls}\)</span></td>
<td style="text-align: left;">文档类文件。文档类名称与文件名一致。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.bib}\)</span></td>
<td style="text-align: left;">参考文献数据库文件。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.bst}\)</span></td>
<td style="text-align: left;">用到的参考文献格式模板。</td>
</tr>
</tbody>
</table>
<p>在编译过程中可能会生成相当多的辅助文件和日志。一些功能如交叉引用、参考文献、目录、索引等，需要先通过编译生成辅助文件，然后再次编译时读入辅助文件得到正确的结果，所以复杂的源代码可能要编译多次。</p>
<table>
<colgroup>
<col style="width: 20%" />
<col style="width: 80%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">中间文件</th>
<th style="text-align: left;">作用</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.log}\)</span></td>
<td
style="text-align: left;">排版引擎生成的日志文件，供排查错误使用。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.aux}\)</span></td>
<td
style="text-align: left;">生成的主辅助文件，记录交叉引用、目录、参考文献的引用等。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.toc}\)</span></td>
<td style="text-align: left;">生成的目录记录文件。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.lof}\)</span></td>
<td style="text-align: left;">生成的图片目录记录文件。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.lot}\)</span></td>
<td style="text-align: left;">生成的表格目录记录文件。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.bbl}\)</span></td>
<td style="text-align: left;">BibTeX生成的参考文献记录文件。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.blg}\)</span></td>
<td style="text-align: left;">BibTeX生成的日志文件。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.idx}\)</span></td>
<td style="text-align: left;">生成的供 <code>makeindex</code>
处理的索引记录文件。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.ind}\)</span></td>
<td style="text-align: left;"><code>makeindex</code> 处理 <span
class="math inline">\(\texttt{.idx}\)</span>
生成的用于排版的格式化索引文件。</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.ilg}\)</span></td>
<td style="text-align: left;"><code>makeindex</code>
生成的日志文件。</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\texttt{.out}\)</span></td>
<td style="text-align: left;"><code>hyperref</code> 宏包生成的 PDF
书签记录文件。</td>
</tr>
</tbody>
</table>
<hr />
<h2 id="编译过程发生了什么">编译过程发生了什么</h2>
<p>以<code>xelatex</code>编译命令为例（其他编译命令类似），结合一个包含参考文献、图表的最简示例，详细描述编译流程，并说明中间文件的作用。</p>
<h3 id="最简-latex-示例代码">最简 LaTeX 示例代码</h3>
<p>先定义一个包含文档结构、图表、参考文献的示例文件
<code>main.tex</code>：</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\documentclass</span>&#123;article&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;graphicx&#125;  <span class="comment">% 插入图片</span></span><br><span class="line"><span class="keyword">\usepackage</span>&#123;caption&#125;   <span class="comment">% 图表标题</span></span><br><span class="line"><span class="keyword">\usepackage</span>&#123;biblatex&#125;  <span class="comment">% 参考文献管理</span></span><br><span class="line"><span class="keyword">\addbibresource</span>&#123;refs.bib&#125;  <span class="comment">% 关联参考文献库</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line"><span class="keyword">\section</span>&#123;引言&#125;</span><br><span class="line">这是一个示例文档，包含图<span class="keyword">\ref</span>&#123;fig:example&#125;和参考文献<span class="keyword">\cite</span>&#123;knuth1984tex&#125;。</span><br><span class="line"></span><br><span class="line"><span class="keyword">\begin</span>&#123;figure&#125;[h]</span><br><span class="line">  <span class="keyword">\centering</span></span><br><span class="line">  <span class="keyword">\includegraphics</span>[width=0.5<span class="keyword">\textwidth</span>]&#123;example.png&#125;  <span class="comment">% 插入图片</span></span><br><span class="line">  <span class="keyword">\caption</span>&#123;示例图片&#125;</span><br><span class="line">  <span class="keyword">\label</span>&#123;fig:example&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;figure&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">\printbibliography</span>  <span class="comment">% 输出参考文献列表</span></span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure>
<p>配套文件：</p>
<ul>
<li><code>refs.bib</code>（参考文献库，包含一条条目）；</li>
<li><code>example.png</code>（图片文件）。</li>
</ul>
<h3 id="xelatex-编译全流程分阶段解析">XeLaTeX
编译全流程（分阶段解析）</h3>
<p>XeLaTeX 编译本质是 <strong>XeTeX 引擎加载 <code>xelatex.fmt</code>
格式文件，解析 <code>.tex</code>
源文件，经宏展开、排版计算、调用外部工具（如 Biber），最终生成
PDF</strong> 的过程。核心步骤如下：</p>
<h4 id="阶段-1初始化与格式文件加载xelatex-main.tex-启动时">阶段
1：初始化与格式文件加载（<code>xelatex main.tex</code> 启动时）</h4>
<ol type="1">
<li><p><strong>XeTeX 引擎启动</strong></p>
<p>XeLaTeX 是 XeTeX 引擎的 “前端命令”，执行
<code>xelatex main.tex</code> 时，实际启动的是 XeTeX
二进制程序（底层为汇编指令实现的机器码，如内存分配、文件 IO
等系统调用）。</p></li>
<li><p><strong>加载 <code>xelatex.fmt</code> 格式文件</strong></p>
<ul>
<li><code>xelatex.fmt</code> 是预编译的二进制格式文件（类似
“预编译的宏库”），包含 LaTeX 核心宏定义（如
<code>\documentclass</code>、<code>\section</code>）、XeTeX 特有的
Unicode 和字体处理宏（如 <code>fontspec</code> 基础定义）。</li>
<li>加载目的：避免每次编译重新解析 LaTeX 核心宏，加速启动（类似 C
程序加载预编译的标准库 <code>.so</code>/<code>.dll</code>）。</li>
</ul></li>
</ol>
<h4 id="阶段-2解析-.tex-源文件宏展开与结构分析">阶段 2：解析
<code>.tex</code> 源文件，宏展开与结构分析</h4>
<p>XeTeX 引擎逐行读取
<code>main.tex</code>，对指令进行<strong>宏展开</strong>（文本替换）和<strong>语义分析</strong>（识别文档结构、图表、引用等）。</p>
<ol type="1">
<li><strong>预处理与宏展开</strong>
<ul>
<li>遇到 <code>\documentclass&#123;article&#125;</code>：展开为
<code>article.cls</code>
文档类的宏定义（如页面大小、字体默认设置等），本质是一系列底层 TeX
原语（如 <code>\textwidth=345pt</code> 等长度设置）。</li>
<li>遇到 <code>\usepackage&#123;graphicx&#125;</code>：加载
<code>graphicx.sty</code> 宏包，展开为图片处理的宏（如
<code>\includegraphics</code> 对应处理图片路径、缩放的底层指令）。</li>
<li>遇到
<code>\begin&#123;document&#125;</code>：标志文档内容开始，触发页面初始化（如页眉页脚、页边距设置）。</li>
</ul></li>
<li><strong>处理交叉引用与标签</strong>
<ul>
<li>遇到 <code>\label&#123;fig:example&#125;</code>：将标签
<code>fig:example</code> 与当前图号（如 “1”）关联，写入
<strong><code>.aux</code>
辅助文件</strong>（文本格式），供后续编译解析引用（如
<code>\ref&#123;fig:example&#125;</code> 需要读取 <code>.aux</code>
中的图号）。</li>
<li>此时 <code>\ref&#123;fig:example&#125;</code>
暂时无法确定具体数值（因标签定义和引用可能跨页），会先记录为占位符（如
<code>??</code>）。</li>
</ul></li>
<li><strong>处理图片</strong>
<ul>
<li><code>\includegraphics&#123;example.png&#125;</code>：调用 XeTeX
内置的图片处理模块（底层为图像处理库的汇编指令，如解析 PNG
格式、计算像素与 TeX 单位的转换），记录图片在 PDF
中的位置和尺寸，但不直接嵌入（需后续步骤生成时写入）。</li>
</ul></li>
</ol>
<h4 id="阶段-3第一次编译生成中间文件未完成引用和参考文献">阶段
3：第一次编译生成中间文件（未完成引用和参考文献）</h4>
<p>XeTeX 引擎完成源文件解析后，进行排版计算（如行间距、分页），生成
<strong><code>.xdv</code> 中间文件</strong> 和其他辅助文件：</p>
<ol type="1">
<li><strong><code>.xdv</code> 文件</strong>
<ul>
<li>全称 “eXtended Device Independent”，是 XeTeX
特有的中间格式，包含排版后的文本、字体、图形位置信息（但未包含实际图片和完整字体数据），类似
“排版指令清单”。</li>
</ul></li>
<li><strong>其他辅助文件</strong>
<ul>
<li><code>.aux</code>：记录交叉引用（标签与编号的映射，如
<code>\newlabel&#123;fig:example&#125;&#123;&#123;1&#125;&#123;1&#125;&#125;</code> 表示图 1 在第 1
页）、参考文献引用信息（如
<code>\abx@aux@cite&#123;knuth1984tex&#125;</code>）。</li>
<li><code>.log</code>：编译日志，包含宏展开过程、错误信息（如宏未定义、图片缺失）、加载的宏包和字体列表（用于调试）。</li>
<li><code>.out</code>：部分图表位置信息（如浮动体位置计算结果）。</li>
</ul></li>
</ol>
<h4 id="阶段-4调用参考文献工具biber处理引用">阶段
4：调用参考文献工具（Biber）处理引用</h4>
<p>由于 LaTeX 无法直接解析 <code>.bib</code>
文件，需通过外部工具生成可识别的参考文献列表：</p>
<ol type="1">
<li><strong>执行 <code>biber main</code></strong>
<ul>
<li>Biber 读取 <code>.aux</code> 中记录的引用条目（如
<code>knuth1984tex</code>），解析 <code>refs.bib</code> 中的 BibTeX
格式数据（如 <code>@book&#123;knuth1984tex, ...&#125;</code>），生成
<strong><code>.bbl</code> 文件</strong>（LaTeX
可识别的参考文献列表宏代码）。</li>
<li>例如，<code>refs.bib</code> 中的条目会被转换为 <code>\bibitem</code>
或 <code>biblatex</code> 专用的宏定义，包含作者、标题、出版信息等。</li>
</ul></li>
</ol>
<h4 id="阶段-5第二次-xelatex-编译解决引用和参考文献">阶段 5：第二次
XeLaTeX 编译（解决引用和参考文献）</h4>
<p>再次执行 <code>xelatex main.tex</code>，目的是读取第一次编译生成的
<code>.aux</code>（交叉引用）和
<code>.bbl</code>（参考文献），填充占位符：</p>
<ol type="1">
<li><strong>解析 <code>.aux</code> 中的交叉引用</strong>
<ul>
<li><code>\ref&#123;fig:example&#125;</code> 读取 <code>.aux</code> 中的
<code>\newlabel</code> 指令，替换为实际编号 “1”。</li>
</ul></li>
<li><strong>插入参考文献列表</strong>
<ul>
<li><code>\printbibliography</code> 展开为 <code>.bbl</code>
中的宏代码，将参考文献条目排版到文档末尾。</li>
</ul></li>
<li><strong>更新 <code>.aux</code> 和生成最终 <code>.xdv</code></strong>
<ul>
<li>此时交叉引用和参考文献已确定，<code>.aux</code>
会被更新（确保无遗漏），生成包含完整内容的 <code>.xdv</code> 文件。</li>
</ul></li>
</ol>
<h4 id="阶段-6转换-.xdv-为-pdf最终输出">阶段 6：转换 <code>.xdv</code>
为 PDF（最终输出）</h4>
<p>XeTeX 引擎调用内置的 PDF 生成模块，将 <code>.xdv</code> 转换为
PDF：</p>
<ol type="1">
<li><strong>嵌入字体</strong>
<ul>
<li>XeTeX 基于 <code>xelatex.fmt</code>
中的字体配置，调用系统字体库（如计算机中的 <code>Times New Roman</code>
或中文字体），将文档中使用的字体轮廓数据（TrueType/OpenType）嵌入
PDF（避免字体缺失导致乱码）。</li>
</ul></li>
<li><strong>嵌入图片</strong>
<ul>
<li>读取 <code>example.png</code> 的二进制数据，按 <code>.xdv</code>
中记录的位置和尺寸嵌入 PDF，底层通过图像压缩算法（如 PNG
解码）处理像素数据。</li>
</ul></li>
<li><strong>生成 PDF 结构</strong>
<ul>
<li>构建 PDF
的页面树、目录（若有）、交叉引用表（点击引用跳转）等结构，最终生成
<code>main.pdf</code>。</li>
</ul></li>
</ol>
<h3 id="为什么需要多轮编译">为什么需要多轮编译？</h3>
<p>核心原因是 <strong>LaTeX 是 “单遍扫描”
引擎</strong>，无法在一次编译中同时确定 “引用” 和 “被引用对象” 的位置 /
编号：</p>
<ul>
<li>第一次编译：识别 “被引用对象”（如图、文献条目），记录到
<code>.aux</code>，但无法知道它们最终的编号 / 页码。</li>
<li>第二次编译（或调用 BibTeX/Biber 后）：读取 <code>.aux</code> 或
<code>.bbl</code> 中的记录，反向填充 “引用” 的内容。</li>
<li>若内容长度导致页码变化（如参考文献列表增加新页），需额外编译一次同步页码引用。</li>
</ul>
<p>调用参考文献工具处理引用的参考文献工具通常有两种：BibTeX和Biber。</p>
<table>
<colgroup>
<col style="width: 6%" />
<col style="width: 40%" />
<col style="width: 52%" />
</colgroup>
<thead>
<tr class="header">
<th>工具</th>
<th>编译流程（标准轮次）</th>
<th>核心中间文件变化</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>BibTeX</td>
<td>xelatex → xelatex → bibtex → xelatex</td>
<td>.aux（记录引用）→ .bbl（BibTeX 生成）→ 最终 PDF</td>
</tr>
<tr class="even">
<td>Biber</td>
<td>xelatex → biber → xelatex</td>
<td>.aux（记录引用）→ .bbl（Biber 生成）→ 最终 PDF</td>
</tr>
</tbody>
</table>
<p><em>注：Biber 流程通常比 BibTeX 少一轮初始 <code>xelatex</code>，因为
<code>biblatex</code> 对 <code>.aux</code> 的处理更高效。</em></p>
<p><strong>使用 BibTeX 时，标准流程需要 “xelatex 两次 → bibtex → xelatex
一次（或多次）”</strong>，这是因为传统 BibTeX 对 <code>.aux</code>
的依赖更严格，需要两次初始编译确保标签信息完整。而现代 Biber 配合
<code>biblatex</code> 可简化流程，但本质仍是通过多轮编译解决 “引用 -
被引用” 的依赖关系。</p>
<p>实际使用中，无论哪种工具，<strong>最终目标都是确保交叉引用、页码、参考文献列表完全同步</strong>，因此建议在复杂文档中多编译
1-2 次，避免遗漏。</p>
<h3 id="中间文件汇总及作用">中间文件汇总及作用</h3>
<table>
<colgroup>
<col style="width: 11%" />
<col style="width: 19%" />
<col style="width: 68%" />
</colgroup>
<thead>
<tr class="header">
<th>文件名</th>
<th>类型</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>main.aux</code></td>
<td>辅助文件</td>
<td>记录交叉引用（标签与编号）、参考文献引用信息，供多轮编译同步数据</td>
</tr>
<tr class="even">
<td><code>main.log</code></td>
<td>日志文件</td>
<td>记录编译过程（宏加载、错误信息、字体使用），用于调试</td>
</tr>
<tr class="odd">
<td><code>main.xdv</code></td>
<td>中间格式</td>
<td>包含排版后的文本、图形位置信息，是 XeTeX 特有的 “排版指令集”</td>
</tr>
<tr class="even">
<td><code>main.bbl</code></td>
<td>参考文献</td>
<td>BibTeX/Biber 生成的 LaTeX 宏代码，包含格式化后的参考文献条目</td>
</tr>
<tr class="odd">
<td><code>main.blg</code></td>
<td>BibTeX/Biber 日志</td>
<td>记录 BibTeX/Biber 处理参考文献的过程（如条目解析、格式转换）</td>
</tr>
<tr class="even">
<td><code>main.out</code></td>
<td>浮动体信息</td>
<td>记录图表等浮动体的位置计算结果，辅助排版优化</td>
</tr>
</tbody>
</table>
<h3 id="底层技术补充汇编-编译器视角">底层技术补充（汇编 /
编译器视角）</h3>
<ul>
<li><strong>XeTeX 引擎的本质</strong>：是用 C 语言编写的程序（最终编译为
x86-64/ARM 汇编指令），核心逻辑包括：
<ul>
<li>词法分析（识别 <code>\section</code> 等指令为 “宏” token）；</li>
<li>语法分析（解析宏的嵌套结构，如 <code>\begin&#123;figure&#125;</code> 与
<code>\end&#123;figure&#125;</code> 的匹配）；</li>
<li>内存管理（分配缓冲区存储宏展开结果、排版数据）。</li>
</ul></li>
<li><strong>宏展开的底层</strong>：类似 C 预处理器的
<code>#define</code> 替换，但更复杂（支持参数、条件判断），由 XeTeX
引擎中的 “宏处理器” 模块通过字符串操作指令（汇编层面的
<code>mov</code>、<code>cmp</code>）实现。</li>
<li><strong>PDF 生成</strong>：最终调用系统的文件写入指令（如
<code>write</code> 系统调用，对应汇编的
<code>sys_write</code>），将二进制数据（字体、图片、文本）按 PDF
规范组织成 <code>main.pdf</code>。</li>
</ul>
<p>(PS：这样的编译器我们能写出来吗，怎么都是老外写的？)</p>
<hr />
<h2 id="参考文章">参考文章</h2>
<ol type="1">
<li><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">一份 (不太)
简短的 LaTeX2ε 介绍<i class="fa fa-external-link-alt"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly93d3cuY25ibG9ncy5jb20vZmlqaWlzbGFuZC9wLzE1Njk3NDcyLmh0bWw=">杂谈：
Tex 排版系统历史及各引擎版本梳理<i class="fa fa-external-link-alt"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly96aHVhbmxhbi56aGlodS5jb20vcC8xODE1NTcyNTM=">LaTeX引擎、格式、宏包、发行版大梳理<i class="fa fa-external-link-alt"></i></span></li>
</ol>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX环境配置</category>
      </categories>
      <tags>
        <tag>编译</tag>
        <tag>术语</tag>
        <tag>发行版</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX之宏包更新与安装</title>
    <url>/2025/03/15/tools/latex/environment-configuration/latex-macro-package-update-installation/</url>
    <content><![CDATA[<p>本文总结了如何更新和安装<span
class="math inline">\(\LaTeX{}\)</span>的宏包。</p>
<span id="more"></span>
<hr />
<h2 id="更新和安装宏包">更新和安装宏包</h2>
<p><span class="math inline">\(\textrm{TeX Live}\)</span> 和 <span
class="math inline">\(\textrm{MiKTeX}\)</span>
分别提供了图形界面的宏包管理器 <span class="math inline">\(\textrm{TEX
Live Manager}\)</span> 和 <span class="math inline">\(\textrm{MiKTeX
Package
Manager}\)</span>，用于安装、管理和更新宏包。一般情况下，直接在图形界面的工具下按提示操作即可（<span
class="math inline">\(\textrm{MiKTeX Package Manager}\)</span>
有普通权限和管理员权限的版本，建议<strong>总是使用管理员权限</strong>的版本）。</p>
<p>两者也可以通过各自的命令行工具安装和更新宏包：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">% TeX Live 命令行工具 tlmgr 的使用示例</span><br><span class="line">% 安装/卸载宏包</span><br><span class="line">tlmgr install &lt;package-name&gt;</span><br><span class="line">tlmgr remove &lt;package-name&gt;</span><br><span class="line">% 更新所有宏包（包括 tlmgr 本身）</span><br><span class="line">tlmgr update --all --self</span><br><span class="line">% 列出所有可更新的宏包</span><br><span class="line">tlmgr update --list</span><br><span class="line">% 指定更新源地址</span><br><span class="line">% &lt;CTAN mirrors&gt; 形如 https://mirrors.tuna.tsinghua.edu.cn/CTAN</span><br><span class="line">tlmgr repository <span class="built_in">set</span> &lt;CTAN mirrors&gt;/systems/texlive/tlnet</span><br><span class="line">% 查看宏包信息，加 --list 参数可列出宏包的所有文件</span><br><span class="line">tlmgr info &lt;package-name&gt;</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">% MiKTeX 命令行工具 mpm 的使用示例</span><br><span class="line">% 建议始终加 --admin 参数使用</span><br><span class="line">% 安装/卸载宏包</span><br><span class="line">mpm --admin --install &lt;package-name&gt;</span><br><span class="line">mpm --admin --uninstall &lt;package-name&gt;</span><br><span class="line">% 更新所有宏包</span><br><span class="line">mpm --admin --update</span><br><span class="line">% 列出所有可更新的宏包</span><br><span class="line">mpm --admin --find-updates</span><br><span class="line">% 指定更新源地址</span><br><span class="line">mpm --admin --set-repository=&lt;CTAN mirrors&gt;/systems/win32/miktex/tm/packages</span><br><span class="line">% 查看宏包信息</span><br><span class="line">mpm --admin --print-package-info &lt;package-name&gt;</span><br></pre></td></tr></table></figure>
<p><span class="math inline">\(\textrm{TeX Live}\)</span>
默认安装所有宏包， <span class="math inline">\(\textrm{MiKTeX}\)</span>
的安装程序只包含了 <span class="math inline">\(\LaTeX{}\)</span>
的一些基本宏包。从 <span class="math inline">\(\textrm{TeX
Live}\)</span> 的光盘镜像和 <span
class="math inline">\(\textrm{MiKTeX}\)</span> 的安装包体积可见一斑。
默认情况下，编译过程中如果遇到宏包未安装而报错的情况下，<span
class="math inline">\(\textrm{MiKTeX}\)</span>
会弹出一个对话框，让用户可以选择临时安装宏包，安装成功后继续编译。</p>
<hr />
<h2 id="手动安装宏包">手动安装宏包</h2>
<p><span
class="math inline">\(\textcolor{red}{如非万不得已，尽量不要手动安装宏包}\)</span>。绝大多数宏包都已打包到
<span class="math inline">\(\textrm{TeX Live}\)</span> 和 <span
class="math inline">\(\textrm{MiKTeX}\)</span>
两大发行版的安装源，可用宏包管理器安装。如果用户知道某个宏包的名称，但不确定是否在发行版中已打包，可在<span class="exturl" data-url="aHR0cHM6Ly93d3cuY3Rhbi5vcmcv">CTAN<i class="fa fa-external-link-alt"></i></span>中搜索，。</p>
<p>在手动安装之前，有必要了解一下 <span
class="math inline">\(\TeX{}\)</span> 目录结构（<span
class="math inline">\(\TeX{}\)</span> Directory Structure, TDS）。它是
<span class="math inline">\(\TeX{}\)</span>
发行版中宏包、字体、帮助文档等文件的组织结构。TDS 有时也称为 <span
class="math inline">\(\textrm{TEXMF}\)</span> 树，取 <span
class="math inline">\(\TeX+\textrm{METAFONT}\)</span> 之意。</p>
<p>以 <span class="math inline">\(\textrm{TeX Live}\)</span>
为例，假设系统的 <span class="math inline">\(\textrm{TEXMF}\)</span>
树根目录为 <span
class="math inline">\(\textrm{C:\\texlive\\2020\\texmf-dist}\)</span>，其下有很多子目录，仅举几例：</p>
<table>
<colgroup>
<col style="width: 29%" />
<col style="width: 70%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;"><span
class="math inline">\(\textrm{tex/latex}\)</span></th>
<th style="text-align: left;"><span
class="math inline">\(\LaTeX{}\)</span> 宏包</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\textrm{doc/latex}\)</span></td>
<td style="text-align: left;"><span
class="math inline">\(\LaTeX{}\)</span> 宏包的帮助文档</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\textrm{source/latex}\)</span></td>
<td style="text-align: left;"><span
class="math inline">\(\LaTeX{}\)</span> 宏包的源代码</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\textrm{bibtex}\)</span></td>
<td style="text-align: left;"><span
class="math inline">\(\textrm{BibTeX}\)</span>
工具相关文件，许多宏包配套的 <span
class="math inline">\(\textrm{BibTeX}\)</span> 格式文件位于子目录 <span
class="math inline">\(\textrm{bst}\)</span> 中</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\textrm{fonts/tfm}\)</span></td>
<td style="text-align: left;">$ $ 使用的字体文件，<span
class="math inline">\(\textrm{TFM}\)</span> 格式</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span
class="math inline">\(\textrm{fonts/type1}\)</span></td>
<td style="text-align: left;"><span
class="math inline">\(\textrm{PostScript}\)</span> 字体文件（<span
class="math inline">\(\textrm{Type1}\)</span>），<span
class="math inline">\(\textrm{PFB}\)</span> 格式</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span
class="math inline">\(\textrm{fonts/opentype}\)</span></td>
<td style="text-align: left;"><span
class="math inline">\(\textrm{OpenType}\)</span> 格式的字体文件</td>
</tr>
</tbody>
</table>
<p>需要手动安装的宏包，一般已经按照上述目录结构打包完成。手动安装时，尽量不要拷贝到系统的
<span class="math inline">\(\textrm{TEXMF}\)</span>
树，而是拷贝到发行版提供的用户 <span
class="math inline">\(\textrm{TEXMF}\)</span> 树，如 <span
class="math inline">\(\textrm{TeX Live}\)</span> 的 <span
class="math inline">\(\textrm{C:\\texlive\\texmf-local}\)</span>。安装完成后，还需<strong>刷新
<span class="math inline">\(\TeX{}\)</span>
发行版的文件名数据库</strong>，令新安装的宏包文件能够被系统找到。<span
class="math inline">\(\textrm{TeX Live}\)</span> 用户须在 Windows
命令行或者 Linux 终端执行命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mktexlsr</span><br></pre></td></tr></table></figure>
<p><span class="math inline">\(\textrm{MiKTeX}\)</span>
用户的命令为：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">initexmf --update-fndb</span><br></pre></td></tr></table></figure>
<p>下载下来的宏包，有的直接包含<span
class="math inline">\(\textrm{.sty}\)</span> 文件（<span
class="math inline">\(\textrm{.sty}\)</span>文件就是package的最终形式）；有的下载下来只包含了<span
class="math inline">\(\textrm{.ins}\)</span>,<span
class="math inline">\(\textrm{.dtx}\)</span>，以及一个 <span
class="math inline">\(\textrm{readme}\)</span>
文件，这时需要自己编译生成 <span
class="math inline">\(\textrm{.sty}\)</span> 文件，以及生成相应的 <span
class="math inline">\(\textrm{manual}\)</span> 。下面详细介绍生成 <span
class="math inline">\(\textrm{.sty}\)</span>
文件以及帮助文档的步骤。</p>
<blockquote>
<ul>
<li><p>第一步：下载宏包，解压，<span class="exturl" data-url="aHR0cHM6Ly93d3cuY3Rhbi5vcmcvcGtn">下载地址<i class="fa fa-external-link-alt"></i></span>；</p></li>
<li><p>第二步：找到你的解压后安装包地址，打开电脑的命令提示符界面，切换路径到这个package所在文件夹；</p></li>
<li><p>第三步：生成<code>*.sty</code>，在命令提示符界面输入
<code>latex *.ins</code>；生成帮助文档manual,
在命令提示符界面输入<code>latex *.dtx</code>，编译完成以后，会生成一个叫做<code>*.dvi</code>的文件，这个文件也可以用ctex自带的yap程序打开，不过不是很方便，所以最好还是把它转为pdf格式，分为两步，先输入
<code>dvips *.dvi</code>，生成
<code>*.ps</code>文件，再输入<code>ps2pdf *.ps</code>，最终就生成了<code>*.pdf</code>的帮助文档，可以打开查看这个package的基本用法，命令的格式等。</p></li>
<li><p>第四步：生成
<code>*.sty</code>文件以后还需要把这个文件放到LaTeX能够识别的路径下，在CTEX下，可以是
<code>~\CTEX\MiKTeX\tex\latex</code>，<span
class="math inline">\(\textrm{TeX Live}\)</span> 下，可以选择
<code>~\texlive\texmf-local\tex\latex\local</code>,
你可以在这个路径下建立一个文件夹，文件夹的名字即是宏包的名字，把<code>*.sty</code>放到这里，接着找到MikTeX的Console，以管理员方式打开，点击“任务”——“刷新文件名数据库”。</p></li>
</ul>
</blockquote>
<p>下面的表格中给出了文件存放的规则：</p>
<table>
<colgroup>
<col style="width: 6%" />
<col style="width: 44%" />
<col style="width: 49%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: center;">Type</th>
<th style="text-align: center;">Directory (under <code>texmf/</code> or
<code>texmf-local/</code>)</th>
<th style="text-align: center;">Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: center;">.afm</td>
<td
style="text-align: center;">fonts/afm/<em>foundry</em>/<em>typeface</em></td>
<td style="text-align: center;">Adobe Font Metrics for Type 1 fonts</td>
</tr>
<tr class="even">
<td style="text-align: center;">.bst</td>
<td style="text-align: center;">bibtex/bst/<em>packagename</em></td>
<td style="text-align: center;">BibTeX style</td>
</tr>
<tr class="odd">
<td style="text-align: center;">.cls</td>
<td style="text-align: center;">tex/latex/base</td>
<td style="text-align: center;">Document class file</td>
</tr>
<tr class="even">
<td style="text-align: center;">.dvi</td>
<td style="text-align: center;">doc</td>
<td style="text-align: center;">package documentation</td>
</tr>
<tr class="odd">
<td style="text-align: center;">.enc</td>
<td style="text-align: center;">fonts/enc</td>
<td style="text-align: center;">Font encoding</td>
</tr>
<tr class="even">
<td style="text-align: center;">.fd</td>
<td style="text-align: center;">tex/latex/mfnfss</td>
<td style="text-align: center;">Font Definition files for METAFONT
fonts</td>
</tr>
<tr class="odd">
<td style="text-align: center;">.fd</td>
<td style="text-align: center;">tex/latex/psnfss</td>
<td style="text-align: center;">Font Definition files for PostScript
Type 1 fonts</td>
</tr>
<tr class="even">
<td style="text-align: center;">.map</td>
<td style="text-align: center;">fonts/map/</td>
<td style="text-align: center;">Font mapping files</td>
</tr>
<tr class="odd">
<td style="text-align: center;">.mf</td>
<td
style="text-align: center;">fonts/source/public/<em>typeface</em></td>
<td style="text-align: center;">METAFONT outline</td>
</tr>
<tr class="even">
<td style="text-align: center;">.pdf</td>
<td style="text-align: center;">doc</td>
<td style="text-align: center;">package documentation</td>
</tr>
<tr class="odd">
<td style="text-align: center;">.pfb</td>
<td
style="text-align: center;">fonts/type1/<em>foundry</em>/<em>typeface</em></td>
<td style="text-align: center;">PostScript Type 1 outline</td>
</tr>
<tr class="even">
<td style="text-align: center;">.sty</td>
<td style="text-align: center;">tex/latex/<em>packagename</em></td>
<td style="text-align: center;">Style file: the normal package
content</td>
</tr>
<tr class="odd">
<td style="text-align: center;">.tex</td>
<td style="text-align: center;">doc</td>
<td style="text-align: center;">TeX source for package
documentation</td>
</tr>
<tr class="even">
<td style="text-align: center;">.tex</td>
<td style="text-align: center;">tex/plain/<em>packagename</em></td>
<td style="text-align: center;">Plain TeX macro files</td>
</tr>
<tr class="odd">
<td style="text-align: center;">.tfm</td>
<td
style="text-align: center;">fonts/tfm/<em>foundry</em>/<em>typeface</em></td>
<td style="text-align: center;">TeX Font Metrics for METAFONT and Type 1
fonts</td>
</tr>
<tr class="even">
<td style="text-align: center;">.ttf</td>
<td
style="text-align: center;">fonts/truetype/<em>foundry</em>/<em>typeface</em></td>
<td style="text-align: center;">TrueType font</td>
</tr>
<tr class="odd">
<td style="text-align: center;">.vf</td>
<td
style="text-align: center;">fonts/vf/<em>foundry</em>/<em>typeface</em></td>
<td style="text-align: center;">TeX virtual fonts</td>
</tr>
<tr class="even">
<td style="text-align: center;">others</td>
<td style="text-align: center;">tex/latex/<em>packagename</em></td>
<td style="text-align: center;">other types of file unless instructed
otherwise</td>
</tr>
</tbody>
</table>
<p>如果一个宏包还顺带安装了一些字体，还应该更新字体的映射。详细方法参详<span class="exturl" data-url="aHR0cDovL3d3dy50dWcub3JnL2ZvbnRzL2ZvbnRpbnN0YWxsLmh0bWw=">这里<i class="fa fa-external-link-alt"></i></span>。</p>
<hr />
<h2 id="参考博文">参考博文</h2>
<p>[1] <span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0NUZVgtb3JnL2xzaG9ydC16aC1jbg==">Partl H, Hyna
I, Schlegl E. 一份 (不太) 简短的 LATEX2ε 介绍[J]. 2024.<i class="fa fa-external-link-alt"></i></span></p>
<p>[2] <span class="exturl" data-url="aHR0cHM6Ly93d3cuY25ibG9ncy5jb20vY3N1Y2F0L3AvNTE0MjQ1OS5odG1s">LaTeX手动安装宏包（package）以及生成帮助文档的整套流程<i class="fa fa-external-link-alt"></i></span></p>
<p>[3] <span class="exturl" data-url="aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L21lbXJheS9hcnRpY2xlL2RldGFpbHMvNDUyNDgyNTk=">Latex之安装宏包<i class="fa fa-external-link-alt"></i></span></p>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX环境配置</category>
      </categories>
      <tags>
        <tag>宏包</tag>
        <tag>安装</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX发行版安装配置教程</title>
    <url>/2025/03/27/tools/latex/environment-configuration/latex-distribution-install-configure-tutorial/</url>
    <content><![CDATA[<p>本文介绍在不同操作系统下如何安装和配置<span
class="math inline">\(\LaTeX{}\)</span>的发行版（包括TeX
Live、MiKTeX、MacTeX、以及CTEX套装），并且介绍了一些常用的编辑器的安装和配置。</p>
<span id="more"></span>
<hr />
<h2 id="前言">前言</h2>
<p><span class="math inline">\(\TeX\)</span>/<span
class="math inline">\(\LaTeX\)</span> 并不是单独的程序，现在的<span
class="math inline">\(\TeX\)</span>系统都是复杂的软件包，里面包含各种排版的引擎、编译脚本、格式转换工具、管理界面、配置文件、支持工具、字体及数以千计的宏包和文档。一个TeX发行版就是把所有这样的部件整合起来，打包发布的软件。</p>
<p>什么是<span class="math inline">\(\TeX\)</span>/<span
class="math inline">\(\LaTeX\)</span>，TeX
Live、MiKTeX、MacTeX以及CTEX套装这些是什么，<code>pdflatex</code>、<code>xelatex</code>、<code>lualatex</code>这些又是什么，如何通过一堆代码就能生成优雅的pdf文件，底层究竟发生了什么？</p>
<p>关于这些问题可参考<span class="exturl" data-url="aHR0cHM6Ly93d3cuY25ibG9ncy5jb20vSW52aW5jLVovcC8xOTE3NjYxNy9sYXRleC1pbnRyby10ZXJtcw==">一份简短的LaTeX相关术语的介绍<i class="fa fa-external-link-alt"></i></span>。</p>
<p>关于<span
class="math inline">\(\LaTeX{}\)</span>环境的安装配置也可参考<span class="exturl" data-url="aHR0cHM6Ly90dWcuY3Rhbi5vcmcvaW5mby9pbnN0YWxsLWxhdGV4LWd1aWRlLXpoLWNuL2luc3RhbGwtbGF0ZXgtZ3VpZGUtemgtY24ucGRm">一份简短的关于LATEX安装的介绍<i class="fa fa-external-link-alt"></i></span>，关于TeX
Live的介绍和安装参考<span class="exturl" data-url="aHR0cHM6Ly90dWcub3JnL3RleGxpdmUvZG9jL3RleGxpdmUtemgtY24vdGV4bGl2ZS16aC1jbi5wZGY=">Tex
live指南<i class="fa fa-external-link-alt"></i></span>，<span
class="math inline">\(\LaTeX{}\)</span>的基础知识和入门使用可参考<span class="exturl" data-url="aHR0cHM6Ly9taXJyb3ItaGsua29kZG9zLm5ldC9DVEFOL2luZm8vbHNob3J0L2NoaW5lc2UvbHNob3J0LXpoLWNuLnBkZg==">一份（不太）简短的LATEX2ε
介绍<i class="fa fa-external-link-alt"></i></span>。</p>
<p>若本地Tex
Live环境已经安装配置好，可在本地打开一些使用手册，具体命令如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">texdoc install-latex-guide-zh-cn    <span class="comment"># 一份简短的关于LATEX安装的介绍</span></span><br><span class="line">texdoc texlive-zh-cn                <span class="comment"># Tex Live 指南</span></span><br><span class="line">texdoc lshort-zh-cn                 <span class="comment"># 一份（不太）简短的LATEX2ε 介绍</span></span><br></pre></td></tr></table></figure>
<p>其中<code>texdoc</code>是 TeX Live
发行版中自带的一个命令行工具，用于快速查找和打开 TeX
相关的文档（包括宏包、程序或格式的手册、说明等）。类似于Linux系统的man手册。</p>
<p>本文主要介绍如何安装和配置latex的编译环境。TeX
Live可配合TeXstudio或VS
Code编辑器使用，CTEX套装配合WinEdt编辑器使用。这些配套方案是流行的、容易设置的、经过验证行之有效的搭配选择。</p>
<h2 id="windows系统">Windows系统</h2>
<h3 id="tex-live">Tex Live</h3>
<h4 id="图形界面安装步骤">图形界面安装步骤</h4>
<ol type="1">
<li>下载<span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL0NUQU4vc3lzdGVtcy90ZXhsaXZlL0ltYWdlcy8=">镜像文件<i class="fa fa-external-link-alt"></i></span>;
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20241108203305777.png" alt="image-20241108203305777" style="zoom: 33%;" /></li>
<li>在英文路径下解压<code>texlive.iso</code>文件（或右键装载，完成安装后再弹出），右键以管理员方式运行<code>install-tl-windows.bat</code>；
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20241108204658453.png" alt="image-20241108204658453" style="zoom: 33%;" /></li>
<li>选择安装路径（使用英文路径（路径名从数字字母下划线中选择）），点击安装；
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20241108204923261.png" alt="image-20241108204923261" style="zoom:50%;" /></li>
<li>漫长等待（Windows系统安装较慢，四十分钟左右，较老机型时间会更长）直到安装完成；
<img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20241112162248529.png" alt="image-20241112162248529" style="zoom:50%;" /></li>
</ol>
<p>texlive2025安装完成界面如下：</p>
<p>（可能是去掉了欢迎界面，查看安装目录下<code>texlive\2025\install-tl.log</code>最后出现
<strong>欢迎进入 TeX Live
的世界！</strong>的日志，基本上可以放心点击关闭）</p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20250417144717830.png" alt="image-20250417144717830" style="zoom:50%;" /></p>
<p><kbd>Win</kbd>+<kbd>R</kbd>输入<code>cmd</code>，输入<code>latex -v</code>或<code>xelatex -v</code>或<code>pdflatex -v</code>进行验证。</p>
<h4 id="命令行安装步骤">命令行安装步骤</h4>
<blockquote>
<ol type="1">
<li>下载镜像文件<code>.iso</code>，并装载；</li>
<li>以管理员方式打开<code>cmd</code>，切换到装载磁盘；</li>
<li>输入 <code>install-tl-windows.bat --no-gui</code>；</li>
<li>输入<kbd>D</kbd>设置路径，再输入数字<kbd>1</kbd>改变安装路径，
之后输入 <code>d:/install/texlive/2025</code>；</li>
<li>输入<kbd>R</kbd>返回主菜单，之后输入<kbd>I</kbd>开始安装。</li>
</ol>
</blockquote>
<h4 id="历史版本">历史版本</h4>
<p>有些模板使用特定年份版本的TeX Live，可从下面网址获取。</p>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly9waS5rd2FyYy5pbmZvL2hpc3RvcmljL3N5c3RlbXMvdGV4bGl2ZS8=">texlive历史版本<i class="fa fa-external-link-alt"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL3RleC1oaXN0b3JpYy1hcmNoaXZlL3N5c3RlbXMvdGV4bGl2ZS8=">清华镜像texlive历史版本<i class="fa fa-external-link-alt"></i></span></li>
</ul>
<h3 id="texstudio">Texstudio</h3>
<p><strong>安装包下载：</strong></p>
<ul>
<li><p><span class="exturl" data-url="aHR0cHM6Ly90ZXhzdHVkaW8uc291cmNlZm9yZ2UubmV0Lw==">官网主页<i class="fa fa-external-link-alt"></i></span></p></li>
<li><p><span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL2dpdGh1Yi1yZWxlYXNlL3RleHN0dWRpby1vcmcvdGV4c3R1ZGlvL0xhdGVzdFJlbGVhc2Uv">国内镜像<i class="fa fa-external-link-alt"></i></span></p></li>
<li><p><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL3RleHN0dWRpby1vcmcvdGV4c3R1ZGlvL3JlbGVhc2VzP3BhZ2U9MQ==">历史版本<i class="fa fa-external-link-alt"></i></span></p></li>
</ul>
<p><strong>安装步骤</strong>：傻瓜式安装，中间可以切换安装路径，不带中文路径，C盘容量较小可以切换其他盘符。</p>
<h3 id="vs-code">VS code</h3>
<h4 id="latex环境配置">LaTeX环境配置</h4>
<ol type="1">
<li><p>打开VS
Code，点击左侧扩展图标，搜索<code>LaTeX Workshop</code>，点击下载安装。</p></li>
<li><p>配置环境，在VS
Code界面快捷键<code>Ctrl Shift + P</code>，输入<code>Open User Settings</code>，点击<strong>首选项：打开用户设置(JSON)</strong>,
打开<code>settings.json</code>文件。在文件中的<code>{}</code>中添加以下代码：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 编译工具和命令</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.tools&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk-pdflatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;-pdflatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-synctex=1&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-interaction=nonstopmode&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-file-line-error&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;%DOC%&quot;</span></span><br><span class="line">        <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk-xelatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;-xelatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-synctex=1&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-interaction=nonstopmode&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-file-line-error&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;%DOC%&quot;</span></span><br><span class="line">        <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk-lualatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;-lualatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-synctex=1&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-interaction=nonstopmode&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-file-line-error&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;%DOC%&quot;</span></span><br><span class="line">        <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line"><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">// 用于配置编译链</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.recipes&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk(pdflatex)&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;tools&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;latexmk-pdflatex&quot;</span></span><br><span class="line">        <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk(xelatex)&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;tools&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;latexmk-xelatex&quot;</span></span><br><span class="line">        <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk(lualatex)&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;tools&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;latexmk-lualatex&quot;</span></span><br><span class="line">        <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//tex文件浏览器，可选项为&quot;none&quot; &quot;browser&quot; &quot;tab&quot; &quot;external&quot;</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.view.pdf.viewer&quot;</span><span class="punctuation">:</span> <span class="string">&quot;tab&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//设置是否自动编译tex文件，设置为保存时自动编译</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.autoBuild.run&quot;</span><span class="punctuation">:</span> <span class="string">&quot;onSave&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//显示右键菜单的latex相关选项</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.showContextMenu&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//显示错误</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.message.error.show&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//显示警告</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.message.warning.show&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//从使用的包中自动补全命令和环境</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.intellisense.package.enabled&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//设置为never，为不自动清除辅助文件</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.autoClean.run&quot;</span><span class="punctuation">:</span> <span class="string">&quot;never&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//设置清除辅助文件的方法为command，即使用latexmk的-clean选项</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.clean.method&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//设置清除辅助文件时，是否清除子文件夹中的辅助文件</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.clean.subfolder.enabled&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//设置vscode编译tex文档时的默认编译链</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.recipe.default&quot;</span><span class="punctuation">:</span> <span class="string">&quot;lastUsed&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">// 用于反向同步的内部查看器的键绑定。ctrl/cmd +点击(默认)或双击</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.view.pdf.internal.synctex.keybinding&quot;</span><span class="punctuation">:</span> <span class="string">&quot;double-click&quot;</span><span class="punctuation">,</span></span><br></pre></td></tr></table></figure>
<p>英文一般选择<code>latexmk(pdflatex)</code>编译，中文一般选择<code>latexmk(xelatex)</code>编译，初始时使用第一个配方，后续默认使用上次使用的编译方式，保存文件触发自动编译。</p>
<p>清理辅助文件会清除中间生成的辅助文件，不会清除<code>*.pdf</code>、<code>*.tex</code>、<code>*.bib</code>等有用的资源文件。</p>
<p>重启VS Code配置生效。</p></li>
</ol>
<h4 id="环境配置测试文档">环境配置测试文档</h4>
<p>新建一个文档，复制下面代码，保存到一个工作目录（空文件夹）中（方便统一管理生成的中间文件），编译运行查看是否可以生成PDF文件。</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="meta">% !TEX program = xelatex</span></span><br><span class="line"><span class="keyword">\documentclass</span>&#123;article&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;ctex&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">\title</span>&#123;<span class="keyword">\LaTeX</span>&#123;&#125;测试文档&#125;</span><br><span class="line"><span class="keyword">\author</span>&#123;Invinc-Z&#125;</span><br><span class="line"><span class="keyword">\date</span>&#123;<span class="keyword">\today</span>&#125;  <span class="comment">% 自动显示当前日期</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line"><span class="keyword">\maketitle</span>  <span class="comment">% 生成标题</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">\section</span>&#123;引言&#125;</span><br><span class="line">这是一个测试<span class="keyword">\LaTeX</span>&#123;&#125;环境是否正确配置的文档，使用XeLaTeX编译。</span><br><span class="line"></span><br><span class="line"><span class="keyword">\subsection</span>&#123;二级标题&#125;</span><br><span class="line">这是二级标题下的内容。</span><br><span class="line"></span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure>
<h3 id="miktex">MiKTeX</h3>
<p>✒️</p>
<p>✒️</p>
<p>✒️</p>
<h3 id="ctex">CTEX</h3>
<p>官网网站：<span class="exturl" data-url="aHR0cHM6Ly9jdGV4Lm9yZy8=">https://ctex.org/<i class="fa fa-external-link-alt"></i></span></p>
<p>CTEX的使用场景主要是一些比较旧的中文期刊和毕业论文等模板，可查看需要排版的模板的说明或者更新时间，如果时间比较久远，优先安装旧版本2.9.2.167（不要安装2.9.2.164，
系统环境变量Path过长可能被覆盖，就是别的变量值都没了）。<span class="exturl" data-url="aHR0cHM6Ly9jdGV4Lm9yZy9jdGV4L3JlbGVhc2Utbm90ZXMv">CTEX更新记录<i class="fa fa-external-link-alt"></i></span>里说明了这个Bug:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CTEX更新记录</span><br><span class="line">v2.9.2.167 — 2022.10.10</span><br><span class="line">升级安装程序为 NSIS 3.08</span><br><span class="line">修正安装时 Path 过长可能被覆盖的错误</span><br><span class="line"> </span><br><span class="line">v2.9.2.164 — 2012.03.22</span><br><span class="line">修正 WinEdt 中 CCT 菜单设置错误</span><br><span class="line">修改 WinEdt 不使用 UTF-8 作为缺省编码</span><br><span class="line">升级 WinEdt 7.0 20120321</span><br><span class="line">升级 GSview 5.0</span><br><span class="line">升级 Ghostscript 9.05</span><br><span class="line">更新 SumatraPDF 2.0 r5987</span><br><span class="line">更新 MiKTeX 2.9</span><br></pre></td></tr></table></figure>
<h4 id="稳定版本">稳定版本</h4>
<p><span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL2N0ZXgvMy4wLw==">CTeX
3.0.216.3<i class="fa fa-external-link-alt"></i></span></p>
<p><img data-src="https://invinc-z-drawing-bed.oss-cn-shanghai.aliyuncs.com/img/image-20240805160610675.png" alt="image-20240805160610675" style="zoom: 33%;" /></p>
<p>下载图中三个文件，其中<span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL2N0ZXgvMy4wL0NUZVhfMy4wLjIxNS4yX0Z1bGwuZXhl">CTeX_3.0.215.2_Full.exe<i class="fa fa-external-link-alt"></i></span>为32位/64位通用版本，<span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL2N0ZXgvMy4wL0NUZVhfMy4wLjIxNS4yX0Z1bGwubnNpc2Jpbg==">CTeX_3.0.215.2_Full.nsisbin<i class="fa fa-external-link-alt"></i></span>是数据文件，<span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL2N0ZXgvMy4wL0NUZVhfMy4wLjIxNi4zX1VwZGF0ZS5leGU=">CTeX_3.0.216.3_Update.exe<i class="fa fa-external-link-alt"></i></span>是更新文件。</p>
<p>下载后将这三个文件放在<strong>同一文件夹</strong>下，以管理员方式启动exe文件开始安装，安装路径选择<strong>不带中文路径与空格</strong>，安装时间取决于具体机型。安装完成后提示是否在线更新miktex，可以选择否，后面可根据需要更新宏包。</p>
<h4 id="旧版本2.9.2">旧版本2.9.2</h4>
<p><span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL2N0ZXgvbGVnYWN5LzIuOS9DVGVYXzIuOS4yLjE2N19GdWxsLmV4ZQ==">CTeX_2.9.2.167_Full.exe<i class="fa fa-external-link-alt"></i></span></p>
<p><strong>安装步骤</strong>：傻瓜式安装，中间可以切换安装路径，不带中文路径，C盘容量较小可以切换其他盘符。</p>
<h4 id="winedt11">WinEdt11</h4>
<p>CTEX2.9.2版本安装后默认的编辑器为WinEdt7.0，CTEX3.0以上版本默认编辑器为WinEdt11.1。目前<span class="exturl" data-url="aHR0cHM6Ly93d3cud2luZWR0LmNvbS9kb3dubG9hZC5odG1s">WinEdt官网<i class="fa fa-external-link-alt"></i></span>最新版本为11.2，可进行覆盖安装，具体方法是安装时将路径设置为CTEX安装路径下面的WinEdt文件夹，例如：<code>D:\install\CTEX\WinEdt</code>。</p>
<p><strong>注意：
一定不要卸载CTEX自带的WinEdt</strong>。完成覆盖安装后，打开WinEdt
11，会出现Warning警告提示本地配置是之前的版本，要你按照提示进行更新（Options-&gt;Maintenance-&gt;Upgrade
Local…，按照提示的步骤进行即可）进行更新升级，并重新存储更新后的本地配置即可。</p>
<p>可用注册码：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">name: *60[20220501] Warez_Down [RU-BOARD] (100 users)</span><br><span class="line">code:4049089118892183088</span><br></pre></td></tr></table></figure>
<p>无法双向跳转（No SyncTex
Data”“）的问题可能是由于tex文件名带了空格，删除空格后可以跳转。</p>
<hr />
<h2 id="ubuntu24.04">Ubuntu24.04</h2>
<h3 id="tex-live安装">Tex Live安装</h3>
<h4 id="下载镜像文件并装载">下载镜像文件并装载</h4>
<p>由于后续内容涉及字体和文本编辑, 建议用户先确定系统内是否已经有
<code>fontconfig</code> 和 <code>gedit</code>，需在Terminal中执行</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt install fontconfig gedit</span><br></pre></td></tr></table></figure>
<p>下载<span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL0NUQU4vc3lzdGVtcy90ZXhsaXZlL0ltYWdlcy8=">镜像文件<i class="fa fa-external-link-alt"></i></span>，该文件夹下包含
3 个 <code>.iso</code> 文件，这 3
个文件仅有文件名不同，内容完全相同，可以任选一个下载。将下载的镜像进行装载</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo mkdir /mnt/texlive</span><br><span class="line">sudo mount ./texlive2025.iso /mnt/texlive</span><br></pre></td></tr></table></figure>
<h4 id="开始安装">开始安装</h4>
<p>接下来执行</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo /mnt/texlive/install-tl</span><br></pre></td></tr></table></figure>
<p>进行安装。在屏幕上应该能见到以下内容</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">======================&gt; TeX Live installation procedure &lt;=====================</span><br><span class="line"> </span><br><span class="line">======&gt;   Letters/digits in &lt;angle brackets&gt; indicate   &lt;=======</span><br><span class="line">======&gt;   menu items for actions or customizations      &lt;=======</span><br><span class="line">= help&gt;   https://tug.org/texlive/doc/install-tl.html   &lt;=======</span><br><span class="line"> </span><br><span class="line"> Detected platform: GNU/Linux on x86_64</span><br><span class="line"> </span><br><span class="line"> &lt;B&gt; set binary platforms: 1 out of 15</span><br><span class="line"> </span><br><span class="line"> &lt;S&gt; set installation scheme: scheme-full</span><br><span class="line"> </span><br><span class="line"> &lt;C&gt; set installation collections:</span><br><span class="line">     40 collections out of 41, disk space required: 8779 MB (free: 970330 MB)</span><br><span class="line"> </span><br><span class="line"> &lt;D&gt; set directories:</span><br><span class="line">   TEXDIR (the main TeX directory):</span><br><span class="line">     /usr/local/texlive/2025</span><br><span class="line">   TEXMFLOCAL (directory for site-wide local files):</span><br><span class="line">     /usr/local/texlive/texmf-local</span><br><span class="line">   TEXMFSYSVAR (directory for variable and automatically generated data):</span><br><span class="line">     /usr/local/texlive/2025/texmf-var</span><br><span class="line">   TEXMFSYSCONFIG (directory for local config):</span><br><span class="line">     /usr/local/texlive/2025/texmf-config</span><br><span class="line">   TEXMFVAR (personal directory for variable and automatically generated data):</span><br><span class="line">     ~/.texlive2025/texmf-var</span><br><span class="line">   TEXMFCONFIG (personal directory for local config):</span><br><span class="line">     ~/.texlive2025/texmf-config</span><br><span class="line">   TEXMFHOME (directory for user-specific files):</span><br><span class="line">     ~/texmf</span><br><span class="line"> </span><br><span class="line"> &lt;O&gt; options:</span><br><span class="line">   [ ] use letter size instead of A4 by default</span><br><span class="line">   [X] allow execution of restricted list of programs via \write18</span><br><span class="line">   [X] create all format files</span><br><span class="line">   [X] install macro/font doc tree</span><br><span class="line">   [X] install macro/font source tree</span><br><span class="line">   [ ] create symlinks to standard directories</span><br><span class="line">   [X] after install, set CTAN as source for package updates</span><br><span class="line"> </span><br><span class="line"> &lt;V&gt; set up for portable installation</span><br><span class="line"> </span><br><span class="line">Actions:</span><br><span class="line"> &lt;I&gt; start installation to hard disk</span><br><span class="line"> &lt;P&gt; save installation profile to &#x27;texlive.profile&#x27; and exit</span><br><span class="line"> &lt;Q&gt; quit</span><br><span class="line"> </span><br><span class="line">Enter command:</span><br></pre></td></tr></table></figure>
<p>点击键盘<code>I</code>进行默认安装，默认安装是为所有用户安装。（硬盘容量紧张可点击键盘<code>C</code>并回车进行精简安装，输入集合对应的字母可以选中/取消选中相应宏包功能，支持批量输入，如<code>deghijkstuvwxyznoABCEHIKLMNS</code>，后续可通过
<code>tlmgr</code>
工具来单独安装它们，因此不用太担心）。默认安装安装完毕后,
系统会提示用户添加环境变量，例如</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">export PATH=/usr/local/texlive/2025/bin/x86_64-linux:$PATH</span><br><span class="line">export MANPATH=/usr/local/texlive/2025/texmf-dist/doc/man:$MANPATH</span><br><span class="line">export INFOPATH=/usr/local/texlive/2025/texmf-dist/doc/info:$INFOPATH</span><br></pre></td></tr></table></figure>
<p>此处显示信息可能会略有不同， 比如电脑使用了基于 arm64
架构的处理器，则 <code>x86_64-linux</code>
将被替换为<code>aarch64-linux</code>。 为安全起见,
用户需将此处的系统提示记录。</p>
<h4
id="弹出装载的光盘镜像并删除文件夹">弹出装载的光盘镜像并删除文件夹</h4>
<p>将装载的光盘镜像弹出并删除文件夹, 即执行</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo umount /mnt/texlive</span><br><span class="line">sudo rm -r /mnt/texlive</span><br></pre></td></tr></table></figure>
<h4 id="设置环境变量">设置环境变量</h4>
<p>对于<code>bash</code>，可以修改 <code>~/.bashrc</code>
文件，执行<code>vim ~/.bashrc</code>或其他任意文本编辑器（<code>gedit</code>、<code>vi</code>、<code>nano</code>等）打开<code>~/.bashrc</code>文件，</p>
<p>在打开的文件末尾添加刚刚记录的系统提示, 如</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_">#</span><span class="language-bash">Add TeX Live to the PATH, MANPATH, INFOPATH</span></span><br><span class="line">export PATH=/usr/local/texlive/2025/bin/x86_64-linux:$PATH</span><br><span class="line">export MANPATH=/usr/local/texlive/2025/texmf-dist/doc/man:$MANPATH</span><br><span class="line">export INFOPATH=/usr/local/texlive/2025/texmf-dist/doc/info:$INFOPATH</span><br></pre></td></tr></table></figure>
<p>并保存退出，重启 <code>bash</code> 或者执行命令
<code>source ~/.bashrc</code> 来重载 <code>bash</code> 配置。</p>
<h4 id="验证安装是否成功">验证安装是否成功</h4>
<p>然后退出当前用户并重新登录, 再打开 Terminal 并执行</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">tex -v</span><br></pre></td></tr></table></figure>
<p>若显示</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">TeX 3.141592653 (TeX Live 2025)</span><br><span class="line">kpathsea version 6.4.1</span><br><span class="line">Copyright 2025 D.E. Knuth.</span><br><span class="line">There is NO warranty. Redistribution of this software is</span><br><span class="line">covered by the terms of both the TeX copyright and</span><br><span class="line">the Lesser GNU General Public License.</span><br><span class="line">For more information about these matters, see the file</span><br><span class="line">named COPYING and the TeX source.</span><br><span class="line">Primary author of TeX: D.E. Knuth.</span><br></pre></td></tr></table></figure>
<p>即为安装成功。</p>
<h4 id="刷新字体缓存">刷新字体缓存</h4>
<p>接下来处理字体。在 Terminal 中执行</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo cp /usr/local/texlive/2025/texmf-var/fonts/conf/texlive-fontconfig.conf /etc/fonts/conf.d/09-texlive.conf</span><br></pre></td></tr></table></figure>
<p>将配置文件复制到系统，然后继续执行</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo fc-cache -fsv</span><br></pre></td></tr></table></figure>
<p>刷新字体缓存。 这样一来, TEX Live
中的字体才能够被正确调用。如果用户更改了安装路径, 要注意将
<code>/usr/local/texlive/2025/</code>在各处替换。</p>
<h3 id="升级与安装宏包">升级与安装宏包</h3>
<p>升级所有宏包包括<code>tlmgr</code>自身：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo $(which tlmgr) update --self --all</span><br></pre></td></tr></table></figure>
<p>安装指定宏包：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo $(which tlmgr) install mcmthesis</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="mac系统">Mac系统</h2>
<h3 id="安装-mactex">安装 MacTEX</h3>
<p>MacTEX 是 TEX Live 在 macOS 下的再打包版本，
并额外加入了一些辅助程序。在 macOS 上安装 MacTEX
可以选择以下两种方式:</p>
<ul>
<li>直接下载 MacTEX 安装包进行安装 将安装文件下载到本地，
然后按照安装提示进行安装</li>
<li>借助 <code>Homebrew</code> 以命令行方式安装 先安装
<code>Homebrew</code>， 然后通过命令行进行在线安装</li>
</ul>
<p>推荐直接下载 MacTEX 安装包的安装方式。</p>
<h4 id="直接下载-mactex-安装包进行安装">直接下载 MacTEX
安装包进行安装</h4>
<p>下载 MacTEX 的安装包<span class="exturl" data-url="aHR0cHM6Ly9taXJyb3IuY3Rhbi5vcmcvc3lzdGVtcy9tYWMvbWFjdGV4L01hY1RlWC5wa2c=">MacTeX.pkg<i class="fa fa-external-link-alt"></i></span>，
在系统内点击已下载的 MacTeX.pkg 文件,
根据系统提示进行安装即可。这个过程与安装 macOS
上的其他软件并无太多不同。另外， 用户也可以选择国内各个镜像来下载
MacTeX.pkg。例如，阿里云的镜像地址为
<code>https://mirrors.aliyun.com/CTAN/systems/mac/mactex/</code>,
北京交通大学的镜像地址为
<code>https://mirror.bjtu.edu.cn/ctan/systems/mac/mactex/</code>。</p>
<hr />
<h2 id="参考文章">参考文章</h2>
<ol type="1">
<li><span class="exturl" data-url="aHR0cHM6Ly90dWcuY3Rhbi5vcmcvaW5mby9pbnN0YWxsLWxhdGV4LWd1aWRlLXpoLWNuL2luc3RhbGwtbGF0ZXgtZ3VpZGUtemgtY24ucGRm">一份简短的关于LATEX安装的介绍<i class="fa fa-external-link-alt"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly90dWcub3JnL3RleGxpdmUvZG9jL3RleGxpdmUtemgtY24vdGV4bGl2ZS16aC1jbi5wZGY=">Tex
live指南-2025<i class="fa fa-external-link-alt"></i></span></li>
<li><span class="exturl" data-url="aHR0cHM6Ly93d3cuY25ibG9ncy5jb20vZXNsenp5bC9wLzE3MzU4NDA1Lmh0bWw=">TeX Live
2025 安装教程（Windows/WSL/Linux）<i class="fa fa-external-link-alt"></i></span></li>
</ol>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX环境配置</category>
      </categories>
      <tags>
        <tag>宏包</tag>
        <tag>发行版</tag>
        <tag>安装</tag>
      </tags>
  </entry>
  <entry>
    <title>VS Code中 LaTeX 与 SumatraPDF 正反向跳转配置</title>
    <url>/2024/11/13/tools/latex/environment-configuration/latex-vscode-sumatrapdf/</url>
    <content><![CDATA[<p>本文介绍如何在 VSCode 中实现 LaTeX 与 SumatraPDF 正反向跳转功能。</p>
<span id="more"></span>
<hr />
<h2 id="vs-code环境配置">VS Code环境配置</h2>
<p>配置环境，在VS Code界面快捷键 <code>Ctrl Shift + P</code>，输入
<code>Open User Settings</code>，点击
<strong>首选项：打开用户设置(JSON)</strong>, 打开
<code>settings.json</code> 文件。在文件中的 <code>{}</code>
中添加以下代码：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="attr">&quot;latex-workshop.latex.tools&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk-pdflatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;-pdflatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-synctex=1&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-interaction=nonstopmode&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-file-line-error&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;%DOC%&quot;</span></span><br><span class="line">        <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk-xelatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;-xelatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-synctex=1&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-interaction=nonstopmode&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-file-line-error&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;%DOC%&quot;</span></span><br><span class="line">        <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk-lualatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;-lualatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-synctex=1&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-interaction=nonstopmode&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-file-line-error&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;%DOC%&quot;</span></span><br><span class="line">        <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line"><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">// 用于配置编译链</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.recipes&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk(pdflatex)&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;tools&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;latexmk-pdflatex&quot;</span></span><br><span class="line">        <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk(xelatex)&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;tools&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;latexmk-xelatex&quot;</span></span><br><span class="line">        <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk(lualatex)&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;tools&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;latexmk-lualatex&quot;</span></span><br><span class="line">        <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">]</span><span class="punctuation">,</span>  </span><br><span class="line"><span class="comment">//设置是否自动编译tex文件，设置为保存时自动编译</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.autoBuild.run&quot;</span><span class="punctuation">:</span> <span class="string">&quot;onSave&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//显示右键菜单的latex相关选项</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.showContextMenu&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//显示错误</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.message.error.show&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//显示警告</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.message.warning.show&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//从使用的包中自动补全命令和环境</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.intellisense.package.enabled&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//设置为never，为不自动清除辅助文件</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.autoClean.run&quot;</span><span class="punctuation">:</span> <span class="string">&quot;never&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//设置清除辅助文件的方法为command，即使用latexmk的-clean选项</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.clean.method&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//设置清除辅助文件时，是否清除子文件夹中的辅助文件</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.clean.subfolder.enabled&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//设置vscode编译tex文档时的默认编译链</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.recipe.default&quot;</span><span class="punctuation">:</span> <span class="string">&quot;lastUsed&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//tex文件浏览器，可选项为&quot;none&quot; &quot;browser&quot; &quot;tab&quot; &quot;external&quot;</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.view.pdf.viewer&quot;</span><span class="punctuation">:</span> <span class="string">&quot;external&quot;</span><span class="punctuation">,</span>   <span class="comment">// tab 为内部查看器</span></span><br><span class="line"><span class="comment">// 用于反向同步的内部查看器的键绑定。ctrl/cmd +点击(默认)或双击</span></span><br><span class="line"><span class="comment">//&quot;latex-workshop.view.pdf.internal.synctex.keybinding&quot;: &quot;double-click&quot;,</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.view.pdf.external.synctex.command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;F:/install/SumatraPDF/SumatraPDF.exe&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.view.pdf.external.synctex.args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="string">&quot;-forward-search&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;%TEX%&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;%LINE%&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;-reuse-instance&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;-inverse-search&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;\&quot;F:/install/Microsoft VS Code/Code.exe\&quot; \&quot;F:/install/Microsoft VS Code/e7fb5e96c0/resources/app/out/cli.js\&quot; --ms-enable-electron-run-as-node -r -g \&quot;%f:%l\&quot;&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;%PDF%&quot;</span></span><br><span class="line"><span class="punctuation">]</span><span class="punctuation">,</span></span><br></pre></td></tr></table></figure>
<p>其中 SumatraPDF 与 VS Code 路径修改为自己的安装路径，
<code>e7fb5e96c0</code> 目录名称每个人不一样，需要修改。</p>
<hr />
<h2 id="sumatrapdf设置">SumatraPDF设置</h2>
<p>SumatraPDF的设置-高级选项中，修改变量内容：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">InverseSearchCmdLine = &quot;F:/install/Microsoft VS Code/Code.exe&quot; -r -g &quot;%f:%l&quot;</span><br><span class="line">EnableTeXEnhancements = true</span><br></pre></td></tr></table></figure>
<p><strong>v3.5.2版本支持，v3.6此方法暂不支持。</strong></p>
<hr />
<h2 id="编译与跳转说明">编译与跳转说明</h2>
<h3 id="编译">编译</h3>
<p>英文一般选择<code>latexmk(pdflatex)</code>编译，中文一般选择<code>latexmk(xelatex)</code>编译，初始默认使用第一个配方，后续默认使用上次使用的编译方式，保存文件触发自动编译。</p>
<p>清理辅助文件会清除中间生成的辅助文件，不会清除<code>*.pdf</code>、<code>*.tex</code>、<code>*.bib</code>等有用的资源文件。</p>
<p>重启VS Code配置生效。</p>
<h3 id="跳转">跳转</h3>
<p>tex –&gt; SumatraPDF: <kbd>Ctrl</kbd> + <kbd>Alt</kbd> +
<kbd>J</kbd></p>
<p>SumatraPDF –&gt; tex: 双击</p>
<hr />
<h2 id="参考博文">参考博文</h2>
<ul>
<li><span class="exturl" data-url="aHR0cHM6Ly96aHVhbmxhbi56aGlodS5jb20vcC8xOTc2NzE0OTMwNzQ2MDU3ODQz">VS Code +
SumatraPDF 反向搜索问题<i class="fa fa-external-link-alt"></i></span></li>
</ul>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX环境配置</category>
      </categories>
      <tags>
        <tag>VS Code</tag>
      </tags>
  </entry>
  <entry>
    <title>在VS Code中简洁高效配置LaTeX编译链</title>
    <url>/2024/12/10/tools/latex/environment-configuration/latex-vsode-config/</url>
    <content><![CDATA[<p>本文给出一种在VS
Code中简洁高效配置LaTeX编译链的方案，旨在提高编译和写作的效率。</p>
<span id="more"></span>
<hr />
<h2 id="vs-code环境配置">VS Code环境配置</h2>
<ol type="1">
<li><p>打开VS Code，点击左侧扩展图标，搜索
<code>LaTeX Workshop</code>，点击第一个进行下载安装。</p></li>
<li><p>配置环境，在VS Code界面快捷键 <code>Ctrl Shift + P</code>，输入
<code>Open User Settings</code>，点击
<strong>首选项：打开用户设置(JSON)</strong>, 打开
<code>settings.json</code> 文件。在文件中的 <code>{}</code>
中添加以下代码：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 编译工具和命令</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.tools&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk-pdflatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;-pdflatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-synctex=1&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-interaction=nonstopmode&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-file-line-error&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;%DOC%&quot;</span></span><br><span class="line">        <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk-xelatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;-xelatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-synctex=1&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-interaction=nonstopmode&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-file-line-error&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;%DOC%&quot;</span></span><br><span class="line">        <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk-lualatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;-lualatex&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-synctex=1&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-interaction=nonstopmode&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;-file-line-error&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="string">&quot;%DOC%&quot;</span></span><br><span class="line">        <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line"><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">// 用于配置编译链</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.recipes&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk(pdflatex)&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;tools&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;latexmk-pdflatex&quot;</span></span><br><span class="line">        <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk(xelatex)&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;tools&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;latexmk-xelatex&quot;</span></span><br><span class="line">        <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;latexmk(lualatex)&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;tools&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="string">&quot;latexmk-lualatex&quot;</span></span><br><span class="line">        <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//设置是否自动编译tex文件，设置为保存时自动编译</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.autoBuild.run&quot;</span><span class="punctuation">:</span> <span class="string">&quot;onSave&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//显示右键菜单的latex相关选项</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.showContextMenu&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//显示错误</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.message.error.show&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//显示警告</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.message.warning.show&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//从使用的包中自动补全命令和环境</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.intellisense.package.enabled&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//设置为never，为不自动清除辅助文件</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.autoClean.run&quot;</span><span class="punctuation">:</span> <span class="string">&quot;never&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//设置清除辅助文件的方法为command，即使用latexmk的-clean选项</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.clean.method&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//设置清除辅助文件时，是否清除子文件夹中的辅助文件</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.clean.subfolder.enabled&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//设置vscode编译tex文档时的默认编译链</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.latex.recipe.default&quot;</span><span class="punctuation">:</span> <span class="string">&quot;lastUsed&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">//tex文件浏览器，可选项为&quot;none&quot; &quot;browser&quot; &quot;tab&quot; &quot;external&quot;</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.view.pdf.viewer&quot;</span><span class="punctuation">:</span> <span class="string">&quot;tab&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="comment">// 用于反向同步的内部查看器的键绑定。ctrl/cmd +点击(默认)或双击</span></span><br><span class="line"><span class="attr">&quot;latex-workshop.view.pdf.internal.synctex.keybinding&quot;</span><span class="punctuation">:</span> <span class="string">&quot;double-click&quot;</span><span class="punctuation">,</span></span><br></pre></td></tr></table></figure>
<p>英文一般选择<code>latexmk(pdflatex)</code>编译，中文一般选择<code>latexmk(xelatex)</code>编译，初始默认使用第一个配方，后续默认使用上次使用的编译方式，保存文件触发自动编译。</p>
<p>清理辅助文件会清除中间生成的辅助文件，不会清除<code>*.pdf</code>、<code>*.tex</code>、<code>*.bib</code>等有用的资源文件。</p>
<p>重启VS Code配置生效。</p></li>
</ol>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX环境配置</category>
      </categories>
      <tags>
        <tag>编译</tag>
        <tag>VS Code</tag>
      </tags>
  </entry>
  <entry>
    <title>国内外期刊和学位论文等LaTeX模板编译报错如何解决</title>
    <url>/2025/07/21/tools/latex/environment-configuration/solutions-latex-templetes-compile-errors/</url>
    <content><![CDATA[<p>本文主要介绍国内外学术期刊和学位论文等LaTeX模板在本地编译报错的解决方案。</p>
<span id="more"></span>
<hr />
<h2 id="引言">引言</h2>
<p>我们进行学术投稿或者写毕业论文时，可能会被要求使用latex格式编写。当我们下载好期刊或学校提供好的latex模板后，在本地编译后发现竟然有问题，然后可能会怀疑自己的环境出了问题，于是又去找之前编译没问题的文档测试，发现环境并没有问题。这是怎么回事呢？</p>
<p>很多时候可能是因为他们提供的这些模板与你本地的latex发行版版本或宏包版本不匹配导致的，要么是他们提供的模板太旧了，需要使用以前的发行版来运行模板，要么是你本地的环境版本比较老，而模板比较新。也有可能是模板太老了，本身在现在的计算机架构上就存在一些问题，模板本身需要做一些改动。</p>
<p>本文首先给出遇到上述问题的一般性解决方案，然后列举出一些目前遇到的需要特定latex环境和设置的一些国内外期刊和毕业论文模板。如果您遇到过需要特定环境才能编译的模板，请在评论区提供相关的模板名称和所需环境，或者与其相关的材料等，以完善这篇文章，来帮助更多遇到这类问题的朋友。</p>
<hr />
<h2 id="一般性解决方案">一般性解决方案</h2>
<p>本节主要介绍几种通用的判断latex模板需要什么特定环境来运行的方法。</p>
<h3 id="官网下载区模板的上传日期">官网下载区模板的上传日期</h3>
<p>首先，当确定要选择哪家杂志期刊投稿后（其他类型模板类似），先去他们官网的下载区下载相应的latex模板，有时候这个资源会给出一个对应的上传时间，从这个上传时间大概可以判断需要使用什么版本的环境。例如上传时间是零几年的（一般都是老旧的中文期刊模板），那大概率需要使用CTEX
2.9版本的环境。</p>
<h3 id="模板本身信息">模板本身信息</h3>
<p>当下载模板解压后，可能会有<code>*.tex</code>、<code>*.cls</code>、<code>README.md</code>几种文件，一般可以先看看<code>README.md</code>里有没有说明编译此模板所需的特定的环境，也可以在<code>*.tex</code>、<code>*.cls</code>的开头注释部分寻找相关的信息。看看有没有给出此模板的github仓库等，如果有可以去仓库的主页看看有没有给出编译此模板的相关信息。此外仓库的更新日期也可以作为一个参考，比如此仓库最后的更新是2019年的，在你的本地的Tex
Live 2025环境下编译不了，那么不妨再重新装一个Texlive 2019版的。</p>
<blockquote>
<p>本地可以同时装不同版本的Tex
live环境，切换环境时可以去系统环境变量path处把想要使用版本的路径上移，比如想用2019版的编译，那么把2019的路径移到2025版的上面就可以。当然也可以去Texstudio里面做一些配置，那样就不用去移动path的路径了，配置可参考<span class="exturl" data-url="aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FpbmduaXlhb2ppemh1d28vYXJ0aWNsZS9kZXRhaWxzLzEzOTYyOTk4MQ==">科研干货
| 安装配置TeXLive和CTeX并实现自由切换<i class="fa fa-external-link-alt"></i></span>。</p>
</blockquote>
<h3 id="网络搜索">网络搜索</h3>
<p>也可以直接在浏览器中搜索，这是最简单最直接的方式。搜索的关键词可以是“xxx
latex模板 编译报错 解决
编译环境”等。如果模板本身有一些问题，有可能会找到一些解决方案，或者别人已经修正过上传到Overleaf或Github，直接下载使用就行。</p>
<h3 id="与原作者沟通">与原作者沟通</h3>
<p>如果网上也找不到相关的编译此模板的解决方案，那么大胆真诚的去沟通吧，原作者可能也不知道模板在现在的环境下编译有问题，你提出了这个issue帮助原作者修复了这个bug，就能帮助完善这个latex模板项目，造福更多的后来者。原作者的联系方式一般可以在模板的<code>*.tex</code>、<code>*.cls</code>、<code>README.md</code>开头注释部分获取。对于投稿的话也可以与编辑部联系问问他们用的是什么环境。</p>
<hr />
<h2 id="中文模板">中文模板</h2>
<h3 id="投稿期刊">投稿期刊</h3>
<h4 id="系统工程学报">系统工程学报</h4>
<p><strong>编译环境：</strong><span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL2N0ZXgvbGVnYWN5LzIuOS8=">CTEX
2.9.2.167<i class="fa fa-external-link-alt"></i></span></p>
<p><strong>编译方式：</strong><code>pdflatex</code></p>
<p><strong>编译报错：</strong></p>
<ul>
<li><code>\bibname undefined</code>，具体报错为：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">! LaTeX Error: \bibname undefined.</span><br><span class="line"></span><br><span class="line">See the LaTeX manual or LaTeX Companion for explanation.</span><br><span class="line">Type  H &lt;return&gt;  for immediate help.</span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">l.43   &#123;\renewcommand\bibname&#123;?196?215&#125;&#125;</span><br><span class="line"></span><br><span class="line">?</span><br></pre></td></tr></table></figure>
<p><strong>解决方案</strong>：下载<code>gb.cap</code>和<code>gb.cpx</code>两个文件（<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0RlYXRoS2luZy9MYVRlWC1UZW1wbGF0ZS1Dbi90cmVlL21hc3Rlci9CVUdGSVgvZ2I=">下载链接<i class="fa fa-external-link-alt"></i></span>），拷贝至<code>CTEX\MiKTeX\tex\latex\cjk\GB</code>文件夹中，替换原有文件即可。</p>
<ul>
<li><code>Unknown graphics extension: .eps. \includegraphics[width=2.2in]{NN1.eps}</code></li>
</ul>
<p><strong>解决方案</strong>：加载 <code>epstopdf</code> 宏包（需要在
<code>graphicx</code>
宏包加载后加载）。<code>\usepackage{amsmath,amsfonts,amssymb,graphicx,epstopdf}</code></p>
<p><strong>部分警告：</strong></p>
<ul>
<li><p><code>You have requested document class 'ARTICLE', but the document class provides 'article'.</code></p></li>
<li><p><code>Unused global option(s): [10.5pt].</code></p></li>
</ul>
<p><strong>解决方案</strong>：将<code>\documentclass[10.5pt,onecolumn,twoside]{ARTICLE}</code>改成<code>\documentclass[10pt,onecolumn,twoside]{article}</code>。</p>
<h4 id="自动化学报">自动化学报</h4>
<p><span class="exturl" data-url="aHR0cHM6Ly93d3cuYWFzLm5ldC5jbi8=">官网首页<i class="fa fa-external-link-alt"></i></span></p>
<p><strong>编译环境：</strong><span class="exturl" data-url="aHR0cHM6Ly9taXJyb3JzLnR1bmEudHNpbmdodWEuZWR1LmNuL2N0ZXgvbGVnYWN5LzIuOS8=">CTEX
2.9.2.167<i class="fa fa-external-link-alt"></i></span></p>
<hr />
<h2 id="外文模板">外文模板</h2>
<h3 id="投稿期刊-1">投稿期刊</h3>
<h4 id="elsevier期刊">Elsevier期刊</h4>
<p><span class="exturl" data-url="aHR0cHM6Ly96aHVhbmxhbi56aGlodS5jb20vcC8xOTAwNjM1NDgwMzkyOTU5MjU3">常见问题以及解决方案<i class="fa fa-external-link-alt"></i></span></p>
<hr />
<h2 id="参考文章">参考文章</h2>
<ol type="1">
<li><span class="exturl" data-url="aHR0cHM6Ly9zaWtvdWhqdy5naXRodWIuaW8vMjAyMC8wNy8xMy8yMDIwLTA3LTEzLUpvdXJuYWwtb2YtU3lzdGVtcy1FbmdpbmVlcmluZy8=">如何编译通过《系统工程学报》LaTeX
模板<i class="fa fa-external-link-alt"></i></span></li>
</ol>
]]></content>
      <categories>
        <category>文档与排版（Documentation）</category>
        <category>LaTeX</category>
        <category>LaTeX环境配置</category>
      </categories>
      <tags>
        <tag>编译</tag>
        <tag>报错</tag>
      </tags>
  </entry>
</search>
